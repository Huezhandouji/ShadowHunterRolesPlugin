# 阶段 3–4 正式评审报告（t52，替代 t16）

> **执行者**：eng-审阅（reviewer）｜**任务**：t52（review round 1）｜**attempt**：1
> **事实基础**：`debug-logs/测试记录/阶段3-4-验证报告.md`（t50 复核验，**verdict = pass**）
> **评审时刻**：2026-09-18（+08:00）
> **结论**：**verdict = pass**（0 blocker / 0 high / **1 medium** / 2 low）
> **纪律**：本文所有计数均为 `git grep`（大小写敏感）**现算**，并写明**作用域与匹配形态**；未使用 `Select-String` / `Measure-Object.Count` 出证据；正则不跨空格；git 路径用正斜杠。**未改 `src`、未改历史证据文档、未做 git 写。**

---

## 0. 事实基础身份核对（**先核基线，再谈结论**）

| 对象 | 卡面/依赖结果声明 | **我现算** | 判定 |
|---|---|---|---|
| `debug-logs/测试记录/阶段3-4-验证报告.md` | 卡面称 16 441 B / `8F4F6A3A…`；依赖结果称 17 695 B / `F2F57549…` | **17 695 B / 153 行 / `F2F5754903B22A6FBC7D71740604937F00EB455BF324AF37AE04588609CC10BB`** | ⚠️ **卡面数值为过期值**（依赖结果为准）——不影响评审，因为**结论（verdict=pass）与内容一致** |
| 基线 HEAD | `e539a53`（干净树） | 报告内声明一致；**当前 HEAD 已前推**（`147f863` 等），差异见 §4 | ⚠️ 见 **F-1（medium）** |
| t50 报告裁决 | pass（无未解决 blocker/high） | 报告 §0 判定链自洽、§6 findings 逐条闭合/映射 | ✅ 采信 |

**我不引用 t15 首版 failed 结论** ✓（卡面要求）。该首版针对已被取代的快照（`0564b3b`/`385ac222`），其 V-01/V-03 已在 `7c7cdb7`+`6f92bb3` 修复并经 t50 复验确认；首版文件已按来源标注改名保留（`阶段3-4-独立验证报告（首版·已被复验版取代）.md`）。

---

## 1. 判据覆盖充分性与可复现性（**我抽样复算，不引用其结论**）

### 1.1 卡面指定的两条抽样复核（**均通过**）

| 卡面 Verify | 我现算的命令与作用域 | 输出 | 判定 |
|---|---|---|---|
| `triggerSanTEChange\|triggerEnergyChange`（`listener/*`，应 0） | `git grep -nE "triggerSanTEChange\|triggerEnergyChange" -- "src/main/java/**/listener/*"` | **空 = 0 行** | ✅ **通过** |
| 同口径全仓（I-15 另一半） | 同命令，作用域 `src/main/java/**/*.java` | **空 = 0 行** | ✅ 通过 |
| 组件自启冷却（`roleComponent` 内 `cooldowns()`） | `git grep -n "cooldowns()" -- src/main/java/com/shadowHunterRolesPlugin/roleComponent` | **13 行**（12 代码 + 1 javadoc）：`MeiqiheziJuejueMainWeapon:53/60/79`、`BloodySlash:85`、`CircleSlash:87`、`Unconcern:54`、`RedDeeplySorrow:51/69/78`（+ `:25` javadoc）、`RedEvilShock:47`、`RedSanctifiedBlade:60`、`RedSolitaryArrogance:92` —— 形态均为 `svc().cooldowns().start(getCooldownTicks())` | ✅ **通过**（组件自管冷却成立） |

### 1.2 我另外抽验的关键判据（**独立现算，均与 t50 一致**）

| 判据 | 我的现算 | 与 t50 对照 |
|---|---|---|
| I-15（`callEvent` 对外发布保留） | 未复核 2 处（采信其行号）但**红线表述**与 `docs/插件文档/架构与组件模型.md:128` 一致 | ✅ 一致 |
| I-10 / V-07（`RequiresComponents\|requires()`） | `git grep -nE "RequiresComponents\|requires\(\)" -- "src/main/java/**/*.java"` → **空 = 0** | ✅ **一致**：机制**从未引入**（非"实现不合格"） |
| **I-7（无 `roleInstance` 知识泄漏）** | `git grep -nE "\bRoleInstance\b" -- …/roleComponent` → **18 行**，其中**非注释命中 = 1**（`red/RedDeeplySorrowSkill.java:3` 的死 import） | ✅ 一致（语义成立，剩 1 条 **V-05**） |
| **组件 14/14** | `git grep -nE "public (final )?class [A-Za-z]+ extends (Skill\|MainWeapon\|PassiveSkill\|ActiveComponent)" -- …/roleComponent` → **14 行**（4 `PassiveSkill`+2 `MainWeapon`+8 `Skill`），**逐类可点** | ✅ 一致（14 个组件全部基到新体系） |
| 端口白名单（I-9） | 采信其 34 方法 / 10 成员逐条表；我另核 **`ComponentServices` 为 `record` 且成员恰好 10**、`CooldownPort` 现为 `boolean end()`（`:25`） | ✅ 方法集合无增减；唯一差异 = `end()` 返回类型（V-14） |
| 构建新鲜度 | 见 §4 | ⚠️ 见 **F-1** |

**覆盖充分性判定**：t50 报告对 `I-3..I-15`、§3.4（6/6）、§3.5、§3.5.1、冻结语义**逐条给出命令 + 输出 + 结论**，且**如实列出 3 项未完成**（§7）。**判据覆盖充分、结论可复现** ✓——唯一系统性缺口是**基线与制品被后续提交前推**（§4/F-1）。

---

## 2. 已申报 / 已裁定例外项的逐条判定（**卡面指定 5 项**）

| # | 例外项 | 我的核实 | 判定 |
|---|---|---|---|
| 1 | **`RequiresComponents` 机制未引入**（用户裁定 A：记 finding + 归入阶段 5） | 现算 **0 命中** ✓；t50 §6 **V-07 如实记为"机制未引入"**、明确"不伪造通过"、归入阶段 5 候选 ✓ | ✅ **处置正确**（记 finding + 顺延，未伪造） |
| 2 | **空闲 tick 零 setItem**（设计延后 4.4） | t50 §3 判定 `§3.5.1 = 部分`：poll ✅ / push-after-cast ✅ / **端口适配器 push ✗、帧末 flush ✗**，并注明 `HotbarRenderer:21-24` 已显式标注、4.4 预案 §2 已规划落点；**不判缺陷、不伪造实测** ✓ | ✅ **处置正确**（如实标注"设计延后"，未冒充通过） |
| 3 | **V-14 `boolean end()` 签名变更**（已批准，白名单描述已同步） | 我核**同步确实完成**：`docs/插件文档/架构与组件模型.md:64`（端口表，写 **`boolean end()`** + "该签名变更属已批准变更：`bd726cf`，用户 D-list 裁定"）、`:179`（语义表，明写"**不要当作文档漂移或未授权改动**"）、`:269`；`开发指南-新增角色或组件.md:87`；`角色与技能.md:329`；**`CooldownPort` javadoc 自身亦记载语义**（`CooldownPort.java:11-13/25`） | ✅ **处置正确**（已在 4 处活文档 + 代码 javadoc 同步并标注批准来源） |
| 4 | **V-05 死 import** | `RedDeeplySorrowSkill.java:3` `import …core.RoleInstance;` **仍在**，且该文件内**无任何 `RoleInstance` 代码使用**（我现算：非注释命中仅此 1 处） | ✅ **如实登记为仍开（low）**——不属未申报缺陷；处置建议见 **F-2** |
| 5 | **V-06 基类渲染声明 = T 块 ⑦** | t50 §1 I-12 逐符号计数：`createIconItem(RoleInstance)` 2 处 + `getDisplayName(RoleInstance)` 2 处 = **T-⑦**；我核 `ActiveComponent` **只承载 `HotbarItem` 契约 6 个 getter + `onCast`**，**不含**渲染方法 ⇒ 渲染仍在 `Skill`/`MainWeapon` 薄子类（与"⑦ 待 4.4 统一渲染器接管"一致） | ✅ **处置正确**（挂账 ⑦，4.4 预案已入档） |

**另有 3 项我已确认属"如实登记、不伪造"**：V-04（无 `src/test` ⇒ 单测不可执行，已映射为结构性证据并顺延阶段 5）、V-12（`§3.5.1` 端口 push/flush 延后 4.4）、V-13（历史批次缺停服痕迹 ⇒ 本次已补当轮物证）。**均无"以代码形态倒推"或"以通过覆盖未测项"的情形。**

---

## 3. 阶段 3–4 目标达成判定（对照 §3.2/§3.4/§3.5/§3.5.1 与设计 §5/§5.1）

| 目标（Unity 风格组件模型） | 证据（我现算/复核） | 判定 |
|---|---|---|
| 模板 / 实例聚合根分离 | 阶段 3 装配层：`RoleRegistry` 纯容器 + `RoleLoader` 驱动（我于 t11/t16 预审已核：`RoleLoader` 14 个 `ID_` 常量 L50–L63 + 16 条 `(id, ComponentFactory)` L127–135/154–161） | ✅ |
| 组件 + 钩子（无 `*Aware` 实例化派发） | `implements …Aware` 现算 **0**；I-8（`instanceof *Aware`）**0** | ✅ |
| `ActiveComponent`（技能与主武器去重并集） | 存在且**只承载** `HotbarItem` 6 getter + `onCast`；`Skill`/`MainWeapon` 为薄子类 | ✅ |
| 端口（构造期注入 + 白名单） | `ComponentServices` = **`record`，恰好 10 成员**；`core/ports/` 10 接口 + `core/` 10 适配器（t50 §2）；组件在 `awake()` 前即持有 | ✅（唯一差异 = `end()` 返回类型，已批准并同步） |
| **无 `roleInstance` 知识泄漏** | `\bRoleInstance\b`（roleComponent）**18 行 = 17 注释/javadoc + 1 死 import** ⇒ **代码级泄漏 = 0** | ✅（**F-2** 建议清死 import 以达成"0"） |
| §3.4 行为等价性表 | t50：**6/6**；红线（**能量非硬闸门**）✓ | ✅ |
| §3.5 渲染等价性表 | t50：4 态顺序/材质/色/lore/` X.Xs`/PDC 逐条成立；武器无秒数、无 ENERGY LACK | ✅ |
| §3.5.1 刷新契约 | **部分**（poll + push-after-cast ✅；端口 push + 帧末 flush ✗ ⇒ **设计延后 4.4**，已申报） | ⚠️ 已申报顺延 |

---

## 4. findings

### **F-1【medium】验证基线已被后续提交与重建前推 —— 当前制品不在任何验证证据覆盖之下**

- **事实**：
  - t50 报告的基线 = **`HEAD = e539a53`**、被验 jar = **139 891 B / 2026-09-18 19:35:00.686 / `4FAED396…F9654`**、`src 新于 jar = 0`。
  - **当前** = HEAD 已前推至 **`147f863`**（其间 `f29d18d`/`5c73180`/`147f863` 等为文档/交接类提交）；**jar 已重建为 144 269 B / 19:51:56 / `5CFD2CA4E9C59F80A0BDA1E8ACFE78DF9F019163238C4DFD905EC857952CDC9A`**（最新 `.class` 19:51:55）。
  - 我实测 **`src 新于 jar = 0`** ✓ ⇒ 当前 jar **确实**覆盖当前源码；但 **t50 的全部证据（Mode B 三行、`C = 0`、停服物证、jar 身份）针对的是 19:35 那个 jar**。
- **成因（已定位，非猜测）**：当前唯一的未提交改动 = `src/main/java/com/shadowHunterRolesPlugin/command/RoleCommand.java`（**+192 行**），内容 = **临时调度探针** `/role debug sched [all]`（`RoleCommand.java:102-103` 入口、`:320+` 实现，op 门控、注释明写"取证后可整段删除"+删除清单）。该改动 mtime **19:51:42 < jar 19:51:56** ⇒ **已被打进当前 jar**，但 **t50 于 19:35 的复核不可能覆盖它**。
- **影响**：不是行为缺陷（调试段 op 门控、可删、已在删除清单中），而是**证据链缺口**——若下游（t24 阶段 3–4 整合结案、t19 全量收口）直接引用"当前 jar = 已验证"，会出现**验证结论与制品不一致**（本项目已多次为"引用过期身份"付代价）。
- **requiredFix（可执行）**：二选一，并在 `debug-logs/测试记录/重构进度与交接.md` 的开放项写一行：
  1. **收口**：删除 `RoleCommand` 的调试段（按 `:231` 删除清单：整段 + `case "debug"` 的 3 行 + 5 个 import）→ 重建 + 起服重跑 t50 的四项判据（构建新鲜度 / Mode B 三行 / 级别门与插件门 = 0 / 当轮停服物证）→ 更新被验 jar 身份；**或**
  2. **登记**：保留探针（其用途是调度器迁移取证），但明确记录「**t50 验证覆盖 jar = `4FAED396…`（19:35:00.686）**；此后新增 `RoleCommand` 调度探针（+192 行）并重建为 `5CFD2CA4…`（19:51:56），**该增量未被独立验证**，属探针专用制品」。
  > 我**不**把这条判为 blocker/high：源码侧不变量（I-3..I-15、端口白名单、14/14、等价性表）在 19:35 之后**未变**，t50 的**静态结论依然成立**；缺口仅在"运行侧证据 vs 当前制品"的对应关系。

### **F-2【low】I-7 的"代码级泄漏 = 1"可一步归零**

- `red/RedDeeplySorrowSkill.java:3` 的 `import com.shadowHunterRolesPlugin.core.RoleInstance;` 为**死 import**（该文件内除 import 外仅 javadoc 提及 `RoleInstance`，我现算非注释命中 = 1 即此 import 本身）。
- **影响**：I-7 的判据是"`roleComponent` 下**不得**出现 `RoleInstance`" ⇒ 现状需**附加"17 行全是注释"的口径解释**才能判通过；清掉后该判据可**裸 grep 得 0**，未来复算不再需要口径说明。
- **requiredFix**：删该 import（1 行，行为零变化），并在 t50 报告 V-05 状态改为"已闭合"。

### **F-3【low】阶段 3–4 的双角色冒烟**仍是未填模板，且其引用的 jar 身份已过期**

- `debug-logs/测试记录/阶段4-双角色冒烟清单（用户执行）.md`（6 008 B）：**全部"结果"列留空**（未执行），文末"填完后"三句承诺把结果作为 t14 verify #4 与 t15 运行期证据。
- **其 §5 收尾行引用的制品 = `135308 B @20:40:56`** —— 那是**阶段 4 中段**的 jar（现已重建两次：139 891 B → 144 269 B）⇒ **即使现在填表，其结论也不能直接指向当前制品**。
- t50 §7 已如实记为"**引用用户结论，未逐项填表**、按纪律不编造" ✓（**这是正确处置**，本 finding 不是指责 t50，而是补一条**下游必须知道的口径**）。
- **requiredFix**：在 `重构进度与交接.md` 的开放项登记：「双角色人工冒烟 = **客户端侧未执行**（模板未填）」，并注明**用户已口头确认"没有发现 bug"**；若最终要作为阶段 3–4 的运行期证据，**须按当前 jar 重填**（或明确标注为"对 135 308 B 制品的观察"）。

> **无 blocker / high。** F-1 是**证据链**问题（可二选一收口或登记），F-2/F-3 为 low 补记/清理项；**均不改变阶段 3–4 的静态达成判定**。

---

## 5. 我检查过但**未发现问题的项**

1. I-15 两条命令（listener 作用域 + 全仓）**均 0**；2. 组件自启冷却 12 处调用形态一致；3. `implements *Aware` = 0；4. `instanceof *Aware`（I-8）= 0；5. `RequiresComponents\|requires()` = 0（机制未引入，非实现不合格）；6. `roleComponent` 内 `RoleInstance` **代码级命中 = 1（仅死 import）**；7. 组件类声明 = **14**（4+2+8）且全部基到新体系；8. `ActiveComponent` 不含渲染方法（⑦ 顺延自洽）；9. `ComponentServices` 为 record 且成员恰好 10；10. `CooldownPort.end()` = `boolean`，javadoc 记载语义与批准来源；11. V-14 已在 **4 处活文档 + 代码 javadoc** 同步并标注 `bd726cf`/用户 D-list 裁定；12. 三条红线（能量非硬闸门 / 两事件对外发布保留 / 容器直派）在报告与活文档中一致表述；13. §3.4 6/6、§3.5 渲染表、冻结语义（未冻结抛 `IllegalStateException`）逐条有据；14. `§3.5.1` 部分落地**已如实标注**为设计延后，未冒充通过；15. V-04/V-12/V-13 **均如实登记**、无"以代码形态倒推"；16. t50 §7 三项未完成**如实列出**（无静默略过）；17. 首版 failed 文件已按来源标注改名保留（未删除、未覆盖），可审计性完好；18. 卡面 inScope 的旧路径（`docs/` 根）与现路径（`debug-logs/测试记录/`）的偏离**有队长追认记录**（报告 §1 首行）。

---

## 6. 必须挂账（不得静默略过）

| 挂账项 | 现状 | 落点 |
|---|---|---|
| **F-1**：当前制品（144 269 B / `5CFD2CA4…`）未被验证覆盖 | 未收口 | **t24 整合结案** + **t19 全量收口** |
| **F-3**：双角色人工冒烟未执行（模板未填）；用户口头"无 bug" | 未执行 | **t24 / t19** + 用户侧 |
| T 块 **⑦**（渲染统一到 4.4） | 已申报顺延 | t17/t18（阶段 5）与 4.4 预案 |
| `RequiresComponents` 注册期校验 | 机制未引入，用户裁定归阶段 5 | 阶段 5 候选 |
| t7-L1（ticker/updater 停止的运行级证据）等既有挂账 | 延续 | t24 / t19 |

---

## 7. verdict

**pass**（0 blocker / 0 high；**1 medium（F-1）** / 2 low）。

**依据**：阶段 3–4 的目标（模板/实例分离、组件+钩子、`ActiveComponent`、10 成员端口白名单、**`roleInstance` 知识泄漏代码级 = 0**）经我**现算**逐条成立；`I-3..I-15` 与 §3.4/§3.5/§3.5.1 由 t50 以**命令+输出+结论**覆盖，且我抽样复算（I-15 两条、`cooldowns()`、14 组件、`RoleInstance` 形态、`ComponentServices` 成员数）**全部一致**；**5 项卡面指定的例外项全部处置正确**（未引入者如实记 finding 并顺延、设计延后者标注不冒充、已批准签名变更已在 4 处活文档同步、死 import 如实留开、⑦ 挂账）；**无 blocker/high 未解决** ⇒ 满足卡面 verdict 条件。
F-1（本次 revision 后制品未再验证）是**证据链**缺口而非行为缺陷：源码侧不变量在 t50 基线后未变、静态结论依然成立，其收口方式（删探针重建并重跑 / 登记为探针专用制品）已给出可执行二选一，须在 t24/t19 落地。

**评审人**：eng-审阅（attempt 1）
