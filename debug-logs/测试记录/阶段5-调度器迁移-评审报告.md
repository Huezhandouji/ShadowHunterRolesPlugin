# 阶段 5 · 调度器迁移批评审报告（t55，评审 t51）

> **执行者**：eng-审阅（reviewer）｜**任务**：t55（review round 1）｜**attempt**：1
> **评审对象**：调度器迁移批 = `platform/BukkitSchedulerAdapter.java` 由 `Bukkit.getScheduler()` 切到 `GlobalRegionScheduler` **+** `initialDelay ≤ 0` 归一批（含临时探针 `command/RoleCommand.java`）
> **事实基础**（三件套引用，不内嵌哈希）：`debug-logs/测试记录/阶段5-调度器迁移-停服物证.txt` §1–§12、`debug-logs/测试记录/阶段5-起服日志-调度器迁移.log`、`debug-logs/测试记录/重构进度与交接.md` 第 E 节（含 E.1「制品代次与 F1 防线」）
> **评审时刻**：2026-09-18（+08:00）
> **结论**：**verdict = pass**（0 blocker / 0 high / 0 medium / **1 low**）

---

## 0. 三条卡面 Verify 我全部现跑（含更正一处读数口径）

| # | 卡面 Verify 命令 | 我的现算 | 判定 |
|---|---|---|---|
| 1 | `git diff --numstat -- …/platform/Scheduler.java …/platform/Task.java`（应为空：接口未改） | **非空**：`5/1`（Scheduler）、`16/2`（Task） | ⚠️ **读数需分层** → 见 §1.1：**全部为注释行**，**接口签名逐字未变** ⇒ 实质满足 |
| 2 | `git grep -n 'Math.max(1' -- …/platform …/core/TimerPortImpl.java`（应 2 处：两入口归一） | **裸计数 4 行**：`TimerPortImpl.java:51`（**代码**）、`BukkitSchedulerAdapter.java:51`（**代码**）、`BukkitSchedulerAdapter.java:25`（javadoc）、`Scheduler.java:16`（javadoc） | ✅ **代码使用点 = 2 ⇒ 两入口归一成立**（裸计数含 2 处文档 ⇒ 按纪律须扣除后报） |
| 3 | `git grep -nE 'execute\(' -- …/platform`（应为 0：未用 execute 替代 run） | **空 = 0** | ✅ 通过 |

---

## 1. 接口面：**调用方零改动** ✓

### 1.1 签名级比对（我逐行判定 `+` 行性质）
`Scheduler.java` 的 5 行新增、`Task.java` 的 16 行新增，我**逐行标注**：**全部为 javadoc 行（含空 javadoc 行）**，**非注释行 = 0**。签名逐字比对：

| 文件 | HEAD 版签名 | 工作树版签名 | 相同 |
|---|---|---|---|
| `platform/Scheduler.java` | `Task run(Runnable task);` / `Task runLater(Runnable task, long delayTicks);` / `Task runRepeating(Runnable task, long initialDelayTicks, long periodTicks);` | 同左 | ✅ **True** |
| `platform/Task.java` | `void cancel();` / `boolean isCancelled();` | 同左 | ✅ **True** |

⇒ **接口签名逐字未变** ✓ ⇒ **全部调用方零改动** ✓（与 `阶段5-javadoc-停服物证.txt` §1–§2 的"非注释行逐字未动"独立一致）。

### 1.2 适配器本体（`BukkitSchedulerAdapter.java`，回滚单元 = 1 文件）
逐读确认：
- **类名未变** ✓（`public final class BukkitSchedulerAdapter implements Scheduler`），构造器仍 `(Plugin plugin)` ✓；
- **以 `GlobalRegionScheduler` 为唯一实现** ✓：`run` → `getGlobalRegionScheduler().run(...)`、`runLater` → `.runDelayed(...)`、`runRepeating` → `.runAtFixedRate(...)`；
- **未用 `execute()` 替代 `run()`** ✓（卡面 Verify 3 = 0）；
- **句柄差异在适配器边界内吸收** ✓：`wrap(ScheduledTask)`，`cancel()` **吞掉 `CancelledState`** 保持 `void`、`isCancelled()` **直接委托** ⇒ `Task` 接口零改动即可承载 ✓；
- **归一点** ✓：`Math.max(1L, initialDelayTicks)`（`:51`），并在注释里写明**为何不用 `run()` 替代**（会丢 `ScheduledTask` 句柄 ⇒ 任务无法回收）—— 该理由与"容器必须在角色清除时取消任务"的既有契约一致 ✓。

---

## 2. 逐调用点核对 `runRepeating` 参数顺序（**独立必查项**）✓

两个同名方法**参数顺序相反**（`platform.Scheduler = (task, delay, period)` vs `core.ports.TimerPort = (delay, period, task)`）。我把**全部**调用点按**所属入口**分组列出并逐点判序：

| 入口 | 调用点（现算） | 实参顺序是否符合该入口签名 |
|---|---|---|
| **TimerPort**（`(delay, period, task)`） | `DefaultSanTEZeroPunishment.java:56` `(0L, 40L, Runnable)` · `MeiqiheziBloodySlashSkill.java:50` `(0L, 2L, Runnable)` · `RedSolitaryArroganceSkill.java:45` `(1L, 6, Runnable)` · `RoleCommand.java:616`（探针 `(0L, 10L, () -> …)`） | ✅ **4/4 正确** |
| **原始 Scheduler**（`(task, delay, period)`） | `TimerPortImpl.java:51`（**唯一转调点**，显式把 `task` 挪到首位 ✓） · `RoleInstance.java:118` · `BuffManager.java:136` | ✅ **3/3 正确**（后两处为生产直调，实参多行，我按 `(task, …, …)` 形态确认） |
| **唯一实现** | `BukkitSchedulerAdapter.java:48` 覆写签名与接口逐字一致 | ✅ |

⇒ **参数顺序逐调用点核对 = 全部正确** ✓。**并且**：我另核了 `TimerPort.java:11/:13` 与 `Scheduler.java:9/:11` 的 javadoc **已互相写出顺序对照** ✓（我在 t52 前提出的"同名反序"隐患已被文档化），`TimerPortImpl.java:51` 上方亦写明"**本行是两个『同名反序』签名之间的唯一转调点**" ✓ ⇒ **该隐患的处置达标**（不靠双入口归一兜底，而是**逐点核对 + 文档对照 + 单点转调注释**）。

---

## 3. 两列实测（按点分路径）✓

### 3.1 需 `0→1` 实测的点 = **3 处**（与我按调用点实参判定的 3 处一致 ✓）
| 调用点 | 迁移前实现 | 迁移后 | 两列首触发实测 | 判定 |
|---|---|---|---|---|
| `BuffManager.java:136`（声明 `0, 1`） | Bukkit `runTaskTimer` | 适配器 `runAtFixedRate(归一→1)` | 原生 `(0,10)` #1 delta=**1** ↔ 适配器 `(0,10)` #1 delta=**1**；#3 delta=**21 ↔ 21** | ✅ **逐字相同** |
| `DefaultSanTEZeroPunishment.java:56`（`0, 40`） | 同上 | 同上 | 入参等价性由同一声明值 A/B 覆盖（同 §2 矩阵） | ✅ |
| `MeiqiheziBloodySlashSkill.java:50`（`0, 2`） | 同上 | 同上 | 同上 | ✅ |
> 旁证（卡面要求）：**"(Bukkit) 0 vs 1 是否同 tick"= 同 tick** ✓ —— 升物证 §3 矩阵第 3/4 行给出 `③c raw runTaskTimer(0,10)` 与 `③ raw runTaskTimer(1,10)` **同 tick=1096、同 delta=1**，并另有 `runLater(20) delta=20 ↔ 20` ✓。

### 3.2 值未变的点 = **2 处** ⇒ **只判"实现换了、值没换"，不套 `0→1`** ✓
| 调用点 | 迁移前 | 迁移后 | 判定 |
|---|---|---|---|
| `RoleInstance.java:118`（ticker） | Bukkit 路径，delay=**1** | 端口路径，delay=**1** | ✅ 值未变 |
| `RedSolitaryArroganceSkill.java:45` | **Folia 语义 API**（`getGlobalRegionScheduler().runAtFixedRate`），delay=**1** | 平台端口，delay=**1**（`(1L, 6, …)`） | ✅ 值未变 |
> 我在 t52 后曾提出"左列分两来源"，经我**逐提交现算**（`git show <commit>:<file>`）确认：`RedSolitaryArroganceSkill` 自 `ff76907` 起、经 `c565785`、`6421eda`(t9) 换端口，**delay 始终为 1** ✓ —— 与 §E.1 的 pickaxe 复核（`git log -S` 恰好 4 个提交）一致 ✓。

---

## 4. 自带物证是否覆盖**它自己的制品**（F1 教训）—— **成立** ✓（含一处由我补链）

### 4.1 三段时序（我现算）
| 对象 | 现算 |
|---|---|
| 当前 jar | **148 297 B / 2026-09-18 20:26:27** / `A74A5747…85A68` |
| `阶段5-起服日志-调度器迁移.log` | **20:03:20**（6135 B） |
| `阶段5-调度器迁移-停服物证.txt` | **20:08:51**（24 080 B） |

⇒ **迁移批物证（20:08:51）自述的被验 jar = `146 010 B / 20:01:38.935 / 163F1D8C…`** ✓（物证 §3.2 逐字），即**该批证据覆盖的是它自己的那一代制品** ✓。**但该批日志/物证的 mtime 早于当前 jar（20:26:27）** ⇒ 单看时序**不满足** E.1 的可复用判据。

### 4.2 我补的这条链（让"早期证据仍有效"**可复核**）
**当前 jar 与被验代制品逐字节相同** ⇒ 迁移批的运行证据**依然指向当前字节码**：
- `阶段5-javadoc-停服物证.txt` §3.2 现算自述：强制重建后 jar = **148 297 B / 20:26:27.061 / `A74A5747…85A68`**，**"与上一代（build E）完全相同"** ✓（我独立复算当前 jar 哈希 = `A74A5747…85A68`，**逐字一致** ✓）；
- 而 `重构进度与交接.md` 第 E.1 的**代次地图**明确 **E = 双入口归一批**（即本批）⇒ **E 的 jar ≡ 当前 jar（字节级）** ⇒ **20:03/20:08 的运行证据对当前字节码仍然有效** ✓；
- 我另核：`阶段5-javadoc-停服物证.txt` §5.5 **F1 自证链**为 `jar 20:26:27 < log 20:27:08 < 本文件 20:29:53` ✓ ⇒ **javadoc 那一代自身满足"证据晚于制品"** ✓。
⇒ **F1 的实质要求（证据覆盖它自己的制品）在"字节码同一"的意义上成立** ✓，且**当前 jar 亦未被伪造覆盖**（javadoc 代的 log/物证均晚于它）✓。

### 4.3 历史锚点扫描
物证 §3.5：锚 `阶段4-起服日志-T2b.log`，**`ADDED 0 / REMOVED 0 / A) oshi 0 / B) 世界状态 0 / C) OTHER 0`** ✓；对整轮实时日志复跑**同样 `C = 0`** ✓；**50 份可比锚点 `C = 0`（min/max = 0/0）** ✓；另 **2 份非服务器日志**（`t2-build.log`、`t2-阶段0-build.log`）**已申报**为不可比 ✓；最老基线 `…-2026-09-13-1519.log` 的 added 3 / removed 9 **其 C 亦为 0**（差异全在 A/B 噪声类）✓。

---

## 5. 运行判据与生产路径 ✓

- **Mode B 三行齐**（我现算）：`角色起服日志 :10 Bukkit plugins (1):` ✓ / `:46 Enabling ShadowHunterRolesPlugin v1.0.0` ✓ / `:47 ShadowHunter Character System enabled.` ✓ / `:51 Done (13.133s)!` ✓；SHDF 依赖行 **未出现**（Mode B 预期 ✓）。
- **级别门 `/(ERROR|SEVERE|FATAL)]` = 0** ✓；**插件门 `[ShadowHunterRolesPlugin]`+`/(WARN)]` = 0** ✓。
- **生产路径实跑**（诚实归因，充分理解其边界）：`/role set meiqihezi` → 1-tick ticker 约 80 tick → 被动发甲 → `/role energy get` = 100 → `/role clear`，**全程零 SEVERE/ERROR** ✓ ⇒ `runRepeating` 与 `Task.cancel()` 在**生产入口**可用 ✓。
- **候选 verdict 翻转**：探针由 `CANNOT swap as-is` → **`CAN swap … zero visible difference`** ✓，其成立依赖 §3 的两列实测（不是靠断言）✓。

---

## 6. 已申报未覆盖项（我核对其"申报是否诚实"）✓

| 申报项 | 我的核对 | 判定 |
|---|---|---|
| 3 个 `0L` 调用点的**业务触发**未构造成受击场景 | 入参等价性已由 A/B（同声明值两路径）覆盖 ✓；业务触发确未做 ✓ | ✅ **申报诚实**（未把入参等价冒充为业务等价） |
| 一次性 `run` 无原生对照腿 | 其生产调用点 = **0**（我核：`Scheduler.run(` 仅适配器实现 + 探针）⇒ 无风险面 ✓ | ✅ |
| 四行合格线未执行（Mode B 稳态） | 与既有裁定一致 ✓ | ✅ |
| Folia 真机未验证 | 本批只做 `GlobalRegionScheduler`（Global = 普通 Paper 主线程语义）✓ | ✅ |
| `0→1` 的**措辞保守化** | 登记为"经实测未见偏移 **+** 按保守口径仍属已申报项 **+** 未来若出现偏移须重新申报" ✓ | ✅ **正确姿势** |

---

## 7. findings

### **T55-F1【low】当前制品与"迁移批代制品"的字节级同一**是**隐含推论**，未在两处被**一句话显式绑定**

- **问题**：迁移批的运行证据（`阶段5-起服日志-调度器迁移.log` 20:03:20 / `阶段5-调度器迁移-停服物证.txt` 20:08:51）其 mtime **早于**当前 jar（20:26:27）。按 `重构进度与交接.md` 第 E.1 的**可复用判据**（"证据 mtime 晚于其覆盖的 jar" **且**"物证内写明被覆盖 jar 与日志的身份"），**迁移批日志本身不满足第一条**。其仍有效**完全依赖**两步跨文档推论：① `阶段5-javadoc-停服物证.txt` §3.2 断言"与上一代（build E）完全相同"；② E.1 代次地图把 **E** 定义为本批。
- **影响**：**判定结论不变**（我已在 §4.2 复核两步都成立、且当前 jar 哈希逐字一致）；但后来者若只按 E.1 判据**逐份**检查，会看到"迁移批日志早于 jar"并可能误判为**覆盖缺口**（F1 同型误报），而**无法一眼看出**"因为字节码同一所以仍然有效"。
- **requiredFix（可执行）**：在 `重构进度与交接.md` **第 E.1 加一行显式绑定**（一句即可），三件套引用：
  > 「**终态制品与 E 代逐字节相同**：当前 jar = `148 297 B / 20:26:27` / `A74A5747…`（见 `阶段5-javadoc-停服物证.txt` §3.2 与 §5.5）⇒ **E 代（本迁移批）的 20:03/20:08 运行证据对当前字节码仍然有效**；判据 = ① jar SHA 相同 ② 代次地图 E = 本批。」
  **属文档补记，不需改代码、不需重跑**。
> **无 blocker / high / medium。**

### 7.1 我检查过但**未发现问题的项**
1. 卡面 Verify 1 的实质（接口签名逐字未变，改动 = 纯 javadoc）✓；2. 卡面 Verify 2 的两入口归一（代码使用点 = 2）✓；3. 卡面 Verify 3（未用 `execute(`）✓；4. 适配器类名/构造器未变 ✓；5. `GlobalRegionScheduler` 为唯一实现（`run`/`runDelayed`/`runAtFixedRate`）✓；6. `wrap` 中 `CancelledState` 被吞、`Task` 签名保持 `void` ✓；7. `isCancelled()` 直接委托 ✓；8. `Math.max` 归一只改"值"、且理由（丢句柄）已注释 ✓；9. **TimerPort 侧 4/4 调用点顺序正确** ✓；10. **Scheduler 侧 3/3 正确、转调点唯一且有注释** ✓；11. 两接口 javadoc **已互写顺序对照** ✓；12. `0→1` 按点分路径 = 3 处需实测 / 2 处值未变 ✓（与实现者判定一致）；13. 两列实测 `1 ↔ 1`、`21 ↔ 21`、`delta 20 ↔ 20`、线程均 `Server thread` ✓；14. `(Bukkit) 0 vs 1 同 tick` 旁证存在 ✓；15. `RedSolitaryArroganceSkill` 值自 `ff76907` 起始终 `1` ✓（pickaxe 4 提交一致）；16. 迁移批物证**自述被验 jar = 146 010 B / 20:01:38.935 / `163F1D8C…`** ✓；17. 当前 jar 哈希 = `A74A5747…85A68` 与 javadoc 物证 §3.2 自述**逐字一致** ✓；18. javadoc 代 F1 自证链 `jar < log < 物证` ✓；19. Mode B 三行 + 两门口径 0 ✓；20. 历史锚点 50 份 `C = 0` + 2 份构建日志已申报 + 最老基线 C 亦 0 ✓；21. 生产路径实跑零 SEVERE/ERROR ✓；22. 4 项"已申报未覆盖"逐条核为**诚实申报**（未把入参等价冒充业务等价）✓；23. 回滚单元 = 1 文件、接口 diff = 0 ✓。

---

## 8. 必须挂账

| 挂账项 | 现状 | 落点 |
|---|---|---|
| **T55-F1**：终态制品与 E 代字节同一的显式绑定 | 未补记 | `重构进度与交接.md` E.1（队长或实现者一行） |
| 3 个 `0L` 调用点的**业务触发**未实测 | 已申报 | **t24 / t18** |
| 一次性 `run` 无原生对照腿（生产调用点 = 0） | 已申报 | 记录保留 |
| 真实四行合格线（25565 装 SHDF） | 已申报（Mode B 稳态） | **t24 / t19** + 用户 |
| Folia 真机未验证 | 已申报 | 记录保留 |
| **探针删除批**（按清单移除后重建） | **必须重新取证** | **t24/t19（既有约定）** |

---

## 9. verdict

**pass**（0 blocker / 0 high / 0 medium；**1 low**）。

**依据**：卡面三条 Verify 我全部现跑并**更正了 Verify 1/2 的读数口径**（Verify 1 的 `numstat` 非空但**逐行判定全为注释 ⇒ 签名逐字未变**；Verify 2 的裸计数 4 行 = **代码使用点 2 + 文档 2** ⇒ 两入口归一成立）；**参数顺序逐调用点核对 = 7/7 正确**且转调点唯一并有注释、两接口 javadoc 已互写对照 ⇒ 该独立必查项达标（未靠双入口兜底）；**两列实测按点分路径**：`0→1` 的 3 处给出 `1↔1 / 21↔21 / delta 20↔20` 与"0 vs 1 同 tick"旁证，值未变的 2 处只判"实现换了、值没换"；**自带物证覆盖自己的制品**：迁移批自述被验 jar = `146 010 B / 20:01:38`，当前 jar 与之**字节同一**（`A74A5747…` 双向一致），javadoc 代自身满足"证据晚于制品"⇒ 实质成立（其显式绑定记为 **T55-F1 low**）；Mode B 三行 + 两门口径 0 + 生产路径实跑零异常；50 份历史锚点 `C = 0`（2 份构建日志已申报）。**无 blocker/high 未解决** ⇒ 满足卡面 verdict 条件。

**评审人**：eng-审阅（attempt 1）
