# 外观冻结清单（热键栏物品的声明面）

> **用途**：把「能出现在热键栏的物品」的**声明数据**逐行冻结下来，日后外观改动可用本文 §3 的命令**自动 diff**。
> **基线（现算）**：HEAD = **`f88afeb15a403578378fb995cde662a1eeefded3`** · `porcelain -- src` = **0**。
> **★ 条数以实测为准**：本文实测 **9 行**（“31 label”是**会话中的简写口径**，出处 = 台账待续队列第 1 条，**那是待办任务名、不是可比清单**）⇒ 本表**不为凑 31 增删任何行**。

---

## 1. 清单表（每行 = 角色 · 槽位 · 组件 id 常量 · 材质 · 显示名 · 描述行）

| # | 角色 | 槽位 | 组件 id（`RoleLoader` 常量） | 材质（`icon`） | 显示名（`displayName`） | 描述行（`description`） |
|---|---|---|---|---|---|---|
| 1 | meiqihezi | 1 | `ID_MEIQIHEZI_UNCONCERN` | `Material.BLAZE_POWDER` | 漫不经心 | 获得2秒速度5 |
| 2 | meiqihezi | 2 | `ID_MEIQIHEZI_BLOODY_SLASH` | `Material.IRON_INGOT` | 血腥连斩 | 向前移动4格并斩击，重复四次 |
| 3 | meiqihezi | 3 | `ID_MEIQIHEZI_CIRCLE_SLASH` | `Material.GOLD_INGOT` | 圆弧斩 | 前摇1秒后对7m范围内所有敌人造成20真实伤害 |
| 4 | meiqihezi | 0 | `ID_MEIQIHEZI_JUEJUE_MAIN_WEAPON` | `Material.DIAMOND_HOE` | Jue Jue | A ShadowHunter mainWeapon |
| 5 | red | 0 | `ID_RED_SANCTIFIED_BLADE` | `Material.IRON_SWORD` | 至洁之刃 | 攻击施加流血效果 |
| 6 | red | 1 | `ID_RED_SOLITARY_ARROGANCE` | `Material.FERMENTED_SPIDER_EYE` | 孤妄自赏 | 连续捅击四次。每次造成伤害，如果命中敌人，回复生命 |
| 7 | red | 2 | `ID_RED_EVIL_SHOCK` | `Material.REDSTONE` | 煞气震赫 | 对周围5格范围内的敌人造成3秒致盲和缓慢III，结算他们5层流血。恢复[红]的10点TE值 |
| 8 | red | 3 | `ID_RED_DEEPLY_SORROW` | `Material.REDSTONE_BLOCK` | 黯然销魂 | 持续扣减[红]的TE值，每秒10点，在TE值归零前获得持续的生命恢复5与力量2，在TE值归零后结束这个技能 |
| 9 | example | 0 | `ID_EXAMPLE_SELF_REFRESHING` | `Material.CLOCK` | 自刷新示例 | 外观会自己变：组件只请求重绘，不写物品 |

**行数 = 9**（现算来源：`registry/RoleLoader.java` 的 9 行 `.addComponent(…, new XxxSpecification().setSlot(n))`）。

**声明值的三段来源（每一段都已现算核对）**
- **槽位**：`RoleLoader.java:148–:151`（meiqihezi 四件）· `:176–:179`（red 四件）· `:207`（example 一件）。
- **显示名 / 描述行 / 材质**：各组件**嵌套 `Specification`** 的构造器实参（`Component.text("…")` ×2 + `Material.…`）。
- **工厂入口**：`base/Skill.java:84` 与 `base/MainWeapon.java:76` 的 `HotbarSpecification.of(<kind>, id, displayName, description, icon, cooldown, energyCost)`。

---

## 2. 口径（冻结面 / 允许变化 / 不可达）

**属冻结面（改动即需 diff 并说明）**
- 上表六列：**角色 · 槽位 · 组件 id 常量 · 材质 · 显示名 · 描述行**。
- **状态文案规则**（由基类默认画法产出，四态）：`READY` 绿色 + 粗体；`COOLING` 灰色 + 粗体 + **`" x.xs"` 后缀**；`DISABLED` 红色 + 粗体；`ENERGY_LACK` 灰色 + 粗体。
- **冷却文案格式**：`String.format("%.1f", 剩余刻 / 20f) + "s"` —— 现算**代码命中恰 1 处** = `roleComponent/base/Skill.java:182`（`%.1f` 全仓 2 命中，另 1 处是注释）。
- **识别键（PDC）**：`SKILL_KEY` / `MAIN_WEAPON_KEY` 共 **16 行**命中；由基类默认画法在**最后一步**写入。
- **★ 主武器末位恒传 `0`**：`HotbarSpecification.of(…, 0)`（`base/MainWeapon.java:76`）⇒ 主武器**永远不会**进入 `ENERGY_LACK` 状态 ⇒ 该态对主武器**不可达**（不得为它造新外观）。

**允许变化（不算破坏冻结面）**
- 冷却**秒数数值**随剩余刻变化（`x.xs` 中的 `x` 是运行期读数，不是冻结值）；`energyCost` 声明值（技能可有非零值）。
- 物品的 lore 其余行、附魔、耐久等**未在上表列出的装饰**（见 §4 未纳入项）。

---

## 3. 可复跑比对方法（日后外观改动可自动 diff）

```powershell
# ① 行数与槽位（期望：9 行；槽位序列 = 1,2,3,0 / 0,1,2,3 / 0）
git -C ShadowHunterRoles grep -nF -e "setSlot" -- src/main/java/com/shadowHunterRolesPlugin/registry/RoleLoader.java

# ② 工厂入口（期望：2 处 = base/Skill.java:84 与 base/MainWeapon.java:76，后者末位恒 0）
git -C ShadowHunterRoles grep -nF -e "HotbarSpecification.of" -- src/main/java

# ③ 逐件的显示名/描述行/材质（逐件读文件，勿用正则；每件看其嵌套 Specification 的构造器）
#    期望：9 件的 (Component.text("…") ×2 + Material.…) 与上表逐字相同
git -C ShadowHunterRoles grep -nE 'Component\.text\("|Material\.' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent/custom src/main/java/com/shadowHunterRolesPlugin/roleComponent/builtin

# ④ 冻结面格式与识别键（期望：%.1f 代码命中 1 处；SKILL_KEY/MAIN_WEAPON_KEY 共 16 行）
git -C ShadowHunterRoles grep -nF -e "%.1f" -- src/main/java
git -C ShadowHunterRoles grep -nE "SKILL_KEY|MAIN_WEAPON_KEY" -- src/main/java
```

**diff 判读**：②③ 的**逐字差异**即外观变更；① 的差异说明**栏位/装配序**变了（渲染序 = 装配序 ⇒ 影响可见顺序）；④ 的差异说明**冷却文案格式或识别键**被改动。

---

## 4. 未纳入本清单的外观面（如实申报）

1. **被动组件不占栏位** ⇒ 无外观行（`RoleLoader` 里无 `setSlot`）——本表 9 行**不含任何被动**。
2. **快捷栏状态装饰的渲染结果**（材质三态、名称着色、lore 的其余行、附魔/耐久）只冻结**规则**（§2），未冻结**逐格成品**；成品需运行级 dump（`t130`/`t131` 一类窗口卡读数），**本卡未引用**（引用须标注来源与代次）。
3. **组件 id 的字符串字面量**未逐条取到（本表用 `RoleLoader` 的**常量名**）；若需字面量，须读 `RoleLoader` 顶部常量定义。
4. **技能/主武器的 `cooldown` 与 `energyCost` 数值**未逐条取到（本卡 7 元组不含这两列；已知三处：`RedSolitaryArrogance` `200,0` · `RedEvilShock` `120,0` · `RedDeeplySorrow` `600,0`）。
5. **基类默认画法的逐行实现**未抄录（只冻结其产出规则与格式串）；如需逐行冻结，须单独一张卡。
