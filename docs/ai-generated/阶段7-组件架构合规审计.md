# 阶段7 · 组件架构合规审计（以 `组件模型.md` §1/§2 为尺子 · 只读）

> **本件是只读审计**：`src/` **零改动**，产出只有本文件一个路径；所有判定 = 结论 + 证据（file:line + 逐字原文）。
> **尺子** = `docs/ai-generated/组件模型.md`（93 行）的 **§1 三层模型两条不做项** 与 **§2 判据集 8 条**。

---

## 0. 取数口径与锚点（先读这一节，否则读数无法复核）

| 项 | 值 |
|---|---|
| **取数时刻** | HEAD = **`a25790a`**（`docs(使用者手册): the component-table rows…`）；`porcelain` 当时 = **3 行** |
| ★ **在飞的卡** | **`t30` 正在改** `core/RoleInstance.java` · `listener/SkillListener.java` · `listener/MainWeaponListener.java`（`git diff --stat`（RoleInstance）= **1 插入 / 65 删除**，1083 → **1019** 行）⇒ ★★ **本件对这三件的读数一律标注「`t30` 在飞 ⇒ 读数可能已变」**；§3 另给「HEAD 读数 vs 在飞读数」对照 |
| 读取通道 | `[System.IO.File]::ReadAllLines(<绝对路径>)` 逐行 + `-cmatch`（**大小写敏感**）；**不用 `Get-Content`**（含 CJK 无 BOM 的 UTF-8 上会静默打坏行数） |
| 行数口径 | **全文行数**（含空行与注释）；凡"命中行数"都指**原始文本命中行数**，**注释与 javadoc 计入**（本件逐条写明是否含注释） |
| 计数口径 | 每处命中都列 `文件:行号｜逐字原文`；**不给裸数字**（本会话已因裸数字踩过 15 次） |
| gitignored 目录 | `debug-logs/**` 被 `.gitignore:47` + `:85` 覆盖 ⇒ **`git grep` 对它恒空** ⇒ 本件凡涉该目录只用逐行读盘（本件未涉及） |

---

## 1. `组件模型.md` §1「三层模型」的两条**不做项**

### 1.1 不做项①「**不在组件里持有聚合根引用做中转**」

**可复跑命令（本件实跑）**

```powershell
$S = (Resolve-Path 'ShadowHunterRoles/src/main/java/com/shadowHunterRolesPlugin').Path
$rc = Get-ChildItem "$S\roleComponent" -Recurse -Filter *.java
foreach ($p in @('import com\.shadowHunterRolesPlugin\.core\.RoleInstance','RoleInstance\s+[a-zA-Z_]','new RoleInstance','(^|[^a-zA-Z])Role\s+[a-zA-Z_]')) {
  $hit = @(); foreach ($f in $rc) { $n=[IO.File]::ReadAllLines($f.FullName)
    for ($i=0; $i -lt $n.Count; $i++) { if ($n[$i] -cmatch $p) { $hit += "$($f.Name):$($i+1)|$($n[$i].Trim())" } } }
  "[$p] => $($hit.Count)"; $hit }
```

**实测输出（`roleComponent/**` 全文行数口径，注释计入）**

| 模式 | 命中 | 逐字 |
|---|---|---|
| `import …core.RoleInstance` | **1** | `SanTEComponent.java:3｜import com.shadowHunterRolesPlugin.core.RoleInstance;` |
| `RoleInstance <标识符>`（字段 / 局部 / 形参） | **0** | —— |
| `new RoleInstance` | **1** | `SanTEComponent.java:167｜delivery.accept(new RoleInstance.ChangeDelivery(entry.owner(), () -> entry.listener().accept(change)));` |
| `Role <标识符>` | **1** | `ServiceComponents.java:58｜Role role,`（`ServiceComponents.Input` 的 record 组件） |

**判定**

- **组件面 = 合规**：`roleComponent/**` 内**没有任何** `RoleInstance`/`Role` 类型的字段或局部变量；唯一那处 `new RoleInstance.ChangeDelivery(...)` 构造的是**框架侧的值载体**（`ChangeDelivery` 是 record），**不持有实例、不中转**。
- **清单面 = 待裁定（低危）**：`ServiceComponents.Input` 以 `Role`（聚合根）为构造参数——`ServiceComponents` **不是组件**（不 `extends RoleComponent`），而是框架级清单/工厂；它只读两个上限（`in.role().getMaxEnergy()` / `getMaxSanTE()`，见该件 `:131`/`:137`）⇒ **可机械改为传两个 `int`**，从而让 `roleComponent/**` 对聚合根的类型引用归零。

**修复面建议**：`roleComponent/frameworkLevel/ServiceComponents.java` 一处（机械，1 行改签名 + 2 处调用点）；**不跨包**。

### 1.2 不做项②「**不从组件直接写背包**」

**可复跑命令（本件实跑）**

```powershell
$S = (Resolve-Path 'ShadowHunterRoles/src/main/java/com/shadowHunterRolesPlugin').Path
$rc = Get-ChildItem "$S\roleComponent" -Recurse -Filter *.java
$hit = @(); foreach ($f in $rc) { $n=[IO.File]::ReadAllLines($f.FullName)
  for ($i=0; $i -lt $n.Count; $i++) { if ($n[$i] -cmatch 'getInventory\(\)\.set|getInventory\(\)\.add|getInventory\(\)\.clear') { $hit += "$($f.Name):$($i+1)|$($n[$i].Trim())" } } }
$hit.Count; $hit
```

**实测输出（命中 **16** 行；**全部**是两个装备被动的**装备槽**写口）**

```
MeiqiheziEquipmentsPassive.java:71|player.getInventory().setHelmet(helmet);      :72|setChestplate(chestplate);
MeiqiheziEquipmentsPassive.java:73|player.getInventory().setLeggings(leggings);  :74|setBoots(boots);
MeiqiheziEquipmentsPassive.java:83|player.getInventory().setHelmet(null);        :84|setChestplate(null);
MeiqiheziEquipmentsPassive.java:85|player.getInventory().setLeggings(null);      :86|setBoots(null);
RedEquipmentsPassive.java:71|…setHelmet(helmet);  :72|…setChestplate(chestplate);
RedEquipmentsPassive.java:73|…setLeggings(leggings); :74|…setBoots(boots);
RedEquipmentsPassive.java:83|…setHelmet(null); :84|…setChestplate(null); :85|…setLeggings(null); :86|…setBoots(null);
```

另：`roleComponent/**` 内 `getInventory()` 共 **18** 命中 = 上列 16 + `HotbarItems.java:35`（`Inventory inv = player.getInventory();`，**读句柄**，为清栏位）+ `hotbar/HotbarRenderer.java:121`（写点侧取句柄）——这两处属**允许的写点/清空路径**。

**判定 = ★ 待裁定（中危：口径缺口，不是代码缺陷）**

- **热键栏物品写点唯一性未破**（见 §2⑧：`inv.setItem(` = 2）。
- 但按 §1 第 2 条**字面口径**（"不从组件直接写背包"），这两件**确实**由组件直接改玩家背包**装备槽**；而 `组件模型.md` 的 R⑧ 判据只谈"物品写入 = 帧末 flush"⇒ **两条口径在装备槽上不相交**。
- 旁证：`使用者手册.md` §6 Q6 **已把**"清角色时 4 个装备槽的原有装备被一并删除"记为**已知代价**（不是缺陷）⇒ 说明该行为是**有意**的，但**没有**任何文档把它写成"允许的例外"。

**修复面建议**（三选一，需先裁定口径）：① 在 `组件模型.md` §1 显式豁免「组件声明面内的装备槽写入」；② 立卡把装备写入收进组件侧的单一入口（`roleComponent` 内，不改行为）；③ 维持现状 + 在文档里登记为"例外"。★ **跨包**（roleComponent + docs 口径）⇒ 按卡面要求**须另立卡并说明原因**（同一改动同时触及行为面收口与文档口径，且"穿戴装备"语义需人肉读上下文）。

---

## 2. `组件模型.md` §2 判据集（**8 条全覆盖**）

### ① 能力接口的合并条件

**判据原文摘引**：「两条**同时成立**才合并 —— ① 实现者集合相同；② 一方的方法语义必须读另一方的状态」

**命令与实测**

```powershell
git -C ShadowHunterRoles grep -n '合并条件' -- docs          # => 2 行
```
```
README.md:88｜| `docs/架构/adr/0003-能力接口的合并条件.md` | `组件模型.md` §2 判据集「能力接口的合并条件」+ §5「能力接口合并」 |
组件模型.md:28｜| **能力接口的合并条件**：两条**同时成立**才合并 … |
```

接口清点（逐行读盘，全文行数口径，**含 javadoc 提及**）：`roleComponent/**` **12** 命中 = 真接口 **10**（`ComponentFactory:24` · `OperationProvider:42` · `EnergyCosting:80` · `RepaintSink:62` · `RenderCallback:269` · `TaskTrack:89` · `TaskCancelAll:95` · `TaskSink:35` · `Participant:71` · `Plan:47`）+ javadoc 里的旧接口名 **2**（`EnergyComponent:22` 的 `ChangeSink` · `SanTEComponent:29` 的 `Subscriber`，**均已删除**，仅存于"旧形态"说明里）；`core/**` **5** 命中 = `ChangeListenerSource:683` · `QuarantineHandler:779` · `ComponentLookup:38` · `RoleInfo:19` · `Self:12`。`implements` 全库 **33** 行（逐行清单见复核命令）。

**判定 = 合规**（无"实现者集合相同 **且** 互读状态"的未合并对）。

★ **登记一处接口冗余观察（不构成应合并）**：`ServiceComponents.TaskTrack`（`:89`）与 `TaskCancelAll`（`:95`）由**同一个实现者对象**提供（`RoleInstance:133` `componentRegistry::track` · `:134` `componentRegistry::cancelAll`）⇒ 条件①成立；但两者**各自读容器资源表**、**不互读对方状态** ⇒ 条件②不成立 ⇒ 按判据**不算应合并**。另 `TimerComponent.TaskSink`（`:35`）已是"双方法同族"形态，三者形态重叠 ⇒ 仅登记为**低危观察**。

**修复面建议**：无必须项；若要收口，`ServiceComponents.java` 一处（机械，1 处）。

### ② 单一冷却命名空间（冷却归组件实例自持）

**判据原文摘引**：「**冷却归组件实例自持** —— 每个实例一份 `cooldownUntilTick`（同 id 的两个实例各自独立），**框架不登记、不派发、不落表**；`startCooldown()` 的返回值 = 本实例是否真的进入了冷却」

**命令与实测**（doc §2 后的整段命令照跑）

```
ActiveComponent.java:43｜* 每实例一份 {@code cooldownUntilTick}（同 id 的两个实例**各自独立** ✓），框架既不登记、也不派发、
ActiveComponent.java:74｜private int cooldownUntilTick = 0;
ActiveComponent.java:130｜public int getCooldownTicks() {           # 声明值 = 唯一真值来源
ActiveComponent.java:156｜public boolean startCooldown() {
ActiveComponent.java:170｜public boolean startCooldown(int ticks) {
ActiveComponent.java:180｜public void stopCooldown() {
ActiveComponent.java:192｜public boolean isCoolingDown() {
ActiveComponent.java:197｜public int remainingCooldownTicks() {
```
```
[CooldownBearing|cooldownTable] 全库 => 0 命中
[Map<…cooldown…> 声明] 全库 => 0 命中（框架侧无冷却表）
[框架侧冷却表已删除] => 1 命中：RoleInstance.java:64（容器自陈）
```

**判定 = 合规**：冷却状态与判定全在 `ActiveComponent` 的实例字段（`:74`），框架侧既无表也无派发；容器侧只有"转问"读口。

**修复面建议**：无。

### ③ 角色信息归聚合根、不组件化

**判据原文摘引**：「显示名/描述/图标/阵营由角色模板持有；需要"必须存在"用**装配期校验**保证」；反例 = 新造 `RoleProfile` 组件。

**命令与实测**

```
[RoleInfo] 全库 => 32 命中（其中 27 处在 api/** 的 javadoc 提及；类型面 = core/ports/RoleInfo.java:19 + RoleInfoImpl + RoleInstance.roleInfo()）
[RoleProfile] 全库 => 0 命中（反例不存在 ✓）
[Role.java 的展示信息持有] => getIcon():287 · faction 字段:69/:366 · getDisplayName/getDescription（经 Builder）
[组件内 getDisplayName|getDescription|getIcon] => 17 命中 —— 全部是**组件的物品声明面**（读自身描述符 / 画物品时用自己的显示名），不是"角色级展示信息"
[组件内读阵营] => 1 命中：DefaultSanTEZeroPunishment.java:149｜Faction faction = svc().roleInfo().faction();   ← 经合法唯一入口
```

**判定 = 合规**（附口径说明）：角色级展示信息（显示名 / 描述 / 图标 / 阵营）在聚合根 `Role`；组件侧的 `getDisplayName()` 是**物品声明面**（数据源 = 描述符），两者不是同一关注点。

**修复面建议**：无。

### ④ 默认画法落组件基类

**判据原文摘引**：「`buildItem()` 在 `roleComponent/base/{Skill,MainWeapon}` 给默认实现，组件可覆写或 `super` 叠加；**描述符只声明**」

**命令与实测**

```
[base 内 buildItem] => 7 命中：Skill.java:151｜public ItemStack buildItem() {  ·  MainWeapon.java:130｜public ItemStack buildItem() {
                        （另 5 处为 javadoc 说明）
[全库 buildItem 定义/覆写] => 4 命中：ActiveComponent.java:95｜public abstract ItemStack buildItem();（抽象声明）
                        Skill.java:151 · MainWeapon.java:130（默认实现）· ExampleSelfRefreshingSkill.java:97｜public ItemStack buildItem(){（覆写示例 ✓）
[描述符是否产出物品] => RoleComponent.java:113 为 javadoc 说明「热键栏物品完全由组件的 buildItem() 控制」；**无 ItemStack 产出代码**
```

**判定 = 合规**：默认画法在组件基类、可覆写（有真实覆写者）、描述符只声明。

**修复面建议**：无。

### ⑤ 允许覆写识别键与状态文案

**判据原文摘引**：「组件可在 `buildItem()` 里自写识别键与状态文案」；反例 = 框架在最后一步强制覆盖。

**命令与实测**

```
[键写入（PersistentDataContainer.set）] => 2 命中：
    Skill.java:202｜meta.getPersistentDataContainer().set(Utils.SKILL_KEY, PersistentDataType.STRING, id);
    MainWeapon.java:180｜meta.getPersistentDataContainer().set(Utils.MAIN_WEAPON_KEY, PersistentDataType.STRING, id);
[键声明] => 2 命中：Skill.java:211｜public static final NamespacedKey SKILL_KEY = KeyFactory.Registry.of(
                     MainWeapon.java:189｜public static final NamespacedKey MAIN_WEAPON_KEY = KeyFactory.Registry.of(
[框架侧强制覆盖识别键]（RoleInstance + frameworkLevel/hotbar 两处扫） => 0 命中
```

**判定 = 合规**：键由**组件侧**在 `buildItem()` 里写；框架侧零覆盖点（读侧只读键，`KeyFactory:13-15` 记录"必须在类初始化时取值"的既有约束）。

**修复面建议**：无。

### ⑥ 第三个实例测试

**判据原文摘引**：「新增/改动扩展点时，必须有一个**第三个真实实例**证明扩展点可用（新组件 = 加一个文件，不改框架）」

**命令与实测**

```
git -C ShadowHunterRoles ls-files src/main/java/com/shadowHunterRolesPlugin/roleComponent/builtin
=> builtin/AutoRecoverEnergyPassive.java · AutoRecoverSanTEHealthPassive.java · DefaultSanTEZeroPunishment.java · ExampleSelfRefreshingSkill.java  （4 件）
[ExampleSelfRefreshingSkill] => 13 命中：RoleLoader.java:191（角色 selfUpdateExample 的说明）· :207｜.addComponent(ID_EXAMPLE_SELF_REFRESHING, new ExampleSelfRefreshingSkill.Specification().setSlot(0))
                                 · ExampleSelfRefreshingSkill.java:97｜public ItemStack buildItem(){（覆写画法）
                                 · Skill.java:143（记录该示例把 dependsOnLiveState 覆写回 false 的用法）
```

**判定 = 合规**：`selfUpdateExample`（第三个角色）＋ `ExampleSelfRefreshingSkill`（第三个实例）真实存在，且演示了两个扩展点（组件覆写 `buildItem()`、覆写"外观是否依赖活状态"）；`RoleLoader.java:207` 是"加一行注册 + 一个文件"的形态 ⇒ 新增组件不改框架。

**修复面建议**：无。

### ⑦ 组件一律经 `svc()`/容器自助取用

**判据原文摘引**：「组件需要的一切平台能力，经 `svc()`（`ComponentServices`）或容器查找（`svc().components()`）取用」；反例 = 组件持有聚合根引用做中转。

**命令与实测（逐件计数，全文行数口径）**

```
roleComponent/** 合计 svc(). = 72 命中 / 21 件
  2 ActiveComponent · 2 RoleComponent · 3 AutoRecoverEnergyPassive · 4 AutoRecoverSanTEHealthPassive · 6 DefaultSanTEZeroPunishment
  2 ExampleSelfRefreshingSkill · 6 MeiqiheziJuejueMainWeapon · 2 MeiqiheziEquipmentsPassive · 6 MeiqiheziBloodySlashSkill
  6 MeiqiheziCircleSlashSkill · 2 MeiqiheziUnconcernSkill · 4 RedBleedPassive · 4 RedDeeplySorrowSkill · 2 RedEquipmentsPassive
  4 RedEvilShockSkill · 3 RedSanctifiedBladeMainWeapon · 5 RedSolitaryArroganceSkill · 2 BuffComponent · 1 EnergyComponent
  5 HotbarRenderComponent · 1 VitalsComponent
[组件侧 componentRegistry()] => 0 命中
[组件侧 getByType(/getAllByType(] => 2 命中，**均为 javadoc**：HotbarRenderComponent.java:259 · VitalsComponent.java:67（代码 0 命中）
[组件侧 servicesOf(] => 0 命中
[组件侧 instance. 调用] => 5 命中，**均为注释/javadoc**（MeiqiheziJuejueMainWeapon:56 · RedDeeplySorrowSkill:21-23 · RedSanctifiedBladeMainWeapon:51 —— 记"旧形态 → 新形态"的说明）
```

**判定 = 合规**。★ 口径注记：`roleComponent/**` 内 **6 个"组件=True"的件零 `svc().`**（`MainWeapon` · `PassiveSkill` · `Skill` · `SanTEComponent` · `TimerComponent` · `HotbarSpecification`）—— 它们是**组件基类 / 描述符**（不取外部能力 ⇒ 不需要 `svc()`），**不是违规**；真正的"组件"（可实例化业务组件）**全部 ≥ 1 次** `svc().`。

**修复面建议**：无。

### ⑧ 受限写点（物品写入只有帧末 flush 一条路径）

**判据原文摘引**：「物品写入只有一条路径（帧末 flush）」；反例 = 组件直接 `player.getInventory().setItem(...)`。

**命令与实测**

```
[inv.setItem(] 全库 => 2 命中：roleComponent/HotbarItems.java:40（清空） · frameworkLevel/hotbar/HotbarRenderer.java:136（写入）
[.setItem(   ] 全库 => 2 命中（同上 ⇒ 无隐藏变体）
[setItemInMainHand] => 0 命中 · [setItemInHand] => 0 命中 · [.setItemInOffHand] => 0 命中
[requestRepaint 定义] => 1 命中：HotbarRenderComponent.java:225｜public void requestRepaint() {
[requestRepaint 调用者（代码）] => 2 命中：manager/BuffManager.java:114｜render.requestRepaint();   · builtin/ExampleSelfRefreshingSkill.java:79｜renderComponent.requestRepaint();
[requestRepaint 其余命中] => 7 处为 javadoc/注释（RoleInstance:108/:421 · ActiveComponent:110 · ExampleSelfRefreshingSkill:28 · HotbarRenderComponent:110/:153/:328）
```

**判定 = 合规**：写点唯一（2 处，且都在渲染组件持有的渲染器/清空路径上）；**置脏通道单条**（`requestRepaint()` 一个定义、两个调用者：框架侧 manager 1 + 组件侧 1，最终都落渲染组件自己的脏标记）。
★ 与 §1.2 的装备槽写口**不是同一口径**：那 16 处写的是**装备槽**（不产出热键栏物品），故**不破**本条；但它破 §1 第 2 条的字面口径 ⇒ 已单列（§1.2 待裁定）。

**修复面建议**：无（本条的例外在 §1.2 统一处理）。

---

## 3. 已知违规面的**独立复算**与架构判定（★ 不得转抄：以下均为本件实算）

**@HEAD `a25790a`（t30 编辑前）**

| # | 靶点 | 命中 | 逐字证据（HEAD 行号） |
|---|---|---|---|
| 1 | 三个平行类型视图 | **13 行** | 声明 `:70｜private final Map<String, MainWeapon> mainWeaponMap = new HashMap<>();` · `:71` skillMap · `:72` passiveMap；写入口 `:429｜if(component instanceof Skill skill) skillMap.put(componentId, skill);` · `:430` mainWeaponMap · `:431` passiveMap；读取点 `:466` `:474` `:484`（skillMap）· `:498` `:526` `:534` `:542`（mainWeaponMap） |
| 2 | 六个委派包装 + 私有转发 | **7 行** | `:464 castSkillLeftClick` · `:472 castSkillRightClick` · `:482 castSkillQDrop` · `:491 private boolean runComponentPipeline(CastTrigger trigger, Player caster)` · `:524/:532/:540 castMainWeapon{LeftClick,RightClick,QDrop}` |
| 3 | `getMainWeaponById` 类型逃逸口 | **1 行** | `:497｜public MainWeapon getMainWeaponById(String weaponId){` |
| 4 | 两个 pure-passthrough `damage` | **2 行** | `:568｜public void damage(double amount){` · `:571｜public void damage(double amount, Entity source){` |

**判定（架构依据）**

1. **三视图 = 确属违规** —— 框架替组件做**类型分桶**并维护**平行视图**（与 §2⑦ 反例面同族，也与 `t7` 刚清掉的"框架认识具体组件类"同族）。细化证据：`passiveMap` **纯死码**（全库只有声明 `:72` + 写入 `:431`，**零读取**）；`skillMap`/`mainWeaponMap` 的读取点**只做存在性检查**（`:466`/`:484`/`:526`/`:534`/`:542`），其中 `:474｜Skill skill = skillMap.get(skillId);` 是**死赋值**（取出的值连 `if` 都没进）⇒ 存在性判定本可"按 id 问容器"（`componentRegistry.getById(...)`）。
2. **六包装 + `runComponentPipeline` = 确属违规（薄转发）** —— 唯一语义 = 把触发枚举交给 `handleCast`；调用者（HEAD）：`SkillListener.java:62`（RIGHT_CLICK）· `:103`（LEFT_CLICK）· `:143`（DROP）· `MainWeaponListener.java:78`（LEFT_CLICK）· `:102`（RIGHT_CLICK）· `:137`（DROP）⇒ 改直达**不影响行为**（映射须逐条对照，勿错位）。
3. **`getMainWeaponById` = 确属违规（类型逃逸口）** —— 它把**具体组件类** `MainWeapon` 交给监听器（HEAD 调用点 = `MainWeaponListener.java:43` `:71` `:95` `:120` 四处 `MainWeapon weapon = instance.getMainWeaponById(weaponId);`）。
4. **两个 `damage` = 确属违规（纯转发口）** —— **全库零消费者**：唯一 `.damage(` 命中是 `DamageUtil.java:63｜victim.damage(amount);`（那是 Bukkit `Player#damage`，不是本口）；技能伤害走 `VitalsComponent.damage(Player,double,DamageKind)`（`MeiqiheziBloodySlashSkill.java:125`）⇒ 该口既无消费者、也不承担"视图"语义（纯转调 `player.damage`），应当删除。

★ **`t30` 在飞 ⇒ 读数可能已变**：工作区（本件取数时）`RoleInstance` = **1019 行**（HEAD 1083，`git diff --stat` = **1 插入 / 65 删除**），四靶点工作区命中 = **3 / 3 / 0 / 0** ⇒ 表 1/2/3/4 **正在或已被 `t30` 清除**；`isSkillReady|isMainWeaponReady` 在工作区仍 **4 命中**（按队长裁定**保留** ✓，且 `RoleApiSurfaceTest:33/:34` 锁的是 `RoleAPI` 的签名，与实现卡无关）。

---

## 4. 主动搜寻：**未被点名的**同类违规（四个方向）

### 4.1 组件持有 `RoleInstance`/`Role` 引用并做中转

| 模式 | 命中 | 结论 |
|---|---|---|
| `new RoleInstance` | 1（`SanTEComponent:167`，构造 `ChangeDelivery` 值载体） | **非违规**（不持有实例） |
| `RoleInstance` 作字段/局部/形参 | **0** | 合规 |
| `Role` 作类型 | 1（`ServiceComponents:58`，`Input` 的 record 组件） | **待裁定（低危）**，见 §1.1 |
| `instance.…` 调用 | 5（**全为注释**） | 合规 |

⇒ **本方向的新增违规 = 0**。

### 4.2 组件绕过 `svc()` 直接摸容器内部

| 模式 | 组件侧命中 | 结论 |
|---|---|---|
| `componentRegistry()` | **0** | 合规 |
| `getByType(` / `getAllByType(` | **2（均为 javadoc）**：`HotbarRenderComponent:259` · `VitalsComponent:67` | 合规（代码 0） |
| `servicesOf(` | **0** | 合规 |

⇒ **本方向的新增违规 = 0**（两处 javadoc 是"容器查取入口"的说明性提及，非调用）。

### 4.3 第二个物品写点 / 第二套渲染或置脏通道

| 模式 | 命中 | 结论 |
|---|---|---|
| `inv.setItem(` / `.setItem(` | 2 / 2（同一对：`HotbarItems:40` · `HotbarRenderer:136`） | **单写点成立** |
| `setItemInMainHand` / `setItemInHand` / `setItemInOffHand` | 0 / 0 / 0 | 合规 |
| `getInventory().set*` | **16**（两个装备被动的装备槽，见 §1.2） | ★ **待裁定** |
| `requestRepaint()` | 定义 1 + 调用者 2 | **单置脏通道成立** |

⇒ 本方向**新发现 = 1 项**（装备槽 16 处，落 §1.2 待裁定）。

### 4.4 框架侧按**具体组件类型**分桶 / 持有具体组件句柄

**命令与实测（框架侧 = `src/main/**` 去掉 `roleComponent/**`；全文行数口径，注释计入）**

```
[instanceof (Energy|SanTE|Vitals|Buff|Timer|HotbarRender)Component] => 0 命中
[具体组件类作类型（<类> <标识符> = / ; / )）] => 2 命中：
    manager/BuffManager.java:112｜HotbarRenderComponent render = instance.getByType(HotbarRenderComponent.class);
    command/DebugSchedCommand.java:342｜TimerComponent portTimer = portInstance != null ? portInstance.getByType(TimerComponent.class) : null;
[new (六类)Component（框架侧）] => 0 命中（构造集中在 roleComponent/frameworkLevel/ServiceComponents.build ✓）
```

**判定 = ★ 新发现 2 处同族违规（低危）**

1. **`BuffManager.java:112`** —— 框架侧 manager 用 `getByType(<具体组件类>)` 取到**渲染组件的具体句柄**，并在 `:114` 调 `requestRepaint()`。这与 `t7` 刚清掉的"框架认识具体组件类"**同族**（t7 清的是容器；manager 侧还有 1 处）。
   **修复建议**：改走 `ServiceComponents` 的取用面（框架级清单已具备"按 id → 服务入口"的形态）⇒ **机械，1 处，不跨包**（`manager/BuffManager.java`）。
   ★ 口径注记：`BuffManager` 不是组件（`manager/` 层），所以它**不违反** §1 第 1 条（组件不持有聚合根）；违反的是"框架侧按具体类型取用组件"这条**同族**纪律（`t6`/`t7` 的既定方向）。
2. **`DebugSchedCommand.java:342`** —— 诊断指令按具体类型取计时组件。
   **修复建议**：机械 1 处（同族走清单入口），**或**显式豁免（诊断工具自有其取数需求 ⇒ 建议在 `组件模型.md` 口径里写清"诊断面例外"）。

---

## 5. 其它发现（不属 8 条判据，但属"架构收口"面）

| # | 发现 | 命中 | 判定 | 建议 |
|---|---|---|---|---|
| 5.1 | `isSkillReady`（`:441`）与 `isMainWeaponReady`（`:453`）**两体逐字相同**（各 3 行：`getById` → `instanceof ActiveComponent` → `!isCoolingDown()`） | 2 个公开方法 | **代码重复（低危 smell）**，两者均为**活码**（`SkillListener:59/:101/:141` · `MainWeaponListener:76/:100/:133`） | 合并为 1 个私有实现 + 保留 2 个公开签名（机械，1 处；★ **不得改签名**：`RoleApiSurfaceTest:33/:34` 锁的是 `RoleAPI` 面，本处签名属实现面但已有既有调用者） |
| 5.2 | `ServiceComponents.Input` 持有 `Role` 却只读两个上限 | 1 处 | 待裁定（低危） | 传两个 `int`（机械，1 处）；见 §1.1 |
| 5.3 | `TaskTrack` / `TaskCancelAll` / `TaskSink` 三形态重叠 | 3 个接口 | 观察（不构成应合并，条件②不成立） | 可选收口；见 §2① |
| 5.4 | 测试面对本卡靶点的锁定面 | `RoleApiSurfaceTest:33/:34` 仅锁 `isSkillReady` 的**签名** | 合规（说明"删包装/删 map 不需要改测试"） | 无 |

---

## 6. 合规总表（10 条一览）

| # | 判据 | 判定 | 命中（全文行数口径） |
|---|---|---|---|
| §1-① | 不在组件里持有聚合根引用做中转 | **合规**（组件面 0）／待裁定 1（`ServiceComponents:58`） | 4 模式 = 1 / 0 / 1 / 1 |
| §1-② | 不从组件直接写背包 | **待裁定**（装备槽 16 处，口径缺口） | 16（+2 允许的句柄取用） |
| ① | 能力接口的合并条件 | **合规**（1 处冗余观察） | doc 2 · 接口 10+5 · implements 33 |
| ② | 单一冷却命名空间 | **合规** | 组件侧 14 · 框架侧 0 · 容器自陈 1 |
| ③ | 角色信息归聚合根不组件化 | **合规**（附口径说明） | RoleInfo 32 · RoleProfile 0 · 组件声明面 17 · 阵营经端口 1 |
| ④ | 默认画法落组件基类 | **合规** | base 7（定义 2）· 全库定义/覆写 4 |
| ⑤ | 允许覆写识别键与状态文案 | **合规** | 键写入 2 · 声明 2 · 框架覆盖 0 |
| ⑥ | 第三个实例测试 | **合规** | builtin 4 件 · 示例 13 命中 |
| ⑦ | 组件一律经 `svc()`/容器自助取用 | **合规** | `svc().` 72 / 21 件 · 违规 0 |
| ⑧ | 受限写点（帧末 flush 唯一） | **合规** | 写点 2 · 变体 0 · 置脏通道 1 定义 + 2 调用者 |

**新增发现（未被点名的同类违规）= 3 处**：§4.4 的 `BuffManager:112` · `DebugSchedCommand:342`（同族框架侧具体类型取用）· §1.2 的装备槽 16 处（口径缺口，待裁定）。

---

## 7. ★ 修复面优先级表（队长下一批立卡的输入）

| 优先级 | 违规项 | 影响文件 | 片型 | 预估处数 | 是否跨包 |
|---|---|---|---|---|---|
| P0（在飞） | 三个平行类型视图 | `core/RoleInstance.java` | **机械**（存在性判定改按 id 问容器） | 13 行（3 声明 + 3 写入 + 7 读取） | 否 —— **`t30` 在飞，本件取数时工作区已降到 3** |
| P0（在飞） | 六个委派包装 + `runComponentPipeline` | `core/RoleInstance.java` · `listener/SkillListener.java` · `listener/MainWeaponListener.java` | **机械**（触发枚举映射须逐条对照） | 7 定义 + 6 调用点 | 否 —— `t30` 在飞 |
| P0（在飞） | `getMainWeaponById` 类型逃逸口 | `core/RoleInstance.java` · `listener/MainWeaponListener.java` | **需人肉读上下文**（要判断每处是否需要专有面，逐条申报理由） | 1 定义 + 4 调用点 | 否 —— `t30` 在飞 |
| P0（在飞） | 两个 pure-passthrough `damage` | `core/RoleInstance.java` | **机械**（零消费者，直接删） | 2 | 否 —— `t30` 在飞（工作区已 0） |
| **P1（建议新立）** | **框架侧按具体类型取渲染组件** | `manager/BuffManager.java`（用 `ServiceComponents` 取用面） | **机械** | 1 | **否** |
| **P1（建议新立）** | **诊断面按具体类型取计时组件** | `command/DebugSchedCommand.java` | **机械**（或显式豁免并写进口径） | 1 | **否**（若选豁免 ⇒ 只改 `组件模型.md` 口径） |
| **P2（需先裁定口径）** | **组件侧装备槽写口（§1 第 2 条不做项的字面命中）** | `roleComponent/custom/meiqiHezi/passive/MeiqiheziEquipmentsPassive.java` · `roleComponent/custom/red/RedEquipmentsPassive.java` · （口径件）`docs/ai-generated/组件模型.md` | **需人肉读上下文**（先定"装备槽是否属允许面"，再决定是否收进组件侧单一入口） | 16 | ★ **是（跨包）** ⇒ 按卡面要求**须另立卡并说明原因**：它同时触及 **`roleComponent/**` 行为面收口**与 **`docs/` 口径**，且"穿戴装备"的语义与既有"已知代价"（手册 §6 Q6）需要人肉对齐 |
| P3（可选） | `isSkillReady` / `isMainWeaponReady` 重复实现 | `core/RoleInstance.java` | **机械** | 1（3 行 ×2 → 私有 3 行） | 否 |
| P3（可选） | `ServiceComponents.Input` 持有 `Role` | `roleComponent/frameworkLevel/ServiceComponents.java` | **机械**（传两个 `int`） | 1 | 否 |
| P3（观察） | `TaskTrack` / `TaskCancelAll` / `TaskSink` 形态重叠 | `roleComponent/frameworkLevel/ServiceComponents.java` · `TimerComponent.java` | **需人肉读上下文**（先确认判据②不成立才不动） | 1–3 | 否 |

---

## 8. 复核命令（照抄可复现本件任一条读数）

```powershell
$repo = (Resolve-Path 'ShadowHunterRoles').Path
$S    = "$repo\src\main\java\com\shadowHunterRolesPlugin"
$rc   = Get-ChildItem "$S\roleComponent" -Recurse -Filter *.java
$java = Get-ChildItem "$repo\src" -Recurse -Filter *.java

# ① 写点唯一性（期望 2 行，且只有这两个文件）
foreach ($p in @('inv\.setItem\(','\.setItem\(','setItemInMainHand','setItemInHand')) {
  $h=@(); foreach($f in $java){ $n=[IO.File]::ReadAllLines($f.FullName)
    for($i=0;$i -lt $n.Count;$i++){ if($n[$i] -cmatch $p){ $h+="$($f.Name):$($i+1)" } } }
  "[$p] => $($h.Count) : $($h -join ' ')" }

# ② 置脏通道（定义 1 + 调用者 2）
foreach($f in $java){ $n=[IO.File]::ReadAllLines($f.FullName)
  for($i=0;$i -lt $n.Count;$i++){ if($n[$i] -cmatch 'requestRepaint'){ "$($f.Name):$($i+1)|$($n[$i].Trim())" } } }

# ③ 组件侧对容器内部的越权（期望代码 0）
foreach($p in @('componentRegistry\(\)','getByType\(|getAllByType\(','servicesOf\(')){
  $h=@(); foreach($f in $rc){ $n=[IO.File]::ReadAllLines($f.FullName)
    for($i=0;$i -lt $n.Count;$i++){ if($n[$i] -cmatch $p){ $h+="$($f.Name):$($i+1)|$($n[$i].Trim())" } } }
  "[$p] => $($h.Count)"; $h }

# ④ 组件侧持有 RoleInstance/Role（期望：类型 0；仅两条边界命中）
foreach($p in @('RoleInstance\s+[a-zA-Z_]','new RoleInstance','(^|[^a-zA-Z])Role\s+[a-zA-Z_]')){
  $h=@(); foreach($f in $rc){ $n=[IO.File]::ReadAllLines($f.FullName)
    for($i=0;$i -lt $n.Count;$i++){ if($n[$i] -cmatch $p){ $h+="$($f.Name):$($i+1)|$($n[$i].Trim())" } } }
  "[$p] => $($h.Count)"; $h }

# ⑤ 组件直接写背包（期望 16 + 2）
$h=@(); foreach($f in $rc){ $n=[IO.File]::ReadAllLines($f.FullName)
  for($i=0;$i -lt $n.Count;$i++){ if($n[$i] -cmatch 'getInventory\(\)'){ $h+="$($f.Name):$($i+1)|$($n[$i].Trim())" } } }
$h.Count; $h

# ⑥ 框架侧按具体类型取用（期望 2）
$fw = $java | Where-Object { $_.FullName -notmatch '\\roleComponent\\' }
$h=@(); foreach($f in $fw){ $n=[IO.File]::ReadAllLines($f.FullName)
  for($i=0;$i -lt $n.Count;$i++){ if($n[$i] -cmatch '(Energy|SanTE|Vitals|Buff|Timer|HotbarRender)Component\s+[a-zA-Z_]+\s*[=;\)]'){ $h+="$($f.Name):$($i+1)|$($n[$i].Trim())" } } }
$h.Count; $h

# ⑦ §1 两条不做项 + 已知靶点（在飞对照用）
$ri = [IO.File]::ReadAllLines("$S\core\RoleInstance.java")
foreach($p in @('passiveMap|skillMap|mainWeaponMap','castSkill\w*\(|castMainWeapon\w*\(','getMainWeaponById','public void damage\(','isSkillReady|isMainWeaponReady')){
  $c=0; for($i=0;$i -lt $ri.Count;$i++){ if($ri[$i] -cmatch $p){ $c++ } }; "[$p] => $c" }

# ⑧ 只读纪律
git -C $repo status --porcelain          # 期望：只有本报告一个新路径（本件取数时另有 t30 在飞的 3 件）
```

> ★ 口径纪律：本件每条命中都按 **`-cmatch`（大小写敏感）+ `[IO.File]::ReadAllLines()` 逐行读盘**给出；**"命中行数"= 全文原始文本命中行数（含注释与 javadoc）**，凡代码 0 / 注释非 0 的情形已在文中逐条标注。
> ★ `debug-logs/**` 被 `.gitignore:47` + `:85` 覆盖 ⇒ 该目录**只能逐行读盘**（`git grep` 恒空）；本件未使用该目录作判据。
---

## 9. `RC3-F1` + 四条 low 落档（`t14` R-B 评审 · 本卡只落档与更正，不回退）

> **本节由 `t45` 追加**：把 `t14`（R-B 独立评审）报的 `RC3-F1`（medium）与四条 low 落成可检索记录。
> ★ **目标是落档与架构确认，不是回退** —— 冻结面未破（见 §9.3），`api/` 零改动，两个新增嵌套类型**保留**。
> ★ 本节行号取数锚点 = 工作区（累计态，`RoleInstance` **995 行**）；两态口径见 §9.5②。

### 9.1 `RC3-F1`：一处已落库纪律句被**局部删除**（未申报）

**事实（逐行读盘）**：`core/RoleInstance.java` 在 `t6`（`9c97a03`）里删掉了一处已落库纪律句。

| 项 | 值 |
|---|---|
| `d5fc8fa` 原文（该件 `:792`） | `* **凡关注点已是组件 ⇒ 不得再为它新增能力接口** （SanTE 的家就是 \`SanTEComponent\`）。` |
| 现值 | 该处**已无此句**（`RoleInstance` 内命中 = **0**） |
| 性质 | **局部删除** —— 不是成族清理 |

**① 全库仍存 `4 处`（逐行原文）**：

| # | 位置 | 逐行原文 | 形态 |
|---|---|---|---|
| 1 | `roleComponent/RoleComponent.java:90` | `    //  —— SanTE 的家是组件 ⇒ **不得再为它新增能力接口** ✗。` | 残句（后半） |
| 2 | `roleComponent/custom/red/RedDeeplySorrowSkill.java:53` | `     * **不得再为它新增能力接口** ✗）。` | 残句（后半） |
| 3 | `roleComponent/frameworkLevel/EnergyComponent.java:29` | ` * ★ **用 JDK 的 {@code Consumer}**（**不新增自定义接口** ✗ —— 「凡关注点已是组件 ⇒ 不得再为它新增能力接口」` | **完整谚语** |
| 4 | `roleComponent/frameworkLevel/SanTEComponent.java:35` | 同 #3（逐字相同） | **完整谚语** |

★ **口径注**：全库命中合计 **4 行**；其中**只有 2 行含逐字完整的谚语**（#3 `EnergyComponent:29` · #4 `SanTEComponent:35`），另 2 行（#1 #2）只是同一纪律的**后半截残句**。⇒ 「仍存 4 处」按**宽口径**（含残句）成立；引用时请写明是宽口径，否则严格口径只有 2 处。★ 另：**卡面写 `core/RoleComponent.java`，实际路径为 `roleComponent/RoleComponent.java`**（`core/` 下不存在该件）—— 一并更正。

**② 删除理由**：`t6` 结卡说明里**未申报该句被删的理由** ⇒ **如实记「未申报理由」**。
**判定：该句在当前架构下仍成立** —— 依据：它管的是「能力接口」（关注点已是组件则不得再为它造接口），而 `t6` 新增的两个类型**不是**某个关注点的能力接口，而是**框架↔提供者之间的投递协议**（见 §9.2②）。⇒ 纪律未被推翻，被删的是**该处对本条的具体引用**。

### 9.2 两个新增 public 嵌套类型：架构确认

| 类型 | 位置 | 签名 |
|---|---|---|
| `ChangeDelivery` | `core/RoleInstance.java:637` | `public record ChangeDelivery(RoleComponent owner, Runnable action)` |
| `ChangeListenerSource` | `core/RoleInstance.java:646` | `public interface ChangeListenerSource` |
| 其中唯一方法 | `core/RoleInstance.java:654` | `void forEachChangeListener(int previous, int current, Consumer<ChangeDelivery> delivery);` |

**① 为什么必须 public（跨包实现，有代码证据）**：实现方 `SanTEComponent` 在**另一个包** `roleComponent.frameworkLevel`（该件 `:49` = `public class SanTEComponent extends RoleComponent implements OperationProvider, RoleInstance.ChangeListenerSource`），其 `:158` 签名与 `:167` 构造 `new RoleInstance.ChangeDelivery(...)` 都要求这两个类型**跨包可见** ⇒ `public` 是**功能必需**，不是顺手放宽。
**② 是否与「不得再为它新增能力接口」冲突 —— 判定：不冲突，但要如实承认它扩大了公共面**：
- `ChangeListenerSource` 描述的是「**框架向提供者索要本轮条目**」这一**投递协议**，其接受集由**订阅名单**表达，而**不是**「某个关注点的能力」；纪律句管的是后者（反例 = `CooldownBearing` / 「能上热键栏」式接口，两者都已折进组件）。
- ★ **不声称它"不算接口"**：它**确实是**新增的 `public` 嵌套接口，且**确实**扩大了公共面 —— 事实见 §9.4 括注更正。
- **替代方案与代价**：若放到 `api/` 或顶层 ⇒ 直接冲击冻结面（`RoleApiSurfaceTest` 对 `RoleAPI.class.getDeclaredMethods()` 逐条比对，见该测试件 `:91`/`:107`）；若用「组件自持扇出 + 容器不参与」⇒ 破逐条 `guardedCall` 隔离语义；若用「容器强转并遍历组件内部名单」⇒ 正是 `t6`/`t7` 要消灭的点名与强转。⇒ **嵌套 + public 是代价最小的形态**（宿主已有 `RoleInstance`，不新增顶层类型文件）。
**③ 冻结面未破的实证（本卡现算）**：
- `api/` 与 `internal/api/` 的 `git status --porcelain` = **0 行**（零改动）；
- 快照 `FROZEN_SIGNATURES` 逻辑 = `RoleAPI.class.getDeclaredMethods()` 逐条相等 + **条目数相等** ⇒ `api/RoleAPI.java` 现算公开方法 **58** 条，与冻结快照 **58** 一致；
- 两个新类型是**嵌套在 `core/RoleInstance` 内**，不在 `RoleAPI` 的方法面上 ⇒ 不增 `getDeclaredMethods()` 计数。

### 9.3 本节判据（零代码改动，仅供复核）

闸门 **117 / 0 / 0** · 不变量 **1 / 1 / 10** · `inv.setItem(` = **2** · `notifyPlatform` = **3** · 快照 **58**。本卡 **`src/` 零改动**（只动本报告一个路径）。

### 9.4 卡面括注更正（★ 事实更正）

`t6` 的 A2 括注明写「**无需新增公共 API**」—— ★ **该括注与事实不符、不成立**：`t6` 实际**新增了 2 个 `public` 嵌套类型**（`ChangeDelivery` · `ChangeListenerSource`，见 §9.2）。
⇒ **事实以 §9.2 为准**；该括注**作废**。若要保留"未破冻结面"这层意思，正确写法是「**未改动 `api/` 公共面、未破快照 58**」，而**不是**「无需新增公共 API」。

### 9.5 附带三条 low 的口径更正

① **`RC3-F2`（取数域必须写明）**：「三处触须全 0 命中」的 **0** 是**`RoleInstance` 域内**口径 —— `markHotbarDirty` 在**跨件** `roleComponent/frameworkLevel/HotbarRenderComponent.java:36`（注释行，述及历史成员）**仍有 1 处**；全 `src` 域内该串命中合计 **1**，且**不在 `RoleInstance` 域内** ⇒ 两个数都对，**混写即误判**。
② **`RC3-F3`（两态不得混写）**：引用 `V-B` 读数须写明是哪一态 —— **卡提交态**：`t6` = `9c97a03` 的 `RoleInstance` **1130 行** · `t7` = `3147239` 的 **1083 行**；**累计态**（HEAD 工作区）= **995 行**。两态结论一致（清零面全 0），但**行号/行数不同**，不得混引用。
③ **`RC3-F4` / `RC3-F5`（两处口径更正）**：
   - `RC3-F4`：`java.util.function.Function` **不是**「改动前就已未用」—— `d5fc8fa` 里它是**活引用**（`:49` import + `:411` `pickValue(String, Class<T>, Function<T, R>, R)` 的参数类型），是**随 `pickValue` 一并删除才失效**的。★ 原结卡的「改动前就已未用」对 `frameworkLevel.Buff` / `PotionEffectType` 成立，**对 `Function` 不成立**。
   - `RC3-F5`：`t7` 卡面「现算 **14** 行」与实测 **18 行**未同步 ⇒ **卡面须标注已由报告 §2.2 更正**（`§2.2` 现标题已带「★ 编辑前读数 / 编辑后现值 0 ⇒ 见 §8.1」）。
