================================================================================
协同规范 · **起服 / 取证 / 停机**（团队级可复用；本文件本身即"物证「协同规范」小节"的落点）
—— 立规来源：2026-09-18 的两起真实事故 + 三条仪器陷阱（均已入台账）
—— 适用：任何需要独占 `runServer`（25566）做 Mode B / 探针取证的成员
================================================================================

§0 一句话
--------------------------------------------------------------------------------
**端口空闲 ≠ 对方取证窗口已结束。** 起服方必须**等停机方明确广播"已释放（物证已落）"**；
停机方必须**在服务器仍在线时就冻结日志**（`Done` + 空闲点 → `Copy-Item` → **然后才** `job_kill`）。
两条一起，才消除"停机与复制之间的竞态窗口" + "后继起服覆盖他人证据"这两个缺口。

§1 起服方（谁要起服，谁执行）
--------------------------------------------------------------------------------
[1.1] **起服前广播**：说明预计占用时长、证据用**新文件名**、并声明"**你若正在取证请回我一句，我等你**"。
[1.2] **窗口三路核查**（缺一不可）：
      ① `TcpClient.Connect('127.0.0.1',25566)` 应**失败**；
      ② `netstat -ano | Select-String ":25566"` 应**无 LISTENING**（仅 `TIME_WAIT` 残影可接受）；
      ③ `Get-Process java` 的**进程数与启动时间**应看不出"最近起过一个服务器"（例如只剩 19:01 的 Gradle daemon）。
[1.3] **不得把"端口空闲"当作放行** ✗：**等停机方的"已释放（物证已落）"广播** ✓。
      （依据：`026915a` 记录的事故 —— 停机方 20:18:0x 停服、我 20:18:05 起服，`runServer` **立刻重写 `run/logs/latest.log`**，
        使对方"停机后复制日志"的取证窗口被覆盖 ⇒ 那一轮集合差结论**只能作废并如实声明**。）

§2 停机方（谁停服，谁执行；**顺序不可颠倒**）
--------------------------------------------------------------------------------
[2.1] **先冻结，后停机（"双点冻结"）** ✓ —— 由 `eng-验证` 精化、`eng-平台` 采纳（2026-09-18）：
      ① **主冻结（权威、抗并发）**：在 Mode B 的 `Done` + 空闲点、**服务器仍在线时**执行
         `Copy-Item run\logs\latest.log → debug-logs\测试记录\<本批名>.log`，并**当场**记录 `长度 / mtime / SHA256`。
         ⇒ 此后任何后继起服都**污染不到它** ✓（本团队三代证据实测有效）。
      ② **补冻结（尽力而为，不得喧宾夺主）**：停机后**再尝试**复制一份，用于捕获"空闲点 → 停机之间"可能出现的行
         （例如延迟初始化后抛出的异常）。**若该副本已被后来的起服覆盖 ⇒ 一律以主冻结为准**，并在物证里
         **如实标注"补冻结失败"** ✗ —— **绝不**把补冻结当必需，也**绝不**用补冻结去覆盖主冻结 ✓。
      ③ **记录要求（可自证）**：物证里必须写明**主冻结的时刻 + 该冻结件的身份**，并附一句**可核事实**：
         **"冻结件早于停机/端口释放"**（例："在服务器仍在线时冻结，随后才 `job_kill`"）✓ ——
         读者据此可自行验证顺序，不会再像 20:18 那起事故一样把"端口已空闲"误读为"对方取证已完成" ✗。
[2.2] **只 `job_kill` + 等待** ✓：不对 java 进程动手；等作业终结。
[2.3] **停服三路互证**（判据）：
      ① `TcpClient.Connect` **失败**；② `[System.IO.File]::Open('run\logs\latest.log','Open','Read','None')` **成功**（无持有者）；
      ③ `netstat` 中 `25566` **无任何条目**（连 TIME_WAIT 都应清空）；④ `Get-Process java` 只剩常驻 daemon。
[2.4] **广播"已释放（物证已落）"** ✓ —— 这一句才是放行信号 ✓。

§3 取证与制品身份（与 §2 配套）
--------------------------------------------------------------------------------
[3.1] **证据必须晚于其制品**（F1）：`jar mtime < log mtime < 物证 mtime`，且**物证内写明本代 jar 身份**（自证绑定，不止靠 mtime）。
[3.2] **jar 的 SHA256 不是"同一份代码"的稳定指纹** ✗：`jar` 记录的 `.class` 时间戳与 `LineNumberTable` 行号会随**重编译**变化
      （实测：同一平台层代码出现 `A74A5747…` / `CD4FD3B2…` / `CDAA4F63…` 三份不同 SHA，而 `javap -c -p` 反汇编 **79/79 行全等**）。
      ⇒ 判"同代码"用**反汇编**或**源码非注释内容**；判"制品身份"才用 jar SHA + mtime 自证链。
[3.3] **改注释的零行为变化**可用两条机械证明（本团队已用过三次）：
      ① 剥块注释 + 行注释 → 逐行 `Trim()` → 丢空行 → 与 HEAD 版**整串逐字比较** = True（**非注释行改动 = 0**）；
      ② 受控 javac A/B：同一编译器编译新旧源码 ⇒ `.class` 的 `javap -c -p` 反汇编**相同**（差异只允许出现在 `LineNumberTable`）。
[3.4] **★ 重建的时机纪律（由 `eng-验证` 提出、我采纳）** ✗：**"删除 jar + 重建"只允许发生在一代证据【尚未取证】之前** ✓；
      **早已冻结证据的代次上绝不可再跑重建** ✗ —— 重建会改该 jar 的 **mtime** ⇒ 把 F1 链 `jar 20:58:55 < log 20:59:50 < 说明` 变成
      "**新 jar mtime > log mtime**"的自相矛盾 ✗（字节可能相同，链却断了 ✗）。
      若确要验证"重建后逐字节相同" ✓ ⇒ **在 `build/` 的副本上做删除+重建比对**（或先复制 jar 到临时目录），
      **工作树制品零接触** ✓ —— 既不破坏 F1 链、又能得到 `identical=True` 的证据 ✓。
      （我早前的 t53/t59 重建之所以合法，是因为那时**该代证据尚未冻结** ✓；顺序永远是"**先定源 → 重建 → 起服 → 停机前冻结 → 归档**" ✓。）

§4 事后补记（post-hoc）的硬约束
--------------------------------------------------------------------------------
若原始日志已被覆盖/不可再取，必须在**文件名与文首双标注**："**事后补记（post-hoc）、内容为当轮逐字转抄、非可二次读取的原始物证**" ✗
—— **不得**冒充原始物证 ✓（本轮我有一条会话 1 的控制台堆栈就是按此标注的 ✓）。

§5 仪器口径（三连 + 第 4/5/6/7/8/9/10 族）
--------------------------------------------------------------------------------
| # | 陷阱 | 纪律 |
|---|---|---|
| 1 | `Get-NetTCPConnection -State Listen` **在服务器确在监听时也可能返回 0** ✗ | 停服判定用 **TCP connect + `latest.log` 独占打开 + netstat** 三路互证 ✓。**⚠ 追加（2026-09-18 实测）**：**独占打开本身是"互斥探针"** ✗ ⇒ **两名验证者同时做三路**时，会各自看到对方的持有句柄、**双双报出"有人在跑"的假警报** ✗（实测：`eng-验证` 报 `独占打开=False`，我在同一分钟测得 `TCP 失败 + netstat 空 + 独占打开=成功` ✓）⇒ 纪律：**三路互证要么串行做** ✓，要么只读探测改用**共享读**（`FileShare.ReadWrite`）✓，且**结论以"最后一次完整三路"为准** ✓。**⚠ 另一面（`eng-验证` 补、我采纳）**：**`netstat` 的"行数 > 0" ≠ 有人监听** ✗ —— 实测 4 行**全是 `TIME_WAIT`** 客户端残影 ✓（此时 TCP connect = 失败、日志可独占打开 ⇒ 实为空闲 ✓）⇒ **禁用计数式判据**：`netstat` 必须**按 `LISTENING` 状态过滤** ✓（或直接以 TCP connect + 日志打开为准 ✓） |
| 2 | 行数口径：按 `\r?\n` 切分会**多算文件尾 1 个空元素** ✗ | 行数 = **换行符计数**（`read` 工具 / `Get-Content` 一致）✓ |
| 3 | `Get-Content … \| Select-Object -Skip N` 对含 CJK 的 UTF-8 无 BOM 文件会**静默行号漂移**（取到别的方法体却不报错 = 假数据）✗ | 行号定位一律用 **`git grep -n`（可带 `-A`）** 或 **`read` 工具** ✓ |
| 4 | **git 路径引用**：`git ls-files` 默认对非 ASCII 路径输出**外层双引号 + 八进制转义** ⇒ 直接喂 `Test-Path` 报 `Illegal characters in path` ✗ | 非 ASCII 路径一律 `-LiteralPath` ✓；或 `git -c core.quotepath=false ls-files` / `git ls-files -z` ✓ |
| 5 | **工具不存在 ⇒ 空输出比较 = 假 `True`** ✗（`javap` 不在 `PATH`、`JAVA_HOME` 未设时，两侧都得到空串 ⇒ 任何"逐字相同"判定都会**假通过**） | 任何"两侧比较"必须**先断言工具可用且输出非空** ✓（例：`Get-Command javap` + `$a.Count -gt 0 -and $b.Count -gt 0` 再比较）；比较类结论**必须打印两侧行数** ✗ —— 由 `eng-验证` 在独立复现 class 级等价时实测踩到并**自曝作废重做** ✓ |
| 6 | **格式导致误读**：登记册里 `key =` 与**值分两行**时，按"`key = value` 同行"解析会**把上一条的值配给本条** ✗（实测于 `回滚快照/jars/MANIFEST.txt` 第二条登记） | 机器可解析的登记一律 **一行一值** ✓；人读的续行块必须显式标注"续行" ✗（该件已规范化为一行一值 ✓） |
| 7 | **管道截断**：对以**原生进程**为生产者的管道用 `Select-Object -First N` ⇒ 提前关闭上游 ⇒ 生产者可能**被终止**并报出**伪非零退出 / 伪 stderr** ✗（实测：我据此误报过脚本 `exit=1` 并白解释一轮；**纯 cmdlet 管道未复现** ⇒ 仅按"原生进程生产者"适用 ✗） | 取样**先落变量再筛** 或用 `-Last N` ✓；判读前**先断言输出非空**（同第 5 族）✓ —— 由 `eng-验证` 建议入册 ✓ |
| 8 | **`git grep` 默认不搜【未跟踪】文件** ✗ ⇒ 任何"旧表述 grep = 0"的判据，在新**未跟踪**文件上会**真空通过** ✗（实测：探针文件含 `Folia-ready` ⇒ 普通 `git grep` = **0**、`--untracked` = **1**）。**更隐蔽的下游后果 = 假进展** ✗：把代码**搬进新文件**后普通 grep 计数会**下降**、**看起来像"已清理"** —— 2026-09-18 **真实发生** ✓（探针被迁入未跟踪的 `DebugSchedCommand.java`）。**★ 活数据（`eng-验证` @21:2x 现算）**：`handleDebugSched` 普通 **0** / `--untracked` **1** · `[sched]` 普通 **0** / **27** · `threadedregions.scheduler` 普通 **2** / **4** —— 而其 21:06 基线（普通口径）为 **5 / 26 / 3 / 6** ⇒ **普通口径"从 5/26 掉到 0/0"并非清理进展，而是整体搬迁** ✗✓ | 判据一律用 **`git grep --untracked`** ✓（或先断言 `git ls-files --others --exclude-standard` 为空 ✗）；`platform-wording-check.ps1` 已据此加固并**用探针当场复现** `[MISMATCH] expected 0 / actual 1` ✓。**追加两条（由 `eng-验证` 补入）**：① 任一"归零"判据**必须与未跟踪计数联合读** ✗ —— 判据**前后各打印一次** `git ls-files --others --exclude-standard -- <scope>` ✓；② 清理类判据另要求 **`git status` 的 `??` 计数归零** ✓（**"搬迁"与"删除"在普通 grep 下不可区分** ✗）。**③ 判据表不得"跨时刻拼接"** ✗：一张表里的每个数字都**必须带自己的测量时刻** ✓ —— 实测：我 21:25:31 的四项读数（`[sched]` = 0）与 21:27 的（= 27）**差异源于探针文件 21:25:57 才落盘** ✗，而表里未标时刻 ⇒ 被读成"同一时刻的结论" ✗（`eng-验证` 据此校正了我 ✓）。**④ 白名单过滤必须作用在【行】上、且模式必须锚定"使用点"** ✗（`eng-验证` 发现、我修复）：① **不能把白名单当 pathspec 传** ✗（`git pathspec` 不支持 `()` 分组 ⇒ 会得到无意义值 ✓）；② **裸词模式太宽** ✗ —— `sched` 会匹配生产 API 名（`scheduler` / `scheduler()`）⇒ 实测白名单外 **22 条全是合法 API 名** ✗ ⇒ 改用**锚串** `matrixAllPairsMatch=|portLegEquivalent=|\[sched\]` ⇒ 白名单外 = **0** ✓（我的工装已按此修好并试跑验证 ✓）。**⑤ 忽略树（`debug-logs/**`）里的文件可以"既被忽略、又被跟踪"** ✗（`-f` 强加的情形 ✓）⇒ 三条命令各司其职：① 判"**落在忽略规则下**"用 **`git check-ignore -v --no-index <path>`** ✓（**`--no-index` 不可省** ✗ —— 我实测：**不带它时对【tracked】文件静默返回空** ⇒ 会被误读成"未被忽略" ✗，而带它则命中 `.gitignore:84:debug-logs/**` ✓）；② 判"**已被 `-f` 纳入**"用 **`git ls-files --error-unmatch <path>`** ✓（exit 0 = 已纳入 ✓）—— **两者可同真** ✓（实测同一文件：`--no-index` 命中规则 ✓ **且** `ls-files` = tracked ✓）；③ "**还差 `-f`**"的清单 = **`git ls-files --others --ignored --exclude-standard -- <subtree>`** ✓（实测未跟踪探针 = 1 ✓）。**禁用 `git ls-files --others --exclude-standard` 作该子树的"清零/干净"判据** ✗（实测恒 **0** ⇒ **真空判据** ✗）。（由 `eng-取证` 发现、队长授权补入 ✓；`--no-index` 与 ③ 由我补测后补正 ✓）。**⑥ 忽略子树（`debug-logs/**`）的内容检索** ✗（队长授权补入 ✓）—— **在此子树上加 `--untracked` 会把【已跟踪】文件也一并排除** ✗ ⇒ **`git grep --untracked` 谎报"没有"** ✗。实测（同一关键字 `t63-smoke`，同一子树）：**默认 = 2 个文件 ✓ / `--untracked` = 0 ✗ / `--untracked --no-exclude-standard` = 2 ✓**；且被找到的样本两件**均为 tracked** ✓（`debug-logs` 子树在 HEAD 下共 **455** 件 ✓）**且仍命中 `.gitignore:84`** ✓ ⇒ **现象比"尚未 `-f` 的窗口"更广** ✗（我最初把它解释成"窗口内未跟踪文件" ✗，**实测后更正** ✓）。`eng-验证B` 另测 **0 → 8** ✓。**而该窗口/该子树恰是验证者通常读数的时刻** ✓ ⇒ 可能漏掉**正在产出的权威证据** ✗。⇒ **正确形态**：内容检索一律 **`git grep --untracked --no-exclude-standard`** ✓（它同时覆盖已跟踪与被忽略-未跟踪两类 ✓）；枚举窗口文件用 **`git ls-files --others --ignored --exclude-standard -- <path>`** ✓（**注意**：`--ignored` **必须**配 `--exclude-standard` 或 `--exclude=<pattern>` ✗ —— 我实测不带时直接报 `fatal: ls-files --ignored needs some exclude pattern` ✗）；归属用 `git check-ignore --no-index -v` ✓；追踪态用 `git ls-files --error-unmatch` ✓（两者可同真 ✓）。**反例（我的自曝）**：我曾据不带 `--ignored` 的枚举得出"整批已 `-f` 入库" ✗ —— **假结论** ✓ |
| 9 | **.NET 文件 API 的相对路径按【进程 CWD】解析**，**不随 PowerShell 当前目录变化** ✗ ⇒ `ReadAllLines('相对路径')` 抛 `DirectoryNotFoundException` ⇒ 变量为空 ⇒ 后续判据全报 `0 / False`（**假结论** ✗）。实测证据：我在 `ShadowHunterRoles` 下运行时，报错路径却落在**上一级工作区根** ✓ ⇒ 证实是进程 CWD，不是 shell 位置 | **文件 API 一律用绝对路径**（`(Resolve-Path x).Path`）或 **`-LiteralPath`** ✓；并与**族 5 合用**（"先断言输入非空"）✓ —— 由 `eng-验证` 提出（其 t15/t49 亦踩过）✓ |
| 10 | **断言/模式自身的可靠性** ✗ —— 两类实测：① **非 ASCII 字面量在命令行走廊里可能被转码** ⇒ `Contains("§7.4")` 返回 **False**，而文件里确实含有该串（同一处改用 ASCII 锚 `[7.4]` ⇒ **True** ✓）；② **断言串写错**（我少写一个反引号 ⇒ 误报"编辑未落地" ✗） | 断言/模式**优先用 ASCII 锚**（`[7.4]`、`??`、`Matrix` 等）或**码点**构造 ✓；**先确认该断言在"已知为真"的样本上会通过**（同族 5 的精神 ✓）；**多口径交叉**（`Contains` / `[regex]::IsMatch` / 逐行 `Select-String`）✓ —— 由我在 2026-09-18 一小时内两次踩到并自曝 ✓ |

§6 立规依据（可追溯）
--------------------------------------------------------------------------------
- `026915a` —— 停机后 3 秒被并发起服覆盖日志，该轮集合差结论作废并如实声明（**§1.3 + §2.1** 的来源）。
- 2026-09-18 20:14:42–20:17:46 —— 25566 被他人占用，我 **等到其释放后才起服**（§1.2 三路核查的正向应用 ✓）。
- `ff76058` —— 台账已把"证据必须晚于其 jar 且自证该 jar 身份"写成可复用规则（**§3.1**）。
- `50171e5` —— 覆盖等价改按 **class 级**判定而非 jar 哈希（**§3.2** 的团队结论）。
- 仪器陷阱 1–3 由多位成员实测并分别入档；第 4 族（路径引用）由 `eng-会话` 触发、我复现并给出修法（**§5**）。
- 第 5 族（工具缺失 ⇒ 假 True）由 `eng-验证` 独立复现 class 级等价时踩到并自曝 ✓；第 6 族（格式导致误读）由我审计 `148297` 归属时定位 ✓。

§7 使用方式
--------------------------------------------------------------------------------
后续任何卡片的**物证**只需引用本文件：`debug-logs/测试记录/协同规范-起服停机取证.txt`（并把本卡实际执行到的行（§1.2/§2.1/§2.3/§2.4）逐条打勾说明 ✓）。
[7.1] **引用纪律（引用证据/制品/规范时必须三件齐）** ✓：**① 权威路径**（例：`回滚快照/jars/<规范命名>.jar` —— **不要**引用已撤的临时副本路径 ✗）·
      **② 引用当时的 rev/身份**（本规范自身也要带 rev：`≤本次提交后的 SHA256`；**活文档的引用方式另见 §7.3** ✓）；**③ "按测量时刻读"** ✓ ——
      报文/消息里的历史引文**只是那一刻的读数**，不代表当前值 ✗（已四次出现"引了更新前那一版"的错位 ✓）。
      ⚠ **同一族的另一种错法** ✗：把**两个不同时刻的读数**并列，当成"同一文件的先后序列"（例：把 `21:14:54` 引用的版本与 `21:19` 实测的版本比 ⇒ 误判"曾增后减" ✗ —— `eng-验证` 已实测并**撤回** ✓）⇒ 比"序列"前先确认**两次读数是否同一时刻口径** ✓。
      ⚠ **不要引"条数/族数/行号"** ✗（活文档里它们是移动靶 ✓）⇒ 引 **§章节 + 条目标题**（例："§5 族 8（`git grep` 不搜未跟踪）" ✓）或 "§5 @时刻" ✓ —— 由 `eng-验证` 提出、我采纳 ✓。
[7.4] **产物换代 ⇒ "待提交清单"与"引文"同时失效** ✓（`eng-验证` 提出、我采纳）：任一次 **jar 换代**（例：`DBE04077…` → `53CBEB65…`）
      ⇒ 上一封报文里的**制品身份**、**待提交清单**、**基线**都可能同时过期 ✗ ⇒ **每封都按 §7.1 + §7.2 现算一次** ✓（否则会按过期身份执行 ✗）。
[7.2] **引用前先现算一次（`eng-验证` 的实践，我采纳）** ✓：对要引用的**路径**做 `Test-Path`、对**跟踪态**做 `git ls-files --error-unmatch`，
      **再**引用 ✓ —— "引身份/路径"与"按测量时刻读"这两条必须**一起**用才不互相拆台 ✗（否则会按一份**已撤或未跟踪**的路径去执行 ✗）。
[7.3] **★ 活文档 vs 冻结件：引用方式分两类** ✓（由 `eng-验证` 提出、我采纳）：
      · **冻结件**（证据文件 · `回滚快照/jars/` 留档 jar · 已交付且不再改的脚本）⇒ 引 **文件名 + 完整 SHA256** ✓（这类文件**不得再被改动** ✗）；
      · **活文档**（**本规范** · 验证报告 · 进度交接 —— 仍在被持续扩写 ✗）⇒ 引 **文件名 + §章节 + 测量时刻**（例："本规范 §5 族 5，@21:19 现算"）✓，
        **不要**引字节数 / 行号 / 身份 ✗ —— 实测：本件曾 **8820 → 10608 → 11638 B 三测三值** ✗、`阶段5-调度器迁移-验证报告.md` 的行号亦从 `:161` 漂到 `:167` ✗；
      · 若确需对活文档做**身份级**引用 ⇒ **不必手写"不动的记录"** ✗ —— **提交本身就是冻结点** ✓（`eng-验证` 提出、我采纳）：
        引「**`git rev-parse <commit>:<path>` 的 blob 身份** + §章节」（例：`193a5e4:debug-logs/测试记录/协同规范…` = `9df4497eaa28…`）✓ ⇒ 该身份**不因后续扩写而失效** ✓ 且**无需人工登记** ✓。
        ⚠ 三条配套口径（我实测校准，勿照搬断言 ✗）：① **blob 身份是"提交范围"的** ⇒ 引它必须带 **commit**（换一个提交就是另一个 blob ✓，例：同一份规范在 `f7cd2725` 与 `193a5e4` 的 blob 不同 ✓）；
        ② **blob 与工作树的哈希是否同口径，必须先测** ✗ —— 实测本仓库 `core.autocrlf=true`，但**我经手写入的文件是 LF** ⇒ blob 与工作树**字节等同** ✓（规范 15217 B/102 行、脚本 7520 B/134 行、MANIFEST 3076 B/34 行，三件 `字节等同=True` ✓）；
        只有当文件被 git **重新物化**（如 `git checkout` 之后）才会变 CRLF、两口径才分歧 ✓ ⇒ **先测再断言** ✓；
        ③ **别用 `git cat-file -p … | Measure-Object -Line` 数行** ✗（实测给出 **94** 行而真值 **102** 行 ⇒ 管道口径不可靠 ✗）⇒ 一律 **blob 落盘后用 `ReadAllLines` / 字节数** 计 ✓（与族 2 同族 ✓）。
================================================================================
