# 阶段 0–1 独立验证报告（t7）

> **执行者**：eng-验证（verifier）｜**任务**：t7｜**attempt**：1｜**执行时刻**：2026-09-16 23:10–23:20（+08:00）
> **结论**：**verdict = pass**（无 blocker / high / medium；4 条 low，其中 1 条为**必须挂账**的档位说明）
> **本文所有数字均我自己复算**，不引用实现者的结论；每项附命令原文 + 关键输出 + 文件:行号 或 日志行。

---

## 0. 身份锚定（按 §6.6：仪器身份必须钉死、引用须附内容片段）

| 对象 | 身份（本次复核时现算） |
|---|---|
| 探针仪器 `t6-quit-persistence.js` | **22223 B / mtime 2026-09-16 22:54:17.046 / SHA256 `EFDBF9EB95949FAD73A63DAC9F3216111BFF87FCAA8BD0B9E48617C1A84C9770`**（含 F-3 修复版） |
| 受管副本 | `docs\回滚快照\工装\t6-quit-persistence-EFDBF9EB-22223.js` —— **逐字节一致** ✓ |
| 仪器基础自检 | `check-instruments.ps1`（4166 B / `CAB0BDD6…`）→ **exit 0, failures = 0**（7 件工装 ASCII-only + 已归档） |
| Mode B 锚点日志 | `docs\阶段1-起服锚点-t2ModeB.log` = 6199 B / `2A4BD077852D213901506205E15AC64C2F5D6A27C965D59F922D31FEE5ADB524` |
| 我本次起服日志 | `.scratch\bot\t7-runserver-20260916-231457.log` = 7467 B / `88A7B7D5707CCB34FD85003CB5B48CB3E9292044AF89B3624860CE2CB7D0B515` |
| 我本次探针 JSON | `.scratch\bot\verify-run\t6-quit-persistence.result.json` = 6913 B / `42B455F33369A486F792323B24919BA244661BC8C2D7C3AE2E069F2B9459F0A7` |
| 我本次构建产物 | `build\libs\ShadowHunterRolesPlugin-1.0.0.jar` = 99659 B / mtime 23:11:26.395 / `F94CDE7128F5A5F067F4286B89FF09CB9023EFCB7C2DD6F8892F9DA34C151908` |
| 被验小结 | `docs\阶段1-交付小结.md`（t5）、`docs\阶段1.8-交付小结.md` = 19925 B / `AAD0CDDD60310A1DACD8204A9B146E4BE142C2D9F198DFAA3E929CB567898048`（t6） |

**仪器绑定（t7 唯一一次正式复检）**：
```
JSON 自带：bytes=22223 mtime=2026-09-16T14:54:17.047Z sha256=EFDBF9EB…C9770
磁盘现算：bytes=22223 mtime=2026-09-16T14:54:17.046Z sha256=EFDBF9EB…C9770
        → 字节一致=True  哈希一致=True；probedAt(14:54:31Z) 晚于脚本 mtime ✓
```
→ **结果与仪器版本相符，采用**。

### 0.1 仪器身份变更史与 **F-3** 时间线（应队长要求补记；本节的教训是后续阶段的准入样板）

> 本节记录一次**真实的假阳性缺陷**及其被发现/修复的完整链路。它不是"过程叙事"，而是**为什么"结构核验通过"不等于"断言可信"**的实证。

**时间线（全部时刻可现算复核）**

| 时刻 | 事件 | 身份 |
|---|---|---|
| 21:58–22:18 | 探针迭代 v1→v8（脚本身份逐版变更） | `9235 → 11971 → 12185 → 12881 → 15621 → 15962 → (18236 仅被消息引述、磁盘未观测到) → 20981` |
| 22:18:03 | v8 落盘；**我对其做正式复检并 PASS**（F-1/F-2 关闭；`--selftest` 12/12、exit 0） | `20981 B / 5E7CABED…` |
| 22:54:17 | **v9 落盘**：以 F-3 修复 + 自检扩容（12→14 项） | `22223 B / EFDBF9EB…` |
| 22:54:31 | t6 用 v9 跑探针 → `result.json` | 自带 `scriptIdentity = EFDBF9EB…` |
| 23:0x | **我在 t7 入场前现算仪器身份**，发现磁盘 ≠ 我 PASS 过的版本 → 判定"我的 PASS 已被取代"，并要求按新规则改绑 | 同上 |
| 23:1x（t7） | 我对 v9 重新复核：`node --check` exit 0、`--selftest` **14/14 true**、与 v8 差异 **12 行且全部限于 F-3**、其余接线未回退 | 同上 |

**F-3 是什么（缺陷本体）**
```
旧谓词（v8 及更早）：hasRoleHealthModifier = modifiers.some(m => String(m.key || m.name || '').includes('role_health_modifier'))
实测报文（mineflayer）：{"uuid":"shadowhunterrolesplugin:role_health_modifier","amount":20,"operation":0}
                        ↑ 标识在 `uuid` 字段 —— 旧谓词恒为 false
后果：C_persistence_modifierGone = tri(maxHealthAttr !== null && !hasRoleHealthModifier, …)
      → 无论修饰符是否残留都判 true ⇒ I-16 的"持久化"半边**无条件通过**
新谓词（v9）：String(m.uuid || m.key || m.name || '')
```

**排练是如何抓到的**：eng-会话 在**真实起服 + 机器人掉线**的排练里，把客户端 `update_attributes` 的**原始报文 JSON** 打出来比对（上面那行就是实证），发现代码假设的字段名（`key`/`name`）与报文实际字段（`uuid`）不一致 → 恒假谓词暴露。

**为什么我的结构性核验漏过（必须写清的区分）**：我核 F-1/F-2 时验的是**结构** —— 三态分支是否可达、证据强度是否进入判定路径、退出码语义、接线是否死代码 —— 这些**都是必要的**，但**不充分**：**"结构正确 + 字段名错误"仍会产出假阳性**。更具体地说，旧版自检的 stub 用的是 `key`/`name` 形状，**与错谓词同源**，所以 14 项"通过"只是把 bug **认证**了一遍（**测试镜像了 bug**）。补上 F-3 后新增的两项断言（`modifier_detected_when_present_uuid` / `modifier_absent_when_cleared`）才改用**实测报文形状**。

**我在 t7 里对这条事实的独立确认**：本次探针运行（v9）的掉线前读数**直接给出**了该报文形状 ——
`"modifiers":[{"uuid":"shadowhunterrolesplugin:role_health_modifier","amount":20,"operation":0}]`，
重登后为 `"modifiers":[]` ⇒ **"从有到无"是我自己观测到的转移**，不依赖任何谓词的解释。

**若未被抓到会怎样**：t6 会拿到一个 `C_persistence_modifierGone = true`，而它**什么也没证明** —— 正是 §6.6 反复强调的"仪器错 → 结论错，且比不测更危险"。

**已升格为通用条款的教训（§6.6）**：① 自检/单测的 **stub 形状与取值必须来自真实报文或真实运行观测**；② 仪器改动后**先在排练环境跑一次**再交付（本轮正是排练抓到的）；③ **结构性核验不能替代对报文事实/字段名的独立核对**。
（同族案例：指南 §5 曾写"1.7 有 11 处字面量调用点"，而实测自 `before/` 起就只有 1 处 —— **口径/字段假设错了，结论就错**。该条已由 `c77a2c7` 修正。）

---

## 1. 结论摘要（每一项都标档位）

| # | 判据 | 档位 | 结果 |
|---|---|---|---|
| 1 | 构建：BUILD SUCCESSFUL + jar/classes mtime 刷新 | **运行实测（强）** | ✅ 强制重建后 `3 actionable tasks: 3 executed`，jar/class mtime 刷新 |
| 2 | 起服合格线（Mode B：三行 + 第 3 行缺席） | **运行实测（强）** | ✅ 三行齐（日志 `:43`/`:44`/`:48`），`Succeed to find the dependency` 0 命中 |
| 3 | 无新增 ERROR/Exception（集合差） | **运行实测（强）** | ✅ ADDED 0 / REMOVED 0 / A0 / B0 / **C0** |
| 4 | I-16 掉线即销毁（A/B/C/C/D/E 六项） | **运行实测（强，我独立复现）** | ✅ 六项全 true，exit 0 |
| 5 | quit 窗口持久化（max_health 归 20 + 药水清空） | **运行实测（强，协议级前后对照）** | ✅ 掉线前修饰符在、药水在 → 重登后两者皆无 |
| 6 | 数值字面量多重集 | **静态计算（强，双实现互证）** | ✅ 新增 = 0；下降 6 条**逐条归属**；剔除已申报项后 diff = 0 |
| 7 | 字符串字面量多重集 | **静态计算（强，双实现互证）** | ✅ 26 条差异**逐条归属**；剔除已申报项后 diff = 0 |
| 8 | 值不变判据（附录 A 冻结值在声明处逐字未变） | **静态（强）** | ✅ 由"新增=0 + 6 条归属"推得，并抽查声明处 |
| 9 | **"掉线后 ticker 与 BuffManager updater 两个 1-tick 任务停止"** | **静态证据（+ 运行反证）** | ⚠️ **运行级证据在本环境不可得**（见 §7）→ 按 §6.6 ②(3) **落静态档位并挂账** |

---

## 2. 构建判据（**不得以退出码判定**）

第一次 `.\gradlew build -x copyPluginJar --console=plain` → 输出含 `BUILD SUCCESSFUL in 12s`，但**全部任务 UP-TO-DATE、mtime 未刷新** → **不满足本项目判据**（§6.3 明写"UP-TO-DATE 不等于重新编译过"）。

强制重建（取得字面证据）：
```
.\gradlew build -x copyPluginJar --rerun-tasks --console=plain
  > Task :compileJava   > Task :classes   > Task :jar   > Task :assemble   > Task :build
  BUILD SUCCESSFUL in 20s
  3 actionable tasks: 3 executed          ← 非 UP-TO-DATE
```
产物新鲜度（附录 B 口径）：
```
jar = 99659 B / mtime 2026-09-16 23:11:26.395 / SHA256 F94CDE71…151908
最新 class = 2026-09-16 23:11:24.867
src 新于 jar 的文件数 = 0                  ← 期望 0 ✓
```
**两处需说明**：① 退出码 = 1，是 §6.3 已载明的沙箱文件监视线程故障（`Couldn't open current thread, error = 5`），**非构建失败**；② 重建后的 jar SHA256 与 t6 当次 jar **完全相同**（`F94CDE71…`）→ 被验代码同一、且构建可重现。

---

## 3. 起服合格线（Mode B）与"无新增 ERROR"

模式明写：**本次采用 Mode B**（不安装 SHDFGamePlugin）。
```
:6  [23:11:57] [PluginInitializerManager] Initializing plugins...
:8  [23:11:58] [PluginInitializerManager] Bukkit plugins (1):          ← 只有我方插件，无 SHDF
:43 [23:12:18] [ShadowHunterRolesPlugin] Enabling ShadowHunterRolesPlugin v1.0.0      ← 第 1 行
:44 [23:12:18] [ShadowHunterRolesPlugin] ShadowHunter Character System enabled.       ← 第 2 行
:48 [23:12:18] Done (22.705s)! For help, type "help"                                  ← 第 4 行
```
第 3 行 `Succeed to find the dependency 'ShadowHunterRolesPlugin'!` → **0 命中**（Mode B 预期）✓；`SHDFGamePlugin` 全文 **0 命中** ✓。
**替代证据三条**：① 静态 `ShadowHunterRolesPlugin.java:43` 注册 API；② 运行第 2 行（在注册之后打印，同线程顺序推理）；③ 待用户在 25565 外服确认真实四行 —— **不计为已通过**。

运行反证（`log-delta.ps1`，正序 `anchor(pre) -> current(post)`）：
```
anchor WARN/ERROR/SEVERE = 13 ; current = 13 ; ADDED = 0 ; REMOVED = 0
A) oshi = 0   B) Entity uuid = 0   C) OTHER = 0
```
→ **C = 0**（不变量达成），且**连 A/B 也为 0**（两侧噪声行完全相同），这是"无新增 ERROR/Exception"的最强形式。

---

## 4. I-16：**我独立复现**（不是引用 t6 的输出）

为避免覆盖 t6 的证据文件（脚本把 JSON 写到固定名 `t6-quit-persistence.result.json`），我在**同字节副本**上跑：`.scratch\bot\verify-run\t6-quit-persistence.js`（SHA256 与原件同为 `EFDBF9EB…` ✓，`node_modules` 向上解析可用）。这是我自己的运行，日志与 JSON 均为本次新产。

```
步骤1 掉线前客户端读数 =
  {"health":40, "maxHealthAttr":{"value":20,"modifiers":[{"uuid":"shadowhunterrolesplugin:role_health_modifier","amount":20,"operation":0}]},
   "hasRoleHealthModifier":true, "effects":[{"id":0,"amplifier":4,"duration":40}],
   "hotbar":["diamond_hoex1","structure_voidx1","iron_ingotx1","gold_ingotx1",null×5]}
断言A 本人查询(在线) = The current energy level of [QuitProbeBot] is: 100
旁证A' Observer 查询   = 同上
步骤3 掉线后 Observer 查询 = Cannot find the player you provided: QuitProbeBot   （原始记录，非断言）
断言B 重登后本人查询      = QuitProbeBot has no role!
步骤4 重登后客户端读数 =
  {"health":20, "maxHealthAttr":{"value":20,"modifiers":[]}, "hasRoleHealthModifier":false,
   "effects":[], "hotbar":[null×9], "inventoryItems":[]}
判定(三态) = {"A_online_hadRole":true,"B_rejoin_noRole_mapEntryRemoved":true,
              "C_persistence_modifierGone":true,"C_persistence_maxHealthIs20":true,
              "D_hotbarClean":true,"E_effectsCleared":true}
E 证据强度 = E: 测得值=true / 强度=strong / 依据=primedBeforeQuit=true
结论：全部断言测得为真且无 vacuous 项（含 quit 窗口持久化的运行实测通过）
EXITCODE=0
```
关键强化（比 t6 的原始输出更强）：**掉线前** `modifiers` 里**确有** `shadowhunterrolesplugin:role_health_modifier`（amount 20，value=20）且 `effects` 有 1 条（`id:0, amplifier:4, duration:40`，由步骤 1b `slot1` 右键制造成功）→ **重登后** `modifiers:[]`、`effects:[]`、`health:20`。
→ **C 槽的"从有到无"是直接观测到的转移，不是推断**；E 非空转（`primedBeforeQuit=true` → strong）。

> ⚠️ 我的探针 JSON 用**另一个脚本输出名/目录**（`verify-run\`）保存；**t6 的证据文件未被覆盖**（实测仍为 6901 B / 22:54:49 / `FECFD179…9EAB81`）。

---

## 5. 双多重集（数值 + 字符串），两个数同报

工具：`numeric-multiset.ps1 -Mode both`（5035 B / `584537DD…`，默认 numeric 行为不变）+ **我自己的独立实现**，两者互证。

### 5.1 数值（剔注释/字符串/字符后的代码区）

| 口径 | before | after | diff rows |
|---|---|---|---|
| **原始**（整树 47 → 44 `.java`） | **387** | **381** | **6（全部删除方向；新增 = 0）** |
| **剔除已申报项后** | **381** | **381** | **0** |
| 存活文件面（排除 3 个被删类） | **383** | **381** | **2** |

6 条差异**逐条归属**（并已核 §2.5 行确实存在）：

| 文件 | 字面量 | 归属类别 | §2.5 指针（我核过在位） |
|---|---|---|---|
| `MeiqiheziBloodySlashSkill.java` | `160` ×1 | **(b) 1.7 调用点塌缩** | §5 1.7；声明值 `:32 160,` 原样保留 |
| `RedBleedPassive.java` | `0` ×1 | (a) 1.6 死代码（**存活文件内被删的死方法**） | §2.5 `:131` 行「`RedBleedPassive.requestBleedResolve(...)` @ `:67-76`」 |
| `SampleMainWeapon.java` | `20` ×1 | (a) 1.6 死代码（整文件删除） | §2.5 `:134` 行「三个未注册/未使用类」 |
| `TestSetSanTEToZeroSkill.java` | `0`×2、`100`×1 | (a) 1.6 死代码（整文件删除） | §2.5 `:134` 行同上 |

**值不变判据（③）**：
- **结构性论证**：`新增方向 = 0` ⇒ 全树没有任何新增数值字面量；6 条删除全部已归属 ⇒ **其余每处字面量（含附录 A 的全部声明值）与冻结基线逐字相同**。
- 声明处抽查（当前树）：`RoleRegistry.java:55-58` `.maxHP(40) / .baseATK(10) / .maxEnergy(100) / .maxSanTE(100)`；`:84-87` red `.maxHP(40)/.baseATK(10)/.maxEnergy(0)/.maxSanTE(100)`；`BloodySlashSkill:32 160,`、`:33 8,`；`CircleSlashSkill:35 200,`、`:36 15,`；`RedBleedPassive:29 MAX_BLEED_STACK = 15`、`:32 BLEED_DAMAGE_PER_SECOND = 2d`、`:35 BLEED_SANTE_RECOVER = 4`、`:41 BLEED_SETTLE_INTERVAL_TICKS = 20` —— 与附录 A 逐字一致。

### 5.2 字符串（§6.2 `:619` 新增条款）

| 口径 | before | after | diff rows |
|---|---|---|---|
| **原始** | **211** | **197** | **26（删 20 / 增 6）** |
| **剔除已申报项后** | **197** | **197** | **0** |

26 条**逐条归属**（我按文件逐条核，全部落在允许类别）：

| 文件 | 方向 | 条数 | 归属 |
|---|---|---|---|
| `SampleMainWeapon` / `TestSendSanTEAndEnergyChangeMsgPassive` / `TestSetSanTEToZeroSkill` | 删 | 3 / 5 / 3 = **11** | §2.5「三个未注册/未使用类」→ 1.6 整文件删除（类从未注册 ⇒ **不可见**） |
| `RoleRegistry.java` | 删 | 2（`"meiqihezi"`/`"red"`） | §2.5「`RoleRegistry.getBuilder`」→ 1.6 删 `BUILDERS` 缓存；同字面量在 `new Role.Builder(...)`/`ROLES.put(...)` 处**仍在** |
| `Role.java` | 删 4 / 增 6 | 10 | **§10 裁决 1（fail-fast）**：旧告警串 → 复合消息；属**装配失败路径**文案，已裁决、已申报 |
| `Skill.java` | 删 | 1（`"skill_id"`） | **O-17** → 改走 `SKILL_KEY` 常量 |
| `RedEquipmentsPassive.java` | 删 | 1（`"xxb111"`） | **O-14** 删除调试输出 |
| `RedBleedPassive.java` | 删 | 1（`"unchecked"`） | §2.5 死方法 `requestBleedResolve` 随删的 `@SuppressWarnings("unchecked")`（基线 `:66`）；**与数值侧那个 `0` 同源同一次删除** |

**玩家可见回执串零改动**：`command/RoleCommand.java` **不在** 26 行差异中（该文件本轮被改，但命令回执串未动）✓。

### 5.3 冷却**调用点总数 15 → 14** 的归属（应队长要求定位，不猜）

我把基线/活动树的**全部** `startSkillCooldown(`/`startMainWeaponCooldown(` 调用点逐处列出并求差（14 处基线内容→活动树按内容一一对应），唯一消失的一处是：

```
基线（after-baseline）：roleComponent/TestSetSanTEToZeroSkill.java:26
                        instance.startSkillCooldown(getId(), getCooldown());
活动树：该文件已删除（§2.5「三个未注册/未使用类」→ 1.6）
```

⇒ **归属 = (a) 1.6 删死代码**，不是引擎/框架行为变化。同一次比对还确认：`MainWeaponListener.java:54` 与 `MeiqiheziJuejueMainWeapon.java`（2 处）等 13 处调用点在两棵树中**内容一致**（仅行号因前文编辑而平移），唯一发生"形式变化"的是 `MeiqiheziBloodySlashSkill.java:40 → :45`（字面量 `160` → `getCooldownTicks()`，即 1.7 塌缩）✓。

---

## 6. t5 / t6 小结声明逐条核（抽验 + 关键项全核）

| 声明 | 我的核验 | 判定 |
|---|---|---|
| t5：O-3/O-4/O-5/O-6 任务句柄化 | 3 处 `new BukkitRunnable` **全部入字段**（`DefaultSanTEZeroPunishment:43 taskId=`、`BloodySlash:46 attackTask=`、`CircleSlash:58 castTask=`）；四个类均有 `stop()` + 取消点（`BloodySlash:95`、`CircleSlash:100`、`SolitaryArrogance:98`、`DefaultSanTEZeroPunishment:132/138`） | ✅ |
| t5：O-6 重入保护 | `DefaultSanTEZeroPunishment:32 NO_TASK_ID`；`cancelPunishmentTask()` 在起新任务**之前**调用；任务体 `if(this.isCancelled()) return;`；`stop()` 复位 | ✅ |
| t5：O-5 用 `ScheduledTask` 句柄 | `RedSolitaryArroganceSkill:25 private ScheduledTask attackTask;` + import 在位；`runAtFixedRate` 结果入句柄 | ✅（**仍用 `getGlobalRegionScheduler`** —— O-5 的"Folia 粒度"半项属阶段 2，非 t5 缺陷，见 §8 low-4） |
| t5 数值/字符串两个数 | 与我独立复算**逐条一致**（387/381/6；211/197/26，删 20/增 6） | ✅ |
| t5 §8.2 子口径 383→381 diff 2 | 我独立实现复现：存活文件面 383→381，仅 `BloodySlash:160` 与 `RedBleedPassive:0` | ✅ |
| t6：Mode B 三行 | 我**自己起服**复现三行齐、第 3 行缺席 | ✅ |
| t6：jar 身份与新鲜度（`F94CDE71…` / `srcNewerThanJar=0`） | 实测一致；且 `PlayerListener.java` mtime 22:35:54 **早于** jar 22:36:34 → 当次起服**确含**该修复 | ✅ |
| t6：未引入 `config.yml` | `src/main/resources` 仅 `plugin.yml` | ✅ |
| t6：六项 verdict + E 强度 | 我独立跑得同一结论（§4） | ✅ |

### 6.1 O 项 `文件:行号` 与内容**逐条对应**核验（我逐条打开代码核，不引用小结）

> 口径：以下是**当前树**的行号；阶段推进后行号会漂移（例如 t2 小结引的 `:74-82` 在本轮已是 `:73/:77-80`），因此**判据以"内容片段"为准**（§6.6:755）。

| O 项 | 当前树证据（内容已核） | 结论 |
|---|---|---|
| **O-1** | `MeiqiheziEquipmentsPassive.java:73 public void stop(...)` → `:77-80 setHelmet/setChestplate/setLeggings/setBoots(null)`，与 `start` 的 `:66-69` 逐件对称 | ✅ 已关闭 |
| **O-2** | `RedEquipmentsPassive.java:73 stop` → `:77-80` 同样四槽清空，与 `:66-69` 对称 | ✅ 已关闭 |
| **O-3 / O-4 / O-5** | 三处 `new BukkitRunnable` 全部入句柄字段（`BloodySlash:46 attackTask=`、`CircleSlash:58 castTask=`、`SolitaryArrogance:37 attackTask=`）+ stop 取消（`:95` / `:100` / `:98`） | ✅ 已关闭（O-5 的 Folia 粒度半项属阶段 2，见 §8 t7-L3） |
| **O-6** | `DefaultSanTEZeroPunishment.java:32 NO_TASK_ID`；`:47 cancelPunishmentTask()` **先取消旧任务再起新任务**；任务体 `if(this.isCancelled()) return;`；stop → `:132` 取消 + 复位 | ✅ 已关闭 |
| **O-7** | `RoleInstance.java:42 appliedPotionTypes`（账本）→ `:504-505` 施加时记账 → `clear()` `:763-766` **只**移除账本内的类型 | ✅ 已关闭 |
| **O-8** | `ShadowHunterRolesPlugin.java:50 onDisable` → `:53 roleManager.clearAllPlayersRole()` → `RoleManager.java:81` 内 `:83 instance.clear()`（回收属性/药水/热键栏/任务）+ `:85 playerRoleMap.clear()` | ✅ 已关闭 |
| **O-9** | `RedEvilShockSkill.java:35 if(resolveRequests == null) continue;`（原 NPE 点已判空） | ✅ 已关闭 |
| **O-13** | 全仓 `start*Cooldown(getId(), <数字>)` = **0**；`getCooldownTicks()` = 14 处（2 getter + 12 调用点）→ 声明值成为唯一真值源 | ✅ 已关闭 |
| **O-14** | `RedEquipmentsPassive` 的 `"xxb111"` 已从字符串多重集消失（§5.2 归属表） | ✅ 已关闭 |
| **O-17** | `Skill.java:126 public static final NamespacedKey SKILL_KEY`，使用点 `:93/:136/:145/:146`；原 `"skill_id"` 字面量已消失 | ✅ 已关闭（常量**按 §2.4 保留**至阶段 4.4） |
| **O-18** | `core/RoleInstance.java` 内 `isReady` 命中 = **0**（死代码已删） | ✅ 已关闭 |
| **O-25** | `PlayerListener.java:40 onPlayerQuit` → `:42 roleManager.clearRole(player.getUniqueId())`；I-16 由 §4 我自己的探针运行实测覆盖 | ✅ 已关闭 |

**结论：O-1..O-9、O-13、O-14、O-17、O-18、O-25 的 `文件:行号` 与内容逐条对应，无"只写已修复但代码里仍在"的情形。**

### 6.2 import 清理核验（覆盖 §2.5「未使用 import」行与 t5 acceptance「§2.5 表全部为空」）

**总量**：同名存活文件的 `import` 行 **396 → 388（净 −8）**。

| 类别 | 数量 | 逐处 |
|---|---|---|
| **纯清理删除（= 队长标注的"实测删 14"）** | **14** | `RoleCommand` 4（`RoleInstance`/`PlayerDropItemEvent`/`PlayerLevelChangeEvent`/`BukkitScheduler`）、`SkillListener` 4（`NamespacedKey`/`ItemMeta`/`PersistentDataContainer`/`PersistentDataType`）、`MeiqiheziJuejueMainWeapon` 4（`ShadowHunterRolesPlugin`/`RoleCommand`/`RoleManager`/`BukkitRunnable`）、`PassiveSkill` 1（`Player`）、`Role` 1（`ShadowHunterRolesPlugin`） |
| 改换 import（**不计入**"未使用清理"） | 2 | `BloodySlash` / `CircleSlash` 各删 `RoleManager`，同时新增 `BukkitTask` + `LifecycleAware`（O-3/O-4 句柄化的必然结果） |
| 新增 | 8 | **O-7** 药水 API ×2（`RoleInstance`：`PotionEffect`/`PotionEffectType`）；**O-25** quit 事件 ×1（`PlayerListener`：`PlayerQuitEvent`）；**O-3/O-4/O-5** 句柄化 ×5（`BukkitTask` ×2、`LifecycleAware` ×3） |
| 随 3 个死类整文件删除而消失 | 14 | `SampleMainWeapon` 3 + `TestSetSanTEToZeroSkill` 5 + `TestSendSanTEAndEnergyChangeMsgPassive` 6（属 1.6 文件删除，不是"未使用 import 清理"） |

**结论**：import 侧无"多删/少删"。卡面旧数 **11** 与实测 **14** 的差额，已由"删除死代码后**额外失效**的 import"解释（逐处清单以 t5 小结为准）；8 处新增全部归属已申报工作项（O-7 / O-25 / O-3·O-4·O-5），**无未申报的依赖变化**。

---

## 7. 未决 / 未能实测（如实声明，不冒充）

| 项 | 状态 | 原因与所需条件 |
|---|---|---|
| **"掉线后 ticker 与 updater 两个 1-tick 任务停止"的运行级证据** | **未能实测** | ① `jcmd`/attach 已定论不可用（我三次实测，含**存活同沙箱子 JVM**，见 `.scratch\bot\jvm-probe\`）；② **`spark` 采样在本执行条件下不可执行** —— 需要在服务器控制台/OP 权限下执行 `/spark profiler start|stop`，而 `runServer` 由后台作业承载（无法向其 stdin 送命令）、机器人无 OP；此外实测 spark 在本机 **async-profiler 引擎不受支持**（`[spark] The async-profiler engine is not supported for your os/arch (windows11/amd64)`），只能用内置 Java 采样引擎。**按 §6.6 ②(3) 落静态档位并挂账** |
| 真实四行合格线（含第 3 行 SHDF 依赖发现） | **未能实测** | 只在装了 SHDFGamePlugin 的 25565 外服可能出现；本环境为 Mode B。**需用户确认** |
| 客户端视觉呈现（渲染/粒子/显示名逐帧） | **未能实测** | 属 §6.6 封闭清单（t15 两项），t7 不适用 |
| 集成环境（装 SHDF）下的 I-16 | **未执行** | 需 SHDF 环境；本环境无该分支（`run\plugins` 无 SHDF、日志 0 命中） |

### 7.1 卡面判据已被裁决取代（说明，非缺口）

t7 任务卡 acceptance 里仍写着"**保留期两种情况实测记录齐全：保留期内重连、超时清理（含离线 `pendingCleanup` 在下次登录补做）**"。该判据**已随作者裁决作废**：

- 权威记录：**指南 §10 第 12 条**（"玩家掉线后的角色处置 → **立即销毁**：`PlayerQuitEvent` → 完整 `clearRole(uuid)`，与死亡同一条路径；**不挂起、不保留、无计时器、无 `config.yml`**；`pendingCleanup` 只在"quit 窗口内的清除未被持久化"被实测证实时才作为兜底实现"）、**§5 阶段 1.8**、**§6.5**（"本工程继续没有 config.yml"）。
- 因此"保留期机制"整条链路（`config.yml` 键、挂起/恢复、超时清理、`pendingCleanup`）**均不在本次范围内**；卡面该行的正确替代判据 = **I-16（掉线即销毁、零残留、重登无角色）**，已由本报告 §4 以运行实测覆盖。
- 而"quit 窗口内清除是否被持久化"这一**未知项**已在本轮被实测证**实**（重登后 `max_health` 归 20 且修饰符消失、药水清空）→ **`pendingCleanup` 兜底无需实现**（只保留"若实测未持久化才补"的条件，条件未成立）。
- 实测佐证"未引入 `config.yml`"：`src/main/resources` 下只有 `plugin.yml`（t6 小结 §6 亦声明一致）。

---

## 8. 问题清单（分级；无 blocker / high / medium）

| ID | 级别 | 问题 | 位置 | 处置 |
|---|---|---|---|---|
| t7-L1 | **low（必须挂账）** | "两个 1-tick 任务停止"只能给静态档位，运行级证据在本环境不可得 | 见 §7 | 按 §6.6 ②(3) 挂账进 **t8 / t24 / t19**；**不判** needs_revision |
| t7-L2 | low | 指南 §5 1.7「11 处字面量待改」与代码不符：`before/`（9/15 21:56）即只有 **1 处**字面量调用点；实测 `.getCooldown()` = 0、`getCooldownTicks()` = 14（2 getter + 12 调用点） | 指南 §5 1.7 | **已由 captain 提交 `c77a2c7` 修正**（归属 5+1、「11 处」标为过期前提）→ 记录为历史，不再作为缺陷 |
| t7-L3 | low | `RedSolitaryArroganceSkill:37` 仍用 `getGlobalRegionScheduler`（O-5 的 Folia 粒度半项） | `roleComponent/red/RedSolitaryArroganceSkill.java:37` | 属**阶段 2** 平台层范围（§5 阶段 2），**非 t5 缺陷**；登记待 t9 处理 |
| t7-L4 | low | 编译期告警：`api/RoleAPIImpl.java` 使用或覆盖了已过时的 API | `api/RoleAPIImpl.java` | 既有问题、非本阶段引入；记录备查（阶段 3 会移位该文件） |
| t7-L5 | low（方法说明） | 我本次探针在**同字节副本**（`.scratch\bot\verify-run\`）上执行，以**避免覆盖 t6 的证据文件**（脚本把 JSON 写到固定名） | `.scratch\bot\verify-run\t6-quit-persistence.js`（SHA256 与原件同为 `EFDBF9EB…`） | 非缺陷；已在此披露，t6 证据文件实测未被改动 |

---

## 9. 未纳入本报告（属其他任务）

- 阶段 1 的整合结案、台账与 §7 清单勾选、`after-phase1` 快照 → **t8**（eng-会话）
- 阶段 0–1 的契约符合性评审（对照 §5 完成判据与 §7 清单）→ **t10**（eng-审阅）
- 我此前完成的 `t4`（基线冻结）与 `t12`（回滚演练）另有独立报告与记录，不在此重复

---

## 10. verdict

**pass**（无 blocker / high / medium；4 条 low，其中 **t7-L1 必须挂账**）。

依据：构建（强制重建 + mtime 刷新 + jar 身份 + `srcNewerThanJar=0`）、起服（Mode B 三行 + 第 3 行缺席 + **C=0** 的集合差）、I-16（**我独立复现**，六项全 true，含**协议级前后对照**证明持久化）、双多重集（数值 387/381/6 与字符串 211/197/26，**逐条归属 + 剔除后 diff = 0 + 值不变结构性论证**）、以及 t5/t6 小结关键声明的逐条复核 —— **全部通过**。唯一不可得的运行级证据（两个 1-tick 任务停止）已按 §6.6 ②(3) 落静态档位并挂账，**不构成 need_revision**。
