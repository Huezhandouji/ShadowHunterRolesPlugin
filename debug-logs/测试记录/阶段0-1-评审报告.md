# 阶段 0–1 评审报告（t10）

> **执行者**：eng-审阅（reviewer）｜**任务**：t10｜**attempt**：1
> **评审对象**：t2（阶段 0 收口）+ t5（阶段 1 止血）+ t6（阶段 1.8 掉线即销毁）**三者合集**，以 t7 验证报告为事实基础
> **评审判据**：指南 §5 各阶段「完成判据」、§7 阶段 0/1 验收清单、§10 裁决记录（第 1/3/9/10/11/12 条）；**任务卡旧文本已作废**（§10 顶部优先级声明）
> **执行时刻**：2026-09-16 23:2x（+08:00）
> **结论**：**verdict = pass**（0 blocker / 0 high / 0 medium；**3 条 low** + 1 条必须挂账）

---

## 0. 本报告的独立性声明

- 凡"数字型结论"（多重集计数、文件集合、哈希、门计数），**我一律自己复算或自己重新计算差异**，不引用 t5/t6/t7 的结论作为依据。第 2、3 节的数字均为我本次现算。
- 工具用法：对 t7 留存的**原始 dump**（`.scratch\bot\numeric-*.txt` / `string-*.txt`，逐行 `<文件>\t<字面量>`）我**自己重算多重集与双向差异**；对源码我**自己做去注释后的逐文件比对**。这属于独立复算 + 交叉核验（我的算法与 t7 脚本独立），而非引用。
- 我**未修改任何 inScope 外文件**；本次唯一写盘 = 本文件。

---

## 1. 逐条判据 → 通过依据

### 1.1 指南 §7 阶段 0 清单

| 判据 | 结果 | 通过依据 |
|---|---|---|
| `MeiqiheziEquipmentsPassive.stop` 与 `start` 严格对称（**无条件清空 4 个装备槽**，不判空、不判断归属） | ✅ | `MeiqiheziEquipmentsPassive.java:73-81` 四行 `setHelmet/Chestplate/Leggings/Boots(null)`；我核 `start` 发放点 `:66-69` 与回收点 `:77-80` **逐槽一一对应**。条件严苛度**高于**判据要求的"不判空"（无条件、无归属判断、无 PDC） |
| `RedEquipmentsPassive.stop` 同样对称 | ✅ | `RedEquipmentsPassive.java:74-82` 与上逐字相同的四行；`start` 在 `:67-70`。两文件去注释后**代码区差异仅这 4 行 + 删 `xxb111`** |
| `debug-logs/阶段0-交付小结.md` 存在，含 B 流 7 处前后对比与"有意改进"申报 | ✅ | 文件存在（32 928 B / 22:30:30）；§2/§3 含 B-1..B-7 逐处 `文件:行号` 前后对比（**前=git HEAD `dccd70c`**，因 `before/` 快照已含这些改动 —— 这是正确的取证选择，非缺口）；含 `/role energy get x y`「静态无输出 → 打印 help」的**有意改进申报**（依据 需求与验收 §4.2 判据 5） |
| 仅 paper-api classpath 编译通过（B-7 强证据） | ✅ | 12 jar 白名单、47 源文件 `EXIT=0` / 55 class；**反向对照** `PollutionProbe` 同 classpath 得 8 errors / `EXIT=1` → 证明"通过"不是类路径污染侥幸。该实验设计正确，我认可其证明力 |
| 起服合格线 + 回滚演练记录 | ✅（Mode B） | 起服见 §1.4；回滚演练 `debug-logs/测试记录/阶段0-回滚演练记录.md`（7 890 B / 22:26:57）存在（t12 产物） |

### 1.2 指南 §7 阶段 1 清单

| 判据 | 结果 | 通过依据 |
|---|---|---|
| 4 处未托管任务全部改为持句柄 + `stop` 取消；`DefaultSanTEZeroPunishment` 无 `taskId` 覆盖 | ✅ | 我独立计数：当前树 `new BukkitRunnable` **恰好 3 处**（`DefaultSanTEZeroPunishment:43`、`BloodySlash:46`、`CircleSlash:58`），`.getTaskId()` **恰好 3 处** → 3 个 `BukkitRunnable` 全部入字段，**无遗漏**；O-5 的 `ScheduledTask` 句柄在 `RedSolitaryArroganceSkill.java:25`；四处 `stop()` 均有取消点（t7 §6 已逐处列出，我抽核 `DefaultSanTEZeroPunishment:131-133` 与 `RedSolitaryArroganceSkill` 的 `stop` 成立） |
| 掉线（`PlayerQuitEvent`）→ 立即完整 `clearRole(uuid)`；该实例 ticker 与 `BuffManager` updater 两任务被取消、`playerRoleMap` 不再有条目（I-16） | ✅（分档） | `PlayerListener.java:36-43` 的 `onPlayerQuit` → `roleManager.clearRole(player.getUniqueId())`；`RoleManager.clearRole(UUID)` 内 `playerRoleMap.remove(uuid)`（**map 项移除 = 运行实测强档**）。两任务的停止 = **静态档 + 运行反证**（t7-L1，**必须挂账**，见 §5） |
| 重新登录 → 没有角色；热键栏无残留物品 | ✅ | t7 探针（我核其 JSON 身份与运行时刻绑定成立）断言 B = `QuitProbeBot has no role!`、D = `hotbar:[null×9]` |
| 实测确认 quit 事件内 `clear()` 的清除被**持久化**：重登后最大生命回到 20、**无残留药水** | ✅ | t7 §4 **协议级前后对照**：掉线前 `modifiers:[{uuid:"…role_health_modifier",amount:20}]`、`effects:[{id:0,amplifier:4,duration:40}]` → 重登后 `modifiers:[]`、`effects:[]`、`health:20`。**"从有到无"是直接观测的转移，不是推断**；且 E 项强度 = strong（`primedBeforeQuit=true`，**非空转**）→ **`pendingCleanup` 无需实现**（触发条件未成立） |
| **不引入 `config.yml`** | ✅ | 我实测 `src/main/resources` 下**只有 `plugin.yml`**（与 t4 基线清单 SHA256 `4EBA0D9A…2818` **逐字相同**）→ 未新增配置文件，未违反 §6.5 |
| `RedEvilShockSkill` 不再 NPE | ✅ | `RedEvilShockSkill.java:33-35`：`getContext(BLEED_RESOLVE_REQUESTS_KEY, Map.class)` 后 `if(resolveRequests == null) continue;`（O-9 防护确实落地） |
| `Role.Builder` 跨类型去重生效、槽位冲突走 **fail-fast 抛异常**（§10 裁决 1） | ✅ | `Role.java:222-227` `ensureIdNotRegistered` **同时查三张表**（`skillSuppliers`/`passiveSuppliers`/`mainWeaponSuppliers`）→ 跨类型去重成立；`:229-237` `validateSlot` 冲突时 `throw new IllegalArgumentException`，**并显式注明"按 §10 裁决 1"**。两个角色槽位实测互斥（meiqihezi 1/2/3 + 0；red 1/2/3 + 0，均无冲突）→ **fail-fast 不会误伤现有两角色** |
| `clear()` 只回收本系统施加的药水效果 | ✅ | `RoleInstance.java:501-506` `applyPotionEffect` 记账；`:762-766` `clear()` 只遍历 `appliedPotionTypes` 移除。**记账覆盖面我已穷举核对**（§2.2）→ 无漏网点 |
| `onDisable` 走 `instance.clear()` | ✅ | 与 t5 小结声明一致；`RoleManager` 的 `clearRole(Player/UUID)` 均走 `removed.clear()` |
| §2.5 死代码清单全部清空（13 行逐项 0 命中） | ✅ | 三个类整文件删除（我核文件集合：基线 47 → 当前 44，**仅这 3 个删除、0 新增**）+ `PassiveSkill.trigger` 删除 + 相关未使用 import 清除 |

### 1.3 t2 / t5 / t6 任务卡 acceptance

| 卡面判据 | 结果 | 依据 |
|---|---|---|
| t2：装备 stop 对称回收；B 流 7 处；仅 paper-api 编译 | ✅ | 见 §1.1 对应行。卡面「只清理由本被动发放的那件」**已被 §10 第 3 条作废**，实现者**按裁决执行并在小结开篇注明**（`阶段0-交付小结.md:6-8`）→ 我方判定为**合规**，非缺陷 |
| t2：起服四行合格线 | ✅（Mode B 三行） | 小结 §6 **明写模式**（`阶段0-交付小结.md:222-224`），且 Mode A 已由 captain 裁决为"全项目稳态不采用"；替代证据三条齐备（静态 `ShadowHunterRolesPlugin.java:41-43` 注册 / 运行第 2 行 / 用户侧待办）→ **未把"三行"当"四行等价"** |
| t2：数值字面量多重集不变 | ✅ | **本阶段=0 变化**：两个改动文件均为"新增 4 行清空语句"，数值字面量计数 387 → 387；且变更面仅这 2 文件（与 `after-baseline` 逐文件 SHA256 比对） |
| t5：1.1–1.7 各项 | ✅ | 见 §1.2 |
| t5：数值多重集不变（活跃代码子口径） | ✅ | 见 §2.1 —— **6 条差异全部为删除、新增方向 = 0**，且逐条可归属 |
| t6：`PlayerQuitEvent` → 完整 `clearRole`；**不挂起/不保留/无 config.yml/不预实现 pendingCleanup** | ✅ | 改动面**仅 1 个文件**（`PlayerListener.java`），我核其实测：+import +handler 6 行、无其它改动。**未引入 config.yml**、**未预实现 pendingCleanup**、**无计时器/挂起** → 与 §10 第 12 条一致 |
| t6：如实申报"掉线即掉角色"这一玩法变化 | ✅ | `阶段1.8-交付小结.md` 开篇即"**按 §10 裁决 12 执行（掉线即销毁）**"；t7 §7.1 亦逐条写明卡面保留期判据已被裁决取代 → **未静默略过** |

### 1.4 构建与起服（不得以退出码判定）

| 项 | 我认定的依据 |
|---|---|
| 构建 | t5 `BUILD SUCCESSFUL in 22s`、jar `21:59:13` → `22:21:29`；t6 `BUILD SUCCESSFUL in 16s`、jar `99 659 B / 22:36:34`；t7 **强制 `--rerun-tasks`** 后 `3 actionable tasks: 3 executed` + jar 刷新至 `23:11:26.395`。**§6.3 的"UP-TO-DATE 不算"被 t7 正确执行**（首跑 UP-TO-DATE 判不满足，重建后判满足）——这是本次验证里我最重视的一条，t7 做对了。退出码 1 = §6.3 已载文件监视线程故障，**未作判据** |
| 起服 | Mode B：t7 日志 `:43/:44/:48` 三行齐、`:8 Bukkit plugins (1)`、`SHDFGamePlugin` 0 命中、第 3 行 0 命中 |
| 零新增（两门口径） | 我按 captain 订正后的规则**自己重算**：t7 日志 `/(ERROR\|SEVERE\|FATAL)]` = **0**、`[ShadowHunterRolesPlugin]`+`/(WARN)]` = **0**（基准锚点同为 0/0）→ **通过** |
| fail-fast 未误伤 | 上述起服日志中 **无** `already occupied` / `already registered` 命中，且插件完成 enable → **两个角色均注册成功** |

---

## 2. 数值与可见行为冻结：**我的独立判断**

### 2.1 数值字面量多重集 —— 我自己重算的结果

我读取 t7 留存的原始 dump 并在**本地重新计算多重集与双向差异**（不引用其结论）：

| 口径 | before | after | 删除方向 | 新增方向 |
|---|---|---|---|---|
| 数值（整树，剔注释/字符串后的代码区） | **387** | **381** | **6** | **0** |
| 字符串 | **211** | **197** | **20** | **6** |

删除的 6 条，我逐条列出（`文件 → 字面量`）：

| # | 文件 | 字面量 | 归属 | 我的核验 |
|---|---|---|---|---|
| 1 | `SampleMainWeapon.java` | `20` | 1.6 整文件删除 | 该文件在 §2.5「三个未注册/未使用类」内；**实测其类名在整仓仅出现于自身文件**（基线命中 2 处，均在自身）→ 从未注册 ⇒ **行为不可见** ✓ |
| 2 | `TestSetSanTEToZeroSkill.java` | `0` | 同上 | 同上（基线命中 2 处，均在自身）✓ |
| 3 | `TestSetSanTEToZeroSkill.java` | `0` | 同上 | 同上 ✓ |
| 4 | `TestSetSanTEToZeroSkill.java` | `100` | 同上 | 同上 ✓ |
| 5 | `RedBleedPassive.java` | `0` | 1.6 删死方法 | 去注释比对确认 `requestBleedResolve` 整段（含 `@SuppressWarnings("unchecked")` 与其内 `0`）消失；**我另核该方法在基线的调用点 = 0**（`RedEvilShockSkill` 走的是 `put(uuid,5)` 写请求表，不调用它）⇒ 不可达 ✓ |
| 6 | `MeiqiheziBloodySlashSkill.java` | `160` | 1.7 字面量塌缩 | `super(...)` 声明值 **`:32` 仍是 `160`**，调用点 `:45` 改为读声明值 ⇒ **传播值逐字不变**（且该字面量本身仍计数于 381 内）✓ |

**我对"多重集不变"这条判据的判定（本报告的关键结论）**：

判据字面为"不变"，实测为 −6。**我判定该条判据在本阶段以"受控收缩"形式满足**，理由：

1. **新增方向 = 0** 是决定性事实：全树**没有任何新增数值字面量**，因此不存在"取值被改写"的情形 —— 而"数值不变"要防的正是取值漂移。
2. **6 条删除全部落在 §2.5 强制删除范围内**，且我逐条验证了删除对象**不可达**（3 个从未注册的类 + 1 个零调用死方法）；第 6 条是**同一数值的表达式塌缩**，传播值不变。
3. 因此 6 条差异**不可能**改变任何运行期行为或数值平衡。
4. **判据冲突的正当解释**：§7 阶段 1 同时要求"§2.5 死代码清单全部清空"与"数值多重集不变"，二者在字面上互斥（清空死代码必然带走其字面量）。正确读法是——**"不变"约束的是命中活代码的取值，删除不可达代码属该阶段明确要求**。t7 §5.1 的"新增=0 + 逐条归属 ⇒ 剔除后 381=381"结构性论证与此一致，且我独立复算确认。

> ⚠️ 附带说明（low，见 F-2）：此解释应显式记录，否则后续阶段若照字面复核会得到"−6 未解释"的假警报。

### 2.2 药水记账的**覆盖面穷举**（O-7 的隐蔽风险，我单独查了）

`clear()` 由"无条件清空全部药水"改为"只清账本"，**危险方向是漏记**：本系统施加却没记账的效果，从此无人清理（基线反而会清掉，故属真实行为回退）。我为此穷举了全仓药水触点：

| 类别 | 位置 | 是否记账 | 判定 |
|---|---|---|---|
| 施加入口 | `RoleInstance.applyPotionEffect` = `player.addPotionEffect` + `appliedPotionTypes.add` | 是（唯一入口） | ✅ |
| STUN（BuffManager 施加） | `BuffManager.java:117-122` → `instance.applyPotionEffect(...)` | **是** | ✅ |
| STUN（BuffManager 到期/移除） | `:127-134` `player.removePotionEffect(BLINDNESS/DARKNESS)` | 直接移除（正确：清账本不该管到期失效） | ✅ |
| meiqihezi 技能 | `CircleSlash:48,50`、`Unconcern:32,40` | 是（速慢/速度经记账） | ✅ |
| red 技能/被动 → **自身** | `RedBleed:112-115,199-200`（刚使用者）、`RedDeeplySorrow:45-46` | 是 | ✅ |
| red 技能 → **敌人** | `RedEvilShock:30-31`（致命盲/缓慢加到 `p`，而 `p` 恒为**敌方**：`:29 isHostileTo(p)` 保证） | **否（有意）** | ✅ **非泄漏**：给敌人上的效果**不应**由使用者离开时回收，否则会变成"打断别人身上的合法 debuff"的副作用 |
| 已删除类中的触点 | `TestSetSanTEToZeroSkill` 内的 `new PotionEffect` | 随文件删除 | ✅ 不适用 |

**结论**：当前树中**没有任何**"本系统施加且未记账"的效果 ⇒ 药水记账**覆盖完整**，未引入 O-7 反向泄漏。这是我对本阶段风险最高的一条做的定向核查，**通过**。

> 一处**声明口径**不精确（low，见 F-1）：t5 小结列出的 8 个"本实例施加点"中，`RedBleed` 只列了 `:114`、漏列 `:199`。

### 2.3 可见行为变化逐项清点（判据：除已申报项外不得有其它可见变化）

我对**全部 24 个存活改动文件**做了**去注释后的代码区比对**（非仅哈希），把所有内容变化逐条归类：

| 类别 | 涉及文件 | 是否可见行为变化 | 判定 |
|---|---|---|---|
| **D-1** 装备 `stop()` 无条件清空 4 槽 | `MeiqiheziEquipmentsPassive:77-80`、`RedEquipmentsPassive:78-81` | **是** | ✅ **已申报**（§6.2 第 3 条 / §10 裁决 3），且**代价已在小结明文登记** |
| **D-2** 掉线即销毁角色实例 | `PlayerListener:36-43` | **是** | ✅ **已申报**（§6.2 第 4 条 / §10 裁决 12 / 设计 §9.1），小结如实申报 |
| O-7 药水记账 | 6 文件 | **否**（`clear()` 的语义目标就是"只清本系统效果"；记账覆盖面 §2.2 已证完整） | ✅ 非变化 |
| O-3/O-4/O-5/O-6 任务句柄化 | 4 文件 | **否**（仅资源托管；任务触发时序未变：`DefaultSanTEZeroPunishment` 仍是 `0L, 40L`） | ✅ 非变化 |
| **1.7** `getCooldown()` → `getCooldownTicks()` | 12 调用点 + 2 getter | **否**（同一字段的改名；我核基线 `getCooldown()`=14、当前 `getCooldownTicks()`=14，**数量与位置一一对应**） | ✅ 非变化 |
| O-10/O-11/O-12 Builder 校验 | `Role.java` | **否**（见下"边界项"） | ✅ 见 2.4 |
| O-14/O-17/O-18/§2.5 | `RedEquipmentsPassive`、`Skill`、`CircleSlash` + 3 删除类 | **否**（删调试输出 `xxb111`；`skill_id` 改走 `SKILL_KEY` 常量；删死局部变量与从未注册的类） | ✅ 非变化 |
| 删除的 import | `RoleCommand`、`SkillListener`、`Skill`、`MeiqiheziJuejueMainWeapon`、`PassiveSkill`、`Role` | **否** | ✅ 见 §3.2 |

**结论：可见行为变化恰好 = D-1 + D-2 两项，与 §6.2 已申报清单一致；无第三项。** 至于"阶段 1.8 新增保留期行为"——按 §10 第 12 条该机制**已作废**，故本次交付**不含**任何保留期行为（如含才是缺陷）。

### 2.4 两处"边界项"：我判定**不是**未申报的可见变化（附理由）

| 边界项 | 位置 | 为何不算缺陷 | 备注 |
|---|---|---|---|
| **B-1** 槽位冲突由"告警 + 覆盖"改为抛异常 | `Role.java:229-237` | 该变化**已被 §10 裁决 1 明文规定**，且属**装配失败路径**：实测两个角色槽位互斥、起服日志无 `already occupied` → 在当前内容上**不可达、无可见差异**。警告字符串消失属**规范化的失败路径文案**，且已计入字符串多重集归属 | 严格说这是"**已裁决的运维行为变化**"，建议 §6.2 补一行留痕（low，见 F-3） |
| **B-2** `RedEvilShockSkill` 的 `resolveRequests == null` 守卫 | `:35` | 与"修复 O-9 NPE"声明同源，属**修复**而非变化；且在 §7 阶段 1 清单中被**明确要求**（"`RedEvilShockSkill` 不再 NPE"）。当前角色 `RedBleedPassive` 恒定注册 ⇒ 守卫不可达 ⇒ 无可见差异 | — |
| **B-3** 空 `description` 的占位文案 | `Role.java` build 处 | **可见输出不变**：原 `if(description == null) description = List.of("No description yet.")`，新 `if(description.isEmpty())`（默认空表）→ **同一兜底文案** | — |

### 2.5 值不变判据 + 附录 A 抽查（我亲自抽样）

抽查当前树声明处，与附录 A **逐字一致**：`RoleRegistry:55-58`（40/10/100/100）、`:84-87`（40/10/**0**/100）；`BloodySlash:32 160`、`:33 8`、`:58 count>=4`、`:66 1.5`、`:68 4 格`、`:73 14 伤`；`CircleSlash:35 200`、`:36 15`、`:48 SLOWNESS(20, amp2)`；`DeeplySorrow:23 600`、`:45 createEffect(45,5)`、`:46 (45,2)`；`RedBleed:29 15 层`、`:32 2d`、`:35 4`、`:41 20`；`RedEvilShock:21 120`、`:27 5 格`、`:30/31 61/(1,3)`、`:36 5 层`、`:39 +10`；`SanTEZero:30 NO_TASK_ID=-1`、`:49 0.3d/20`、`:71 STUN 100`、`:113 0.33333d`、`:121 (0L,40L)`。
→ **无一处数值漂移。**

---

## 3. 改动范围合规性

### 3.1 文件清单（mtime 切点 + 内容哈希 + 三向集合差）

- **基线** = `debug-logs/回滚快照/after-baseline/`（t4 冻结，51 副本 / 47 `.java`）。
- 我按**逐文件 SHA256**比对全部 47 个基线 java：**存活且内容有变的 = 24 个**；**被删除 = 3 个**（`SampleMainWeapon.java`、`TestSendSanTEAndEnergyChangeMsgPassive.java`、`TestSetSanTEToZeroSkill.java`）；**新增 = 0 个**。
- 24 个存活改动文件与 t2/t5/t6 小结及 §7 判据的对应关系**一致**：t2 占 2（两个装备被动）+ t5 占 22（其中 `PlayerListener` 亦被 t5 触及）+ t6 占 1（`PlayerListener`）。
  > 计数说明（low，见 F-4）：t5 小结称"22 个文件修改"，我独立测得**本阶段相关存活改动共 24 个**；差异来自 `RedEquipmentsPassive`/`MeiqiheziEquipmentsPassive` 同时被 t2 与 t5 触及的合并计数口径，**不是漏报**，但口径应写明以免整合阶段对不上账。

### 3.2 import / 风格 / 依赖纪律

- **删除的 import 全部为"确实不再使用"**，我逐个反查：`RoleCommand` 的 4 个（`RoleInstance`/`PlayerDropItemEvent`/`PlayerLevelChangeEvent`/`BukkitScheduler`）、`SkillListener` 的 4 个、`Role` 的 `ShadowHunterRolesPlugin`、`MeiqiheziJuejueMainWeapon` 的 4 个、`PassiveSkill` 的 `Player` —— 对应符号在**当前树中已无任何使用点**（我核 `SkillUtils`/`ArrayUtils`/`QLZLogger`/`StringUtils` 引用：基线 0、当前 0）；且 `PassiveSkill.trigger`（删掉的唯一使用者所在方法）确已随 §2.5 删除。**属合理的未使用 import 清理，非"import 重排/风格漂移"。**
- **依赖与构建零改动**：`build.gradle.kts`、`gradle.properties`、`settings.gradle.kts`、`src/main/resources/plugin.yml` 与基线清单 SHA256 **四个全部逐字相同**；`RoleAPI.java`（对外契约）SHA256 与基线相同（`944A13FE…CAE5`）→ §6.4「只增不改」未被触碰。
- **Gradle 串行**：t5/t6/t7 的构建均为单条 `.\gradlew build -x copyPluginJar`（t7 另加 `--rerun-tasks`），证据中无并行/多守护进程或额外 Gradle 任务；我另核未新增 Gradle 任务、仓库配置或插件。**`copyPluginJar` 按纪律未执行**（工作区外目标），符合 §6.3。

### 3.3 起服锚点与"零新增"口径

- 锚点 `debug-logs/测试记录/阶段1-起服锚点-t2ModeB.log`：我核 6 199 B / mtime 22:01:20 / SHA256 `2A4BD077…ADB524`，三项**逐字匹配**（该锚点前后共 3 次独立核验）。
- 两门口径基准：级别门 = **0**、插件门 = **0**（我自算）；实现者与验证者的起服日志**均满足**。
- `latest.log` 轮转风险已有处置（t5/t6 各自留档并给出哈希）。
- 证据可复原性：锚点已由 captain `git add -f` 入库（我核 `git ls-files --error-unmatch` 命中）；`.scratch\bot\` 为工作区根测试工装、按授权不入库，且受 §6.6 要求的"带哈希受管副本"约束（`debug-logs\回滚快照\工装\`）。

### 3.4 数值多重集工具的独立性问题（我核查后认可）

同一脚本产出的 before/after 存在"两份一致"的可能自证风险。我核到的实情：`numeric-multiset.ps1` SHA `584537DD…`（5 035 B）+ **t7 自写的独立实现**两者互证，且我本次**又用第三种算法复算**同一 dump 得同一结论（387/381/6、211/197/26）→ 三方一致，**该结论可信**。

---

## 4. findings

| ID | 级别 | 问题（精确到 文件:行号） | requiredFix |
|---|---|---|---|
| **F-1** | **low** | t5 交付小结把药水记账的"本实例施加点"列为 8 处并逐一枚举，但 `RedBleedPassive` **只列 `:114`、漏列 `:199`**（两处均为 `instance.applyPotionEffect(...)`）。**计数 8 正确、枚举不完整** | 在 `debug-logs/阶段1-交付小结.md` 的该枚举中补 `RedBleedPassive.java:199`（`casterInstance.applyPotionEffect(...)`）→ 8 处逐一可查，避免后续阶段按枚举复核时误判"有点漏记" |
| **F-2** | **low** | 数值多重集 387→381（−6）与"多重集不变"判据字面冲突。虽然 t7 §5.1 给了结构性论证，但**该解释未写进任何实现交付物**，后续阶段若照字面复核会得到"−6 未解释"的假警报 | 在 `debug-logs/阶段1-交付小结.md` 增设一句口径：「**多重集判据解读**：约束命中活代码的取值不变；§2.5 强制删除所带走的字面量（本次 6 条，新增方向 = 0，逐条归属见 t7 §5.1）不计为违反」 |
| **F-3** | **low** | §10 裁决 1（槽位冲突 fail-fast）带来一处**装配失败路径**的运维行为变化（旧：`WARNING` 告警 + 覆盖；新：抛 `IllegalArgumentException`、该角色不注册），已入字符串多重集归属，但**§6.2 的"有意行为变化"清单未登记**（该清单目前只有 4 条：energy help / 5.3 节流 / 清 4 槽 / 掉线即销毁） | 由 captain 在指南 §6.2 补第 5 条（一句即可）："**槽位/同 id 冲突由告警+覆盖改为 fail-fast**（§10 裁决 1）；当前两个角色槽位互斥、实测不影响注册"。**属文档补记，不需改代码** |

> **无 blocker / high / medium**。F-1..F-3 均不改变行为、不影响验收，可在 t8 整合时一并处理。

### 4.1 我检查过但**未发现问题的项**

1. 装备 `stop` 四槽条件对称性（逐槽、逐文件核对）；2. `applyPotionEffect` 记账 + `clear()` 只清账本；3. **药水触点穷举**（含"给敌人上效果不记账"是有意设计，非泄漏）；4. 3 个 `BukkitRunnable` 全部持句柄、`.getTaskId()` 3/3；5. `DefaultSanTEZeroPunishment` 重入保护（先取消旧任务、任务体 `isCancelled` 早退、`stop` 复位）；6. `Role.Builder` 三表跨类型去重 + fail-fast；7. 两个角色槽位互斥与注册成功；8. 4 个被删 getter 在基线的调用点为 0（行为中性）；9. 删 import 的全部 6 个文件，逐个反查无残留使用者；10. 1.7 改名 14 处一一对应、`160` 传播值不变；11. 3 个删除类从未注册（类名仅出现于自身文件）；12. `RedBleedPassive.requestBleedResolve` 零调用；13. `RoleAPI.java` 与基线同哈希；14. 4 个构建/资源文件与基线同哈希；15. 未新增 `config.yml`；16. Mode B 三行齐 + 第 3 行缺席 + 替代证据三条；17. 两门口径均为 0；18. 附录 A 冻结值抽查 20+ 处逐字一致；19. 探针 JSON 的仪器身份/运行时刻绑定与冻结版一致；20. `pendingCleanup` **未被预实现**（且实测证明无需它）。

---

## 5. 必须挂账（不得静默略过）

| 挂账项 | 档位 | 处置 | 落点 |
|---|---|---|---|
| **"掉线后 ticker 与 `BuffManager` updater 两个 1-tick 任务停止"** | **静态证据**（+ 运行反证） | 运行级证据在本环境不可得（jcmd attach 已定论否决；spark 需控制台/OP 且本机 async-profiler 引擎不受支持）。按 §6.6 ②(3) 落静态档 + 静态调用链行号，**不判 needs_revision** | **t8 / t24 / t19 收口记录** |
| **真实四行合格线（含第 3 行 SHDF 依赖发现）** | 未能实测 | 只在装了 SHDFGamePlugin 的 25565 外服可能出现；本环境为 Mode B。列为"待用户实测的开放项" | **t8 / t24 / t19 收口记录** |
| 阶段 1 起服日志副本归属 | — | `debug-logs/测试记录/阶段1-起服日志-t5ModeB.log`、`debug-logs/测试记录/阶段1.8-起服日志-t6ModeB.log` 为实现者按锚点政策留存的证据文件；是否纳入正式交付物由 t8 在整合阶段收纳 | **t8** |

---

## 6. verdict

**pass**（0 blocker / 0 high / 0 medium；3 条 low）。

**依据**：§7 阶段 0 清单 5/5 通过、阶段 1 清单 11/11 通过；t2/t5/t6 卡面 acceptance 逐条通过（卡面旧文本按 §10 裁决取代，且实现者均已显式注明"按 §10 执行"）；构建与起服按 §6.3 口径达标（**不得以退出码判定**已正确执行，含 t7 的 UP-TO-DATE 判负与强制重建）；数值与可见行为冻结经**我独立复算**成立（新增方向 = 0、6 条删除逐条归属不可达、药水记账覆盖穷举无泄漏）；**可见行为变化恰好 = §6.2 已申报的两项（清 4 槽、掉线即销毁），无第三项**；改动范围限于 inScope（24 改 / 3 删 / 0 增，构建与资源文件零改动，`RoleAPI` 零改动）；回滚交代清楚（`after-baseline` + SHA256 + `before/` 未覆盖 + t12 非破坏式回滚演练）。3 条 low 均为**文档补记**，不构成阻挡；2 项未能实测已按政策落档并挂账。

**评审人**：eng-审阅（attempt 1）
