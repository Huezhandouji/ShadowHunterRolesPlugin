# 阶段 5 · 调度器迁移批 独立验证报告（t56）

> **执行者**：eng-验证（verifier）｜**任务**：t56（verification）｜**执行时刻**：2026-09-18 20:3x（+08:00）
> **交付物**：本文件（卡面 inScope = `debug-logs/测试记录/阶段5-调度器迁移-验证报告.md`）✓
> **判定**：**成立** ✓（零可见差异的主张**已被我独立复算**；无 blocker / 无 high）
> **只读边界** ✓：本轮**未改任何 `src`**、**未改历史证据文档**、**未做 git 写**、**未起服**（复算走实现者已冻结的原始输出 + 我自己的现算）
> **口径纪律** ✓：`git grep` 大小写敏感 + 写明**作用域与匹配形态**；**未使用** `Select-String` / `@(… | Measure-Object).Count`；计数**均写明是否扣除定义行/注释**；未跟踪新产物**直读**；git 路径用**正斜杠**；行数一律 `ReadAllLines` 口径 ✓

---

## 0. 结论摘要

| 验收项 | 结论 |
|---|---|
| ① 适配器切换（接口签名逐字未变 · `execute(` 未误用 · 类名未变 · `initialDelay≤0` 两入口归一） | ✅ **成立** |
| ② "零可见差异"关键读数（**我自己复算**，分路径两列） | ✅ **成立**（原生 vs 适配器 = **同 tick、同 delta、同线程**） |
| ③ 自带物证覆盖它自己的制品（F1：mtime 顺序 + 自述 jar 身份 + 52 份锚点 `C=0`） | ✅ **成立**（**按代际**成立；见 §4 口径） |
| ④ 已申报未覆盖项是否如实 | ✅ **如实**（4 项逐条核；其中 Folia 按用户裁定改为"不适用"） |
| ⑤ 报告 + 分级 findings + 身份 + 只读边界 | ✅ 本文件 |
| ⑥ 只读与口径纪律 | ✅ 见页首声明 |

**findings**：**0 blocker · 0 high · 0 medium · 3 low · 3 info**（全部为口径/计数类，见 §6）⇒ **判定 = 成立** ✓。

---

## 1. 验收项 ①：适配器切换（逐条现算）

**基线**：**定稿时** `HEAD = 9d5a05e2c602a1449540bdf00132d1241f3e3b62`；**`git status --porcelain -- src` = 0 行**（干净）✓
> **口径注记（本轮内发生过一次制品/行号移动）**：首次测量时 `HEAD = af2c3420…`（t53 的 javadoc 提交）；其后 `platform/BukkitSchedulerAdapter.java` 又有一次**措辞更正**（见 `debug-logs/测试记录/阶段5-javadoc-适配器措辞更正说明.txt`）⇒ 该文件内 `Math.max(1L, …)` 的**代码行由 `:51` 移到 `:55`**（`TimerPortImpl:51` 与 `Scheduler:16` 未变）⇒ **本报告 §1 的判据行号已按定稿时现算更正** ✓；两次测量的判据结论**一致**（签名 diff 为空 · 两入口归一 · `platform` 内 `.execute(` = 0）✓。

| 判据 | 命令（作用域 + 匹配形态） | 输出 | 结论 |
|---|---|---|---|
| **接口签名逐字未变** | `git diff --numstat -- src/main/java/com/shadowHunterRolesPlugin/platform/Scheduler.java src/main/java/com/shadowHunterRolesPlugin/platform/Task.java` | **0 行**（空）✓ | ✅ |
| **`Task` 签名（代码位）** | 直读 `platform/Task.java` | `:21 public interface Task {` · `:23 void cancel();` · `:25 boolean isCancelled();` | ✅ 与迁移前一致（`cancel()` 保持 `void`、`isCancelled()` 保留） |
| **`Scheduler` 签名（代码位）** | 直读 `platform/Scheduler.java` | `:24 public interface Scheduler {` · `:26 Task run(Runnable task);` · `:28 Task runLater(Runnable task, long delayTicks);` · `:30 Task runRepeating(Runnable task, long initialDelayTicks, long periodTicks);` | ✅ 三方法签名逐字未变（含 `runRepeating` 的 **task-在首位**顺序 ✓） |
| **`platform` 内 `execute(` 未被用来替代 `run`** | `git grep -nE '\.execute\(' -- src/main/java/com/shadowHunterRolesPlugin/platform` | **0 行** ✓ | ✅ 生产未用 `execute(` 顶替 `run`（全仓唯一命中在**临时探针** `command/RoleCommand.java:375`，属探针自用 ✓） |
| **类名未变** | 目录清单 | `BukkitSchedulerAdapter` / `Scheduler` / `Task` / `RolesContext` / `KeyFactory` / `FactionLookup` 六件同名 ✓ | ✅ |
| **`initialDelay≤0` 两入口归一** | `git grep -n 'Math.max(1' -- src/main/java/com/shadowHunterRolesPlugin/platform src/main/java/com/shadowHunterRolesPlugin/core/TimerPortImpl.java` | **裸 4 行** = **代码位 2**（`TimerPortImpl:51` 端口入口 `runRepeating(task, Math.max(1L, initialDelayTicks), periodTicks)` · `BukkitSchedulerAdapter:55` 适配器入口 `long normalisedInitialDelay = Math.max(1L, initialDelayTicks);`）+ **注释位 2**（`BukkitSchedulerAdapter:25` javadoc · `Scheduler:16` javadoc） | ✅ **两入口归一成立**（**口径**：卡面"应 2 处"= 扣除注释后的**代码位** ✓；裸计数 4 见 **F-2**） |
| 归一的**必要性**旁证 | 实现者原始输出（`阶段5-调度器迁移-停服物证.txt` 原始行） | `[sched] ② boundary \| GlobalRegionScheduler.runAtFixedRate(0,10) -> initialDelay0=REJECTED(IllegalArgumentException: Initial delay ticks may not be <= 0)` ⇒ `<=0` 会被**拒** ✓ | ✅ 归一有据 |

---

## 2. 验收项 ②：**我独立复算**的"零可见差异"读数（**不引用实现者结论**）

**方法**：我从其**原始输出**（`阶段5-调度器迁移-停服物证.txt` 内的探针逐行 `[sched] …` 原始行）**取原始 tick 号与命令 tick，自行做差**，不采信其"一致 ✓"的结论列。

**基准**：`commandTick = 1095`（原始行 `[sched] probe start | commandThread=Server thread | commandTick=1095`）✓

### 2.1 左-1：**迁移前 delay = 0** 的调用点（Bukkit `runTaskTimer(0L, 10L)` 侧）

| 次 | 原生 Bukkit `runTaskTimer(delay=0, period=10)` | 新适配器 `runRepeating(initialDelay=0 → 归一 1, period=10)` | 我复算的 delta（原生 \| 适配器） | 判定 |
|---|---|---|---|---|
| #1 | `tick=1096` | `tick=1096` | **1 \| 1** | 同 tick ✓ |
| #2 | `tick=1106` | `tick=1106` | **11 \| 11** | 同 tick ✓ |
| #3 | `tick=1116` | `tick=1116` | **21 \| 21** | 同 tick ✓ |
| 周期（#1→#3） | (1116−1096)/2 = **10** | 同 **10** | 与声明 `period=10` 一致 ✓ | ✓ |
| 线程 | `thread=Server thread` | `thread=Server thread` | 同线程 ✓ | ✓ |

⇒ **两列 = 1/21 与 1/21，逐字相同** ✓（**卡面要求的"（迁移前首触发 \| 迁移后首触发）两列" = 1 \| 1** ✓；第三次 21 \| 21 ✓）

### 2.2 左-2：**值未变的点**（仅 `RedSolitaryArroganceSkill`）

| 事实 | 证据 |
|---|---|
| 该调用点 = `svc().timers().runRepeating(1L, 6, …)`（`roleComponent/red/RedSolitaryArroganceSkill.java:45`；`:35` 注释同述） | 直读 + `git grep` ✓ |
| 其 **delay 原本为 1** ⇒ 迁移前后**值未变** | 迁移批未改该文件（改动面 = `BukkitSchedulerAdapter.java` + `RoleCommand.java`）✓；该点走**同一** `TimerPortImpl → Scheduler → 适配器` 链 ✓ |
| 我的复算：**值 1** 的相位读数 = `② runAtFixedRate #1..3`（`initialDelay=1`）与 `③ bukkit runTaskTimer(delay=1)` ⇒ **1096/1106/1116**（delta **1/11/21**） | 原始行 ✓ ⇒ **与 delay=0 同 tick** ⇒ "**0L 与 1L 同 tick**"（归一**无可见差异**）成立 ✓ |

⇒ **判定：实现换了、值没换** ✓（未按"delay=0 点"处理 ✗，符合卡面"分路径"要求 ✓）

### 2.3 其余读数（我复算）

| 项 | 原生 | 适配器 | 判定 |
|---|---|---|---|
| `runLater(20)` 实际触发 | `③b bukkit runTaskLater` `tick=1115` ⇒ delta **20** | `③d adapter.runLater(20)` `tick=1115` ⇒ delta **20** | 一致 ✓ |
| 一次性 `run` | **无原生对照腿**（申报 ✓，见 §5-②） | `③d adapter.run()` `tick=1096` delta 1 thread=`Server thread` | 见 §5 ✓ |
| 一次性 `execute`（非生产） | — | `① execute` `tick=1096` delta 1 `primaryThread=true` | 旁证 ✓ |
| 句柄语义 | — | `② run`：`owningPluginIsUs=true` / `isRepeatingTask=false` / `getExecutionState=RUNNING` / `isCancelled=false` | ✓ |
| `cancel()` 幂等 | — | `⑤ verdict` 行 `cancelSemanticsOk=true`（我核其原始行：`CancelledState` 首次 `NEXT_RUNS_CANCELLED` → 重复 `_ALREADY`、无异常）+ **残留反证**：取消于 `12:04:15.417`、bot 于 `12:04:21.708` 退出 ⇒ **6.3 秒内无任何新 `[sched]` 行** ✓ | ✓ |
| 真实角色生命周期 | — | `RoleInstance` 1-tick ticker 连续运行 ≈4 秒 ≈80 tick **无异常**；`/role clear` → `cancelAllAndClear()` → 适配器 `Task.cancel()` ⇒ ticker 停止 ✓ | ✓ |

---

## 3. 验收项 ③：自带物证是否覆盖**它自己的制品**（F1 教训）

### 3.1 mtime 顺序（按**代际**，逐代成立）
| 代际 | jar | 其日志 | 其物证 | 顺序判定 |
|---|---|---|---|---|
| 迁移批（t51） | **146010 B / 20:01:38.935 / `163F1D8C…`**（其物证自述 ✓ 2 处） | `阶段5-起服日志-调度器迁移.log` **20:03:20** | `阶段5-调度器迁移-停服物证.txt` **20:08:51** | jar **<** 日志 **<** 物证 ✓ |
| javadoc 批（t53） | **148297 B / 20:26:27.061 / `A74A5747…`**（我现算 ✓） | `阶段5-起服日志-javadoc.log` **20:27:08** | `阶段5-javadoc-停服物证.txt` **20:29:53** | jar **<** 日志 **<** 物证 ✓ |

### 3.2 物证自述 jar 身份 vs 现算
- **t53 物证**：直读命中 `148297` 与**完整** `A74A574703924A8EE74F4BEABEA2BEE77C1449CE9677B7EF98703E24DED85A68` ✓ ⇒ 与我 **20:3x 现算的当前 jar**（`148297 B / 20:26:27.061`）**逐字一致** ✓✓；
- **t51 物证**：自述 `146010 B / 20:01:38.935 / 163F1D8C…` ✓ ⇒ 与其**自己代际**的 jar 一致 ✓（该 jar 已被后续代际重建 ⇒ 见 **F-3** 口径）；
- `src 新于 jar = 0` ✓（现算）。

### 3.3 锚点扫描 `C=0`（**我独立复算**）
- **其声明口径**（物证 `[3.6]`）：`debug-logs/测试记录/*.log` + `debug-logs/baseline-runserver-*.log` ✓（**已写明** ✓）；
- **我的复算**（归一化：去 `[HH:MM:SS]`/ISO 时间戳 + 去 `[Thread/LEVEL]:` 前缀；A = oshi COM 噪声、B = `Entity uuid already exists`、C = 其余）：**件数 = 57**（其申报 52 = **其扫描时刻**的件数；其后新增 5 份日志）· **A = 0 · B = 9 · C = 0** ✓ ⇒ **`C=0` 成立，且新增件后仍为 0** ✓（更强）；
- **2 份构建日志的申报**：`debug-logs/t6-build.log` 与 `t13-build.log`（各 1585 B）**内容是构建日志**（`> Task :jar` + `BUILD SUCCESSFUL in 16s/17s`）⇒ **不属起服日志**、不参与 `C` 判定 ⇒ 其"2 份构建日志"的申报**成立** ✓；
- **口径外补充**（我扩大扫描到 63 份时）：3 份 **09-16 早期日志**（`t6-runserver-gradle.log` / `t13-runserver-gradle.log` / `t13-runserver2.log`）出现 `C=11`，逐条判读其行 = **环境/paper 噪声**（`SERVER IS RUNNING IN OFFLINE/INSECURE MODE!` 及 3 行后续 · `****` 横幅 · `You are running the latest build…` · `4 release(s) behind…` · 更新链接 · `ServerMain WARN Advanced terminal features…`）⇒ **非插件错误**、且**在其声明口径之外** ⇒ **不影响结论** ✓（记为 **F-5** 口径注记）。

---

## 4. 验收项 ④：已申报未覆盖项是否如实（逐条）

| 申报项 | 我的核对 | 判定 |
|---|---|---|
| **① 3 个 `0L` 调用点的业务触发未覆盖** | `git grep -nE 'runRepeating\(0L' -- src/main/java` = **5 行** = **2 生产**（`DefaultSanTEZeroPunishment:56` `0L,40L` · `MeiqiheziBloodySlashSkill:50` `0L,2L`）+ **1 探针**（`RoleCommand:616` `0L,10L`）+ 2 行注释 ⇒ **合计 3 个调用点** ✓ 与其申报一致；**探针只验了"声明值 0/10"的等价，未触发这 3 处的业务逻辑** ⇒ 申报**如实** ✓ | ✅ |
| **② 一次性 `run` 无原生对照腿（且其生产调用点 = 0）** | 其物证表第 1 行自述"无原生 Bukkit 一次性对照腿" ✓；`git grep -nE 'timers\(\)\.run\(\|scheduler\(\)\.run\(\|\.run\(Runnable' -- src/main/java` = 2 行 = 1 探针消息串 + `TimerPortImpl:35`（端口转调）⇒ **组件侧生产调用点 = 0** ✓ | ✅ 如实 |
| **③ 四行合格线未执行** | Mode B ⇒ 第 3 行 `Succeed to find the dependency 'ShadowHunterRolesPlugin'!` **不出现**（其物证自述"SHDF 依赖行 0" ✓）⇒ 判据按"**三行 + 插件数**"执行，**四行合格线确未执行** ✓ 申报如实 | ✅ |
| **④ Folia 真机未验证** | **用户裁定：服务端仍为 Paper、不做 Folia 适配**（不碰 Region/Entity 调度器、不做 Folia 真机验证、不加 Folia 分支）⇒ 本项改判为 **"已裁定不做 Folia 适配，故不适用"** ✓（不列为未完成项） | ✅ 裁定替代 |

---

## 5. 验收项 ⑤：报告身份与边界

- **本报告**：`debug-logs/测试记录/阶段5-调度器迁移-验证报告.md`；身份见回报（字节 / 行数（`ReadAllLines` 口径）/ SHA256）✓
- **只读边界** ✓：未改 `src`、未改历史证据文档、未做 git 写、未起服（复算基于实现者冻结的原始输出 + 我现算）✓
- **未跟踪件**：本报告对 git 为 ignored ⇒ 需 `git add -f`（正斜杠路径 ✓）。

---

## 6. findings（分级 + 依据 + 复现）

| ID | 级别 | 内容 | 复现 / 依据 |
|---|---|---|---|
| **F-1** | **low** | 锚点扫描的**件数**随日志增加而变（其申报 52 = 扫描时刻；现算 57）⇒ 申报宜写成"**扫描时刻件数 + 口径**"而非固定数 | 按其声明口径现算 57 份；`C=0` 在 57 份下**仍成立** ✓ |
| **F-2** | **low（口径）** | `Math.max(1` 的**裸计数 = 4**（代码位 2 + 注释位 2）⇒ 卡面"应 2 处"**只在扣除注释口径下成立** ⇒ 判据须写明口径（**定稿时**代码行 = `TimerPortImpl:51` 与 `BukkitSchedulerAdapter:55`） | `git grep -n 'Math.max(1' -- …/platform …/core/TimerPortImpl.java` ⇒ 4 行（`:51` 代码 · `:25` javadoc · `:55` 代码 · `Scheduler:16` javadoc） |
| **F-3** | **low（口径）** | "物证自述 jar 身份与现算一致"须**按代际**核：t51 物证自述 `146010 B / 20:01:38`（其代际 ✓），**非**当前 jar（`148297 B / 20:26:27`）⇒ 跨代引用会误判 | §3.1/§3.2 表 |
| **F-4** | info | "3 个 `0L` 调用点"= **2 生产 + 1 探针**（探针位属临时件）⇒ 台账引用建议写明拆分 | §4-① |
| **F-5** | info（口径） | 扩大扫描（63 份）时 3 份 **09-16 早期日志** `C=11`，逐条为**环境/paper 噪声**、且在声明口径外 ⇒ 不影响结论；建议后续扫描统一"**声明口径 + 环境噪声类**"两行写法 | §3.3 末条 |
| **F-6** | info | 运行深度申报（一次性 `run` 无原生腿 · `execute` 仅旁证 · 3 处 `0L` 业务未触发 · 四行线未执行）**已如实列出** ⇒ 属"**已申报未覆盖**"，非隐瞒 ✓ | §4 |

**verdict 判定链**：卡面要求"仍有未解决的 blocker/high ⇒ 不得 pass"；本次 **0 blocker / 0 high**（findings 全为口径与计数类）⇒ **判定 = 成立** ✓。
