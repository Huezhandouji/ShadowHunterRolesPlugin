# 阶段 5 ·《初学者指南》独立复核报告（t13）

> **复核对象**：`t12` —— `docs/插件文档/初学者指南.md`（新增，面向「只会变量 / 赋值 / 列表 / if / 循环 / 函数」的读者）+ `docs/插件文档/README.md` 文档地图加一行。
> **执行者**：`eng-规范`（spec / 仪器口径）· **卡**：`t13`（kind = verification）。
> **点名提交**：开工 `90e6c06ed98f15dea4dd1ca4b151dd0e6d3899d4` · **结卡 `08fe741c692c09230a82c39f26a33801fd7906f9`**（卡外前移 1 个提交 = 队长提交 t12 的两件产物：`README.md` **1 插入 / 0 删除** + `初学者指南.md` **439 插入 / 0 删除**（新文件）；`docs/插件文档` 下**无第三个文件被触**）。本卡**零 git 写**。
> **复核基线（点名）**：判据自检的旧侧样本 = `0c6930d:docs/插件文档/架构与组件模型.md`（blob `954fe1c43e15c17ce53f697e998eac4f4a7cede4` / 53075 B / 367 行）。
> **身份纪律**：本件**不内嵌自身哈希**（避免自指失效）；他人身份一律现算并给命令。

---

## §0 结论（verdict 与分级计数）

**verdict = `pass`** ｜ **blocker 0 · high 0 · medium 1 · low 5**

| 复核面 | 结果 |
|---|---|
| A2 痕迹判据独立复算（新件） | 7 条集合 **0 命中（exit 1）** + A2b **2 行**（均为版本号）；**两种仪器同值** |
| A3 判据自检（改前旧文档） | **8/8 非 0**（27 / 30 / 44 / 31 / 27 / 19 / 4 / **58**）⇒ 判据有区分力，**无 criterion defect** |
| A4 未解释术语扫描（卡面最重要一条） | 核心 12 类术语**全部在首次出现处解释**；**6 个词未解释/弱解释**（`private` `final` `void` `Paper` `编译器` `父类`）⇒ **F-1（medium）+ F-4** |
| A5 代码引用可定位 | 抽查 **27 条**；直接命中 24；3 条"初判 MISSING"经更正后**全部为真**⇒ **0 条查无实据** |
| A6 不引用过程文档 | `debug-logs` = **0 命中**（正对照：旧侧样本 26 命中） |
| A7 初学者可读性抽样 | 问 **10 题**：**8 题完全答出**、2 题"答出但不足"（含"改坏了怎么退回"的通用缺口）⇒ **F-5** |
| A8 动手清单可执行性 | **构建实跑成功**：exit 0 · `BUILD SUCCESSFUL in 23s` · `:jar UP-TO-DATE`；起服命令与端口口径**核对一致**（起服**未实跑**，如实声明） |
| A9 既有 4 份未被改 | 3 份与 HEAD **blob 逐字相同**；README 在 `08fe741` 内 **1 插入 / 0 删除**、正文明细 = 文档地图 1 行 |
| A10 制品代次 | jar **前后逐字一致**：159512 B / `2026-09-19 12:54:07.286` / `6A249993…F734` ⇒ **未重建** |
| A11 判据缺陷 | 单列 1 条（**F-3**：卡面 verify(3) 对未跟踪件是结构性盲区；我已独立复现并在跟踪态复验） |
| A13 边界 | 未改 src / 未改被复核手册 / 未改既有证据与冻结件 / **零 git 写**（全仓 porcelain = 0 行） |

> **一句话**：《初学者指南》的骨架（三层模型 → 六个词 → tick/事件 → 逐行读一个技能 → 动手改数值）对"只会变量/列表/函数"的读者**成立且可读**；finding 集中在**少数未解释的词**与**两处"照抄会踩空"的细节**上，均不推翻该件的主干主张。

---

## §1 边界与身份

- **只写本报告**：`debug-logs/测试记录/阶段5-初学者指南-独立复核报告.md`。
- **未改**：`src/**`（porcelain `-- src` = 0 行）· `docs/插件文档/**`（一个字都没动）· `debug-logs/测试记录/` 下既有文件 · `debug-logs/回滚快照/**` · 任何 `*.log`。
- **非交付物的仪器落点（透明声明）**：`ShadowHunterRoles/.scratch/t13/`（被 `.gitignore:89` 忽略）—— 模式文件 `p-A1/A2a/A2b/A3/A4/A5/A6/verify.txt`（与 t10 复核中我独立重建的那一套**逐字节相同**：`FB8E7DE66867…` / `5FF7C308C2BE…` / `5B5ED5BEEB7F…` / `21BD473E9959…` / `5417CA572EB4…` / `1B4EABD6FF0D…` / `9D5E55CD518B…` / `5971C7D8AAB3…`）· 旧侧样本 `old-arch.md`（53075 B，`git hash-object` = `954fe1c4…` = 点名 blob）· 抽查脚本与日志 · jar **备份副本** `jar-before-6A249993.jar`（防"万一被重新打包"时可比对，事后核对与工作树 jar **逐字节相同**）。
- **被复核件身份（现算）**：`初学者指南.md` = 22804 B / 439 行（`ReadAllLines`）/ mtime `2026-09-19 14:34:41.301` / SHA256 `55EE19D707B7680EC972C643BD154CC36C75C11CEE3497A0E5FE66126DC11F72`；`阶段5-初学者指南-说明.txt` = 13912 B / 175 行。

---

## §2 方法与仪器（先说会骗人的地方）

| 用途 | 仪器 | 自证 |
|---|---|---|
| 痕迹检索（权威） | `git grep -nE -f <模式文件>`（**大小写敏感**、**模式先落盘**） | 正对照：旧侧样本 8 条全部非 0；迁移/含痕迹样本亦非 0 |
| 未跟踪新件检索 | `git grep --untracked --no-exclude-standard` | **对照实测**：同一模式 `A2b` 带该旗 = **2 行**，不带 = **0 行**（见 §3.3） |
| 第二仪器（交叉量法） | `[System.IO.File]::ReadAllLines(UTF8)` + PowerShell/.NET 正则逐行匹配 | 与 `git grep` 逐条比对，**7/8 条同值**，1 条差异已定位到引擎（§3.4） |
| 旧侧样本落盘 | `git -c core.quotepath=false ls-tree -r -l` → `cmd /c "git cat-file blob <sha> > <file>"` | 落盘件与 blob **同尺寸 + 同 blob 名**（本轮 1 件：`old-arch.md` 53075 B / 367 行；t10 复核中另 5 件同样验证） |
| 直读（受忽略/未跟踪文件） | `ReadAllLines(UTF8)`（**不用 `Get-Content`**） | 见 §8 的 `run/server.properties` |
| 身份 | `ReadAllBytes` / `Get-Item` / `Get-FileHash` / `git hash-object` / `git ls-tree -l` | 见 §0 表 |

**⚠️ 本轮我自己踩到并当场纠正的 2 个仪器坑（如实自曝）**：

1. **卡面 verify(2) 的字面形式 `git show <rev>:<path> > $env:TEMP\...`（PowerShell 5.1）不产出逐字节副本** ✗：实测落盘件 **72804 B / 首 4 字节 = `255,254,35,0`**（UTF-16LE + BOM），而 blob 是 **53075 B / UTF-8**。⇒ 本次改判据自检的旧侧样本一律走 **blob + `cmd /c` 原始重定向**（53075 B，逐字节）。**（值得注意：该 UTF-16LE 件经 `ReadAllLines(UTF8)` 读入时 .NET 会按 BOM 自动改用 UTF-16 解码 ⇒ 行数与命中数仍对，但文件本体已不是副本；若下游用它算字节哈希就会得出"样本不一致"的假结论。）**
2. **`git grep` 对未跟踪/被忽略文件恒返 0** ✗（本轮出现两次）：一次是新指南（`docs/**` 下未跟踪，§3.3），一次是 **`run/server.properties`**（§8，我用 `git grep -F '25566'` 得 0 行，改直读后看到 `server-port=25566`）⇒ 凡"0 命中"的结论，先证该路径**落在被检索的集合内**。

---

## §3 A2 / A3 · 痕迹判据独立复算 + 判据自检

### §3.1 命令与口径（三件齐）

```
# 新侧（工作树文件；本卡开工时该件尚未被跟踪 ⇒ 须 --untracked；08fe741 之后已跟踪 ⇒ 两态都测）
git -C ShadowHunterRoles grep -nE --untracked --no-exclude-standard -f <绝对路径模式文件> -- "docs/插件文档/初学者指南.md"
# 旧侧（.scratch 内 ⇒ --no-index）
git -C ShadowHunterRoles grep -nE --no-index -f <绝对路径模式文件> -- .scratch/t13/old-arch.md
```
口径：**行级命中数**；`git grep` **默认大小写敏感**（未加 `-i`）；模式文件 UTF-8 无 BOM；作用域逐条写明。

### §3.2 两侧计数表（我现算 vs t12 声称）

| 判据 | 旧侧实测 | 旧侧 t12 声称 | 新侧实测 | 新侧 t12 声称 |
|---|---|---|---|---|
| A1 卡号/批次号 | **27** | 27 | **0**（exit 1） | 0 |
| A2a 阶段号 | **30**（见 §3.4） | 30 | **0** | 0 |
| A2b 数字点数字 | **44** | 44 | **2** | 2 |
| A3 协作语（18 词） | **31** | 31 | **0** | 0 |
| A4 过程文件名（9 串） | **27** | 27 | **0** | 0 |
| A5 元口径/过程记号（12 串） | **19** | 19 | **0** | 0 |
| A6 流程章节（5 串） | **4** | 4 | **0** | 0 |
| 卡面 verify 合并模式（31 行） | **58** | 58 | **0** | 0 |

⇒ **A3 通过**：同一套模式在**改写前旧文档**上 8/8 非 0（判据有区分力，**不是**"改前也 0"的无效判据）。
⇒ **A2 通过**：新件 7 条 0、A2b 2 行（逐行判定见下）。

### §3.3 A2b 的 2 行——逐行判定 + 未跟踪盲区的实测对照

```
git -C ShadowHunterRoles grep -nE -f .scratch/t13/p-A2b.txt -- "docs/插件文档/初学者指南.md"     （字面形式，已跟踪后）
  L357:- 第三行开始构建。看到 `BUILD SUCCESSFUL` 就算成功，产物是 `build/libs/ShadowHunterRolesPlugin-1.0.0.jar`；
  L366:- 它会拉起一个 Paper 1.21.11 调试服，服务端目录是仓库里的 `run/`；
```
两行均为**版本号语境**（工程产物版本 `1.0.0`、服务端版本 `1.21.11`）⇒ 与 t12 的判定一致，**无小节编号/阶段号残留**。
**同一命令加不加 `--untracked` 的实测差**：A2b **2 行 ↔ 0 行**；A1/A2a/A3/A4/A5/A6/verify 在两态下均为 0 ⇒ 证明 t12 自曝的"字面命令对未跟踪件是结构性盲区"**属实**（本卡把它记为 **F-3 / criterion defect**，见 §12）。

### §3.4 两引擎差异（我新发现，登记为 F-6）

同一份旧侧样本、同一批模式，**两个引擎**（git 的 POSIX ERE vs .NET 正则）逐条比对：

| 模式 | .NET | git grep | 一致 |
|---|---|---|---|
| A1 / A2b / A3 / A4 / A5 / A6 / verify | 27 / 44 / 31 / 27 / 19 / 4 / 58 | 同值 | ✓ 7 条 |
| **A2a**（`\b阶段\s?[0-9]`） | **31** | **30** | ✗ |

**机制已定位**：把 `\b` 去掉后，两引擎**同为 33 行且行集合完全相同**（`阶段\s?[0-9]`）；差异**全部来自 `\b` 在 CJK 上的引擎语义**（行集差：git grep 命中 L106、.NET 命中 L122/L279）。大小写不是因素（31 = 31）。
⇒ **判据必须与仪器同写**：引用 A2a 的旧侧计数时要注明"git grep 口径 30 / .NET 口径 31"；新侧 0/0 一致 ⇒ 结论不受影响。

---

## §4 A4 · 未解释术语扫描（卡面最重要的一条）

**读者前置假设**（指南 §"读之前"自述 = 只会变量/赋值、列表/数组、`if`、循环、函数、算术、字符串）⇒ 以下术语**必须**在首次出现处用读者已知概念讲清：Java 语法词（`public` `private` `final` `static` `void` `abstract` `@Override`）、类/对象/继承/接口/注解、事件、线程、`tick`、Bukkit/Paper、Gradle。

### §4.1 逐个判定表

| # | 术语 | 首次出现（正文，非"假定你不会"清单） | 是否已解释 | 判定依据 |
|---|---|---|---|---|
| 1 | **类（class）** | L104-109（`public abstract class RoleComponent`） | ✅ | L104「类是一张"模板 + 造物机"的合体」+ L114 就地解释 `public abstract` + §八 表 |
| 2 | **对象（object）** | L118-123 | ✅ | L118「类 = 表格模板，对象 = 照模板填出来的一张具体的表」+ L126 `new` 的作用 + 表 |
| 3 | **方法（method）** | L132-137 | ✅ | L132「方法**就是函数**…`对象.方法(参数)`」+ `vitals.heal(4)` 对照 |
| 4 | **字段（field）** | L144-149 | ✅ | L144「字段**就是变量**，只是不写在函数里面」+ 局部变量对照 |
| 5 | **继承（inheritance）** | L158-163 | ✅ | L158「把别人的定义整个抄过来，再写自己不同的部分」+ L166 读作"是一种" |
| 6 | **接口（interface）** | L172-177 | ✅ | L172「一张"必须提供的函数清单"，只写函数名和参数，不写做法」+ 表 |
| 7 | **`tick`（游戏循环）** | L190-197 | ✅ | L190「把服务器想成一台不停转的唱机，每 1/20 秒转一圈…这一圈就叫一个 tick」+ L200 解释 `1L 1L` + 表 |
| 8 | **事件（驱动）** | L213-222 | ✅ | L213「事件就是门铃…在门铃上登记一条」+ L217/L225 解释 `@EventHandler` 与 `event` 参数 + 表 |
| 9 | **注解（annotation）** | **L217**（正文首次） | ✅（词本身未定义，作用已讲） | L217「登记门铃的函数，头上有一个注解 `@EventHandler`」；L297 解释 `@Override` 的作用（"重写父类里同名的函数…相当于盖了个章"）⇒ 按**功能**已解释，但**"注解是什么"没有一句话定义**（t12 已自曝其未入术语小表） |
| 10 | **构造函数** | L243-261 | ✅ | L261「这个名字和类名一模一样 —— 这种函数叫构造函数，造对象时自动执行一次」 |
| 11 | **重写 / 父类** | L209 / **L297** | ⚠️ 重写✅、父类弱 | L297「重写父类里同名的函数」；`父类` 一词首次出现即在此，**未**与 §三 的"别人的定义整个抄过来"挂钩 ⇒ **弱解释**（可推断，但没点明） |
| 12 | **`service()` 工具箱 / 局部变量 / 类型 / UUID / Map** | L263 / L144、L316 / L299 / L324 | ✅ | 各自有就地解释（L301「向框架要工具箱」、L316「当场问一句…只在本次函数里用」、L299「只能取几个固定值的类型」、L324「键是编号，不是玩家对象」） |
| 13 | **`void`** | **L178**（`void heal(double amount);`）、L205 | ❌ **未解释** | 全文无任何一句说明"void = 这个函数不返回东西"；不在术语小表 ⇒ **F-1** |
| 14 | **`private` / `final`** | **L137**（`public final String getId()`）、**L149**（`private final String id;`） | ❌ **未解释** | 只有 L114 解释了 `public abstract`；`private` / `final` 无就地解释、不在术语小表；而 L19-21 明文承诺"第一次用到它们时都会…讲清楚" ⇒ **F-1** |
| 15 | **`static`** | 仅 L21（清单） | N/A（空满足） | 正文**从未使用** ⇒ 无解释必要（承诺的空满足，如实登记） |
| 16 | **Paper** | **L366**（"拉起一个 Paper 1.21.11 调试服"） | ❌ **未解释** | 承诺清单点名了 Bukkit / Paper；正文对 Paper 无任何一句话（读者不知道它是什么、与"服务端"什么关系）⇒ **F-1**；`Bukkit` 正文 0 次（空满足） |
| 17 | **Gradle** | L350-356（`$env:GRADLE_USER_HOME` / `.\gradlew build`） | ✅（功能口径） | L356「把构建工具的缓存放在仓库内」+ L357-358 说明产物与 `-x` 含义 ⇒ 读者能知道"有个构建工具、怎么用、产出什么"；**未点名 "Gradle" 这个词**（仅 L27 清单）——登记为口径而非缺陷 |
| 18 | **jar** | L357 | ✅（功能口径） | L357「产物是 `build/libs/….jar`」+ L358「拷到服务端目录」⇒ 读者知道"构建出来的那个文件" |
| 19 | **编译器** | **L297** | ❌ **未解释** | "编译器会报错"——初学者未必知道编译器是什么（可推断"写错会报错"）⇒ 计入 F-1（低权重） |
| 20 | **端口 / 客户端 / 服务端 / 阻塞** | L367 / L367 / L190 / L369 | ✅ | L367 端口说明"客户端连 `localhost:25566`"；L190 唱机（服务端）；L369「这一步是阻塞的：服务端在跑，这个窗口就一直在」⇒ 就地解释 |
| 21 | **增幅档位 / 等级** | L303 | ✅ | L303「第 3 个参数 `4` 是游戏里的增幅档位，游戏内的等级显示为 5 级 —— 这个插件里"增幅 0"对应游戏里的 I 级」 |
| 22 | **多线程 / 线程** | 仅 L24（清单） | N/A（空满足） | 正文 **0 次**（`线程` 全篇仅 1 行 = 该清单行）⇒ 无解释必要 |

### §4.2 结论（未解释者单列 finding）

- **核心教学术语 12 类全部在首次出现处用读者已知概念解释过**（第 1–8、10–12、20–21 行）⇒ 指南的核心承诺**基本兑现**。
- **未解释（计入 F-1）**：`void`（L178/L205）· `private`、`final`（L137/L149）· `Paper`（L366）· `编译器`（L297）—— 共 **5 个词**；**弱解释**：`父类`（L297）。
- **空满足（如实登记，不记缺陷）**：`static`（正文 0 次）· `多线程/线程`（正文 0 次）· `Bukkit`（正文 0 次）。

---

## §5 A5 · 代码引用可定位性抽查（27 条）

**口径**：把指南里每条"符号/常量/数值/文件 + 命令"抽出来，用 `git grep -n -F -f <单模式文件> -- <源码路径>` 独立命中（作用域逐条给出）。**模式经模式文件落盘**，避免命令行 CJK 转码。

| 结果 | 条数 | 说明 |
|---|---|---|
| 直接命中 | **24** | 例：`new Definition("meiqihezi", RoleLoader::meiqiheziBuilder)` → `RoleLoader.java:75`；`public abstract class RoleComponent` → `RoleComponent.java:17`；`public final String getId` → `RoleComponent.java:33`；`private final String id;` → `RoleComponent.java:19`；`class MeiqiheziUnconcernSkill extends Skill` → `MeiqiheziUnconcernSkill.java:15`；`interface VitalsPort` → `VitalsPort.java:7`；`Component.text("漫不经心")` → `MeiqiheziUnconcernSkill.java:21`；`Component.text("获得2秒速度5")` → `:22`；`Material.BLAZE_POWDER` → `:25`；`cooldowns().start(getCooldownTicks())` → `:54`；`public Skill(String id, ComponentServices services` → `Skill.java:28`；`applySkill` → `HotbarRenderer.java:111`；`canCastSkill` → `BuffManager.java:86`；`copyPluginJar` → `build.gradle.kts:60`；`pluginCopyPath` → `:62`；`CastTrigger.RIGHT_CLICK` → `RedDeeplySorrowSkill.java:45`；`clearRole` → `PlayerListener.java:25`；`onPlayerQDropSkillItem` → `SkillListener.java:108`；`onQDrop` → `MainWeaponListener.java:106`；`Map<UUID, Integer> playerBleedRecord` → `RedBleedPassive.java:47`；`Player player = svc().self().player();` → `MeiqiheziEquipmentsPassive.java:27`；`@EventHandler` → `SkillListener.java:27` … |
| **初判 MISSING（3 条）→ 更正后为真（0 条查无实据）** | 3 | ① **`runRepeating(this::triggerUpdate, 1L, 1L)`**：源码是**多行调用**（`RoleInstance.java:124` `updateTask = platform.scheduler().runRepeating(` + `:125` `this::triggerUpdate,` + `1L,` / `1L` / `);`）⇒ 单行模式必然 0；按符号级复核 **`this::triggerUpdate` 1 行 + `runRepeating(` 1 行** ✓（记 **F-2**）。② **主武器 `energyCost` 恒 0**：我首版模式把实参顺序写错；实际 `MainWeapon.java:32` = `super(id, services, displayName, description, cooldown, 0, icon, ItemKind.MAIN_WEAPON);`（**第 6 位 = `0`**）⇒ 指南 L401 的说法**为真**。③ **端口 25566 的出处**：指南说"写在 `run/server.properties` 的 `server-port`"；`git grep` 对该文件返 0（**该文件未被跟踪，落在检索集合外** —— §2 的仪器坑②）；直读 `run/server.properties` 得 `server-port=25566` ✓（另有 `query.port=25566` / `rcon.port=25576`）⇒ 指南**为真**。 |
| 查无实据 | **0** | ⇒ **A5 通过** |

---

## §6 A6 · 不引用过程文档

```
git -C ShadowHunterRoles grep -n --untracked --no-exclude-standard -F "debug-logs" -- "docs/插件文档/初学者指南.md"
   ⇒ 0 命中（exit 1）
# 正对照（同一把尺子在确实引用过程文档的旧侧样本上）
git -C ShadowHunterRoles grep -n --no-index -F "debug-logs" -- .scratch/t13/old-arch.md
   ⇒ 26 命中
```
⇒ 新指南**不引用任何过程文档**；且"0"不是假绿（同一模式在同一批文件上能命中 26 行）。

---

## §7 A7 · 初学者可读性抽样（**只从这份指南出发**，10 题）

| # | 初学者会问 | 指南出处（章节） | 能否答出 | 缺口 |
|---|---|---|---|---|
| ① | 这个插件是干什么的 | §一（四步 + "这就是它全部的对外表现"） | **能** | — |
| ② | 我该先看哪个文件 | §二 + §"接下来看哪一份"（按目标分派到四份手册） | **能** | — |
| ③ | 类和方法有什么区别 | §三「类」vs「方法」（模板+造物机 / 就是函数、点号调用） | **能** | `private`/`final`/`void` 未解释（§4 F-1） |
| ④ | 为什么改完代码要重新构建 | §七 第 1 问（忘构建 / 服务端跑旧的 / jar 不会自动拷） | **能** | — |
| ⑤ | 图标变灰是什么意思 | §一 第 4 步 + §七 第 2 问（冷却中：灰色虚空方块 + ` x.xs`） | **能** | — |
| ⑥ | 掉线后角色为什么没了 | §七 第 6 问（设计如此：`onPlayerQuit` → `clearRole`） | **能** | — |
| ⑦ | 我怎么把某个技能的冷却改短 | §六（改构造函数第 5 个参数；单位 tick；1 秒 = 20 tick） | **能** | — |
| ⑧ | 我改坏了怎么退回 | §六 第 5 步（把 `200` 改回 `100` 再构建） | **部分** | 只覆盖"改数值"这一例；**没有**通用退路（版本控制恢复 / 重新克隆 / 备份）⇒ **F-5** |
| ⑨ | 图标红字 ` DISABLED` 是什么意思 | §七 第 3 问（被沉默/眩晕；`canCastSkill()`） | **能** | — |
| ⑩ | 按了技能却什么也没发生 | §七 第 5 问（四种原因，含"只认某一种按键"） | **能** | — |
| （附） | 为什么技能物品丢不掉 | §七 第 7 问 | **能** | — |

⇒ **10 题中 8 题完全答出、2 题（③/⑧）有已登记缺口** ⇒ 指南对"初学者能不能自己走下去"这一主张**成立**。

---

## §8 A8 · 动手清单可执行性（含构建实跑）

### §8.1 命令与工程实际的一致性核对

| 指南处 | 指南给的 | 工程实际 | 判定 |
|---|---|---|---|
| L351-353 构建 | `cd ShadowHunterRoles` · `$env:GRADLE_USER_HOME = "$PWD\.gradle-work"` · `.\gradlew build -x copyPluginJar --console=plain` | 与本工程既定口径**逐字一致**（含 `GRADLE_USER_HOME` 与 `-x copyPluginJar`） | ✅ |
| L363 起服 | `.\gradlew runServer` | `build.gradle.kts` 注册 `run-paper` ⇒ `runServer` 可用（**未实跑**，见 §8.3） | ✅（静态核） |
| L367 端口 | "**端口是 25566**（写在 `run/server.properties` 的 `server-port`），客户端连 `localhost:25566`" | 直读 `run/server.properties`：`server-port=25566`（另有 `query.port=25566`） | ✅ |
| L358/L389 `copyPluginJar` / `pluginCopyPath` | "只有配置了 `pluginCopyPath` 才会自动拷贝" | `build.gradle.kts:60` `copyPluginJar`、`:62` `pluginCopyPath`（并有 `finalizedBy`） | ✅ |
| L356 缓存提示 | "把构建工具的缓存放在仓库内（避免写系统目录，也可以不写）" | 与 `.gradle-work/` 实际存在一致 | ✅ |

### §8.2 构建实跑（**本卡唯一的运行级动作**，逐字执行指南第 2 步）

```
PS> cd C:\Users\ROG\Desktop\插件\ShadowHunterRoles
PS> $env:GRADLE_USER_HOME="$PWD\.gradle-work"
PS> .\gradlew build -x copyPluginJar --console=plain
exit code = 0
> Task :processResources UP-TO-DATE
> Task :compileJava UP-TO-DATE
> Task :classes UP-TO-DATE
> Task :jar UP-TO-DATE
> Task :assemble / :testClasses / :check / :build  UP-TO-DATE
BUILD SUCCESSFUL in 23s
3 actionable tasks: 3 up-to-date
```
⇒ **退出码 0 · `BUILD SUCCESSFUL` 判定为真 · `:jar UP-TO-DATE`（未重新打包）**。
**前提已核**：所有构建输入 mtime **早于** jar —— `build.gradle.kts` 2026-09-17 18:55:29 · `settings.gradle.kts` 2026-08-24 · `gradle.properties` 2026-09-17 18:55:16 · `src` 最新 2026-09-19 12:53:38 < jar 12:54:07.286 ⇒ 这正是本轮能"既实跑构建、又不重建 jar"的结构性原因。

### §8.3 起服一步：**未实跑（如实声明）**

- 理由：本卡硬约束"**不得重建 jar**" + 本工程"同一时刻只允许一次起服（需窗口互证与停机物证纪律）"；起服还会改写 `run/logs/latest.log` 等运行目录。
- 已做的替代核对：`runServer` 任务由 `run-paper` 提供（`build.gradle.kts` 静态核）、端口 25566 与 `run/server.properties` 一致、`run/` 目录存在 ⇒ **静态级**通过；**运行级未覆盖**（最小闭合见 §13.2 第 ① 条）。

---

## §9 A9 · 既有 4 份未被改（含 HEAD 前移后的正确口径）

- 卡面 verify(6) 的**字面命令** `git diff --numstat -- docs/插件文档` 在**结卡时为空** —— 因为队长已把 t12 的两件产物**提交**（`08fe741`）⇒ 该命令现在测的是"工作树 vs HEAD"，而非"这批改了什么"。**正确口径 = 点名提交的 `--numstat`**：
```
git -C ShadowHunterRoles show --numstat --format='%h %s' 08fe741 -- docs/插件文档
1    0  docs/插件文档/README.md          ← 恰 1 行新增
439  0  docs/插件文档/初学者指南.md        ← 新文件（439 行）
```
- README 的 +1 行正文明细（`git show --unified=0 --format='' 08fe741 -- docs/插件文档/README.md`）：`+| \`docs/插件文档/初学者指南.md\` | 刚学编程的新人 | 从零开始：…` ⇒ 就是**文档地图加 1 行**，无其它改动。
- **另 3 份**：`角色与技能.md` / `架构与组件模型.md` / `开发指南-新增角色或组件.md` 在 `08fe741` 的 numstat 中**完全缺席**（未出现 = 未改）；并且**与 HEAD 的 blob 逐字相同**（`70930c5d12af…` / `8c7f97b81f6d…` / `1bad9e3b561b…`，`git hash-object` 与 `HEAD:<path>` 双侧相等 3/3）⇒ 连"改了又被改回"的路径也排除。
⇒ **A9 通过**（1 行新增，3 份 0 改动）。

---

## §10 A10 · 制品代次未受影响

| 时点 | bytes | mtime | SHA256 |
|---|---|---|---|
| 构建**前** | 159512 | `2026-09-19 12:54:07.286` | `6A249993ECDE32C9AAB4E651191C5277C198F97DB5C35A603FC68893C67FF734` |
| 构建**后** | 159512 | `2026-09-19 12:54:07.286` | `6A249993ECDE32C9AAB4E651191C5277C198F97DB5C35A603FC68893C67FF734` |
| 卡面基线 | 159512 | `2026-09-19 12:54:07.286` | `6A249993…` |

⇒ **逐字一致（三列全同）** ⇒ 本卡**未重建 jar**；`Gradle :jar UP-TO-DATE` 为独立旁证；备份副本（`.scratch/t13/jar-before-6A249993.jar`）与工作树 jar 逐字节相同。本卡**未起服**，故不存在第二次"是否重建"的时点。

---

## §11 Findings（分级 + requiredFix）

### F-1 · medium · 5 个超出读者前置假设的词**未解释**（与指南自己的承诺不符）

- **问题**：指南 L19-21 明文承诺「**假定你不会**…本文第一次用到它们时，都会用你已经知道的东西打比方讲清楚，不会默认你已经会」，并列出了 `public`/`private`/`final`/`static`/`@Override` 等词。实测：`public abstract` 有就地解释（L114），但 **`void`（L178/L205）、`private` / `final`（L137/L149）、`Paper`（L366）、`编译器`（L297）没有**；`父类`（L297）属弱解释。术语小表（§八，8 词）也未收录它们。
- **证据**：术语首次出现与上下文逐条见 §4.1 第 13/14/16/19/11 行；`static`/`多线程`/`Bukkit` 属"正文 0 次"的空满足（不记缺陷）。
- **影响**：读者在"一行一行看懂"这一节会碰到 `private final String id;` 与 `public final String getId()`（第 14 行判定），在动手第 3 步会碰到"Paper 调试服"（第 16 行）——这两处是**承重路径**（读代码 / 起服）。
- **requiredFix**（二选一，推荐 ①）：
  ① 在**首次出现处**各补一句读者可懂的话，例如：`void`＝"这个函数不返回东西"；`private/final`＝"`private`＝只有这个类自己能用；`final`＝定下来之后不再改"；`Paper`＝"Minecraft 的服务端软件之一，`runServer` 拉起来的就是它"；`编译器`＝"把你写的文字变成服务端能跑的程序的工具"；
  ② 或把这 5 个词补进 §八 术语小表，并在正文首次出现处加**指路**（"（见术语小表）"）。

### F-2 · low · 指南 L197 的代码片段是**多行调用的合并重排版**

- **问题**：L197 把 `RoleInstance.java:124-128` 的四行调用写成一行 `updateTask = platform.scheduler().runRepeating(this::triggerUpdate, 1L, 1L);` ⇒ 读者照抄"那一行"去 `git grep` 会**查不到**（我首轮抽查即因此判 MISSING）。
- **证据**：`git grep -F 'runRepeating(this::triggerUpdate, 1L, 1L)'` = 0；符号级 `this::triggerUpdate` = 1 行（`:125`）、`runRepeating(` = 1 行（`:124`）。
- **requiredFix**：在该片段下补一句"（源码里这次调用写成了四行，这里合并成一行便于阅读）"；或改成逐字的多行片段。

### F-3 · low · **判据缺陷**：卡面 verify(3) 的字面命令对未跟踪新件是结构性盲区（单列，见 §12）

- **问题**：`git grep -nE -f <模式文件> -- "docs/插件文档/初学者指南.md"` **不加** `--untracked --no-exclude-standard` 时，对被 `.gitignore` 忽略 **或** 尚未 `git add` 的新件**恒返 0** ⇒ 与"干净"不可区分（**假绿**）。
- **证据**：同一模式 `A2b`：带旗 **2 行** / 不带 **0 行**（§3.3）。t12 已自曝并改用带旗版本 ✓。
- **requiredFix**：卡面 verify(3) 补一句"新件未跟踪，须 `--untracked --no-exclude-standard`"；本卡开工时该件未跟踪，**结卡时已被跟踪**（`08fe741` 后 `ls-files --error-unmatch` = exit 0）⇒ 现状下字面命令也能工作（我已复验：不带旗时 `verify 合并` = 0 命中、`A2b` = 2 行）。

### F-4 · low · 术语小表（8 词）未覆盖正文出现的全部超前置术语

- **问题**：§八 表只列 类/对象/方法/字段/继承/接口/tick/事件；正文另有 构造函数、注解、增幅档位、`void`、`private`、`final`、Paper、编译器 等（其中构造函数/注解/增幅**在正文解释了**，其余见 F-1）。
- **处置**：t12 已自曝（其未覆盖项⑤）⇒ 本卡登记为**已知限度**；与 F-1 的 ② 方案合并处理即可。

### F-5 · low · "改坏了怎么退回"只覆盖了改数值这一例

- **问题**：§六 第 5 步给的是"把 `200` 改回 `100`，再构建"；对**把别的东西改乱**（删错行、改错文件）的初学者**没有**通用退路（版本控制恢复 / 重新克隆 / 备份）。
- **证据**：§7 第 ⑧ 题只能"部分答出"；全篇无 `git checkout`/`git restore`/`git stash`/备份之类字样（`A5`/`A4` 模式集亦无）。
- **requiredFix**：§六 末补一小段"万一改乱了"（例如：用版本控制把改动还原到上次提交；或先备份整个 `src` 目录再动手）。

### F-6 · low · 判据口径：A2a 的旧侧计数**依赖仪器**

- **问题**：`\b阶段\s?[0-9]` 在 git（POSIX ERE）与 .NET 下 **30 vs 31**；去 `\b` 后两引擎同为 **33 行且行集相同** ⇒ 差异全来自 `\b` 在 CJK 上的语义。
- **requiredFix**：引用 A2a 旧侧计数时**必须写仪器**（"git grep 口径 30 / .NET 口径 31"）；新侧 0/0 一致 ⇒ 结论不受影响。

---

## §12 A11 · 判据缺陷申报（单列）

| 判据 | 是否缺陷 | 依据 / 处置 |
|---|---|---|
| **卡面 verify(3)**：`git grep -nE -f <模式文件> -- "docs/插件文档/初学者指南.md"` | **是（low）** | 对未跟踪件结构性盲区（实测 2 vs 0）⇒ **单列 F-3**，requiredFix = 补 `--untracked --no-exclude-standard` 口径；**不得**以"改期望值为 0"回避（本卡用带旗口径 + 跟踪态复验两条读数） |
| 卡面 verify(2)：`git show … > $env:TEMP\old-arch.md` | **是（low，仪器型）** | PS 5.1 `>` 产出 UTF-16LE（72804 B ≠ blob 53075 B）⇒ 不是"落盘副本"；本卡改用 blob + `cmd /c` 重定向，并**逐字节验证**（`hash-object` = blob 名） |
| 卡面 verify(4)：jar 身份三列 | 否 | 前后逐字一致（§10）；`Get-FileHash` 与 `ReadAllBytes+SHA256` 两法同值 |
| 卡面 verify(5)：`git diff --numstat -- docs/插件文档` | 否（但**口径过时**） | 因队长已提交 ⇒ 该命令结卡时为空；应改用 `git show --numstat 08fe741 -- docs/插件文档`（§9）。属"时点口径"而非缺陷 |
| t12 的痕迹判据（A1–A6 + 合并模式） | 否 | 旧侧 8/8 非 0（27/30/44/31/27/19/4/58）⇒ 有区分力；新侧 7 条 0 + A2b 2 行（逐行判定为版本号） |
| t12 的"不编造 API"主张 | 否 | 本卡独立抽查 27 条，更正 3 条模式侧误报后 **0 条查无实据** |

---

## §13 A12 · 静态级 / 运行级分开写 + 未覆盖项

### §13.1 静态级 vs 运行级

- **静态级（本卡绝大多数）**：痕迹检索、术语逐条判读、代码引用命中、文档 diff/blob 比对、jar 身份、命令与配置核对。
- **运行级（本卡仅 1 项）**：**构建实跑**（§8.2，exit 0 / `BUILD SUCCESSFUL` / `:jar UP-TO-DATE`）。
- **未产生运行级证据的**：起服与游戏内行为（§8.3 未实跑）；指南描述的玩家可见行为（图标外观、命令回显、按键触发）本卡**只做阅读一致性核对**，不构成运行级验证。

### §13.2 未覆盖项（逐条给最小闭合方式）

1. **起服一步未实跑** ⇒ 最小闭合：在有窗口与停机物证纪律的前提下 `.\gradlew runServer`，核 `ShadowHunter Character System enabled.`、端口 25566 监听、`/role set meiqihezi` 后按 §六 第 4 步对照秒数（**注意**：本轮不得重建 jar 的约束已结束，下一批若起服须自出物证）。
2. **读者实际水平未经真人测试**（t12 已申报）⇒ 最小闭合：找一位真·初学者按 §一→§六 走一遍，记录卡点。
3. **类比是否奏效无法客观验证**（唱机/门铃/模板造物机等）⇒ 最小闭合：同上，用"能否复述 tick 与事件"做代理指标（本卡 §7 已给可答性代理，但非真人）。
4. **术语扫描的完整性**：我的术语表来自"指南自述的假定不会清单 + 全文人工通读"，可能漏掉边缘词 ⇒ 最小闭合：加一条机械扫描（把源码关键字表 + 常见 CS 词表作为模式集跑一次，人工判读差集）。
5. **未核对本机 `localhost:25566` 可达性**（需起服）⇒ 最小闭合同上第 1 条。
6. **不覆盖既有 4 份手册的内容质量**（本卡只核"未被改"，不重评其正确性；t10 已复核过 4 份的痕迹面）。
7. **未核 README 加行后另 3 份手册是否需要同步**（t12 的未覆盖⑥）：本卡确认它们不引用文档地图 ⇒ 无同步义务；但"是否需要"属内容判断 ⇒ 最小闭合：由后续整卡统一扫"文档地图"引用。

---

## §14 A13 · 边界声明

- **未改 `src/**`**：`git status --porcelain -- src` = **0 行**。
- **未改被复核的手册**：`docs/插件文档/**` 五个文件**一个字节都没动**（3 份新老手册与 HEAD blob 相同；README 与指南的改动全部来自 `08fe741`，非本卡）。
- **未改既有证据与冻结件**：未触碰 `debug-logs/测试记录/` 下既有文件、`*.log`、停服物证、`debug-logs/回滚快照/**`。
- **零 git 写**：未执行 `add`/`commit`/`checkout`/`restore`/`stash`/`reset`；结卡时全仓 `git status --porcelain` = **0 行**。
- **唯一的写盘**：本报告 + `.scratch/t13/`（被忽略、非交付物：模式文件、旧侧样本、抽查脚本与日志、jar 备份副本）——均已声明。
- **未申请提权**；**未起服**；**唯一运行级动作 = 一次构建**（且未重建 jar，§10）。
- **本报告位于 `debug-logs/**`** ⇒ 被 `.gitignore:84` 忽略、**当前未跟踪** ⇒ 入库需队长 `git add -f`。

---

## §15 结卡自查

- [x] A1–A13 逐条给证据（§0 表 + §3–§14）
- [x] 判据自检**两侧计数都写进报告**（§3.2 旧侧/新侧两列）
- [x] 每条检索判据写明**作用域 + 匹配形态 + 口径**（§3.1、§5）
- [x] 未解释术语**逐条给「术语 + 位置 + 是否已解释 + 依据」**（§4.1），未解释者单列 **F-1**
- [x] 代码引用抽查 **27 条 ≥ 8**，3 条初判 MISSING 全部更正并给依据（§5）
- [x] 静态级/运行级分开写（§13.1）；未覆盖项逐条给最小闭合（§13.2）
- [x] **判据缺陷单列**（§12：verify(3) 未跟踪盲区、verify(2) 重定向非逐字节）且**未通过改判据回避**
- [x] 硬约束遵守：**jar 前后逐字一致**（§10）、**起服未实跑已如实声明**（§8.3）
- [x] 仪器双向自证：正对照（旧侧 8/8 非 0；`debug-logs` 旧侧 26 命中；`A2b` 带旗 2 / 不带 0）+ 自曝两处坑（§2）
- [x] 身份现算；**不内嵌本件自身哈希**；引"代"点名 commit（`90e6c06` / `08fe741` / `0c6930d` + blob 名）
- [x] 零 git 写 / 未改 src / 未改被复核件（§14）
