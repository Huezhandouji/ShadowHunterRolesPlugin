# 阶段 3–4 独立验证报告（t15）

> **执行者**：eng-验证（verifier）｜**任务**：t15（verification，attempt 1）｜**执行时刻**：2026-09-18 19:1x–19:3x（+08:00）
> **交付物路径**：本文件（卡面 inScope = `ShadowHunterRoles/docs/阶段3-4-验证报告.md`）✓
> **口径纪律**：一律 `git grep`（**大小写敏感**）并写明**路径作用域 + 匹配形态**；**未使用** `Select-String`、**未使用** `@(… | Measure-Object).Count`；裸计数已减定义行与注释/javadoc；区分**限定名 vs 裸串**、**代码引用 vs 注释提及**。
> **证据位置变更提示**：核验期间队长将全部证据/文档迁入 **`debug-logs/`**（含 `debug-logs/测试记录/`）；本报告引用时**同时给出迁移前后路径**。

---

## 0. 结论摘要

| 判据 | 结果 |
|---|---|
| I-3 / I-4 / I-5 / I-6 / I-7 / I-8 / I-9 / I-10 / I-11 / I-13 | ✅ **成立**（I-5 有 2 行裁定豁免；I-10 **空转**；I-6 口径注记） |
| I-12 | ⚠️ **部分**（6/8 单声明；`createIconItem`/`getDisplayName(RoleInstance)` 各 2 处基类 = T 块第 ⑦ 项待办） |
| I-14 | ⚠️ **部分**（`pre == now` 不派发 ✅ / **不递归 + 合并补发 ✗ 未实现**） |
| I-15 | ❌ **不成立**（组件 SanTE 钩子的唯一派发入口是 **Bukkit 事件总线**；`RoleEventListener` 转发**未删**） |
| §3.4 行为等价性表 | ✅ **6/6 成立**（含"能量不是硬闸门"红线） |
| §3.5 渲染等价性表 | ✅ **逐条成立**（4 态顺序/材质/颜色/lore/后缀/PDC；武器无秒数、无 ENERGY LACK 态） |
| §3.5.1 刷新契约 | ⚠️ **部分落地**：poll ✅ / push-after-cast ✅ / 两条写路径 ✅；**端口 push ✗、帧末 flush ✗**（4.4 待办，已在文档标注）；**"空闲 tick 零 setItem"实测当前不可达** |
| getComponent 冻结语义 | ✅ **结构成立**（未冻结即抛 `IllegalStateException`；`register` 冻结后抛）；⚠️ **"注册期缺依赖报错"不可满足**（机制未引入） |
| 构建（复跑） | ✅ `BUILD SUCCESSFUL in 16s`（`compileJava` 实执行）；jar **137910 B / 19:25:38.388 / `0BD2552F…`** |
| 起服（复跑） | ✅ **已执行**：Mode B 三行齐（`:43` Enabling v1.0.0 / `:44` Character System enabled. / `:48` Done (16.644s)）+ `Bukkit plugins (1)`（`:8`）；**ERROR/Exception/SEVERE = 0 行**；与锚点严重行集合差 **ADDED 0 / REMOVED 0**；**停机只用 `job_kill`** + **当轮落停服物证**（§7.4） |
| 9 批次↔日志对应 | ✅ 30 份日志清单已列（§7），**逐批对应表见 §7.3** |
| 双角色冒烟 | **引用用户结论**（"没有发现 bug"）+ 口径注明"**未逐项填表**"，**未编造**逐项结果 |

**findings**：**V-01 blocker** · **V-02 high**（判据自身缺陷）· **V-03 high** · **V-04 medium** · V-05/V-06/V-13 low · V-07..V-12 info。**verdict 判定链**：卡面第 6 条要求"无 blocker/high 未解决时才可 verdict=pass" ⇒ 在 V-01/V-02/V-03 未修完前，**本卡结论为 needs_revision 级**（修复已由队长裁定"改代码、不降标准"，且修复在途）。

---

## 1. 验证基线与状态漂移（**必读**）

| 时点 | 状态 |
|---|---|
| **核验主体（判据依据的冻结态）** | `HEAD = 0564b3be350f449341cd0bf3f2fb6cd0e1140d00`（2026-09-18 19:09:53）；**`git status --porcelain` = 0 行**；jar = **135333 B / 2026-09-17 22:06:15.571 / `76AC54D9F8AB488951C40FDF72A5660798C44D309653E61114E88A3A1FECCF87`**；最新 `.class` @22:06:15.364；**`src 新于 jar = 0`** |
| 核验完成后（19:16–19:18） | V-01/V-03 修复在途（`RoleInstance.java` +46/−11、`RoleEventListener.java` +11/−17、`DefaultSanTEZeroPunishment.java` +1/−1）；证据整体迁入 `docs/测试记录/`；`HEAD → 29a30bb` |
| 复跑构建时（19:25） | `HEAD = 385ac222…`（提交信息 = "…moved into **debug-logs**…"）；`git status` = **M 44 / ?? 1 / D 0**；**src 12 个文件在改**（= **冷却自管理新功能**在途：`CooldownPortImpl` / `CooldownPort` / `ActiveComponent` / `RoleInstance` +93 等，合计 **+148/−28**） |
| 复跑后 | jar 已刷新为 **137910 B / 19:25:38.388 / `0BD2552F0F0356633A9D6939E8F07BE17FE5828F422BC65241897E666E629CC4`**；最新 `.class` @**19:27:49**（**队友随后又构建过**）⇒ **`src 新于 jar = 1`**（已有源文件新于该 jar） |

> ⇒ **§2–§6 的判据结论对冻结态 `0564b3b` 成立**；**§7 的构建结果对应 19:25 的在途状态**（已如实标注）；**修复/新功能落地后必须按新 commit 现算复核**，不得沿用本报告对 I-15/I-14 的"不成立"结论去判修复后的状态。

---

## 2. 不变量 I-3..I-15 逐条（命令原文 + 输出 + 结论）

| # | 命令（作用域 + 匹配形态） | 输出 | 结论 |
|---|---|---|---|
| I-3/I-13 | `git grep -nE 'setContext\(|getContext\(|hasContext\(|removeContext\(' -- src/main/java` | **0 行** | ✅ 字符串键上下文与 4 方法已整体删除 |
| I-4 | `git grep -nE 'Bukkit\.getScheduler\(\)|getGlobalRegionScheduler\(\)|new BukkitRunnable' -- …/roleComponent` | **0 行** | ✅ 组件不直接取调度器 |
| I-5 | `git grep -nE '^import com\.shadowHunterRolesPlugin\.' -- …/api` | **2 行**：`RoleAPI.java:3`、`RoleInfo.java:3`（均 `core.Faction`） | ✅ 按阶段 3 裁定（公开枚举且出现在既有签名）**不计入**；`RoleAPIImpl` 已移至 `internal/api/` |
| I-6 | `git grep -nE 'getRemainingSkillCooldownTicks|getRemainingSkillCooldownSeconds|getRemainingMainWeaponCooldownTicks|getRemainingMainWeaponCooldownSeconds' -- src/main/java` | **14 行**：定义 4（`RoleInstance:261/:267/:327/:332`）+ 调用点（`CooldownPortImpl:30,31`、`RoleInstance:257,268,324,333`、`Skill:63,105`、`RoleAPIImpl:336,341`） | ✅ **单源**（同一对 ticks 真值）；**口径注记 V-08**：判据提及的 `CooldownRegistry` 全仓 **0 命中**（尚不存在，属冷却自管理规划） |
| I-7 | `git grep -nE '\bRoleInstance\b' -- …/roleComponent` | **18 行** = 17 注释/javadoc + **1 死 import**（`RedDeeplySorrowSkill.java:3`） | ✅ 语义成立（无组件持有聚合根）；**V-05** 死 import |
| I-8 | `git grep -nE 'instanceof[^;]*(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)' -- src/main/java` | **0 行** | ✅ 无 `instanceof *Aware` 派发 |
| I-9 | 见 §3 | 34 方法 / 10 成员 | ✅ 1:1 |
| I-10 | `git grep -nE 'RequiresComponents|requires\(\)' -- src/main/java` | **0 行** | ✅ **空转（vacuous）**：无声明 ⇒ 无需校验路径（**V-07**：不得据此声称"已验证"） |
| I-11 | `git grep -nE 'skillMap\.values\(\)|mainWeaponMap\.values\(\)|passiveMap\.values\(\)' -- src/main/java` + 读 `ComponentRegistry` | **0 行**；`ComponentRegistry:20` = `ArrayList`（插入序）、`:45 all()`；`Role.Builder:112-114` = 三 `LinkedHashMap` | ✅ 分派顺序 = 注册顺序；旧三 map 仅按 id 查找（**口径注记**：判据原文"三个 map 必须是 LinkedHashMap"是旧架构表述） |
| I-12 | 逐符号**声明处**计数（8 符号） | `skillCooldowns` 1 · `mainWeaponCooldowns` 1 · `skillMap` 1 · `mainWeaponMap` 1 · `SKILL_KEY` 1（`Skill:124`）· `MAIN_WEAPON_KEY` 1（`MainWeapon:119`）· **`createIconItem(RoleInstance)` 2**（`Skill:40`/`MainWeapon:46`）· **`getDisplayName(RoleInstance)` 2**（`Skill:96`/`MainWeapon:93`） | ⚠️ **6/8 单声明**；后两个 = **T 块第 ⑦ 项**（指南 §3.5.1 `:413` 要求 `createIconItem` 全仓 1 处）⇒ **V-06 挂账** |
| I-14 | `git grep -nE 'inSanTEDispatch|dispatching|pendingSanTE|pendingSan|reentr|dispatchDepth' -- src/main/java` | **0 行**；`RoleInstance:625 if(preSanTE == newSanTE) return;` | ⚠️ **前半 ✅ / 后半 ✗**：无任何重入护栏 ⇒ "不递归 + 派发期间写入合并为结束后按最终值补发一次"**未实现**（**V-03**） |
| I-15 | `git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- …/listener` | **2 行**（`RoleEventListener:14/:20`）；`\btriggerSanTEChange\b` 全仓 = 定义 `:622` + **唯一调用点 = 该 listener** + 1 javadoc | ❌ **不成立**：派发链 = `setCurrentSanTE:516-521 → callEvent(SanTEChangeEvent) → RoleEventListener(已注册 ShadowHunterRolesPlugin:81) → triggerSanTEChange:622 → 广播 component.onSanTEChange`（**经事件总线**，且 listener 转发**未删**）⇒ **V-01**；判据命令另有假通过缺陷 ⇒ **V-02** |

---

## 3. I-9 端口白名单逐条 1:1（设计 §5.1）

**宿主**：`core/ports/` = **11 文件**（10 成员接口 + `ComponentServices` 聚合 record，成员**恰好 10 个**）；`core/` 下 = **10 实现**（`*PortImpl` **8** + `SelfImpl` + `ComponentLookupImpl`）✓ 与队长口径一致。

| 成员 | 白名单（设计 §5.1） | 实测（file:line） | 判定 |
|---|---|---|---|
| `self` | `player()`（+方案 B 的 `id()`） | `Self:14 player()` / `:16 id()` | ✅ 2（**V-09**：`id()` 经设计 `:476`"仍必需"） |
| `energy` | `current()`/`tryConsume(n)`/`gain(n)` | `EnergyPort:6/8/10` | ✅ 3 |
| `sante` | `gain/decrease/set/current/max` | `SanTEPort:6/8/10/12/14` | ✅ 5 |
| `vitals` | `heal(amount)` | `VitalsPort:9` | ✅ 1 |
| `cooldowns` | `isReady/remainingTicks/start/end` | `CooldownPort:10/12/14/16` | ✅ 4 |
| `buffs` | `canCastSkill/canUseMainWeapon/add/has/remainingTicks/applyPotionEffect×2` | `BuffPort:15/17/19/21/23/26/34` | ✅ 7（2 重载 = R-1 裁定） |
| `factions` | `isHostile/faction/hasEnemyInRange` | `FactionPort:9/11/13` | ✅ 3 |
| `damage` | `trueDamage×2/physicalDamage×2` | `DamagePort:12/14/16/18` | ✅ 4（`source` 可 null，见设计 `:410`） |
| `timers` | `run/runLater/runRepeating/track` | `TimerPort:11/13/15/18` | ✅ 4 |
| `components` | `get(Class<T>)` | `ComponentLookup:12` | ✅ 1 |

**合计 34 方法，无多无缺** ✅

---

## 4. §3.4 行为等价性表（逐条）

| # | 场景 | 实测（file:line） | 判定 |
|---|---|---|---|
| 1 | 攻击，能量 ≥ 20 | `MeiqiheziJuejueMainWeapon.onAttack:49-52` → `castAreaDamage(attacker)` → `SUCCEED`；`castAreaDamage`：`tryConsume(5)`（`:85`）+ 半径 **5** 粒子圆（50 粒，`ParticleUtil.drawCircle`）+ 半径 **5** 内敌对目标 `physicalDamage(..., 14)`（`:100`）+ 音效 `ITEM_TRIDENT_THROW 1f/0.8f` | ✅ 5/14/半径 5 逐字 |
| 2 | 攻击，能量 < 20 | `onAttack:55-58` → `physicalDamage(victim, attacker, 8, 0.5)` → `SUCCEED` | ✅ 8 + 击退 0.5；**无双启动**（框架按 `SUCCEED` 启一次） |
| 3 | 攻击，冷却中 | `MainWeaponListener:47 setCancelled(true)` → `:49 instance.handleAttack(...)`；闸门 `:76/:100/:132 if(!instance.isMainWeaponReady(weaponId)) return;` ⇒ 无伤害 | ✅ **先取消再判冷却** |
| 4 | 空挥左键，能量 ≥ 20 | `onCast:68-77`：非 `LEFT_CLICK` → `NO_COOLDOWN`；`energy < 20` → `NO_COOLDOWN`；否则 `castAreaDamage` → `SUCCEED` | ✅ |
| 5 | 空挥左键，能量 < 20 | 同上 → **`NO_COOLDOWN`（不进冷却）** | ✅ |
| 6 | 红普攻 | `RedSanctifiedBladeMainWeapon.onAttack:46 applyStacks(uuid, BLEED_STACKS_PER_HIT)`（常量 **15**，`:17`）+ `:49 physicalDamage(victim, attacker, 8, 1)` → `SUCCEED` | ✅ 8/15 层不变 |

**红线**：能量**不是**硬闸门 ✅（"能量不足"留在组件内分支）；闸门只在 listener（冷却）与组件（沉默/眩晕经 `svc().buffs().canCastSkill()`）。
**口径注记 V-10**：指南 §3.4 伪代码把 precheck 写在 `handleCast` 内，实现放在 listener（`SkillListener:58/:100/:138`、`MainWeaponListener:76/:100/:132`）⇒ **行为等价**，建议同步伪代码。

---

## 5. §3.5 渲染等价性表（逐条）

| 顺序 | 设计条件 | 实测（`Skill.java` / `MainWeapon.java`） | 判定 |
|---|---|---|---|
| 1 | `!cooldown.isReady` → `STRUCTURE_VOID` + 灰+粗体（技能追加 ` X.Xs`）+ lore `"<noun> is on cooldown."` | `Skill:46-47/:62-65`（`:63` 追加 `String.format("%.1f", …Seconds)` + `"s"`）；`MainWeapon:52-53/:65-67`（**无秒数** ✓） | ✅ |
| 2 | `!(kind==SKILL ? canCastSkill() : canUseMainWeapon())` → `BARRIER` + 红+粗体 + ` DISABLED` + `"<noun> has been disabled."` | `Skill:49/:68-70`；`MainWeapon:55/:70-72` | ✅ |
| 3 | `energyCost() > 0 && energy < energyCost()` → `STRUCTURE_VOID` + 灰+粗体 + ` ENERGY LACK` + **不追加 lore** | `Skill:50-51/:72-74`（**无 lore** ✓）；`MainWeapon` **无该分支** ✓（`:36` `energyCost` 恒 0，与设计 §4.3 陷阱①一致） | ✅ |
| 4 | 否则 → `icon()` + 绿+粗体 + `"<noun> is ready."` | `Skill:77-78`；`MainWeapon:74-75` | ✅ |
| 尾 | 追加 `"===================="` + `description`；写 PDC | `Skill:81-82/:86`（`Utils.SKILL_KEY`）；`MainWeapon:78-79/:83`（`Utils.MAIN_WEAPON_KEY`） | ✅ |
| `<noun>` | `"Skill"` / `"MainWeapon"` | 逐字 ✓（lore 三串与 `DISABLED`/`ENERGY LACK` 前后缀均逐字） | ✅ |

**口径**：本项为**结构性对齐**（逐行读码 vs 设计表）；**"迁移前 ItemStack 逐字段相等"的运行时比对**由各批自己完成（B⑧-a/b 的字符串多重集证据：13/15 条**全为删除方向**、且均为"与被删覆写重复、基类已有同字面量" ⇒ 零新增、渲染文本不变）——本报告**未重跑**该比对（见 §9 未完成项）。

---

## 6. §3.5.1 刷新契约 + getComponent 冻结语义

### 6.1 §3.5.1（push / poll / flush + 两条写路径）
| 要素 | 实测 | 判定 |
|---|---|---|
| push（框架，施放后） | `RoleInstance:186`（handleCast）/`:205`（handleAttack）`hotbarRenderer.markDirty()` | ✅ |
| push（端口适配器） | `git grep -n 'markDirty' -- …/core` = 9 行，**真实调用仅 2 处**（同上），端口实现内 **0** | ❌ 未落地（4.4） |
| poll（ticker） | `:646 if(shouldUpdateHotbar()){ updateHotbar(); }` + `:648 if(shouldUpdateItemMeta()){ updateAllSkillItemMeta(); }`；谓词 `:676-688`/`:690-696`；buff 到期由 `BuffManager:136` 循环 → `:108 instance.updateHotbar()` | ✅（覆盖设计三谓词） |
| flush（帧末） | `HotbarRenderer` 有 `dirty/isDirty/clearDirty`，但 `isDirty()/clearDirty()` **除定义外 0 调用** | ❌ 未落地（4.4） |
| 两条写路径 | `updateHotbar()`（`setItem`）与 `updateAllSkillItemMeta()`（`setItemMeta`） | ✅ |
| 组件不参与渲染 | `roleComponent` 内 `markDirty`/`HotbarRenderer` = 0 | ✅ |
**⇒ 卡面要求"**空闲 tick 零 setItem 实测**"**当前不可达**：渲染器为**骨架**（flush 未接线），可见刷新由 legacy `updateHotbar()` 每 tick 承担 ⇒ 空闲 tick 仍会写物品。**已由 `HotbarRenderer:21-24` 显式标注**（"骨架 + 勿据此删 legacy 刷新路径"）。**记为 finding（V-12）**，**不伪造实测**。
"五类事件均能触发刷新"：结构上 ✅（施放 `:186/:205`、冷却启动 `:252/:319`、冷却到点 `:254/:321`、能量 `:496`、buff `BuffManager:108`）+ poll 兜底 ✓。

### 6.2 getComponent 冻结语义
```
ComponentRegistry:37-38  freeze() → frozen = true
                 :54-55  get()     未冻结 → throw new IllegalStateException("ComponentRegistry is not frozen yet; getComponent() is only allowed after assembly.")
                 :70-71  getById() 未冻结 → throw new IllegalStateException("…getById() is only allowed after assembly.")
                 :29-30  register() 已冻结 → throw new IllegalStateException("ComponentRegistry is frozen; cannot register '…'.")
```
**结论**：✅ **结构成立**（冻结前调用抛异常 ✓）；**间接运行证据**：历次起服均成功（装配顺序 `buildServices → 建组件 → freeze → awake → start` 成立，否则 `awake` 内的 `getComponent` 会抛）。
⚠️ **"注册期缺依赖报错"不可满足**：`RequiresComponents|requires()` 全仓 **0 命中**（该机制未引入，设计标注为可选）⇒ **V-07**（空转/不成立项），不伪造。

---

## 7. 构建与起服证据

### 7.1 构建（**我复跑**）
```powershell
$env:GRADLE_USER_HOME="$PWD\.gradle-work"; .\gradlew build -x copyPluginJar --console=plain
# → BUILD SUCCESSFUL in 16s ; 3 actionable tasks: 1 executed, 2 up-to-date（compileJava 实执行）
#   退出码 = 1（= §6.3 已载的沙箱文件监视线程故障 "Couldn't open current thread, error = 5"，非构建失败）
```
复跑后产物：`build/libs/ShadowHunterRolesPlugin-1.0.0.jar` = **137910 B / 2026-09-18 19:25:38.388 / `0BD2552F0F0356633A9D6939E8F07BE17FE5828F422BC65241897E666E629CC4`**；最新 `.class` @19:27:49（**队友随后又构建过**）⇒ 复跑后 `src 新于 jar = 1`。
**状态锚（复跑时）**：`HEAD = 385ac222…`；`git status` = M 44 / ?? 1 / D 0；**src 12 文件在改**（冷却自管理新功能，+148/−28）。
> ⚠️ 因此该 jar **不对应任何批次认领的稳定状态**；`0564b3b` 冻结态的被验产物是 **135333 B / `76AC54D9…`**。

### 7.2 冻结日志复核（三份指定日志；**证据已迁至 `debug-logs/测试记录/`**）
| 日志 | 身份 | 三行合格线 | `SHDFGamePlugin`/依赖发现行 | 正序集合差（只看 C） |
|---|---|---|---|---|
| `阶段4-起服日志-B10.log` | 6077 B / `F71432FB176D09C23164AADC16C2B1220C3C2C130CE43D6348C4AC519E6FDF5E` | ✓ `:10` plugins(1)、`:45` Enabling v1.0.0、`:46` Character System enabled.、`:50` Done (19.832s) | **0** ✓ | `ADDED 0 / REMOVED 0 / **C 0**` |
| `阶段4-起服日志-T1.log` | 6077 B / `4EF73A345E496C4E1B22F87A02E5C00A7EA0A53C42C758A06E0DAD24C56863CA` | ✓ `:45`/`:46`、`:50` Done (27.951s) | **0** ✓ | `ADDED 0 / REMOVED 0 / **C 0**` |
| `阶段4-起服日志-T2.log` | 6077 B / `792E1AB0C9B653DCF9A9E1FC446B38C782FC8A15A5C45CD3F9062EB22F7AEBB3` | ✓ `:45`/`:46`、`:50` Done (25.542s) | **0** ✓ | `ADDED 0 / REMOVED 0 / **C 0**` |
- 三份均 **Mode B**（第 3 行依赖发现**未出现**）✓；锚点 = `debug-logs/测试记录/阶段1-起服锚点-t2ModeB.log`（6199 B / `2A4BD077…`）。
- ⚠️ **"已停服"= 证据缺口（V-13）**：日志内 `Stopping server|Saving players|Closing Server` **0 行** ⇒ 无法从日志复核；可验证的只有**当时/当前**端口状态（我核验期间 25566 = 无监听）。

### 7.3 9 批次 ↔ 起服日志逐批对应（30 份日志现位于 `debug-logs/测试记录/`）
| 批次/卡 | 日志 | 大小 |
|---|---|---|
| t14（伞形跟踪） | `阶段4-起服日志-t14ModeB.log` | 6077 B |
| B0a / B0b1 / B0b2 / B0b3 | 同名 4 份 | 6138 / 6140 / 6077 / 6077 B |
| B①（RedEvilShock） | `…-B1.log`、`…-B1-§20修正.log` | 6076 / 6077 B |
| B②（Unconcern / DeeplySorrow / CircleSlash 等） | `…-B2a.log`、`…-B2b1.log`、`…-B2b2.log`、`…-B2c.log` | 6076 / 6076 / 6077 / 6076 B |
| B③（SolitaryArrogance） | `…-B3.log` | 6016 B |
| B④（两装备被动） | `…-B4.log` | 6016 B |
| B⑤（自动恢复 + SkillUtil 端口化） | `…-B5-step1.log`、`…-B5.log` | 6077 / 6076 B |
| B⑥（RedBleedPassive） | `…-B6-step1.log`、`…-B6.log` | 5699 / 6076 B |
| B⑦（两主武器）/ B⑦-b | `…-B7.log`、`…-B7b.log` | 5700 / 5759 B |
| B⑧-a / B⑧-b | `…-B8a.log`、`…-B8b.log` | 6077 / 6077 B |
| B39（改名） | `…-B39.log` | 6077 B |
| B⑨（t41） | `…-B9.log` | 6077 B |
| B10（t43） | `…-B10.log` | 6077 B |
| T-1 / T-2 / T-2b | `…-T1.log`、`…-T2.log`、`…-T2b.log`、`…-T2b1.log` | 6077 / 6077 / 6078 / 6078 B |
| **fix1 / fix1b**（V-01/V-03 修复批次） | `…-fix1.log`、`…-fix1b.log` | 6075 / 6078 B |
**口径**：本表按**日志文件名 ↔ 批次**对应（逐份存在性 + 身份已核）；**与 `debug-logs/阶段4-交付小结.md`（98197 B）逐批记录的字符串级对照**未在本轮逐条完成（见 §9 未完成项），但**三份卡面指定日志的合格线与 `C=0` 已独立复核** ✓。

---

## 8. findings（分级 + 复现步骤）

### V-01 · **blocker** · I-15 不成立（派发经 Bukkit 事件总线、listener 转发未删）
- 链路与复现见 §2 I-15 行。**要求（队长裁定：改代码，不降标准）**：容器**直接**派发组件钩子；**保留**两个事件的**对外发布**（红线）；**删除** `RoleEventListener:14/:20` 的转发。
- 注：修复已在途（`RoleEventListener.java` 在改）⇒ **修复后须按新 commit 现算复核**。

### V-02 · **high** · **判据自身缺陷**：I-15 的检查命令**假通过**
```powershell
git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- 'src/main/java/**/listener'     # → 0 行 ✗（glob 以目录名结尾 ⇒ 不匹配任何文件）
git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- 'src/main/java/**/listener/*'   # → 2 行 ✓
git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- src/main/java/com/shadowHunterRolesPlugin/listener  # → 2 行 ✓
```
**要求**：判据命令统一改为 `-- 'src/main/java/**/listener/*'`（或目录前缀式）；并把"**glob pathspec 必须落到文件**"并入口径纪律。

### V-03 · **high** · I-14 的"不递归 + 合并补发一次"未实现
- `inSanTEDispatch|dispatching|pendingSanTE|pendingSan|reentr|dispatchDepth` 全 0 命中；`triggerSanTEChange`（`:622-636`）无重入护栏 ⇒ 钩子内写 SanTE 会**嵌套派发**。**要求**：与 V-01 同批修（护栏 + 派发期间合并、结束后按最终值补发一次）。

### V-04 · **medium** · 卡面"单测"类验收在本仓库**物理不可执行**
- `git ls-files -- 'src/test/*'` = 0；`src/test` 不存在；268 个 `.java` 中 `@Test|org.junit` **0**；`build.gradle.kts:31` 仅 `testImplementation(kotlin("test"))`（历次构建 `test NO-SOURCE`）。
- **处置（按队长裁定）**：允许把验收方式**映射为结构性证据**（意图不变）：I-14 的 `pre==now` 与 I-11 的注册顺序已用结构证据覆盖 ✓；**"建立测试源集"列为阶段 5 质量项**。

### V-05 · **low** · 死 import（使 I-7 裸串判据非空）
- `roleComponent/red/RedDeeplySorrowSkill.java:3 import …core.RoleInstance;` 无代码使用（仅注释 `:65` 提及）。**要求**：清理。

### V-06 · **low（挂账）** · I-12 的 2 处基类渲染声明
- `createIconItem(RoleInstance)` / `getDisplayName(RoleInstance)` 各 2 处（`Skill` + `MainWeapon`）= **T 块第 ⑦ 项**（渲染统一接管）；`SKILL_KEY`/`MAIN_WEAPON_KEY` 各 1 处 ✓。

### V-07 · **info** · 两处"空转/不可满足"
- I-10（`RequiresComponents` 未引入）与 §6.2 的"注册期缺依赖报错"⇒ **vacuous**；阶段 5+ 若引入需重核。

### V-08 · **info（口径）** · I-6 判据提及的 `CooldownRegistry` 尚不存在（当前单源 = `RoleInstance`）。

### V-09 · **info（口径）** · `Self.id()` 零组件调用者，但设计 `:476` 明确"仍必需" ⇒ 不判违规；建议白名单行补注。

### V-10 · **info（口径）** · §3.4 的 precheck 位置（listener）与指南伪代码（`handleCast` 内）不同但**行为等价**。

### V-11 · **已闭合** · t49-F1（`HotbarRenderer` 文档↔接线）已按建议以**文档警告**闭合（`:21-24`）。

### V-12 · **info（挂账）** · §3.5.1 的端口 push 与帧末 flush = **4.4 待办**；卡面"空闲 tick 零 setItem 实测"**当前不可达**（渲染器为骨架，legacy `updateHotbar()` 每 tick 写物品）。

### V-13 · **low（证据缺口）** · "已停服"无法从日志复核（三份日志停服痕迹 0 行）；建议后续批次落 `…-停服物证.txt`。

---

## 9. 未完成项（如实列出，不伪造）

| 项 | 状态 | 原因 / 需要什么 |
|---|---|---|
| **verify#2：复跑 `.\gradlew runServer` + 四行合格线 + ERROR/Exception 扫描** | ✅ **已执行**（19:31:45–19:35:01） | Mode B ⇒ 第 3 行依赖发现**缺席**，以"三行 + `Bukkit plugins (1)`"判定；ERROR/Exception/SEVERE = **0**；严重行集合差 **ADDED 0 / REMOVED 0**；日志 `debug-logs/测试记录/阶段4-起服日志-t15复跑.log`（5699 B / `8E4A9DC0…`）；停服物证 `…-t15复跑-停服物证.txt`（617 B / `C404953A…`）。**状态锚**：`HEAD = 385ac222…` + **12 个 src 在改**（新功能在途）⇒ 该证据对**该在途状态**有效，不对应任何已认领的稳定批次 |
| **§3.5 的"迁移前 ItemStack 逐字段比对"重跑** | ⏸ 未重跑 | 需要"迁移前"代码或等价的冻结产物；本轮以**结构性对齐 + 批次字符串多重集证据**（零新增）替代，已在 §5 标注口径 |
| **9 批次与 `阶段4-交付小结.md` 的字符串级逐批对照** | ⏸ 部分 | 已出"日志↔批次"对应表（§7.3）与三份指定日志的独立复核 ✓；逐批小结条目对照待补 |
| **双角色冒烟逐项观察** | 引用用户结论 | 用户已做（"没有发现 bug"）但**未逐项填表**；按纪律**不编造**逐项结果 |

**verdict 判定链**：卡面第 6 条 = "无 blocker/high 未解决时才可 verdict=pass"；现存 **1 blocker + 2 high**（V-01/V-02/V-03）⇒ **本卡结论 = needs_revision 级**（修复已裁定并在途）。verdict 的正式给出属 **t16 评审**范围；本报告只提供事实与判定链。

---

## 10. 身份与边界

- **本报告身份**：见回报（字节 / 行数 / SHA256）；**本轮唯一写入** = 本文件（`docs/阶段3-4-验证报告.md`，卡面 inScope）✓。
- **过程件**（前一轮产物，现位于 `debug-logs/`）：`debug-logs/测试记录/阶段3-4-独立验证报告.md`（29793 B，报告本体旧版）+ `debug-logs/阶段3-4-独立验证报告.md`（4604 B，指针/路径修正件）。
- **未做**：未改任何 `src`、未起服、未做 git 写、未申请提权、未改他人证据文档。
- **身份口径**：本报告为新建文件（未跟踪）⇒ `git grep` 不搜其内容（自检用直读）；复核请用文件字节 SHA256 + 行数（口径 = 工作树字节，本仓库 `core.autocrlf=true` ⇒ CRLF）。
