name: ShadowHunterProbe
version: '1.0.0'
main: com.shadowHunterProbe.ShadowHunterProbe
api-version: '1.21.11'
# 探针依赖主插件（要读它的容器/能力面 ✓）；主插件**不**依赖探针 ✓（单向 ✓）
depend: [ShadowHunterRolesPlugin]
commands:
  probe:
    description: ShadowHunter 运行级探针（工装件；t129）
    usage: /probe <install|dup|throw|status|list> ...
