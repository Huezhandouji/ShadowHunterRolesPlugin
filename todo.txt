重构计划：
贯彻组件设计模式，ComponentService只应该提供获取玩家实例和组件服务（组件查找，动态组件添加）功能，其他功能应
做成组件。所有系统级能力也该做成组件而不是能力接口，如果可能。角色实例应该被设计成组件的容器

举个例子：
比如现在的物品渲染的能力接口应该做成一个物品渲染的组件，提供在玩家Inventory指定位置渲染不可动物品的支持，
而且提供回调。
Faction也可以做成组件。

组件在装配时可以检查自己需要的依赖（其他组件），如果发现没有获取到依赖应该抛出异常让框架阻止当前角色的加载和注册。
