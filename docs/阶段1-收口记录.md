# 阶段 1 收口记录（t8 整合结案）

> **任务**：t8 — 阶段 1 整合结案：台账与验收清单同步 + after-phase1 快照 + 阶段 2 准入判定
> **inScope**：`docs/最终重构指南.md`、`docs/阶段1-收口记录.md`（本文件）、`docs/回滚快照/after-phase1/`
> **依据**（全部为他人独立产物，本记录只做整合与交叉核对，不重新判定被验事实）：
> - t7《阶段0-1-验证报告》= `docs/阶段0-1-验证报告.md` —— **章节式引用**（§6.6："锁死器物用哈希，指向论证用章节"）：**§4 I-16 / §5 多重集 / §6.1 O 项表 / §6.2 import / §0.1 F-3 时间线**；verdict = **pass**。
>   · **身份不在本记录钉死**：该报告在收口期仍在补精度（v1 → v2 `4435c2f`（23 076 B / `D5FAE4A2…C275`）→ v3 `3fe4372`（26 721 B / `D61E388C…4978`）→ v4（28 366 B / `6F323451…A32C`）→ **末版身份待出**）；**要写身份，以现算为准**。
> - t10《阶段0-1-评审报告》= `docs/阶段0-1-评审报告.md` —— 章节式引用：**§1.2 清单对照 / §2.2 药水穷举 / §2.3 B-1 可见行为清点 / §5 必须挂账**；verdict = **pass**。
>   · **评审覆盖声明（eng-审阅 原版措辞，本记录原样引用）**：t10 评审覆盖的 t7 报告记为 **v2（`4435c2f`，23 076 B，`D5FAE4A2…C275`）**——实际通读的正文主体自 v1 即存在；此后 t7 报告补 §0.1/§5.3/§6.1 至 **v3（`3fe4372`，26 721 B，`D61E388C…4978`）**。**verdict 未变（`:4` 与结尾均 `pass`）、4 low 含 t7-L1 必须挂账**；补记属 verifier 补完自身清单项，**不构成"评审失效"**。
> - 交付小结：`docs/阶段0-交付小结.md`、`docs/阶段1-交付小结.md`（38 004 B / 23:07:59 / `965BFD25…4409`）、`docs/阶段1.8-交付小结.md`
> - 回滚演练：`docs/阶段0-回滚演练记录.md`（t12）
> - **仪器身份与结果绑定（t8 交叉核对，2026-09-16）**：t6 结果由探针 `EFDBF9EB95949FAD73A63DAC9F3216111BFF87FCAA8BD0B9E48617C1A84C9770`（22 223 B）产出，**四路身份链全等** = `result.json.meta.scriptIdentity.sha256` = 磁盘脚本 = 仓库受管副本（`docs/回滚快照/工装/t6-quit-persistence-EFDBF9EB-22223.js`）= 执行侧归档（`.scratch\bot\instrument-versions\`）。**t7 的 `verdict=pass` 与报告内全部运行证据均绑定该版**（t7 入场前自行发现版本变更并**主动改绑**，时间线见 `docs/阶段0-1-验证报告.md` §0/§0.1）⇒ 不存在"PASS 覆盖错误版本"的缺口。

---

## 1. 台账同步（指南 §2.3）

**做法**：§2.3 原表是"开工时的开放项快照"，**正文不改**（保留历史可追溯），在其后新增 **`#### 2.3.1 阶段 0–1 结案状态`**，逐项标注 状态 + 关闭依据（证据位置）。

| 状态 | ID | 关闭依据（一句话） |
|---|---|---|
| **已关闭（12 条）** | O-1、O-2 | t7 §6.1：两个装备被动 `stop` 四槽与 `start` 逐件对称 |
| | O-3、O-4、O-5 | t7 §6.1：三处 `new BukkitRunnable` 全部入句柄 + `stop` 取消（**O-5 的 Folia 粒度半项 → 阶段 2**，t7-L3 登记待 t9） |
| | O-6 | t7 §6.1：`NO_TASK_ID` + 先取消再起 + `isCancelled` 早退 + `stop` 复位 |
| | O-7 | t7 §6.1 + t10 §2.2：药水账本记账/回收；**触点穷举无漏网** |
| | O-8 | t7 §6.1：`onDisable → clearAllPlayersRole() → 逐个 instance.clear()` |
| | O-9 | t7 §6.1：`RedEvilShockSkill.java:35` 判空在位 |
| | O-10、O-11、O-12 | t10 §1.2：三表跨类型去重 + 默认空表归一 + fail-fast（§10 裁决 1） |
| | O-13 | t7 §6.1：`start*Cooldown(getId(), <数字>)` = 0；`getCooldownTicks()` = 14 处 |
| | O-14 | t7 §6.1/§5.2：`"xxb111"` 从字符串多重集消失 |
| | O-17 | t7 §6.1：`SKILL_KEY` 常量在位（按 §2.4 保留至阶段 4.4） |
| | O-18 | t7 §6.1：`isReady` 命中 = 0 |
| | **O-25** | t7 §6.1 + §4：`onPlayerQuit → clearRole(uuid)`；I-16 由独立探针**运行实测**覆盖 |
| **仍开放（8 条，各有归属阶段）** | O-15、O-16、O-19、O-20 | 阶段 4（施放管道 / listener 瘦身 / 组件复位通道） |
| | O-21、O-21b、O-22、O-23 | **阶段 5**（性能组） |
| | O-24 | **阶段 3**（`RoleAPIImpl` 移位） |

> **核对方式**：上表每条关闭依据均可在 t7 **§6.1** 或 t10 **§1.2** 找到同一处引用（**同一文件:行号或同一内容片段**）；行号随阶段漂移，故判据以内容片段为准（§6.6）。
> **计数口径（避免与 t7/t10 的数字打架）**：本表为 **12 行 / 17 个 O 号**；**t7 §6.1 的覆盖 = 11 项（10 个表格行）**（O-1、O-2、**O-3/O-4/O-5 合并一行**、O-6、O-7、O-8、O-9、O-13、O-14、O-17、O-18、O-25）；t10 §1.2 为逐条对照行、按其自身计数。

---

## 2. 验收清单同步（指南 §7）

**阶段 0：5/5 勾选；阶段 1：10/10 勾选**（指南实际为 10 项 —— t10 报告写作"11/11"是按行计数，已在 §7 收口注中说明，避免下游把两个数字当矛盾）。每条勾选后附 **`→ 证据：…`** 指向 t7/t10 的具体小节。

- 阶段 0 证据集中在 t10 §1.1（装备对称、B 流前后对比、仅 paper-api 编译 + PollutionProbe 反向对照、Mode B 与回滚演练记录）。
- 阶段 1 证据集中在 t10 §1.2 与 t7 §4/§5/§6.1；其中两条需分档说明：
  - **I-16 的"两个 1-tick 任务停止"** = **静态档 + 运行反证**（t7-L1，**必须挂账**，见 §4）；
  - **"多重集不变"** = 按 §5 阶段 1 / §6.2 的**改写后判据**：`新增方向 = 0` + 下降逐条归属 + **剔除已申报项后 diff = 0**（数值 6 条、字符串 26 条）。
- §7 收口注同时写明：**"多重集不变"约束活代码取值**，§2.5 强制删除造成的下降逐条归属后不视为行为变化（并与 §2.3.1 备注互相指向）。

---

## 3. 阶段 1 结束的新基线快照：`docs/回滚快照/after-phase1/`

| 项 | 值 |
|---|---|
| 文件数 | **48** = src/** 45（44 个 `.java` + `src/main/resources/plugin.yml`）+ `build.gradle.kts` + `gradle.properties` + `settings.gradle.kts` |
| 与 after-baseline 的差集 | **少 3 个** = 阶段 1 按 §2.5 强制删除的 3 个未使用类（`SampleMainWeapon.java`、`TestSendSanTEAndEnergyChangeMsgPassive.java`、`TestSetSanTEToZeroSkill.java`）；即 47 → 44 个 `.java` |
| `SHA256.txt` | 48 行，`<SHA256>  <相对路径>`，UTF-8 无 BOM + CRLF（整表重写） |
| **为什么是 48 行而不是 51 行** | **51 = `after-baseline` 的基数**（47 个 `.java` + `plugin.yml` + 3 构建文件）。阶段 1 按 §2.5 强制删除 3 个未使用类后，`src/**` = 44 个 `.java` + `plugin.yml` = 45，+3 构建文件 = **48**。**差集恰好等于那 3 个被删类，不多不少** —— 这正是"阶段 1 只删死代码、未新增文件"的账目证明（与 t10 §1.2 "47→44 且 0 新增"一致）。 |
| 清单自身身份 | 6 688 B / mtime 2026-09-16 23:23:16 / SHA256 `DFEBE24621D546C7AC0EAB78499752DF97B1C580681F3111166A5F0F030A8163` |
| 自身校验 | 逐行复算：**checked = 48 / bad = 0** |
| 元数据 | `after-phase1/_meta/阶段1-收口证据.txt`（含生成命令、被验事实身份、旧快照校验结果） |
| 生成约束 | 生成前 `Test-Path`，已存在即**拒绝**（未覆盖任何既有快照） |

**旧快照未被改动（本次实测复算）**：

| 快照 | 校验对象 | 结果 | 清单自身身份 |
|---|---|---|---|
| `before/` | `integrity-sha256.txt` 中 `src/**` 48 个文件 | **checked = 48 / bad = 0** | 7 213 B / 2026-09-15 21:56:53 / `CE3E5E7B…D50C`（未变） |
| `before/` 清单第 49 行 | `docs/回滚快照/session-start-baseline.patch` | 存在且哈希一致 | — |
| `after-baseline/` | 自身 `SHA256.txt` 51 个文件 | **checked = 51 / bad = 0** | 7 155 B / 2026-09-16 21:52:42 / `23046432…FEAF`（未变） |

### 3.1 生成命令（可复现，供后续阶段照抄）

```powershell
$root="C:\Users\ROG\Desktop\插件\ShadowHunterRoles"; $snap="$root\docs\回滚快照\after-phaseN"
if(Test-Path $snap){ "REFUSE: exists"; exit 1 }
New-Item -ItemType Directory -Force -Path $snap | Out-Null
Copy-Item "$root\src" "$snap\src" -Recurse -Force
foreach($f in @('build.gradle.kts','gradle.properties','settings.gradle.kts')){ Copy-Item "$root\$f" "$snap\$f" -Force }
$rows = Get-ChildItem $snap -Recurse -File | Sort-Object FullName | ForEach-Object {
  $rel = $_.FullName.Substring($snap.Length+1).Replace('\','/')
  "{0}  {1}" -f (Get-FileHash $_.FullName -Algorithm SHA256).Hash, $rel }
[System.IO.File]::WriteAllText("$snap\SHA256.txt", (($rows -join "`r`n") + "`r`n"), (New-Object System.Text.UTF8Encoding($false)))
# 校验：逐行复算，期望 bad = 0
```

---

## 4. 承接的挂账与发现（不得静默略过）

| 来源 | 项 | 档位/级别 | 本记录处置 | 落点 |
|---|---|---|---|---|
| t7-L1 / t10 §5 | "掉线后 ticker 与 `BuffManager` updater 两个 1-tick 任务停止"的运行级证据 | **静态证据**（+ 运行反证） | **挂账确认**：本环境不可得（`jcmd`/attach 已定论否决；`spark` 需控制台/OP 且本机 async-profiler 引擎不受支持）。**不得当作运行实测**；不判 needs_revision | **t24 / t19 继续挂账**（本记录即 t8 的落点） |
| t10 §5 | 真实四行合格线（含第 3 行 SHDF 依赖发现） | 未能实测 | 列为**待用户实测的开放项**（需装有 SHDFGamePlugin 的 25565 外服）；Mode B 下三行 + 替代证据已足够判定本阶段 | t24 / t19 + 用户 |
| t10 §5 | 阶段 1/1.8 起服日志副本归属 | — | **已收纳**：`docs/阶段1-起服锚点-t2ModeB.log`（6 199 B / 22:01:20）、`docs/阶段1-起服日志-t5ModeB.log`（6 076 B / 22:22:30 / `A3EB7CB6…F63C`）、`docs/阶段1.8-起服日志-t6ModeB.log`（9 786 B / 22:54:49 / SHA256 `0D0C5F3B3FE39B8798697CF50710E08E8DA297DD96BE246518B51D778CA976A2`）——三份均作为**正式证据文件**在本记录与快照 `_meta` 中登记，归属 = 各阶段实现者的起服取证（captain 已 `git add -f` 入库） | 本记录 §3/§4/§5 + `_meta` |
| t10（补充核验，captain 转述） | 退出码四态 `0/2/3/4` 与优先级 `2 > 3 > 4 > 0`；断言 ↔ 读数一一对应 | **运行实测（`--selftest` exit 0）** | **已并入**：见本记录 **§4.1(a)/(b)**（六条断言逐条覆盖四态与优先级；`verdict` 六键与 A/B/C/C'/D/E 读数一一对应） | 本记录 §4.1 |
| t10 F-1 | t5 小结药水记账枚举漏 `RedBleedPassive.java:199` | low | **已闭环**：核实 `docs/阶段1-交付小结.md:137` 现含 `:114` / `:199` 两处 | t5 交付小结 |
| t10 F-2 | "多重集不变"与 −6 的字面冲突需落进交付物 | low | **已闭环（双落点）**：① t5 交付小结 §8 已写"原始 diff = 6 / 剔除已申报项后 diff = 0"并声明措辞纪律；② 指南 §7 收口注 + §2.3.1 备注固定该读法 | t5 交付小结 + 指南 |
| t10 F-3 | §10 裁决 1 的 fail-fast 未登记进 §6.2"有意行为变化" | low | **已闭环**：指南 §6.2 第 3 条清单已含该条（标注"评审 T10-L3 要求补记"） | 指南 §6.2 |
| t7-L2 | §5 1.7「11 处字面量」与代码不符 | low（历史） | 已由 captain 提交 `c77a2c7` 修正 | 指南 §5（历史） |
| t7-L3 | `RedSolitaryArroganceSkill.java:37` 仍用 `getGlobalRegionScheduler` | low | 属**阶段 2**（O-5 的 Folia 粒度半项）→ 已在 §2.3.1 标注"登记待 t9" | t9 |
| t7-L4 | `api/RoleAPIImpl.java` 过时 API 告警 | low | 既有问题、非本阶段引入；阶段 3 会移位该文件 | t13 |

---

## 4.1 承接 t10 的补充核验（本轮按 captain 要求并入）

### （a）退出码四态 `0/2/3/4` 且优先级 `2 > 3 > 4 > 0` —— **由 `--selftest` 逐条断言，证明可达而非仅文档声明**

探针 `--selftest`（不连服务器；`node t6-quit-persistence.js --selftest` → **exit 0**）实测输出（本轮复跑，2026-09-16）：

| 断言 | 结果 | 覆盖的语义 |
|---|---|---|
| `exit_allTrue_strong_is_0` | true | 全真且无 vacuous → **0** |
| `exit_allTrue_vacuous_is_4` | true | 全真但有 vacuous → **4** |
| `exit_anyFalse_is_2` | true | 有 false → **2** |
| `exit_anyNull_is_3` | true | 有 null（未测到）→ **3** |
| `exit_falseBeatsVacuous_is_2` | true | **false 优先于 vacuous**（`[F] + ['vacuous']` → 2） |
| `exit_nullBeatsVacuous_is_3` | true | **null 优先于 vacuous**（`[N] + ['vacuous']` → 3） |

→ 四条分支**逐条被断言为可达**；且 `4` 只在"全真 + 存在 vacuous"时出现，**不与 2/3 并存**（优先级 `2 > 3 > 4 > 0` 由 `computeExitCode()` 纯函数实现，自检直接调用该函数）。本次 t6 实测的实际取值为 **0**（E 强度 = strong，无 vacuous）。

### （b）断言 ↔ 验收读数一一对应（**无遗漏**）

`t6-quit-persistence.result.json` 的 `verdict` 实测为 **6 个键**，与验收读数一一对应：

| 断言键 | 验收读数 | 取值 |
|---|---|---|
| `A_online_hadRole` | 掉线前在线且有角色 | true |
| `B_rejoin_noRole_mapEntryRemoved` | 重登无角色 ⇒ `playerRoleMap` 条目已移除 | true |
| `C_persistence_modifierGone` | `MAX_HEALTH` 修饰符消失 | true |
| `C_persistence_maxHealthIs20` | 最大生命回到 20 | true |
| `D_hotbarClean` | 热键栏无残留 | true |
| `E_effectsCleared` | 无残留药水（强度 strong） | true |

（C 拆两条 = 同一验收读数的两个可观测面"修饰符 + 数值"；E 独立于 C，**不得互相覆盖**。）

### （c）三条 low 的归属（captain 已分流，本记录据此登记）
- L1（药水施加点枚举漏列 `RedBleedPassive:199`）、L2（补"多重集判据解读"）→ 已派 **eng-止血** 在 `docs/阶段1-交付小结.md` 处理；本记录实测核实**两者均已闭环**（见 §4 表）。
- L3（§6.2 补"槽位冲突改 fail-fast"）→ **captain 已完成**（指南 §6.2 第 3 条清单内已含该条）。

---

## 5. 阶段 2 准入判定

### 结论：**可以进入阶段 2**（附 3 条前置约束）

**理由（依据 = 两级把关均已通过 + 冻结判据成立 + 回滚交代清楚）**：

1. **验证与评审双通过**：t7 verdict = **pass**（5/5 验收判据，0 blocker/high/medium）；t10 verdict = **pass**（0 blocker / 0 high / 0 medium，3 条 low 均为文档补记且已闭环）。
2. **指南 §7 阶段 0/1 清单 15/15 勾选**，每条附证据位置（本记录 §2）。
3. **数值与可见行为冻结成立**：数值 `387→381`（**新增 0**、6 条逐条归属）、字符串 `211→197`（删 20 / 增 6，逐条归属）；**剔除已申报项后 diff = 0**；t10 另做**可见行为逐项清点**（24 个存活改动文件、去注释代码区比对）后判定"可见变化恰好 = §6.2 已申报的两项（清 4 槽、掉线即销毁），无第三项"。
4. **回滚可执行**：`after-phase1/`（本记录 §3）+ `after-baseline/` + `before/` 三份快照齐备且旧快照实测未被改动；t12 已做非破坏式回滚演练。

**前置约束（进入阶段 2 必须遵守）**：

| # | 约束 | 依据 |
|---|---|---|
| P-1 | **不得把 t7-L1 的静态档当作运行实测**；阶段 2 及后续收口（t24/t19）继续挂账 | t10 §5、§6.6 ②(3) |
| P-2 | **`getInstance()` 计数基线已变**：阶段 0–1 后 `core` 17→**15**、全仓 26→**24**（t5 小结）；阶段 2 的 I-1/I-2 复核**必须用新数字**并改用 `Get-ChildItem -Recurse` 口径（附录 C 旧通配会漏 7 个文件，t4 V-01） | t5 小结、t4 V-01 |
| P-3 | **`RoleCommand` 可见回执串**（如 `<名> has no role!`）改动前必须广播 —— 机器人探针断言 B 直接依赖该串；同理阶段 2 不得顺手改可见文案 | 指南 §6.2 末条 |

**并入阶段 2 的既有项**：`RedSolitaryArroganceSkill.java:37` 的 Folia 粒度半项（O-5 半项，t7-L3）；`api/RoleAPIImpl` 过时 API（t7-L4，阶段 3 移位时一并处理）。

---

## 6. 本记录自身与范围合规

- 本轮**只写**三个 inScope 目标：`docs/最终重构指南.md`（§2.3.1 新增、§7 勾选）、本文件、`docs/回滚快照/after-phase1/**`。
- **未触碰**：`src/**`、`docs/组件系统设计-Unity风格.md`、`docs/回滚快照/before/**`、`docs/回滚快照/after-baseline/**`（后两者已由 §3 的表证明"未被改动"）。
- **未做任何 git 写操作**；提交由 captain 统一执行。
- 生成/校验命令均在本记录内可复现（§3.1）。
