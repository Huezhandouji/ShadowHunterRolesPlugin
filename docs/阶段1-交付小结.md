# 阶段 1 交付小结（止血：1.1–1.7 + §10 裁决 + O-14/O-11）

> 任务：`t5` 阶段 1 止血 · attempt 1 · 执行者 `eng-止血`
> 依据：《最终重构指南》§5「阶段 1 · 止血」、§2.5「死代码与死契约清单」、§2.3 台账、§6.3/§6.6、§10「裁决记录」、附录 A/B；《重构前准备-需求与验收.md》§5/§6
> 日期：2026-09-16 · 数值 before 口径 = **阶段 0 结束时的工作树**（t5 开工时冻结副本，47 个 `.java`）
> 起服模式：**Mode B（全项目稳态，captain 已裁决：不安装 SHDFGamePlugin）**
> 变更面：**22 个文件修改 + 3 个类删除**（`src/main/java` 下 47 → 44 个 `.java`），全部在 inScope 内

---

## 0. 结论摘要

| 判据 | 结果 | 证据位置 |
|---|---|---|
| 指南 §2.5 死代码与死契约表全部为空 | **通过**（逐项 0 命中，3 个类已删除） | §6 |
| O-1..O-9、O-13、O-14、O-17、O-18 逐条关闭 | **通过**（O-1/O-2 由 t2 关闭，其余本轮关闭） | §2–§7 |
| 数值字面量多重集（按 §6.2 改写后的判据，captain 已裁定） | **通过**：**原始 diff = 6**（全为删除方向、**新增 = 0**）；**剔除已申报项后 diff = 0**。详见 §8（含每条差异原文、归属、声明值位置） | §8 |
| 起服四行合格线齐全且无新增 ERROR/Exception | **Mode B：三行齐 + 零新增错误行**（对照 t2 锚点，新日志 − 锚点 = 0 行） | §10 |
| 槽位冲突策略已择一并说明 | **fail-fast 抛异常**（§10 裁决 1）；两个角色仍正常注册 | §4.3 |
| 构建判据 | `BUILD SUCCESSFUL` + jar/最新 `.class` mtime 刷新 | §9 |

**额外关闭（1.4 顺带，均为本轮范围内的既有缺陷）**：O-10（跨类型去重失效）、O-11（`description` 默认 null → NPE）、O-12（槽位冲突"告警+覆盖"）。

---

## 1. 变更面

### 1.1 修改的 22 个文件（SHA256 前 16 位，`after(t5) | before(t5)`）

> **基数说明（防止审计时把两个数字看成矛盾）**：本节"22 改"是相对 **t5 开工时冻结的副本（= 阶段 0 结束时的树）**。若改用 **`docs/回滚快照/after-baseline`** 作基数，则 **modified = 23 / deleted = 3 / added = 0** —— 多出来的那 1 个是 `roleComponent/meiqiHezi/passive/MeiqiheziEquipmentsPassive.java`（阶段 0/t2 的改动，after-baseline 早于 t2）。两个数字都对，差别只在基数；`RedEquipmentsPassive.java` 被 t2 与 t5 各改过一次，故只算 1 个。

| 文件（`src/main/java/com/shadowHunterRolesPlugin/`） | after | before |
|---|---|---|
| `command/RoleCommand.java` | `DC748E7598819367` | `D3B790C218B6EE14` |
| `core/MainWeapon.java` | `60F50ECDCDF70C1D` | `18768AB7423239FD` |
| `core/PassiveSkill.java` | `15017912A1F3E90C` | `F62EEAC63C21C7C7` |
| `core/Role.java` | `96C1BC6AA6F18488` | `1D7639FB5DEE64C8` |
| `core/RoleInstance.java` | `7B72E3C4E3CF23BC` | `6A763BF6DCE86EE1` |
| `core/Skill.java` | `E511E1085E673EA6` | `3C4E6DC3E3D72C77` |
| `listener/MainWeaponListener.java` | `6F1608BBFFEFD1D0` | `5B5C4F01122CF3D6` |
| `listener/SkillListener.java` | `EC88652EDADD1319` | `7E86C52E69D68127` |
| `manager/BuffManager.java` | `F617CFA91D535114` | `42D8732270C5CEF7` |
| `manager/RoleManager.java` | `8642032E751A5E12` | `713D0E4E0358EF58` |
| `registry/RoleRegistry.java` | `1DEB2BD1BEDF0030` | `B6B07CEECF2466E7` |
| `roleComponent/DefaultSanTEZeroPunishment.java` | `7FF12783FF970667` | `57A7F886D1E94124` |
| `roleComponent/meiqiHezi/mainWeapon/MeiqiheziJuejueMainWeapon.java` | `DA0D42CEA0541514` | `2A03596D0B0CC92E` |
| `roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java` | `B06F0864A81B6280` | `702AED08FFE52F6D` |
| `roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java` | `CE4E7E2DDB54FE6D` | `E30CE61E0D25D00B` |
| `roleComponent/meiqiHezi/skill/MeiqiheziUnconcernSkill.java` | `48A292C097843679` | `3AC53332EAEF5911` |
| `roleComponent/red/RedBleedPassive.java` | `A912FBCF16B1E1E1` | `BFE08602FF05DD03` |
| `roleComponent/red/RedDeeplySorrowSkill.java` | `0EE4993DCECCC787` | `B50F777425639833` |
| `roleComponent/red/RedEquipmentsPassive.java` | `844CAD6D3B8BEE60` | `9269CDDC10970BAB` |
| `roleComponent/red/RedEvilShockSkill.java` | `81F87D847BB87B05` | `E08E6096417EE09C` |
| `roleComponent/red/RedSanctifiedBladeMainWeapon.java` | `50E0C25637FE9290` | `C17C914CD7893D42` |
| `roleComponent/red/RedSolitaryArroganceSkill.java` | `6C3BD2BADD6AAB15` | `7367350C7D93667F` |

### 1.2 删除的 3 个类（删除前 SHA256，可从 `after-baseline` 一字不差恢复）

| 文件 | 删除前完整 SHA256 | 基线副本 |
|---|---|---|
| `roleComponent/SampleMainWeapon.java` | `1C6D342B0EB35FBE…`（523 B） | `docs/回滚快照/after-baseline/src/…/roleComponent/SampleMainWeapon.java` |
| `roleComponent/TestSetSanTEToZeroSkill.java` | `2D5CEC52EE28D42C…`（922 B） | 同上 |
| `roleComponent/TestSendSanTEAndEnergyChangeMsgPassive.java` | `5529FB86869F4C5B…`（1462 B） | 同上 |

> 三份基线副本均实测存在（`Test-Path` 通过），满足"可按文件回滚"。

### 1.3 产物与日志

- 构建产物：`build/libs/ShadowHunterRolesPlugin-1.0.0.jar` = **99 539 B @2026-09-16 22:21:29**（阶段 0 结束为 102 926 B @21:59:13）；`build/classes` 52 个 `.class`，最新 `ShadowHunterRolesPlugin.class @22:21:29`。
- 阶段 1 起服日志（本任务留存，供阶段 2 对照）：
  - `docs/阶段1-起服日志-t5ModeB.log`（6 076 B，SHA256 `A3EB7CB65081824039D0EB592CF98A4B6F22E648B946491D5C74B65E484CF63C`）
  - `插件\.scratch\bot\baseline-runserver-t5-2026-09-16-2222.log`（同一份，SHA256 相同）
- 阶段 1 对照锚点（不变）：
  - `docs/阶段1-起服锚点-t2ModeB.log`（6 199 B，SHA256 `2A4BD077852D213901506205E15AC64C2F5D6A27C965D59F922D31FEE5ADB524`，与本轮起服前后复核一致、未被改写）

---

## 2. 1.1 未托管任务句柄化（O-3 / O-4 / O-5）

**统一做法**：每个延时/周期任务改为**持有句柄字段** → 组件实现 `LifecycleAware` → 在 `stop()` 取消；任务体内加"实例已失效"守卫（读 `RoleInstance.isValid()`）。守卫与 `stop()` 是双保险：`clear()` 先 `triggerLifecycleStop()`，正常情况下任务在 stop 里就被取消。

| 缺陷 | 组件 | 句柄字段 | stop 取消 | 失效守卫 | 原问题 |
|---|---|---|---|---|---|
| **O-3** | `MeiqiheziBloodySlashSkill.java` | `:24 private BukkitTask attackTask;` | `:94-96` | `:54 if(!instance.isValid()){ this.cancel(); return; }` | `:46` `runTaskTimer(…, 0L, 2L)` 未托管 → 施放中角色被清除后最多 4 次 ×2t 僵尸伤害（`:63` 14 伤） |
| **O-4** | `MeiqiheziCircleSlashSkill.java` | `:27 private BukkitTask castTask;` | `:99-101` | `:63`（实例失效） | `:58` `runTaskLater(…, 20L)` 未托管 → 前摇 1 秒内角色被清除仍结算 20 真伤 |
| **O-5** | `RedSolitaryArroganceSkill.java` | `:25 private ScheduledTask attackTask;` | `:97-99` | `:47`（在既有条件串末尾追加 `\|\| !casterIns.isValid()`） | `:37` `GlobalRegionScheduler.runAtFixedRate` 未托管 → 4 次 ×6t 窗口内角色被清除 → 僵尸伤害 + 旧实例判阵营 |

- 三个组件均新增 `implements LifecycleAware`（`:21` / `:24` / `:22`，各自行号见 §2 表）与空 `start()`（今天无 start 语义）+ `stop()`。
- **未做**（属阶段 2/4）：把 `GlobalRegionScheduler` 换掉（Folia 粒度问题）属平台层剥离，本轮保留原调度器、只补句柄。
- 组件内的**数值、结算次数、间隔、伤害全部未动**：4×2t / 20t / 4×6t、14 伤 / 20 真伤 / 8 伤 + 回血 4 逐字不变。

## 3. 1.2 `DefaultSanTEZeroPunishment` 重入保护（O-6）

| 项 | 内容 |
|---|---|
| 缺陷 | 单 `taskId` 字段无重入保护：惩罚进行中 SanTE 再次归零 → `taskId` 被覆盖 → **老任务永远无法取消**（任务泄漏 + 结束后被老任务改写 SanTE） |
| 修复 | `:30 private static final int NO_TASK_ID = -1;` + `:32 int taskId = NO_TASK_ID;`；`:41` 起新任务**之前**调用 `cancelPunishmentTask()`；`:130-135` 新增私有方法（取消 + 复位）；`:138-140` `stop()` 改为 `cancelPunishmentTask()` |
| 行为 | 连续触发惩罚时：旧任务被取消 → 只有最新一轮惩罚在跑；不再出现"两轮惩罚同时结算"。惩罚内容（3 次 × 40t、`0.3×最大生命×0.33333`、眩晕 100t）**逐字未变** |
| 数值 | `-1` 只出现一次（与修改前相同，见 §8），守卫比较用常量、不新增字面量 |

## 4. 1.4 `Role.Builder` 三处校验（O-10 / O-11 / O-12）

### 4.1 跨类型去重（O-10）

- 原实现：`addPassive` 查 `skillSuppliers`、`addMainWeapon` 也查 `skillSuppliers` → **自身类型的重复完全查不出**。
- 修复：`:223-227` 新增 `ensureIdNotRegistered(String type, String id)`，对 `skillSuppliers`/`passiveSuppliers`/`mainWeaponSuppliers` **三张表**统一查重；`:175` / `:192` / `:206` 三处装配入口改用它。
- 异常文案与原先保持同构（`"<Type> already registered: <id>"`）。

### 4.2 `description` 默认空表（O-11）

- `:94 private List<Component> description = new ArrayList<>();`（原为 `null`，仅 `build()` 兜底）；
- `:122` `description(List<Component>)` 归一为**可变列表**（`new ArrayList<>(description)`），避免调用方传 `List.of(...)` 后再 `addLineOfDescription` 抛 `UnsupportedOperationException` —— 这是同一缺陷的完整修复（默认空表 + 归一）；
- `:242` `build()` 保留"空表给占位文案"的可见输出（`"No description yet."`），与修改前一致。

### 4.3 槽位冲突策略（O-12）—— **选择 fail-fast 抛异常**

- **选择**：`validateSlot` 冲突时 `throw new IllegalArgumentException("Slot N is already occupied by '<id>'.")`（`:235`），非法槽位仍抛原异常（`:232`）。**未**采用"覆盖前移除旧组件"。
- **依据**：《最终重构指南》§10 裁决 1（fail-fast 抛异常、该角色不注册），并与阶段 3.2 的 `RoleLoader` fail-fast 一致。
- **副作用（已知且有代价）**：冲突时角色不会被注册，而不是"静默丢掉一个技能继续跑"。今天两个角色无冲突。
- **两个角色仍正常注册的实测证据**：起服日志 `:45-46`（插件 enable 成功、静态注册表构造未抛异常）→ 见 §10。
- 顺带：`Role.java` 不再需要 `ShadowHunterRolesPlugin.getInstance()`（原仅用于打印警告）→ 与 O-17 修掉的 `Skill.getSkillId` 里的那处相加，**`core` 里的 `getInstance()` 由 17 处降为 15 处**（全仓 26 → **24**：core 15 / roleComponent 5 / listener 2 / manager 2，实测分组计数）。阶段 2 复核 I-1 时请以新数字为准；另注意附录 C 的计数命令必须用递归写法。

## 5. 1.5 药水记账 + `onDisable` 走 `instance.clear()`（O-7 / O-8）

### 5.1 药水记账（D6 / O-7）

- `RoleInstance`：`:42 private final Set<PotionEffectType> appliedPotionTypes = new LinkedHashSet<>();`；`:502-506 public void applyPotionEffect(PotionEffect effect)`（施加 + 记账）；`:763-766 clear()` 只移除**账本内**的类型，**删除**了原 `player.getActivePotionEffects().forEach(...)` 的全量清空。
- 全部"施加到**本实例玩家**身上"的调用点改为经实例施加（同一 `PotionEffect` 对象，可见行为逐字不变）：

| 调用点 | 效果 |
|---|---|
| `manager/BuffManager.java:117` / `:120`（STUN） | BLINDNESS / DARKNESS（duration, amp 1, 与原先同一构造参数） |
| `roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java:50` | SLOWNESS 20t amp2 `(ambient=true, particles=false)` |
| `roleComponent/meiqiHezi/skill/MeiqiheziUnconcernSkill.java:40` | SPEED 40t amp4 |
| `roleComponent/red/RedDeeplySorrowSkill.java:45` / `:46` | REGENERATION 45t amp5 / STRENGTH 45t amp2 |
| `roleComponent/red/RedBleedPassive.java:114` / `:199` | RESISTANCE 100t amp1 |

- **未记账（有意）**：`RedEvilShockSkill.java:30-31` 把致盲/缓慢施加到**受害者**身上 —— 那不是本实例玩家的效果，本就不该由本实例的 `clear()` 回收，保持原样。

### 5.2 `onDisable`（O-8）

- `manager/RoleManager.java:79-86 public void clearAllPlayersRole()` 由"只清 map"改为**逐个 `instance.clear()`**（回收生命修饰符、账本内药水、热键栏、1-tick 任务）再清 map；
- `ShadowHunterRolesPlugin.java:50-53 onDisable()` 调用链不变（`roleManager.clearAllPlayersRole()`），即已走 `instance.clear()`。

## 6. 1.6 §2.5 死代码与死契约清空

**逐项复检命令（递归，`src/main/java` 全树）与结果（全部 0 命中）**：

| §2.5 项 | 复检结果 | 处理 |
|---|---|---|
| `PassiveSkill.trigger(Player, RoleInstance)` | `\.trigger\(` = **0** | 删除声明（`core/PassiveSkill.java`，另删因此未使用的 `org.bukkit.entity.Player` import） |
| `MainWeapon.canRightClick()` | `canRightClick` = **0** | 删除声明（`core/MainWeapon.java`） |
| `RoleInstance` 的 6 个集合/单件查询 | `getSkills(`/`getPassives(`/`getMainWeapons(`/`getSkill(`/`getPassive(`/`getMainWeapon(` 各 = **0** | 6 个方法全删（`RoleInstance.java`） |
| `RoleInstance.getSkillById` | = **0** | 删除 |
| `RoleInstance.endSkillCooldown` / `endMainWeaponCooldown` | 各 = **0** | 删除 |
| `RoleInstance.hasEnoughEnergy` | = **0** | 删除 |
| `RoleInstance.isInSanTEPunishment()`（getter） | = **0**（setter 仍被调用 4 处） | 删除 getter |
| `Role.getMainWeaponSupplier`/`getSkillSupplier`/`getPassiveSupplier` | 各 = **0** | 删除 |
| `RedBleedPassive.requestBleedResolve(RoleInstance, Player, int)` | = **0** | 删除（逻辑并入 `requestResolve(Player,int)` 一事属阶段 4.5，本轮先按 §2.5/1.6 删除死方法） |
| `RoleRegistry.getBuilder` | = **0** | 删除；其唯一读者 `BUILDERS` map 随之成为只写死状态 → **一并删除**（含两处 `put`） |
| `RoleManager.getCurrentRole(Player/UUID)`、`getPlayersWithRoleCount`、`getAllPlayersWithRole` | 各 = **0** | 4 个方法删除 |
| 三个未注册/未使用类 | 全仓引用仅自身 | **删除 3 个文件**（见 §1.2） |
| 未使用 import | 逐名 body 引用计数 = 0 | 删除 **14 处**：`MeiqiheziJuejueMainWeapon.java` 4 个（`ShadowHunterRolesPlugin`、`RoleCommand`、`RoleManager`、`BukkitRunnable`）、`SkillListener.java` 4 个（`NamespacedKey`、`ItemMeta`、`PersistentDataContainer`、`PersistentDataType`）、`RoleCommand.java` 4 个（`RoleInstance`、`PlayerDropItemEvent`、`PlayerLevelChangeEvent`、`BukkitScheduler`）、`MeiqiheziCircleSlashSkill.java` 1 个（`RoleManager`，删掉 `:72` 死局部变量后失效）、`core/PassiveSkill.java` 1 个（`Player`，删 `trigger` 后失效） |

- 表内 `RoleManager` 的 4 个"半死接口"已确认**无反射引用**（全仓无字符串常量形式的类名/方法名查找），按 1.6 删除。
- **数量口径**：卡面写"11 处未使用 import"，实测为 **14 处**（其中 2 处是删除死代码后才失效的），差异已在此列明。

## 7. 1.7 冷却双真值消除（O-13）+ O-17 / O-18 / O-14

### 7.1 O-13

- 唯一仍是字面量的调用点：`MeiqiheziBloodySlashSkill.java:45` 原 `startSkillCooldown(getId(), 160)` → **`getCooldownTicks()`**；构造函数声明值 `160` 未动（`:32`），故数值不变。
- **实测与指南口径的差异（需修正文档）**：1.7 表列出"11 处"待改字面量（BloodySlash:40、CircleSlash:41、Unconcern:30、DeeplySorrow:30/46/55、EvilShock:39、SolitaryArrogance:82、Juejue:33/40、RedBlade:50），**实测其中 12 处早已是 `getCooldown()`**，只有 BloodySlash 一处是字面量。逐处复核见 §8（搬走的字面量只有 1 个 `160`）。
- **改名 `getCooldown()` → `getCooldownTicks()`**：`core/Skill.java:45`、`core/MainWeapon.java:143` 两处 getter 声明 + **12 处调用点**（`MainWeaponListener.java:54`、`MeiqiheziJuejueMainWeapon.java:29,36`、`BloodySlash:45`、`CircleSlash:46`、`Unconcern:30`、`DeeplySorrow:30,47,56`、`EvilShock:41`、`RedBlade:50`、`SolitaryArrogance:88`；另有 1 处在被删除的 `TestSetSanTEToZeroSkill:26`）。设计文档写"影响面仅 `MainWeaponListener:54` 一行"是**过期口径**（当时组件里还是字面量），实际 12 处调用点，本轮已全部改到位（`\.getCooldown\(\)` 全仓 = 0）。

### 7.2 O-17：`Skill.Utils.getSkillId` 不再重建 `NamespacedKey`

- `core/Skill.java:139-147`：删除方法体里的 `new NamespacedKey(ShadowHunterRolesPlugin.getInstance(), "skill_id")`，改用类内已有常量 `SKILL_KEY`（`:145`/`:146`）。
- `Skill.Utils.SKILL_KEY` **保留为 `public static final`**（`:126`，阶段 4.4 统一渲染器之前不得删除，符合阶段 2.4 的约束）。

### 7.3 O-18：`updateHotbar()` 的两个死局部变量

- `RoleInstance.updateHotbar()` 中 `boolean isReady = isMainWeaponReady(id);` / `boolean isSkillReady(id);` 赋值后从未使用（0 引用）→ 已删除；`updateHotbar` 的可见结果由 `createIconItem(this)` 决定，**渲染行为不变**（它内部各自再算一次 ready/disabled）。

### 7.4 O-14：删除 `xxb111` 调试输出

- `roleComponent/red/RedEquipmentsPassive.java:23` 原 `player.sendMessage(Component.text("xxb111"))` → 已删除（全仓 `xxb111` = 0）。可见变化：上角色不再刷这条调试消息（即 O-14 的修复目标）。

---

## 8. 数值字面量多重集比对（剔注释/字符串后的代码区）

**方法**（与阶段 0 同口径）：整棵 `src` 递归取全部 `.java` → 单遍正则剥离 `/*…*/`、`//…`、字符串、字符字面量 → `\b\d+(?:\.\d+)?(?:[eE][-+]?\d+)?[fFdDlL]?\b` 提取 → 每行 `相对路径<TAB>字面量` → 仅 `Sort-Object`（**不去重**，保留重复计数）→ `Compare-Object`。
**before** = t5 开工时冻结的 `src` 副本（47 个 `.java`，**387** tokens）；**after** = 当前树（44 个 `.java`，**381** tokens）。

### 8.1 全树差异（6 行，**全部为删除方向，新增方向 = 0**）

| 方向 | 文件 | 字面量 | 归属类别（§6.2 改写版） | 依据（指向具体行） |
|---|---|---|---|---|
| 删除 | `roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java` | `160` | **(b) 1.7 调用点塌缩** | §5 阶段 1 的 1.7 项 / O-13：`:45` 由 `startSkillCooldown(getId(), 160)` 改为 `getCooldownTicks()`；**声明值仍在**：`:32 → 160,`（构造函数第 3 个参数，即附录 A 的"冷却 160"），该技能里 `160` 的出现次数 2 → 1，数值本身未变 |
| 删除 | `roleComponent/red/RedBleedPassive.java` | `0` | **(a) 1.6 删除的死代码（存活文件内被删的死方法）** | §2.5 表「`RedBleedPassive.requestBleedResolve(RoleInstance, Player, int)`」行；基线副本 `:67-68`：`public static void requestBleedResolve(...)` / `if(instance == null \|\| victim == null \|\| stacks <= 0) return;`（整段方法已删，当前树 0 命中） |
| 删除 | `roleComponent/SampleMainWeapon.java` | `20` | **(a) 1.6 删除的死代码（未使用类整文件）** | §2.5 表「三个未注册/未使用类」行（构造参数 `20`） |
| 删除 | `roleComponent/TestSetSanTEToZeroSkill.java` | `0`、`0`、`100` | **(a) 1.6 删除的死代码（未使用类整文件）** | §2.5 表同上（构造参数 `100`、`0` 与 `setCurrentSanTE(0)`） |

**"新增方向 = 0"是硬结论**：本轮所有新代码（句柄字段、`isValid()`、`NO_TASK_ID` 常量、药水账本、fail-fast 校验、`ensureIdNotRegistered`）**没有引入任何新的数值字面量**。唯一需要哨兵值的地方（O-6 的 `-1`）改为常量，字面量总数与位置不变。

### 8.2 子口径（排除 3 个被删除的类，即改动前后都存在的 44 个文件）

- before = **383** → after = **381**，差异 **2 行**：`MeiqiheziBloodySlashSkill.java` 的 `160`、`RedBleedPassive.java` 的 `0`；**新增方向 = 0**。
- 结论：**活跃代码的数值字面量只少了 2 个（一个搬进"读声明值"、一个随死方法删除），没有任何数值被改写或新增。**

### 8.3 按 captain 裁定（§6.2 改写版）的两个数字与值不变判据

**⑤ 两个数（严禁只报一个）**

| 口径 | before | after | diff rows |
|---|---|---|---|
| **原始**（整树 47 → 44 个 `.java`） | **387** | **381** | **6**（全部删除方向；**新增方向 = 0**） |
| **剔除已申报项后** | **381** | **381** | **0** |

剔除的 6 项与逐条对应（`before` 侧移除，`after` 侧不动）：`SampleMainWeapon.java:20` ×1、`TestSetSanTEToZeroSkill.java:0` ×2、`TestSetSanTEToZeroSkill.java:100` ×1、`MeiqiheziBloodySlashSkill.java:160` ×1、`RedBleedPassive.java:0` ×1；移除后两侧均为 381，`Compare-Object` = **0 行**。

- **③ 值不变判据（逐项）**：附录 A 的冻结值**在声明处逐字未变** —— 本阶段唯一被"调用点塌缩"影响的是 `MeiqiheziBloodySlashSkill`：**声明值 `:32 → 160,` 原样保留**，变化的只是调用点 `:45` 改为读该声明值；其余 12 个冷却调用点原本就读声明值，未动。伤害/层数/能量/药水时长等值与阶段 0 逐字相同。
- **② 归属只落在允许的两类**：4 项属 (a) 1.6 删除的死代码（3 个未使用类 + 1 个存活文件内的死方法），1 项（共 1 个 token）属 (b) 1.7 调用点塌缩；每项都指向 §2.5 表或 §5 工作项的具体行（见 §8.1 表）。
- **措辞纪律**：本报告的结论是"**原始 diff = 6 / 剔除已申报项后 diff = 0**"，**不**写成"多重集完全一致"。

> 与卡面旧判据「before_count == after_count 且多重集相同」的关系：该表述与 §2.5「表全部为空」在本阶段互斥（删除代码在定义上不可能保持字面量计数）。captain 已裁定改用 §6.2 口径（新增=0 + 下降逐条归属 + 值不变判据），本节即按该口径给出证据。

---

## 9. 构建判据

```powershell
cd C:\Users\ROG\Desktop\插件\ShadowHunterRoles
$env:GRADLE_USER_HOME="$PWD\.gradle-work"; .\gradlew build -x copyPluginJar --console=plain
```

| 判据 | 证据 |
|---|---|
| `BUILD SUCCESSFUL` | `BUILD SUCCESSFUL in 22s`；`3 actionable tasks: 2 executed, 1 up-to-date`；`> Task :compileJava`、`:jar` 均**执行**（非 UP-TO-DATE） |
| jar mtime 刷新 | 102 926 B @2026-09-16 21:59:13 → **99 539 B @2026-09-16 22:21:29** |
| **最新 `.class` mtime**（§6.6 新判据，不用目录 mtime） | `build/classes/java/main/.../ShadowHunterRolesPlugin.class` @**2026-09-16 22:21:29**；52 个 `.class`（阶段 0 为 55，删除 3 个类） |
| 无源码新于 jar | `src` 中 mtime > jar 的 `.java` = **0** |
| 唯一提示 | 既有 `RoleAPIImpl.java uses or overrides a deprecated API`（阶段 0 已存在） |

> 退出码 = 1（`File watcher server … error = 5` 沙箱噪声），按纪律**不以退出码判定**。`copyPluginJar` 用 `-x` 排除（工作区外目标），**该外部拷贝未执行**。

## 10. 起服冒烟（Mode B）与锚点对照

命令：`$env:GRADLE_USER_HOME="$PWD\.gradle-work"; .\gradlew runServer --console=plain`（后台作业；**起服完成后已停止实例**，实测无 `java.exe` 服务进程残留，端口未监听）。

### 10.1 四行合格线（Mode B：三行齐）

```
[22:22:28 INFO]: [ShadowHunterRolesPlugin] Enabling ShadowHunterRolesPlugin v1.0.0
[22:22:28 INFO]: [ShadowHunterRolesPlugin] ShadowHunter Character System enabled.
（第 3 行 [SHDFGamePlugin] … 缺失：Mode B 稳态，run/plugins 不装 SHDFGamePlugin）
[22:22:28 INFO]: Done (11.009s)! For help, type "help"
```

- 对应 `run/logs/latest.log` 行号：`:45`（第 1 行）、`:46`（第 2 行）、`:50`（第 4 行）。
- `Bukkit plugins (1): - ShadowHunterRolesPlugin (1.0.0)`；run-paper 直接从 `build\libs\ShadowHunterRolesPlugin-1.0.0.jar` remap 投放。
- **第 3 行的替代证据（①②③口径）**：①静态 `ShadowHunterRolesPlugin.java:41-43`（`new RoleAPIImpl` + 无条件 `ServicesManager.register(RoleAPI.class, …)`）；②运行 = 第 1、2 行均出现且第 2 行（`:45`）晚于注册（`:43`）、启用无异常；③**开放项**：真实四行（含第 3 行）只能在装了 SHDF 的 25565 外服确认。

### 10.2 新增 ERROR / Exception 对照（锚点 = 阶段 0 日志）

| 项 | 锚点 `docs/阶段1-起服锚点-t2ModeB.log` | 本次 `docs/阶段1-起服日志-t5ModeB.log` |
|---|---|---|
| `ERROR/SEVERE/FATAL` 级别行 | **0** | **0** |
| 含 `Exception`/`Caused by` 的行 | 3（全部是引导期 `oshi…WmiQueryHandler` 的 WARN） | 3（同样的 3 条 WARN） |
| **新增行（本次 − 锚点）** | — | **0 行** |

- 3 条相同 WARN 发生在 `[22:22:18] [ServerMain/WARN]`，即**插件加载之前**的引导阶段（沙箱缺 WMI 权限），与插件无关，且**在锚点里同样存在**（属于"锚点既有行"，不计为新增）。
- **判据 = 集合差**（captain 口径）：把两边的行首时间戳去掉归一化后，只报"本次 − 锚点"的**新增行** = **0 行**；`ERROR/SEVERE/FATAL` 级别行两侧均为 0。
- **未使用**"含 `exception` 字样"的朴素扫描作判据（那样会把 3 行锚点既有的 oshi `COM exception querying Win32_…` WARN 每轮都算成新错）。
- **结论：零新增 ERROR/Exception**，锚点路径与集合差结论已按 captain 要求写明。

## 11. 有意行为变化申报（本轮）

| # | 变化 | 类别 | 说明 |
|---|---|---|---|
| 1 | `clear()` **不再**清空玩家身上全部药水效果，只清账本内效果 | **缺陷修复（O-7/D6），可见** | 第三方/其它系统施加、且未经本系统记账的效果会保留。这是 O-7 的修复目标；代价是"本系统施加但未记账的效果"不再被回收（已把全部本系统施加点改为记账，见 §5.1） |
| 2 | 连续触发 SanTE 惩罚时**旧轮次被取消** | **缺陷修复（O-6），可见** | 原行为：旧任务泄漏且继续结算伤害/眩晕，可能叠加；现行为：只有最新一轮。惩罚内容不变 |
| 3 | 角色被清除后技能任务不再结算（僵尸伤害消失） | **缺陷修复（O-3/4/5），可见** | 原来靠"任务没人管"会继续打；现在 `stop()` 取消 + 失效守卫双重拦截 |
| 4 | 上角色不再打印 `xxb111` | **缺陷修复（O-14），可见** | 纯删除调试输出 |
| 5 | 槽位/id 冲突由"告警 + 覆盖"变为**抛异常、不注册** | **校验策略（§10 裁决 1）** | 今天两个角色无冲突 → 无可见变化；将来装配写错会 fail-fast |
| 6 | 未赋值 `description` 的角色仍得到 `"No description yet."` | **保持原样** | 默认空表 + `build()` 占位，可见输出与修改前一致 |
| 7 | `getCooldown()` → `getCooldownTicks()`；`PassiveSkill.trigger`、`MainWeapon.canRightClick`、若干查询方法删除 | **内部 API/死代码** | 不属 `RoleAPI`（跨插件契约未动），对下游 SHDFGamePlugin 无影响 |

> 未申报任何数值/伤害/冷却/层数/能量/时长变化 —— 与 §8 的"0 新增字面量"互证。

## 12. 未执行的动作及原因

| 动作 | 状态 | 原因 |
|---|---|---|
| `copyPluginJar` 外部拷贝 | **未执行** | 目标 `C:/Users/ROG/Desktop/paper1.21.11/plugins` 在工作区外（沙箱不可写），用 `-x copyPluginJar` 排除 |
| Mode A（安装 SHDFGamePlugin） | **不采用** | captain 裁决 Mode B 为全项目稳态 |
| 掉线/离线清理（O-25）、`config.yml`、挂起恢复 | **未执行** | 属 `t6`（阶段 1.8）；`PlayerListener.java` 本轮未改 |
| 游戏内人工冒烟（选角色、施放技能、观察惩罚/装备回收） | **未执行** | 无客户端；本轮只做服务端加载冒烟（§10），行为由 t7/t12 独立复核 |
| 阶段 1 整合结案（§2.3 台账勾选、§7 清单、快照） | **未执行** | 属 `t8`（依赖 t10/t7） |

## 13. 观察项与文档需修正处（供评审/台账）

1. **§2.5 的 import 清单不完整**：漏列 `MeiqiheziJuejueMainWeapon.java:4`（`ShadowHunterRolesPlugin`，实测 0 引用）与"删死代码后才失效"的 `core/PassiveSkill.java` 的 `Player`；本轮一并删除并在 §6 列明。**low**
2. **`core/Role.java` 仍有 13 个未使用 import**（`EnergyChangeEvent`、`SanTEChangeEvent`、`NamedTextColor`、`Title`、`Location`、`Particle`、`Sound`、`Attribute`、`PlayerRespawnEvent`、`BukkitRunnable`、`Duration`、`Consumer` 等，逐名实测 body 引用 = 0）—— **不在 §2.5 表内，本轮未动**（避免扩大改动范围）。建议台账新增条目。**low**
3. **`ShadowHunterRolesPlugin.getInstance()` 由 26 处降为 24 处**（`core` 17 → 15：`Role.java` 的槽位校验 + `Skill.getSkillId` 各少 1 处；分组实测 core 15 / roleComponent 5 / listener 2 / manager 2）。阶段 2 的 I-1 与"26 处替换"清单请以新数字为准；附录 C 的计数命令**必须**用 `Get-ChildItem -Recurse`（t4 的 V-01）。**medium（口径）**
4. **1.7 的"11 处冷却字面量"是过期口径**：实测仅 1 处（BloodySlash:40）是字面量，其余 12 处已是 `getCooldown()`；`getCooldown()`→`getCooldownTicks()` 的实际影响面是 **12 处调用点 + 2 处 getter**，不是设计文档写的 1 行。**low（文档）**
5. **`RedEvilShockSkill` 对受害者施加的药水不记账**（有意）：`clear()` 只回收"本实例玩家"的效果；受害者身上的致盲/缓慢由自然到期消失。**说明项**
6. **`DefaultSanTEZeroPunishment` 的 `isInSanTEPunishment` 标志**在本轮仍未复位（`stop()` 只取消任务）；该 getter 已删除、字段当前无读者，阶段 4.6 会把它降为组件私有字段。**low**
7. `roleComponent` 下仍保留 `RoleManager.getInstance()`（`SkillUtil.java:22`）与 `ShadowHunterRolesPlugin.getInstance()`（5 处）—— 属阶段 2 的 I-1/I-2，本轮**没有**顺手改。

## 14. 复现命令

```powershell
cd C:\Users\ROG\Desktop\插件\ShadowHunterRoles
# 构建（不看退出码）
$env:GRADLE_USER_HOME="$PWD\.gradle-work"; .\gradlew build -x copyPluginJar --console=plain
Get-Item build\libs\ShadowHunterRolesPlugin-1.0.0.jar | Select-Object Length,LastWriteTime
(Get-ChildItem build\classes -Recurse -Filter *.class | Sort-Object LastWriteTime -Descending | Select-Object -First 1) | Select-Object Name,LastWriteTime
(Get-ChildItem src -Recurse -Filter *.java | Where-Object { $_.LastWriteTime -gt (Get-Item build\libs\ShadowHunterRolesPlugin-1.0.0.jar).LastWriteTime } | Measure-Object).Count   # 期望 0

# §2.5 逐项复检（全部期望 0；必须递归）
$s = "src\main\java\com\shadowHunterRolesPlugin"
$f = Get-ChildItem $s -Recurse -Filter *.java
foreach ($p in '\.trigger\(','canRightClick','getSkills\(','getPassives\(','getMainWeapons\(','getSkill\(','getPassive\(','getMainWeapon\(','getSkillById\(','endSkillCooldown\(','endMainWeaponCooldown\(','hasEnoughEnergy\(','isInSanTEPunishment\(','getMainWeaponSupplier\(','getSkillSupplier\(','getPassiveSupplier\(','requestBleedResolve\(','getBuilder\(','getCurrentRole\(','getPlayersWithRoleCount\(','getAllPlayersWithRole\(','\.getCooldown\(\)','xxb111','SampleMainWeapon|TestSetSanTEToZeroSkill|TestSendSanTEAndEnergyChangeMsgPassive') {
  "{0,-60} {1}" -f $p, (($f | Select-String -Pattern $p | Measure-Object).Count)
}

# 起服（Mode B；起服后请停掉实例）
$env:GRADLE_USER_HOME="$PWD\.gradle-work"; .\gradlew runServer --console=plain
# 错误行对照（锚点 vs 本次）
$pat='ERROR|SEVERE|FATAL|Exception|Caused by'
$a=(Select-String -Path docs\阶段1-起服锚点-t2ModeB.log -Pattern $pat | % { $_.Line -replace '^\[\d\d:\d\d:\d\d\]\s*','' }) | Sort-Object
$b=(Select-String -Path run\logs\latest.log -Pattern $pat | % { $_.Line -replace '^\[\d\d:\d\d:\d\d\]\s*','' }) | Sort-Object
Compare-Object $a $b     # 期望 0 行
```
