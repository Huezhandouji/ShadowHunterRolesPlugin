# 重构进度与交接（ShadowHunterRoles）

> **用途**：机器随时可能关机、会话可能中断 —— 本文件让接手者（或未来的自己）在 5 分钟内恢复上下文。
> **权威细节**见《最终重构指南》与《组件系统设计-Unity风格》；本文件只记 **状态 / 身份 / 开放项 / 纪律**。
> 最后更新：2026-09-16 深夜（阶段 1.8 收口后）。

---

## 1. 阶段状态

| 阶段 | 任务 | 状态 | 交付物 | commit |
|---|---|---|---|---|
| 0 前置（基线冻结） | t4 | ✅ completed | `debug-logs/回滚快照/after-baseline/`（51 文件 + `SHA256.txt`） | `ff76907` |
| 0 收口（装备 stop 对称） | t2 | ✅ completed | `debug-logs/阶段0-交付小结.md` | `ff76907` |
| 0 补全（回滚演练 + 需求收口标记） | t12 | ✅ completed | `debug-logs/测试记录/阶段0-回滚演练记录.md` + `debug-logs/回滚快照/drill/` | `d7736de` |
| 1 止血（句柄化 / Builder / 药水账本 / 死代码） | t5 | ✅ completed | `debug-logs/阶段1-交付小结.md` | `c565785` `ba4d493` `d6517c9` `a7e1b12` |
| **1.8 掉线即销毁（修 O-25）** | t6 | ✅ completed | `debug-logs/阶段1.8-交付小结.md` | `dc42203` `59b06c8` |
| 0–1 独立验证 | t7 | ✅ **completed（verdict = pass，5/5）** | `debug-logs/测试记录/阶段0-1-验证报告.md` | `ff95fa4` |
| 0–1 评审 | t10 | ✅ **completed（verdict = pass；3 low 文档项 + 2 挂账）** | `debug-logs/测试记录/阶段0-1-评审报告.md` | 见提交 |
| 1 整合结案 | t8 | ✅ **completed — 阶段 2 准入 = GO** | `debug-logs/阶段1-收口记录.md`（**引章节**：§1 依据 / §3 快照账目 / §4 挂账承接 / §4.1 t10 补充核验；**身份以现算为准**） | 见 log |
| **2 平台层剥离** | t9 | ✅ completed（platform/ 六件套；getInstance() 全仓 **0**） | `debug-logs/阶段2-交付小结.md`（引 §2 逐处表 / §3 六件套 / §6.3 冻结件 / §6.4 错误对照 / §9 回滚清单） | `6421eda` `00ae0ee` |
| **4 组件重构（t14）** | t14 | 🔄 **进行中（attempt 4）** —— **B0 机制 / B① / B② 已验收入库（3/14 组件已迁移）**；下一块 **B②-c（容器生命周期广播）**；见下方「阶段 4 块计划」 | `debug-logs/阶段4-交付小结.md` + `debug-logs/阶段4-迁移清单.md`（§0–§11） | 见 log |

**阶段 4 块计划（每块 = 一个自洽连续块，块内不停顿、块间可停；硬约束 §10/§12/§13）**：
| 块 | 内容 | 状态 |
|---|---|---|
| A / B / C | 契约层 20 文件 / 10 个包级私有端口适配器 + `ComponentRegistry` / `HotbarRenderer` 骨架 | ✅ 入库 |
| ④a / ④b | 组件唯一创建点（`Role.createComponent`）/ `RoleInstance` 装配 + `ComponentServices` + `handleCast/handleAttack` + 开关（默认 false）+ `clear()` 兜底 | ✅ 入库 |
| ⑤ | 删 D-2 静态桥 + `RoleCommand`/`RoleManager` 注入 + 收官验证（双锚点集合差 C=0；`RoleCommand` 指纹 `74D9FAF0…/36 条` 未变） | ✅ 入库 |
| **B0a** | `ActiveComponent.isMigrated()`（默认 false）+ 6 个旧入口短路 + 开关 false → **可证不可达** | ✅ 入库 `b18de63` |
| **B0b-1/2/3** | 三薄基类改基：`PassiveSkill` → `Skill` → `MainWeapon`（改完 14 个组件类全在新基类子树下，但 `isMigrated()` 全 false ⇒ 仍走旧路径） | ✅ 入库 `20b73b5` / `e9de986` / `60d6d94` |
| **B①** | `RedEvilShockSkill` 迁移（含 §20 总闸修正：删 `USE_COMPONENT_PIPELINE`、`runComponentPipeline` 单一入口委派 `handleCast`）+ W1 前移 `RedBleedPassive.requestResolve` | ✅ 入库 `1ab11a5` |
| **B②-a** | `BuffPort` R-1 端口方法（3 参）+ `BuffPortImpl` 委托已记账路径（纯新增、零调用点） | ✅ 入库 `f830b38` |
| **B②-b-1** | `MeiqiheziUnconcernSkill` 迁移（`applyPotionEffect(SPEED,40,4)` 四项等价） | ✅ 入库 `5d0c2d3` |
| **B②-b-2** | `MeiqiheziCircleSlashSkill` 迁移（**5 参保真** `(SLOWNESS,20,2,true,false)` + `timers().runLater` 前摇 + `isValid()` 删除带四步链）⇒ **`rolesContext()` 5 → 4（首次下降）**、全仓 10 → 9；`BuffPort` **7 方法**、端口方法总数 **34** | ✅ 入库 `6fa2887` |
| **B②-c** | **容器为注册组件广播新生命周期钩子**（`awake/start/stop`，同一时序点、`stop()` 早于 `cancelAllAndClear()`、legacy 扇出保留、双取消幂等）+ `CircleSlash` 去掉 `implements LifecycleAware` | ✅ 入库 `a69cdd8` |
| **B③** | `RedSolitaryArroganceSkill` 迁移（`runRepeating` + 三处端口化 + 去 legacy ⇒ 新钩子）；**`rolesContext()` 4 → 3**、`markMigrated();` 3 → 4 | ✅ 入库 `cb6c1c0` |
| **B④** | 两个 `*EquipmentsPassive` 迁移（无参 `start()/stop()`、去 legacy；**四槽材质与顺序逐件不变**——两角色胸/腿材质正好相反）⇒ 组件 `LifecycleAware` 实现者 **5 → 3** | ✅ 入库 `4dcc6e9` |
| **B⑤** | 两个 `AutoRecover*Passive` + `SkillUtil` 端口化 + `FactionPortImpl` + **`RoleInstance` 的 `component.update()` tick 广播**（5 文件；不同批落地 ⇒ 被动 `update()` 静默失效） | 🔄 已授权（在途） |
| **B⑥…B⑨** | 其余组件类迁移（**现 6/14 已迁移**：主动 4 + 被动 2）；**B⑥ 先于 B⑦** | ⏳ 待做 |
**B⑦ 冻结清单（4 文件，t33 入库后可直接执行；由 eng-止血 预检 + 队长核验）**：
1. `roleComponent/red/RedSanctifiedBladeMainWeapon.java`（53 行 / `50E0C256…78EF`）：删 `RoleInstance` import、加 `CastResult`/`CastSignal`；去 `:31 @SuppressWarnings("unchecked")`；`:32 onAttack(Player,Player,RoleInstance)` → **`public CastResult onAttack(AttackSignal signal)`**（`attacker = svc().self().player()`、`victim = signal.victim()`）；`:35` → `RedBleedPassive bleed = getComponent(RedBleedPassive.class);`；`:36` → `if(bleed != null && RedBleedPassive.canReceiveBleed(victim)){`（**保留 `:33` 原意：拿不到时只跳过流血、不得 return**）；`:38-40` → **`bleed.applyStacks(victimPid, BLEED_STACKS_PER_HIT);`**；普攻伤害行 → `svc().damage().physicalDamage(victim, attacker, 8, 1)`（`8`/`1` 不变）；删 `:50` 冷却启动 ⇒ 末尾 **`return CastResult.CAST;`**。
2. `roleComponent/meiqiHezi/mainWeapon/MeiqiheziJuejueMainWeapon.java`（62 行 / `DA0D42CE…6601`）：`:25 onAttack(...)` → `public CastResult onAttack(AttackSignal signal)`（删 `:29` 冷却启动 ⇒ `return CAST`）；`:34 onLeftClick(Player,RoleInstance)` → 并入 **`onCast(CastSignal)`**（`LEFT_CLICK` 分支；**开工先通读全文确认语义**）删 `:36` 冷却启动 ⇒ `return CAST`；**能量档位常量（`20`/`5`）留在组件 `static final`**（按设计 D5）；`:48 getNearbyPlayers(5)` 与表现层逐字不动。
3. `roleComponent/red/RedBleedPassive.java`（**t33 交付后**才可动）：删 `:24`/`:26` 两个 `*_CONTEXT_KEY` + `:94/:95` 两处 `setContext`（过渡桥）+ `implements LifecycleAware` 与 `:93 start` / `:99 stop` ⇒ **组件 `LifecycleAware` 3 → 2**。
4. `core/RoleInstance.java`：删 `setContext:251` / `getContext:255` / `removeContext:261` / `hasContext:265` + 字段 `context:72` + `context.clear():883`（**分项各自为 0**）。
**B⑦ 必证**：全仓 context 命中 **= 0 且分项各自为 0**（附命令与输出）· `LifecycleAware` **3 → 2** · 两武器 `onAttack`/`onLeftClick` ⇒ `return CAST` · 数值 `8`/`1`/`5`/`15` 逐字不变 · §2.0 三条 · 两武器 `RoleInstance` 判据（严格）· 双口径含 (d) · 构建新鲜度 · Mode B 三行 + 只看 `C` + 停服（**只 `job_kill` + 等待**）· `getInstance()` 0 · `rolesContext()` 判据仍 **2** · 4 文件逐文件 SHA256 · 运行深度声明。

**B⑧ / B⑨ 预检结论（eng-止血 只读预检 + 队长核验，2026-09-17）**：
- **渲染覆写只存在于 2 个组件**：`MeiqiheziBloodySlashSkill:100/:156`、`MeiqiheziCircleSlashSkill:114/:170`（基类 `Skill:43/:91`、`MainWeapon:52/:99` 不在 B⑧）；**`RedBleedPassive` 渲染覆写 = 0**。
- ⚠️ **架构澄清（纠正先前假设）**：删掉这两份覆写 ⇒ **渲染回归基类** `Skill.createIconItem/getDisplayName`，**不是**切到框架渲染器 —— **`HotbarRenderer` 目前仍是骨架**，`updateHotbar()` 仍在调组件/基类的这两条方法。⇒ B⑧ 的等价性必须用「**覆写 ↔ 基类**」的**逐字段四段对照表**（冷却中 / 禁用 / 能量不足 / 就绪 + lore + `instance==null` + id 来源 `getId()` vs `id`），文本与数值**逐字**核对（含 `" " + %.1f + "s"` 的**前导空格**与 `s`、`" DISABLED"`、`" ENERGY LACK"`、`====================`、`"Skill has been disabled."`）。
- **秒数后缀只在技能路径**（`Skill:63/:100` + 两技能各 2 处；**武器路径 0**）⇒「技能冷却名带秒数 / 主武器不带」这条冻结差异在**删覆写后仍由基类技能渲染提供** ✓。
- **B⑧ 范围**：`MeiqiheziBloodySlashSkill`（**尚未 `markMigrated()`**，其 `onCast` 迁移属 B⑧）+ `MeiqiheziCircleSlashSkill` + **删两份渲染覆写**。
- **B⑨ 范围**：`DefaultSanTEZeroPunishment`（同时是 `LifecycleAware` + `SanTEChangeAware` 使用者 ⇒ 按 B②-c/B③ 范式转 `awake/start/stop/update/onSanTEChange`；容器四钩子广播已就位 ✓）。
- **口径纪律**：`LifecycleAware` 实现者计数必须用**仅代码口径**并以**文件清单**呈现（现算 **3**：`DefaultSanTEZeroPunishment:18`、`MeiqiheziBloodySlashSkill:20`、`RedBleedPassive:34`）；行级含注释会虚高（曾误报 4）。

**B⑦ 范围裁定 (A)（2026-09-17，第 7 次开工前拦下）**：攻击路径**未接线** —— `MainWeaponListener.onAttackPlayer`（`:47` 取消原版事件、`:55` 自启冷却、`:58` 直调 legacy `weapon.onAttack(...)`），而 `RoleInstance.handleAttack:199` **全树 0 调用点** ⇒ 若只按 4 文件迁移两武器的 `onAttack`，**编译通过但近战静默空操作**（冷却仍被吃、原版伤害仍被取消）。
⇒ **B⑦ 扩为 5 文件**（+ `listener/MainWeaponListener.java`）：`onAttackPlayer` 保留 `:47` 取消、紧接 `if(instance.handleAttack(victim, attacker)) return;`（先新后旧，镜像施放路径），`:55` 冷却启动**移入 legacy 分支**；`handleAttack` 在 `CAST` 时启动冷却、判据与施放路径同源（`instanceof ActiveComponent && isMigrated()`，禁 `instanceof RoleComponent`）；必证新增"`handleAttack` 调用点 ≥ 1"与"未迁移武器行为逐字不变"。

**B⑧ 范围裁定 (a)**：两份渲染覆写（`BloodySlash`/`CircleSlash`）**比基类多一个可见状态 `ENERGY LACK`**（`STRUCTURE_VOID` + `" ENERGY LACK"`、无 lore）⇒ **不能顺带删**。裁定 **(a)**：**先把该分支补进基类 `Skill`**（禁用之后、就绪之前；材质/文案逐字、**不加 lore**；与骨架 `IconState` 顺序一致），**再删覆写**；**前置**：现算其余 5 个技能的 `energyCost` —— 全为 0 则新分支永不触发（零可见变化 ✓），有非 0 则逐条申报。⇒ **B⑧ = 3 文件**（`MeiqiheziBloodySlashSkill`（含 `onCast` 迁移）+ `MeiqiheziCircleSlashSkill` + `core/Skill.java`），**不动** `HotbarRenderer`（骨架留待 4.4/T）。

**流程教训（2026-09-17）**：`build.gradle.kts` 的实质改动（`copyPluginJar` 改为"属性存在才注册"、`into(dest)`）**长时间未入库**，直到收尾提交才补上 ⇒ **构建脚本/配置的改动必须随"第一个用到它的批次"一起提交**，否则工程处于"源码依赖未入库配置"的状态（新人克隆会得到不同行为）。

**范围纪律（2026-09-17 第 9 次开工期拦下的沉淀）**：
> **卡面的文件清单是"建卡时现算"的快照，不是闭集** —— 执行者须在**开工前现算**一遍：若发现**清单外文件含本批要改的标识符/符号**（改名、签名、常量一类），**一并改并如实申报**（实例：t39 改名漏列 `MeiqiheziBloodySlashSkill.java`，它是 t38 新增的 `return CastResult.CAST`，不改必编译失败）；反之清单内有文件**已不含**目标符号也要申报。**判据以"全仓命中数达到卡面目标"为准**，不以"改了清单里那几个文件"为准。
>
> **改派纪律**：**改派前先确认执行者是否已在途** —— t35 的事故即"队长为解除立卡冲突把卡改派到队长名下，而执行者正在执行该卡"⇒ 其完成提交被拒（`assigned to captain, not you`）。**责任在队长**；处置：队长代为结卡并补齐逐条 acceptance 证据。

**计数陷阱（第 10 例，2026-09-17）**：**PowerShell `Select-String` 默认不区分大小写** —— 判据含**大小写敏感标识符**（如 `CAST`）时必须显式 `-CaseSensitive`，或直接改用 **`git grep -E`（默认大小写敏感）**。实例：t39 改名后我用 `Select-String '\bCAST\b'` 报"4 处残留"，实为 `type.cast(component)` 与 `"…trying to cast a skill!"` 的小写 `cast` ✗；`git grep -E '\bCAST\b' -- src` = **0** ✓（实施者结论正确）。
⇒ **纪律**：**标识符类判据一律用 `git grep -E`（或 `-CaseSensitive`）**；`Select-String` 只用于"人类可读的探查"，不用于出证据。

**计数陷阱（第 11 例，2026-09-17）**：**PowerShell 读取 `git show <rev>:<path>` 的输出会归一化行尾与末尾换行** ⇒ 用 `Out-String` / `-join` 拼出来的内容**与 blob 字节不一致**，据此算出的 SHA256 会"复现不出"。实例：t38 交付版 `BloodySlash` 的内容哈希实为 **LF + 含尾换行 = `4F24BB31…64B94`**（实施者口径 ✓），而用 PowerShell 拼接得到的是 **LF + 去尾换行 = `10844812…`** / **CRLF + 去尾换行 = `5C58D1B4…`** ✗。
⇒ **纪律**：**跨版本比对内容一律用 `git rev-parse <rev>:<path>`（blob id）或 `git diff`（口径无关）**；确需"文件字节 SHA256"时**以工作树文件为准**，并**显式声明行尾口径**（LF/CRLF、是否含末尾换行）。t42 的"差异限定"判据已据此改为「与 blob `bda103f` 相比恰为 2 行标识符改名」。

**字节级样例（2026-09-17 实测，把上一条从"规则"升级为"样例"）**：本仓库 `core.autocrlf = true` 且**无 `.gitattributes`** ⇒ 同一版内容有两种**正当**字节形态：
| 形态 | 字节 | 末尾 | SHA256（实例：t38 版 `BloodySlash`） |
|---|---|---|---|
| **库内 blob（LF）** | 3 699 B | `7D 0A`（LF+尾换行） | **`4F24BB31…64B94`** ✓（实施者报的值） |
| **工作树/检出（CRLF）** | 3 799 B | `0D 0A`（CRLF+尾换行） | `74EB88D8…3262A` ✓ |
| 去尾换行变体（PowerShell 拼接所得，**非正当形态**） | — | 无 | `10844812…`（LF）/ `5C58D1B4…`（CRLF） ✗ |
⇒ **取证规范**：**库内身份用 blob id；工作树身份用 `Get-FileHash` 并声明"CRLF + 含尾换行"**；**跨版本一律 blob id / `git diff`**。裸 blob 导出可用 `git -c core.autocrlf=false archive <rev> -- <path>`；要复现检出形态则用默认 `archive`。

**判据自相矛盾（第 3 例，2026-09-17，t40）**：卡面判据写「`docs/插件文档/**` 内 `CAST` = 0 且 `SUCCEED` = 14」，但**同卡又要求**写入"过渡说明行"（原文含旧名 `CAST`）与"首批次行注（当时写作 `CAST`）"⇒ **物理最小 `CAST` = 2 行、`SUCCEED` = 15** ✗。裁定 **(i)**：**保留这 2 处旧名**（追溯性优先），判据改写为「**`CAST` 仅允许出现在说明行与首注这 2 处；其余一律 `SUCCEED`（命中 15）**」。
⇒ **纪律**：**写卡面判据时必须把"同卡强制要求的文本"计入计数**（凡卡面点名要写入的字样，都会进入 grep 命中）✓；判据要写"允许的最大/最小集合"，而不是拍一个理想值 ✓。
⇒ **口径纪律**：引用计数一律写明**统计对象**（`src` / `docs`）与**口径**（仅代码 / 含注释）；实例：`CastResult.SUCCEED` 在 `src` = **12 行**、在 `docs/插件文档` = **14 处替换**（9 + 5）—— 二者对象不同、不构成矛盾 ✓。

**第 12 次拦下（2026-09-17，B⑨）：映射建议不得替代源码** —— 卡面（源自清单 §15.2 的映射）写 `:39 → svc().timers().runRepeating(1L, 1L, …)` ✗，而源码实为 `runRepeating(new Runnable(){…}, 0L, 40L)`（周期 **40 tick = 2 秒**、`count` 上限 **3** ⇒ 惩罚总时长 **6 秒**）；照 `1L,1L` 改会让惩罚**快 40 倍** = **未申报的可见行为变化** ✗，而它**编译通过、Mode B 三行齐、集合差 `C=0`、双口径也未必报** ✗。执行者**以源码为准保留 `0L, 40L` 并申报** ✓（裁定：追认 ✓，`1L,1L` 作废 ✗）。
⇒ **纪律**：**凡"旧写法 → 新写法"的逐行映射，映射行的旧侧必须逐字取自源码**；执行者开工前**逐条比对**，不符者**以源码为准并申报** ✓。卡面/清单里的映射只是"建议"，**源码是唯一权威** ✓。
⇒ **计数表格纪律**（本轮三方各犯一次的沉淀）：**任何"总数 + 分项"的表格必须让分项之和当场等于总数**；**跨口径的数字不得混排在同一行/同一表**（要么全用改名前的口径，要么全用改名后的）✓。

**队长自查规则（2026-09-17，因当日两次踩同一坑而立）**：**队长本人做验收读数时，禁止使用 `Select-String` 出证据** —— 一律用 **`git grep -nE`（大小写敏感）** 或 `Select-String -CaseSensitive` ✓。当日两次假读数的实例：① 把 `\bCAST\b` 报成 4 处（实为 `type.cast`、`"…cast a skill!"` 的小写）；② 再次把 `\bCAST\b` 报成"1 个文件命中"（实为 `core/dispatch/ComponentRegistry.java:59` 的 `Class.cast` ✓，与 t41 无关）✗。
⇒ **常驻假阳性清单（供判据旁注）**：`core/dispatch/ComponentRegistry.java:59` 的 `type.cast(component)`（小写 `Class.cast`）是 `\bCAST\b` 判据的**固有陷阱** ⇒ 该判据**必须大小写敏感**；另 `debug-logs/回滚快照/**` 内 17 个文件含旧快照的 `CAST` 字样，属预期、不影响 `src` 判据 ✓。

**计数陷阱（第 12/13 例，2026-09-17）——「裸串 ≠ 实现者数」与「作用域必须写死」**：
- **第 12 例（前缀/裸串）**：`CastResult.SUCCEED` 与 `\bSUCCEED\b` **不是一回事** —— 组件 javadoc 里写的是反引号 `` `SUCCEED` ``（**无 `CastResult.` 前缀**）⇒ `CastResult\.SUCCEED` = **1 行**、`\bSUCCEED\b` = **2 行** ✗。**纪律**：**判据必须写清"查的是限定名还是裸串"**，并给出该写法下的期望值 ✓。
- **第 13 例（作用域）**：`git grep -n "implements.*LifecycleAware"` **不限定路径**会把 `debug-logs/回滚快照/**` 的**旧副本**算进来 ✗；限定 `src/main/java` 后仍会把**接口定义本身**（1 行）与 **`RoleInstance` 的 legacy 扇出**（`instanceof` 9 处、`awake(` 7 处）算进去 ✗ ⇒ **"裸串 = 0"永远不成立**。
  **纪律**：**每一条 grep 判据都必须连"路径作用域 + 匹配形态"一起写**，形如：`roleComponent/**` 内 **`implements …LifecycleAware`（真实实现）= 0**（javadoc 提及属预期，不算实现者）✓；**不得**用"裸串 = 0"当判据 ✓。
- **口径实例（B⑨ 交付时）**：`LifecycleAware` 在 `src/main/java` 内裸串 = 25 行 / 5 文件（接口定义 1 + `RoleInstance` 扇出 21 + 3 处 javadoc），而 **`roleComponent/**` 内 `implements …LifecycleAware` = 0** ✓ —— 两者都对，口径不同 ✓。

**计数陷阱（第 14 例，2026-09-17，由实施者自查发现）**：**`@( … | Measure-Object ).Count` 恒等于 1** ✗ —— 它数的是"**1 个 `Measurement` 对象**"，不是**命中行数/文件数**。实例：用 `@(git grep -c … | Measure-Object).Count` 报"1 文件命中"，而真实命中为 **0** ✗。
⇒ **纪律**：**判"有无命中"用 `git grep -n`（看输出或退出码：1 = 无匹配）**；**判"命中文件数"用 `git grep -l … | Measure-Object | ForEach-Object Count`**；**严禁**用 `@(… | Measure-Object).Count` 当计数 ✓。

**第 14 类隐患：文档驱动的回归（t49/F1，2026-09-17 独立验证发现）**：`core/hotbar/HotbarRenderer` 的 **javadoc 声称**"施放/冷却启动/冷却到点/能量/buff 五类事件由**各自的端口适配器**调用 `markDirty()`"，而实测 **端口适配器调用 = 0 行** ✗（全仓 `markDirty()` 真实调用仅 `RoleInstance:186/:205`；`isDirty()/clearDirty()` **只有定义、无消费者** ✗）⇒ **骨架未接线**，可见刷新实际仍由 legacy `updateHotbar()` 承担。
⇒ **风险**：后续若**据该 javadoc** 误判"置脏已覆盖刷新"而删 legacy 刷新路径 ⇒ **可见刷新会断** ✗（编译通过、Mode B 三行齐、集合差 `C=0` 都不报 ✗）。
⇒ **纪律**：**javadoc/注释不得声称尚未接线的行为** —— 骨架类必须在类注释里显式标注"**当前为骨架：X 仅翻标志、无帧末 flush；实际行为由 Y 承担**"，并把接线点写清（如"属阶段 4.4"）✓。**凡"实现者 = 0 的注释声称"都要在验证时单独列一条 finding** ✓。（处置：已书面授权 eng-会话 最小修 —— 仅改 javadoc/注释、不动逻辑 ✓。）

**第 15 类隐患：停服证据缺口（t49/F5）**：四份起服日志（`阶段4-起服日志-B9/B10/T1/T2.log`）内 `Stopping server|Saving players|Closing Server` = **0 行** ✗ ⇒ **"已停服"无法从日志复核**，只能验证**当前** 25566 无监听 ✓（验证者按要求**如实标注缺口而非推定** ✓，处置正确）。
⇒ **纪律**：**"已停服"必须留下可复核的物证** —— 每批起服结束后，除报"已停服"外，**另存停服证据**（控制台停服行；若 `job_kill` 导致日志被截断，则记录"停服时刻 + `25566` 监听 = 0 的现算命令与输出"到该批日志同目录的 `…-停服证据.txt`）✓；**验证者在无物证时只能标注缺口，不得推定** ✓。

**第 16 例陷阱（PowerShell 5.1 字符串切分强转，2026-09-17 由实施者实测发现）**：`"a`r`nb".Split("`r`n")` 会被**强转成 `char[]`** ⇒ 实际按 `\r`、`\n` **分别**切分 ✗；若随后 `-join "`r`n"` 回写文件，**行数会翻倍** ✗（对"整文件重写"类脚本是灾难性 ✗）。
⇒ **纪律**：**逐行读写文件一律用 `[System.IO.File]::ReadAllLines()` / `WriteAllLines()`**；确需按 CRLF 切分时写 `Split([string[]]@("`r`n"), 'None')` ✓；**任何"整文件重写"脚本，落盘后必须现算行数并与改写前比对** ✓（行数变化本身就是要拦下的信号 ✓）。

**并发写纪律（2026-09-17，⑤ 批次的实战教训）**：当**两名成员在同一棵树上工作**时，**共同文件**（本例 `core/RoleInstance.java`）必须遵守：
1. **落笔前重读该文件**（不得用更早读入的缓冲整文件回写 ✗）；
2. **提交方（队长）只提交独占/已完成文件**，绝不把在途的部分改动混入提交 ✗；
3. 完成后由**未写该文件的一方**做只读复核 ✓；
4. 若注释/代码被误回退：**不回退修复**（避免二次覆盖 ✗），由原修改者在其窗口补齐并记录 ✓。

**⑤ 独立复核的四条裁定（2026-09-17，报告 `debug-logs/测试记录/阶段4-T块⑧④①③⑤独立复核.md` = 9 757 B / `BB912530…`，判据 6/6 通过）**：
- **F5-1（low）= 入账为"证据缺口"，不补写** ✗：本批**没有**独立停服物证文件，事后补写只能是"当前态"而非"停服时刻"的证据 ⇒ **不得追溯补写成"已证实"** ✓；旁证（日志可独占读 + 端口无监听 + 无 server 进程）只作**辅助** ✓；**F5 物证纪律自下一批起强制执行** ✓。
  ⇒ **细化（同日，因实施者补记了物证文件而修订）**：**允许事后补记**，但必须 ① **文件名显式标注来源**（实例：`debug-logs/测试记录/阶段4-起服日志-T2b-停服证据（事后补记，非当时物证）.txt`）② **文首声明 post-hoc** 并**分节**（§1 当时观察 vs §2 事后现算，且写明"不等价"）③ **不得与当时物证混列**、**不得伪造控制台停服行** ✓。理由：防的是"**被后人误读为当时物证**"，而不是"不许留痕" ✓ —— 如实标注来源的补记**有实质价值**（本批 §1 含"20:45:37 `FileShare.None` 独占打开成功"这一**当时**观察 ✓）。

## 4.4 渲染器接管（T 块 ⑦ 的前置）：预案已就绪 + 三条裁定（2026-09-17）

**产物**：`debug-logs/阶段4-4.4渲染器接管预案.md` = 22 302 B / 178 行 / `42ADB2F2…B69B`（入库 `465ea70`；**只写文档、未碰 `src`** ✓）。含：① 五类事件的 `markDirty()` **逐字接线锚点**（施放已在位 `RoleInstance:186/:205`；冷却启动 `CooldownPortImpl:37/:39`；冷却到点 `RoleInstance:257/:324` 的 `runLater(updateHotbar,…)` → `runLater(markDirty,…)`；能量单一入口 `setCurrentEnergy:495`；buff 仅 `BuffManager.removeBuff:107-109`）② 帧末 flush 落点 `triggerUpdate():638-657` 尾 + `clearDirty()` 唯一消费者 ③ **9 维等价性对照表** ④ **三条顺序红线 + 5 文件可回滚单元** ⑤ 验证方案（静态 + Mode B + **F5 当轮物证** + 外观逐维取证 + **如实标注不可自动验证项**）⑥ **分级风险表**（B-1/B-2 blocker · H-1..H-3 high · M-1..M-3 medium · R-1..R-4 low）。

**三条勘察结论（只有读码才知道，已确认为本批最有价值产出）**：
1. **`shouldUpdateHotbar()` 一旦有过冷却记录就恒真**（冷却表条目只增不删 ✗）⇒ **首次施放后每 tick 无条件 `setItem`×2** ⇒ "**空闲 tick 零 `setItem`**"**今天并不成立** ✓ ⇒ 4.4 是真正的改善点（也是 **t17** 的靶子 ✓）。
2. **`shouldUpdateItemMeta()` ⇒ 冷却中每 tick 重刷技能名** ⇒ 秒数文本 `" x.xs"` **逐 tick 变化** ✓ ⇒ 4.4 **必须保留"冷却中置脏"**，否则外观冻结 ✗（**B-2**）。
3. **buff 只有"移除"刷新、"添加"不刷新** ⇒ 是 4.4 唯一会**改变可见时机**的接线点 ✓。

**三条裁定（队长）**：
- **R-1（`BuffManager.add` 是否置脏）⇒ 4.4 保持等价、不接线 `add`** ✓ —— 接线会让 `BARRIER` **提前出现** = 可见时机变化 ✗，属"本可避免的差异" ⇒ 不混进 4.4 ✗；该改进留 **t17/阶段 5** 并按其五要素申报 ✓（4.4 内 `markDirty()` 预期区间 **3~5** 按"不含 add"口径 ✓）。
- **R-2（能量"跨阈值才置脏"优化）⇒ 4.4 用无条件置脏** ✓（优化属阶段 5 ✓）。
- **R-3（与 `t17` 抢代码）⇒ 4.4 只定义语义、不合并 ticker** ✓。

**4.4 的 `markDirty()` 判据口径（R-1 收敛后，实施批直接照用）**：
- **口径必须先写死 = "仅代码 + 仅调用点"**（**排除定义行**与**注释/javadoc 提及** ✗）—— 因为裸 `git grep -n "markDirty()"` 在**改造前**就已是 **7 行**：真实调用 **2**（`RoleInstance:186/:205`）+ **定义 1**（`HotbarRenderer:25 public void markDirty() {`）+ **注释 1**（`RoleInstance:289`）+ **javadoc 3**（`HotbarRenderer:9/:11/:21`）⇒ 拿 7 去比 5 必然误判 ✗。
- **4.4 完成后（仅调用点口径）预期 = 5**：`RoleInstance:186`（handleCast）· `:205`（handleAttack）· `CooldownPortImpl.start`（冷却启动）· `RoleInstance.setCurrentEnergy`（能量单一入口）· `BuffManager.removeBuff`（buff 移除）✓。
- ⚠️ **冷却到点那条不在该计数内** —— 其形态是 **`runLater(this::markDirty, remaining)`（方法引用，不带 `()`）** ⇒ 必须**另用 `::markDirty` 或 `markDirty,` 单独测**（改造前现算 = **0** ✓，改造后应为 **1** ✓；否则会被误算成 0 或漏检 ✗）。⇒ 判据写作「**`markDirty()` 调用点 = 5 + 方法引用 1 处**」，**不要**写"预期区间 3~5"（口径不硬 ✗）。
- 同族口径提醒：`updateHotbar()` 带括号**改造前 = 8 行**（含定义与调用），**⑦ 的判据同样必须写"仅调用点"** 并**开工前现算** ✓。

## `t14` verify #3 的 I-x 现算证据（2026-09-17，队长执行 · 可直接作结卡依据）

作用域 = `src/main/java/**`，`git grep` **大小写敏感**，口径 = **仅代码 / 仅实现者或调用点**（**不减注释就会误判** ✗）：

| 项 | 现算 | 备注 |
|---|---|---|
| I-3 四类 `*Aware` **实现者** | **0** ✓ | `implements.*(LifecycleAware\|UpdateAware\|SanTEChangeAware\|EnergyChangeAware)` |
| I-4 `instanceof` 四类 | **0** ✓ | — |
| I-6 迁移标记 | **0** ✓ | `isMigrated\|markMigrated` |
| I-7 `bind(` / `bindAndRegister` | **0 / 0** ✓ | 异常文案字面量随方法删除一并消失 ✓ |
| I-8 组件侧 `rolesContext()` | **0** ✓ | 限 `roleComponent/**` |
| I-9 `USE_COMPONENT_PIPELINE` / `getInstance()` | **0 / 0** ✓ | — |
| I-10 `core/Role.java` 内 `Supplier` | **0** ✓ | 装配已改 `(id, ComponentFactory)` |
| I-11 组件渲染覆写 | **0** ✓ | 全仓 **4** = 基类（属 ⑦ ✓） |
| I-13 `setIsInSanTEPunishmentState\|isInSanTEPunishment` | **仅代码 0** ✓ | **含注释 2 处**（`DefaultSanTEZeroPunishment:24/:44` 的"已删"说明 ✓）⇒ **引用必须写"仅代码口径"** ✗ |
| I-14 `\bCAST\b` / `CastResult.SUCCEED` | **0 / 13 行** ✓ | 13 = 12 代码引用 + 2 javadoc − …（口径：`git grep -c` 逐文件求和 ✓） |

**两处队长自曝的量法滑手（已修正，供后人避坑 ✗）**：① `I-13` 不减注释 ⇒ 得 2 ✗（代码口径 0 ✓）；② 端口文件数用 **flat 目录** + `-Filter '*PortImpl.java'` ⇒ 得 0/1 ✗。
**端口宿主权威口径**（引用必须写明目录 ✓）：**接口 = `core/ports/` 下 11 个**（`Self`/`EnergyPort`/`SanTEPort`/`VitalsPort`/`CooldownPort`/`BuffPort`/`FactionPort`/`DamagePort`/`TimerPort`/`ComponentLookup` + `ComponentServices` ✓）；**实现 = `core/` 下 10 个**（`*PortImpl` **8** + `SelfImpl` + `ComponentLookupImpl` ✓）。

**第 17 例陷阱（glob pathspec 必须落到文件，由验证者 t15 开工前发现）**：`git grep -nE '…' -- 'src/main/java/**/listener'` 以**目录名**结尾 ⇒ **不匹配任何文件** ⇒ 返回 **0 行** ✗ —— **0 不是"干净"，而是"路径没落到文件"** ✗✗（**假绿灯**，比不检查更危险 ✗）。
⇒ **纪律**：**glob pathspec 的末段必须匹配到文件**（`-- 'src/main/java/**/listener/*'` ✓ 或目录前缀式 `-- src/main/java/com/…/listener` ✓）；**任何"判据返回 0"的结论，必须先拿一个"已知有命中"的样例会诊该判据** ✓（**判据自检** ✓）。
⇒ 实例：**I-15 的判据行本身有此缺陷** ⇒ 裁定：**判据缺陷单列一条 finding（criterion defect）** ✓，**不得只写"不成立"** ✓。

**t15 开工前侦察发现的两处真实缺口（2026-09-18，待修；属阶段 4 收尾后的追补）**：
1. **I-15（blocker）· 组件 SanTE 钩子仍穿过 Bukkit 事件总线** ✗：链路 = `RoleInstance.setCurrentSanTE(:516-521)` → `callEvent(new SanTEChangeEvent(...))` → `RoleEventListener.onSanTEChange(:18-21)`（已注册 ✓）→ `instance.triggerSanTEChange(:622)` → 广播组件钩子(`:633-635`) ⇒ 与 I-15"**由容器直接派发**"不符 ✗。**修法**：容器**直接**派发组件钩子 ✓；**保留两个事件的对外发布**（红线 ✓）；**删 listener 转发** ✓。同类死路径：`RoleEventListener.onEnergyChange` → 空壳 `triggerEnergyChange` ✓。
2. **I-14（high）· 缺重入护栏与"合并补发一次"** ✗：`pre == now` 不派发**已实现** ✓（`:625`），但"派发期间同类型写入合并、结束后按最终值补发一次"**未实现** ✗（护栏关键词全仓 **0** 命中 ✓）⇒ 结合缺口 1，**钩子内写 SanTE 会嵌套派发** ✗。**修法**：护栏 + 合并补发（与缺口 1 同批 ✓）。
3. **I-11/I-14 的验收方式（medium）· 判据方法物理不可执行** ✗：原文要求"**单测**"，但仓库**无测试源集**（`src/test` 不存在 ✓、268 个 `.java` 中 0 个含 `@Test` ✓、构建 `test NO-SOURCE` ✓、`build.gradle.kts:31` 仅一行 `testImplementation` ✓）。
   ⇒ **裁定（原则性区分）**：**"代码没达到判据" ⇒ 必须改代码** ✗（不得改判据 ✗）；**"判据方法物理不可执行" ⇒ 允许把验收方式映射为结构性证据** ✓（**意图不变** ✓），并把"**建立测试源集**"单列为**阶段 5 质量项** ✓。

**用户的六项裁定（2026-09-18，冷却自管理 + 顺序；均为"推荐案 A"）**：
1. **D1 = A**：框架**不再自动启动**冷却 ⇒ 组件显式 `svc().cooldowns().start(getCooldownTicks())` ✓（`RoleInstance:184/:203` 两处自动启动将删除 ✓）；
2. **D3 = A**：刷新**分两步** ✓ —— 本项**保留** `runLater(updateHotbar)` ✓，等 **4.4 接管后**再换 `markDirty()` ✓（否则 = "看不见的刷新丢失" ✗，F1 同类 ✗）；
3. **D4 = A**：回调三分法 `onCooldownEnd(EXPIRED / ENDED_BY_COMPONENT / RESTARTED)` ✓；
4. **D6 = A**：本项**保持**技能/主武器两张冷却表 ✓（合并留阶段 5 ✓）；
5. **顺序 = A**：**先补 I-15/I-14 两处缺口** ✓，**再做**冷却自管理 ✓；
6. **小修 = A**：`MeiqiheziCircleSlashSkill` 的过期 javadoc **由队长立即改** ✓（已入库 `3608b34` ✓）。
规划文档：`debug-logs/冷却自管理架构规划.md` ✓（只规划未写码 ✓）。

**工具层已知限制（第 3 次遭遇，务必记住）**：**作废依赖会永久阻塞下游卡** ✗（`t47` 依赖已作废的 `t46` ⇒ 它**既不能改派也不能作废** ✗，却**占着** `core/Skill.java`、`core/MainWeapon.java`、`core/RoleInstance.java` ⇒ **任何需要这些路径的新卡都会被"路径重叠"拦下** ✗）。
⇒ **处置**：**书面授权执行 + 队长结卡**（`t39`/`t48` 先例 ✓）；**拆卡时依赖只能指向"会真正完成"的卡** ✓；**凡 `RoleInstance` 相关批次，默认走书面授权路径** ✓（不要浪费时间试立卡 ✓）。

**用户过程偏好（2026-09-18 明示）**：**需要用户拍板时，主动用"提问"形式一次性问清**（不要散在正文里 ✗）✓ —— 本轮六个设计点即以此方式拍板 ✓。
- **F5-2（low）= `debug-logs/测试记录/阶段4-起服日志-T2b1.log` 仅为"卡面交付名副本"**（与 `T2b.log` **逐字节相同**，非第二份证据 ✓）⇒ 后续引用一律以 **`T2b.log`** 为权威 ✓，避免"两份证据"的误读 ✗（**冻结日志不追加注释** ✓，口径写在清单/交接里 ✓）。
- **INFO-1（方法学）= 新增陷阱：正则字符集必须覆盖目标字符集** —— 复核者第一版用 `"([a-z_]+)"` 比对 id，因 id 含大写（如 `red_deeplySorrow_skill`）**漏 9 条并得出假结论** ✗；改用"取**全部**字符串字面量 + 多重集比对"才正确 ✓。
  ⇒ **纪律**：**凡用正则筛选目标（id/常量/路径）时，必须先用"反例抽样"验证字符集覆盖** ✓；**且结论必须换一种量法复现**（本项目已有的双口径/多重集双读正是为此 ✓）。
- **INFO-2（口径）= `git grep -c "::new" -- registry` = 17 = 16 条装配行 + 1 处 javadoc 示例** ⇒ 引用"16 条装配"时须写明**装配行粒度** ✓（与"路径口径 vs 值口径"同族 ✓）。

## T 块进度（2026-09-17 20:1x，入库 `2811e06`）

| 项 | 状态 | 说明 |
|---|---|---|
| ② 删全局开关 | ✅ 提前完成（B① 修正中） | — |
| ⑧ 删 legacy `*Aware` 接口与转发 | ✅ T-1（t44） | `core/RoleComponentAware/**` 4 接口 + 目录已删；`RoleInstance` **六组** legacy 扇出删除；`implements.*(四类)` 与 `instanceof (四类)` **均 0** |
| ④ 删旧派发入口 | ✅ T-1（t44）+ 残留待 T-2c | `MainWeapon` 四条 legacy 声明、`Skill.onLeftClick/onDrop`、listener legacy 分支、5 个入口回退均已删；**残留** = `Skill.onRightClick` 死声明（T-2c 清） |
| ① 删迁移标记 | ✅ T-2（t45） | 代码口径 = 0；仅剩 **3 行注释**（T-2c 清） |
| ③ 6 入口无条件走新管道 | ✅ T-2（t45） | `runComponentPipeline` = **定义 1（纯委派）+ 调用点 6** |
| ⑤ `bind` → `ComponentFactory` 构造期注入 | ⏳ **T-2b（t46）** | 需 `registry/**` 同批（`RoleLoader` 16 条 `::new` + `RoleRegistry` 的 `Supplier` 签名）；`bind(` 现 **6 处** |
| 残留清理 | ⏳ **T-2c（t47，依赖 t46）** | 3 行注释 + `Skill.onRightClick` 死声明 + `RoleInstance` 的 T-1 回退；**必须给"热键栏刷新契约"的结构性证明** |
| ⑦ 删基类渲染方法 | ⏸ 待渲染器接管 | `roleComponent` = 0 / 全仓 = **4**（`Skill:43/:99`、`MainWeapon:52/:99`）；**前置事实**：`HotbarRenderer` 仍是骨架、无帧末 flush ⇒ 可见刷新仍由 `RoleInstance.updateHotbar()` 承担 |

**第 13 次拦下（T-1 批内自查，2026-09-17）**：按 ④ 字面删掉 `castSkillRightClick` 的 legacy 回退后，**未迁移**组件的施放会**静默失效**（旧路径还会做 `skill.onRightClick(...)` + 1 tick 热键栏刷新）⇒ 实施者恢复回退并显式标注"T-1 残留 / T-2c 收口" ✓（**根因**即上表 ⑦ 的前置事实）。

| **T** | 收尾块（**8 项**；**第 ② 项已提前完成**；第 ⑧ 项 = `EnergyChangeAware` 链路删除） | ⏳ 待 B⑨ |
**阶段 4 关键裁决**：① **过渡期 `svc` = `bind` 等价实现**（五条件；终态切回构造期注入，§9 条）；② **逐批派据必须显式**（`isMigrated()`，**禁 `instanceof RoleComponent`** —— 改基后人人 instanceof ⇒ 未迁移组件会被送进 `onCast` 默认 no-op 的新管道 = **行为静默回归**，§12 条）；③ **增量优先模式**（先落可证不可达的接线，再改基/启用，§13 条）；④ 每批收官须跑 **Mode B + 正序集合差只看 `C`**，且**运行深度如实声明**（组件侧运行期等价由 t15/t18 探针覆盖）。

**阶段 4 的四次「开工前拦下」**（全部属于「绿灯全过但语义错」—— 构建 / Mode B 三行 / 集合差 C=0 / 双口径 **四盏灯都发现不了**）：
1. `instanceof RoleComponent` 作派据 ⇒ 未迁移组件被送进 `onCast` 默认 no-op（§12）；
2. `USE_COMPONENT_PIPELINE` 总闸遮挡已迁移组件 ⇒ 旧覆写已删、落到空实现 = 功能静默失效（§20，由 **文档工程师只读发现**）；
3. 药水 `ambient/particles` 标志位不保真（3 参签名表达不了 `CircleSlash` 的 `true,false`）⇒ 粒子/图标可见变化（§15 + R-1 方法族）；
4. 去掉 `LifecycleAware` 而无新钩子广播 ⇒ 前摇任务跨角色清除仍执行 = **O-4 回归**（⇒ B②-c）。

**据此固化的四条纪律**：① **每批三条结构性证明**（判据只认标记 / 旧回调实现体为 0 / 单一入口委派）；② **开工前现场复核**（超窗口就在块边界处停并上报，不得硬启动）；③ **不得静默产生可见差异**（本可避免者一律加方法族/重载，而非「申报变化」）；④ **计数与身份一律「命令 + 截至 commit/时点」**（不写漂移中的绝对值；契约级数字除外但须附命令）。
**防返工纪律**：变更面以「**实施前最后一次裁定的原文**」为准；若收到新裁定而块已开工，**在块边界处停并确认**（不得中途改范围，也不得按旧裁定硬做完再补）。
| 2 评审 | t11 | ✅ **completed（verdict = pass；2 low）** | `debug-logs/测试记录/阶段2-评审报告.md`（引 §0 方法 / §1 I-1,I-2 / §2 行为等价） | 见 log |
| 3 装配层 | t13 | 🔄 **进行中**（`registry/RoleRegistry.java` 等在途；与 t11 并行） | — | — |
| 4 组件重构（10 组件分批） | t14 | ⏳ pending | — | — |
| 5 运行时性能 | t17 | ⏳ pending | — | — |

## 2. 关键身份（引用证据时**现算**，勿手抄）

| 对象 | 路径 | 身份 |
|---|---|---|
| Mode B 锚点（阶段 1+ 的"改动前"基线） | `debug-logs/测试记录/阶段1-起服锚点-t2ModeB.log` | 6 199 B / SHA256 `2A4BD077…B524` |
| 阶段 1 起服日志 | `debug-logs/测试记录/阶段1-起服日志-t5ModeB.log` | 6 076 B / `A3EB7CB6…F63C` |
| **阶段 0–1 验证报告** | `debug-logs/测试记录/阶段0-1-验证报告.md` | **引用以章节为主**（§4 I-16 / §5 多重集 / §6.1 O 项表 / §6.2 import / §0.1 F-3）；**数值身份以现算为准**（末版实测 28 366 B / `6F323451…A32C` / commit 见 log） |
| 阶段 0–1 评审报告 | `debug-logs/测试记录/阶段0-1-评审报告.md` | **引用以章节为主**（§1.2 清单 / §2.2 药水账本 / §2.3 B-1 / §5 挂账）；数值身份以现算为准（末版实测 26 588 B / `CCE2F5E0…D9952`） |

> **报告身份链（t7 报告，勿引用中间版）**：`ff95fa4`=19 295 B/`FE951307…` → `4435c2f`=23 076 B/`D5FAE4A2…` → `3fe4372`=26 721 B/`D61E388C…` → `49a8abd`=28 366 B → v5 `d2da5ab` = 30 474 B / 332 行 / `E137C0D6…4607` → **终版 v6 = 31 282 B / 336 行 / 文件 SHA256 `E2411833…8066`（commit `0db5995`）**。**引用一律现算**；报告只引章节号即可。
> **读法澄清（防误读为矛盾）**：t7 报告 §6.1 的"`isReady` 命中 = 0"指 **`core/RoleInstance.java` 内**的定义/使用 = 0；全仓另有 20 处 `isReady` 均为**局部变量名**（调用的是 `instance.isMainWeaponReady(...)` / `isSkillReady(...)`）→ **O-18 确已关闭**。
| 阶段 1.8 起服日志 | `debug-logs/测试记录/阶段1.8-起服日志-t6ModeB.log` | 9 786 B / `0D0C5F3B…76A2` |
| 探针（**产出 t6 结果的那一版**） | `.scratch\bot\t6-quit-persistence.js` | 22 223 B / `EFDBF9EB…9770` |
| 探针结果 | `.scratch\bot\t6-quit-persistence.result.json` | `meta.scriptIdentity.sha256 = EFDBF9EB…9770`；`meta.probedAt = 2026-09-16T14:54:31.156Z` |
| 仪器受管副本区（已入库、队长授权） | `debug-logs/回滚快照/工装/` | 内容寻址副本 + `MANIFEST.txt`（`ok=7 bad=0`） |
| 多重集（数值 / 字符串） | — | 阶段 1 末 **381 / 197**；t6 收工仍 **381 / 197**（diff 0） |

## 3. 开放项（按挂账处检索）

1. **用户侧**：真实四行合格线（含第 3 行 `[SHDFGamePlugin] Succeed to find the dependency…`）**只能在装了 SHDF 的 25565 外服确认** —— 尚未执行；本工程 `runServer`（25566）原理上不产生该行 → 采 Mode B（三行 + 替代证据），**不得把三行当四行等价**（指南 §6.3）。
2. **阶段 4 完成后**：客户端视觉验收清单（t15 的"双角色冒烟""§3.5.1 五类事件"）—— **一次性**跑完，不放宽。
3. **实例 ②（条件性）**：`ticker` 与 `BuffManager` updater 两个 1-tick 任务是否停止 —— 档位 = **静态证据 + 运行反证**（`jcmd`/JVM attach 在本沙箱**结构性不可用**，三次实测否决）；若日后有更宽执行模式方可升级档位（指南 §6.6 ②）。
4. **跨插件**：SHDF 对局中介入时，掉线重连会被下游**重建为新实例**（§6.4.1）→ I-16 的"重登无角色"**只在本插件单独运行时成立**，t7/t15 **不得**据字面判"裁决未生效"。
5. **计数器口径**：`ShadowHunterRolesPlugin.getInstance()` 现为 **24（core 15）** —— t9 的替换目标与 t11 的 I-1 以此为准（26/17 是阶段 1 之前的历史值）。

## 4. 纪律（不可违反）

- **不申请提权**：approval policy = `ask`，无人应答即失败关闭 → 被沙箱拒绝就**停下、原样留痕、报告"需用户放行"**，不绕道、不换写法硬试。
- **不执行 git 写操作**（`add`/`commit`/`checkout`/`restore`/`stash`/`reset`）—— **提交权在队长**；工作树状态一律用 `git status --porcelain` **现算**。
- **版本/身份一律现算**：以「字节数 + mtime + SHA256」认定；**行号仅辅助、必须附内容片段**；引用实测证据须带**仪器身份 + 运行时刻**。
- **仪器纪律**：改动后**先跑排练/自检**再交付；自检 stub **必须来自实测报文**，不得沿用被测代码的字段假设；被引用的仪器版本**必须可复原**（仓库内受管副本）。
- **收尾纪律**：起服实例与任务一一对应、**用完停服**；起服日志在下次起服前**另存副本**。

## 5. 中断后的恢复步骤

1. `git log --oneline -15` → 看检查点（应有 13 个，最新为 t6 收口）；
2. `git status --porcelain` → 确认是否干净（不干净时先判断是哪个任务的在途改动，勿回滚）；
3. 读《最终重构指南》§6.6（证据与验收政策）+ §7（各阶段清单）；
4. `agent_teams_status` 看任务面板 → **依赖满足后调度器通常会自动派发**（t10 实测：自动派发 + `claim_task` 直接成功，无报错；t5/t6 的"没派"更像时序差而非故障）→ **仅当某任务在依赖满足后长时间仍 pending 时**，才给拥有者下一封"开工授权"消息（本会话曾以此解除 t5/t6 的停滞）；
5. 继续前先确认工作树干净，再让对应阶段按 §7 判据执行。

**插件文档（t26，eng-文档 交付）**：`docs/插件文档/` 5 份（README / 角色与技能 / 架构与组件模型 / 开发指南-新增角色或组件 / 日志与证据归档规范）+ `debug-logs/README-文档索引.md`；**日志二分法**：`debug-logs/`（调试日志、被 .gitignore 忽略）vs `docs/*.log`（冻结证据、`git add -f`、引用带 SHA256+时点）。

**T 块进度（本行原为阶段 1 时期旧注，2026-09-18 更正）**：**T 块 ①–⑥⑧ 已全部落地** ✓；**⑦（基类渲染方法）唯一保留**，依设计待 **4.4 渲染器接管**（`HotbarRenderer` 仍是骨架、无帧末 flush ✓；预案 `debug-logs/阶段4-4.4渲染器接管预案.md` + 前置清单 `debug-logs/阶段4-T块⑦前置清单.md` ✓）；**⑤ `bind`→`ComponentFactory` 构造期注入**已落地（`bind(` 两口径 **0/0** ✓）；`t47` 为工具死锁空卡（内容已随 `04c0fd1` 落地 ✓）。**用户下一项需求 = 冷却自管理**（规划 `debug-logs/冷却自管理架构规划.md` ✓，待其确认 4 个设计点后派卡）。

## 2026-09-18 晚 · 现状快照与新增纪律（本文件已迁至 `debug-logs/测试记录/`）

**目录与规则（用户定）** ✓：`docs/` **只留 `插件文档/`** ✓；测试记录/日志/验证评审报告/agent 过程记录**统一在 `debug-logs/`** ✓（`debug-logs/**` 默认被忽略 ⇒ **新文件入库需 `git add -f`** ✓；**已跟踪文件不受忽略规则影响** ✓）；仓库根有**精简 `README.md`** ✓（不得含 agent 临时信息 ✗）。

**三条新陷阱（第 18–20 例，均本轮实测）** ✗：
- **第 18 例：正则 `.*` 会跨空格/换行 ⇒ 误报**。实例：判"旧路径残留"时用 `docs/阶段[0-9].*起服` 得 4 行"残留" ✗，真实 token 是**未移动**的 `docs/阶段4-迁移清单.md` 等，只是同行后文出现"起服日志" ✓；严格口径（不跨空格）+ **逐文件反查** 后为 **0** ✓。
- **第 19 例：git 命令的路径必须用正斜杠**。反斜杠会让 `git rev-parse HEAD:<path>` / `ls-tree` **解析失败**并报误导性的 **"exists on disk, but not in HEAD"** ✗（而 `git add`/`status` 容忍 ✗）—— 我据此一度误判"证据没入库" ✗。
- **第 20 例：`git grep` 只搜索"已跟踪"文件** ⇒ 对**未跟踪的新产物**恒返 0 = **假阴性** ✗（与"glob pathspec 末段必须落到文件"同族 = **先确认工具能不能看到目标** ✓）。⇒ **纪律**：**未跟踪/新产物一律用直读复核**（`Select-String`/`ReadAllLines` ✓ 并写明工具与作用域 ✓）；`git grep` 的结论**只在 commit 或 `git add -f` 之后**才作数 ✓。

**命令编写规范（用户定，已入 `docs/插件文档/开发指南-新增角色或组件.md` §4.5）** ✓：既有 `/role` **保持传统实现不迁移** ✓（迁移会改 tab 补全/用法文案等**可见行为** ✗，无功能收益 ✗）· **新增子指令 ⇒ 一个子指令一个类**（对齐 `SHDFGamePlugin` 的 `SubCommand` 接口 + `command/` 包各自成类 ✓，**禁止**往中央 `switch` 堆分支 ✗）· **将来新增顶层命令 ⇒ Brigadier + `LifecycleEvents.COMMANDS`** ✓（**绝不**同时写进 `plugin.yml commands:` ✗）· **临时/调试入口可内联** ✓（判据 = 可整段删除 + 删除清单 ✓）· 文案一律 Adventure `Component` ✓。

**调度器迁移（用户裁定：方案 → 实证探针 → 再切适配器；后改为"现在就做"并合批）** ✓：迁移面 = **唯一 1 文件** `platform/BukkitSchedulerAdapter.java` ✓（平台外调度调用 = 0 ✓）；已核实 `GlobalRegionScheduler`/`ScheduledTask` 签名（`javap` ✓：`run/runDelayed/runAtFixedRate(Plugin, Consumer<ScheduledTask>, …)`；**`cancel()` → `CancelledState`（非 void）**）；**禁用 `execute(Plugin,Runnable)` 替代 `run(...)`** ✗（差别 = **有无句柄** ⇒ 会丢句柄、任务无法回收 ✗）；方案 `debug-logs/调度器迁移方案.md`（`5B0406D3…`）✓。

---

## 附录 · 目录调整与新规则（2026-09-17 记录）

> **本文件是项目过程记录**，现位于 **`debug-logs/测试记录/`**（该目录集中存放测试记录、运行证据与项目过程记录）。

1. **新规则**：工程师成员修改代码时，由文档工程师同步整理文档（文档与代码并行推进，避免"代码先行、文档滞后"）。
2. **新布局**：新增 `debug-logs/测试记录/`，把**测试与证据类**文件集中归位 ——
   - 冻结证据（起服/构建日志）`*.log`；
   - 独立验证 / 评审报告；
   - 测试与演练记录（回滚演练、冒烟清单、`t2-*.txt`、停服证据等）；
   - 项目过程记录（含本文件）。
   设计与规范类、各阶段交付记录、迁移清单、`docs/插件文档/`、`debug-logs/回滚快照/` 保持原位。
3. **路径同步已完成**：全仓 `docs/**` 内指向已移动文件的引用已改为 `debug-logs/测试记录/…`（脚本化替换：`docs/` 前缀 139 处、`docs\` 前缀 19 处、裸反引号文件名 46 处，共 204 处 / 24 个文件）；`debug-logs/回滚快照/**` 内的历史记录**保持原样**（属"当时路径"）。
4. **引用纪律不变**：身份一律现算；历史记录中的旧路径视为"当时路径"，引用时以当前实际位置为准并注明。

---

## 附录二 · 纪律台账（2026-09-18 深夜；本文件属**滚动件** ⇒ 不挂身份哈希，引用请用"文件名 + 章节 + 内容片段"）

### A. 陷阱第 18–23 例（全部当日实测，务必先读）

| # | 陷阱 | 正确口径 |
|---|---|---|
| **18** | **正则 `.*` 跨空格/换行 ⇒ 误报** | 判"旧路径残留"时 `docs/阶段[0-9].*起服` 得 4 行假残留；改用**不跨空格**的正则 + **逐文件反查** ⇒ 0 ✓ |
| **19** | **git 命令路径必须正斜杠** | 反斜杠令 `rev-parse HEAD:<path>`/`ls-tree` 解析失败并报误导性的 `exists on disk, but not in HEAD` ✗（`add`/`status` 却容忍 ✗） |
| **20** | **`git grep` 只搜已跟踪文件** ⇒ 对**未跟踪新产物**恒返 0 = **假阴性** ✗ | 新产物一律**直读复核**；`git grep` 结论只在 commit/`git add -f` **之后**才作数 ✓ |
| **21** | **别把易变身份当锚点** | 跨消息引用用「**文件名 + 章节 + 内容片段**」✓；哈希**只**用于"同一时刻的验收读数"且必须**现算** ✓。**滚动件**（验证报告/交接/迁移清单/交付小结）**不挂身份** ✗；身份链只挂**冻结件**（jar/快照/工装/提交点）✓；**自检脚本不得内嵌哈希** ✗（否则脚本成为又一个过期副本 ✗） |
| **22** | **工具：`changedPaths` 必须与卡面 `inScope` 同形（仓库相对）** | 写成带仓库前缀的 workspace 相对形式 ⇒ 报 `is undeclared` ✗，且**失败尝试的路径会被记为"声明"并污染后续尝试** ✗ ⇒ 后果就是 `t51` 卡面 output 只剩占位串、结构化数组未落卡，且 **completed 卡不可 reassign ⇒ 无法回填** ✗ |
| **23** | **`Get-NetTCPConnection -State Listen -LocalPort 25566` 在服务器确实监听时也可能返回 0** ✗ | **不得单独用它判"已停服"** ✓；改用**三路互证**：`latest.log` 独占打开 ✓ + TCP connect 结果 ✓ + `netstat` 是否 LISTENING ✓ |
| **24-a**（**A 组 · 计数/枚举口径**：工具**看得见** ≠ **看到的是目标** ✗） | 处方 = **改命令/改口径** | `git grep` 看不见未跟踪件（假阴性）✗ · `ScheduledTask` 11 行含 1 行既有 javadoc ✗ · `getScheduler()` 8 行含 1 文案 + 1 注释 ✗ ⇒ **作用域 + 匹配形态 + 是否扣除定义/注释/文案；未扣除者显式标为"裸计数"** ✓ |
| **24-b**（**B 组 · 时点/证据层级**：读数的**时点/层级** ≠ **断言的对象** ✗） | 处方 = **换证据来源**（不是改命令 ✗） | 把**已迁移后**的文件当**迁移前** ✗ · 用**现状树**读数断言**历史** ✗ ⇒ **断言"迁移前/当时"必须换层级** ✓ |

**B 组的可操作判据（`eng-审阅` 实操得出）** ✓ —— 断言"某形态在过去存在/不存在"时，**必须给出最小充分证据**：
1. 「**该对象在 `<commit>` 的形态**」= `git show <commit>:<path>` 的**逐字片段**（带行号或函数名锚点 ✓）；
2. 「**该形态的引入/消失点**」= `git log -S "<符号>" -- <path>` **定位提交** ✓，再用其**父提交**看迁移前形态 ✓（今天正是 `6421eda~1` 这一步纠正了误判 ✓）；
3. **只用 `git grep`（当前树）不构成**以上任何一条 ✗。

**实例（本批迁移的左列）** ✓：逐提交查出 `RedSolitaryArroganceSkill` 自**阶段 0/1**（`ff76907`/`c565785` 各 1 行）起就在用 Folia 语义 API（**宿主 Paper**），直到 **`6421eda`（t9）**才换成平台端口 ✓ ⇒ **"迁移前就用 Folia API"的唯一例外** ✓ ⇒ 本批裁定：两列**按调用点逐点填** ✗（左-1 = Bukkit `runTaskTimer(…, 0L, …)` ⇒ `0→1` 归一并实测首触发 tick ✓；左-2 = Folia `runAtFixedRate(…, 0L, …)`（该技能，**值原本就是 1**）⇒ 只需证"**实现换了、值没换**" ✓），**不做全局二选一** ✗。

**贯穿 18–24 的元纪律（当日三度复现）**：**任何计数/枚举结论必须写明「作用域 + 匹配形态 + 是否已扣除定义行/注释/文案」**；未扣除者**显式标注为"裸计数"** ✗。实例：`git grep` 看不见未跟踪件 ✗ · `ScheduledTask` 11 行含 1 行既有 javadoc ✗ · `getScheduler()` 8 行 = 适配器 **3** + 探针代码 **4** + 探针**文案/注释 1** ✗（不是"探针 5" ✗）。

### B. 当前进度与在办（截至本附录写入时）

- **阶段 4 全部落地** ✓（组件 14/14、T 块 ①–⑥⑧、⑦ 待 4.4）· 改名 `CAST→SUCCEED` ✓ · `fix1`（I-15 容器直派 + I-14 重入护栏 + F6）✓ · **冷却自管理**（组件自启 `start` / 重启覆盖 / `boolean end()` / 每 tick 扫描关 **O-21** / `onCooldownEnd` 三分法 / 保留 `runLater`）✓ · 临时调试入口（op 门控、可删）✓ · 目录重整（`docs/` 只留 `插件文档/`、根 `README.md`、`debug-logs/**` 默认忽略 + 新文件需 `-f`）✓ · 命令编写规范（开发指南 §4.5）✓；
- **阶段 3–4 验证与评审双向闭环** ✓：`t50` 复核验 = **pass**（V-01/V-03 已确认修复；干净树 `e539a53` 复跑：Mode B 三行 + 0 ERROR + `C=0` + 当轮物证）· `t52` 正式评审 = **pass**（0 blocker/0 high；1 medium + 2 low；`t16` 因 `t15` 终态 failed 永久阻塞 ⇒ `t52` 为其替代卡）；
  - **⚠️ `t52` 评审锚点的增量说明**（`t52` 与 `t50` 均为终态卡、**不可回填** ✗ ⇒ 按纪律落到本台账 ✓，**按三件套引用、不挂哈希** ✗）：`t52` 评审完成于权威验证报告（`debug-logs/测试记录/阶段3-4-验证报告.md`）的三处增补**之前** ⇒ 由 `eng-验证` 逐项评估**增补净效果** ✓：① 冷却自管理复算（框架不再代启动、组件自启 15 处、`end()` 真语义、每 tick 扫描 ⇒ **O-21 关闭**、`runLater(updateHotbar)` 保留）= **正向** ✓；② **§4.3 复跑 #2**（含探针 jar：Mode B 三行 + 0 ERROR + `C=0` + 当轮物证）= **正向** ✓；③ V-14 由"签名漂移 low"改为"**已裁定：非缺陷（口径）**"（该变更属用户 D-list 批准）= **降级** ✓ ⇒ **三处增补均未引入新的 blocker/high** ⇒ **`t52` 的 pass 结论依然成立** ✓。后续引用该报告请以 **§4.3「复跑 #2」** 与 **V-14 段落**为准 ✓。
- **`t51` 调度器实证探针 = completed**（**卡面 output 仅占位串** ✗，见陷阱 22 ⇒ **其结论与结构化验收以执行者结卡报文 + `debug-logs/测试记录/阶段4-schedprobe-停服物证.txt` 为准** ✓）：结论 = **裸切适配器会炸** ✗（`runAtFixedRate` 拒绝 `initialDelay ≤ 0`，生产侧**显式 `0L` 2 处**：`DefaultSanTEZeroPunishment:56`、`MeiqiHeziBloodySlashSkill:50`；另 `RoleInstance:118`、`BuffManager:136` 多行实参待摊平 ✓），**其余四维（线程/周期/延时/取消）实测等价** ✓；
- **在办 = 切适配器**（`platform/BukkitSchedulerAdapter.java`，**回滚单元 = 1 文件** ✓）：裁定 = **适配层归一 `Math.max(1, initialDelayTicks)`** ✓ + **实测首触发 tick 等价性** ✓ + **逐调用点核对 `runRepeating` 参数顺序** ✓（⚠️ `Scheduler` = `(task, delay, period)` 而 `TimerPort` = `(delay, period, task)`，**同名反序** ✗ —— 归一只能归一"值"、**不能归一"顺序"** ✗ ⇒ 顺序核对是**独立必查项**，不能靠"双入口归一"兜底 ✗）+ 修正方案里被证伪的 **D6「语义应等价」** ✓。

### C. 已排定但未执行的约定

1. **T52-F1（medium，当前制品不在 t50 覆盖之下）→ 已闭合** ✓（时序：jar 19:51:56 ⇒ `schedprobe.log` 19:52:21 ⇒ 停服物证 19:55:47 ⇒ **运行证据晚于制品** ✓），**但闭合路径的后续两棒仍要盯**：适配器切换会**再次重建**制品 ✗ ⇒ **该批必须自出物证** ✓；冒烟后**删除探针** ⇒ 又一次重建 ⇒ **那一批必须重新取证** ✓；
1b. **⚠️ 队长自己的结卡字段也内嵌过易变身份** ✗（`t50` 的 output 写了当时报告的哈希，其后报告又更新两轮 ⇒ **已过期** ✗；**终态卡不可改** ✗）。**补救以本台账为准** ✓：t50 的验收以 **`debug-logs/测试记录/阶段3-4-验证报告.md` 的 §4.3「复跑 #2」段落**为准（**按三件套引用，不挂哈希** ✗）。**纪律延伸** ✓：**结卡 output / acceptance 字段一律不写哈希** ✗（同第 21 例：别把任何易变身份当锚点 ✓）；`t50` 的 run#2 证据两份亦已按卡面 `inScope` 名对齐并入库 ✓（`阶段4-起服日志-t15复跑2.log` / `…-t15复跑2-停服物证.txt` ✓）；
2. **T52-F2（low）**：删 `RedDeeplySorrowSkill.java:3` 死 import ⇒ 判据 = **I-7 裸 grep 归零** ✓（不把"注释口径说明"当长期解法 ✗）；
3. **T52-F3（low）→ 已修** ✓：冒烟清单改为"**运行时现算制品身份**"+ 待填表 + 运行深度声明 ✓；
4. **阶段 5 候选**：`RequiresComponents`（机制未引入 ⇒ 用户裁定 A：如实记 finding + 归入阶段 5 ✓）· 建立**测试源集**（本阶段验收方式已映射为结构证据 ✓）· T 块 ⑦（4.4 渲染器接管 ✓）· `t24` 阶段 3–4 整合结案（**评审前置已满足** ✓）；
5. **清账（写在 `.gitignore` 注释与规范里）** ✓：`debug-logs/**` **新**文件（含 `.md`）默认忽略 ⇒ 入库需 `-f` ✓；**判定是否被忽略必须用 `git check-ignore --no-index -v <path>`** ✓（裸 `check-ignore` 对已跟踪件**不给结论** ✗）；陈旧规则 `!docs/测试记录/**/*.log` 已删 ✓。

### D. 测量口径类陷阱（第 4 个实例，2026-09-18 晚新增）

- **行数口径必须统一到 `[System.IO.File]::ReadAllLines`** ✓；**用 `-split "`r?`n"` 会把文件尾换行多算 1 个空元素** ✗。实例：调度器迁移批的适配器 `70 → **69**` 行、探针 `582 → **581**` 行（**字节与 SHA256 未变** ⇒ 判据与结论不受影响 ✓）。与既有"`Split("`r`n")` 被强制转 `char[]`" ✗ 同族 ⇒ **测量口径类陷阱**（第 4 例 ✓）。
- **git 在非 ASCII 路径上默认输出【转义引号路径】** ✗（`core.quotepath`）⇒ 形如 `"debug-logs/\346\265\213…"` 的字符串**直接喂给 `Test-Path`/文件 API 会报 `Illegal characters in path`** ✗，导致"哈希取不到"这类**假缺失** ✗。⇒ **正确姿势**：`git -c core.quotepath=false ls-files …` ✓ 或 `git ls-files -z` ✓；**需要文件身份时先经该口径取路径再算哈希** ✓（测量口径类陷阱第 5 例 ✓；与第 20 例"工具看不见目标"同族 ✓）。
- **【纪律 22 的缺口】"同一时刻只允许一个起服"保护的是【占用期】，不保护【停机后的取证窗口】** ✗。实例（2026-09-18 复跑 #3）：停机后约 **3 秒**即有一次并发起服接续 ⇒ `run/logs/latest.log` 被覆盖 ✗ ⇒ 该轮**无法给出 `C=0` 结论**（只能如实声明缺口 ✓，`C=0` 由复跑 #1/#2 的完整副本承担 ✓）。⇒ **纪律延伸（2026-09-18 晚，按 `eng-平台` 的提案强化）** ✓：**停机方必须在【仍在线】时就冻结日志** ✗ —— 即在 `Done` + 空闲点先 `Copy-Item run/logs/latest.log → debug-logs/测试记录/<本批日志名>` ✓，**然后才** `job_kill` ✓ ⇒ 此后**任何**后继起服都无法污染其证据 ✓（这比"停机后立即复制"更稳 ✓，因为停机与复制之间存在竞态窗口 ✗）。**相应地，起服方**除核对端口/进程外，**还必须等到停机方明确的"已释放（物证已落）"广播** ✓（**端口空闲 ≠ 对方取证窗口已结束** ✗）；并发者之间**不得**在他人刚停机后的数秒内起服 ✗。**处置范本** ✓：误捕到的他人日志**改名标注归属** ✗（如"…误捕（属 HH-mm-ss 那一次他人起服）" ✓）**且不得当作本次证据引用** ✗。**实例** ✓：`eng-验证` 复跑 #3（停服后约 3 秒被并发起服覆盖 ✗）⇒ 该轮**无法给出 `C=0` 结论** ✓（改为如实声明缺口 ✓，`C=0` 由复跑 #1/#2 的完整副本承担 ✓）；`eng-平台` 已主动认领该次并发起服（其判断依据只有"端口已释放" ✗）✓。
- **【仪器口径三连】行号/存在性类读数有三个"不报错的假数据"来源** ✗：① `Get-NetTCPConnection -State Listen` 在服务器**确实监听**时也可能返回 0 ✗（改用 `latest.log` 独占打开 + TCP connect + `netstat` 三路互证 ✓）；② **行数口径**：`-split "`r?`n"` 会把文件尾换行多算 1 个空元素 ✗（统一用 `[System.IO.File]::ReadAllLines` ✓）；③ **`Get-Content <file> | Select-Object -Skip N` 对含 CJK 的 UTF-8 无 BOM 文件会静默行号漂移** ✗（实测取到**别的方法体**，而同批另几个文件恰好对上 ⇒ **假数据不报错** ✗✗）⇒ **行号定位一律用 `git grep -n`（可带 `-A`）或 `read` 工具** ✗，不得用"`Get-Content` + 偏移" ✓。
- **【自相矛盾的遗留措辞】** ✗：`platform/BukkitSchedulerAdapter.java` 的 javadoc 曾写"**首触发相位最多相差 1 tick**" ✗，而后续实测已证明**原生 `0L` 本就是"下一 tick"** ⇒ 归一后**无相位差** ✗ ⇒ 该类注释须在 `t53` 内更正为"**归一后首个触发 tick 与原生逐字相同（已实测）**" ✓。**教训** ✓：**同一变更里的"论证性措辞"必须随实测结论一起回收** ✗（否则文档自相矛盾，且比缺文档更坏 ✗）。
- **【左列分组的两处订正（我错、实现者纠正）】** ✗✓：我原把「`RoleInstance` ticker」列入"左-1（迁移前 `0L`）"✗ —— 逐提交现算（`6421eda~1:105-106`）= **`1L, 1L`** ⇒ 它属**"值未变"**组 ✓。⇒ **定稿分组** ✓：**左-1（需 `0→1` 实测）= 3 处**（`BuffManager`(0,1) ✓ / `DefaultSanTEZeroPunishment`(0,40) ✓ / `MeiqiheziBloodySlashSkill`(0,2) ✓ —— 与"显式 `0L` = 3 处"自洽 ✓）；**值未变 = 2 处**（`RoleInstance` ticker **1→1** ✓、`RedSolitaryArroganceSkill` **1→1** ✓，后者迁移前即 Folia 语义 API、值自 `ff76907` 起始终 `1L,6` ✓）；**旁证** ✓：同 period 仅 delay ∈ {0,1} 的三组清白对照（period 1/2/10）首触发**均为 1** ⇒ **声明 `0L` 与 `1L` 同 tick** ⇒ 归一**不产生相位增量** ✓。**教训** ✓：**"一个全局左列覆盖两个迁移"的同型风险发生在队长自己的表上** ✗ ⇒ **分组必须逐调用点现算迁移前实参**（`git show <rev>:<path>` ✓），**不得按印象归类** ✗。
- **【迁移期口径残留 = 文档驱动回归的第二形态】** ✗（2026-09-18 实测）：阶段 4 收口后，**`开发指南` 头部仍写着迁移期规则** ✗ ——「新写的主动组件**必须走"旧回调"形态**才真正生效」「`onCast` 因**开关为 `false` 不可达**」+ 样板把某组件标为"旧回调形态" ✗ ⇒ **照它写代码会直接写出失效组件** ✗✓。⇒ **纪律** ✓：**每个"收口批"必须扫一遍活文档的"迁移期口径"残留** ✗（判据例：`开关为 \`false\`` 残留 = 0 ✓、`旧回调` 不得出现在"新组件应如何写"的语境 ✓）；这与"文档滞后于代码"不同 ✗ —— **它是"文档比代码更旧且会导致错误实现"，危害更高** ✗。同批还纠正了两处事实：适配器行"全仓唯一直接 `Bukkit.getScheduler()` 的地方" ✗ ⇒ 现状为"唯一实现 = `getGlobalRegionScheduler()` 的 run/runDelayed/runAtFixedRate + `initialDelay ≤ 0` 归一" ✓、`Task` 行"包装 `BukkitTask`" ⇒ "包装 `ScheduledTask`" ✓。

### E. 调度器迁移批（已完成并入库 `cba1488`；**入账方式 = 台账注明** ✓）

#### E.1 制品代次与 F1 防线（2026-09-18 晚，实测核算 ✓）

- **代次地图** ✓（每代**自有** Mode B 日志 + 停服物证、**从不覆盖前代** ✗ ⇒ 台账不会双计 ✓）：**B** = 探针本体（结论 `CANNOT 裸切` ✗）→ **C** = 适配器已切 + 探针 A/B（`0/10` 两路径 `1/21` ✓）→ **D** = **归一化矩阵**（8 组 × 两路径全同 ✓）+ 全量调用点审计 + **方案 D6 改判** ✓ → **E** = **双入口归一** + 两接口 javadoc 反序对照 + **④p 端口链直测**（`1/11` ✓）→ **逐调用点四列表**（doc-only，引 D/E 实测 ✓）。
- **⭐ F1 教训的正面防线（四代全合格 ✓）** ✗：**① mtime 排序** `log > jar` **且** `doc > log` ✓；**② 自证绑定**：**每份物证都写明"本代 jar 身份 + 本代日志身份"** ✓（**比只靠 mtime 强** ✓ —— 后继重建无法伪造这种绑定 ✓）；**③ 机制**：实现者**每次都在服务器仍在线时先冻结日志、之后才 `job_kill`** ✓ ⇒ 其证据**天然免疫**后继并发起服 ✓。
- **可复用判据** ✓（今后任何"制品 + 运行证据"批次都照此）：**"证据 mtime 晚于其覆盖的 jar" + "物证内写明被覆盖 jar 与对应日志的身份" 两条同时成立**，才算"**证据覆盖它自己的制品**" ✓。
- **`0→1` 的措辞裁定** ✓ = **两说并存**（保守写法）：「**经实测未见偏移**（同 period 下声明 `0L` 与 `1L` 首触发同 tick ✓；8 组两路径逐字相同 ✓），**但按保守口径仍登记为已申报项**」✓ + **Folia 前提声明** ✓（若未来出现 `0L` 与 `1L` 首触发不同的实证 ⇒ 本条**须重新申报** ✗）。
- **⭐ 显式绑定（T55-F1 的 fix，2026-09-18 晚补记）** ✓：**终态制品的字节码 = E 代**（"双重归一 + 参数顺序"那代）**逐 class 相同** ✓ ⇒ **E 代的全部运行证据（Mode B 三行 + `C=0` + 停服三路互证）对当前制品仍然有效** ✓。**判据** ✓：① 改动为**纯注释**（`git diff` 剥块注释与行注释后**非注释行 = 0** ✓，与 HEAD 逐字比较 True ✓）⇒ 注释不进 `.class` ✗ ⇒ 字节码不变 ✓；② 代次地图中 **E = 本批**（见 E.1 ✓）。
  - **⚠️ 不得用 jar 的 SHA256 判"字节同一"** ✗（**新陷阱，2026-09-18 晚实测，含精确机制** ✓）：**受控实验**（同一 `javac`、仅注释不同）证明 —— **`javap -c -p` 反汇编逐字相同（79/79 行 ✓）**，差异**只在 `LineNumberTable` 的约 20 行行号** ✗（javadoc 变长 ⇒ 方法体源码行号位移 ⇒ `.class` 字节变 ⇒ jar SHA 变 ✗）。同一平台层代码因此出现**三份不同 SHA**（`A74A5747…` / `CD4FD3B2…` / `CDAA4F63…` ✓）。⇒ **"改注释 jar 不变"只在未触发重编译时成立** ✗（Gradle `:jar UP-TO-DATE` ✓）；**一旦重编译，jar SHA 必变而语义未变** ✗。
    - **口诀** ✓：**"同一份代码"用【反汇编】或【源码非注释内容】判定** ✓；**jar SHA 只用于"制品身份"** ✓（配 mtime 自证链 ✓），**不用于"代码是否相同"** ✗。
    - **唯一可观察差异** ✓（须如实申报 ✗）：**异常栈里打印的行号**（如 `LineNumberTable` 位移 ⇒ 栈帧行号 ±几行 ✓）；**行为语义不变** ✓。
    - **同源状态口径** ✓（实现者自纠 ✗）：判"重建是否逐字节相同"必须**同源状态**比较（"删除前那一刻 vs 重建后" ✓），**不得拿不同源状态的另一代 jar 相比** ✗（否则会得出"重建后 SHA 变"的错误结论 ✗）。
  - **为何需要这一行** ✗：迁移批的 log/物证 mtime（20:03:20 / 20:08:51）**早于**当前制品（20:33:19）⇒ 若逐份套用 E.1 的"证据 mtime 晚于 jar"判据，**会误判为覆盖缺口** ✗（**F1 同型误报** ✗）。⇒ **判据补充** ✓：**"证据覆盖它自己的制品"有三种成立方式 —— ① 证据晚于该制品 ✓；② 该制品与证据所覆盖的那一代【字节码相同】（须给出"纯注释改动"或 class 级比对 + 代次归属 ✓）；③ 语义等价证明（"源码非注释内容零改动" + `javap -c -p` 反汇编全等 ✓ + 改动面仅注释 ⇒ 唯一可观察差异 = 异常栈行号 ✓）—— 用 ③ 时**必须在台账注明"该制品身份未被运行证据直接覆盖，按语义等价接受"** ✗，不得静默当作 ① ✓。**本批裁定** ✓：`t57` 后的制品（适配器注释回收）按 **③** 接受 ✓；其**制品身份级**运行覆盖交由 **`t56`（验证者会重建制品并自出证据）** ✓ + **用户冒烟**（在现制品上运行 ✓）共同闭合 ✓。
**⭐ 已闭合（2026-09-18 晚，证据级而非仅语义级 ✓）**：`eng-平台` 在"**Folia 动因回收**"增量（3 个 `platform/` javadoc 纯注释改动 ✓，`Folia-ready` 在 src 全树 = **0** ✓、非注释行改动 = **0** ✓）时，**让该轮的 Mode B 跑的就是含 `t53`+`t57`+本增量全部注释改动的制品** ✓ ⇒ **当前树第一次拥有"制品身份级"的运行证据**（`阶段5-起服日志-非Folia口径.log` ✓ + 说明文件 ✓；F1 链 `jar 20:37:49 < log 20:38:39 < 说明 20:39:53` ✓；Mode B 三行 + **只看 `C=0`** ✓ + 停服三路互证 ✓）⇒ **`t55` 的 T55-F1（low）从此可按判据 ① 直接引用，不再需要 ③ 兜底** ✓。
- **范围外发现（只上报未改 ✓）**：`roleComponent/red/RedSolitaryArroganceSkill.java:22` 有一行**历史注释**提到"去掉 **Folia** 全局调度器误用"（O-5 留痕 ✓）—— 该行**陈述属实、属组件层历史注记** ✓（裁定只针对 `platform/` 现状口径 ✓）⇒ 保留 ✓，供活文档统一口径时参考 ✓。
- **左列分组的定稿** ✓（我原分组错 ✗，实现者纠正 ✓）：**需 `0→1` 实测 = 3 处**（`BuffManager`(0,1) · `DefaultSanTEZeroPunishment`(0,40) · `MeiqiheziBloodySlashSkill`(0,2) ✓ —— 与"显式 `0L` = 3 处"自洽 ✓）；**值未变 = 2 处**（`RoleInstance` ticker **1→1** ✓、`RedSolitaryArroganceSkill` **1→1** ✓，后者迁移前即 Folia 语义 API、值自 `ff76907` 起始终 `1L,6` ✓）。**pickaxe 复核** ✓：`git log -S "getGlobalRegionScheduler"` = **恰好 4 个提交**（`ff76907` 引入 → `c565785` 在用 → `6421eda`(t9) 换端口 → 本批探针/适配器 ✓）。

> **入账方式** ✗：`t51` 已是 `completed` 终态、**不可 reassign/回填** ✗ ⇒ 本批增量**记不进该卡** ✓ ⇒ **以执行者结卡报文 + `debug-logs/测试记录/阶段5-调度器迁移-停服物证.txt` 为准** ✓（按三件套引用、**不挂哈希** ✗；另 `阶段5-起服日志-调度器迁移.log` ✓）。

- **结论** ✓：`platform/BukkitSchedulerAdapter.java` 已切到 **`GlobalRegionScheduler`** ✓（**回滚单元 = 1 文件** ✓；`Scheduler`/`Task` 接口 **diff = 0** ⇒ 调用方零改动 ✓；**未误用 `execute(`** ✓）；`initialDelay ≤ 0` 由适配层 **`Math.max(1, …)` 归一**（适配器 + `TimerPortImpl` **两入口** ✓）。
- **零可见差异【实测】** ✓：原生 `runTaskTimer(0,10)` 首次/第三次 = **1/21** ＝ 新适配器 `runRepeating(0,10)` **1/21** ✓；`runLater(20)` delta **20** ✓；线程 **`Server thread`** ✓；`cancel()` **幂等** ✓ ⇒ 探针 verdict 由 `CANNOT swap as-is` 翻为 **`CAN swap … zero visible difference`** ✓。
- **生产路径实跑** ✓：`/role set meiqihezi` → **1-tick ticker 约 80 tick** → 被动发甲 → `/role energy get` = 100 → `/role clear` → **全程零 SEVERE/ERROR** ✓ ⇒ `runRepeating` 与 `Task.cancel()` **生产路径可用** ✓。
- **已申报未覆盖** ✗（挂 `t24`/`t18` 收口）：① 3 个 `0L` 调用点的**业务触发**（需受击构造；其**入参等价性**已由 A/B 覆盖 ✓）；② 一次性 `run` 无原生对照腿（但其生产调用点 = **0** ✓）；③ **真实四行合格线**（25565 装 SHDF）未执行 ✓；④ Folia 真机未验证（本项只做 Global ✓）。
- **收尾** ✓：`t53`（纯 javadoc 清账：`platform/Task.java` 等把"统一包装 `BukkitTask`/Folia `ScheduledTask`"改成现状口径 + 注明 `initialDelay` 归一为 `≥1` 及理由 ✓）已立卡派发 ✓；随后 `eng-审阅` 评审该批 ✓（必查：自带物证覆盖自己的制品 ✓ / 参数顺序逐调用点核对 ✓ / `0→1` 按点分路径申报与实测 ✓ / 逐调用点四列表 ✓）。

### F. 卡务两条硬约束（2026-09-18 晚实测，给后续实现者省一轮返工 ✓）

**三条申报口径精化（`t56` 独立验证的 findings，2026-09-18 晚）** ✓ —— 都属"**申报须带口径**"家族 ✗：① **件数/计数会随流水增长** ⇒ 申报必须写「**扫描时刻 + 口径**」✗（实例：锚点扫描 52 → **57** 份 ✓，两次都成立但固定值会被当成错 ✗）；
② **裸计数与扣除口径不同** ⇒ 写"应 N 处"必须注明**扣除注释/javadoc** ✗（实例：`Math.max(1` 裸 **4** = 代码 **2** + javadoc **2** ✓）；
③ **跨代引用会误判** ⇒ "物证自述 jar 身份与现算一致"必须**按代际**核 ✗（实例：`t51` 物证自述 `146 010 B` 那代 ≠ 当前 `148 297 B` 那代 ✓，两者都对、但不能跨代比 ✓）。

**旧表述的两类区分（2026-09-18 晚，`eng-平台` 纠正我第 5 次误读后立规 ✓）** ✗：
- **① 事实被证伪的旧断言** ⇒ **必须回收** ✓，且**不得在原处复述旧原句** ✗（实例：适配器"首触发相位最多相差 1 tick" ✗ 被实测推翻 ⇒ 已改成"归一不产生相位差（已实测）" ✓）。
- **② 属实的历史留痕** ⇒ **保留** ✓（可审计 ✓），但**必须显式标注"已作废 / 仅存留痕"** ✓（实例：`Task.java:9` 复述早期"统一包装 `BukkitTask`/Folia `ScheduledTask`"的表述 ✓ —— 属实 ✓ ⇒ 保留 ✓）。
- **共同点** ✗：**裸 grep 对两类都会命中** ⇒ **判据必须逐行判读 + 写明口径** ✓；另：**`porcelain = 0` 的正确读法是"已提交"，不是"未做"** ✗（终态卡场景下极易混淆 ✓）。

**⭐ 归档纪律的回报（2026-09-18 晚实测 ✓）**：**"class 级等价、差异仅 `LineNumberTable`"已被【第三方独立复现】** ✓ —— `eng-验证` 用**两代归档 jar**（`7A21729B…` / `DBE04077…`）自行比对：**逐类字节比对（96 个 `.class`）仅 2 个类有差异** ✓（`BukkitSchedulerAdapter.class` + `$1.class` ✓，与"仅 1 个源文件改动"自洽 ✓）· **`javap -c -p` 语义层 79 vs 79 行逐字相同** ✓ · **`javap -v -p` 304 vs 304 行、26 处差异 = 文件头（路径 + 自身 SHA-256）+ `LineNumberTable` 的 `line NN:` 项** ✓。⇒ **`t56` §7-4 的"未独立复现"对【已归档的代次对】已解除** ✓；**build E（`A74A5747…`）仍不可复现** ✗（当时无副本 ✓，标注保持 ✓）。**教训** ✓：这条纪律当时看似"以防万一" ✗，实际把**只能自证**的主张变成了**任何人都能验证**的事实 ✓。

**新纪律：外部工具必须先确认可调用，再采信其输出** ✗（仪器口径族第 6 例 ✓）：实例 —— `javap` **未找到**（`Get-Command` 失败 + `JAVA_HOME` 未设 ✓）⇒ 比对得到的两行"逐字相同 = True"是**空输出比较的假阳性** ✗✓（当事者当场作废并用绝对路径 `C:\Program Files\Java\jdk-21\bin\javap.exe` 重跑 ✓）。⇒ **判据** ✓：**任何外部工具（`javap`/`git`/`netstat`/`7z`…）先跑一条存在性或 `--version` 判据** ✓，**其输出为空或异常时不得当"相等/通过"** ✗（空 vs 空 = 相等是此处最危险的假阳性 ✓）。

**覆盖必须【按代际认领】（2026-09-18 晚，`eng-验证` §7-5 立规 ✓）** ✗：**"当前制品已有身份级运行证据" ≠ "某一历史批次的覆盖"** ✓ —— 前者只针对**当前那一代** ✓，历史批次（如迁移批 build E）仍靠它**自己那代**的证据 + class 级等价 ✓ ⇒ **每条运行证据只认领【它自己那一代】制品** ✗，**不得用新证据冒充旧代覆盖** ✗。**推论** ✓：制品归档是**向后有效**的 ✗（现存档案只有当前代 ✓；B/C/D/E 及 t57 那代**未留副本 ⇒ 其 class 级等价主张标注为"未独立复现"** ✓，**不可追溯补齐** ✗）。

**新纪律：被验制品必须随代留档（2026-09-18 晚，`eng-验证` 建议 ⇒ 已采纳并当场执行 ✓）** ✗：
- **问题** ✓：此前每代只留"身份读数"（尺寸 / mtime / SHA256 ✓）⇒ **代际一旦重建即无法回溯** ✗ ⇒ "**class 级等价**""**`javap -c -p` 反汇编逐字相同**"这类主张**只能靠当事人自证** ✗、**无法被独立复现** ✓。
- **规则** ✓：**凡有运行期证据覆盖的制品，都把该 jar 复制到 `debug-logs/回滚快照/jars/` 并在 `MANIFEST.txt` 登记**（命名含 mtime + SHA256 前 8 位 ✓）；MANIFEST 记录 **bytes / mtime / SHA256 / 覆盖证据 / 备注** ✓。
- **已执行** ✓：本轮已把**被运行证据覆盖的那一代**存档并登记 ✓（覆盖证据 = `阶段5-起服日志-非Folia口径.log` ✓）。**无法回溯的历史代次**（B/C/D/E 及中间重建）已在 MANIFEST 中**如实标注"class 级等价主张未独立复现"** ✓。
- **与既有纪律的关系** ✓：既有"证据须晚于其 jar 且自绑定该 jar 身份"管"**证据认领制品**" ✓；本条管"**制品本身可回溯**" ✓ —— 两者合起来才让"独立复现"在**证据链层面**成立 ✓。
1. **卡面 `verify` 的每一条都必须出现在 `commandsRun` 里** ✗ —— 否则校验器打回：`implementation completion requires a passed commandsRun entry for every verify command`（实例：`t53` 首次结卡只报了自选命令 ⇒ 被拒 ✓，补齐 3 条后才受理 ✓）。⇒ **立卡人**把 verify 写成"执行者真会跑的命令" ✓；**执行者**开工前先取**卡面原文** ✓。
2. **证据文件名必须与卡面 `inScope` 逐字一致** ✗ —— 否则 `changedPaths ⊆ inScope` 拒收（实例：`t53` 起初用 `阶段5-起服日志-Task-javadoc.log` ⇒ 与卡面 `阶段5-起服日志-javadoc.log` 不符 ✓，改名对齐后才过 ✓）。⇒ **立卡人**把 inScope 写成**最终文件名** ✓；**执行者**改名后要**同步修掉文件内的自引用** ✓。

### G. 未闭合的技术尾巴（截至本附录写入时）

- **`platform/BukkitSchedulerAdapter.java:25` 仍写着被实测推翻的旧说法** ✗：「…归一为 1 tick；**首触发相位最多相差 1 tick**、…」 —— 而 `t51` 的 C 项与归一化矩阵已实测「**原生 `0L` 本就是下一 tick ⇒ 归一后无相位差**」 ✓（8 组两路径逐字相同 ✓）⇒ **自相矛盾** ✗ ⇒ 已立补救卡 ✓：纯注释 + **"非注释行改动 = 0" + "强制重建后 jar 与上一代逐字节相同"** 两条证明 ⇒ **既有 build E 的运行证据仍覆盖该制品、无需再次起服** ✓（`t53` 已先证此法可行 ✓）。
- **两条教训** ✓：① **"论证性措辞"必须与实测结论同批回收** ✗（否则文档自相矛盾，比缺文档更坏 ✗）；② **卡面 acceptance 与消息里的追加要求要写在同一处** ✗ —— 本次漏项正是因为那条追加要求只在**消息**里、且与执行者轮次交叉 ✗。