# 阶段 4 · 双角色冒烟清单（由用户执行 · 填表即可）

> **目的**：满足 `t14` 卡面 verify 第 4 条「双角色（meiqihezi / red）各技能、被动、主武器人工冒烟，逐项记录结果」，同时作为 `t15`（阶段 3–4 独立验证）的**运行期证据**。
> **执行者**：用户（需真人客户端）。**服务端**：本机 `25566`（`online-mode=false`）。
> **记录方式**：在每行「结果」列填 `OK` / `FAIL + 现象` / `N/A`；有异常请附**截图或聊天栏原文**。
> **口径**：只记**你实际看到的现象**，不要推测；看不清就填 `未观察`。

---

## 0. 准备（3 步）

1. 启动服务端（若你希望由我起服，说一句即可；起服窗口**同时只允许一个**）。
2. 进服（`localhost:25566`），**先不选角色**，确认：`/` 之类命令可用、聊天栏无报错刷屏。
3. 记录起始时间与你用的客户端版本：__________

**基线观察（选角色之前）**
| 项 | 期望 | 结果 |
|---|---|---|
| 热键栏 | 空/默认，无角色物品 | |
| 聊天栏 | 无 `Exception` / `Error` 刷屏 | |

---

## 1. 选角色 → 热键栏基线

| 步骤 | 期望现象 | 结果 |
|---|---|---|
| 选 **meiqihezi** 角色 | 热键栏出现：槽 0 = 主武器、槽 1/2/3 = 三个技能 | |
| 每个槽的四段渲染 | ① 图标 ② …… ③ **名称** ④ **冷却秒数**（就绪时应为无/0） | |
| **主武器槽**（槽 0） | ⚠️ **不显示秒数**（这是与技能槽的**冻结差异**，不是 bug） | |
| 装备被动 | 角色身上**自动穿上**装备 | |

选 **red** 角色时重复上表（槽 0 = 主武器、槽 1/2/3 = 三个技能）。

---

## 2. 技能逐项（每个技能三态：就绪 / 冷却中 / 能量不足）

> **三态期望**（`Skill` 基类四段，按此顺序判定）：
> ① **冷却中** → 图标为冷却态 + 名称灰 + 秒数倒数；
> ② **被禁用**（眩晕/沉默等）→ 材质 `BARRIER` + 文案 `Skill has been disabled.`；
> ③ **能量不足** → 材质 `STRUCTURE_VOID` + 名称灰 + `" ENERGY LACK"`，且 **lore 不额外加行** ✗；
> ④ **就绪** → 正常图标 + `Skill is ready.`。

### meiqihezi（3 技能 + 1 主武器）
| 组件 | 触发 | 期望（概述） | 结果 |
|---|---|---|---|
| `meiqihezi_skill_unconcern` | 右键 | 进入冷却并倒数；冷却中再右键**无效且不扣能量** | |
| `meiqihezi_skill_bloody_slash` | 右键 | 前方范围伤害；**能量 8**，不足 → ③ 态 | |
| `meiqihezi_skill_circle_slash` | 右键 | 圆形范围伤害/粒子；**能量 15** | |
| `meiqihezi_mainWeapon_juejue` | 左键 / 右键 | 主武器攻击路径（左键近战、右键技能）；主武器槽**无秒数** | |

### red（3 技能 + 1 主武器 + 流血被动）
| 组件 | 触发 | 期望（概述） | 结果 |
|---|---|---|---|
| `red_solitaryArrogance_skill` | 右键 | 进入冷却并倒数 | |
| `red_evilShock_skill` | 右键 | 伤害/粒子表现 | |
| `red_deeplySorrow_skill` | 右键 | 施放 + **减少自身 SanTE** | |
| `red_mainWeapon_sanctifiedBlade` | 左键命中 | **给目标叠加流血层数**（见 §3） | |

---

## 3. 被动逐项

| 组件 | 触发条件 | 期望现象 | 结果 |
|---|---|---|---|
| `meiqihezi_equippments_passive` / `red_equippments_passive` | 选角色 / 销毁角色 | 选角色**自动穿戴**；**角色销毁时装备清空**（⚠️ 这是阶段 0 的修复点，务必看「销毁后是否清空」） | |
| `autoRecoverEnergy_passive` | 周围 10 格无敌人 | **每秒 +3 能量** | |
| `autoRecoverSanTEPassive` | 周围 10 格无敌人持续 5 秒 | 开始**每秒 +3 SanTE** | |
| `red_bleed_passive` | 被 `red` 主武器命中 | 流血**层数叠加**、按层数结算伤害；**账本已私有化到组件内**（外观应与改造前一致） | |
| `default_san_te_zero_punishment` | **SanTE 降到 0** | 出现惩罚表现（粒子 / 标题 / 眩晕 `100t`），**约 2 秒一次、最多 3 次、总约 6 秒** | |

### ⚠️ 本次唯一的**已申报行为变化**（请重点观察）
> **「真变化才派发」**：SanTE **已经是 0** 时再次受到 `decreaseSanTE`（例如 `red_deeplySorrow_skill` 的减 SanTE），**不再重置/延长**惩罚。
> - **旧行为**：SanTE 0 → 0 也会**重新触发**惩罚（粒子/标题/眩晕重启）；
> - **新行为**：**不重置、不延长**（惩罚自然走完）。
> - 请记录：**触发过惩罚后，再让 SanTE 保持 0 触发一次减 SanTE，惩罚是否被重启？** 结果：______（期望：**不重启** ✓）

---

## 4. 生命周期与隔离

| 项 | 期望 | 结果 |
|---|---|---|
| **掉线即销毁** | 退出服务器再进 ⇒ **无角色**（需重新选角色） | |
| 选新角色替换旧角色 | 旧角色物品/装备被正确清空，新角色正常 | |
| **双角色隔离** | 两个角色各玩一轮，**不出现**串物品/串冷却/串被动 | |
| 冷却跨角色 | A 角色的冷却不影响 B 角色 | |
| 服务端日志 | 整场**无新增 `Exception` / `Caused by`** | |

---

## 5. 收尾

| 项 | 期望 | 结果 |
|---|---|---|
| 全场结束 | 无卡顿、无物品错位、无残留角色状态 | |
| **销毁角色后**再选另一个 | 一切正常 | |

**结束时间**：__________　**客户端版本**：__________　**服务端 jar**：`135308 B @20:40:56`

---

## 填完后

把本文件的「结果」列填好（或直接在聊天里回我一份）⇒ 我会：
1. 把它作为 **`t14` verify #4 的证据**结卡（**不伪造、不代填** ✓）；
2. 同步把它作为 **`t15`（阶段 3–4 独立验证）的运行期证据** ✓；
3. 任何 `FAIL` 项 ⇒ 开修复卡（按"分级 findings + 复现步骤"处置 ✓）。

> 备注：**`t14` verify #3（I-x 静态核对）我已现算跑完**（结果见 `docs/重构进度与交接.md` 与结卡说明 ✓），所以你只需完成**第 4 条（人工冒烟）**即可 ✓。
