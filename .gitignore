# ============================================================================
# ShadowHunterRolesPlugin —— git 忽略规则
# 覆盖：Java/Gradle 构建产物、Paper 服务端运行物、IDE、操作系统、临时文件、本地配置。
# 注意：本文件不支持行尾注释（# 只有在行首才是注释），故所有说明均独立成行。
# 提示：已被 git 跟踪的文件不受本文件影响；要停止跟踪需先执行 git rm --cached <路径>。
# ============================================================================

# ---------- Java / Gradle 构建产物 ----------
build/
out/
target/
bin/
*.class
*.jar
!gradle/wrapper/gradle-wrapper.jar
.gradle/
.kotlin/
# 工作区内的 GRADLE_USER_HOME（本次重构设置，构建缓存、不入库）
.gradle-work/

# ---------- 本地 / 机器相关配置 ----------
# 含本机路径（pluginCopyPath 指向本机测试服）；构建脚本按“可选属性”处理，缺失不影响构建
gradle.properties
local.properties
*.local
.env
.env.*
!gradle/wrapper/gradle-wrapper.properties

# ---------- Paper 服务端运行物 ----------
run/
server/
plugins/
logs/
# 说明：测试记录 / 冻结日志 / 验证评审报告 / agent 过程记录统一放在 debug-logs/（其忽略规则见下方 debug-logs 段）
*.log
eula.txt
server.properties
ops.json
whitelist.json
banned-ips.json
banned-players.json
usercache.json
world/
world_nether/
world_the_end/
debug-logs

# ---------- IDE ----------
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath
.factorypath
*.code-workspace
nbproject/

# ---------- 操作系统 ----------
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ---------- 临时 / 崩溃转储 / 补丁残留 ----------
*.tmp
*.bak
*.swp
*.swo
*~
*.orig
*.rej
hs_err_pid*
replay_pid*

# ---------- 调试/工作日志目录（内容默认不入库，只保留说明；冻结证据需要入库时用 git add -f） ----------
# 说明：测试记录、回滚快照与过程文档自 2026-09-18 起统一放在 debug-logs/ 下；
#       本规则只影响“新增/未跟踪”文件 —— 已被 git 跟踪的历史证据仍留在版本控制内（用户裁定 A）。
debug-logs/**
!debug-logs/README.md

# ---------- 协作与工具产物 ----------
.agent-teams/
.scratch/
.b-flow-scratch/
