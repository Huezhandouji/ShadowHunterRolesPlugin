# 阶段 5 · t63 权威取证轮 · **独立复核报告**（t64 / eng-验证B）

> **复核者**：eng-验证B（验证工程师）· **复核对象**：`t63`（eng-取证）「t61 控制台输出取证 + bot 冒烟」轮
> **报告写入时刻**：2026-09-18 22:0x（本条所有数字均为**我现算**；引用请重算）
> **方法**：**不引用实现者结论** —— 每一条都用自己的命令重跑；对引用的每个身份做 `Test-Path` + `Get-FileHash` 现算（协同规范 §7.2）
> **边界（卡面 nonGoal 全部遵守）**：未改 `src` · 未改任何 t59/t61/t63 证据文件 · 未构建 · **未起服**（25566 未触碰）· **未做 git 写** · **本文件是本次唯一写入**

## 0. 结论摘要

**verdict = pass** —— t63 交付**已由我独立复现**：冻结件身份、六条运行级锚、Mode B 三行、C=0、基线 91 文件自证、三代锚、四条申报，**逐条成立**。

**0 blocker / 0 high / 0 medium / 2 low**（V-01 / V-02；两条都是"**读法**"层面的提醒，**不是 t63 轮的缺陷**）。

| # | 验收判据 | 判定 |
|---|---|---|
| 1 | 运行级锚 + `ERROR/SEVERE` + F1 ORDER + C=0 | **通过**（F1 ORDER 见 §2.4 的**双读法**） |
| 2 | 基线重取与自证（修正版判据 4 件套） | **通过**（并做到**比卡面更强**的一层，见 §3.4） |
| 3 | 三代锚复核（t59 / t61 / t63） | **通过** |
| 4 | 作用域标注逐条写明 | **通过** |
| 5 | t63 四条申报逐条核验 | **通过**（含我**实跑**其工装自测） |
| 6 | 已知未闭合项裁断（F-1 + t61 措辞） | **通过** |

---

## 1. 交付物身份（我的现算 vs 卡面声称）

> **身份判据（队长 2026-09-18 裁定，本报告遵此）**：**身份一律以 `bytes + SHA256` 认定** ✓；下表"行"列**仅为描述性**（口径 = `[System.IO.File]::ReadAllLines`，全文行口径），**不得作为身份判据** ✗ —— 依本仓已记录的规则"**行号与「行数」都不可作为版本标识**"（同一份文件曾被两个工具数出 215 / 165 行）。**另**：本报告不出现"`395 行`"这一读数 —— 它已被队长撤回，属口径错误 ✗。

| 交付物 | 我现算（bytes / 行 / mtime / SHA256） | 声称 | 判定 |
|---|---|---|---|
| `阶段5-起服日志-控制台输出-取证.log` | 39033 / 223 / 21:55:30.018 / `972AAE6E…D326` | 同 | ✓ **逐字一致** |
| `阶段5-控制台输出-停服物证.txt` | 7624 / 87 / 21:57:37.572 / `4DED88D3…8B34` | 同 | ✓ **逐字一致** |
| `阶段5-控制台输出-取证说明.txt` | **15381 / 149 / 22:06:45.892 / `A31F741A…`** | 13865 / 138 / 21:57:37.587 / `09D43F7A…` | ⚠ **已被追加** → V-02 |
| `debug-logs/回滚快照/jars/MANIFEST.txt` | **7919 / 74 / 22:05:30.615 / `3A1049B7…`** | 6612 / 64 / 21:56:40.648 / `F32EA1CF…` | ⚠ **已追加「登记 6」** → V-02 |

**关键**：**两份原始证据件（冻结日志 + 停服物证）字节完全未动** ✓；被追加的两份是**说明书**与**登记册**（活文档，见 §8 V-02）。
另核：`t63-freeze-A-online.log` = **4960 B / 21:49:37.907 / `FF04BA72…`** ✓（在 `.scratch/bot/`）。

---

## 2. 判据 1：运行级锚 + 级别字段 + C=0 + F1 ORDER

### 2.1 六条运行级锚（我逐条独立计数，与卡面**完全一致**）
```
[command-debug]        occ=99    lineHits=99     (声称 99)  ✓
[sched]                occ=72    lineHits=72     (声称 72)  ✓
matrixAllPairsMatch=   occ=6     lineHits=6      (声称 6)   ✓
portLegEquivalent=     occ=6     lineHits=6      (声称 6)   ✓
CONCLUSION=            occ=6     lineHits=6      (声称 6)   ✓
级别字段锚定 /ERROR]|/SEVERE]|/FATAL] 行 = 0    (声称 0)   ✓  ← 不用裸词 ERROR
```
**附赠复核（t63 报文提及、卡面未列）**：`[cooldown]` = **21** ✓ · `issued server command` = **44** ✓。

### 2.2 Mode B 三行（冻结件内）
```
:8   [PluginInitializerManager] Bukkit plugins (1):
:44  [ShadowHunterRolesPlugin] ShadowHunter Character System enabled.
:48  Done (14.701s)! For help, type "help"
```
三行齐 ✓；时刻 21:49:25–21:49:37，与 freezeA（21:49:37.907）**同一空闲点**，佐证"仍在线时主冻结"✓。

### 2.3 C=0（锚 `阶段4-起服日志-T2b.log`）
按冻结工装 `log-delta-4A5A88B0-4588.ps1` 的**同一规则**复算（**内联跑、不写任何文件**，避免触碰冻结工装目录）：
```
anchor  WARN/ERROR/SEVERE 行 = 13    current = 13
ADDED = 0   REMOVED = 0
A) oshi = 0   B) entity = 0   C) OTHER = 0   ← 只有 C 能指示真问题
```
⇒ **C = 0 成立** ✓（13/13 全等、ADDED=0，与声称逐字一致）。

### 2.4 F1 链 —— **必须双读法报告**（这是本报告唯一需要"读法"澄清的地方）
| 读法 | jar 选取 | 序列 | ORDER |
|---|---|---|---|
| **(A) 归属该代世的归档件** ✓ | `…verified-20260918-214102-0F2D901C.jar` = **159652 B / 21:41:02.153 / `0F2D901C…DF74`** | 21:41:02.153 < freezeA 21:49:37.907 < freezeB 21:55:30.018 < 说明 21:57:37.587 | **True** ✓ |
| **(B) 当前 `build/libs/*.jar`** | = **159661 B / 22:01:06.944 / `21002ABC…BA408`** ✗ | 22:01:06.944 **>** freezeA … | **False** ✗ |

**(B) 失败的根因不是 t63**：
- t63 **确实没重建**（其红线段 §4 明写"未重建"，且四代归档件**零接触** —— 我核过 `0F2D901C` 仍 = 159652 B / 21:41:02.153 / 同 SHA ✓）；
- 22:01:06.944 的这次重建属于**清理批（t60）**，且**已被合法登记**为 **`MANIFEST` 登记 6**（22:05:30：`159661 B / 21002ABC…`，自带运行证据 `阶段5-起服日志-清理批.log` = 6074 B / 56 行 / 22:03:14.958 / `3B6788C9…`，F1 链 `22:01:06.944 < 22:03:14.958 < 22:05:30`）✓
⇒ **判据 1 的 F1 意图（证据晚于其所覆盖的制品、无"事后重建冒充覆盖"）成立** ✓；仅"用哪个 jar 读"需写明 → 记 **V-01（low）**。

---

## 3. 判据 2：基线重取与自证（**修正版 4 件套**，逐件分别报告）

### 3.1 `git ls-files --others -- <path>`（**禁用 `--exclude-standard`**）
```
git ls-files --others -- debug-logs            = 0
```

### 3.2 `git status --porcelain --ignored` —— **`??` 与 `!!` 分列**
```
?? (untracked) = 0
!! (ignored)   = 6   →  .gradle-work/  .gradle/  .idea/  build/  gradle.properties  run/
 M (modified)  = 3   →  debug-logs/测试记录/t60-criteria.ps1
                        debug-logs/测试记录/协同规范-起服停机取证.txt
                        src/main/java/…/roleComponent/red/RedDeeplySorrowSkill.java
```

### 3.3 `git check-ignore -v --no-index`（**`--no-index` 不可省**）—— **"两者可同真"已在真实路径上复现**
| 路径 | `ls-files --others` | `--error-unmatch` | `check-ignore --no-index` |
|---|---|---|---|
| `…控制台输出-取证说明.txt` | 0 | exit 0 = **TRACKED** | exit 0 = **命中 `.gitignore:84 debug-logs/**`** |
| `…控制台输出-取证.log` | 0 | TRACKED | **命中忽略规则** |
| `…控制台输出-停服物证.txt` | 0 | TRACKED | **命中忽略规则** |
| `回滚快照/jars/MANIFEST.txt` | 0 | TRACKED | **命中忽略规则** |
| `…verified-…-0F2D901C.jar` | 0 | TRACKED | **命中忽略规则** |
| `阶段5-取证轮-独立复核报告.md`（本件） | 0 | exit 1 = **untracked** | **命中忽略规则** |

⇒ 五份已入库证据件**同时**"**已跟踪**"且"**落在忽略规则下**" ⇒ 证实协同规范 **§5 族 8 ⑤** 的情形 ✓；且**本报告自身**正处在"未跟踪 + 被忽略"状态 ⇒ **若用旧口径（`--exclude-standard` / 裸 `--untracked`）复核本件，会得到"没有新文件"的真空绿灯** ✗（与我此前上交的活反例同族）。

### 3.4 基线快照自证（**做得比卡面要求更深一层**）
`t61-final-before/`：目录结构为 `main/java/…`（**是 `src/main` 的副本**，非 `src`）；**91 个 `.java`**；`SHA256.txt` = 12713 B / 91 行 / 21:48:43.484。
```
CHECK 1  逐文件 SHA256 与 SHA256.txt 比对        → 91/91 一致，mismatch = 0
CHECK 2  逐文件与 git blob 比对（git hash-object vs rev-parse 643302d:src/main/java/…）
         → 91/91 字节等同，mismatch = 0
计数      SHA256.txt 非空行 91  =  提交树 src/main 的 .java 数 91
```
⇒ **基线快照 ≡ 被验提交 `643302d` 的 `src/main`（逐字节）**，且该 tree（`beed92cb9997049d8b086f164b9f386a74eae1ba`）在 `b42d775` / `889e95c` / `643302d` **三代相同** ⇒ 即便工作树现已漂移，被验态**仍可精确复原** ✓。

---

## 4. 判据 3：三代锚复核

### 4.1 t59 代 —— **真旧侧 = `193a5e4`（诱饵 `7959369` 勿用）**
| 侧 | 文件 | 行 | 四层口径（A 出现 / B 命中行 / C 仅代码行 / **D 字面量**） | 文案指纹 |
|---|---|---|---|---|
| 旧 | `RoleCommand.java@193a5e4` | 688 | 26 / 26 / 26 / **26** | **`B411290116ADA5D57BAEBB72B49F45F23D10FFCDB1D0793844A9454CCA93CB06`** |
| 新 | `DebugSchedCommand.java@5eea716` | 404 | 27 / 27 / 26 / **26** | **`B4112901…93CB06`（逐字节相同）** |
`Compare-Object`（排序、不去重）**diffRows = 0** ⇒ **26 条多重集全等成立** ✓
> ⚠ **诱饵警告**：`7959369` 只有 **14** 条；它是祖先但**不是** t59 前状态 ⇒ 误用会得出"14 vs 26"的**假差异**。**真旧侧是 t59 的父提交 `193a5e4`。**

### 4.2 t61 代 —— **必须写「26 保留 + 1 新增」**
`DebugSchedCommand.java@b42d775`：D 口径 **27**，指纹 `1EDDF67A6BFFFA43C988AAEF6B9C11010C38C5B583DE43A1C24A1A367375DF8A`；与 t59 比 **diffRows = 1 = 新增**：
```
+ "[sched] ④m matrix: per-row tick detail for the 8 declared pairs x 2 paths stays player-side only; the key verdict line follows"
  @ DebugSchedCommand.java:292（DebugCommand.log 单写 ⇒ 【只走控制台】）
```
**独立仪器交叉印证**（我自建跨版本口径：逐 blob 读、与冻结工装 `numeric-multiset-CA32FE74` **同规则**、不落盘）：
- **数值 `5eea716 → b42d775`：658 → 658，diffRows = 0** ✓
- **字符串：393 → 396，diffRows = 3** ✓ = `" "` · `"[command-debug]"` · ④m 矩阵说明句
⇒ 与 t61 声称**逐条吻合**；且第 3 条**正是**上面那 +1 条 `[sched]` 字面量 ⇒ **两套互相独立的仪器同结论** ✓

### 4.3 t63 代 —— 见 §1 + §2（身份与锚行全部一致 ✓）

---

## 5. 判据 4：作用域标注（**同一串在不同作用域下数值不同，必须逐条写明**）

| 判据 | **作用域** | 数值 | 备注 |
|---|---|---|---|
| `command/` 内原生调度 API | `command/**` | **行级 24 / 原始出现 30** | `git grep -c` = 24（同行多处算 1）；`-o` = 30。目录恰 9 个 `.java`，**只有 `DebugSchedCommand`** 命中，其余 8 类 = 0 |
| `schedprobe` | **src** | **1** | `platform/BukkitSchedulerAdapter` 对证据文件名的**合法引用**；**全仓 = 7**（另 5 份为证据文档 + 1 份 after-phase4 归档副本）⇒ **不得要求归零** |
| `io.papermc.paper.threadedregions.scheduler` | **src** | **4** | `DebugSchedCommand` 2（探针**故意**调原生 API 做 A/B）+ `BukkitSchedulerAdapter` 2；**仅 `platform/` = 2** ⇒ **不得要求归零** |
| `io.papermc.paper.plugin.loader.*` | src | 2 | 属 **Loader**，**确认不并入**上一格（否则会数成 6 ✗） |
| 白名单外 `[sched]` | `command/**` | **0** ✓ | 命中只在 `DebugSchedCommand` = **28 行**（26 条带 `①…⑤` 标记 + `probe start` + `plugin instance unavailable`）；用**锚串**不用裸词 `sched` |
| 证据出口 | `command/**` | `getLogger` **1**（仅 `DebugCommand`）· `Bukkit.getLogger` **0** · `System.out` **0** · `System.err` **0** | 单一出口 ✓ |
| 前缀计数 | `command/**` | `command-debug` **4**（`DebugCommand` 2 + `DebugCooldownCommand` 1 + `DebugSchedCommand` 1） | 行口径 = 出现口径 = 4，本例不歧义 ✓ |

**⚠ ③ 双写点数 —— 最终口径 = 【仅调用点】**（队长 2026-09-18 裁定，采纳本报告建议；逐行列明后）：
```
DebugSchedCommand.sendKey(  = 14 调用点   （:68 :74 :88 :95 :140 :166 :168 :191 :251 :304 :331 :347 :373 :382）
DebugCooldownCommand.send(  = 10 调用点   （:46 :50 :56 :64 :69 :74 :84 :94 :103 :109）
【含定义口径】= 15 / 11     （:408 / :118 为各自的定义行）—— 允许并列，但【同一行里不得混用】✗
```
**假失败来源（我实测的机制）**：把"含定义"的 15 与"仅调用点"的 10 并列成 `15 / 10` ⇒ 用**同一条命令无法同时复现**（`git grep -c -E 'send\('` = **11** ✗ 对不上 10）⇒ 会把一条**正确的实现**判成"与期望不符"。
**⇒ 本报告（及引用本报告处）一律以【仅调用点】为准：`sendKey 14` / `send 10`。**
**对 t61 措辞的连带更正**：t61 报文写的"**15 处关键行**"同为【含定义】读数 ⇒ **实际双写调用点 = 14**，勿让"15 / 10"继续流传。

---

## 6. 判据 5：t63 **四条申报**逐条独立核验

| 申报 | 我的核验 | 结论 |
|---|---|---|
| **① 机械口径放行**（未收到队长直投【编辑停止】即起跑） | 说明书 §5.1 已**逐字写明**：给出起跑瞬间现算（`HEAD=b42d775`、`command-debug` ∈ HEAD = 4、`porcelain -- src` = 0）+ 明确"**自绑定 commit、可整轮作废重跑**" | **已完成充分申报** ✓（程序性提醒，**不记缺陷**）|
| **② 不重建**（护住登记 4 的 F1 链） | 四代归档件**逐件现算零接触**：`7A21729B`/`DBE04077`/`53CBEB65`/`0F2D901C` 均在位，`0F2D901C` 仍 = **159652 B / 21:41:02.153 / 同 SHA** ✓ | **成立** ✓ |
| **③ 工装对象数组 bug**（自测同形状未抓到；已归一化 + 加对象夹具） | **代码级 + 实跑双重证实**：`t63-smoke-runner.js:110` 有形状归一化 `if (!Array.isArray(v)) return v`；`:220-223` 自测夹具已改为**对象形状** `opRoot: [{match:'debug'},…]`；`selftest` 的 EXPECTED 明列 "**1 object-shaped gate**"。**我实跑 `node t63-smoke-runner.js --selftest`：exit 0，`ok=9 bad=0`** ✓ | **成立** ✓（属**族 10**的完整形态，自曝到位）|
| **④ 首轮 `Done (` 假通过 + 非 op bot 用户名 18 字符超限** | 说明书 §5.4 写明两处：命中**上轮遗留日志** ⇒ 假通过（改用**会话轮转 + 首行时刻证新鲜度**）；`smoke_probe_<6位>` = **18 > 16** ⇒ 从未 spawn（改名 `probe<6位>`）。诊断正确（离线名上限确为 16） | **成立** ✓ |

**⑥ out-of-scope 写入申报我也核了**：两份 runner 副本与 `.scratch` 原件**逐字节相同** ✓
（`t61-smoke-runner.js` = 8173 B / `E42015DE…`；`t63-smoke-runner.js` = 22587 B / `AD843412…`）。

**额外旁证（卡面未列，我顺手核）**：转录件 `.scratch/bot/t63-smoke-transcript.txt` = 32942 B / 257 行 / 21:55:16 / `49740CA4…`，含 `NON-OP` 标记与 `10462`（op 窗）/ `720`（非 op 窗）⇒ op 门控双窗主张**有物证** ✓。

---

## 7. 判据 6：已知未闭合项裁断

### 7.1 F-1：`portLeg=portLeg=SKIPPED(…)` 重复键
- **源码级根因**：`:330` 赋的值**自带** `portLeg=` 前缀，`:391` 又拼一次 `" | portLeg="` ⇒ 仅在 **SKIPPED 分支**重复；覆盖分支（`:347`）不重复。
- **归属裁断**：该字面量**属冻结的 26 条之一**（旧树同值 ⇒ 指纹相同已证）⇒ **非 t61/t63 引入**，且说明书 `:81`（旧件）已如实交代。
- **建议**：**挂账阶段 5/6**；因它是**玩家可见**文案且属冻结字面量，改动须**按冻结口径申报**，不得顺手改。

### 7.2 t61 措辞裁断
对 t61 **必须**写「**原有 26 条逐字保留 + 1 条已申报的 console 追加**」；单独写"26 条多重集全等"**只对 t59 的比较成立**（对 t61 为 26 → 27）。**采纳并写入本报告** ✓。

---

## 8. Findings

### V-01（low · 读法）F1 链必须按"**该代世的归档件**"读，不能按 `build/libs/*.jar`
- **现象**：按当前 `build/libs/*.jar`（159661 B / 22:01:06.944 / `21002ABC…`）做 ORDER 机器校验 = **False**（jar 晚于 freezeA/freezeB）。
- **根因**：该 jar 已被**清理批（t60）**重建并**合法登记为 `MANIFEST` 登记 6**（自带 `阶段5-起服日志-清理批.log` 运行证据与自己的 F1 链）⇒ `build/libs` 现属**新一代**，**不代表** t63 覆盖的那一代。
- **判定**：**不是 t63 的缺陷**；t63 未重建（归档件零接触已证）。
- **要求修正（文档面）**：登记册/报告述 F1 链时，**逐代写明所引 jar 的归档路径**；`build/libs` 只能用于"当前树制品"语境。

### V-02（low · 活文档 vs 冻结件）两份**说明书/登记册已被追加**，其 SHA 引文已失效
- `阶段5-控制台输出-取证说明.txt`：`13865 / 138 行 / 09D43F7A…` → **`15381 / 149 行 / 22:06:45.892 / `A31F741A…`**（+1516 B）
- `MANIFEST.txt`：`6612 / 64 行 / F32EA1CF…` → **`7919 / 74 行 / 22:05:30.615 / `3A1049B7…`**（追加**登记 6**）
- **两份原始证据件字节未动** ✓（`…取证.log` = `972AAE6E…`；`…停服物证.txt` = `4DED88D3…`）。
- **判定**：符合协同规范 **§7.3**（"活文档"引 **§章节 + 测量时刻**，引完字节/行号会被扩写打脸）；**登记册 append-only 属设计意图** ⇒ **非缺陷**。
- **要求修正（引用纪律）**：本轮的 说明/MANIFEST 一律**按 §章节 + 时刻**引用；需身份级引用时用 **`git rev-parse <commit>:<path>` 的 blob**。

**无 blocker / 无 high / 无 medium。**

---

## 9. 仪器自证与我这边的错误台账（"先自证，再下结论"）

**已自证的仪器**（每条都先证"在已知真样本上会通过"）：
1. **字节级抽取自证**：凡 `git show <rev>:<path>` 抽取都断言 `抽取件字节数 == git cat-file -s <rev>:<path>`，不一致即抛错；
2. **CJK 路径纪律**：只走**进程内 `Set-Location`**，**绝不把仓库路径传给 `cmd`**（PS 5.1 会把 `插件` 转成 `鎻掍欢`）；
3. **跨版本字面量口径**：与冻结工装**同一正则**；**不落盘**（避免污染工作树/触发闸口）；
4. **C=0 复算**：内联复刻冻结工装规则，**不写文件**（不触碰冻结工装目录）；
5. **`--selftest` 先判"是否有写盘副作用"**：确认 `outFile` 仅在 `live()` 内设置 ⇒ `--selftest` 不写盘，才执行。

**我自己犯过并已更正的三个错**（均为"模式/形状 ≠ 内容"族）：
1. `-like '??*'` 把 `?` 当**通配符** ⇒ `??` 计数恒等于总行数 → 改用 `StartsWith('??')`；
2. 基线快照路径拼接假设有 `src/` 前缀 ⇒ 实际布局是 `main/java/…` ⇒ 91 条全报 MISSING（**假"缺件"**）→ 改用 `-replace '^src/',''`；
3. 首次推断 freezeA 文件名错误 ⇒ 报 MISSING → 用**内容哈希反查**定位（`.scratch/bot/t63-freeze-A-online.log`）。
> 三条都是**先看"全 0/全缺"**这一可疑形态才发现的 —— 与族 5"工具/输入缺失 ⇒ 假结论"同族。

---

## 10. 复算命令（供第三人重跑；全部只读）

```powershell
# 身份
Get-FileHash -Algorithm SHA256 <path>; (Get-Item <path>).Length
# 运行级锚（六条）
$t=[System.IO.File]::ReadAllText((Resolve-Path '<log>').Path)
'[command-debug]','[sched]','matrixAllPairsMatch=','portLegEquivalent=','CONCLUSION=' | % { "$_ = $([regex]::Matches($t,[regex]::Escape($_)).Count)" }
# 级别字段（不用裸词）
(Get-Content '<log>') | ? { $_ -match '/ERROR\]|/SEVERE\]|/FATAL\]' } | Measure-Object
# 基线自证（4 件套）
git ls-files --others -- debug-logs
git status --porcelain --ignored
git check-ignore -v --no-index -- <path>
git ls-files --error-unmatch -- <path>
# 快照 ≡ 提交树（逐文件字节等同）
git hash-object -- <snapshotFile>   vs   git rev-parse 643302d:<repoRelPath>
# 三代锚（字面量多重集 + 指纹）
#   D 口径规则 = 冻结工装 numeric-multiset-CA32FE74-5415.ps1:56-58（剥块/行注释 → 取 "…" 原文）
#   旧侧 193a5e4:RoleCommand.java  新侧 5eea716:DebugSchedCommand.java  → diffRows 必须 = 0
#   工装自测（离线、不写盘）
node <repo>/debug-logs/测试记录/t63-smoke-runner.js --selftest    # 期望 exit 0 / ok=9 bad=0
```

---

## 11. 边界声明（钩不过）

- **未改 `src`** ✓（`git status --porcelain -- src` 仅 `M RedDeeplySorrowSkill.java`，属 **t60 在途**，非我所改）
- **未改任何 t59/t61/t63 证据文件** ✓（只读）
- **未构建** ✓ · **未起服** ✓（25566 未触碰）· **未做 git 写** ✓
- **唯一写入 = 本文件**（`debug-logs/测试记录/阶段5-取证轮-独立复核报告.md`，卡面 inScope）
- 引用纪律：本报告对**冻结件**引 SHA256 ✓；对**活文档**（说明书/规范/登记册）引 **§章节 + 时刻** ✓

---

## 12. 补核（队长追加范围）· t63 轮是否跑到「**有角色**」的冷却三态？

> 追加缘由：`eng-指令` 主动申报 —— **`t61` 那轮**账号无角色 ⇒ `cooldown` 的 `end`/`restart` 与"有角色的 `status`"三条**未被触发**；队长据此要求我核 **t63 轮**是否同样未覆盖。
> 复核结论：**该缺口在 `t63` 轮并不存在** ✓ —— **它是 `t61` 轮的缺口，`t63` 轮已修掉**；但发现**三条更窄的分支缺口** ✗（见 §12.3）。**未起服、只读** ✓。

### 12.1 ① t63 的 bot 冒烟**包含**"先设角色"这一步 —— **有，3 次** ✓
冻结件内 `issued server command` 共 **44** 行，其中**逐字命中**：
```
:58   /role set meiqihezi      ← 先设角色
:109  /role set meiqihezi
:168  /role set meiqihezi
```
执行者 = **op 机器人 `HueZhandouji`**（`run/ops.json` 内 level 4，uuid `40671423-…`/`21de2f97-…`）；同轮另两个 `probe159935` / `probe715922` 是**非 op** 负样本 ⇒ 与 t63 申报的"**op 门控双窗**"一致 ✓。
**op 门控旁证**：`:161/:162` 与 `:220/:221`（非 op 的 `status 0`/`end 0`）**其后没有任何 `[command-debug]` 行** ⇒ 非 op 窗控制台输出 = **0** ✓（与声称一致）。

### 12.2 ② 冻结件内【有角色】分支的冷却输出 —— **逐字命中** ✓
同一条状态机在 op 窗内**完整跑了 3 遍**（:58–:72 / :109–:123 / :168–:182）：
```
:60  [cooldown] meiqihezi_skill_unconcern | cooling=false | remainingTicks=0   | remainingSeconds=0.00 | declaredTicks=100
:62  [cooldown] meiqihezi_skill_unconcern | cooling=false | remainingTicks=0   | remainingSeconds=0.00 | declaredTicks=100
:64  [cooldown] restart(meiqihezi_skill_unconcern) wasCooling=false | no old segment was cooling | newRemainingTicks=100
:66  [cooldown] meiqihezi_skill_unconcern | cooling=true  | remainingTicks=63  | remainingSeconds=3.15 | declaredTicks=100
:68  [cooldown] meiqihezi_skill_unconcern | cooling=true  | remainingTicks=33  | remainingSeconds=1.65 | declaredTicks=100
:70  [cooldown] end(meiqihezi_skill_unconcern) returned=true | wasCooling=true | ENDED_BY_COMPONENT dispatched (onCooldownEnd)
:72  [cooldown] meiqihezi_skill_unconcern | cooling=false | remainingTicks=0   | remainingSeconds=0.00 | declaredTicks=100
```
**逐字命中计数（我现算）**：
| 锚 | 命中 | 判 |
|---|---|---|
| `returned=true` | **3**（:70/:121/:180） | ✓ **命中（运行级）** |
| `ENDED_BY_COMPONENT dispatched (onCooldownEnd)` | **3** | ✓ **命中** |
| `wasCooling=` | **6**（restart×3 + end×3） | ✓ **命中** |
| `cooling=true`（冷却真在走，63/33 ticks） | **6** | ✓ **命中** |
| `You have no role yet` | **0** | ✗ 未命中（见 §12.3） |
| **`RESTARTED dispatched`** | **0** | ✗ **未命中** |
| `no-op (was not cooling)` | **0** | ✗ **未命中** |

⇒ **`end` 与"有角色 `status`"两条，t63 轮已用运行级证据覆盖** ✓（**不是**静态级代替）。
⇒ 仅 **`restart` 的"冷却中重启"分支**未覆盖 —— 因为在 3 遍里 `restart` 都是在 `cooling=false` 时发出的。

### 12.3 ③ **open item（三条窄分支缺口）** —— 未覆盖，且**可构造** ✓（建议下一轮小冒烟补，**不要**为此单独起服）
均以源码为期望值（`DebugCooldownCommand.java` 的 `switch(action)`）：
| # | 缺口 | 期望值（**逐字**，源码口径） | 构造步骤（一轮内可做完） |
|---|---|---|---|
| G-1 | **`restart` 在冷却中**（源码 `:104` 的 `wasCooling=true` 支） | `[cooldown] restart(<id>) wasCooling=true \| RESTARTED dispatched (old segment dropped) \| newRemainingTicks=<declared>` | `/role set <roleId>` → 触发一次技能使冷却启动（或先 `restart` 一次）→ **在仍冷却时**再 `restart` |
| G-2 | **`end` 在未冷却时**（源码 `:96` 的 `no-op` 支） | `[cooldown] end(<id>) returned=false \| wasCooling=false \| no-op (was not cooling)` | 紧跟 G-1 之后**连续第二次** `end` |
| G-3 | **无角色的冷却分支**（op 窗内） | `[command-debug] You have no role yet!` | 用 **op** 机器人且**未** `set` 角色时发 `debug cooldown status <slot>`（本轮该尝试由**非 op** 发出 ⇒ 被 op 门控拦下、不进入 handler，故 `You have no role yet` = 0） |

**为什么值得补**：G-1/G-2 正是**双写口径**里被点名的两条文案（说明书 §5 的 `RESTARTED dispatched` / `no-op`），却**尚无运行级命中**；若后续以"这两条已覆盖"作断言，属**以静态级冒充运行级** ✗。三条都在**同一次起服**内可完成，附 `jar` 身份后即形成一条完整的运行级证据。

**与既有判据的关系** ✓：本条**不改** §2–§7 已确认的 6 条判据、也不改两处修正（③ 仅调用点 `14/10`；身份只用 `bytes + SHA256`）✓；**不构成 blocker**（既有判据全过），属**新增 open item**。
**边界**：本条**未起服**、未改任何证据文件、未改 src ✓（§11 的边界声明同样适用于本节）。

### 12.4 ⚠ 判据修正（队长裁定）：cooldown 侧**必须改用【特征输出形态】**，**不得**采信 `t61-smoke-runner.js` 的断言
> 队长转来 `eng-指令` 用真证据交叉验证后报出的**真空通过**缺陷。我**独立复核并确认**，机制如下（三项均我现算）：

**(a) 该 runner 的 cooldown 三项 `expect` 是【析取】** ✗（`t61-smoke-runner.js`）：
```
:39-40  cooldown/status   expect: [/cooldown/i]
:41-42  cooldown/end      expect: [/cooldown|end/i]
:43-44  cooldown/restart  expect: [/cooldown|restart/i]
```
**(b) 而日志里的 usage 行【同时含三个词】** ⇒ 只靠它就能让三项全部 `[OK]` ✗：
```
阶段5-起服日志-控制台输出.log :61  [command-debug] Usage: /role debug cooldown <status|end|restart> <slot|componentId>
    contains cooldown=True  end=True  restart=True
```
**(c) 该冻结件里**根本没有任何真实 cooldown 输出**，runner 却全过** ✗：
```
'[cooldown] ' = 0      'cooldown end' = 0      'cooldown restart' = 0
```
⇒ 结论：**该 runner 对 cooldown 给出的是"真空通过"**，**不能**用来判"是否跑到有角色的四态" ✗（改 AND 也无效 —— usage 文案把四态词列全了）。属本团队反复防的「**声明 ≠ 行为 / 真空通过**」族，**应改为按【特征输出形态】直接 grep 冻结件**。

**(d) 改用特征形态后的现算结果（形态不含 usage 行 ✓ ⇒ 可判别）**：
| 形态 | t63 冻结件 | t61 冻结件 |
|---|---|---|
| `status` → `\[cooldown\] \S+ \| cooling=` | **15** ✓ 覆盖 | **0** ✗ |
| `end` → `\[cooldown\] end\(.+\) returned=` | **3** ✓ 覆盖 | **0** ✗ |
| `restart` → `\[cooldown\] restart\(.+\) wasCooling=` | **3** ✓ 覆盖 | **0** ✗ |

⇒ **与 §12.2 一致并更精确**：`t63` 轮**三个状态动作本身均已覆盖** ✓（`status` 15 处、`end` 3 处、`restart` 3 处）；未覆盖的**只是两个子分支**（见 §12.3 的 G-1 / G-2）与**无角色支**（G-3）✗。`t61` 轮则三者**皆 0** ⇒ 与 `t59`/`t61` 冻结件里自述的"**未覆盖（如实）**"相符 ✓。

**处置建议**：① 以本节的**特征形态**替换该 runner 的 cooldown 判据（并把 expect 改成**含形态的合取**，例如 `[\[cooldown\] \S+ \| cooling=]`）；② 该 runner 的**其余项（`[sched]` / `CONCLUSION=` / `matrixAllPairsMatch=` / `portLegEquivalent=`）判据不受影响** ✓（这些形态 usage 行不含）；③ 记录为**仪器缺陷（medium）**：它能把"**未覆盖**"判成"**已覆盖**"，与族 5「工具缺失 ⇒ 空比较假 True」同族。
**边界**：本节同样**未起服**、未改任何证据文件、未改 src ✓。
