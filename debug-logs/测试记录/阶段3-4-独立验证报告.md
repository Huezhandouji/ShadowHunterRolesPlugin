# 阶段 3–4 独立验证报告（t15）

> **执行者**：eng-验证（verifier）｜**任务**：t15（verification）｜**attempt**：1
> **执行时刻**：2026-09-18 19:1x–19:3x（+08:00）
> **验证基线（冻结态）**：`HEAD = 0564b3be350f449341cd0bf3f2fb6cd0e1140d00`（`git status --porcelain` = **0 行**，工作树与 HEAD 一致）
> **被验证产物**：`build/libs/ShadowHunterRolesPlugin-1.0.0.jar` = **135333 B / mtime 2026-09-17 22:06:15.571 / SHA256 `76AC54D9F8AB488951C40FDF72A5660798C44D309653E61114E88A3A1FECCF87`**；最新 `.class` = `MeiqiheziCircleSlashSkill.class` @2026-09-17 22:06:15.364；**`src 新于 jar 的文件数 = 0`** ✓（⇒ jar 覆盖当前全部源文件）
> **性质**：只读验证（未改任何 `src`、未构建、未起服、未做 git 写）；**本报告是唯一写入**。
> **口径纪律**：一律 `git grep`（**大小写敏感**）并写明**路径作用域 + 匹配形态**；**未使用** `Select-String`、**未使用** `@(… | Measure-Object).Count`（计数 = 逐行累加）；区分**限定名 vs 裸串**、**代码引用 vs 注释提及**、**扇出 vs 方法壳**；裸计数已减定义行与注释/javadoc。

---

## 0. 结论摘要

**判据：I-3/I-4/I-5/I-6/I-7/I-8/I-9/I-10/I-11/I-13 成立（含 2 项空转/口径注记）；I-12 部分成立（T-⑦ 待办）；I-14 部分成立；I-15 不成立。§3.4 等价性表 6/6 成立。§3.5.1 部分落地（4.4 待办，已文档标注）。Mode B + 构建证据成立（停服为证据缺口）。**

| 级别 | 数量 | 条目 |
|---|---|---|
| **blocker** | **1** | **V-01**：I-15 不成立（组件 SanTE 钩子派发经 Bukkit 事件总线；listener 转发未删） |
| **high** | **2** | **V-02**：I-15 的**判据命令自身假通过**（criterion defect）｜**V-03**：I-14 的"不递归/合并补发一次"未实现 |
| **medium** | **1** | **V-04**：I-14/I-11 的验收方式（单测）在本仓库**物理不可执行**（无测试源集） |
| **low** | **3** | V-05 死 import（`RedDeeplySorrowSkill:3`）｜V-06 I-12 两处基类声明 = T-⑦ 待办｜V-13 "已停服"证据缺口 |
| **info / 已闭合** | **5** | V-07（I-10 空转）· V-08（I-6 口径：`CooldownRegistry` 尚不存在）· V-09（`Self.id()` 口径）· V-10（§3.4 precheck 位置与伪代码不同但等价）· **V-11（t49-F1 已文档闭合）** · V-12（§3.5.1 部分落地 = 4.4 待办） |

**findings 明细见 §7（含复现命令）。**

---

## 1. 逐条判据（命令原文 + 输出 + 结论）

### I-3 / I-13 · 字符串键上下文（4 方法）
```powershell
git grep -nE 'setContext\(|getContext\(|hasContext\(|removeContext\(' -- src/main/java    # → 0 行
```
**结论**：✅ **0 行**（`context` 字段与 4 个方法已整体删除；组件间通信只经 `getComponent(...)`/端口）。

### I-4 · 组件不得直接取调度器
```powershell
git grep -nE 'Bukkit\.getScheduler\(\)|getGlobalRegionScheduler\(\)|new BukkitRunnable' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent   # → 0 行
```
**结论**：✅ **0 行**。

### I-5 · `api` 包不得 import 内部具体类/实现
```powershell
git grep -nE '^import com\.shadowHunterRolesPlugin\.' -- src/main/java/com/shadowHunterRolesPlugin/api   # → 2 行
```
```
api/RoleAPI.java:3:import com.shadowHunterRolesPlugin.core.Faction;
api/RoleInfo.java:3:import com.shadowHunterRolesPlugin.core.Faction;
```
**结论**：✅ 仅 2 行、均为 `core.Faction`（**公开枚举**且出现在 `RoleAPI` **既有签名**中）⇒ 按**阶段 3 裁定的语义界定不计入** ✓。另 `RoleAPIImpl` 已移出 `api` 包（现位于 `internal/api/RoleAPIImpl.java`）✓。

### I-6 · 冷却读数只有一套 API
```powershell
git grep -nE 'getRemainingSkillCooldownTicks|getRemainingSkillCooldownSeconds|getRemainingMainWeaponCooldownTicks|getRemainingMainWeaponCooldownSeconds' -- src/main/java   # → 14 行
```
分布（逐行核过）：**定义 4 处**（`RoleInstance.java:261/:267/:327/:332` = 技能/武器的 ticks+seconds 四读法）｜**调用点**：`core/CooldownPortImpl.java:30,31`（端口适配器）、`core/RoleInstance.java:257,324`（冷却到点 `runLater` 参数）、`core/RoleInstance.java:268,333`（seconds→ticks 委托）、`core/Skill.java:63,105`（渲染读秒）、`internal/api/RoleAPIImpl.java:336,341`（对外 API）。
**结论**：✅ **单源**（全部读数落到 `RoleInstance` 的同一对 ticks 真值，无第二套冷却存储）✓。**口径注记（V-08）**：判据原文提及"只允许出现在 `CooldownRegistry` 实现与其调用点"，但 `git grep -n 'CooldownRegistry' -- src/main/java` = **0 行**（该类**尚不存在**，属 `dc8c786` 的"冷却自管理"规划）⇒ 现阶段的"实现"就是 `RoleInstance`；建议判据改写为"**单源 + 调用点**"口径。

### I-7 · `roleComponent` 下不得出现聚合根类型
```powershell
git grep -nE '\bRoleInstance\b' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent   # → 18 行
```
分类（逐行核过）：**17 行**为 javadoc/注释（历史对照与说明）｜**1 行是真实 import**：`roleComponent/red/RedDeeplySorrowSkill.java:3 import com.shadowHunterRolesPlugin.core.RoleInstance;`。
**该 import 无任何代码使用**（该文件内 `RoleInstance` 仅出现于 `:3` 与该行之外的注释 `:65`）⇒ **死 import**。
**结论**：✅ **语义成立**（无组件持有聚合根、无组件方法签名含 `RoleInstance`）；⚠️ 裸串判据非空 = 17 注释 + **1 死 import** ⇒ 见 **V-05（low）**。

### I-8 · 不得再用 `instanceof *Aware` 派发
```powershell
git grep -nE 'instanceof[^;]*(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)' -- src/main/java   # → 0 行
```
**结论**：✅ **0 行**（四类接口与目录亦已删除，见 t49 报告 §1①②）。

### I-9 · 端口方法集合 == 设计 §5 白名单（1:1，多一个都不许）
见 **§2**（逐条比对表）。
**结论**：✅ **34 方法 / 10 成员，逐条吻合，无多无缺**。

### I-10 · 组件声明的跨组件依赖必须在注册期可校验
```powershell
git grep -nE 'RequiresComponents|requires\(\)' -- src/main/java    # → 0 行
```
**结论**：✅ **空转成立（vacuous）**：当前**无任何** `RequiresComponents` 声明 ⇒ 不存在"需要注册期校验"的依赖（该机制在设计文档中标注为**可选**，见 D7）。**口径**：本项为 vacuous，**不得**据此声称"校验路径已验证"；阶段 5+ 若引入该机制需重核（**V-07**）。

### I-11 · 分派顺序确定：同类型内部 = 注册顺序
```powershell
git grep -nE 'Map<|LinkedHashMap|HashMap|List<' -- src/main/java/com/shadowHunterRolesPlugin/core/dispatch/ComponentRegistry.java
git grep -nE 'skillMap\.values\(\)|mainWeaponMap\.values\(\)|passiveMap\.values\(\)' -- src/main/java   # → 0 行
```
- **新管道（真正承载分派）**：`core/dispatch/ComponentRegistry.java:20 private final List<RoleComponent> components = new ArrayList<>();` + `:45 public List<RoleComponent> all()` ⇒ **插入序 = 注册顺序** ✓；`:22 frozen` + `:108` 资源表遍历用 `new ArrayList<>(resources.keySet())`（避免边遍历边改）✓。
- **注册工厂**：`core/Role.java:112-114` 三个 `LinkedHashMap`（技能/被动/主武器）✓。
- **旧三 map**：`RoleInstance.java:59,60,61` 为 `HashMap`，但**只按 id 查找**（`values()` 与 `for(... : map)` 命中 = **0 行**）⇒ **无顺序语义** ✓。
**结论**：✅ 成立（分派顺序 = 注册顺序）。**口径注记**：判据原文"**三个组件 map 必须是 `LinkedHashMap`**"是**旧架构表述**；T 块后分派统一走 `ComponentRegistry` 的 `ArrayList`（插入序），旧 map 降级为查找表 ⇒ 建议判据按现架构改写。

### I-12 · 热键栏相关结构各只有一份
逐符号**声明处**计数（scope `src/main/java`）：
| 符号 | 声明数 | 结论 |
|---|---|---|
| `skillCooldowns` | **1**（`RoleInstance:51`） | ✅ |
| `mainWeaponCooldowns` | **1**（`RoleInstance:52`） | ✅ |
| `skillMap` | **1**（`RoleInstance:60`） | ✅ |
| `mainWeaponMap` | **1**（`RoleInstance:59`） | ✅ |
| `SKILL_KEY` | **1**（`Skill.java:124`） | ✅（优于"4.4 前可暂存 2 处"的宽限） |
| `MAIN_WEAPON_KEY` | **1**（`MainWeapon.java:119`） | ✅ |
| `createIconItem(RoleInstance)` | **2**（`Skill.java:40`、`MainWeapon.java:46`） | ⚠️ 见下 |
| `getDisplayName(RoleInstance)` | **2**（`Skill.java:96`、`MainWeapon.java:93`） | ⚠️ 见下 |
**结论**：⚠️ **部分成立（6/8）**：后两个符号各 **2 处基类声明**（指南 §3.5.1 `:413` 要求 `createIconItem` 全仓 1 处）⇒ 属**已登记的 T 块第 ⑦ 项**（渲染统一接管，未做）⇒ **挂账 V-06（low）**，非新缺陷。（`getDisplayName()` 无参重载另有 3 处 = `PassiveSkill:25`/`Role:272`/`ActiveComponent:42`，**签名不同**，不计入本项。）

### I-14 · `onSanTEChange` 只在值真变化时派发，且不递归
```powershell
git grep -nE 'inSanTEDispatch|dispatching|pendingSanTE|pendingSan|reentr|dispatchDepth' -- src/main/java   # → 0 行
```
- ✅ **"真变化才派发"已实现**：`RoleInstance.java:625 if(preSanTE == newSanTE) return;`（注释同时说明"事件对外发布仍无条件"）。
- ❌ **"不递归 + 派发期间同类型写入合并为结束后按最终值补发一次"未实现**：上述 6 个护栏标识**全 0 命中**，`triggerSanTEChange`（`:622-636`）体内无重入保护。
**结论**：⚠️ **部分成立** ⇒ **V-03（high）**。

### I-15 · `onSanTEChange` 由容器直接派发，不经 Bukkit 事件总线
```powershell
git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- src/main/java/com/shadowHunterRolesPlugin/listener   # → 2 行
git grep -nE '\btriggerSanTEChange\b' -- src/main/java                                                          # → 3 行（定义 + 唯一调用点 + 1 javadoc）
```
实测派发链（逐行读码）：
```
RoleInstance.setCurrentSanTE  [:516-521]  → Bukkit.getPluginManager().callEvent(new SanTEChangeEvent(...))   ← 事件总线
    → RoleEventListener.onSanTEChange [:18-21]（已注册：ShadowHunterRolesPlugin:81）
        → instance.triggerSanTEChange(pre, now) [:622]
            → if(pre==now) return; → 广播 component.onSanTEChange(pre, now) [:633-635]
```
**结论**：❌ **不成立**：① 组件钩子的**唯一派发入口就是 Bukkit 事件总线**（无容器直接调用）；② **`RoleEventListener` 的转发未删**（`:14`/`:20` 两行仍在）—— 判据要求"转发已删" ⇒ **V-01（blocker）**。另：判据命令本身有假通过缺陷 ⇒ **V-02（high）**。

---

## 2. I-9 端口白名单逐条 1:1（设计 §5.1 `:418-430`）

**宿主清单**：`core/ports/` = **11 个文件**（10 个成员接口 + `ComponentServices` 聚合）；`core/` 下实现 = **10 个文件** = `*PortImpl` **8** + `SelfImpl` + `ComponentLookupImpl` ✓（与队长给的口径一致）。
`ComponentServices` = `public record`，成员**恰好 10 个**（`Self, EnergyPort, SanTEPort, VitalsPort, CooldownPort, BuffPort, FactionPort, DamagePort, TimerPort, ComponentLookup`）✓ 与设计 §5 的 record 逐字一致。

| 成员 | 白名单（设计 §5.1） | 实测方法集 | 判定 |
|---|---|---|---|
| `self` | `player()`（+ 方案 B 的 `id()`） | `player()`, `id()` | ✅ 2（`id()` 见 V-09） |
| `energy` | `current()` / `tryConsume(n)` / `gain(n)` | 同 | ✅ 3 |
| `sante` | `gain(n)` / `decrease(n)` / `set(v)` / `current()` / `max()` | 同 | ✅ 5 |
| `vitals` | `heal(amount)` | `heal(double)` | ✅ 1 |
| `cooldowns` | `isReady()` / `remainingTicks()` / `start(ticks)` / `end()` | 同 | ✅ 4 |
| `buffs` | `canCastSkill()` / `canUseMainWeapon()` / `add(type,ticks)` / `has(type)` / `remainingTicks(type)` / `applyPotionEffect(...)`（2 重载） | 同（`:15/:17/:19/:21/:23/:26/:34`） | ✅ 7 |
| `factions` | `isHostile(victim)` / `faction()` / `hasEnemyInRange(radius)` | 同 | ✅ 3 |
| `damage` | `trueDamage(v,s,a)` / `trueDamage(v,s,a,kb)` / `physicalDamage(v,s,a)` / `physicalDamage(v,s,a,kb)` | 同（`s` 为 `LivingEntity`，**允许 null** 见设计 `:410`） | ✅ 4 |
| `timers` | `run(r)` / `runLater(ticks,r)` / `runRepeating(delay,period,r)` / `track(task)` | 同 | ✅ 4 |
| `components` | `get(Class<T>)` | `<T extends RoleComponent> T get(Class<T>)` | ✅ 1 |

**合计 34 方法**，**无多余成员、无缺失方法** ✓（`buffs` 的两处 `applyPotionEffect` 重载 = R-1 裁定允许，且要求"必须走已记账路径"）。

---

## 3. §3.4 行为等价性表（逐条）

| # | 场景 | 实测（当前代码） | 判定 |
|---|---|---|---|
| 1 | 攻击，能量 ≥ 20 | `MeiqiheziJuejueMainWeapon.onAttack:49-52` → `castAreaDamage(attacker)` → `SUCCEED`；`castAreaDamage` 内 **`tryConsume(5)`** + 半径 **5** 粒子圆（50 粒）+ 半径 **5** 内敌对目标各 **`physicalDamage(..., 14)`** + 音效 `ITEM_TRIDENT_THROW 1f/0.8f` | ✅ 5 / 14 / 半径 5 逐字 |
| 2 | 攻击，能量 < 20 | `onAttack:55-58` → `physicalDamage(victim, attacker, 8, 0.5)` → `SUCCEED` | ✅ 8 + 击退 0.5；**无双启动**（冷却由框架按 `SUCCEED` 启一次） |
| 3 | 攻击，冷却中 | `MainWeaponListener.onAttackPlayer:47 event.setCancelled(true)` → `:49 instance.handleAttack(...)`；冷却闸门在 listener：`:76/:100/:132 if(!instance.isMainWeaponReady(weaponId)) return;` ⇒ 无伤害 | ✅ **先取消再判冷却**（顺序与 `:341` 要求一致） |
| 4 | 空挥左键，能量 ≥ 20 | `onCast:68-77`：`trigger != LEFT_CLICK → NO_COOLDOWN`；`energy < 20 → NO_COOLDOWN`；否则 `castAreaDamage` + `SUCCEED` | ✅ |
| 5 | 空挥左键，能量 < 20 | 同上 `< 20` 分支 → **`NO_COOLDOWN`（不进冷却）** | ✅ |
| 6 | 红普攻 | `RedSanctifiedBladeMainWeapon.onAttack:46 bleed.applyStacks(uuid, BLEED_STACKS_PER_HIT)`（常量 = **15**，`:17`）+ `:49 physicalDamage(victim, attacker, 8, 1)` → `SUCCEED` | ✅ 8 / 15 层不变 |
**红线复核**：能量**不是**硬闸门 ✓ —— 主武器把"能量不足"留在**组件内分支**（`onAttack`/`onCast` 的 `< 20` 分支），listener 的 precheck 只拦冷却（+ 眩晕/沉默由组件经 `svc().buffs().canCastSkill()` 判定）✓。
**结论**：✅ **6/6 成立**。**口径注记（V-10）**：指南 §3.4 伪代码把 precheck 写在 `handleCast` 内，而实现放在 **listener 层**（`SkillListener:58/:100/:138`、`MainWeaponListener:76/:100/:132`）⇒ **行为等价**（闸门齐、能量不拦），建议同步伪代码或注明实现位置。

---

## 4. §3.5.1 刷新契约（push / poll / flush + 两条写路径）

| 设计要素 | 实测 | 判定 |
|---|---|---|
| **push（框架）**：施放结果分派后 | `handleCast:186` / `handleAttack:205` 调 `hotbarRenderer.markDirty()` | ✅ |
| **push（端口适配器内部）**：`energy.tryConsume`/`buffs.add`/`cooldowns.start` 等 | `git grep -n 'markDirty' -- …/core` = **9 行**，其中**真实调用仅 2 处**（`:186`/`:205`），端口实现内 **0 调用** | ❌ **未落地**（= 4.4 待办，见 V-12） |
| **poll（ticker 每 tick）** | `RoleInstance:646 if(shouldUpdateHotbar()){ updateHotbar(); }` + `:648 if(shouldUpdateItemMeta()){ updateAllSkillItemMeta(); }`；谓词 `:676-688`（任一槽冷却到期 → 全量重建）/`:690-696`（有槽冷却中 → 仅 meta） | ✅（等价覆盖设计的三条谓词；buff 到期由 `BuffManager:136` 的 updater 循环 → `:108 instance.updateHotbar()` 覆盖） |
| **flush（帧末一次）** | `HotbarRenderer` 有 `dirty/isDirty/clearDirty`，但 `isDirty()/clearDirty()` **除定义外 0 调用** ⇒ 无消费者 | ❌ **未落地**（= 4.4 待办，见 V-12） |
| **两条写路径** | 状态变化 → `updateHotbar()`（`setItem`）；仅倒计时 → `updateAllSkillItemMeta()`（`setItemMeta`） | ✅ 两条都在 |
| **组件不参与渲染** | `markDirty` 无组件通道；`roleComponent` 内 `markDirty`/`HotbarRenderer` 命中 = 0 | ✅ |
**结论**：⚠️ **部分落地**：poll + push-after-cast + 两条写路径 ✓；**端口适配器 push 与帧末 flush 未落地**，**但已在 `HotbarRenderer:21-24` 显式标注**（"当前为骨架…不要在 4.4 落地前据本类判断'置脏已覆盖刷新'而删掉 legacy 刷新路径（会断可见刷新）"）⇒ **挂账 V-12（info）**；与指南 `:394` 的"**只做 push 一定会漏刷新，必须由框架 poll**"一致 ✓ —— 当前可见刷新**由 poll 兜底**，故**行为不缺失**。

---

## 5. Mode B 与构建证据（可复现性）

**构建/可编译性**（我复算，未重复构建）：
```
jar = 135333 B / 2026-09-17 22:06:15.571 / SHA256 76AC54D9F8AB488951C40FDF72A5660798C44D309653E61114E88A3A1FECCF87
最新 .class = MeiqiheziCircleSlashSkill.class @22:06:15.364
src 新于 jar 的文件数 = 0   ✓
HEAD = 0564b3b…（2026-09-18 19:09:53）；src porcelain = 0 行
```
**三份指定日志**（作用域 = 该日志文件；`git grep` 大小写敏感）：
| 日志 | 身份 | 三行合格线 | `SHDFGamePlugin` / 依赖发现行 | 正序集合差（锚点 = `debug-logs/测试记录/阶段1-起服锚点-t2ModeB.log`） |
|---|---|---|---|---|
| `debug-logs/测试记录/阶段4-起服日志-B10.log` | 6077 B / 19:59:17 / `F71432FB176D09C23164AADC16C2B1220C3C2C130CE43D6348C4AC519E6FDF5E` | ✓ `:10` plugins(1)、`:45` Enabling v1.0.0、`:46` Character System enabled.、`:50` Done (19.832s) | **0** ✓ | `ADDED 0 / REMOVED 0 / **C 0**` |
| `debug-logs/测试记录/阶段4-起服日志-T1.log` | 6077 B / 20:08:44 / `4EF73A345E496C4E1B22F87A02E5C00A7EA0A53C42C758A06E0DAD24C56863CA` | ✓ `:45`/`:46`、`:50` Done (27.951s) | **0** ✓ | `ADDED 0 / REMOVED 0 / **C 0**` |
| `debug-logs/测试记录/阶段4-起服日志-T2.log` | 6077 B / 20:13:57 / `792E1AB0C9B653DCF9A9E1FC446B38C782FC8A15A5C45CD3F9062EB22F7AEBB3` | ✓ `:45`/`:46`、`:50` Done (25.542s) | **0** ✓ | `ADDED 0 / REMOVED 0 / **C 0**` |
- 三份均为 **Mode B**（`Succeed to find the dependency 'ShadowHunterRolesPlugin'!` **未出现**）✓，`C = 0` ✓（正序、只看 C）。
- ⚠️ **"已停服"= 证据缺口（V-13）**：三份日志内 `Stopping server|Saving players|Closing Server` 命中 **0 行**（副本止于更新提示 WARN）⇒ **无法从日志本身复核**；我可验证的是**当前** 25566 **无监听** ✓。按纪律**如实标注缺口、不推定**。
- 本卡**未起服**（按卡面允许"复用已冻结日志"）；**未落停服物证**（本卡没有起服 ⇒ 无物证需求）。

**双角色冒烟**：**引用用户本轮结论**——"**双角色冒烟没有发现 bug**"；**口径**：用户为**人工/整轮观察**、**未逐项填表**，故本报告**不编造**逐项结果，仅登记该结论为**用户侧实测**（档位：用户观察；覆盖范围未逐项化）。

---

## 6. 与既有验证的分工（避免顶替）

| 报告 | 范围 | 与 t15 的关系 |
|---|---|---|
| `debug-logs/测试记录/阶段4-T块独立验证报告.md`（22338 B / `366177ED…`，t49） | T 块已落地项（①–⑥ 判据、热键栏契约、四份日志） | **分项输入**（本报告引用其 §1/§5/§6 并**在 §7-V11 记录其 F1 已闭合**）；**不顶替** t15 的 I-3..I-15 / 白名单 / §3.4 / §3.5.1 |
| `debug-logs/测试记录/阶段4-T块⑧④①③⑤独立复核.md`（16523 B / `D43BF5BE…`，eng-会话） | 提交 `04c0fd1` 的 T 块 ⑧④①③⑤ | **分项输入**（构造期注入、`bind(` 归零等） |
| **本报告** | **阶段 3–4 全量：I-3..I-15 + 端口白名单 + §3.4 + §3.5.1 + Mode B/构建 + 冒烟口径** | 供 **t16 评审** 与 **t24 结案** |

---

## 7. findings（分级 + 复现步骤）

### V-01 · **blocker** · I-15 不成立：组件 SanTE 钩子派发经 Bukkit 事件总线，listener 转发未删
- **事实**：`setCurrentSanTE:516-521` → `callEvent(SanTEChangeEvent)` → `RoleEventListener:18-21`（**已注册** `ShadowHunterRolesPlugin:81`）→ `instance.triggerSanTEChange:622` → 广播 `component.onSanTEChange`；`triggerSanTEChange` 的**唯一调用点**就是该 listener。
- **复现**：
```powershell
git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- src/main/java/com/shadowHunterRolesPlugin/listener   # → 2 行（:14 / :20）
git grep -nE '\btriggerSanTEChange\b' -- src/main/java                                                       # → 定义 :622 + 调用 :20 + 1 javadoc
```
- **要求（按队长裁定：修代码，不降标准）**：容器**直接**派发组件钩子；**保留** `SanTEChangeEvent`/`EnergyChangeEvent` 的**对外发布**（红线）；**删除 listener 的转发**（`RoleEventListener:14/:20`）⇒ 使 I-15 的检查项（listener 内为空 + 转发已删）成立。

### V-02 · **high** · **判据自身缺陷（criterion defect）**：I-15 的检查命令**假通过**
- **事实**：`git grep` 的 **glob pathspec 必须落到"文件路径"**；以**目录名**结尾的 `src/main/java/**/listener` 不匹配任何文件 ⇒ 返回 0 **不代表干净**。
- **复现**：
```powershell
git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- 'src/main/java/**/listener'     # → 0 行 ✗ 假通过
git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- 'src/main/java/**/listener/*'   # → 2 行 ✓
git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- src/main/java/com/shadowHunterRolesPlugin/listener  # → 2 行 ✓
```
- **要求**：把 I-15 的判据命令统一改为 `-- 'src/main/java/**/listener/*'`（或目录前缀式）；并把"**glob pathspec 必须落到文件**"并入 §6.2 口径纪律。

### V-03 · **high** · I-14 的"不递归 + 合并补发一次"未实现
- **事实**：`pre == now` 不派发 ✅（`RoleInstance:625`）；但 `inSanTEDispatch|dispatching|pendingSanTE|pendingSan|reentr|dispatchDepth` 在 `src/main/java` **全 0 命中**，`triggerSanTEChange`（`:622-636`）无重入护栏 ⇒ 结合 V-01 的事件总线路径，**钩子内写 SanTE 会嵌套派发**。
- **复现**：`git grep -nE 'inSanTEDispatch|dispatching|pendingSanTE|pendingSan|reentr|dispatchDepth' -- src/main/java` → 0 行；读 `RoleInstance.java:622-636`。
- **要求（与 V-01 同批修）**：加重入护栏 + "派发期间同类型写入合并、结束后按最终值补发一次"。

### V-04 · **medium** · I-14/I-11 的验收方式（单测）在本仓库**物理不可执行**
- **事实**：`git ls-files -- 'src/test/*'` = 0 行；`Test-Path src/test` = False；全仓 268 个 `.java` 中 `org.junit|@Test` 命中 **0**；`build.gradle.kts:31` 仅 `testImplementation(kotlin("test"))`（历次构建 `test NO-SOURCE`）。
- **处置（按队长裁定）**：**允许把验收方式映射为结构性证据**（意图不变）——本报告已用结构证据覆盖：`pre==now` 不派发（V-03 前半 ✅）、注册顺序 A→B（§1-I-11：`ArrayList` 插入序 ✅）；**"建立测试源集"单列为阶段 5 质量项**（不塞进本批）。

### V-05 · **low** · 死 import 使 I-7 裸串判据非空
- `roleComponent/red/RedDeeplySorrowSkill.java:3 import com.shadowHunterRolesPlugin.core.RoleInstance;` **无代码使用**（同文件内仅注释 `:65` 提及）。**复现**：`git grep -n '\bRoleInstance\b' -- …/roleComponent` → 18 行（17 注释 + 1 死 import）。**要求**：清理该 import（或经裁定后在判据中注明"仅注释提及属预期"）。

### V-06 · **low（挂账）** · I-12 的 `createIconItem`/`getDisplayName(RoleInstance)` 各 2 处基类声明
- 属**已登记的 T 块第 ⑦ 项**（渲染统一接管未做）；`SKILL_KEY`/`MAIN_WEAPON_KEY` 各 1 处 ✅。**要求**：在 T-⑦ 落地时收敛为 1 处，并在结案报告中复核。

### V-07 · **info** · I-10 空转（vacuous）
- `RequiresComponents|requires()` = 0 行 ⇒ 无声明、无需校验路径；**不得**据此声称"校验路径已验证"；阶段 5+ 若引入该机制需重核。

### V-08 · **info（口径）** · I-6 判据提及的 `CooldownRegistry` 尚不存在
- `git grep -n 'CooldownRegistry' -- src/main/java` = 0 行（属 `dc8c786` 冷却自管理规划）；当前**单源 = `RoleInstance`**（4 个读法）+ 端口/渲染/API 调用点 ⇒ 判据建议改为"单源 + 调用点"口径。

### V-09 · **info（口径）** · `Self.id()` 零组件调用者但**设计明确必需**
- `Self.java:16 UUID id();`；`self().id()` 在 `src/main/java` **0 命中**；设计 `:476` 明写"**`id()` 仍必需**（`clearRole(uuid)`、日志、按 UUID 查询）"，`:455` 亦写"方案 A 下可留可去" ⇒ **不判违规**。建议在 §5.1 白名单行补注"（框架侧使用；组件侧零调用）"以消除与 §3.3 判据三的表面冲突。

### V-10 · **info（口径）** · §3.4 的 precheck 位置与指南伪代码不同（行为等价）
- 实现放在 listener 层（`SkillListener:58/:100/:138`、`MainWeaponListener:76/:100/:132`），伪代码写在 `handleCast` 内；**冷却闸门 ✓、能量非闸门 ✓、沉默/眩晕由组件经端口判定 ✓** ⇒ 行为等价。建议同步伪代码或注明实现位置。

### V-11 · **已闭合（resolution）** · t49-F1（`HotbarRenderer` 文档↔接线）已按建议以**文档警告**闭合
- 现 `HotbarRenderer.java:9`（"空闲 tick 零 setItem（**4.4 生效**）"）与 `:21-24`（"⚠️ 当前为骨架…**不要在 4.4 落地前据本类判断'置脏已覆盖刷新'而删掉 legacy 刷新路径（会断可见刷新）**"）⇒ 风险已在文档层显式标注 ✓。**残留**：`:11` 仍以"五类事件由端口适配器置脏"表述**目标契约**，建议加"（4.4 目标）"字样以免误读（并入 V-12）。

### V-12 · **info（挂账）** · §3.5.1 部分落地（端口 push + 帧末 flush = 4.4 待办）
- 见 §4 表；当前可见刷新**由 poll 兜底**（`RoleInstance:646/:676/:691` + `BuffManager:108/:136`），与指南 `:394` 一致 ⇒ **行为不缺失**；要求在 4.4 接管时补齐并复核 `isDirty()/clearDirty()` 的消费者。

### V-13 · **low（证据缺口）** · "已停服"无法从日志复核
- 三份指定日志（及 t49 核过的四份）内停服痕迹 = **0 行**；可验证的只有**当前** 25566 无监听。**要求**：后续批次在日志尾部保留停服行，或另存停服物证（时间戳 + 端口 + java 进程数 + 日志独占读结果）。

---

## 8. 为后续任务提供的输入

- **t16（评审）**：V-01/V-02/V-03 为**必修项**（I-15 与 I-14 未达标）；V-04 为**口径裁定项**；其余为口径/挂账。
- **t24（结案）**：V-06（T-⑦）、V-12（4.4 接管）、V-13（停服物证）应入台账；V-07（I-10 空转）在阶段 5+ 若引入 `RequiresComponents` 需重核。
- **可复用前置**：本报告 §1 的命令与期望值、§2 白名单逐条表、§3 §3.4 逐条证据、§5 日志身份与 `C=0`、§4 刷新契约现状。

---

## 9. 复算命令（一键核对本报告）

```powershell
cd <repo>/ShadowHunterRoles      # 全部 git grep（大小写敏感），作用域已写明
git grep -nE 'setContext\(|getContext\(|hasContext\(|removeContext\(' -- src/main/java                       # I-3/I-13 → 0
git grep -nE 'Bukkit\.getScheduler\(\)|getGlobalRegionScheduler\(\)|new BukkitRunnable' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent  # I-4 → 0
git grep -nE '^import com\.shadowHunterRolesPlugin\.' -- src/main/java/com/shadowHunterRolesPlugin/api       # I-5 → 2（均 core.Faction，豁免）
git grep -nE 'getRemainingSkillCooldownTicks|getRemainingSkillCooldownSeconds|getRemainingMainWeaponCooldownTicks|getRemainingMainWeaponCooldownSeconds' -- src/main/java  # I-6 → 14
git grep -nE '\bRoleInstance\b' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent                   # I-7 → 18（17 注释 + 1 死 import）
git grep -nE 'instanceof[^;]*(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)' -- src/main/java # I-8 → 0
git grep -nE 'RequiresComponents|requires\(\)' -- src/main/java                                              # I-10 → 0（空转）
git grep -nE 'skillMap\.values\(\)|mainWeaponMap\.values\(\)|passiveMap\.values\(\)' -- src/main/java        # I-11 → 0（旧 map 无顺序遍历）
git grep -nE 'inSanTEDispatch|dispatching|pendingSanTE|pendingSan|reentr|dispatchDepth' -- src/main/java      # I-14 → 0（护栏缺失）
git grep -nE 'triggerSanTEChange|triggerEnergyChange' -- 'src/main/java/**/listener/*'                       # I-15 → 2（应为空）
git grep -n 'markDirty' -- src/main/java/com/shadowHunterRolesPlugin/core                                    # §3.5.1 → 9（真实调用 2）
(Get-Item build/libs/ShadowHunterRolesPlugin-1.0.0.jar).Length                                               # 135333
```

---

## 10. 身份与边界

- **本报告身份**：见回报（字节 / 行数 / SHA256）；**本轮唯一写入** = 本文件。
- **身份口径**：本报告为**新建未跟踪文件** ⇒ `git grep` 不搜其内容（自检必须**直读**，我已用 UTF-8 直读复核）；读者复核请用文件字节 SHA256 + 行数（口径 = 工作树字节，本仓库 `core.autocrlf=true` ⇒ CRLF）。
- **未做**：未改任何 `src`、未构建、未起服、未做 git 写、未申请提权、未改历史证据文档（清单/交付小结/指南/交接）。
- **未决**：V-01/V-02/V-03 需落码/改判据（另开修复卡）；V-12 待阶段 4.4；V-13 需后续批次补留停服证据；V-04 的"建立测试源集"列入阶段 5 质量项。
