# ⚠️ 本目录是**回滚演练**产物，不是一次真实回滚

> 生成：2026-09-16 22:23（+08:00）｜执行者：eng-验证（verifier）｜任务：t12
> 目的：验证 `docs/回滚快照/before/` 的"**逐文件拷回**"流程**真的可执行**（该快照说明书自认从未被执行过，见 `README-回滚说明.md` §4）。
> 性质：**非破坏式** —— 全程只在**本目录内**完成拷贝与自检，**没有改动 `ShadowHunterRoles\src\` 的任何文件**。

## 目录内容

| 路径 | 说明 |
|---|---|
| `restored/**` | 从 `docs/回滚快照/before/**` 逐文件拷回的副本（48 个文件）。**相对路径与真实回滚目标 `ShadowHunterRoles\` 完全一致**（即 `restored/src/main/java/...` 对应 `ShadowHunterRoles/src/main/java/...`），因此这份目录就是"假如真回滚，拷完后 src/ 应该长什么样"的**等价物** |
| `SHA256-selfcheck.txt` | 自检原始输出：**三方比对**（`drill 副本` == `before 副本` == `integrity-sha256.txt` 登记值），逐文件一行，末行给结论 |

## 使用注意

1. **不要把本目录当成回滚来源**。真要回滚时，来源是 `docs/回滚快照/before/`（本目录只是它的验证副本）。
2. 本目录**不参与构建**、**不随插件交付**，可整目录删除而不影响任何回滚能力。
3. 判定标准与真实回滚的逐步命令见 `docs/阶段0-回滚演练记录.md`。
