# 阶段 4 · T 块已落地项独立验证报告（t49）

> **执行者**：eng-验证（verifier）｜**任务**：t49（verification）｜**attempt**：1
> **执行时刻**：2026-09-17 20:3x（+08:00）｜**起点锚**：`HEAD = 0c8ac1a27dee2aacdcfecedb4ad6b7085050df1e`（工作树 `src` porcelain = 0 行）
> **性质**：只读验证（不改任何 `src`、不构建、不起服、不做 git 写）；**本报告是唯一写入**。
> **口径纪律（本报告全程遵守）**：一律 `git grep`（**大小写敏感** 🔴），每条判据写明**路径作用域**与**匹配形态**；区分**限定名 vs 裸串**、**扇出 vs 方法壳**、**代码引用 vs 文档提及**；计数一律**逐行累加**（**未使用** `Select-String`，**未使用** `@(... | Measure-Object).Count`）。

---

## 0. 结论摘要

**判据 ①–⑥ 全部通过（6/6）；未发现 blocker / high；1 条 medium + 4 条 low + 1 项证据缺口。**

| # | 判据 | 结果 | 关键证据 |
|---|---|---|---|
| ① | 四类 `*Aware` 实现者与 `instanceof` 全仓 = 0 | ✅ | 两条 `git grep` 均 **0 行** |
| ② | `core/RoleComponentAware/**` 目录不存在、无引用残留 | ✅（**口径见 F2**） | 目录已删（`ls-files`=0、`Test-Path`=False）；`RoleComponentAware` = **0 行**；四类**裸串 = 4 行**（**全部是 javadoc 提及**） |
| ③ | `isMigrated`\|`markMigrated` 全仓 = 0 | ✅ | **0 行** |
| ④ | `runComponentPipeline` = 定义 1 + 调用 6 | ✅ | **7 行**；定义体 **单语句纯委派** |
| ⑤ | 组件侧 `rolesContext()` = 0；三接口实现者 = 0 | ✅ | **0 行 / 0 行** |
| ⑥ | 渲染覆写声明：`roleComponent` = 0 / 全仓 = 4 | ✅ | **0 行 / 4 行**（`MainWeapon:44,:91`、`Skill:38,:94` 两基类） |
| 构 | 构建可编译性：jar + 最新 `.class` + `src 新于 jar = 0` | ✅ | jar **134336 B / 20:29:54.878 / `4CC363AC…3EFA`**；最新 `.class` @20:29:54.234；`src 新于 jar = **0**` |
| 日 | 四份日志：三行齐 + 正序集合差只看 `C` + 已停服 | ✅ / ⚠️ | 三行齐 ✓、`SHDFGamePlugin`=0 ✓、**`C = 0`** ✓；**"已停服"= 证据缺口（F5）** |
| 热 | 热键栏刷新契约（删回退后仍可见刷新） | ✅（**另见 F1**） | `handleCast:186` / `handleAttack:205` 置脏；五类事件均到达 `updateHotbar()`；**每 tick** 由 `shouldUpdateHotbar()`（`:676`）驱动 |

**findings（分级）**

| ID | 级别 | 问题 | 位置 |
|---|---|---|---|
| **F1** | **medium** | `HotbarRenderer` **文档契约与接线不一致**：javadoc 称"施放/冷却启动/冷却到点/能量/buff 五类事件由**各自的端口适配器**内部调用 `markDirty()`"，实测**端口适配器调用 = 0**、全仓 `markDirty()` 仅 **2 处**（施放/攻击），且 `isDirty()/clearDirty()` **无任何消费者** ⇒ 该渲染器是**未接线的骨架**，`markDirty()` **当前无可见效果**（可见刷新仍由 legacy `updateHotbar()` 承担）。**风险**：若后续依该 javadoc 认为"置脏已覆盖刷新"而删除 legacy 刷新路径，可见刷新会断 | `core/hotbar/HotbarRenderer.java:9-12,20-31`；`core/RoleInstance.java:186,205` |
| F2 | low | 判据②"**无任何引用残留**"需写明口径：**代码引用 = 0** ✓，但**文档提及 = 4 行** | `DefaultSanTEZeroPunishment:20`、`CircleSlash:93`、`RedDeeplySorrow:18`、`RedSolitary:100` |
| F3 | low | T-1"**六组 legacy 扇出删除**"在**方法壳**口径下不成立（6 个方法名仍在；扇出本体确已删） | `RoleInstance.java:579,591,601,615,619,635` |
| F4 | low | **过期注释**：`triggerUpdate()` 注释仍写"放在 legacy 三段扇出**之后**"，而该扇出已删 ⇒ 误导读者 | `RoleInstance.java:637-638` |
| F5 | low（**证据缺口**） | 四份批次日志**内无停服痕迹**（`Stopping server\|Saving players\|Closing Server` = **0 行**）⇒ "已停服"**无法从日志本身复核**；可复核的只有**当前** 25566 无监听 | `docs/测试记录/阶段4-起服日志-{B9,B10,T1,T2}.log` |
| F6 | low | 死路径残留：`RoleEventListener.onEnergyChange` 仍调用已**空壳化**的 `triggerEnergyChange`（体内仅 null 守卫） | `listener/RoleEventListener.java:13-15` → `RoleInstance.java:615-617` |

> **未成立项：无**（判据 ①–⑥ 与热键栏契约均成立），故按卡面要求**不需要** blocker/high。

---

## 1. 逐条判据（命令原文 + 输出 + 结论）

> 全部命令的工作目录 = `ShadowHunterRoles/`；均为 `git grep`（默认**大小写敏感**）。计数方法 = 逐行累加。

### ① 四类 `*Aware` 实现者与 `instanceof` = 0
```powershell
git grep -nE 'implements[^{]*(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)' -- src/main/java   # → 0 行
git grep -nE 'instanceof[^;]*(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)' -- src/main/java    # → 0 行
```
**结论**：✅ 两条均 **0 行**（作用域 `src/main/java`、匹配形态 = `implements` 子句 / `instanceof` 表达式）。

### ② `core/RoleComponentAware/**` 目录不存在 + 引用残留
```powershell
git ls-files -- 'src/main/java/com/shadowHunterRolesPlugin/core/RoleComponentAware/*'  # → 0 行
Test-Path 'src/main/java/com/shadowHunterRolesPlugin/core/RoleComponentAware'            # → False
git grep -n 'RoleComponentAware' -- src/main/java                                        # → 0 行
git grep -nE '\b(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)\b' -- src/main/java  # → 4 行
```
**4 行输出（全部为 javadoc/注释提及，非代码引用）**：
```
roleComponent/DefaultSanTEZeroPunishment.java:20  *   <li>去 legacy `SanTEChangeAware` 与 `LifecycleAware` ⇒ 改走基类新钩子
roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java:93  * **之前**广播 ⇒ 与旧 `LifecycleAware.stop(...)` 的行为等价（O-4 的前摇取消）；框架还会兜底取消本组件
roleComponent/red/RedDeeplySorrowSkill.java:18  *   <li>去 legacy `UpdateAware` 与 `SanTEChangeAware` ⇒ 改走基类新钩子 {@link #update()} 与
roleComponent/red/RedSolitaryArroganceSkill.java:100  * `cancelAllAndClear()` **之前**广播 ⇒ 与旧 `LifecycleAware.stop(...)` 等价（O-5 的取消）；框架另有兜底（幂等）。
```
**结论**：✅ 目录与**代码引用**均已清除；⚠️ **4 行文档提及**按判据字面"无任何引用残留"不成立 ⇒ **F2**（口径需写明；这些提及本身属**有价值的历史对照说明**，不建议删除）。

### ③ `isMigrated` | `markMigrated` = 0
```powershell
git grep -nE 'isMigrated|markMigrated' -- src/main/java   # → 0 行
```
**结论**：✅（含此前 t48 已删的 3 行注释；本次复核确认**已无任何残留**）。

### ④ `runComponentPipeline` = 定义 1 + 调用 6，且定义为**纯委派**
```powershell
git grep -n 'runComponentPipeline' -- src/main/java   # → 7 行
```
```
core/RoleInstance.java:271  if(runComponentPipeline(CastTrigger.LEFT_CLICK, caster)) return true;
core/RoleInstance.java:281  if(runComponentPipeline(CastTrigger.RIGHT_CLICK, caster)) return true;
core/RoleInstance.java:291  if(runComponentPipeline(CastTrigger.DROP, caster)) return true;
core/RoleInstance.java:301  private boolean runComponentPipeline(CastTrigger trigger, Player caster){
core/RoleInstance.java:335  if(runComponentPipeline(CastTrigger.LEFT_CLICK, caster)) return true;
core/RoleInstance.java:345  if(runComponentPipeline(CastTrigger.RIGHT_CLICK, caster)) return true;
core/RoleInstance.java:355  if(runComponentPipeline(CastTrigger.DROP, caster)) return true;
```
定义体（`:301-303`，我逐行读取）：
```java
/** T-2 (3)：**纯委派**（迁移标记已删）—— 组件一律走新管道，单一入口 = {@link #handleCast}。 */
private boolean runComponentPipeline(CastTrigger trigger, Player caster){
    return handleCast(trigger, caster);
}
```
**结论**：✅ **7 行 = 定义 1 + 调用 6**（3 + 3，分别在技能与主武器两组入口）；定义为**单语句纯委派** ✓ —— 与"无第二套判据/无迁移分支"一致。

### ⑤ 组件侧 `rolesContext()` = 0；三接口实现者 = 0
```powershell
git grep -n 'rolesContext()' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent   # → 0 行
# 三接口实现者：见 ① （同为 0 行，scope 更宽）
```
**结论**：✅

### ⑥ 渲染覆写声明：`roleComponent` = 0 / 全仓 = 4（基类）
```powershell
git grep -nE '(createIconItem|getDisplayName)\s*\(\s*(RoleInstance|final RoleInstance)' -- src/main/java                        # → 4 行
git grep -nE '(createIconItem|getDisplayName)\s*\(\s*(RoleInstance|final RoleInstance)' -- src/main/java/…/roleComponent       # → 0 行
```
```
core/MainWeapon.java:44  public ItemStack createIconItem(RoleInstance instance) {
core/MainWeapon.java:91  public Component getDisplayName(RoleInstance instance){
core/Skill.java:38       public ItemStack createIconItem(RoleInstance instance) {
core/Skill.java:94       public Component getDisplayName(RoleInstance instance){
```
**结论**：✅ **roleComponent = 0 / 全仓 = 4**，且 4 处均为 `core` 基类声明（= T 块第 ⑦ 项处理对象，本轮**不要求**删除）。

---

## 2. T-1 / T-2 / 残留清理 的补充核验（背景声明逐条）

| 背景声明 | 我的现算 | 判定 |
|---|---|---|
| T-1：六组 legacy 扇出（`triggerLifecycleAwake/Start/Stop`、`triggerEnergyChange`、`triggerSanTEChange`、`triggerUpdate`）已删 | **扇出本体已删 ✓**（① 的 `instanceof`=0 佐证）；**方法壳全部保留**（`:579,:591,:601,:615,:619,:635`） | ✅（**口径见 F3**） |
| — 六个壳体内容 | `:579` 广播 `component.awake()`；`:591` `start()`；`:601` `stop()`（注册表顺序）；`:615` **空壳**（仅 `if(player==null) return;`）；`:619` `onSanTEChange(pre,now)`（含 B⑨ 的"真变化才派发"）；`:635` `update()` 广播 | 已逐行读取 |
| T-1：调用点 | `:115,:116,:119`（初始化/每 tick）、`:699`（`clear()`）、`listener/RoleEventListener:14,:20` | 保留为**新钩子入口**，非 legacy 扇出 |
| T-2 ①：迁移标记清除 | ③ = **0 行** ✓ | ✅ |
| T-2 ③：单一入口 | ④ ✓（定义 1 + 调用 6 + 纯委派） | ✅ |
| 残留清理：`Skill.onRightClick` 死声明删除 | `onRightClick` 共 **11 行**，其中**唯一真实声明** = `listener/MainWeaponListener.java:82 public void onRightClick(PlayerInteractEvent event)`（**无关的事件处理器**）；其余 10 行 = `MainWeapon:30`/`Skill:27`/`RoleInstance:344,346` + 5 处组件 javadoc 的**历史对照提及** | ✅（按"声明"口径）；裸串口径见 F2 同类 |
| 残留清理：`RoleInstance` 的 T-1 未迁移回退删除 | `handleCast`（`:170-189`）体内**只有** `onCast → 冷却 → markDirty`，**无 legacy 回退** ✓（`:286` 亦留 T-2c 说明注释） | ✅ |
| 开关 `USE_COMPONENT_PIPELINE` | **0 行** ✓ | ✅ |

---

## 3. 构建与可编译性（我复算，未构建）

```
jar = build/libs/ShadowHunterRolesPlugin-1.0.0.jar
    = 134336 B / mtime 2026-09-17 20:29:54.878
    = SHA256 4CC363AC25844A48F901B4CD0F15A9BECF6A8B3049FA657F0F7587271A793EFA
最新 .class = ComponentFactory.class @2026-09-17 20:29:54.234
src 新于 jar 的文件数 = 0   ✓
HEAD = 0c8ac1a…（提交时间 20:30:07）；工作树 src porcelain = 0 行 ⇒ jar 覆盖当前全部源文件
```
**结论**：✅ 可编译性成立（`src 新于 jar = 0` + 产物齐全；本次**未**重复构建，按卡面复用批次证据）。

---

## 4. 日志与运行证据（四份，Mode B）

| 日志 | 身份 | 三行合格线 | `SHDFGamePlugin` | 正序集合差（锚点 = `docs/测试记录/阶段1-起服锚点-t2ModeB.log`） |
|---|---|---|---|---|
| `docs/测试记录/阶段4-起服日志-B9.log`（t41/B11） | 6077 B / 56 行 / 19:49:38 / `D9820CBF38874B752DE3C0FA104D06D826E4AB594ADD6C1480E1E3BFCC594213` | ✓ `:10` 插件数=1、`:45` Enabling、`:46` enabled、`:50` Done (15.723s) | **0** ✓ | `ADDED 0 / REMOVED 0 / A 0 / B 0 / **C 0**` |
| `docs/测试记录/阶段4-起服日志-B10.log`（t43/B10） | 6077 B / 56 行 / 19:59:17 / `F71432FB176D09C23164AADC16C2B1220C3C2C130CE43D6348C4AC519E6FDF5E` | ✓ `:45`/`:46`、`:50` Done (19.832s) | **0** ✓ | `ADDED 0 / REMOVED 0 / A 0 / B 0 / **C 0**` |
| `docs/测试记录/阶段4-起服日志-T1.log`（t44/T-1） | 6077 B / 56 行 / 20:08:44 / `4EF73A345E496C4E1B22F87A02E5C00A7EA0A53C42C758A06E0DAD24C56863CA` | ✓ `:45`/`:46`、`:50` Done (27.951s) | **0** ✓ | `ADDED 0 / REMOVED 0 / A 0 / B 0 / **C 0**` |
| `docs/测试记录/阶段4-起服日志-T2.log`（t45/T-2） | 6077 B / 56 行 / 20:13:57 / `792E1AB0C9B653DCF9A9E1FC446B38C782FC8A15A5C45CD3F9062EB22F7AEBB3` | ✓ `:45`/`:46`、`:50` Done (25.542s) | **0** ✓ | `ADDED 0 / REMOVED 0 / A 0 / B 0 / **C 0**` |

- 三行 = `[PluginInitializerManager] Bukkit plugins (1)` + `[ShadowHunterRolesPlugin] Enabling ShadowHunterRolesPlugin v1.0.0` + `[ShadowHunterRolesPlugin] ShadowHunter Character System enabled.` + `Done (...)`；**Mode B**（`Succeed to find the dependency 'ShadowHunterRolesPlugin'!` 均**未出现**）✓
- 正序集合差由工装 `log-delta.ps1` 计算（正序 `anchor(pre) → current(post)`，**只看 `C`**）：四份**全 `C = 0`** ✓
- ⚠️ **"已停服"复核不了**：四份日志中 `Stopping server|Saving players|Closing Server` 命中 **0 行**（副本止于更新提示 WARN）⇒ **F5 证据缺口**。**当前**状态我可验证：`Get-NetTCPConnection -LocalPort 25566` **无监听** ✓（即此刻确无服务器在跑）。

---

## 5. 热键栏刷新契约（结构性证据，file:line）

**问题**：删掉 T-1 未迁移回退后，施放是否仍会**可见刷新**？

**链路（我逐行读取代码得到）**
```
① 施放/攻击置脏（保留）
   core/RoleInstance.java:170  public boolean handleCast(...)      ← 单一入口（runComponentPipeline 委派至它）
   core/RoleInstance.java:186      hotbarRenderer.markDirty();     ← 施放后
   core/RoleInstance.java:191  public boolean handleAttack(...)
   core/RoleInstance.java:205      hotbarRenderer.markDirty();     ← 攻击后
② 五类事件全部到达「真正写物品」的 legacy 刷新方法 updateHotbar()
   冷却启动 :252（startSkillCooldown）/ :319（startMainWeaponCooldown）→ updateHotbar()
   冷却到点 :254 / :321 → platform.scheduler().runLater(this::updateHotbar, 剩余刻)
   能量变化 :496（setCurrentEnergy → updateHotbar()，before 事件派发）
   buff 变化 manager/BuffManager.java:108（instance.updateHotbar()）
   施放     :186/:205 置脏 + 紧随的冷却启动 :252/:319，并在冷却期间由 ③ 每 tick 覆盖
③ 每 tick 刷新
   core/RoleInstance.java:646  if(shouldUpdateHotbar()){ updateHotbar(); }
   core/RoleInstance.java:676  private boolean shouldUpdateHotbar(){ 任一技能/武器 isReady → true }
   core/RoleInstance.java:648  if(shouldUpdateItemMeta()){ updateAllSkillItemMeta(); }   ← 冷却中刷 meta
   定义 :369 public void updateHotbar()  ← 当前唯一的实际写物品实现
```
**结论**：✅ **契约成立** —— 删回退后 `markDirty()` 仍在 `handleCast:186` / `handleAttack:205` 被调用，且**可见刷新**另有 `updateHotbar()` 的**每 tick**（`:646/:676`）+ 冷却起止（`:252/:254`、`:319/:321`）+ 能量（`:496`）+ buff（`BuffManager:108`）四条独立触发路径 ⇒ **不依赖任何 legacy 未迁移回退**。
⚠️ **但**：`markDirty()` 本身**无消费者**（`HotbarRenderer` 是骨架：`isDirty()/clearDirty()` 全仓 0 调用；端口适配器 `markDirty` 0 调用）⇒ 见 **F1**：其 javadoc 的"五类事件由端口适配器置脏"**尚未实现**，若据该文档删 legacy 路径会断刷新。

---

## 6. findings（分级 + 复现步骤）

**F1（medium）· `HotbarRenderer` 文档契约 vs 接线不一致（未接线骨架）**
- 现状：`HotbarRenderer.java:9-12` javadoc 写"**刷新触发**：施放 / 冷却启动 / 冷却到点 / 能量变化 / buff 变化五类事件都必须置脏（由各自的**端口适配器**在内部调用 `markDirty()`）"；实测：端口适配器（`platform/`、`core/ports`）调用 `markDirty` = **0 行**；全仓 `hotbarRenderer.` 仅 **2 处**（`:186`/`:205`）；`isDirty()`/`clearDirty()` 除定义外 **0 调用** ⇒ **无 flush 消费者**，`markDirty()` 当前**不产生任何可见效果**（可见刷新由 `updateHotbar()` 承担）。
- 风险：`updateHotbar()` 是"现行唯一写物品实现"；若后续按 javadoc 误判"置脏已覆盖刷新"而删除/停用 legacy 路径，**可见刷新会断**（属"文档驱动的回归"）。
- 复现：`git grep -n 'markDirty' -- src/main/java`（7 行，其中真实调用 2）、`git grep -nE 'isDirty|clearDirty' -- src/main/java`（2 行，均为定义）、`git grep -n 'markDirty' -- src/main/java/com/shadowHunterRolesPlugin/platform src/main/java/com/shadowHunterRolesPlugin/core/ports`（**0 行**）。
- 要求（建议）：在阶段 4.4「渲染接管」时**接上 flush 消费者**并同步改写 javadoc；或在文档中显式标注"**骨架未接线，当前可见刷新由 `updateHotbar()` 承担**"，并列入 t15/t16 的检查点。

**F2（low）· 判据②口径**：`git grep -nE '\b(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)\b' -- src/main/java` = **4 行**，全为 javadoc 提及（file:line 见 §1②）。要求：判据写明"**限定名代码引用 = 0**"，并把文档提及列为**预期**。
复现：见 §1② 的命令。

**F3（low）· T-1 口径**：六组方法名仍在（`:579,:591,:601,:615,:619,:635`），**扇出本体**已删（`instanceof`=0、无 `*Aware` 实现者）。要求：判据/背景表述区分"**扇出**"与"**方法壳**"。
复现：`git grep -nE 'triggerLifecycleAwake|triggerLifecycleStart|triggerLifecycleStop|triggerEnergyChange|triggerSanTEChange|triggerUpdate' -- src/main/java` → **13 行**。

**F4（low）· 过期注释**：`RoleInstance.java:637-638` 写"放在 legacy 三段扇出**之后**"，而该扇出已删（同文件 `:589` 的说明是准确的）。建议清掉或改为"扇出已删后的顺序说明"。
复现：读 `RoleInstance.java:635-645`。

**F5（low，证据缺口）· "已停服"不可从日志复核**：四份日志停服痕迹 = 0 行；可复核的只有**当前**端口状态（25566 无监听 ✓）。建议：批次日志尾部记录停服行，或单独留存停服证据（否则 t15 对"已停服"也只能给"当前状态"证据）。
复现：对四份日志 `git grep -nE 'Stopping server|Saving players|Closing Server' -- <log>` → 0 行。

**F6（low）· 死路径残留**：`RoleEventListener.java:13-15 onEnergyChange` → `RoleInstance.triggerEnergyChange`（`:615-617` 体内仅 `if(player==null) return;`）⇒ 事件链已空壳化（与设计 §3.4"energy 链是死机制"一致），保留无害但应登记清理。
复现：读 `listener/RoleEventListener.java` 全文 + `RoleInstance.java:615-617`。

---

## 7. 为 t15 提供的前置（可用 / 待补）

**可直接复用的前置（强档位）**
- 判据 ①/③/④/⑤/⑥ 的**原样命令 + 期望值**（见 §1）与**本报告的命令集**；
- **构建可编译性**：`jar = 134336 B / 20:29:54.878 / 4CC363AC…` + `src 新于 jar = 0`；
- **四份 Mode B 日志 + 正序集合差全 `C = 0`**（§4 表，含身份值）；
- **热键栏刷新契约**的结构性证据（§5，file:line）。

**t15 需补的（本卡范围内无法闭合）**
1. **运行期可见刷新**（真玩家/机器人选角后施放 → 观察热键栏物品刷新）：本卡只有**结构性**证据；建议由 t15 的双角色冒烟覆盖；
2. **F1 的接线**（markDirty → flush）须在阶段 4.4 完成后由 t15 重核；
3. **"已停服"的历史证据缺口**（F5）：t15 若需逐批复核，请以**各自批次的停服检查**为准或要求补留停服证据。

---

## 8. 复算命令（一键核对本报告）

```powershell
cd <repo>/ShadowHunterRoles        # 全部为 git grep（大小写敏感）
git grep -nE 'implements[^{]*(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)' -- src/main/java   # 0
git grep -nE 'instanceof[^;]*(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)' -- src/main/java    # 0
git grep -n 'RoleComponentAware' -- src/main/java                                        # 0
git grep -nE '\b(LifecycleAware|UpdateAware|SanTEChangeAware|EnergyChangeAware)\b' -- src/main/java  # 4（javadoc）
git grep -nE 'isMigrated|markMigrated' -- src/main/java                                  # 0
git grep -n 'runComponentPipeline' -- src/main/java                                      # 7 = 1 定义 + 6 调用
git grep -n 'rolesContext()' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent   # 0
git grep -nE '(createIconItem|getDisplayName)\s*\(\s*(RoleInstance|final RoleInstance)' -- src/main/java                  # 4（基类）
git grep -nE '(createIconItem|getDisplayName)\s*\(\s*RoleInstance' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent  # 0
git grep -n 'markDirty' -- src/main/java                                                 # 7（真实调用 2 + 注释/定义）
git grep -nE 'isDirty|clearDirty' -- src/main/java                                       # 2（均为定义 → 无消费者）
git grep -n 'USE_COMPONENT_PIPELINE' -- src/main/java                                    # 0
(Get-Item build/libs/ShadowHunterRolesPlugin-1.0.0.jar).Length                            # 134336
```

---

## 9. 身份与边界

- **本报告身份**：见回报（字节 / 行数 / SHA256）；**本轮唯一写入** = 本文件。
- **身份口径**：本报告为**新建未跟踪文件**，故 `git grep` 不搜其内容（**自检必须直读**，我已用 UTF-8 直读复核十节与全部关键值）；读者若要复核本报告，请用文件字节 SHA256 + 行数（口径 = 工作树字节，本仓库 `core.autocrlf=true` ⇒ CRLF）。
- **未做**：未改任何 `src`、未构建、未起服、未做 git 写、未申请提权、未改历史证据文档（清单/交付小结/指南/交接）。
- **未决**：F1 的接线须待阶段 4.4；F5 的证据缺口须由后续批次补留或由 t15 以现算方式替代。
