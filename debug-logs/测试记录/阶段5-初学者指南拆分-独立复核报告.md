# 阶段 5 ·《初学者指南》文件夹化 独立复核报告（t15）

> **复核对象**：`t14` —— `docs/插件文档/初学者指南/`（5 份：入口 `README.md` + `01-插件简述.md` + `02-面向对象基础.md` + `03-读懂第一段代码.md` + `04-如何创建一个自己的角色.md`）、旧单文件 `docs/插件文档/初学者指南.md` 已删除、`docs/插件文档/README.md` 文档地图 1 行改指文件夹。
> **执行者**：`eng-规范`（spec / 仪器口径）· **卡**：`t15`（kind = verification）。
> **点名提交**：`eeacceb4c46f82272523ebbd22c620f5d9a7302f`（本卡**认领时**的 HEAD，产物全在工作树）→ **`a8aeaaf6c5e71958852447642728ad8a0a039bd6`**（**结卡前**队长把 t14 的产物入库；工作树与 HEAD 一致、`porcelain` 全仓 0 行）。本卡**零 git 写**。
> **身份纪律**：本件**不内嵌自身哈希**；他人身份一律现算并给命令。

---

## §0 结论（verdict 与分级计数）

**verdict = `pass`** ｜ **blocker 0 · high 0 · medium 1 · low 6**（另有**判据缺陷 3 条**单列于 §14）

| 复核面 | 结果 |
|---|---|
| A2 痕迹判据（**整个文件夹**） | 7 条集合 **0 命中（exit 1）** + A2b **4 行**（逐行判定 = 版本号）；**两种仪器、两种姿势同值** |
| A3 判据自检（改写前旧文档） | **8/8 非 0**（27 / 30 / 44 / 31 / 27 / 19 / 4 / **58**）⇒ 判据有区分力，无 criterion defect |
| A4 + 队长① 术语扫描与**必要性**审查 | **术语 26 个：非必需 5 个 · 未解释 0 个**；`02` 严守六词；禁用词 20 个全部 **0 命中** |
| 队长② 上一轮 F-1 六处复测 | **6/6 全部闭合**（`void`/`private`/`final` 统一带过 · `Paper` 正文删除 · `编译器` 删除 · `父类` 补解释） |
| A5 `javap` 核验 04 的装配 API | 抽查 **16 个类 / 20 条签名**，**0 条查无实据**（`Role$Builder` 13 个方法逐个对齐） |
| A6 04 校验规则 vs 源码 | 5 条规则 + 04 引用的 **5 条报错串**逐条命中（含 `maxHP <= 0` 的真实条件） |
| A7 可照做性（尽力档） | **实跑通过**：仓库外临时目录 · `javac` **exit 0 / 0 输出** · 产出 `MyFirstSkill.class`(2382 B)；**代码块从 04 正文逐行抽出（42 行、逐行差异 0）**；负对照（乱写材质 ⇒ exit 1 / 6 行报错）证明尺子有区分力 |
| A8 内容不丢失 | 旧件 **31 个标题**逐个核对：**30 个有明确去处 + 1 个（术语小表）显式说明理由** ⇒ **无静默丢弃** |
| A9 旧件已删 / 恰 5 份 | `Test-Path 旧件 = False` · 文件夹 **5 份**（两侧计数 5 / 5） |
| A10 既有 3 份未改 / README 1 行 | 3 份在 diff 中**完全缺席**；README 相对 `90e6c06` = `1 0`（1 行变更）；入库提交内 = `1 1` + 旧件 `0 439` |
| A11 不引用过程文档 | 文件夹内 `debug-logs` = **0 命中** |
| A12 初学者可读性 | 问 **10 题**：**10 题全部可答出**（含"槽位冲突""报错去哪看""改坏了怎么退回"） |
| A13 制品代次 | jar **四次现算三列全同**（159512 B / `2026-09-19 12:54:07.286` / `6A249993…`）⇒ **未重建** |
| A15 边界 | 未改 src / 未改被复核文档 / 未改既有证据 / **零 git 写** |

> **一句话**：文件夹化与新增的《如何创建自己的角色》在**判据、术语、API 对齐、可编译性、内容完整性**五个面上都站得住；唯一 medium 是 03 那句"逐字相同"的自述与源文件有 4 处细节不符（修法 = 一句话），其余为轻微一致性问题。

---

## §1 边界与身份

- **只写本报告**：`debug-logs/测试记录/阶段5-初学者指南拆分-独立复核报告.md`。
- **未改**：`src/**`（`porcelain -- src` = 0 行）· `docs/插件文档/**`（一个字都没动）· `debug-logs/测试记录/` 下既有文件（含本卡 Out-of-scope 里点名的 `阶段5-初学者指南-独立复核报告.md`）· `debug-logs/回滚快照/**`。
- **被复核件身份（现算，5 份）**：

| 文件 | bytes | 行数（ReadAllLines） |
|---|---|---|
| `初学者指南/README.md` | 3452 | 56 |
| `初学者指南/01-插件简述.md` | 5264 | 96 |
| `初学者指南/02-面向对象基础.md` | 6382 | 142 |
| `初学者指南/03-读懂第一段代码.md` | 14316 | 244 |
| `初学者指南/04-如何创建一个自己的角色.md` | 18084 | 368 |

- **非交付物的仪器落点（透明声明）**：`ShadowHunterRoles/.scratch/t15/`（被 `.gitignore:89` 忽略）—— 模式文件 `p-A1…p-verify.txt`（与我在 `t10`/`t13` 复核中独立重建的那一套**逐字节相同**）· 旧侧样本 `old-arch.md`（53075 B，`git hash-object` = blob `954fe1c4…`）· 归档件 ASCII 副本 `plugin-6A249993.jar` · 检查脚本与日志；另在**仓库外**的会话临时目录 `%TEMP%\t15c\` 做 A7 的编译（源码 + 依赖 jar + `out/`）。

---

## §2 方法与仪器（先说会骗人的地方）

| 用途 | 仪器 | 自证 |
|---|---|---|
| 痕迹检索 | `git grep -rnE -f <模式文件> -- <路径>` | 正对照 = 旧侧样本 8 条全部非 0（§3） |
| 旧侧样本（**两种姿势**） | ①`git grep -f <模式> 0c6930d -- <仓库内路径>`（**队长提示的版本内搜索**）② blob 落盘 + `--no-index` | 两法逐条同值（§3.2）；落盘法另用 `git hash-object` 证明确为 blob |
| 未跟踪件 | `--untracked --no-exclude-standard -r` | 本卡开工时该文件夹未被跟踪 ⇒ 字面命令是结构性盲区（与 t14 同一结论）；**结卡时已入库**，两种姿势同值（§3.3） |
| `javap` | `C:\Program Files\Java\jdk-21\bin\javap.exe`（**可调用性用 `--help`**，因其无 `--version`） | `--help` 输出 **35 行**；随后 16 个类逐类反汇编均有输出 |
| `javac` | 同上 JDK 的 `javac.exe` | 正跑 exit 0 / 负对照 exit 1（§8） |
| 直读 | `[System.IO.File]::ReadAllLines(…, UTF8)`（**不用 `Get-Content`**） | 全文 5 份逐行读入用于术语与"逐字"对拍 |

**⚠️ 本轮踩到并当场纠正的 3 个坑（如实自曝；两个与卡面命令本身有关，见 §14）**：

1. **卡面 verify(2)/(4) 的 `git show … > $env:TEMP\…` 不产出逐字节副本** ✗：PS 5.1 的 `>` 写 **UTF-16LE** ⇒ 实测 **72804 B**，而 blob 是 **53075 B**。⇒ 本卡改用 `cmd /c "git cat-file blob <sha> > <file>"` + `git hash-object` 自证（并与队长的 `git grep <rev>` 法交叉验证，两者同值）。
2. **卡面 verify(4) 把仓库外临时路径当 `git grep` 的 pathspec** ✗：`git grep` 只搜**仓库内被跟踪**文件 ⇒ 对 `$env:TEMP\old-arch.md` **恒返 0（假绿）**。⇒ 本卡对落盘样本一律用 `--no-index`（或直接 `git grep -f <模式> 0c6930d -- <仓库内路径>`），并**必须**配正对照。
3. **`javac @argfile` 里 `-cp "…"` 的引号不被当 shell 引号处理** ✗：我第一版把 21 个 jar 的 classpath 塞进 `@args.txt`，javac 报 `程序包 com.shadowHunterRolesPlugin.core 不存在`（类路径整体失效）；改为**直接命令行 `-cp`**（1879 字符，远低于上限）后 **exit 0**。

---

## §3 A2 / A3 · 痕迹判据独立复算 + 判据自检

### §3.1 命令与口径（三件齐）

```
# 新侧（整个文件夹，递归；本卡开工时该文件夹未被跟踪）
git -C ShadowHunterRoles grep -rnE --untracked --no-exclude-standard -f <绝对路径模式文件> -- "docs/插件文档/初学者指南"
# 旧侧（判据自检）—— 姿势①：版本内搜索（无落盘、无编码风险）
git -C ShadowHunterRoles grep -nE -f <绝对路径模式文件> 0c6930d -- "docs/插件文档/架构与组件模型.md"
# 旧侧 —— 姿势②：blob 逐字节落盘后 --no-index（用于字节/行数对拍）
cmd /c "git -C <repo> cat-file blob 954fe1c43e15c17ce53f697e998eac4f4a7cede4 > .scratch/t15/old-arch.md"
git -C ShadowHunterRoles grep -nE --no-index -f <绝对路径模式文件> -- .scratch/t15/old-arch.md
```
口径：**行级命中数**；`git grep` **默认大小写敏感**；模式文件 UTF-8 无 BOM；作用域逐条写明。

### §3.2 两侧计数表（我现算 vs t14 声称；旧侧两种姿势）

| 判据 | 旧侧（rev 法） | 旧侧（落盘法） | 旧侧 t14 声称 | 新侧实测 | 新侧 t14 声称 |
|---|---|---|---|---|---|
| A1 卡号/批次号 | **27** | 27 | 27 | **0**（exit 1） | 0 |
| A2a 阶段号 | **30** | 30 | 30 | **0** | 0 |
| A2b 数字点数字 | **44** | 44 | 44 | **4** | 4 |
| A3 协作语（18 词） | **31** | 31 | 31 | **0** | 0 |
| A4 过程文件名（9 串） | **27** | 27 | 27 | **0** | 0 |
| A5 元口径/过程记号（12 串） | **19** | 19 | 19 | **0** | 0 |
| A6 流程章节（5 串） | **4** | 4 | 4 | **0** | 0 |
| verify 合并模式（31 行） | **58** | 58 | 58 | **0** | 0 |

⇒ **A3 通过**：同一套模式在**改写前旧文档**上 8/8 非 0（**不是**"改前也 0"的无效判据）；两侧计数全部写进报告。
⇒ **A2 通过**：文件夹 7 条集合 0 命中，A2b 4 行逐行判定如下。

### §3.3 A2b 的 4 行（逐行判定）与"未跟踪盲区"复核

```
03-读懂第一段代码.md:176:- 第三行开始**构建** —— …成品是 `build/libs/ShadowHunterRolesPlugin-1.0.0.jar`；
03-读懂第一段代码.md:185:- 它会拉起一个 Minecraft 1.21.11 的调试服务端，服务端文件夹是仓库里的 `run/`；
04-如何创建一个自己的角色.md:254:看到 `BUILD SUCCESSFUL` 就说明代码翻译并打包成功，成品是 `build/libs/…-1.0.0.jar`。
04-如何创建一个自己的角色.md:264:- 拉起一个 Minecraft 1.21.11 的调试服务端，文件夹是仓库里的 `run/`；
```
4 行全部为**版本号语境**（工程产物 `1.0.0` / 服务端 `1.21.11`），无小节编号或阶段号残留 ✓。
**跟踪态复核**：`a8aeaaf` 之后该文件夹已被跟踪 ⇒ 带/不带 `--untracked` 的两条命令同值（A2b 4 / 4；verify 0 / 0）⇒ t14 自曝的"字面命令对未跟踪件是盲区"在**当时**成立、在**现态**已消失（两种姿势都能用）。

---

## §4 A4 + 队长① · 术语扫描与**必要性**审查（单列一节）

**读者前置假设**（`README.md` §"读之前"）= 会变量/赋值、列表/数组、`if`、循环、函数、算术、字符串。
**判定口径**（队长①）：① 必需但未解释 ⇒ finding；② 非必需却出现 ⇒ finding（术语过载）；③ 术语表充当许可证（正文甩词、让读者去查表）⇒ finding；④ `02` 超出六词 ⇒ finding。

### §4.1 三个数（先给结论）

> **术语总数 = 26**（超出前置假设、且在正文实际出现的词/词组；清单提到但正文 0 次的不计，见 §4.4）
> **其中非必需 = 5**（`abstract` · 事件/`@EventHandler` · `UUID`/`Map` · `ComponentFactory` · 排错引用的类名 4 个）
> **其中未解释 = 0**

### §4.2 逐词表（四列）

| # | 词 | 出现位置（首现） | 是 04 写出角色所**必需**？ | 首次出现处已一句话解释？ |
|---|---|---|---|---|
| 1 | 类（class） | `02` §一 | **必需**（你要写一个类） | ✅「表格模板 + 造物机的合体」 |
| 2 | 对象（object） | `02` §二 | **必需**（理解 `::new` 与构造时机） | ✅「照模板填出来的一张具体的表」 |
| 3 | 方法（method） | `02` §三 | **必需**（`onCast`） | ✅「就是函数…点号调用」 |
| 4 | 字段（field） | `02` §四 | **必需**（理解值存哪、为何现取玩家） | ✅「写在类里、跟着对象存在的变量」 |
| 5 | 继承 / `extends` | `02` §五 | **必需**（`extends Skill`） | ✅「把别人的定义整个拿过来」+ 「父类的俗称」 |
| 6 | 接口（interface） | `02` §六 | **非必需**（`02` 自己声明"你现在不会直接写接口"）——**但属用户点名的六词** ⇒ 保留合理，不计 finding | ✅「一张"必须提供的函数清单"」 |
| 7 | 构造函数 | `03` 第二段 / `04` 第二块 | **必需** | ✅「名字和类名一模一样…造对象时自动执行一次」 |
| 8 | `@Override` / 标记 | `03` 第三段 | **必需**（照抄） | ✅「我在重写父类里同名的那个函数」 |
| 9 | `package` | `03` 第一段 / `04` L93 | **必需**（照抄） | ✅「这个文件住在哪个文件夹，内容与路径一致」 |
| 10 | `import` | `04` L100 | **必需**（照抄） | ✅「从别的文件借东西用」，逐项点出借了什么 |
| 11 | `public`/`private`/`final`/`void` | `02` L11（统一位置）· 代码在 `02` L20/L60/L78 | **必需**（照抄时满屏都是） | ✅ **统一带过**：「Java 的固定写法（写着"这东西谁能用、能不能改、有没有返回值"），照着抄就行」 |
| 12 | `abstract` | `02` L20 代码 / L23 解释 | **非必需**（只在模板片段里出现） | ✅「这张模板不能直接拿来造东西，只能被别的类继承」 |
| 13 | tick | `03` §tick | **必需**（冷却单位） | ✅「唱机每 1/20 秒转一圈」+ `1L` 逐句解释 |
| 14 | 事件 / `@EventHandler` | `03` §事件 | **非必需**（写技能不注册事件）但**强相关**（解释"谁调用你的 onCast"） | ✅「事件就是门铃」+「`@EventHandler` 是标记」 |
| 15 | `svc()` / 工具箱 | `03` 第三段 / `04` L159 | **必需** | ✅「向框架要工具箱」「工具箱里管生命的那一格」 |
| 16 | `Component.text` | `03` 第二段 | **必需** | ✅「造一段游戏里显示的文字」 |
| 17 | `Material` / 图标物品 | `03` 第二段 / `04` L132 | **必需** | ✅「图标用的物品（烈焰粉）」「铁锭」 |
| 18 | `Sound` / 音效 · `Particle`/粒子 | `03` 第三段 | **必需**（骨架用 `Sound`） | ✅「给玩家的即时反馈」 |
| 19 | `Role.Builder` | `01` L61 / `04` L186 | **必需** | ✅「逐句填表的笔：每调一次填一格」 |
| 20 | `Definition` | `01` L53 / `04` L227 | **必需**（第 2 步要加一项） | ✅「一张小纸条：角色 id + 用哪个函数装配它」 |
| 21 | **`ComponentFactory`** | `04` L121 | **非必需**（读者从不写、不引用它） | ✅（「按图纸造零件的工人」）⇒ **本条即"非必需却出现"的唯一硬案例** |
| 22 | `List.of` | `01` L52 | **必需** | ✅「就是"造一个列表"」 |
| 23 | `Faction` / 阵营 | `04` L197/L214 | **必需**（清单要填） | ✅「只能填 HUNTER 或 SHADOW；不写就等于 UNKNOWN（谁都是敌人）」 |
| 24 | `maxHP`/`baseATK`/`maxEnergy`/`maxSanTE` | `04` L210-213 | **必需** | ✅ 表内逐项：生命上限/基础攻击力/能量上限/理智上限 + 约束 |
| 25 | `jar` | `03` §构建 / `01` L17 | **必需** | ✅「打包好的代码」「服务端能读的文件」 |
| 26 | 日志（`run/logs/latest.log`） | `04` §起服时日志里可能出现的报错 | **必需**（排错） | ✅「服务端会把发生的事情一行行打印在控制台，同时写进…（这叫日志）」 |
| — | `UUID` / `Map<UUID,Integer>` | `03` §为什么要每次现取玩家 | **非必需**（读者不写） | ✅「唯一编号」「键是编号，不是玩家对象」 |
| — | `RoleManager`/`PlayerListener`/`SkillListener`/`HotbarRenderer` | `03` 常见问题（溯源用） | **非必需**（写角色用不到） | ✅ 均随语境给出（"玩家退出时清角色""渲染器据此给红色 DISABLED"） |
| — | 构建工具 / Gradle / `gradlew` / `GRADLE_USER_HOME` / `gradle.properties` / `pluginCopyPath` | `03` §构建 / `README` L21 | **必需**（照抄命令） | ✅ 功能解释（"把构建工具的缓存放在仓库内""配置过才会顺手拷贝"）——**未逐词定义，但读者不需要** |

### §4.3 四条判定口径的结论

| 口径 | 结论 |
|---|---|
| ① 必需但未解释 | **0 条** ✅（26 个词中 21 个"必需"全部有就地解释） |
| ② 非必需却出现（术语过载） | **5 条**，其中 **4 条服务于理解或排错**（`abstract` 解释模板限制 · 事件解释"谁调用 onCast" · `UUID`/`Map` 解释"为什么现取玩家" · 排错引用的类名让读者知道"去哪儿看"）；**仅 `ComponentFactory`（`04` L121）属真正的"读者不会碰的框架类名"** ⇒ 见 **F-6（low）** |
| ③ 术语表充当许可证 | **不成立** ✅ —— 5 份里**没有**"集中术语表"；每个词都在**首次出现的正文**里解释（`02` §小结的六词表是**回顾**，不是入口；`04` 的 `package`/`import`/`@Override` 亦各自就地解释） |
| ④ `02` 超出六词 | **不成立** ✅ —— `02` 只有六节（类/对象/方法/字段/继承/接口）+ 小结；`多态`/`抽象类`/`泛型`/`设计模式`/`注解` 等现算 **0 命中** |

### §4.4 禁用词与"清单提到但正文 0 次"的词（现算）

- **20 个禁用词全部 0 命中**：泛型 · `record` · 枚举 · 注册表 · 依赖注入 · 生命周期钩子 · 脏标记 · 协程 · 线程池 · 调度器 · PDC · 属性修饰符 · 端口 · 适配器 · 反射 · 工厂模式 · 多态 · 抽象类 · 设计模式 · 注解（**与 t14 声称一致，我独立复算**）。
- `static` / `多线程` / `Bukkit` / `Gradle`：正文 **0 次**，只在 `README.md` 「假定你不会」清单里出现（承诺清单，不构成使用）⇒ **空满足**，如实登记、不计缺陷。

---

## §5 队长② · 上一轮（`t13`）F-1 六处的复测

`t13` §11 的 **F-1（medium）** = 5 个词未解释（`void` · `private`/`final` · `Paper` · `编译器`）+ `父类` 弱解释。**逐条复测**：

| # | 上一轮的项 | 现态（现算） | 判定 |
|---|---|---|---|
| 1 | `void` 未解释 | `02` L11 统一带过（"有没有返回值"）；正文 5 处出现，均在此说明之后 | **闭合** ✅ |
| 2 | `private` 未解释 | 同上（"这东西谁能用"）；9 处出现，首现 `02` L78 在说明之后 | **闭合** ✅ |
| 3 | `final` 未解释 | 同上（"能不能改"）；10 处出现 | **闭合** ✅ |
| 4 | `Paper` 未解释 | 正文 **0 次**（`03` L185 / `04` L264 改为"Minecraft 1.21.11 的调试服务端"）；仅 `README.md` L20 的承诺清单提到 | **闭合** ✅ |
| 5 | `编译器` 未解释 | **0 命中**（`03` L116 改为"名字或参数写错时，构建会直接报错"） | **闭合** ✅ |
| 6 | `父类` 弱解释 | `02` L93 补明确解释：「被继承的那份定义俗称**父类**（父 → 子：子从父那里拿东西）」，另见 `02` L139 小结表与 `03` L116 | **闭合** ✅ |

⇒ **6/6 全部闭合**；`t14` 说明件 §十 的"F-1 已修"自述**属实**。
**但**：t13 的 **F-2**（片段重排版）只**部分**闭合 —— 见 **F-5**。

---

## §6 A5 · `javap` 逐符号核验 04 点名的装配 API

**制品**：归档件 `debug-logs/回滚快照/jars/ShadowHunterRolesPlugin-1.0.0-verified-20260919-125407-6A249993.jar`（= 当前代，159512 B / `6A249993…`）；为避开 CJK 参数，先复制到 ASCII 路径再反汇编。**工具可调用性**：`javap --help` = 35 行（该 JDK 的 `javap` 无 `--version`）。

| 04 里的写法 | `javap` 输出（归档件） | 判定 |
|---|---|---|
| `new Role.Builder("myrole")` | `public Role$Builder(java.lang.String)` | ✅ |
| `.displayName(Component.text(...))` | `public Role$Builder displayName(net.kyori.adventure.text.Component)` | ✅ |
| `.description(List.of(...))` | `public Role$Builder description(java.util.List<Component>)` | ✅ |
| `.maxHP(40)` / `.baseATK(10)` | `public Role$Builder maxHP(double)` / `baseATK(double)` | ✅ |
| `.maxEnergy(100)` / `.maxSanTE(100)` | `public Role$Builder maxEnergy(int)` / `maxSanTE(int)` | ✅ |
| `.faction(Faction.HUNTER)` | `public Role$Builder faction(com.…core.Faction)` | ✅ |
| `.addSkill(id, MyFirstSkill::new, 1)` | `public Role$Builder addSkill(String, ComponentFactory<Skill>, int)` | ✅ |
| `.addMainWeapon(id, …, 0)` | `public Role$Builder addMainWeapon(String, ComponentFactory<MainWeapon>, int)` | ✅ |
| `.addPassive(id, MyPassive::new)`（**无槽位**） | `public Role$Builder addPassive(String, ComponentFactory<PassiveSkill>)` | ✅ 与 t14 对卡面的更正一致 |
| `.icon(Material.IRON_INGOT)` | `public Role$Builder icon(org.bukkit.Material)` | ✅ |
| `new Definition("myrole", RoleLoader::myRoleBuilder)` | `RoleLoader$Definition(String, java.util.function.Supplier<Role$Builder>)`（record） | ✅ |
| `ComponentFactory`（`04` 提到的框架侧装配点） | `interface ComponentFactory<T extends RoleComponent> { T create(String, ComponentServices); }` | ✅ |
| `extends Skill` | `public abstract class Skill extends ActiveComponent` + `Skill(String, ComponentServices, Component, Component, int, int, Material)` | ✅ 与 04 的"七样东西"逐位对齐 |
| `extends MainWeapon`（第 6 步可选） | `MainWeapon(String, ComponentServices, Component, Component, Material, int)`（**图标在冷却前**） | ✅ 与 04 L304 的提醒一致 |
| `extends PassiveSkill`（第 6 步可选） | `PassiveSkill(String, ComponentServices, Component, Component)` | ✅ 与 04 L305 一致 |
| `getCooldownTicks()` | `ActiveComponent.getCooldownTicks()`（`public final`） | ✅ |
| `svc().buffs().canCastSkill()` | `BuffPort.canCastSkill()` ✅ / `ComponentServices.buffs()` → `BuffPort` ✅ | ✅ |
| `svc().vitals().heal(2)` | `VitalsPort.heal(double)` ✅ / `ComponentServices.vitals()` ✅ | ✅ |
| `svc().self().player()` | `Self.player()` ✅ | ✅ |
| `svc().cooldowns().start(getCooldownTicks())` | `CooldownPort.start(int)` ✅ | ✅ |
| `CastResult.NO_COOLDOWN` / `.SUCCEED` | 枚举常量二者都在 | ✅ |
| `03` 提到的 `HotbarRenderer.applySkill`（构建"图标外观"） | `private void applySkill(...)`（**用 `javap -p` 才可见**） | ✅ 存在（**注意**：不带 `-p` 会看不到 ⇒ 不得据此判"查无实据"） |

⇒ **抽查 16 个类 / 20+ 条签名，0 条查无实据** ⇒ **A5 通过**。

---

## §7 A6 · 04 的校验规则与源码一致

| 04 的说法 | 源码（现算） | 判定 |
|---|---|---|
| 槽位只能是 0..8，且不能冲突；**由 `addSkill`/`addMainWeapon` 调用** | `core/Role.java:249 private void validateSlot(int slot)`；调用点 `:192`（`addSkill`）/`:227`（`addMainWeapon`）；`addPassive` **不调用**（`04` 说被动没有格子号 ✓） | ✅ |
| 报错串 `Slot must be between 0 and 8, got: 9` | `Role.java:251 throw new IllegalArgumentException("Slot must be between 0 and 8, got: " …)` | ✅ |
| 报错串 `Slot 1 is already occupied by '…'` | `Role.java:254 throw …("Slot " + slot + " is already occupied…")` | ✅ |
| 组件 id 跨类型唯一；`Skill already registered: myrole_skill_first` | `Role.java:242 private void ensureIdNotRegistered(String type, String id)`；`:244 throw …(type + " already registered: " + id)`；三处调用 `:190`(Skill)/`:209`(Passive)/`:225`(MainWeapon) ⇒ **共用命名空间** ✓ | ✅ |
| `Role 'myrole' failed assembly validation and was NOT registered` | `registry/RoleLoader.java:104` 同串 | ✅ |
| `maxHP > 0`（04 说"填了 0 或负数 ⇒ 必须大于 0"） | `Role.java:143 if (maxHP <= 0)` ⇒ `:144 throw …("Role maxHP cannot be negative.")` ⇒ **条件是 `<= 0`**，与 04 的描述一致（报错文案本身写作"negative"） | ✅ |
| `baseATK` / `maxEnergy` / `maxSanTE` ≥ 0 | `Role.java:152/160/168` 三条 `cannot be negative` 分支 | ✅ |
| "某个角色装不上，不影响别的角色" | `RoleLoader.java` 的按角色 try/catch（`04` 指向 `loadInto(...)`） | ✅ |

⇒ **A6 通过**（5 条规则 + 5 条报错串逐条命中）。

---

## §8 A7 · 可照做性（尽力档 → **实跑通过**，深度如实标注）

**做法**（仓库外临时目录 = 会话 `%TEMP%\t15c\`；**不碰仓库**）：
1. **从 `04` 正文的代码围栏里把整份文件抽出来**（不手抄）：按围栏定位 `doc[44..85]` ⇒ **42 行**；与 `04` 正文逐行对拍 **差异 0**；
2. 落盘为 `…\src\com\shadowHunterRolesPlugin\roleComponent\myRole\MyFirstSkill.java`；
3. 类路径 = 归档制品（`6A249993` 代）+ `paper-api-1.21.11-R0.1-SNAPSHOT.jar` + 19 个 `net.kyori:*`（共 21 件，均复制到 ASCII 路径）；
4. 实跑：

```
& 'C:\Program Files\Java\jdk-21\bin\javac.exe' -encoding UTF-8 -nowarn -cp <21 jar 的 classpath>
      -d <TEMP>\t15c\out <TEMP>\t15c\src\…\MyFirstSkill.java
⇒ exit code = 0   输出行数 = 0（0 错误 0 警告）
⇒ 产物：MyFirstSkill.class（2382 B）
```
**负对照（证明这把尺子有区分力）**：把 `Material.IRON_INGOT` 改成 `Material.NOT_A_REAL_MATERIAL` 后同一命令 ⇒ **exit 1 / 6 行报错**。
**深度声明**：本项为**编译级**（源码 → `.class`）；**未**打包进插件 jar、**未**起服、**未**在游戏里验证效果（那是运行级，见 §15.2）。`04` 的目标只是"教会初学者写出角色"，编译通过 + API 签名逐个对齐 **已足以支撑其主张**；但它仍**不等于**"照做一遍就能在游戏里看到铁锭图标"。

---

## §9 A8 · 内容不丢失核对

**旧件（点名=`08fe741`，见 §14 CD-3）**：`docs/插件文档/初学者指南.md` = 439 行 / **31 个标题**（`git show 08fe741:…` 后按 `^#{1,3} ` 逐个数）。
**核对方式**：我自己逐个标题去新 5 份里找去处（读全文 + 关键词定位），并与 `t14` 说明件 §五 的映射表比对。

| 结果 | 数量 | 说明 |
|---|---|---|
| 旧标题在新文件夹有明确去处 | **30** | 总标题→`README` · 给谁看/假定清单→`README` · §一/§二(含清单类比与三层关系表)→`01` · §三(六词)→`02` · §四(tick/事件)→`03` · §五(逐行读 4 小节)→`03` 三段 + 现取玩家 · §六(5 步)→`03`（并**扩充**为三种退回）· §七(7 条问答)→`03`（**我复算 = 7 条**）· §"接下来看哪一份"→`README` 三张表 |
| 未落地新文件、但**显式说明理由** | **1** | 旧 `## 八、术语小表`：说明件 L106 记为「**删除**，理由 = 新结构把每个词的解释放在第一次出现的正文里；六词小结表在 `02` 末尾，完整清单在本件 §八」⇒ **理由成立**（我复核：`02` §小结确有六词表；tick/事件在 `03` 就地解释） |
| **静默丢弃** | **0** | ✅ |

**新增（旧件没有的）**：`04`（端到端教程）· `README` 的「推荐阅读顺序」「读不懂回到哪一份」「这组文档不写什么」· `01` 的「它在服务端里处在什么位置」「与配套插件的依赖关系」—— 与说明件 §五 的"新增"节一致 ✓。
⇒ **A8 通过（无静默丢弃）**。

---

## §10 A9 / A10 / A11

- **A9**：`Test-Path docs\插件文档\初学者指南.md` = **False**；文件夹文件数 = **5**（两侧计数 **5 / 5**）⇒ 未并存 ✓。
- **A10**：
  - `git diff --numstat 90e6c06 -- docs/插件文档` ⇒ `1 0 README.md` + 5 个新文件（96/142/244/368/56 行插入）；**另 3 份手册完全缺席**（0 改动）✓。
  - 口径说明：`90e6c06` **还没有**这份旧单文件（它在 `08fe741` 才出现），所以"旧件删除"不出现在这条 diff 里；在**入库提交** `a8aeaaf` 的 `--numstat` 中可见：`1 1 README.md`（文档地图 1 行改指文件夹）+ `0 439 初学者指南.md`（删除）+ 5 个新文件 ⇒ 与 t14 自述逐条一致 ✓。
- **A11**：`git grep -rn --untracked --no-exclude-standard -F "debug-logs" -- "docs/插件文档/初学者指南"` ⇒ **0 命中** ✓。

---

## §11 A12 · 初学者可读性抽样（只从这 5 份出发，10 题）

| # | 初学者会问 | 出处（文件 + 章节） | 能否答出 | 缺口 |
|---|---|---|---|---|
| ① | 这个插件是干什么的 | `01` §「这个插件在游戏里做了什么」（四步 + 一句话总结） | **能** | — |
| ② | 我该先读哪份 | `README` §「推荐阅读顺序」（4 行表 + 三种目标路径） | **能** | — |
| ③ | 类和方法的区别 | `02` §一 vs §三（模板+造物机 / 就是函数、点号调用） | **能** | — |
| ④ | 为什么改完要重新构建 | `03` §常见问题 Q1（忘了构建 / 服务端跑旧的 / jar 不会自动拷） | **能** | — |
| ⑤ | 我怎么加一个自己的角色 | `04` §「你要做的事，一共五步」+ 第 1–2 步逐句表 + 第 5 步验收现象 | **能** | — |
| ⑥ | 槽位冲突会怎样 | `04` §「这些规则由代码强制检查」+ §「起服时日志里可能出现的报错」的 `Slot 1 is already occupied by '...'` | **能** | — |
| ⑦ | 报错去哪儿看 | `04` §「起服时日志里可能出现的报错」（`run/logs/latest.log`）+ `03` §第 2 步（构建阶段报错带文件名与行号） | **能** | — |
| ⑧ | 改坏了怎么退回 | `03` §第 5 步（改回原值 / 副本盖回 / `git checkout --`）+ `04` §改坏了怎么退回（三种，从省事到彻底） | **能** | — |
| ⑨ | 图标变灰 / 变红 / ENERGY LACK 是什么意思 | `03` §常见问题 Q2/Q3/Q4 | **能** | — |
| ⑩ | 掉线后角色为什么没了 / 物品为什么丢不掉 | `03` §常见问题 Q6/Q7 | **能** | — |

⇒ **10/10 可答出**（`t13` 时"改坏了怎么退回"的缺口已由 `03` 第 5 步的三种方法与 `04` 的三种方法补上 ⇒ t13 **F-5 闭合**）。

---

## §12 A13 · 制品代次未受影响

| 时点 | bytes | mtime | SHA256 |
|---|---|---|---|
| 认领后第一次 | 159512 | `2026-09-19 12:54:07.286` | `6A249993ECDE32C9AAB4E651191C5277C198F97DB5C35A603FC68893C67FF734` |
| `javac` 之前 | 同上 | 同上 | 同上 |
| `javac` 之后 | 同上 | 同上 | 同上 |
| 结卡前最后一次 | 同上 | 同上 | 同上 |

⇒ **四次三列全同** ⇒ 本卡**未重建 jar**（本卡也**未**执行任何构建/起服；唯一运行级动作是仓库外的一次 `javac`，不触碰 `build/`）✓。

---

## §13 Findings（分级 + requiredFix）

### F-1 · medium · `03` 的"其余代码与源文件**逐字相同**"与源文件有 **4 处**不符

- **问题**：`03` L49 对该页的三段节选自述：「原文件在类声明上方还有一段说明文字（本页不引用它），**其余代码与源文件逐字相同，可以逐行对照**」。我按段逐行对拍（源文件 = `src/main/java/com/shadowHunterRolesPlugin/roleComponent/meiqiHezi/skill/MeiqiheziUnconcernSkill.java`，39 行）：
  1. **段落一 · 包名大小写**：doc `package …roleComponent.meiqiHeZi.skill;` vs 源 `package …roleComponent.meiqiHezi.skill;`（**源文件包名是小写 `z`**）；
  2. **段落三 · 省略了 javadoc**：源 L29-36（`/** 批次②（B②）迁移：… */`）在 doc 里没有 —— 而免责句只声明"类声明**上方**还有一段说明文字"，未覆盖方法上方的 javadoc；
  3. **段落三 · 省略了行内注释**：源 L42 `//药水记账（O-7）：…` 没有；
  4. **段落三 · 代码行尾注释被截断**：源 L54 `svc().cooldowns().start(getCooldownTicks());   //D1：组件自启冷却（框架不再代启动）` vs doc L109 同名调用**无注释**。
  前置申明：**段落二（构造函数 11 行）逐行差异 = 0** ✓；第三段的**代码语句本身**（除注释）与源一致 ✓。
- **影响**：`03` 是"逐行读代码"那一课，明确邀请读者去 diff；4 处不符会让人以为是自己看错了；其中①是**包名**，直接影响"这个文件住在哪个文件夹"的认知（且读者抄去用会在大小写敏感的系统上出问题）。
- **requiredFix（二选一，推荐 ①）**：① 把 L49 的免责句改为**如实口径**：「下面三段与该文件**代码语句**一致；为便于阅读**省略了源文件里的注释**（javadoc 与行内注释），**包名以源文件为准**（本仓库里该目录名与包名的大小写不一致，见下）」；② 或者把三段改成**逐字全文节选**（连注释一起引）。
- **附带登记（src 侧既存瑕疵，本卡不改）**：**F-7** —— 源文件 L1 的包名 `…meiqiHezi…` 与它所在目录 `meiqiHeZi` 大小写不一致（Windows 不敏感故能跑）。`src/**` 不在本卡 inScope ⇒ 只登记，建议另立卡或在开发指南里点一句。

### F-2 · low · `03` L49 的另一句自述"**每段的末尾都写了它在原文件里的位置**"未兑现

- **问题**：三段结束后各是一串逐行解释的 bullet，**没有任何一段给出"在原文件里的位置"**（行号或符号锚）；读者无法按自述去定位。
- **requiredFix**：每段末尾补一行锚（如"源文件 L1–L2 / L17–L27 / L37–L56"），或删掉这半句。

### F-3 · low · `04` L216 的 `Material.IRON_INGOT` 解释里残留"**这里先填纸**"

- **问题**：`04` L216 = 「`.icon(Material.IRON_INGOT)` | 角色的图标（别的插件可以读它；**这里先填纸**）」——"纸"是 t14 把图标从 `Material.PAPER` 改成 `IRON_INGOT` 时的残留（t14 说明件已记录这次自查修正），而正文与目标描述（L8"图标是一个铁锭"）都是**铁锭** ⇒ 自相矛盾。
- **requiredFix**：把"这里先填纸"改成"这里填的是铁锭；换成别的材质也行"。

### F-4 · low · `04` L37 把现有组件目录写成 `roleComponent/meiqiHezi/`（实际 `meiqiHeZi`），且与同文件 L307 自相矛盾

- **问题**：磁盘上 `src/main/java/com/shadowHunterRolesPlugin/roleComponent/` 下只有 `meiqiHezi`（**大写 Z**）与 `red`；`04` L37 写 `roleComponent/meiqiHezi/`（小写 z）而 `04` L307 写 `roleComponent/meiqiHeZi/mainWeapon/…`（大写 Z）⇒ 同一份文档两种写法，Windows 能跑、Linux 会断。
- **requiredFix**：统一为**磁盘实际名** `meiqiHeZi`（与 `03` 的路径写法一致），或统一为包名并在旁边注明"源码里包名与目录大小写不一致"（与 F-1 的修法合并处理）。

### F-5 · low · `03` L14 的 `runRepeating` 仍是"多行调用的合并重排版"（上一轮 F-2 仅部分闭合）

- **问题**：`03` L14 写作一行 `updateTask = platform.scheduler().runRepeating(this::triggerUpdate, 1L, 1L);`，而源码是**四行**（`RoleInstance.java:124-128`）。t14 新增的"节选说明"（`03` L49）**只覆盖技能文件那三段**，不覆盖这一行 ⇒ 读者照抄"那一行"去 `git grep` 会查不到（我在本卡与 `t13` 都因此先判 MISSING）。
- **requiredFix**：在该片段下补一句"（源码里这次调用写成了四行，这里合并成一行便于阅读）"，或改成逐字多行。

### F-6 · low · 术语必要性：**非必需却出现 5 项**，其中 1 项建议删（术语过载）

- **问题**（队长① 口径②）：26 个术语里，5 项对"写出一个角色"不是必需：`abstract`、事件/`@EventHandler`、`UUID`/`Map`、排错引用的类名（`RoleManager`/`PlayerListener`/`SkillListener`/`HotbarRenderer`）、**`ComponentFactory`**。前 4 项**服务于理解或排错**（解释"模板限制""谁调用 onCast""为何现取玩家""去哪儿看"），风险低；**`ComponentFactory`（`04` L121）是唯一"读者既不会写也不会引用"的框架类名**。
- **结论数**：术语总数 **26** · 非必需 **5** · 未解释 **0**。
- **requiredFix（可选，非阻塞）**：删掉 `04` L121 里"（框架那边干这件事的地方叫 `ComponentFactory`…）"半句，或保留但移到"想更深入时再看"一节；其余 4 项建议保留（它们有教学价值）。

---

## §14 判据缺陷（`criterion defect`）单列 —— 卡面 `verify` 的三处结构性问题

| # | 卡面命令 | 症状（实测） | 更正后的姿势（本卡采用） |
|---|---|---|---|
| **CD-1** | `verify(2)`/`verify(4)`：`git show <rev>:<path> > $env:TEMP\old-arch.md` | PowerShell 5.1 的 `>` 写 **UTF-16LE** ⇒ 落盘件 **72804 B**（首 4 字节 `255,254,35,0`），blob 是 **53075 B** ⇒ 不能拿它做字节/哈希/行数比对 | `cmd /c "git cat-file blob <sha> > <file>"` 落盘 + `git hash-object` 自证（= blob 名）；或**不落盘**，直接 `git grep -f <模式> <rev> -- <仓库内路径>` |
| **CD-2** | `verify(4)`：`git grep -f <模式文件> -- <落盘的旧样本>` | `git grep` 只搜**仓库内被跟踪**文件 ⇒ 对 `$env:TEMP\…` **恒返 0（假绿）** | 对落盘样本用 `--no-index`（本卡 §3.1 姿势②），或按 CD-1 的"不落盘"姿势；两种姿势都必须配**已知含痕迹的正对照**（本卡用旧侧 8 条非 0） |
| **CD-3** | `acceptance A8`：`git show 90e6c06:docs/插件文档/初学者指南.md` | 该 revision **里没有这个文件**：`git cat-file -e 90e6c06:docs/插件文档/初学者指南.md` ⇒ **exit 128**（它在 `08fe741` 才被引入、在 `a8aeaaf` 被删除） | 用 **`08fe741`**（引入提交）取旧件；删除面用 **`a8aeaaf`** 的 `--numstat`（`0 439`） |

**并行说明**：CD-1/CD-2 是**命令形态**缺陷（队长已在任务里预告）；CD-3 是**revision 写错**。三条都**不得**被读成"被复核对象不成立"——我按更正姿势重跑，结论照常成立。

---

## §15 A14 · 静态级 / 运行级分开写 + 未覆盖项

### §15.1 静态级 vs 运行级

- **静态级（本卡绝大多数）**：痕迹检索、术语逐词判读、`javap` 反汇编、源码规则/报错串比对、内容映射、文档 diff/blob、jar 身份。
- **运行级（本卡仅 1 项）**：**仓库外的一次 `javac` 编译**（§8：exit 0 + `.class` 产物 + 负对照 exit 1）。
- **未做运行级**：未打包进插件 jar、未起服、未进游戏验证图标/按键/回显（`04` 第 3–5 步的实际效果）。

### §15.2 未覆盖项（逐条给最小闭合方式）

1. **未由真人在真机从零照做一遍**（`04` 的"照着做"体验）⇒ 最小闭合：找一位真·初学者按 `README` 的顺序走完 `01→04`，记录卡点与耗时。
2. **阅读顺序是否最优未经测试**（`README` 给的三条路径）⇒ 最小闭合：同上，做 A/B（先 `02` 后 `01` vs 按现顺序）。
3. **`04` 的运行级效果未验证**（铁锭图标、3 秒倒数、回 2 点血、`/role set myrole` 回显）⇒ 最小闭合：把 §8 编译通过的类 + `RoleLoader` 三处改动落到 src（**另立卡**，因为这要改 src），起服后在 25566 上按第 5 步验收。
4. **`04` 的"第 6 步（可选）"未编译验证**（主武器/被动骨架它只给了两句 `.addMainWeapon/.addPassive` 与构造顺序说明，没给完整类）⇒ 最小闭合：照 `§8` 的方式各写一个最小类编译一次。
5. **编译深度只到 `.class`**：未做 jar 打包/重载/`plugin.yml` 影响面检查 ⇒ 最小闭合：同上第 3 条。
6. **`localhost:25566` 可达性未核**（需起服）⇒ 最小闭合同上。
7. **术语扫描的完整性**：我的清单来自"5 份全文逐行读 + 前置假设清单 + 20 个禁用词探针"，不是机械穷举（例如"构建""缓存""命令行"这类通用词未入表）⇒ 最小闭合：把 JDK 关键字表 + 常用 CS 词表当模式集机械扫一遍，人工判读差集。
8. **未复核 `02`/`03` 里"真实例子"的逐字保真**（我核了 `03` 的三段；未逐条核 `02` 的六个例子与 `01` 的两段引用）⇒ 最小闭合：对 `02`/`01` 的每个代码块做与 §9 同样的逐行对拍。

---

## §16 A15 · 边界声明 + 结卡自查

**边界（A15）**
- **未改 `src/**`**：`git status --porcelain -- src` = **0 行**。
- **未改被复核的 5 份文档**：`docs/插件文档/**` 一个字节都没动（本卡只读）。
- **未改既有证据与冻结件**：未触碰 `debug-logs/测试记录/` 下任何既有文件（含卡面 Out-of-scope 点名的 `阶段5-初学者指南-独立复核报告.md`）、未碰 `*.log`、未碰 `debug-logs/回滚快照/**`（只**复制**归档件到 ASCII 路径用于 `javap`，源件未动）。
- **零 git 写**：未执行 `add`/`commit`/`checkout`/`restore`/`stash`/`reset`；结卡时全仓 `git status --porcelain` = **0 行**。
- **唯一写盘** = 本报告 + 被忽略的 `.scratch/t15/`（仪器与旧侧样本）+ **仓库外**的 `%TEMP%\t15c\`（编译实验）；三者均已声明。**未申请提权**、**未起服**、**未构建仓库**（唯一运行级动作 = 仓库外 `javac`）。

**结卡自查**
- [x] A1–A15 逐条给证据（§0 表 + §3–§16）
- [x] 判据自检**两侧计数都写进报告**（§3.2 五列），旧侧**两种姿势**同值
- [x] 术语：**逐词表 + 三个数**（26 / 5 / 0）+ 四条例定口径逐条结论（§4）
- [x] 上一轮 findings **逐条复测**（§5：6/6 闭合；F-2 部分闭合 ⇒ F-5）
- [x] `javap` 逐符号（§6：0 条查无实据）；**工具可调用性用 `--help`**（该 JDK 无 `--version`）
- [x] 可照做性**实跑**并**如实标注深度**（§8：编译级；运行级未做）+ 负对照
- [x] 内容不丢失**逐个标题**核对（§9：30 + 1 显式理由 + 0 静默丢弃）
- [x] **判据缺陷单列**（§14：CD-1/2/3）且**未以改判据回避**；`jar` 四次现算三列全同（§12）
- [x] 静态级/运行级分开写（§15.1）；未覆盖 8 条各给最小闭合（§15.2）
- [x] 身份现算；**不内嵌本件自身哈希**；引"代"点名 commit（`90e6c06` / `08fe741` / `a8aeaaf` / `0c6930d` + blob 名）
- [x] 零 git 写 / 未改 src / 未改被复核文档（§16）
