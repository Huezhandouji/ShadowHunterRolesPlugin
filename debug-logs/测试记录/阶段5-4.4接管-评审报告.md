# 阶段 5 · 4.4 统一渲染器接管批 —— **对抗式评审报告**（t6）

> **被评审对象**：`t2`（实现）· 其独立复核 `t4`（`eng-验证`）· 其外观取证 `t5`（`eng-取证`）· 判据集 `t1`（`eng-规范`）
> **评审者**：`eng-审阅`（reviewer）· **本卡 = `t6`（review，round 1）** · 卡面节选见 §13
> **评审立场**：**不复述验证结论**。目标是找「**构建通过 + Mode B 三行 + 集合差 `C=0` + 双口径**」四盏绿灯**都发现不了**的缺陷（本工程已四次「绿灯全过但语义错」）。
> **口径**：身份一律 **bytes + SHA256（现算）**；行数注明 `ReadAllLines`；行号只作**定位**（移动靶 ✗）；引"代"一律点名 `commit` / `tree` / jar 身份（**禁**"HEAD 的 tree"）；本件**不内嵌自身哈希**（结卡报文现算）。
> **本件所有读数 = 我在 2026-09-19 13:3x–13:5x 的现算**，命令与原始输出逐条内联（§14 附批量清单）。

---

## §0 结论（verdict + 分级计数）

- **verdict = `pass`**（对本批：`t2` 的实现 + `t4`/`t5` 的证据体系）
- **分级计数：blocker 0 · high 0 · medium 3 · low 4**（逐条见 §11）
- **一句话**：4.4 的**功能面**——唯一渲染者（渲染写点 = 1 且落在 `core/hotbar`）、五触点置脏（直呼 5 + 方法引用 1）、帧末 flush 三段有序且 `clearDirty/isDirty` 各 1 消费者、T⑦ 四条旧方法与两条 legacy 刷新方法**同批归零**、冻结差异（技能冷却名带 `x.xs`、主武器不带）在**运行级逐维复现**、三条红线 + R-5/R-7 全绿、**未重建被验证代次**且 F1 链自洽——**我逐条独立复算通过，未发现语义错误**。
- **发现的缺陷全部落在"判据 / 证据口径 / 活文档现状"三处**（不是代码）：① 活文档仍以"渲染器=骨架、刷新仍由 `updateHotbar()` 承担"为**现状**（照它写代码会写出失效实现）⇒ **medium F-1**；② `t1` 的**归零组自检样本在 after 态自身归零**（C-03 / C-09 / C-10 三处），且 `t4` 的 F-2 **枚举不全** ⇒ **medium F-2**；③ 本批"集合差 `C=0`"的证据只覆盖**两个开机窗**（16 s / 15 s），唯一进入 gameplay 的 `t5` 轮在同一锚+同一仪器下 **`C = 1`**（= 那条插件归因 WARN）⇒ **medium F-3**。
- **队长裁定项的落点**：插件归因 WARN = **预先存在、非本批引入、非 blocker**，**记开放项**并另立卡（**F-4 low**）；其"扫描盲区"结论我**同意并加实证**（F-5 low，含我实测的**假绿形态**与双向自证数据）。
- **无一条 finding 要求改 `src` 语义**：F-1 的 `src` 侧两条注释行属"**注释口径回收**"，按本工程纪律**不得夹带进已冻结代次**（见 §11 F-1 的 requiredFix）。

---

## §1 边界与身份（A14）

**我只做三件事**：读（现算）、复算、写本件。逐条：

| 项 | 现算 |
|---|---|
| 本卡写入面 | **仅** `debug-logs/测试记录/阶段5-4.4接管-评审报告.md`（本件） |
| `src` | **未改**：评审开工时 `git status --porcelain -- src` = **0 行**；边界声明时 = **0 行** |
| 既有证据 / 冻结件 | **未改**（`*.log`、t1/t2/t3/t4/t5 的交付件、`debug-logs/回滚快照/`、`docs/` 一律只读） |
| git 写 | **零**：未执行 `add` / `commit` / `checkout` / `restore` / `stash` / `reset`（全仓 `porcelain` 开工/结卡均 **0 行**） |
| 提权 | **未申请**（全程零申请） |
| 起服 / 构建 / 重建 | **未起服、未构建、未重建**（不占 25566；不改 jar mtime） |
| 评审开工时 HEAD | `c59ede3dade994ebd03c4d5265d779a9661b4172`（porcelain 全仓 0 行） |
| 评审期间 HEAD 前移 **两次**（均为**卡外**提交，我未参与） | `713d1c7`（= **`t5` 四件 + 开放项留档**，`--stat` 5 files / 1075 插入）→ **`901f53ac40d135d9cba67af6e88a2d0ff1b6d5cc`**（= `口径澄清-F1链的项序列.md` 追加，6 插入 / 1 删除）；结卡前现算 porcelain 全仓 **0 行**、`-- src` **0 行** |
| 被验证的「代」 | **不是 "HEAD 的 tree"**；= `src/main` tree **`a6a33c9b85a82ea25b15f8417e9dc880c588ecdb`**（`b062252` / `11bb792` / `c59ede3` / `713d1c7` / `HEAD` **五处同值**，我逐条 `git rev-parse <commit>:src/main` 现算）+ jar 身份（§6） |

> **口径提示（给后人）**：HEAD 在本卡开始到结束之间前移了 **两次**（c59ede3 → 713d1c7 → 901f53ac）。若把结论绑到 "HEAD"，两次都会自相矛盾；绑到 **tree `a6a33c9b…` + jar `6A249993…`** 则全程稳定。

---

## §2 必查①：判据区分力（A3）—— 逐条结论 + 我自己的自检

> **判定原则**：一条判据只在"**返回 0 与"路径没落到文件"可区分**"时才有区分力；`t1` 自己的负对照 = `git grep -F 'stateOf' -- …/core/hotba` ⇒ **0 行 · exit 1**（末段不是文件 ⇒ 假 0）。我按同一纪律对 **16 条判据 + §12** 逐条复算，并把每条判据**自带的**已知有命中样本放到 **after 态**再跑一遍（**这是 `t1` 与 `t4` 都没有做全的一步**——见 F-2）。

### 2.1 逐条复算（我的读数 vs `t1` 冻结期望）

| 判据 | 模式 + 定域 + 口径（三件齐） | `t1` 期望 after | **我现算** | 判定 |
|---|---|---|---|---|
| C-01 | ERE `\bmarkDirty\(\);`，逐文件，仅调用点 | RI 3 · CP 1 · BM 1（宿主 = `removeBuff`） | **RI 3**（:202/:219/:537）· **CP 1**（:53）· **BM 1**（:109，宿主 = `removeBuff`；`addBuff` 体内 0） | ✅ |
| C-02 | ERE `\bmarkDirty\(\);`，`src/main/java/**`，仅调用点 | **5** | **5**（CP:53 · RI:202/:219/:537 · BM:109）；裸串 `-F 'markDirty'` = **9** | ✅ |
| C-03 | ERE `::markDirty\b\|\bmarkDirty,`，全 `src/main/java`，行级含注释 | **1** | **1**（RI:87 `hotbarRenderer::markDirty`） | ✅ |
| C-04 | ERE `isDirty\(\)\|clearDirty\(\)` + 单文件定义行；`read` 判同方法体有序 | `triggerUpdate` 尾 `isDirty → render → clearDirty` 有序 | **RI:736 `if(hotbarRenderer.isDirty() \|\| hasCoolingSkill()){` → :737 `hotbarRenderer.render();` → :738 `hotbarRenderer.clearDirty();`**，同方法体（`triggerUpdate` 定义 :719）、顺序正确 | ✅ |
| C-05 | ①`-F` 裸计数 ②ERE `\.clearDirty\(\);`（带接收者 + `();`） | 裸 **2** / 调用 **1** | **2**（RI:738 调用 + HR:62 定义）/ 调用 **1** | ✅ |
| C-06 | ①`-F` 裸计数 ②ERE `\.isDirty\(\)`（不要求 `;`） | 裸 **2** / 调用 **1** | **2**（RI:736 + HR:57 定义）/ 调用 **1** | ✅ |
| C-07 | `-F 'setItem('`（**带左括号**）①全树 ②清空写 ③`core/hotbar/` ④`setItemMeta(` | ①3 ②2 ⇒ 写点 **1**，③ **1**；④允许 | ① **3**（RI:478/:489 清空 + HR:81 渲染）· ② **2** · ⇒ **写点 = 1** · ③ **1** · ④ **9**（不计入）；另 `setItemInMainHand(`/`setContents(`/`addItem(`/`getInventory().setItem` = **0/0/0/0** | ✅ |
| C-08 | `-F` 三串签名限定；归零组 | **0 / 0 / 0** | **0 / 0 / 0**（`createIconItem` · `getDisplayName(RoleInstance` · `getDisplayName(this)` 各 exit 1） | ✅ |
| C-09 | ①`-F 'updateHotbar()'` ②ERE `\bupdateHotbar\(\);` ③定义行 ④`shouldUpdateHotbar` | **0 / 0 / 0 / 0** | **0 / 0 / 0 / 0**（连**裸词** `updateHotbar` 亦 0） | ✅ |
| C-10 | ERE 两符号分开数 + `-F` 裸词 | 结构口径 **0 / 0** | **0**（`\bupdateSkillItemMeta\|\bupdateAllSkillItemMeta` exit 1）· 裸词 **0** | ✅ |
| C-11 | ERE 四串择一，`roleComponent/**`（目录前缀），含注释 | **0** | **0**（自检样本同 pathspec 命中 **1** ⇒ 非假 0） | ✅ |
| C-12 | ERE record 组件声明行，单文件，行级 | **10** | **10**（`ComponentServices.java:11–:20`，`git grep -c` 输出即逐行可读） | ✅ |
| C-13 | 三条命令，`api/**` | 两条空 + `ls-files` = 2 | `status --porcelain` **0** · `diff --name-only 42dde5e5…` **0** · `ls-files` **2** | ✅ |
| C-14 | 本批冻结日志：Mode B 三行 / 集合差 / 级别字段 / F5 物证 | 全部成立 | **Mode B 三行**：t2 轮 `12:57:11 / 12:57:11 / 12:57:12`、t4 轮 `13:09:03 / 13:09:04 / 13:09:04`、t5 轮 `13:20:09 / 13:20:10 / 13:20:10` **各 1 行**；集合差：**我自己复现了仪器算法** ⇒ t2 轮 `C=0`、t4 轮 `C=0`、**t5 轮 `C=1`**（见 §9.3 / F-3）；级别字段 `\] \[[^]]*/(ERROR\|SEVERE)\]` = **0/0/0** | ⚠️ **部分**（C=0 的覆盖有洞 ⇒ F-3） |
| C-15 | 外观级：可见属性（非 Component 文本树） | 逐格一致 | **我独立重算转录**（不看 t5 与自己探针的自报）：`[appearance]` **31** 行；`SKILL+COOLDOWN+suffix=/^[0-9]+\.[0-9]+s$/` = **7** · `MAIN_WEAPON+COOLDOWN+suffix=-` = **2** · `MAIN_WEAPON+COOLDOWN+suffix=x.xs` = **0** · `SKILL+COOLDOWN+suffix=-` = **0** · `state≠derived` = **0**；decay 三点 **29.3 → 19.3 → 9.3**（严格递减）；PDC 读回 `skill_id` **19** 行 / `main_weapon_id` **8** 行 | ✅ |
| C-16 | 三时点 `Length`/`LastWriteTimeUtc`/`SHA256` | 本批只一代 · 不重建 | **归档件 = build/libs 逐字节相同**（159512 B / 12:54:07.286 / `6A249993…`）；5 个旧归档 mtime **仍为 09-18**；`build/classes` 最新 `.class` = **12:54:06.790 < jar 12:54:07.286** | ✅ |
| §12 | A14：`--untracked --no-exclude-standard` + 已知命中串样本 | 样本 ≥1 行 | 命中串 `markDirty` = **47 行**（exit 0） | ✅（但负对照已漂移 ⇒ F-6） |

### 2.2 点名：有没有真空通过 / 假绿灯 / pathspec 未落到文件 / 裸词当锚串

- **真空通过**：**未发现**。16 条判据的每条期望值都在**非空命中**与**归零**之间可区分——除 F-2 的 3 处自检样本（**下一节**）。
- **glob/pathspec 末段未落到文件**：**未发现**。`t1` 的负对照（`core/hotba` ⇒ 0）我复现口径一致；`C-11` 的目录 pathspec 有**已知有命中样本**（`BuffType.` @ `roleComponent` ⇒ **我现算 1 行**，与 `t1` 声明逐字相符）。
- **裸词当锚串**：**本批未发现被咬**。我逐条核了三族：
  - `setItem` ⊂ `setItemMeta`：C-07 全程带 `\(` ⇒ **after 态**裸词 `-F 'setItem'` = **13 行** = `setItem(` **3** + `setItemMeta(` **9** + `HotbarRenderer:26` javadoc「空闲 tick 零 setItem」**1**（**算术闭合**；before 态同式为 16 = 4 + 11 + 1）⇒ 若不带 `\(`，会把 9 行元数据写入误当"写物品"。
  - `cooling=` ⊂ `wasCooling=`：本批无 `cooling=` 锚；级别扫描用 `\] \[[^]]*/(ERROR|SEVERE)\]` **带级别字段**，我实测裸 `ERROR|SEVERE` 会在**每条 oshi WARN 正文**命中（"Error was …"）⇒ 锚定级别字段是必要的。
  - `[sched]` ⊂ 裸 `sched`：与本批无交集（`DebugSchedCommand` 未动）。
  - **大小写**：本卡所有计数命令均 `git grep` 默认**大小写敏感**；我未使用 `-i`；PowerShell 侧一律用 `ReadAllLines`+`-match`（`-match` 默认**不区分大小写**，故我在扫描插件归因时**显式区分**了模式形态——见 F-5，那里正是被咬的现场）。
- **新增发现（F-2）**：`t1` §10「自检样本汇总」里，**C-03 / C-09 / C-10 三条的样本在 after 态自身归零**（因为样本符号就是本批删掉的符号）⇒ 这三条判据在 **after 态没有任何"已知有命中"样本**，"0 命中"与"路径没落到文件"**不可区分**。我给出替代样本（下节），并指出 `t4` 的 F-2 只枚举了 C-03 一处、C-08 一处且对 C-08 的定性不准。

### 2.3 我对每条判据的**自检结果**（after 态，我自己的命令与输出）

| 判据 | `t1` §10 样本 | after 态我现算 | 区分力 |
|---|---|---|---|
| C-01 | `-F 'addBuff'` @ BuffManager | **2**（:35 定义 + :108 注释） | ✅ 仍有命中 |
| C-02 | `-E '\bmarkDirty'` @ HotbarRenderer | **3**（javadoc :26 / 注释 :30 / 定义 :53） | ✅ |
| C-03 | `-E '::updateHotbar\b'` @ src | **0**（exit 1） | ❌ **归零** ⇒ F-2。**替代样本（我现算）**：`-F 'this::triggerUpdate'` = **1**；`-F 'triggerUpdate'` = **4 行** |
| C-04 | `-E '\bisDirty\(\)'` @ HotbarRenderer | **1**（定义 :57） | ✅ |
| C-05 | `-E 'Dirty\(\)'` @ HotbarRenderer | **4**（:26 javadoc / :53 / :57 / :62） | ✅（`t1` 记 before = 6，含已删的 2 条 javadoc 提及） |
| C-06 | `-F 'isDirty'` @ src | **2** | ✅ |
| C-07 | `-F 'setItemMeta('` @ src | **9** | ✅ |
| C-08 | `-F 'getDisplayName('` @ src | **15** | ✅ 仍有命中（**但** `t1` 记的"算术闭合 25 = 22+2+1"已不成立 ⇒ 见 F-2 对 `t4` 定性的更正） |
| C-09 | `-F 'updateHotbar'` @ RoleInstance | **0**（exit 1） | ❌ **归零** ⇒ F-2。替代：`-F 'hasCoolingSkill'` = **2**；`-F 'hotbarItemOf'` = **2** |
| C-10 | `-F 'updateSkillItemMeta'` @ src | **0**（exit 1） | ❌ **归零** ⇒ F-2。替代：`-F 'hasCoolingSkill'` = **2**；`-F 'render()'` @ RoleInstance = **2** |
| C-11 | `-F 'BuffType.'` @ roleComponent | **1**（`DefaultSanTEZeroPunishment.java:84`） | ✅ **与 `t1` 声明的值逐字相符** |
| C-12 | 白名单命令自身 | **10** | ✅ 非空 |
| C-13 | `ls-files -- …/api` | **2** | ✅ |
| C-14 | 锚 `阶段4-起服日志-T2b.log` | **存在**（13 条级别行，逐条可读） | ✅ |
| C-16 | 归档 jar 三时点 | 三时点**逐字一致** | ✅ |
| §12 | `--untracked … 'markDirty' -- <t1 件>` | **47** 行 | ✅；**负对照已漂移 ⇒ F-6** |

---

## §3 必查②：「实现者 = 0 的注释声称」（A4）

> 本工程先例：`HotbarRenderer` 的 javadoc 曾声称"五类事件由**端口适配器**置脏"，实测为 0。
> **判定规则**：javadoc / 注释**不得声称尚未接线的行为**。

**逐条核对（`HotbarRenderer` / `RoleInstance` / `CooldownPortImpl` / `BuffManager` / `Skill` / `MainWeapon` 的全部行为性声称）**：

| # | 声称（文件 : 行 : 片段） | 实现者（我现算） | 判定 |
|---|---|---|---|
| 1 | `HotbarRenderer:26`「空闲 tick 零 setItem：只有 `markDirty()` 被调用后，帧末 flush 才会写物品（冷却中的技能例外…）」 | flush 入口 `RI:736` 判 `isDirty()`；`render()` 仅 2 个调用点（构造器 + flush） | ✅ 属实（**唯一措辞偏差**：冷却中实际是**整栏**重写而非只写该技能槽——已在 `t2` 说明 §6.2/§9(c) **逐条申报**，故不计为"未接线的声称"；建议随 F-1 顺手锐化措辞） |
| 2 | `HotbarRenderer:28`「刷新触发：**施放 / 冷却启动 / 冷却到点 / 能量变化 / buff 移除**五类事件**都必须置脏**」 | 逐类点数：施放 = `RI:202`/`:219`；冷却启动 = `CP:53`；**冷却到点 = `RI:270`/`:337`（`runLater(markHotbarDirty,…)`）+ `scanCooldowns` :399（`markHotbarDirty.run()`）**；能量 = `RI:537`；buff 移除 = `BM:109`。**五类各有实现者，无 0 者** | ✅ **先例缺陷已修好**（**且**新 javadoc 已**不再**声称"由端口适配器置脏"——只有触点②真在端口里，措辞与实现一致） |
| 3 | `HotbarRenderer:30`「组件**不参与**渲染：没有 `HotbarPort`，也没有组件可调用的 `markDirty`」 | `roleComponent/**` 四串扫描 = **0**；`HotbarPort` 全树仅此注释一处提及（无接口/无实现） | ✅ |
| 4 | `HotbarRenderer:43`「初值 = true ⇒ 即便无人置脏，首位 flush 也会完成首刷（构造器另有一次同步首刷）」 | 字段 `dirty = true`（:44）+ 构造器末 `hotbarRenderer.render()`（`RI:132`） | ✅ |
| 5 | `HotbarRenderer:61`「`clearDirty()` 唯一消费者 = flush 尾部」 | 调用点 **1**（`RI:738`） | ✅ |
| 6 | `HotbarRenderer:68`「只对**已注册**的组件生效（未知 id 跳过）——与旧实现查表顺序一致（**主武器优先**）」 | `hotbarItemOf` 先 `mainWeaponMap` 后 `skillMap`（`RI:465-469`）；旧 `updateHotbar` 同序（我在 `42dde5e5` 的 diff 里逐行核过） | ✅ |
| 7 | `HotbarRenderer:88/171/178`「主武器 `energyCost ≡ 0` ⇒ **永不进入** `ENERGY_LACK`」 | `MainWeapon:32` 第 6 位**恒传 `0`**（全部武器子类都经此构造器）· `setCurrentEnergy` `Math.clamp(amount, 0, …)`（`RI:535`）· 判定 `energy < energyCost` ⇒ `energy < 0` 永假 ⇒ **结构上不可达** | ✅（`t5` 另有旁证 + **禁用形状的注入缺陷 n7 被探针抓成 MISMATCH**） |
| 8 | `RI:87`「**唯一的方法引用持有者**：供三条程序化刷新路径共用（冷却到点 / 每 tick 到期扫描 / 显式结束冷却 S3）」 | 三条全在：`:270`/`:337`（`runLater`）· `:399` · `:377` | ✅ |
| 9 | `RI:130`「构造期同步首刷（与迁移前的可见时机逐字一致 = 零延迟）」 | 旧 `42dde5e5` 的构造器末为 `updateHotbar();`，新为 `hotbarRenderer.render();` —— **同一位置**（我 diff 核过） | ✅ |
| 10 | `RI:773-774`「旧的两条每 tick 轮询判定已删——其中一条是恒假死路径，另一条的语义（B-2）并入帧末 flush 入口条件」 | `shouldUpdateHotbar` / `shouldUpdateItemMeta` 全树 **0**；`hasCoolingSkill` = flush 入口（:736/:747） | ✅ |
| 11 | `CP:52`「触点②：端口适配器内置脏——**一个落点覆盖技能与主武器两 kind**」 | `start()` 内 `if(kind==SKILL)…else…` **之后**同一行置脏（:53） | ✅ |
| 12 | `BM:108`「触点⑤（buff 移除）：置脏 + 帧末 flush（`addBuff` **保持不置脏**）」 | 文件内 `hotbarRenderer\|markDirty\|updateHotbar` = **1**（:109，宿主 `removeBuff`） | ✅ |
| 13 | `Skill:32` / `MainWeapon:41-42`「物品构建已上移到统一渲染器：本类不再持有任何渲染入口（T⑦ 收口）」+「主武器冷却名不带秒数…由渲染器的主武器分支保持」 | `createIconItem`/`getDisplayName(RoleInstance)` = 0/0；`%.1f` = **1** 且在 `HR:136` 技能分支 | ✅ |

**结论**：**「实现者 = 0 的注释声称」在本批 = 0 条**（对比先例，这是**修好了**的一条）。**但**发现**反方向**同族问题 2 条——**注释声称的是"已经不存在"的旧现状**（不是"尚未接线的行为"，而是"尚未回收的旧口径"）⇒ 归入 §7 / F-1。

---

## §4 必查③：冻结的可见差异与 9 维等价性（A5）—— 逐维判定

### 4.1 「9 维」的口径（先说清集合，否则"逐维一致"无法判）

`t1` §5 标题写"**9 维**"、A7 亦写"9 维"，但表内实际冻结 **6 维**（材质 / 名字颜色 / 名字后缀 / lore 文本 / PDC 键 / id 来源）+ **§5.4 的第 8 维**（刷新时机）。**维数集合本身没有定义**（F-7 low）。我按"**可见属性 + 影响可见性的结构**"给出可判的 **9 项**，并逐项给判定依据：

| # | 维 | 我的判定依据（独立于 t2/t4/t5 的自述） | 判定 |
|---|---|---|---|
| 1 | 材质（四态 × 两 kind） | 我读 `42dde5e5` 的**被删实现**（`git diff` 逐行）与新 `buildIcon`：READY→`getIcon()`；DISABLED→`BARRIER`；COOLDOWN/ENERGY_LACK→`STRUCTURE_VOID`（旧实现即共用）；运行级：转录 31 条 `[appearance]` 中 `material=barrier` 6 行 = `DISABLED` × (MW 2 + SKILL 4)；`structure_void` 与 `state=COOLDOWN/ENERGY_LACK` 一一对应 | **一致** |
| 2 | 名字颜色 | 旧：GRAY(冷却) / RED(禁用) / GREEN(就绪)；新同；运行级 `name_color_named` 逐行 = `gray`/`red`/`green`，与 `state` 一致（`state≠derived` = 0） | **一致** |
| 3 | 名字后缀（**冻结差异本体**） | 旧 `Skill#createIconItem` 冷却分支拼 `" " + String.format("%.1f", …) + "s"`；旧 `MainWeapon#createIconItem` 冷却分支**只着色**；新 `applySkill`/`applyMainWeapon` 分表复刻。**运行级我独立重算**：`SKILL+COOLDOWN+x.xs` = **7** · `MW+COOLDOWN+'-'` = **2** · `MW+COOLDOWN+x.xs` = **0** · `SKILL+COOLDOWN+'-'` = **0** | **冻结差异未被静默改变**（**最强的一条**） |
| 4 | lore 文本与行序 | 旧：状态行 + 公共两行（`====================` + description）；**ENERGY_LACK 无状态行**（技能侧非对称）；新 `applySkill`/`applyMainWeapon` + `buildIcon` 公共两行**对全部状态与两种 kind** 追加 ⇒ 非对称保留；运行级 `lore0` 与 `lore_n=3` 逐行可读 | **一致** |
| 5 | PDC 键 | 旧：由**具体类**决定（`Skill.createIconItem` 写 `SKILL_KEY`、`MainWeapon.createIconItem` 写 `MAIN_WEAPON_KEY`）；新：由 `item.getKind()` 决定。**二者等价的前提 = kind 由类构造器硬绑**——我核了：`ActiveComponent.getKind()` 是 **`public final`**，`Skill` 传 `SKILL`、`MainWeapon` 传 `MAIN_WEAPON`，**子类无法覆写** ⇒ 等价成立 | **一致（且我给出了等价前提）** |
| 6 | id 来源（PDC 读回） | 旧/新都用 `Utils.getSkillId` / `getWeaponId` 读 PDC；渲染器**引用公开常量**（`Skill.Utils.SKILL_KEY` / 现为 `public` 的 `MainWeapon.Utils.MAIN_WEAPON_KEY`），字面量各仍 **1** 处且**不在** `core/hotbar/**`；运行级 `pdc=shadowhunterrolesplugin:skill_id=…` **19** 行 / `main_weapon_id=` **8** 行 | **一致** |
| 7 | 状态判定顺序 | `stateOf`：`!ready→COOLDOWN` → `!canCast→DISABLED` → `energy<cost→ENERGY_LACK` → `READY`；与被删旧实现的分支顺序**逐行同序** | **一致** |
| 8 | 可达性（MW·ENERGY_LACK） | 结构性不可达（§3 第 7 条）；新实现**未造新外观**（空分支）；探针注入缺陷 n7 被 MISMATCH 抓住 | **一致** |
| 9 | 刷新时机（第 8 维） | 见 §4.2（含**形态变化**与**已申报**项） | **见下** |

### 4.2 第 9 维（刷新时机）的判定与**已申报的形态变化**

- **空闲 tick**：`dirty=false` 且 `hasCoolingSkill()=false` ⇒ flush 入口为假 ⇒ **0 次写物品**（结构性）。`t1` §8.1 已订正基线：**"空闲 tick 零 setItem" 接管前就已成立** ⇒ 本批判"**不退化**"✅。
- **构造期首刷**：构造器末**同步** `render()` ⇒ **零延迟**（= 旧 `updateHotbar()` 的位置）✅；置脏初值 `true` 造成"首个 tick 再写一次**同内容**"（1 次额外写、不可见、**已申报**）✅。
- **冷却中 tick**：旧 = **只改 `displayName`**（`updateAllSkillItemMeta`，**不调 `inv.setItem`**）；新 = **每 tick 全栏重建**（`inv.setItem` ×槽位数，含主武器槽）。这是**写形态变化**，**已在 `t2` 说明 §6.2/§9(c) 逐条申报**，且 `t1` §3.4/§5.4 **明确要求**"每 tick 至少 flush 一次" ⇒ **不是未申报的可见变化**；**量化缺口**指向 `t17`/`t18`（`t1` §11 已声明）✅。
- **五条触发路径**：置脏 + 帧末 flush ⇒ **≤1 tick 延迟**（`t1` §5.4 允许）✅。
- **`t4` F-3 交办的"槽位被玩家清空/替换"场景**：旧路径 `if(item == null) return;`（只改名、空槽不动作）；新 flush **全量重建**（空槽被填回）。**我的裁断**：该场景**不在 `t1` 冻结的 6 维/8 维表内**，且新行为方向与"渲染器 = 唯一渲染者 + 全量重绘"的设计一致（旧行为反而会把玩家放在该槽的**任意物品**改名）；⇒ **不判 fail**，但**必须明示"对该场景不作承诺"**（已由 `t4` §9 F-3 记录；本卡确认该处置，并把它并入"未覆盖"§12）。

---

## §5 必查④：计数口径三件齐（A6）

**逐条审计**（模式 + 定域 + 是否扣除定义/注释/文案）：

| 判据 | 模式 | 定域 | 口径（含/不含定义、注释、文案） | 是否标"裸计数" | 判定 |
|---|---|---|---|---|---|
| C-02 | ERE `\bmarkDirty\(\);` | `src/main/java/**` | **仅调用点**（结构性排除定义行/javadoc；**注释行另行逐行判读**） | 是（并给出裸串 9 的差值 4 逐行归因） | ✅ |
| C-03 | ERE `::markDirty\b\|\bmarkDirty,` | 同上 | 行级，**含注释**，命中 1 无注释 | 明确声明 | ✅ |
| C-05/C-06 | ①`-F` ②ERE 带接收者 | 同上 | ①**裸**（含定义）②**仅调用点** | 是 | ✅ |
| C-07 | `-F 'setItem('`（带 `\(`） | ①②④全树 / ③目录 | **裸计数**，并声明"渲染写点 = ① − ②" | 是 | ✅ |
| C-08/C-09/C-10 | `-F`/ERE | 同上 | **归零组合法**（期望 0） | 是 | ⚠️ 期望合法但**自检样本失效**（F-2） |
| C-12 | ERE record 声明行 | 单文件 | 行级 | 是 | ✅ |
| R-5 | `-F '%.1f'` / `-F 'Locale'` / `-F 'String.format'` | 全树 / **`core/**`** | 行级含注释；**并给出定域理由**（`command/**` 既有 12 行 `Locale.ROOT` ⇒ 不得写成"全仓 0"） | 是 | ✅ **这条口径订正是本批最值钱的一处**（避免必然假失败） |
| A14/§12 | `--untracked --no-exclude-standard` | 单文件（未跟踪 + 被忽略） | 行级含注释；并给**修正命令 + 假 0 负对照** | 是 | ✅（负对照的环境漂移另记 F-6） |

**双口径（减少组）独立复算** —— 我**不看**共享仪器输出，直接按**字符串字面量出现次数**在**两侧 91 个 `.java`** 上重算（`t2` 说明 §10.3 的 8 条）：

| 字面量 | before | after | net | `t2` 申报值 | 判定 |
|---|---|---|---|---|---|
| `" "` | 3 | 2 | −1 | −1 | ✅ |
| `" DISABLED"` | 4 | 2 | −2 | −2 | ✅ |
| `" ENERGY LACK"` | 2 | 1 | −1 | −1 | ✅ |
| `"%.1f"` | 2 | 1 | −1 | −1 | ✅ |
| `"===================="` | 2 | 1 | −1 | −1 | ✅ |
| `"RoleInstance is Null!"` | 2 | 0 | −2 | −2 | ✅ |
| `"s"` | 2 | 1 | −1 | −1 | ✅ |
| `"registered"` | **3** | **3** | **0** | 真值 0（工具报 −1 = 幻影行） | ✅ **幻影行复现成功** |
| **合计（真值）** | | | **−9** | −9 | ✅ |

⇒ 减少组**方向为负且逐条可解释**（指南 §4 第 4 条），**不是"0 行"**；工具 `string net = −10` 与真值 −9 的**唯一差值 = `"registered"`**，`t2` §10.4 与 `t4` F-1 的归因**都正确**。数值侧 net −1 的归因（被删的 `int slot = 0;`）我在 `git diff` 里逐行见过该行 ⇒ 归因成立。

**我自己的一处仪器缺陷（自曝，按本工程纪律）**：我第一版 `log-delta` 复现脚本用**函数形态**包装（`foreach($l in ReadAllLines(...))` 内 `-match`），在**三个**目标件上都**丢掉了 3 条 oshi 行** ⇒ 得到 `t4 轮 ADDED=13 / C=10`、`t5 轮 C=11` 的**假差异**。改用**内联形态**后与冻结值一致（`13/13/14` 行；`t2/t4` 轮 `C=0`；`t5` 轮 `C=1`）。**处置**：本件一律以内联形态为准；并把它作为"**仪器必须双向自证**"的第 N 个实例——**若我未先拿锚（13 行，逐条可读）作正对照，就会把一个假 `C≠0` 写进评审报告。**

**`t4` §2.1 的一处标签微差（不作为 finding）**：裸串 `markDirty` = 9、调用点 5 ⇒ 差值 **4** 的分解，`t4` 写作"方法引用 1 + javadoc 1 + 注释 1 + 定义 1"，**实际是 javadoc 2**（`HR:26` 与 `HR:30` 都是 `*` 行）+ 方法引用 1 + 定义 1。**总数正确、逐行可解释**，故仅记录。

---

## §6 必查⑤：证据的代际认领（A7）

**被验证的「代」只有一个**（我现算）：

| 项 | 身份（现算） |
|---|---|
| jar（`build/libs` 与归档件**逐字节相同**） | **159512 B / 2026-09-19 12:54:07.286 / `6A249993ECDE32C9AAB4E651191C5277C198F97DB5C35A603FC68893C67FF734`** |
| 归档路径 | `debug-logs/回滚快照/jars/ShadowHunterRolesPlugin-1.0.0-verified-20260919-125407-6A249993.jar` |
| `MANIFEST.txt` | **登记 7**（登记时刻 13:01:13），一行一值，与现算逐条一致 |
| `src/main` tree | **`a6a33c9b85a82ea25b15f8417e9dc880c588ecdb`**（`b062252` = 本批 src 的落库提交，`11bb792`/`c59ede3`/`713d1c7`/`HEAD` 同值） |
| 构建产物冻结证据 | `build/classes` 最新 `.class` = **12:54:06.790**（< jar 12:54:07.286）· `src` 新于 jar 的件数 = **0**（最新 src = `HotbarRenderer.java` 12:53:38.126） |

**四批证据 × 各自的轮次与产物（代次认领表）**：

| 轮 | 覆盖它的 jar | 冻结件（我现算身份） | 认领是否越代 |
|---|---|---|---|
| t2 接管轮 | `6A249993…` | `阶段5-起服日志-4.4接管.log` = **6077 B / 56 行 / 13:00:33.240 / `527183B0…3A8A`** · 物证 `…-4.4接管-停服物证.txt` = **9697 B / 122 行 / 13:01:37.789 / `E5C1D63C…9ECC`** | **未越代**（`MANIFEST` 登记 7 的"覆盖证据"逐字指向它） |
| t4 复核轮 | 同一个 `6A249993…` | `阶段5-起服日志-4.4复核.log` = **5698 B / 54 行 / 13:11:59.139 / `8268E32C…3273`** · 物证 = **8873 B / 85 行 / 13:15:01.821 / `0A55D1B6…46BD`** · 报告 = **33032 B / 241 行 / 13:16:23.880 / `5F373172…89F5`** | **未越代**（同 jar、自出轮次、SHA 与前轮不同 ⇒ 非复用旧件） |
| t5 外观轮 | 同一个 `6A249993…` | 日志 = **16493 B / 140 行 / 13:24:24.242 / `B96011E7…7833`** · 转录 = **48275 B / 236 行 / 13:24:00.398 / `13A17F66…DA02`** · 报告 = **47217 B / 511 行 / 13:28:50.458 / `2FE06107…01D3`** · 物证 = **9580 B / 118 行 / 13:29:09.079 / `54EC401B…C683`** | **未越代**（同 jar；与 t67 `73EEF15D…` / t2 `527183B0…` / t4 `8268E32C…` 三个旧 log **SHA 全不同** ⇒ 假新鲜三条件成立） |

**跨代引用误判检查（A7 的核心）**：
1. **`t4`/`t5` 都用新证据冒充旧代了吗？没有。** 两轮**同一 jar 同一 tree**，且各自给出了**自己那一轮**的新冻结件（SHA 互异）；`t4` 用 remap 索引的 **SHA256 映射**自绑定（本轮无 `Remapping` 行），`t5` 用**共享读流的源件 SHA256**（我复算记录与 `t5` 一致口径）。
2. **`t5` 的"接管前对照值"是什么代？** = **点名 `42dde5e5` 的静态 blob**（我逐条核过：`Skill.java` = `beed349a6d1fb7080c3694fa06c6616b0bae61a3` · `MainWeapon.java` = `6dc2a90c49d478f9979f2d99645027a587895c26` · `HotbarRenderer.java` = `35343df17c5adf1f03ce21f6c65c40af08a2e30b` —— **三个 blob 与我自己的 `git diff` 头 `index beed349..` / `6dc2a90..` 逐字吻合**）⇒ **before 侧是"接管前代"的源码，不是新代的自证**，**不构成冒领**。`t5` §8.1#2 同时**如实申报**"接管前的**运行级**外观未覆盖"（历史冻结件从未做物品级 dump）⇒ 我确认：**"逐维一致"是"静态 before vs 运行后"的对照**，不是运行级对运行级（已在 §12 计入未覆盖，不推定为已覆盖）。
3. **`t3` 的两条文档尾巴**（勘误件只指归档路径、复核报告只追加 84 插入 / 0 删除）：`t4` §8 已核；我另行现算其**代次归属**：勘误件引第 4 代 = `0F2D901C…`（159652 B / 09-18 21:41:02.153）**与归档件现算一致** ⇒ 认领正确。

---

## §7 必查⑥：文档驱动的回归（A8）→ **F-1**

> 危害定义（高于"文档滞后"）：**照它写代码会写出失效实现**。我用"同一批文档同一 pathspec + 已知命中样本"证明我的扫描有区分力（正对照 = `开发指南:381`「组件不碰热键栏…`markDirty()` 是框架内部 API」**仍然正确且命中**）。

| # | 文件（活文档 / 批内注释） | 逐字片段（现算现值，行号 = 移动靶） | 现树事实 | 危害 |
|---|---|---|---|---|
| 1 | `docs/插件文档/架构与组件模型.md` | :206「**现状（…**尚未接管**）**：`HotbarRenderer` 仍是**骨架**（只有 `markDirty()` / `isDirty()` / `clearDirty()` / `stateOf()` 四个方法，**不写任何物品**）；**没有帧末 flush**；**可见刷新仍由 `RoleInstance.updateHotbar()` 承担**（触发方式 = "轮询 `shouldUpdateHotbar()` + 立即调用"）。`markDirty()` 当前实际调用点 = **2**…」 | **全句已被本批推翻**（渲染器写物品、有帧末 flush、`updateHotbar` 已删、调用点 = 5） | **最高**：照它写=重建已完成的接管 / 新增第二条写物品路径 |
| 2 | 同上 | :204「刷新触发五类…**由端口适配器内部置脏**」 | 五类里**只有"冷却启动"**在端口适配器（`CooldownPortImpl#start`），其余在 `RoleInstance`/`BuffManager` | 中：把"端口内部置脏"当既定口径 ⇒ 查错落点 |
| 3 | 同上 | :31「`HotbarRenderer`（统一渲染器，**骨架**）」· :182「**换成 `markDirty()` 留到 4.4**（`HotbarRenderer` 仍是骨架）」· :268「设计 4.4 …（**今天只是骨架**、无帧末 flush，可见刷新仍由 `updateHotbar()` 承担）」· :282「**基类仍活 4 个方法**…属 T ⑦ **未收口**部分」 | 均已被推翻 | 中 |
| 4 | `docs/插件文档/角色与技能.md` | :333「保留 `runLater(this::updateHotbar, remaining)`…换成 `markDirty()` **留到 4.4**」· :338「`updateHotbar()` 遍历它逐槽 `inv.setItem(slot, ...)`（`RoleInstance.java:437-463`）」· :339「图标内容由**基类**方法生成…（T⑦ 未收口部分）」· :348「`shouldUpdateHotbar()`…就整栏重建（`updateHotbar()`）」· :349「`shouldUpdateItemMeta()`… `updateAllSkillItemMeta()` → `updateSkillItemMeta()`」 | 五个方法**全树 0 命中**（已删） | 中：**逐字描述一个不存在的调用链** |
| 5 | `docs/插件文档/开发指南-新增角色或组件.md` | :355「热键栏正确：…图标为角色 `.icon(...)` 或组件图标（**`RoleInstance.updateHotbar`**）」· :399「统一渲染器**接管**渲染 … \| 设计 4.4 + 阶段 5；**当前只是骨架** \| `core/hotbar/HotbarRenderer.java:1-14`」· :105「仅**基类 4 个方法**仍活…属 T ⑦ **未收口**部分」 | 均已被推翻（:105 的**禁止性结论**仍正确：新组件不要写这两处覆写） | **高**：这是**新人/新组件作者的验收清单** |
| 6 | `src/main/java/…/core/RoleInstance.java` | :79「与统一渲染器（**骨架**）」· :138「//统一渲染器（阶段 4.1 **骨架**；**接管渲染属 4.4**）」 | 渲染器**已是唯一渲染者**；"接管渲染属 4.4"是**开工前的计划句**，4.4 已完成 | **中**：这两行在**本批自己改过的文件**里；读者据 :138 会认为接管**尚未发生** |

**边界与处置**：① `docs/**` **不在本卡 inScope**（且 `t1` §8.1 已就 :206 一条登记"由 `t7`/`t8` 同步"）⇒ 本卡**只登记、不修改**；② **`t7`（integration，`eng-文档`）已在任务表且依赖本卡** ⇒ 我把 1–5 纳入 **F-1 的 requiredFix（指向 t7）**；③ :79/:138 属 `src` ⇒ 按本工程纪律**不得夹带进已冻结代次**（改 `src` 会改 `src/main` tree ⇒ 与本批证据的 tree 绑定脱钩）⇒ requiredFix 要求在 `t7`/新卡里以"**注释口径回收**"单独申报，并说明其对已冻结证据绑定的影响（建议与下一次 src 批次同行，或按既有"class 级等价"证法证明仅注释变更）。

---

## §8 必查⑦：三条红线（A9）

| 红线 | 命令 + 口径 | 期望 | 我现算 | 判定 |
|---|---|---|---|---|
| **R-1** `BuffManager#addBuff` **不接线** | `git grep -nE 'hotbarRenderer\|markDirty\|updateHotbar' -- …/manager/BuffManager.java`（含注释） | 1 命中且宿主 = `removeBuff` | **1**（:109）· 宿主 = `removeBuff`（定义 :99）；`addBuff`（定义 :35）体内 **0**；`addImmune` 只经 `removeBuff` 间接触发（`t1` 允许） | ✅ **未被偷偷接线** |
| **R-2** 能量**无条件**置脏 | `git grep -nF 'getEnergyCost' -- …/core/RoleInstance.java` | 0 | **0**（exit 1）；`setCurrentEnergy` :535 clamp → :537 `markDirty()`，**无阈值条件** | ✅ |
| **R-3** **不合并 ticker** | `-F 'runRepeating'` @ RI/BM + `-F 'this::triggerUpdate'` @ src | RI 1 · BM 1 · 引用 1（与 before 同） | **RI 1**（:124）· **BM 1**（:137）· `this::triggerUpdate` **1**（:125） | ✅（其它 `runRepeating` 命中全在 `TimerPortImpl`/`DebugSchedCommand`/组件侧，**与本批无关且未动**） |
| R-5 浮点/本地化 | `-F '%.1f'` @ src · `-F 'Locale'` @ `core/**` · `-F 'String.format'` @ src | 1 / 0 / 2 | **1**（`HR:136` 技能冷却分支）· **0** · **2**（渲染器 + `command/DebugCooldownCommand` 的 `%.2f`，**与本批无关**） | ✅ |
| R-6 B-2 谓词 | 静态：谓词存在且被 flush 入口引用 | 成立 | `hasCoolingSkill()`（:747，只遍历 `skillCooldowns`）+ 引用 :736 | ✅ 静态级；运行级递减由 `t5`/我复算覆盖（§2.1 C-15） |
| R-7 PDC 真值来源不分裂 | `-F '"main_weapon_id"'` / `-F '"skill_id"'` | 各 1 且不在 `core/hotbar/**` | **1 / 1**（`MainWeapon.java:47` / `Skill.java:40`），**均不在** `core/hotbar/**` | ✅ |
| §8.2 键可见性 | `MAIN_WEAPON_KEY` 二选一 + 申报 | 已选并申报 | 选 **(a) 提为 `public static final`**（`MainWeapon.java:46`），`t2` 说明 §9(e) **已申报"可见性扩权"** | ✅ |

---

## §9 必查⑧：是否真的**没有重建**被验证代次的 jar（A10）+ F1 链

### 9.1 未重建（我的现算，四项互证）

1. **jar 身份三时点不变**：`build/libs` 与归档件 = **159512 B / 12:54:07.286 / `6A249993…`**（= `t4` §1、`t5` §1.3 记的值，**逐字未变**）⇒ 若有人 `build`/`clean`/删 `build/libs` 重建，mtime 必然前移。
2. **构建产物整体冻结**：`build/classes` 最新 `.class` = **12:54:06.790**（< jar 12:54:07.286）；`src` 新于 jar 的件数 = **0**。⇒ `t4` 自述的"仅 `compileJava` 一次、`.class` mtime 未变"与我现算一致（**UP-TO-DATE，未重编译**）。
3. **5 个旧归档未被触碰**：`7A21729B` 148297 B / 09-18 20:37:49.919 · `DBE04077` 148298 / 20:58:55.755 · `53CBEB65` 158875 / 21:26:16.053 · `0F2D901C` 159652 / 21:41:02.153 · `21002ABC` 159661 / 22:01:06.944 —— **mtime 全部仍是 09-18**。
4. **归档件与 `build/libs` 逐字节相同**：两侧 SHA256 均为 `6A249993…`。

⇒ **未做「删 jar + 重建」**，**未 clean**，**未强推**；本批只产生**一代**（159661 → 159512 B，`MANIFEST` 登记 7 已记"与前代不存在 class 级等价主张"）。

### 9.2 F1 链（按队长口径澄清件的**必备环**判，不套固定项序列）

**必备环**：`jar mtime < log 内容终点` **且** `jar mtime < 该批每一件证据的产生时刻` **且** `log 内容终点 ≤ 冻结动作时刻`。

| 轮 | jar mtime | log 内容终点（我现算，冻结件内时间戳） | 该批每件证据的产生时刻 | 必备环 |
|---|---|---|---|---|
| t2 | 12:54:07.286 | 冻结件**末条时间戳 = 12:57:13**（`t1`/`t2` 记的 `Done (15.686s)` = 12:57:12） | log 13:00:33.240 · 物证 13:01:37.789 · 说明 13:03:28→**13:32:48**（队长追加更正后） | **成立** |
| t4 | 同上 | 冻结件**末条 = 13:09:05** | log 13:11:59.139 · 物证 13:15:01.821 · 报告 13:16:23.880 | **成立** |
| t5 | 同上 | 冻结件**末条 = 13:23:59**（= 源内容终点） | 转录 13:24:00.398 · log 冻结动作 13:24:24.242 · 报告 13:28:50.458 · 物证 13:29:09.079 | **成立** |

⇒ **三批全部合规**；证据件**彼此之间**的先后（物证/报告/说明谁先谁后）**不作为判据**（队长口径澄清件）。**不把 `t5` 的 F1 记为 finding。**

### 9.3 集合差 `C=0` 的**独立复现**（含一处覆盖面缺陷 ⇒ F-3）

我按 `log-delta.ps1` 的**自身算法**（`/` + 级别 + `]` 选行 → 去 `[HH:MM:SS]` → 排序 → 集合差 → 分类 A/B/C）**内联重写**，锚 = `阶段4-起服日志-T2b.log`：

| 轮 | 选行数 | ADDED | REMOVED | A(oshi) | B(world) | **C(OTHER)** | 窗口（首/末时间戳） |
|---|---|---|---|---|---|---|---|
| 锚 | 13 | — | — | — | — | — | — |
| t2 接管轮 | **13** | **0** | **0** | 0 | 0 | **0** | **12:56:57 – 12:57:13（16 s，纯开机）** |
| t4 复核轮 | **13** | **0** | **0** | 0 | 0 | **0** | **13:08:50 – 13:09:05（15 s，纯开机）** |
| t5 外观轮 | **14** | **1** | 0 | 0 | 0 | **1** | 13:19:56 – 13:23:59（4 min，**含 gameplay**） |

**`t5` 轮的那 1 条 `C`（逐字）**：
```
[Server thread/WARN]: [ShadowHunterRolesPlugin] Global task for ShadowHunterRolesPlugin v1.0.0 generated an exception
```
⇒ ① `t2`/`t4` 的 `C=0` **独立复现成立**；② 但**两轮都是"开机窗"**（16 s / 15 s，无任何 gameplay：`issued server command` 在 t2 轮 = 0 行），**唯一进入 gameplay 的轮次 `C=1`** ⇒ `C=0` 这条绿灯**恰好在插件真正运行的窗口上没有覆盖**（**F-3**）。

---

## §10 已知未闭合项的裁断（A11）

| # | 未闭合项（来源） | 我的裁断 | 理由 / 最小闭合 |
|---|---|---|---|
| 1 | **C-15 外观级**（`t2` 未做、`t4` 申报、`t5` 交付） | **已覆盖** | 我独立重算 `t5` 转录（31 条 `[appearance]`、四态×两 kind、`7/2/0`、`state=derived`、decay 三点严格递减、PDC 读回 19/8）⇒ 不再挂账。**残余**：**接管前的运行级外观不存在**（`t5` §8.1#2 已申报）⇒ 见 §12 |
| 2 | **`setItem` 次数（性能契约）未量化**（`t1` §11 / `t2` §14#2 / `t4` §11#2） | **仍缺，归 `t17`/`t18`** | 与 `t1` §3.4/§5.4 的冻结口径一致；**本卡不升为 finding**（不是本批承诺）。**但**要求后续卡把"**冷却中每 tick 全栏 setItem**"这一形态变化写进量化口径（§4.2） |
| 3 | **`BuffManager#addBuff` 的可见时机（R-1 保护的差异）运行级未取证**（`t1` §11 / `t4` §11#3 / `t5` 未做） | **仍缺 ⇒ 建议并入未来的 `t17`/`t18` 或定向冒烟** | 需构造"上 STUN 但不起冷却"用例；`t5` 的两轮都是"先起冷却再 STUN" ⇒ 该差异**仍只有静态依据** |
| 4 | **秒数递减平滑性**（`t1` §11 只承诺抽样） | **已覆盖（抽样级）** | `t5` 三点 29.3→19.3→9.3 + 我复算严格递减；**逐 tick 平滑性**仍不可 grep 证明（`t1` 已申报） |
| 5 | **JVM locale 对 `%.1f` 的影响** | **仍缺（不测，登记为红线 R-5）** | `t5` §4.3 实测小数点为 ASCII `.`（未触发）；本沙箱不可变 locale |
| 6 | **Folia 真机** | **仍缺（与既有裁定一致）** | 本批不做 Folia 适配 |
| 7 | **`t4` F-3（玩家清空/替换冷却中槽位）** | **裁断：不判 fail；明示"不承诺该场景"** | 不在 `t1` 冻结表内；新行为与设计一致（§4.2） |
| 8 | **插件归因 WARN（开放项）** | **记为开放项 · 非本批引入 · 非 blocker · 另立卡闭合** | 见 F-4；`t2`/`t4` 的 `ERROR\|SEVERE = 0` 结论**不受影响**（级别 = WARN） |
| 9 | **`t1` §8.1 登记的活文档过期口径** | **仍需新卡 ⇒ 已并入 `t7` 范围（F-1）** | `t1` 只登记了 1 条（`架构与组件模型` §4.4 末段）；我实测**该族共 6 处**（§7） |
| 10 | **`t47` 死锁空卡 / `t15` 沙箱不可执行三项 / 三个 `0L` 业务触发点** | **与本批无关，不重评** | 沿用既有裁定；本批未触碰相关路径 |
| 11 | **`README:36` 的带时刻历史读数缺"已作废"标注**（`t3` 申报） | **仍需新卡（并入 F-1 的 doc-sync 范围）** | `t3` 已在 §13.5 申报；我确认它仍未被处理 |

---

## §11 Findings（A2：每条 = 对象 + 复现命令 + 原始输出 + 判定依据）

### F-1 · **medium** — 文档驱动的回归：活文档与批内注释仍以"渲染器 = 骨架"为现状
- **对象（文件 + 符号/章节 + 逐字片段）**：`docs/插件文档/架构与组件模型.md` :206（"**尚未接管**…仍是**骨架**…**没有帧末 flush**…可见刷新仍由 `RoleInstance.updateHotbar()` 承担"）· :204（"五类…由端口适配器内部置脏"）· :31 / :182 / :268 / :282 · `docs/插件文档/角色与技能.md` :333 / :338 / :339 / :348 / :349 · `docs/插件文档/开发指南-新增角色或组件.md` :355 / :399 / :105 · `src/main/java/com/shadowHunterRolesPlugin/core/RoleInstance.java` :79 / :138
- **复现命令**（含已知命中正对照，证明扫描区分力）：
  ```
  cd ShadowHunterRoles
  git grep -n -E 'createIconItem|updateHotbar|markDirty|HotbarRenderer|shouldUpdateHotbar|updateSkillItemMeta' -- docs
  git grep -n -E '骨架|接管渲染属 4\.4' -- src/main/java/com/shadowHunterRolesPlugin/core/RoleInstance.java
  ```
- **原始输出（节选，逐字）**：
  ```
  docs/插件文档/架构与组件模型.md:206:| 刷新机制 | ... **现状（与 `debug-logs/阶段4-4.4渲染器接管预案.md` 一致，**尚未接管**）**：`HotbarRenderer` 仍是**骨架**（只有 markDirty()/isDirty()/clearDirty()/stateOf() 四个方法，**不写任何物品**）；**没有帧末 flush**；**可见刷新仍由 `RoleInstance.updateHotbar()` 承担** ...
  docs/插件文档/角色与技能.md:348:  - `shouldUpdateHotbar()`（`RoleInstance.java:820-828`）：只要**存在冷却已结束**的技能或武器，就整栏重建（`updateHotbar()`）；
  docs/插件文档/开发指南-新增角色或组件.md:399:| 统一渲染器**接管**渲染（组件不再有 `createIconItem`） | 设计 4.4 + 阶段 5；当前只是骨架 | `core/hotbar/HotbarRenderer.java:1-14` |
  src/.../core/RoleInstance.java:79:    //阶段 4：组件注册表（...）与统一渲染器（骨架）
  src/.../core/RoleInstance.java:138:    //统一渲染器（阶段 4.1 骨架；接管渲染属 4.4）
  ```
- **判定依据**：同文件的 `updateHotbar` / `shouldUpdateHotbar` / `updateSkillItemMeta` / `createIconItem` 在现树 **全部 0 命中**（§2.1 C-08/C-09/C-10）；`HotbarRenderer` 已是唯一渲染写点（C-07）。⇒ 这些句子**不是"文档滞后"，而是"照它写代码会写出失效实现"**：① 按 :206/:399 会**重建已完成的接管**或**新增第二条写物品路径**（正是 `t1` 判据 C-07 要防的）；② 按 :204 会把"端口内部置脏"当既定口径查错落点（实际五类里只有 1 类在端口）；③ 按 :348/:349 会去找**不存在**的调用链。
- **requiredFix**：**不夹带进已冻结代次**。① `docs/**` 5 处 → 纳入 **`t7`（integration，`eng-文档`，已存在且依赖本卡）**的活文档同步范围，口径以 `t1` §8.1 + 本件 §7 为准（把"尚未接管"改为"4.4 已接管，唯一渲染者 = `HotbarRenderer`；五类触发落点**逐类点名**"）；② `RoleInstance` :79/:138 两行注释属 `src` ⇒ 单列"注释口径回收"项：要么随**下一次 src 批次**同行，要么按既有"class 级等价"证法证明**仅注释变更**后再落库，并在交付里申报其对 `src/main` tree 绑定的影响（**不得**在本批证据冻结后再偷偷改 `src`）；③ 顺手（可选）把 `HotbarRenderer:27` 的"冷却中的技能例外"锐化为"**冷却中整栏每 tick 重写**"，与 `t2` 说明 §6.2 的申报口径一致。

### F-2 · **medium** — 判据缺陷：归零组自检样本在 after 态**自身归零**（`t1` §10 三处），且 `t4` 的 F-2 **枚举不全 / 一处定性不准**
- **对象**：`debug-logs/测试记录/阶段5-需求与验收.md` §10（自检样本汇总）C-03 / C-09 / C-10 三行 + C-08 行的"算术闭合"；`debug-logs/测试记录/阶段5-4.4接管-独立复核报告.md` §9 F-2
- **复现命令 + 原始输出**：
  ```
  cd ShadowHunterRoles
  git grep -n -E '::updateHotbar\b' -- src/main/java          ⇒ 0 行 · exit 1     # C-03 原样本
  git grep -n -F 'updateHotbar' -- src/main/java/com/shadowHunterRolesPlugin/core/RoleInstance.java ⇒ 0 行 · exit 1   # C-09 原样本
  git grep -n -F 'updateSkillItemMeta' -- src/main/java        ⇒ 0 行 · exit 1     # C-10 原样本
  git grep -n -F 'getDisplayName(' -- src/main/java | Measure-Object -Line ⇒ 15     # C-08 原样本（仍有命中）
  git grep -n -F 'this::triggerUpdate' -- src/main/java        ⇒ 1 行               # 我的替代样本
  git grep -n -F 'hasCoolingSkill' -- src/main/java            ⇒ 2 行               # 我的替代样本
  ```
- **判定依据**：`t1` §1.2 自订纪律 =「**任何"判据返回 0"的结论，必须先拿一个"已知有命中"的样例会诊该判据**」。C-03 的被测符号**就是本批删掉的符号**，故其样本在 after 态**必然**归零 ⇒ 该 0 **与"pathspec 没落到文件"不可区分**（`t1` 自己的负对照正是这个失败模式）。C-09/C-10 同理（样本符号 = `updateHotbar` / `updateSkillItemMeta`，现树 0）。⇒ 三条**归零组**判据在 **after 态缺乏区分力**；`t4` 只把 C-03 与 C-08 记入 F-2，**漏了 C-09 / C-10**；而 C-08 的样本 after = **15 行（非 0）**，**仍有区分力**——失效的是"算术闭合 25 = 22+2+1"这句**表述**，不是样本本身。
- **requiredFix**：① 冻结件/后续卡的样本一律改选**不受被测批影响**的符号；本卡给出可直接采用的替代（C-03 → `this::triggerUpdate` = 1、`triggerUpdate` = 4 行；C-09 → `hasCoolingSkill` = 2、`hotbarItemOf` = 2；C-10 → `render()` @ RoleInstance = 2）；② 如坚持用原符号，则**并列 before/after 双样本**并显式标注"after 归零 ⇒ 该样本仅证 before 态路径有效"；③ 在 `t4` F-2 上**追加更正**（补 C-09/C-10，改 C-08 定性），不改写其原句（本工程惯例：追加更正、不改写冻结报告）。

### F-3 · **medium** — `C=0` 的证据覆盖 = 两个**开机窗**；唯一进入 gameplay 的轮次在同一锚 + 同一算法下 **`C = 1`**
- **对象**：`阶段5-起服日志-4.4接管.log`（56 行 / 12:56:57–12:57:13）· `阶段5-起服日志-4.4复核.log`（54 行 / 13:08:50–13:09:05）· `阶段5-起服日志-4.4外观取证.log`（140 行 / 13:19:56–13:23:59）；`t2` 说明 §10 / `t4` §5 的 `C=0` 结论
- **复现命令（仪器算法的内联复现，只读）**：
  ```
  # 选行：/ + 级别 + ]  → 去 [HH:MM:SS] → 排序 → Compare-Object → 分类 A/B/C
  # 锚 = debug-logs/测试记录/阶段4-起服日志-T2b.log
  ```
- **原始输出**：
  ```
  anchor: 13   t2: selected=13 ADDED=0 REMOVED=0 C=0
               t4: selected=13 ADDED=0 REMOVED=0 C=0
               t5: selected=14 ADDED=1 REMOVED=0 C=1
  C> [Server thread/WARN]: [ShadowHunterRolesPlugin] Global task for ShadowHunterRolesPlugin v1.0.0 generated an exception
  ```
- **判定依据**：`t2`/`t4` 的 `C=0` **成立且我复现成功**；但两轮的窗口是**纯开机窗**（各 15–16 s；`issued server command` = 0 行，无角色装配/无技能施放）⇒ 该绿灯**只覆盖"启动期无新增级别行"**，而**唯一让插件真正运行的轮次**（`t5`，`issued server command` 36 行 / `[command-debug]` 20 行）在**同一锚、同一算法**下 `C=1`，那一条恰是 §10#8 的开放项。⇒ 若后人把"两轮 `C=0`"读作"运行期无归因异常"，就会**用一句真话支撑一个假结论**（正是本工程"单一范围扫描不能支撑全局断言"的实例）。
- **requiredFix**：① 在后续批次的运行级验收里**必须**至少一轮**含 gameplay**（起服 → 施放/受击 → 冻结），并**分列**"开机窗集合差"与"运行窗集合差"两个读数（**不得合并成一句 `C=0`**）；② 范围声明里**点明窗口长度与内容**（`t4` §10 已声明"只覆盖同一 artifact identity 的两个已冻结轮"，但**未说明这两轮是开机窗**）；③ 与 F-5 的"插件归因异常"扫描项**并存**。

### F-4 · **low** — 队长裁定的插件归因 WARN：**记开放项 ✓ · 非本批引入 ✓ · 非 blocker ✓ · 仍需另立卡**
- **对象**：`debug-logs/测试记录/阶段5-起服日志-4.4外观取证.log`（`B96011E7…`，第 86–95 行区间）· 留档件 `debug-logs/测试记录/开放项-属性修饰符残留导致STUN首施异常.md`（89 行）
- **复现命令 + 原始输出**：
  ```
  [13:21:26] [Server thread/WARN]: [ShadowHunterRolesPlugin] Global task for ShadowHunterRolesPlugin v1.0.0 generated an exception
  java.lang.IllegalArgumentException: Modifier is already applied on this attribute!
        at ...AttributeInstance.addModifier(AttributeInstance.java:75)
        at ShadowHunterRolesPlugin-1.0.0-1789793818094.jar//com.shadowHunterRolesPlugin.manager.BuffManager.addBuff(BuffManager.java:53)
        at ShadowHunterRolesPlugin-1.0.0-1789793818094.jar//com.shadowHunterRolesPlugin.core.BuffPortImpl.add(BuffPortImpl.java:29)
        at ShadowHunterRolesPlugin-1.0.0-1789793818094.jar//com.shadowHunterRolesPlugin.roleComponent.DefaultSanTEZeroPunishment$1.run(DefaultSanTEZeroPunishment.java:84)
        at ShadowHunterRolesPlugin-1.0.0-1789793818094.jar//com.shadowHunterRolesPlugin.platform.BukkitSchedulerAdapter.lambda$runRepeating$2(BukkitSchedulerAdapter.java:61)
  ```
- **判定依据**：① **非本批引入**：抛点在 `BuffManager#addBuff`，本批对该文件只改 `removeBuff` 一行（`git diff 42dde5e5..HEAD -- manager/BuffManager.java` = 1 增 1 删 1 上下文）；② **级别 = WARN** ⇒ `t2`/`t4`/我 的 `\] \[[^]]*/(ERROR|SEVERE)\]` = 0/0/0 **不受影响** ⇒ **不判 blocker、不算 `t2`/`t4` 的判据缺陷**；③ **已留档**（含现象、通道证据、代码级不对称、影响面、最小闭合方式）；④ 留档 §3 的因果链**未受控复现** ⇒ 我只按"**未受控复现的假设**"引用，**不写成已证实**。
- **requiredFix**：另立一张独立小卡（受控复现 → 对称化（`addBuff` 的 STUN 分支先 `removeModifier` 再 `addModifier`）→ 载入/清账期回收 → **新一代重新取证**）；**不得**作为"顺手修"夹带进任何批次。留档件保持"开放项"状态，结案时按 `t7`/`t9` 的索引挂行登记。

### F-5 · **low** — 建议的新扫描项有**假绿形态**：`at com.shadowHunterRolesPlugin` 在**已知正样本上也返 0**
- **对象**：`开放项-属性修饰符残留导致STUN首施异常.md` §7 的建议扫描项；`阶段5-4.4接管-外观取证报告.md` §8.2 的建议形态
- **复现命令 + 原始输出**（三份冻结件同扫，**双向自证**）：
  ```
  # 对三份冻结 log 各跑三种模式：
  '^\s*at com\.shadowHunterRolesPlugin'   → t2: 0 · t4: 0 · t5: 0     ← 假绿（t5 是已知正样本！）
  'jar//com\.shadowHunterRolesPlugin'     → t2: 0 · t4: 0 · t5: 4     ← 正确形态
  'generated an exception'                → t2: 0 · t4: 0 · t5: 1
  'Global task for ShadowHunterRolesPlugin' → t2: 0 · t4: 0 · t5: 1
  '# 级别字段 (WARN|ERROR|SEVERE)=   → t2: 13 · t4: 13 · t5: 14      ← 正对照（非空）
  ```
- **判定依据**：Paper 的 remap 栈帧形态是 **`<jar 名>//com.…`**（**不是** Java 经典的 `\tat com.…`）⇒ 用"`at ` + 包名"当扫描锚会在**每一个**真实栈帧上漏掉；而 `com.shadowHunterRolesPlugin` 作为**裸子串**（留档件 §7 的写法）**是能命中的**。⇒ 该建议**方向正确、形态危险**：一旦后续卡把它实现成 `at com\.…`，就会得到一个**永不报错的绿灯**（假阴性方向，与"裸 `ERROR` 命中 oshi 正文"的假阳性方向同族）。
- **requiredFix**：后续卡把「**插件归因异常**」扫描项**钉死形态**为 `generated an exception` 或 `Global task for <插件名>` 或 **裸子串** `com\.shadowHunterRolesPlugin`（**禁**加 `at ` 前缀、**禁**加行首锚），并按指南 §4 第 7 条**强制带正/负对照**（正 = 已知含该异常的冻结件；负 = 同一形态对不含它的件）；本卡给出的 0/0/1 与 0/0/4 可直接作为该卡的自检数据。

### F-6 · **low** — `t1` §12 的**负对照已随环境漂移**（该文件已被 `-f` 入库 ⇒ 不再"未跟踪"）
- **对象**：`阶段5-需求与验收.md` §12「不带 `--untracked` 的同一命令 ⇒ **0 行 · exit 1**（假 0）」
- **复现命令 + 原始输出**：
  ```
  git grep -n --untracked --no-exclude-standard -- 'markDirty' -- debug-logs/测试记录/阶段5-需求与验收.md | Measure-Object -Line  ⇒ 47  · exit 0
  git grep -n -- 'markDirty'                                    -- debug-logs/测试记录/阶段5-需求与验收.md | Measure-Object -Line  ⇒ 47  · exit 0   ← 与上面相同（当时是 0）
  git ls-files --error-unmatch -- <该路径>   ⇒ 输出该路径（= **已跟踪**）
  git check-ignore --no-index -v -- <该路径> ⇒ .gitignore:84:debug-logs/**  （= **同时**符合忽略规则）
  ```
- **判定依据**：台账 §7 族 8 的两句并存（"`git grep` 不搜未跟踪件" **且** `check-ignore` 可仍为真）⇒ 队长把该文件 `-f` 入库后，"不带 `--untracked` ⇒ 0 行"**不再复现**。判据**主体（期望 0 命中）仍成立**，但**文档化的负对照**会误导后人以为"命令写错了"或"回归了"。
- **requiredFix**：在冻结件的 after 说明（或 `t9` 的文件级台账）里区分"**当时未跟踪**"与"**现已跟踪**"，并把归属判据固定为 `git check-ignore --no-index` + `git ls-files`；**不改写**冻结件原句。

### F-7 · **low** — "9 维等价性"**没有定义**（标题 9 维 / 表内 6 维 + 第 8 维）
- **对象**：`阶段5-需求与验收.md` §5 标题「**9 维**等价性对照表（A7）」与 §5.2/§5.3 两张**6 列**表 + §5.4「第 8 维」；卡面 A7 亦写"9 维"
- **复现命令 + 原始输出**：标题行 =「## §5 9 维等价性对照表（A7）」；§5.2 表头 =「状态 \| 材质 \| 名字颜色 \| 名字后缀 \| lore 文本（行序）\| PDC 键 \| id 来源」= **6 列**；§5.4 标题 =「第 8 维（刷新时机）」
- **判定依据**：**同一条"逐维一致"在不同人手里会指向不同集合**（`t5` §4 只覆盖 6 维）。本卡按"可见属性 + 影响可见性的结构"给出可判的 9 项并逐项判过（§4.1），**其中 9 项全部有结论**，故**不判 fail**；但口径须写死。
- **requiredFix**：在 `t7` 的活文档/后续冻结件里把 9 项**逐条列名**（建议 = 材质 / 名字颜色 / 名字后缀 / lore 文本与行序 / PDC 键 / id 来源 / 状态判定顺序 / 可达性 / 刷新时机），或把标题改为"**6 维 + 第 8 维**"与表体一致。

---

## §12 未覆盖项如实申报（A12）与评审分离（A13）

### 12.1 本卡**未覆盖**（逐条 + 最小闭合方式；**不推定**）

1. **接管前的运行级外观**（`t5` 已申报）：历史冻结件无物品级 dump；`t5` 的 before 列 = **点名 `42dde5e5` 的静态 blob**（三个 blob 我已逐条核对）+ 一条材质级旁证（仓库外 `t6-quit-persistence.result.json`，**非承重**）。⇒ 我判定"**静态 before vs 运行 after**"这一**限定**必须随结论一起引；要真运行级须在旧代次上跑探针 ⇒ **与"不得重建被验证代次"冲突** ⇒ 建议不做。
2. **`setItem` 调用次数（性能契约）**：只给"空闲 tick 不置脏 / 冷却中每 tick 全栏重写"的**结构性**论证；量化归 `t17`/`t18`。
3. **`BuffManager#addBuff` 可见时机的运行级证据**：未构造"上 STUN 但不起冷却"用例（见 §10#3）。
4. **秒数逐 tick 平滑性**：我复算证明的是**三点严格递减**（29.3/19.3/9.3），**不是**逐 tick 平滑。
5. **`%.1f` 的 JVM locale 影响**：不可变 locale 复现 ⇒ 只登记 R-5。
6. **Folia 真机**、**双角色/双 bot 同场**、**其它角色/其它槽位**：`t5` §9.1 的范围声明已写；本卡沿用，不外推。
7. **`t5` 的 playerdata/NBT 通道证据**（`role_health_modifier` 命中 3 件、`buff_movement_speed_modifier` 16/16 为 0）：**我未重跑**（需 gzip+NBT 解析口径）⇒ 仅按"留档件自述 + 与冻结栈帧相容"引用；其机制仍标"**未受控复现的假设**"。
8. **制品载入自绑定**：`t4` 用 remap 索引的 SHA256 映射（本轮无 `[PluginRemapper] Remapping` 行）、`t2` 用日志自述路径 —— 我**核到了一致性**（`MANIFEST` 登记 7 的"起服侧自述"与 `t5` §1.3 的 `runServer` 输出同源），**未独立打开 `run/plugins/.paper-remapped/…/index.json`** 重算该映射。
9. **共享仪器本身**：`numeric-multiset.ps1` / `t59-literal-net.ps1` 未改、未重跑；我用**自算字面量**（§5）替代，得到与 `t2` 申报**逐条相同**的结果。
10. **构建**：未跑 `gradlew`（只做只读的 mtime/SHA 复核）⇒ "构建通过"由 `t2` 的 `BUILD SUCCESSFUL` + `build/classes` 冻结时刻（12:54:06.790）支撑，**不是**我本轮复跑。
11. **`t1` 的 `C-14` 集"F5 物证"的三路互证细节**（netstat/独占打开/TCP 拒绝的具体时刻）：我核到**冻结件与物证的存在 + 时间窗**；过程性细节按各卡自述采信（无独立复现可能，服务端已停）。

### 12.2 评审分离纪律（A13）

- 本卡为 **`eng-审阅` 名下唯一在办卡**（任务表现算：`t6 [in_progress] review … → eng-审阅`，无第二张）。
- **未 claim** 任何 verification / implementation / repair 卡；未替被评审卡下"业务结论"以外的事；**未**在报文里替 `t7`/`t8`/`t9`/`t10` 预判结论。
- **验证与评审为不同成员**（现算名册）：`t2` = `eng-实现` · `t4` = `eng-验证` · `t5` = `eng-取证` · **`t6` = `eng-审阅`** ⇒ 四人四卡，无"卡 + 其下游评审"同人组合。
- 本卡结论**只约束"该不该算完成"**：对 `t2` 出 `pass` + 分级 findings；对 `t4`/`t5` 只就"其证据的判据面"给 finding（F-2/F-3/F-5/F-6）与裁断（§10），**不改写**其交付件。

---

## §13 与卡面验收条目的对应（A2 / A3–A14）

| 卡面条目 | 落点 |
|---|---|
| **A2** 每条 finding 四件套（对象 + 复现命令 + 原始输出 + 判定依据） | §11 F-1…F-7 逐条 |
| **A3** 必查①判据区分力 + 我自己的自检 | §2（含 §2.1 逐条表 / §2.2 点名 / §2.3 每条判据的 after 态自检） |
| **A4** 必查②注释声称（逐条列 + 判定） | §3（13 条行为性声称逐条；结论：**实现者 = 0 的声称 = 0 条**；反向 2 条 stale 归 F-1） |
| **A5** 必查③冻结可见差异 + 9 维等价性（逐维判定依据） | §4（§4.1 九维逐项 + §4.2 第 9 维与已申报的形态变化） |
| **A6** 必查④计数口径三件齐 + 裸计数标注 | §5（含双口径独立复算 + 我自己的仪器缺陷自曝） |
| **A7** 必查⑤代际认领（有无跨代冒领） | §6（代次表 + 三条跨代检查结论） |
| **A8** 必查⑥文档驱动的回归 | §7 + **F-1** |
| **A9** 必查⑦三条红线 R-1/R-2/R-3（+R-5/R-6/R-7） | §8 |
| **A10** 必查⑧未重建 + F1 链自洽 | §9（§9.1 四项互证 / §9.2 必备环三批成立 / §9.3 `C=0` 独立复现） |
| **A11** 已知未闭合项裁断（覆盖 / 仍缺 / 需新卡） | §10（11 条） |
| **A12** 未覆盖项如实列出 + 本卡只读声明 | §12.1 + §1 |
| **A13** 评审分离纪律 | §12.2 |
| **A14** 边界声明（未改 src / 未改既有证据 / 零 git 写） | §1 |

---

## §14 复算命令清单（可直接粘贴；全部只读）

```powershell
cd C:\Users\ROG\Desktop\插件\ShadowHunterRoles
# 基线 / 代次（§1 §6 §9.1）
git rev-parse HEAD
git status --porcelain ; git status --porcelain -- src
git rev-parse HEAD:src/main ; foreach($c in @('42dde5e5','b062252','11bb792','c59ede3','713d1c7')){ git rev-parse "${c}:src/main" }
Get-Item -LiteralPath build\libs\ShadowHunterRolesPlugin-1.0.0.jar | Select-Object Length,LastWriteTime
Get-FileHash -LiteralPath build\libs\ShadowHunterRolesPlugin-1.0.0.jar -Algorithm SHA256
Get-ChildItem -Recurse -File build\classes | Sort-Object LastWriteTime -Descending | Select-Object -First 1
# 判据逐条（§2）
git grep -n -E '\bmarkDirty\(\);' -- src/main/java
git grep -n -E '::markDirty\b|\bmarkDirty,' -- src/main/java
git grep -n -F 'clearDirty()' -- src/main/java ; git grep -n -E '\.clearDirty\(\);' -- src/main/java
git grep -n -F 'isDirty()' -- src/main/java ; git grep -n -E '\.isDirty\(\)' -- src/main/java
git grep -n -F 'setItem(' -- src/main/java ; git grep -n -F 'setItem(i, null);' -- src/main/java ; git grep -n -F 'setItem(' -- src/main/java/com/shadowHunterRolesPlugin/core/hotbar
git grep -n -F 'createIconItem' -- src/main/java ; git grep -n -F 'getDisplayName(RoleInstance' -- src/main/java ; git grep -n -F 'getDisplayName(this)' -- src/main/java
git grep -n -F 'updateHotbar' -- src/main/java ; git grep -n -E '\bupdateSkillItemMeta|\bupdateAllSkillItemMeta' -- src/main/java ; git grep -n -F 'shouldUpdateHotbar' -- src/main/java
git grep -n -E 'HotbarRenderer|markDirty|createIconItem|getDisplayName\(RoleInstance' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent
git grep -n -c -E '^\s+(Self|EnergyPort|SanTEPort|VitalsPort|CooldownPort|BuffPort|FactionPort|DamagePort|TimerPort|ComponentLookup) ' -- src/main/java/com/shadowHunterRolesPlugin/core/ports/ComponentServices.java
git grep -n -F 'getEnergyCost' -- src/main/java/com/shadowHunterRolesPlugin/core/RoleInstance.java
git grep -n -F 'runRepeating' -- src/main/java/com/shadowHunterRolesPlugin/core/RoleInstance.java src/main/java/com/shadowHunterRolesPlugin/manager/BuffManager.java
git grep -n -F '%.1f' -- src/main/java ; git grep -n -F 'Locale' -- src/main/java/com/shadowHunterRolesPlugin/core
git grep -n -F '"main_weapon_id"' -- src/main/java ; git grep -n -F '"skill_id"' -- src/main/java
# 活文档（§7 / F-1）
git grep -n -E 'createIconItem|updateHotbar|markDirty|HotbarRenderer|shouldUpdateHotbar|updateSkillItemMeta' -- docs
git grep -n -E '骨架|接管渲染属 4\.4' -- src/main/java/com/shadowHunterRolesPlugin/core/RoleInstance.java
# 判据自检样本（after 态；§2.3 / F-2）
git grep -n -E '::updateHotbar\b' -- src/main/java ; git grep -n -F 'this::triggerUpdate' -- src/main/java
git grep -n -F 'updateHotbar' -- src/main/java/com/shadowHunterRolesPlugin/core/RoleInstance.java ; git grep -n -F 'hasCoolingSkill' -- src/main/java
git grep -n -F 'updateSkillItemMeta' -- src/main/java ; git grep -n -F 'hotbarItemOf' -- src/main/java
git grep -n -F 'BuffType.' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent
# 插件归因异常扫描（F-5；三份冻结件）
Select-String -Path debug-logs\测试记录\阶段5-起服日志-4.4*.log -Pattern 'generated an exception','Global task for ShadowHunterRolesPlugin','jar//com\.shadowHunterRolesPlugin','^\s*at com\.shadowHunterRolesPlugin' -CaseSensitive
```

**本件边界（重申）**：只读为主；**只写本件**；未改 `src`、未改任何既有证据与冻结件、零 git 写、未起服、未构建、未重建 jar、未申请提权；本件**不内嵌自身哈希**（身份按结卡报文现算）。
