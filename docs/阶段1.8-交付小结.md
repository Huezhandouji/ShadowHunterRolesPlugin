# 阶段 1.8 交付小结 —— 掉线即销毁角色实例（修 O-25 登出泄漏）

> **按 §10 裁决 12 执行（掉线即销毁）**：`PlayerQuitEvent` → 立即完整 `clearRole(uuid)`，语义与死亡一致（掉线 = 死亡 = `clearRole`）。
> ⚠️ **任务卡 t6 的旧文本已作废**：卡上写的"`config.yml` + 挂起/恢复 + 保留期 + 预实现 `pendingCleanup`"随"掉线即销毁"裁决**整体取消**（指南 §10 第 11、12 条；设计文档 §9.1 整节重写）。本阶段**未引入 `config.yml`、未做 `saveDefaultConfig()`、未挂起/未保留/无计时器**。

---

## 0. 一句话结论

**实现在 1 个文件里（`listener/PlayerListener.java`，+11 行），构建通过、起服达到 Mode B 合格线、数值与字符串多重集逐条不变、O-25 泄漏经**机器人运行实测**确认修复；此前被列为"环境不可测"的"quit 窗口内清除是否被持久化"一项，本轮拿到 `strong` 档位的实测证据（重登后最大生命 20、修饰符消失、药水清空）。**

---

## 1. 改动面（精确到行）

**变更面 = 1 个文件**（与本人开工前自取快照 `src-snapshots\t6-before`（44 个 `.java`）逐文件 SHA256 比对：`modified=1 / added=0 / deleted=0`）：

`src/main/java/com/shadowHunterRolesPlugin/listener/PlayerListener.java`

```java
// :10  新增 import
import org.bukkit.event.player.PlayerQuitEvent;

// :36-43  新增（唯一行为改动）
//阶段1.8（作者最终裁决 §10 第12条 / 设计文档 §9.1）：掉线即销毁角色实例 —— 与死亡同一条 clearRole 路径。
//不挂起、不保留、不引入 config.yml；不预实现 pendingCleanup（仅当实测证明 quit 窗口内的清除未被持久化才补）。
//效果：该实例的 ticker 与 BuffManager updater 两个 1-tick 任务被取消、playerRoleMap 中不再有条目（O-25）。
@EventHandler
public void onPlayerQuit(PlayerQuitEvent event){
    Player player = event.getPlayer();
    roleManager.clearRole(player.getUniqueId());
}
```

- 走的是**既有** `RoleManager.clearRole(UUID)`（`RoleManager.java:117-124`：`playerRoleMap.remove(uuid)` → `removed.clear()`），**未新增任何清理路径**；`playerRoleMap` 的条目在被移除**之后**才 `clear()`，因此"地图无条目"与"实例被清"两件事同时成立。
- 死亡路径（`:21-28`）与复活路径（`:30-34`）**未改动**；未碰 `RoleCommand`、未碰 `roleComponent/**`。
- **未新增数值/字符串字面量**（见 §4 多重集）。

---

## 2. 证据档位总表（**每项都标注档位**）

| # | 判据 | 档位 | 结果 | 证据位置 |
|---|---|---|---|---|
| 1 | 构建通过 + 产物新鲜 | **运行实测** | `BUILD SUCCESSFUL in 16s`；jar 99 659 B / mtime 22:36:34 / SHA256 `F94CDE71…1908`；最新 `.class` = `ShadowHunterRolesPlugin.class @22:36:33`；`src 新于 jar 的文件数 = 0` | `.scratch\bot\t6-build.log`、`runserver-identity.json` |
| 2 | 起服合格线（**Mode B**：三行 + 替代证据） | **运行实测** | 三行齐（见 §3）；**无 SHDF 行** | `t6-runserver-postchange-20260916-225507.log` |
| 3 | 无新增 ERROR/Exception | **运行实测（集合差）** | `ADDED = 0 / REMOVED = 0`，**C(OTHER) = 0** | `.scratch\bot\logdelta-t6.txt` |
| 4 | A 在线且有角色 | **运行实测** | `The current energy level of [QuitProbeBot] is: 100` | `t6-quit-persistence.result.json` |
| 5 | **B 掉线后 `playerRoleMap` 条目已移除**（O-25 核心） | **运行实测** | 重登后本人查询回 `QuitProbeBot has no role!` | 同上 |
| 6 | C 最大生命回到 20 且修饰符消失 | **运行实测** | 重登后 `maxHealthAttr.modifiers = []`、`health = 20` | 同上 |
| 7 | C' 药水清空 | **运行实测** | `effects = []` | 同上 |
| 8 | D 热键栏无残留 | **运行实测** | `hotbar = [null × 9]`、`inventory = []` | 同上 |
| 9 | **E quit 窗口内清除是否被持久化**（原"环境不可测项"） | **运行实测 / 强度 strong** | E 测得 **true**，`primedBeforeQuit = true`（掉线前确实存在药水：speed amp4 40t）→ **不再是 vacuous** | 同上 `E_evidenceStrength.label` |
| 10 | ticker 与 BuffManager updater 两个 1-tick 任务停止 | **静态证据**（+ 运行反证） | 见 §5；**运行期实例计数差分（jcmd）由 t7 独立执行，档位更高，冲突以 t7 为准** | §5 |
| 11 | 数值 + 字符串多重集不变 | **运行实测（比对）** | numeric `381/381 diff 0`；string `197/197 diff 0` | `.scratch\bot\numeric-*-t6.txt`、`string-*-t6.txt` |
| 12 | 未引入 `config.yml` | **静态（清单）** | `src/main/resources` 只有 `plugin.yml`；本阶段未新建/未改 | §6 |

**环境不可测项：本阶段不存在**（原唯一候选"quit 窗口持久化"已由机器人实测拿下 strong 档位；详见 §7 的失败分类与留痕规则）。

---

## 3. 起服取证（Mode B）

```
[22:37:18] [Server thread/INFO]: Starting Minecraft server on *:25566
[22:37:19] [Server thread/INFO]: [ShadowHunterRolesPlugin] Enabling ShadowHunterRolesPlugin v1.0.0
[22:37:19] [Server thread/INFO]: [ShadowHunterRolesPlugin] ShadowHunter Character System enabled.
[22:37:20] [Server thread/INFO]: Done (18.330s)! For help, type "help"
```

- **用了哪个模式：Mode B**（队长裁决：全项目不安装 SHDFGamePlugin，它是稳态）。**第 3 行 `[SHDFGamePlugin] Succeed to find the dependency …` 未出现，且不构成本阶段缺陷**——依据是**改动前基线**（队长指定锚点 `docs\阶段1-起服锚点-t2ModeB.log`，6 199 B / `2A4BD077…ADB524`）同样只有三行、`SHDFGamePlugin` 0 命中；更早的 9/13 日志（13 325 B / `FC5903DC…6CA72EA`）亦然。
- **Mode B 替代证据（定死三条）**：① 静态 `ShadowHunterRolesPlugin.java:43` = `Bukkit.getServicesManager().register(RoleAPI.class, roleAPI, this, ServicePriority.Normal);`（`:41` 构造 `RoleAPIImpl`，**:45 的 `ShadowHunter Character System enabled.` 在注册之后打印** → 第 2 行即"注册已执行"的运行证据）；② 运行 = 四行中的第 1、2 行（上面已给）；③ **待办（用户）**：真实四行只能在用户外服 25565 验证（那台装了 SHDFGamePlugin + AcceptRulesBeforeJoinPlugin，其 plugins 目录在沙箱外、新 jar 拷不进去）。
- **日志集合差**（`log-delta.ps1 -Anchor <t2 锚点> -Current <本次 latest.log>`，**正序**）：`ADDED = 0 / REMOVED = 0`；分类 `A(oshi) = 0 / B(世界状态) = 0 / **C(OTHER) = 0**` → 无新增异常。报告第 3 行显式打印方向 `anchor(pre) -> current(post)`。
- 收尾把本次 `latest.log` 另存为 `t6-runserver-postchange-20260916-225507.log`（9 786 B / SHA256 `0D0C5F3B…76A2`）—— 这是"起服前先另存带时间戳副本"纪律的一部分（9/13 那份就是被轮转掉后才发现需要人工副本的）。

---

## 4. 数值与字符串多重集（before = 本人开工前自取快照）

**基准选择**（队长 §6.2 规范）：**before 必须是与本任务对齐的自取副本**，**不能**用 `after-baseline`（t2/t5 夹在中间，diff 永不为 0 且原因错位）。
- before = `.scratch\bot\src-snapshots\t6-before`（44 个 `.java`，快照时刻 2026-09-16 22:35:38）
- after = 当前 `src`
- 口径：整树递归（**不用**指南附录 C 的旧通配 `$src\*\*.java,$src\*\*\*.java`——那会漏 `roleComponent/meiqiHezi/{mainWeapon,passive,skill}` 下 7 个文件）；`Sort-Object` 且**不用** `-Unique`；`Compare-Object`（计数感知）；**保留 `20f`/`1L` 后缀**。

| 口径 | before | after | diff rows |
|---|---|---|---|
| **数值字面量**（工具 `numeric-multiset.ps1 -Mode numeric`） | 381 | 381 | **0** |
| **字符串字面量**（`-Mode string`，本阶段按 §6.2 新增要求执行） | 197 | 197 | **0** |

**按 §5 阶段 1 数值判据最终版（队长四条）逐条对答：**

1. **新增方向必须为 0**：✅ **新增 = 0**（两个口径都是 `after` 侧无任何 `=>` 行）。
2. **下降必须逐条归属，只允许两类**（1.6 删死代码含存活文件内死方法 / 1.7 调用点塌缩、声明处不变）：**本阶段下降 = 0**，故**无归属项**。
3. **值不变核验**（附录 A 冻结值在声明处逐字未变）：本阶段未触碰任何组件/角色声明处（改动面仅 1 个 listener 文件），声明处逐字未变。
4. **小结必给 before/after/diff rows + 差异原文 + 归属 + 声明值位置**：before/after/diff rows 见上表；**差异原文 = 空**（diff rows = 0）；归属 = 不适用；声明值位置 = 不适用（未改动声明处）。
5. **两个数**：**原始 diff = 0**；**剔除已申报项后 diff = 0**（本阶段无已申报项 → 两个数相等）。

> 说明：`387/47 文件`是**阶段 0 结束**时的经验值；t5 删 3 个死代码类 + O-13 字面量→声明值后为 `381/44`。本阶段**以自取快照为基准**，故 diff 为 0 是"本任务自身数值中性"的直接证明，不与 t5 的已申报差异混淆。

---

## 5. "两个 1-tick 任务停止"——静态证据 + 运行反证（**档位：静态证据**）

> 队长裁决：t6 交静态档位，并显式注明"运行期实例计数差分由 t7 独立执行、档位更高，冲突以 t7 为准"。

**静态调用链**（本插件在 quit 时确实会执行到，因为 handler 直接调 `clearRole`）：

1. `listener/PlayerListener.java:40-43` `onPlayerQuit` → `roleManager.clearRole(player.getUniqueId())`
2. `manager/RoleManager.java:117-124` `clearRole(UUID)`：
   ```java
   public boolean clearRole(UUID uuid){
       RoleInstance removed = playerRoleMap.remove(uuid);
       if(removed != null){
           removed.clear();
   ```
3. `core/RoleInstance.java:777-796` `clear()`（节选）：
   ```java
   triggerLifecycleStop();
   Bukkit.getScheduler().cancelTask(updateTaskId);   // ← ticker（1-tick）取消
   clearHotbar();
   player.getAttribute(Attribute.MAX_HEALTH).removeModifier(roleHealthModifierKey);
   player.getActivePotionEffects().forEach(...removePotionEffect...);
   buffManager.clearAll();                            // ← updater 取消发生在这里
   ```
4. `manager/BuffManager.java:162-170` `clearAll()`：
   ```java
   if(updaterTaskId != -1){
       Bukkit.getScheduler().cancelTask(updaterTaskId);   // ← updater（1-tick）取消
       updaterTaskId = -1;
   ```
   → 两个 1-tick 任务的取消**都在 `clear()` 的同步调用链内**，而 `clear()` 由 quit 事件触发。

**运行反证**：本次起服的日志集合差为 `ADDED = 0 / REMOVED = 0 / C = 0`（无新增异常），且重登后客户端读数显示该玩家的角色态已被完全回收（§2 第 5-8 项）。

**未做/不做**：`jcmd`/`jps` 在本沙箱**无法 attach**（实测：`jcmd -l` 空输出；`jcmd <pid> help` → `java.io.IOException: 拒绝访问。` @ `sun.tools.attach.VirtualMachineImpl.openProcess`；`jps -l` 空；eng-验证 的决定性对照显示"连沙箱内自派生的存活子 JVM 也 attach 被拒、且子 JVM 连 perfdata 都未建立" → **结构性环境限制**）。故实例计数差分**归 t7**（其档位更高，冲突以 t7 为准），**不在本小结宣称运行实测**。

---

## 6. 跨插件明文约定（落实指南 §10 第 2 条；已由队长并入指南 §6.4.1）

本阶段发现的契约事实（只读勘察所得，已写入指南 §6.4.1）：

| 场景 | 重连后实际表现 | 依据 |
|---|---|---|
| **本插件单独运行**（Mode B `runServer` 即该环境，也是本小结的实测语境） | **没有角色**，需重新选 —— 与设计文档 §9.1 一致 | 本小结 §2 第 5 项（运行实测） |
| SHDF **对局中 ≤60s** 重连 | 下游 `PlayerStatus` 保留 `selectedRoleId` → `restoreCombatant → applyRole → RoleBridge.setPlayerRole`，**角色被下游重建为一个新实例**（旧实例确实已销毁；运行时状态归零：冷却/SanTE/流血层数/引导标志都不保留） | `PlayingPhase.java:174-193/:200-219`、`DeploymentController.java:300/:334`、`config.yml:42 = 1200t` |
| SHDF 对局中 **>60s** | 双方都清（我侧早已清，下游那次 `clearPlayerRole` 成空操作） | 同上 + `RoleBridge.java:110-118` |
| SHDF **角色选择阶段** | 退出即 `setSelectedRoleId(null)` → 重连必须重选 | `RoleSelectingPhase.java:719` |
| SHDF 显示层 | 断线窗口内仍用旧 `selectedRoleId` 显示角色名（而实例已销毁） | `MatchDisplayBridge.java:360` |

**明文约定一句话**：`ShadowHunterRolesPlugin` 的契约是「掉线 = 角色实例立即销毁」；**对局内跨断线的角色延续由 `SHDFGamePlugin` 自己负责**（保留 `selectedRoleId` 并在重连时重新 `setPlayerRole`）。**两插件不得互相假设对方保留了状态。** t7/t15 **不得**在集成环境按 I-16 字面判"裁决未生效"。

**未核实风险（开放项，按队长裁决不为它额外花时间）**：若将来出现 `maxHP ≠ 40` 的角色，quit 后残留的 `role_health_modifier`（amount 不同）是否会被 `removeModifier` 正确移除，取决于 `AttributeModifier.equals` 的语义 —— **本轮未核实**。今天两个角色 `maxHP` 都是 40、amount 相同，故幂等；本阶段实测亦确认修饰符被正确清除（§2 第 6 项）。

---

## 7. 环境与工装（诚实记录，含我自己抓到的仪器缺陷）

**仪器版本（本次实测由下列版本产出；均已两处归档）**

| 用途 | 版本身份 | 受管副本 |
|---|---|---|
| 探针 | `t6-quit-persistence.js` **22 223 B / 22:54:17 / SHA256 `EFDBF9EB95949FAD73A63DAC9F3216111BFF87FCAA8BD0B9E48617C1A84C9770`** | `docs\回滚快照\工装\t6-quit-persistence-EFDBF9EB-22223.js` |
| 数值/字符串多重集 | `numeric-multiset.ps1` 5 035 B / `584537DD…5A2E` | `…\工装\numeric-multiset-584537DD-5035.ps1` |
| 日志集合差 | `log-delta.ps1` 4 588 B / `4A5A88B0…082C` | `…\工装\log-delta-4A5A88B0-4588.ps1` |
| 环境身份 | `collect-runserver-identity.ps1` 6 633 B / `26E5A75F…1DFF` | `…\工装\collect-runserver-identity-26E5A75F-6633.ps1` |
| 开工前快照 | `snapshot-src.ps1` 2 438 B / `21A19061…64D3` | `…\工装\snapshot-src-21A19061-2438.ps1` |
| 准入自检 | `check-instruments.ps1` 4 166 B / `CAB0BDD6…6D64`（`failures = 0`） | `…\工装\check-instruments-CAB0BDD6-4166.ps1` |

- **结果与仪器绑定**：`t6-quit-persistence.result.json` 内 `meta.scriptIdentity.sha256 = EFDBF9EB…9770`（脚本运行时自算自身哈希），`meta.probedAt = 2026-09-16T14:54:31.156Z`（UTC；本地 22:54:31），`meta.host = 127.0.0.1:25566`，`meta.runArgs` 6 项齐全。
- **环境身份**：jar = `build\libs\ShadowHunterRolesPlugin-1.0.0.jar` 99 659 B / mtime 22:36:34 / SHA256 `F94CDE71…1908`；构建时刻 22:36:34（jar mtime）、**起服时刻 22:37:18**、`srcNewerThanJar = 0`；`run\plugins` 目录条目 `[".paper-remapped","bStats","ShadowHunterRolesPlugin","spark"]`、`pluginJars = []`；`server.properties` 锚点 `online-mode=false / server-port=25566 / gamemode=creative`；服务器收尾**已停**（25566 无监听）。
- **我自己抓到的仪器缺陷（F-3，诚实记录）**：首跑时 `snapshot().hasRoleHealthModifier` 只读 `key/name`，而 mineflayer 把修饰符标识放在 **`uuid`** 字段（实测 JSON：`{"uuid":"shadowhunterrolesplugin:role_health_modifier",…}`）→ 该判定**恒为 false**，会让 `C_persistence_modifierGone` **无论修饰符是否残留都为真（假阳性）**。发现后已修（三字段都读）**并加进 `--selftest`**（`modifier_detected_when_present_uuid` / `modifier_absent_when_cleared`），**随后重跑探针**；本小结引用的结果为修复版（`EFDBF9EB…`）产出，**首跑结果已作废**。

**F-3 的独立复现与一条方法论结论（eng-验证 复核确认，值得单独记）**：
- 独立复现：`node --check` exit 0；**他自跑 `--selftest` → 14/14 true、exit 0**；与旧版 `5E7CABED…` 的差异 **12 行且全部限于 F-3**（`:145 m.uuid || m.key || m.name`、两个新 stub、`:331/:332` 两条新断言、注释），其余接线（三个常量、A2 仅原始记录、六判据、`computeExitCode` 0/2/3/4、强度字段）**未回退**。
- **他同时如实报告：F-3 在他 PASS 过的旧版里已经存在，他没抓到** —— 原因是旧版自检的 stub 用的字段是 `key`/`name`，**与被测代码的错误假设同源**，因此 14 项自检里没有一项能暴露它。
- **方法论结论（建议进 §6.6）**：**自检/单测的 stub 必须来自"实测报文"，不得沿用被测代码的字段假设** —— 否则自检只是把 bug 认证一遍。本阶段这次是靠**排查实测报文的 JSON 字段**（`maxHealthAttr.modifiers[].uuid`）才发现的；它是**实测驱动的缺陷发现**，不是"顺手改的"。
- **工装性质与清理状态**：`.scratch\bot\`（工作区根，**不在插件仓库内**）= **测试工装，非产品代码**，不参与构建与交付；队长显式授权。**保留中**（状态 = 保留，未清理）。仓库内另有**受管副本区** `docs\回滚快照\工装\`（7 件内容寻址副本 + `MANIFEST.txt`，逐条复算 `ok=7 bad=0`），仅存放被引用的仪器版本，不参与构建。
- **宿主环境导致的失败分类（§6.6 三分类，如实留痕）**：本轮两类③（外部拦截）——① npm 默认缓存目录在工作区外 → `EPERM`（改 `--cache` 指向工作区内即解决，**非杀毒**）；② `jcmd/jps` 无法 attach（**结构性环境限制**，不构成豁免）。均已在 `.scratch\bot\README-工装说明.md` 明确记录，**未写成"能力不可用"**。
- **执行纪律**：`runServer` **自起自停**（不跨任务复用）；采样时 **ObserverBot 先上线并全程在线**，**未落入空服早退分支**（下游 `PlayingPhase.java:205-209 isServerEmptyExcept`、`RoleSelectingPhase.java:711-714 isNoOtherPlayerOnline`；Mode B 的 run 服本就未装 SHDFGamePlugin，该分支在本环境不存在）；**未申请提权**；**未做任何 git 写操作**（提交由队长统一做）。

---

## 8. 与验收清单（指南 §7 阶段 1）的对照

| §7 阶段 1 条目 | 本阶段状态 |
|---|---|
| 掉线（`PlayerQuitEvent`）→ 立即完整 `clearRole(uuid)`：两个 1-tick 任务被取消、`playerRoleMap` 无条目（I-16） | ✅ 条目移除 = **运行实测**（判据 B）；任务取消 = **静态证据**（+ 运行反证），差分归 t7 |
| 重新登录 → **没有角色**（需重新选）；热键栏无残留物品 | ✅ **运行实测**（判据 B / D） |
| 实测确认 quit 事件内 `clear()` 的清除被**持久化**：重登后最大生命 20、无残留药水 | ✅ **运行实测，强度 strong**（判据 C / C' / E；掉线前确有 speed amp4 40t，故 E 非 vacuous） |
| **不引入 `config.yml`** | ✅ 未引入（若将来发现该文件，属偏差） |
| 数值多重集 | ✅ numeric `381/381 diff 0` + string `197/197 diff 0`（以本任务自取快照为基准） |
| O-25 关闭 | ✅ 根因（quit 无处理 → 实例永久驻留）已由本阶段唯一改动消除，并经运行实测确认 |

---

## 9. 给 t7 的复现指令（最小集）

```powershell
cd C:\Users\ROG\Desktop\插件\.scratch\bot
powershell -NoProfile -ExecutionPolicy Bypass -File .\check-instruments.ps1     # 准入：exit 0 / failures=0
node t6-quit-persistence.js --selftest                                          # 仪器自检：exit 0（勿截断 stdout）
node -e "const j=JSON.parse(require('fs').readFileSync('t6-quit-persistence.result.json','utf8'));console.log(j.meta.scriptIdentity.sha256, j.meta.probedAt, JSON.stringify(j.verdict), j.E_evidenceStrength.label)"
# 期望：EFDBF9EB…9770 / 2026-09-16T14:54:31.156Z / 六项全 true / 强度 strong
```
