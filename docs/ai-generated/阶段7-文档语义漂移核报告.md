# 阶段7 · 文档语义漂移核报告（只记录不修）

> **本件的职责**：核 `t23`（渲染器所有权搬进组件）· `t24`（`RoleInstance` 不再持有组件）· `t27`（平台侧只走写入路径、双触发收敛）三处**结构改动**是否使**已有文档陈述失效**，逐条给出可复核的判定与证据。
> **本件不修任何文档、不改任何源码**：`docs/` 下既有文件与 `src/` 全部零改动（`git status --porcelain` 只多出本件一个路径，见 §7）。
> **写作时点**：分支 `hue` · HEAD **`70bcee0`** · 未推 40 · 工作区当时有 `t4` 在飞（`porcelain` = 6 件，均在 `core/**` + `listener/hook/**`）。
> ★ **核本卡期间 `t4` 已落地**：新 HEAD = **`ba06ab8`**（`docs(core,listener): clear residual process markers…`）· 未推 41。已复核（§11.1）：`RoleInstance` 在 `ba06ab8` 与 `70bcee0` **同为 1134 行**，本件引用的**全部锚点逐行一致** ⇒ 本件行号读数**对新 HEAD 仍然成立**。

---

## 0. 方法论（口径原文 + 排除规则 + 为什么这样取数）

### 0.1 行号读数锚点 = **HEAD `70bcee0`**，不取在飞的工作区

- 本轮写作时 `t4`（注释残余清算，inScope = `core/` + `listener/`）**正在进行**、`RoleInstance.java` 就在其改动范围内。
- 按 §7 纪律 2（**绝不在突变在飞时取基线**）：本件所有 `src/` 行号一律用 `git -C ShadowHunterRoles show HEAD:<path>` 逐行读盘取 —— 可复现、且不随在飞改动抖动。
- `RoleInstance` 在 `70bcee0` 与 `ba06ab8`（`t4` 落地后）**同为 1134 行**、本件全部锚点逐行一致（§11.1）⇒ **本件的 `RoleInstance` 行号读数对新 HEAD 仍然成立**。
- ★ 但 **`t6` 还要删一对成员（`markHotbarDirty` + `requestHotbarRepaint`）**、`t7` 要去类字面量并合并 id 常量 ⇒ 之后行号**必定再漂**；`t5`（`roleComponent/**` 注释原地改写）预期不位移、**但不得假设**。⇒ **后续修卡必须按【内容锚点】定位，不得照抄行号**。

### 0.2 判定三类（本件每条只归一类，正交处在小注里写）

| 类 | 名称 | 判据 |
|---|---|---|
| ① | **行为面失效** | 会**误导使用者**（按文档操作/按文档写代码会得到错结果，或文档自称现算的读数已不成立） |
| ② | **位置面失效** | 说法仍对，但 **路径 / 行号** 已漂（含行号越界、目录搬迁） |
| ③ | **语义面失效** | **说法与代码不符**（成员已搬/已删、"谁持有/谁注册"说反、数值口径失效） |

### 0.3 计数口径（报告任何计数都必须给"模式原文 + 排除规则"）

- **靶词模式原文**：`core/hotbar` · `core\.hotbar` · `broadcastChange` · `HotbarRenderer` · `inv\.setItem\(` · `notifyPlatform` · `R-[1-8]` · `\.\./debug-logs`（全部 **`-cmatch` 大小写敏感**，**无词界**）。
- **行号引用模式原文**：显式 `([A-Za-z0-9_]+\.java):([0-9]+)` · 裸续引 `(?<!\.java):([0-9]+)`（裸续引项须"本行无显式引用"，即 t8 的**规则 B**）。
- **读取通道**：`[System.IO.File]::ReadAllLines(<绝对路径>)` 逐行读盘（PowerShell 全语言）。★ **`debug-logs/` 下的件一律不得用 `git grep`** —— 该目录被库内 `.gitignore:47`（`debug-logs`）+ `:85`（`debug-logs/**`）覆盖，`git grep` 对未跟踪件**恒空**（本件 §6 附实测）。
- ★ **禁用 `Get-Content` 取行 / 数行**（含多字节 CJK 且无 BOM 的 UTF-8 上会**静默打坏**行数，不报错）：本件已核过的两例 —— `roleComponent/frameworkLevel/ServiceComponents.java` 权威 **171 行**而 `Get-Content` 报 **148**；`debug-logs/测试记录/阶段13-手册引用维护约定.md` 权威 **40 行**而 `Get-Content` 报 **29**。本件全部读数走 `[IO.File]::ReadAllLines()`（权威）或 `Select-String` 的 `LineNumber`（准）。
- ★ **行数必须写清口径**：「**全文行数**」（含空行/注释，本件一律用这个）与「**非空内容行数**」是**两个数**（同上例 40 vs 29）。本件说"某件 N 行"一律指**全文行数**。

---

## 1. 结论摘要（直接回答卡面最后一问）

1. **`t23`/`t24`/`t27` 三处结构改动确实使文档陈述失效**，不是"文档全都还成立"：
   - `t23`（渲染器所有权）：把**脏标记**从渲染器搬进了组件 ⇒ 手册 §5.5 的"置脏 / 清脏 = 渲染器"那一格**说法已错**（③）；`core/hotbar/` 三件随 `t53` 迁到 `roleComponent/frameworkLevel/hotbar/` ⇒ 手册 **13 行**路径失效（②）。
   - `t24`（不持有组件）：`RoleInstance` 现算 `HotbarRenderer` **0 命中**、无组件字段 ✓ 与交接文档一致；但由此**未**产生新的文档失效（AI 向文档"聚合根"称谓问题**先于**本卡存在，见 ③-1）。
   - `t27`（双触发收敛）：`broadcastChange` **全 src 0 命中** ⇒ 手册 §5.2 的"订阅登记（唯一通道）"那一格引用**已不存在的成员**（③），其来源是 `debug-logs` 的历史读数（见 §6）。
2. **失效项该由哪张卡修**：见 §1.1。本卡只落记录，不代修。
3. **四条"已坐实项"里第 ④ 条在当前树**不再成立**（`../debug-logs/…` 已不存在），本件**如实改判**并附现算证据，见 §5.4。

### 1.1 失效项 → 修卡的归属建议

| 失效面 | 件 | 建议承接卡 |
|---|---|---|
| 手册的**路径**（`core/hotbar/` → `roleComponent/frameworkLevel/hotbar/`）与**行号** | `docs/ai-generated/使用者手册.md` | **手册符号化车道**（`t10`/`t20` 的后续片）+ 需一张**显式路径修订片**（符号化不覆盖目录搬迁） |
| 手册 §5.5 "置脏/清脏在渲染器"、"`broadcastChange`"等**说法** | 同上 | 需一张**手册内容修订卡**（不可由符号化替代） |
| AI 向文档的现算基线（`11 包`/`16 件`/`33 件`/`3 行`/`git grep = 0`/`一张冷却表`/`聚合根`称谓） | `docs/ai-generated/{README,架构总览,组件模型}.md` | 需**新立「AI 向文档现算基线同步」卡**（现板面无此卡；这三件不属手册车道） |
| `SKILL.md` 与 `如何新增一个角色.md` 的**基线 91** | `docs/ai-generated/skills/create-role/SKILL.md` · `如何新增一个角色.md` | 同上（新卡） |
| 方案件口径数字（`189`/`293`）与"非 `.java` 源 6 行" | `debug-logs/测试记录/阶段13-手册引用维护约定.md` | ★ **核本卡期间已被原地更正**（该件由 40 行扩写为 **264 行**，新增 §4「读数更正声明」`:47-58` 与 §5「不可符号化清单」`:44-126`）⇒ 本条**无需新卡**，只需在本件留档（见 §6.2） |
| `debug-logs` 历史读数被当"现状来源"引用 | `阶段13-派发边界-运行级读数.md` 等 | 文档车道加一行**"历史读数，勿当现状来源"**标注（本卡只记录） |
| `src` 内注释的路径漂（`IconState` 的 `core/Skill`） | `roleComponent/frameworkLevel/hotbar/IconState.java:10` | **`t4`/`t5`/`t6`/`t7`** 注释车道 |
| 交接文档的 `serviceComponents/` 子目录写法 | `docs/ai-generated/交接-下一上下文.md:62/:170` | 队长维护（本卡只记录） |

---

## 2. ① 行为面失效（会误导使用者）

| # | 陈述原文（摘引） | 出处 | 现算真值 | 判定 | 证据 |
|---|---|---|---|---|---|
| ①-1 | 「**单一冷却命名空间**：**一张表**，键 = 组件 id；启动冷却的入口返回"是否真的写了表"」 | `组件模型.md:28`（同义重复：`:66`） | **不存在任何框架侧冷却表**：冷却状态是**每个组件实例的字段** `ActiveComponent:74 cooldownUntilTick`，框架既不登记也不派发 | ① 行为面 | `RoleInstance` 现算无冷却表成员；`ActiveComponent.java:43` 注释原文「每实例一份 `cooldownUntilTick`（同 id 的两个实例各自独立），框架既不登记、也不派发」；`ActiveComponent:74` 字段、`:156/:170/:180/:193/:197` 全在组件内 |
| ①-2 | 「`src/test` … **基线 91 不许降**」 | `skills/create-role/SKILL.md:35` | 现算 **`@Test` = 117**（测试件 **17**） | ① 行为面 | `@Test` 计数（`src/test/**/*.java` 逐行读盘）= 117；件数 = 17。角色作者按 91 判回归会**漏判 26 个测试** |
| ①-3 | 「基线 **91** ✓」 | `如何新增一个角色.md:69` | 同上 **117** | ① 行为面 | 同上（两处同源） |
| ①-4 | 「**唯一物品写点** ｜ 帧末 flush（`inv.setItem(` 现算 **3 行** = 真写点 1 + 清栏位 2）」 | `架构总览.md:28` | `inv\.setItem\(` 全库现算 **2 行** = 真写点 1（`HotbarRenderer.java:136`）+ 清栏位 1（`HotbarItems.java:40`） | ① 行为面 | 该件自称"现算"并给出自检命令 `git grep -n 'inv.setItem(' -- src/main/java` ⇒ 一跑即与文档不符 |
| ①-5 | 「**`R-1…R-8` 这套判据编号在本仓库文件内不存在**：`git grep -nE 'R-[1-8]' -- debug-logs docs` = **0 命中**」 | `README.md:33`（同义重复：`组件模型.md:23`） | **实测 19 命中**（全在 `docs`：`SKILL.md:50/:51/:69/:98` 的 `R-1/R-4/R-6/R-7/R-8` + 该行自身）；且 `debug-logs` 半边**恒空**（被 `.gitignore:47/:85` 覆盖，`git grep` 不看未跟踪件）⇒ 该判据**不可复核且结论错** | ① 行为面 | `git grep -nE 'R-[1-8]' -- debug-logs docs` → exit 0 / 19 行；`git grep … -- docs` 同样 19 行；PowerShell 逐行读盘 `debug-logs`（`*.md`/`*.txt`）= **331 行 / 25 件** |
| ①-6 | 「**顶层 11 包** … `event` ｜ 平台事件类型」 | `架构总览.md:4` + `:55` · `组件模型.md:49` · `README.md:4` | 现算 **10 包**：`api · command · config · core · internal · listener · manager · platform · registry · roleComponent`；**`event/` 目录已不存在**（`t16` 删 Bukkit 事件层） | ① 行为面 | `Get-ChildItem … -Directory` 计数 = 10；`Test-Path …/event` = **False**；手册 §5.1:326「本插件**不再发布 Bukkit 事件**」✓ 与此互证 |
| ①-7 | 「`core/` **16 件** · `roleComponent/` **33 件** = 根 6 + `base/` 3 + `builtin/` 4 + `custom/meiqiHezi/` 5 + `custom/red/` 6 + `frameworkLevel/` 8」 | `架构总览.md:4` · `组件模型.md:50` · `README.md:4` | 现算 `core/` **13 件**（`t53` 把 `core/hotbar/` 3 件搬出 ⇒ 16−3）；`roleComponent/` **38 件** = 根 **8** + `base/`3 + `builtin/`4 + `custom/meiqiHezi/`5 + `custom/red/`6 + `frameworkLevel/` **9** + **`frameworkLevel/hotbar/` 3** | ① 行为面 | 逐目录 `-Recurse -File -Filter *.java` 计数；★ 附带发现：该件自身分项和 = **32 ≠ 33**（内部已不一致） |

> **① 类处置建议**（逐条 → 承接卡见 §1.1）：①-1 建议在 `组件模型.md` 判据表里把"单一冷却命名空间"改写成**现实现口径**（"冷却归组件实例自持，框架不登记不派发"，并与 `ActiveComponent` 的行位对齐）；①-2/①-3 建议把"基线 91"改成**现算 117（测试件 17）**并注明取数命令；①-4①-5①-6①-7 属**同一张卡**的活：给三件 AI 向文档做一次"**现算基线同步**"（包数 / 件数 / 写点数 / 自检命令结论 / 冷却口径 / 聚合根称谓），并落实该目录自己的 §0 口径「**与代码冲突时以代码为准并就地修正**」。
> ★ 本卡**只记录**：上述四件（`README.md`/`架构总览.md`/`组件模型.md`）+ 两份角色指南**都不在**本卡 inScope，本卡不动它们。

---

## 3. ② 位置面失效（说法仍对，仅路径 / 行号漂了）

> 全部以 `git show HEAD:70bcee0` 读盘现算；`RoleInstance` 行号见 §0.1 的时效声明。

| # | 陈述原文（摘引） | 出处 | 现算真值 | 判定 | 证据 |
|---|---|---|---|---|---|
| ②-1 | 「**装配顺序 = 渲染顺序 = 派发顺序**（同一份 `LinkedHashMap` 遍历，来源：`core/RoleInstance.java:524-539`、`core/hotbar/HotbarRenderer.java:150-170`）」 | `使用者手册.md:230-231` | 装配序现于 `RoleInstance.java:478-502`（`initComponents`）；`core/hotbar/HotbarRenderer.java` **不存在**，且新件 `roleComponent/frameworkLevel/hotbar/HotbarRenderer.java` 只有 **140 行** ⇒ `:150-170` **越界不存在** | ② 位置面（说法另有 ③-2） | `RoleInstance` @HEAD `private void initComponents` = `:478`；`HotbarRenderer.java` 行数 = 140 |
| ②-2 | 材质映射「`core/hotbar/IconState.java:42-48`」 | `使用者手册.md:322` | 现于 `roleComponent/frameworkLevel/hotbar/IconState.java:42-48`（**行区间逐字仍准**：`:44` READY · `:45` BARRIER · `:46` STRUCTURE_VOID） | ② 位置面 | `IconState.java` = 49 行；`:42 public Material material(` · `:44/:45/:46` 三态 |
| ②-3 | 「判定顺序固定 = 冷却 → 禁用 → 能量不足 → 就绪（来源：`core/hotbar/IconState.java:25-36`）」 | `使用者手册.md:439` | 现于 `…/hotbar/IconState.java:25-36`（**行区间逐字仍准**：`:26/:29/:32/:35` 四个返回） | ② 位置面 | `IconState.java:25 public static IconState of(` · `:26/:29/:32/:35` |
| ②-4 | 状态表「`IconState.java:46`」（短路径） | `使用者手册.md:443` | 真值不变（`:46` COOLDOWN/ENERGY_LACK → STRUCTURE_VOID），但**短路径未按 §0 约定补全**（`iconState` 已不在 `core/hotbar/`） | ② 位置面 | 手册 §0:7-9 自定"短路径以仓库根 + 包路径"；现需写 `roleComponent/frameworkLevel/hotbar/IconState.java` |
| ②-5 | 状态表「`IconState.java:45`」 | `使用者手册.md:444` · `:528` | 真值不变（`:45` DISABLED → BARRIER） | ② 位置面 | 同上 |
| ②-6 | 状态表「`IconState.java:46`」+「`MainWeapon.java:172-174`」 | `使用者手册.md:445` | `IconState:46` 真值不变；`MainWeapon.java` 的 `ENERGY_LACK` case 现于 **`:169-171`**（`:172` 已是 `}`） | ② 位置面 | `MainWeapon.java:169 case ENERGY_LACK -> {` · `:170` 注释 · `:171 }` |
| ②-7 | 状态表「`IconState.java:44`」+「`Skill.java:193-196`」+「`MainWeapon.java:168-171`」 | `使用者手册.md:446` | `IconState:44` 真值不变；`Skill is ready.` 现于 `Skill.java:192`（区间应为 `:192-194`）；`MainWeapon is ready.` 现于 `MainWeapon.java:167` | ② 位置面 | `Skill.java:192` · `MainWeapon.java:167` |
| ②-8 | 「**唯一写点 = 渲染器的那一次槽位写入** ｜ `core/hotbar/HotbarRenderer.java:145-173`（写入在 `:168`）」 | `使用者手册.md:472` | 现件 **140 行**：渲染入口 `:115 public Result render(Player player, Map<Integer, ItemStack> lastRendered)`、**写入点 `:136 inv.setItem(`** | ② 位置面 | `HotbarRenderer.java:115/:136`；全库 `inv.setItem(` = 2 行 ⇒ 写点唯一性本身 ✓ 未破 |
| ②-9 | 「帧末刷新（判脏 → 写 → 清脏，顺序不可换）｜ `core/RoleInstance.java:914-929`」 | `使用者手册.md:473`（同址重复：`:457`、`:596`） | 现于 **`RoleInstance.java:846-857`**（`:850` 调 `HotbarRenderComponent::flush`、`:852` 注释、`:855` 调 `consumeChanged()`） | ② 位置面 | @HEAD `:846`=帧末 flush 注释 · `:850` flush 调用 · `:855` consumeChanged |
| ②-10 | 「入口条件之一："有占栏位组件在冷却且外观依赖活状态"｜ `core/RoleInstance.java:964-974`」 | `使用者手册.md:474` | 现 `:964-974` 是 `guardedCall` 的 javadoc/日志区；该入口条件现于 **`HotbarRenderComponent.java:196-203`**（判据）+ **`:330 dependsOnLiveStateOf`** | ② 位置面 | @HEAD `:964-974` 逐行读盘；`HotbarRenderComponent.java:196/:203/:330` |
| ②-11 | 「把物品拿在**主手**（攻击与施法读的是主手，来源：`core/RoleInstance.java:474-476`）」 | `使用者手册.md:503` | 现 `:474-476` 是 `initComponents` 的 javadoc；主手读取现于 **`:425-427`**（`handleCast` 内） | ② 位置面 | @HEAD `:425 ItemStack item = caster.getInventory().getItemInMainHand();` |
| ②-12 | 「框架侧只**转问**组件（不持表）｜ `core/RoleInstance.java:565-568`（技能就绪）、`:619-622`（主武器就绪）」 | `使用者手册.md:430` | 现 **`:519-522`**（`isSkillReady`）与 **`:569-572`**（`isMainWeaponReady`） | ② 位置面 | @HEAD 两处方法签名逐行读盘 |
| ②-13 | 「清角色 / 死亡 / 重生会清掉热键栏里的这些物品（来源：`core/RoleInstance.java:684-693`（静态清除））」 | `使用者手册.md:466` | 现 `RoleInstance.java:1121` 调 `HotbarItems.clearFrom(player)`；`clearFrom` 本体在 **`roleComponent/HotbarItems.java:34`** | ② 位置面 | @HEAD `:1121`；`HotbarItems.java` = 44 行、`clearFrom` = `:34` |
| ②-14 | 「SanTE … 作为框架服务组件 id `sante` 登记在实例容器里（来源：`core/RoleInstance.java:87`（id 常量）、`:369-376`（登记））」 | `使用者手册.md:543-544` | id 常量现于 **`:70`**（`SERVICE_ID_SANTE = "sante"`）；登记现于 **`:295-301`**（`registerServiceComponents`） | ② 位置面 | @HEAD `:70` · `:295`（方法签名）· `:301`（方法末行） |
| ②-15 | 「框架派发（真变化才派发 + 重入护栏）｜ `core/RoleInstance.java:789`、`:833`」 | `使用者手册.md:369` | 现 **`:757`**（`dispatchSanTEChange`）与 **`:803`**（`broadcastSanTEChange`）；接线在 **`:175`** | ② 位置面 | @HEAD `:757` · `:803` · `:175 change -> dispatchSanTEChange(…` |
| ②-16 | §8 索引「`core/RoleInstance.java` ｜ 86-96、189-203、369-386、471-515、524-539、565-568、619-622、684-693、833-861、880-890、893-931、964-974」 | `使用者手册.md:644` | **12 段全部失效**（抽查：id 常量现 `:68-79`；变更接点现 `:168-175`；登记现 `:295-301`；施放/攻击入口现 `:422-469`；装配序现 `:478-502`；就绪转问现 `:519-522`/`:569-572`；静态清除现 `:1121`；派发+flush 现 `:757-857`；隔离设施现 `:881-980`） | ② 位置面 | @HEAD 逐段读盘（各锚点见上表同行） |
| ②-17 | §8 索引「`core/hotbar/HotbarRenderer.java` ｜ 117-119、125-128、145-173」·「`core/hotbar/IconState.java` ｜ 25-36、42-48」 | `使用者手册.md:648-649` | 目录不存在；`HotbarRenderer` 现 **140 行**（`117-119`/`125-128` 现为 `differsFromLast`/`contentDiffers` 区；`145-173` **越界**）· `IconState` 现 49 行、两个区间仍准 | ② 位置面 | 两件行数 + 逐行读盘 |
| ②-18 | 实现位置表「`roleComponent/frameworkLevel/EnergyComponent.java:176-182`（真值+上限）、`:185-190`（clamp，第 188 行 `Math.clamp`）、`:192-202`、`:204-207`/`:209-212`、`:116`（`addListener`）、`:168-172`（`notifyListeners`）、`:216-262`（grammar）」 | `使用者手册.md:333-339`（同址重复：`:657`、`:681`） | 现算：`addListener` **`:112`** · `notifyListeners` **`:164`** · `current()` **`:172`** · `set(int)` **`:182`** · `Math.clamp` **`:184`** · `increase` **`:201`** · `decrease` **`:206`** | ② 位置面 | `EnergyComponent.java` = 269 行，逐行读盘；⇒ 该表整体偏移约 **−4 ~ +4 行**，无一段可照抄 |
| ②-19 | 实现位置表「`SanTEComponent.java:182`（上限）、`:187-192`（clamp 写入）、`:222-229`（增减）、`:96`（`addListener`）、`:113`（`removeListener`）」 | `使用者手册.md:366-368`（同址重复：`:658`） | 现算：上限字段 **`:166`** · `set(int)` **`:189`** · `Math.clamp` **`:191`** · `addListener` **`:98`** · `removeListener` **`:115`** · `forEachListener` **`:136`** | ② 位置面（`:217 broadcastChange` 另有 ③-4） | `SanTEComponent.java` = 262 行，逐行读盘 |
| ②-20 | 「`HotbarRenderComponent.java:87`（`requestRepaint`）、`:172`（`buildItemOf`）、`:192`（`dependsOnLiveStateOf`）」 | `使用者手册.md:476`（同址重复：`:659`） | 现算：`requestRepaint()` **`:225`**（其体 `:227 repaintSink.markDirty()`）· `buildItemOf` **`:310`** · `dependsOnLiveStateOf` **`:330`** | ② 位置面 | `HotbarRenderComponent.java` = 333 行，逐行读盘 |
| ②-21 | 「`roleComponent/ActiveComponent.java`：`:148-158`（`startCooldown()`）、`:160-177`（覆盖式重启）、`:179-182`（`stopCooldown`）、`:184-192`（`isCoolingDown`/`remainingCooldownTicks`）」 | `使用者手册.md:425-429`（同址重复：`:653`） | 现算：`startCooldown()` **`:156`**（区间 `:148-158` 仍覆盖 ✓）· `startCooldown(int)` **`:170`**（`:160-177` ✓）· `stopCooldown()` **`:180`**（`:179-182` ✓）· `isCoolingDown()` **`:193`** / `remainingCooldownTicks()` **`:197`** ⇒ **`:184-192` 已不覆盖** | ② 位置面 | `ActiveComponent.java` = 213 行，逐行读盘；`:69-74`（冷却字段段）与 `:129-132`（声明值）**仍准** ✓ |
| ②-22 | 「六条英文文案（逐字）（来源：`Skill.java:184,189,195`；`MainWeapon.java:161,166,170`）」 | `使用者手册.md:451` | 文案**逐字全在** ✓，行号现为：`Skill.java` **`:181` / `:186` / `:192`**；`MainWeapon.java` **`:158` / `:163` / `:167`** | ② 位置面 | 六行逐行读盘（内容一致、行号各漂 ±3） |
| ②-23 | 「`SanTEComponent.java:43` 声明实现组件操作面、`:252` 的 `current`、`:253` 的 `max`、`:244-268` 整体实现」 | `使用者手册.md:546-547` | 现算：**`:45`**（`implements … OperationProvider` 段）· `"current"` **`:244`** · `"max"` **`:245`** · 实现体 **`:237-262`** | ② 位置面 | `SanTEComponent.java` 逐行读盘（`:237 public String onOperationCommand`） |
| ②-24 | 「手册引用维护约定 … 新件 `frameworkLevel/serviceComponents/ServiceComponents.java`（171 行）」·「`t52` 新工厂 → `roleComponent/frameworkLevel/serviceComponents/`」 | `交接-下一上下文.md:62` · `:170` | **行数 171 ✓**，但该件在 **`roleComponent/frameworkLevel/ServiceComponents.java`** —— **没有 `serviceComponents/` 这一层目录** | ② 位置面 | `Test-Path …/frameworkLevel/serviceComponents` = False；`frameworkLevel/` 直下 = 9 件（含 `ServiceComponents.java`） |
| ②-25 | 「HEAD ｜ `83ccf90`」·「工作树 ｜ 0 行」·「未推提交 ｜ 37 个」 | `交接-下一上下文.md:15-17` | 现算：HEAD **`70bcee0`** · 未推 **40**；该表自称"写作时点权威快照"⇒ 属**快照类**（低危，见 §8 无漂移栏注记） | ② 位置面（低） | `git log -1 --format=%h` · `rev-list --count origin/hue..hue` |
| ②-26 | 「**组件直达读口**（`RoleInstance:312-322`）｜ **6**」 | `debug-logs/测试记录/阶段13/阶段13-RoleInstance直达钩子盘点.md:14` | 现算：`getByType` **`:315-317`**、`getAllByType` **`:328-330`**（两者均为**公开读口**，仓内 0 调用点，非"直达钩子"） | ② 位置面（历史件） | @HEAD 逐行读盘 |
| ②-27 | 「外观冻结清单 … **基线（现算）**：HEAD = `f88afeb…` · `porcelain -- src` = 0」 | `外观冻结清单.md:4` | 现算 HEAD `70bcee0` ⇒ 该件基线**落后于当前树**；其 §3 的比对命令可复跑（9 行清单未被本卡改动触及） | ② 位置面（低） | `git log -1`；该件 §1 的 9 行清单未核出漂移（见 §8-N7） |

> **② 类处置建议**：**不要**用"把行号整体加/减某个偏移"的方式修（本件已见 `−4 ~ +4` 不等距偏移）；正确做法有两条 ——
> 1. **路径类**（`core/hotbar/**` 13 行、`serviceComponents/`、`../` 基）：属**显式路径修订片**的活（符号化不覆盖目录搬迁）⇒ 建议在手册车道立一张**小片**（≤10 处/片，逐处人肉核）；
> 2. **行号类**：交给**符号化车道**（`t10`/`t20` 后续片）按"引用形态"切片处理；★ `RoleInstance` 的 12 段（②-16）与 §5.5 的实现位置段（②-18/②-19/②-20/②-21）**必须按内容锚点改**（`t6` 落地后行号还会再漂一次）。

---

## 4. ③ 语义面失效（说法与代码不符）

| # | 陈述原文（摘引） | 出处 | 现算真值 | 判定 | 证据 |
|---|---|---|---|---|---|
| ③-1 | 「`core/RoleInstance`（**聚合根**：组件容器 + 两阶段生命周期 + 受保护派发入口）」 | `架构总览.md:15`；同义：`组件模型.md:10`、`架构总览.md:52`（"`core` ＝ 聚合根与端口面"） | **"聚合根"是 `Role`**：`RoleInstance` 自己的 javadoc 说 `roleInfo` 是「**聚合根只读服务面**（`Role` 的只读视图）」，写法在 `Role#setFaction/resetFaction`；手册 §5.3:405 亦写「角色模板（聚合根）`core/Role.java`」⇒ 同一词被两名指代 | ③ 语义面 | `Role.java:69 private Faction faction` · `:606`/`:619`（写侧）· `RoleInstance` 现算 `roleInfo()` 为只读口；三处文档互相冲突 |
| ③-2 | 「**装配顺序 = 渲染顺序 = 派发顺序**（**同一份 `LinkedHashMap` 遍历**）」 | `使用者手册.md:230` | 渲染器现为**纯输入式**：`HotbarRenderer.render(Player, Map<Integer,ItemStack> lastRendered)`（`:115`），逐格数据由渲染组件给出；"同一份 `LinkedHashMap`"的说法已不描述现状（现口径 = **同一注册序**：容器按 `Role.componentIdAtSlot`/组件表遍历，渲染组件按自身订阅面取件） | ③ 语义面 | `HotbarRenderer.java:57`（`record Result(Map<Integer,ItemStack> baseline, boolean changed)`）· `:115`；`HotbarRenderComponent.java:185 buildItemOf(component)` 在组件内被调 |
| ③-3 | 「**置脏 / 清脏** ｜ `core/hotbar/HotbarRenderer.java:117-119`、`:125-128`」 | `使用者手册.md:477` | 渲染器**已不含任何脏标记**（全件 `markDirty`/`clearDirty`/`dirty` **0 命中**）；脏标记现归组件：`HotbarRenderComponent.markDirty()` **`:155`** · `consumeChanged()` **`:144`** · `requestRepaint()` **`:225`**（体 `:227`）· `firstFlush()` **`:124`** | ③ 语义面（t23 的所有权迁移） | `HotbarRenderer.java` 全件 140 行逐行读盘（无脏相关成员）；`HotbarRenderComponent.java:124/:144/:155/:225/:227` |
| ③-4 | 「订阅登记（唯一通道）｜ 同上 `:96`（`addListener`）、`:113`（`removeListener`）、**`:217`（`broadcastChange`）**」 | `使用者手册.md:368` | `broadcastChange` **全 src 0 命中**（本卡现算；`t27` 收敛双触发后该通道已删）。真值：登记口 = `SanTEComponent.addListener :98` / `removeListener :115` / `forEachListener :136`；平台侧 = `notifyPlatform :205`（唯一调用点在 `:192 set()`） | ③ 语义面 | 全 `src/**/*.java` 逐行 `-cmatch broadcastChange` = **0 命中**；`SanTEComponent.java:98/:115/:136/:192/:205` |
| ③-5 | 「能量一变，热键栏就重画一次 …（来源：`core/RoleInstance.java:190` —— **容器注册的**那条监听器无条件置脏）」·「框架侧接到变更（**只置脏**）｜ `core/RoleInstance.java:186-191`（容器注册的那条监听器）」 | `使用者手册.md:323-324` · `:338` | 该接线现于 **`ServiceComponents.build`**：`new EnergyComponent(ID_ENERGY, …, change -> hotbarRender.markDirty())`（`ServiceComponents.java:130-132`）；`RoleInstance:168-175` 只把 `change -> dispatchSanTEChange(…)` 交给工厂（`santeChangeReporter`） | ③ 语义面（t52 构造外移 + t24） | `ServiceComponents.java:132`（`change -> hotbarRender.markDirty()`）· `:138-139`（SanTE 平台侧登记）· `RoleInstance` @HEAD `:175` |
| ③-6 | §8 索引「`roleComponent/RoleComponent.java` ｜ **45-51（冷却能力接口）**」 | `使用者手册.md:652` | `RoleComponent.CooldownBearing` **已整体删除**（全件 0 命中）；现 `:45-51` 是**受保护访问器 `svc()`** | ③ 语义面 | `RoleComponent.java` = 433 行；`CooldownBearing` 0 命中；`:45-51` 逐行读盘（`protected final ComponentServices svc()` 的防御性检查） |
| ③-7 | 手册引用维护约定「口径（定案）：引用计数用**口径 A = 命中行数**（显式 `.java:NNN` **189** + 裸 `:NNN` 续引 **104** = **293**）」·「§4 ① …未对 **293** 处逐条落盘」 | `debug-logs/测试记录/阶段13-手册引用维护约定.md:34` · `:38` | **独立重算**：显式行 **186** · 任含裸引行 **104** · **两类同行 29** ⇒ 规则 B 命中行 **261**（=186+104−29）；出现次数口径 = 显式 221 + 裸 152 = 373 ⇒ `189`/`293`/`264` 三个数字**均不成立** | ③ 语义面（数值口径）—— ★ **核本卡期间已由该件就地更正**：现文 `:47` 起「**读数更正声明**」、`:52`（`189 → 186`）、`:54`（`293 → 186+75=261`）、`:57`（104 是"任含"行数、不可加项；可加项 = 75）、`:58`（失效范围自陈）⇒ **残留的 `:34`/`:38` 按"成文时读数"留档，不算未处置** | 本卡独立跑规则 B（模式原文见 §0.3）：186 / 104 / 29 / **261** —— 与 `t8` 门账一致；该件 264 行版本的更正声明亦与之一致 |
| ③-8 | 组件操作面设计定案「**只读** ｜ query 只读权限；modify 需写权限」 | `debug-logs/测试记录/阶段13/阶段13-组件操作面-设计定案.md:135` | 落定实现只有**一条**节点 `shadowhunterroles.operation.<组件id>`，`query`/`modify` **共用**它（动词只影响回显措辞）；"只读权限 vs 写权限"两档**未落地** | ③ 语义面（设计表未随落定同步） | 手册 §3.4:192-198 与 `ComponentOperationDispatcher.java:42/:126/:132` 三处一致口径；该设计件自身 `:18` 只对**另一处**（`/role operation` 形态）写了"实现态说明" |
| ③-9 | 「装饰序列的其余各步 … 由 {@code **core/Skill#buildItem()}** 与 {@code **core/MainWeapon#buildItem()}** 各自给出」 | `roleComponent/frameworkLevel/hotbar/IconState.java:10`（**源码注释**） | `Skill`/`MainWeapon` 在 **`roleComponent/base/`**（`core/` 下不存在这两个类） | ③ 语义面（src 注释；不在本卡 inScope） | `Test-Path …/core/Skill.java` = False；`roleComponent/base/Skill.java` = 235 行 |
| ③-10 | 「尾部 `santeComponent.broadcastChange(preSanTE, newSanTE)`（`:845` 平台侧通道 ✓）」 | `debug-logs/测试记录/阶段13/阶段13-派发边界-运行级读数.md:29` | 该尾调用**已删**（`broadcastChange` 0 命中）；且该件所引 `:831-845` 在现树中对应的是 `RoleInstance:803-822`（`broadcastSanTEChange`，无尾回调） | ③ 语义面（历史读数被当现状来源） | 0 命中（全 src）；@HEAD `:803-822` 逐行读盘：`withinIterationWindow` + 逐监听器 `guardedCall` + 结尾即 `}` |

> **③ 类处置建议**：这一类**不能靠符号化修**（写法本身错了，改成符号只会把错说法写得更醒目）⇒ 建议**单独立一张"手册说法修订卡"**，范围建议 = 手册 §5.2 订阅/派发那两格 + §5.5 置脏/清脏那一格 + §5.1 置脏接点那两处（③-3/③-4/③-5），**并同步 §5.1 的"容器注册"表述为"装配工厂接线"**；③-1（聚合根称谓）与 ③-2（渲染序）落在 **AI 向文档**与**手册**两侧，建议合并进 §1.1 的「AI 向文档现算基线同步」卡与「手册说法修订卡」；③-7/③-8 分别归**方案件车道**（`t9`/`t19`）与**设计件就地标注**；③-9 归**注释车道**（`t4`/`t5`/`t6`/`t7`）。

---

## 5. 坐实四项逐条复核（①~④）

### 5.1 ① 手册 13 行引 `core/hotbar/**` —— **成立**（判定：② 位置面；其中 2 处另有 ③）

- **13 行逐条落位**（`使用者手册.md`）：`:231 · :322 · :439 · :443 · :444 · :445 · :446 · :472 · :477 · :478 · :528 · :648 · :649`。
- **现算真值**：`core/hotbar/` **目录不存在**（`Test-Path` = False）；两件现在 `roleComponent/frameworkLevel/hotbar/`（`HotbarRenderer.java` 140 行 · `IconState.java` 49 行 · 另有 `HotbarSpecification.java` 154 行）。
- **细分**：**11 行**属纯路径漂（行区间内容仍准：`:322 :439 :443 :444 :445 :446 :478 :528 :648 :649` + `:231` 的路径半边）；**2 行另有语义失效**：`:477`（置脏/清脏已搬进组件 → ③-3）、`:231` 的 `HotbarRenderer.java:150-170` **越界**（新件仅 140 行 → ②-1）。
- **归卡**：手册符号化车道 + 一张显式路径修订片（见 §1.1）。

### 5.2 ② 手册 `:368` 引 `broadcastChange` —— **成立**（判定：③ 语义面）

- **现算**：全 `src/**/*.java` 逐行 `-cmatch broadcastChange` = **0 命中** ✓（与交接文档 §3.3 的 `t27` 硬判据「`broadcastChange` 全库 0」一致）。
- **真值替代**：`:98 addListener` · `:115 removeListener` · `:136 forEachListener` · `:192 notifyPlatform(...)`（唯一调用点）· `:205 notifyPlatform`（定义）。
- **根因链**：该引用源自 `debug-logs` 的阶段13 历史读数（§4 ③-10）。**归卡**：手册内容修订卡（符号化不能把它变成正确引用）。

### 5.3 ③ 手册 `:457` `:473` `:596` 引 `RoleInstance:914-929` / `:916-929` —— **成立**（判定：② 位置面）

- **现算真值（@HEAD `70bcee0`）**：该类 **1134 行**；帧末 flush 现于 **`:846-857`**（`:846` 注释 · `:850 HotbarRenderComponent::flush` · `:852` 变化判据注释 · `:855 consumeChanged`）。
- ⇒ 三处应改为 `:846-857`；★ 但 `t6` 会删掉一对成员（`markHotbarDirty` / `requestHotbarRepaint`）⇒ **修卡必须按内容锚点**（"帧末 flush（`RoleInstance#triggerUpdate` 末尾）"）而不是行号。

### 5.4 ④ 以仓库外为基准的相对路径表述（例：交接文档 §8 的 `../debug-logs/…`）—— ★ **当前树不再成立（改判）**

- **实测**：`\.\./debug-logs` 在 `docs/**` + 其余在库文档（`.md`/`.txt`/`.yml`）中 = **0 命中**；唯一命中在 `.scratch/t1/baseline.md:37`（**gitignored 的作业草稿**，非文档件）。
- **交接文档 §8 现文**（`交接-下一上下文.md:210-211`）已是**仓库相对**写法：`debug-logs/测试记录/阶段13/阶段13-注释沿革-搬迁存档.md` · `debug-logs/测试记录/阶段13-手册引用维护约定.md` ✓ **两条路径都真实存在**（后者核本卡时为 **40 行 / 2133 B**、无 BOM、LF；★ **同卡进行中该件被其它车道扩写为 264 行 / 18223 B**，见 §11.2）。
- **顺手核到的 3 处 `../build` 全是假阳性**：`SKILL.md:34`（`../../../../build.gradle.kts`，从 `docs/ai-generated/skills/create-role/` 上溯 4 层 = 仓库根 ✓）· `如何新增一个角色.md:65`（`../../build.gradle.kts`，自 `docs/ai-generated/` 上溯 2 层 = 仓库根 ✓）—— **按各自目录基**解析均正确，**不构成位置面失效**。
- **反证（说明为什么"仓库外基"本身是坑）**：`插件\build` = **False** · `插件\.gradle-work` = **False**，而 `ShadowHunterRoles\build` = **True** · `ShadowHunterRoles\.gradle-work` = **True** ⇒ 若真写成 `../build` 会指向**不存在**的路径（交接文档 §7 曾用过这个说法，现已随工作区还原消失）。
- **一条**确实存在的**相关口径风险**：`debug-logs/测试记录/阶段13-手册引用维护约定.md` 的**真身位置**常被写错 —— 它是 `debug-logs/测试记录/` 直下（文件名自带 `阶段13-` 前缀），**不是** `debug-logs/测试记录/阶段13/` 目录下。凡把目录写成 `…/阶段13/手册引用维护约定.md` 的卡面/说明，均属位置面失效（真值：`debug-logs/测试记录/阶段13-手册引用维护约定.md`，40 行）。

---

## 6. `debug-logs/` 核查（不限于 `docs/`）

### 6.1 扫描方式（必须如此，不能换 `git grep`）

- 全目录：`Get-ChildItem ShadowHunterRoles\debug-logs -Recurse -File`；逐行 `[System.IO.File]::ReadAllLines(<绝对路径>)` + `-cmatch`。
- **实证 `git grep` 对这里恒空**：`git check-ignore -v debug-logs/测试记录/阶段13/<任意件>` → `.gitignore:47:debug-logs` ⇒ 该目录被忽略；`git grep -nE 'R-[1-8]' -- debug-logs docs` 的 19 命中**全来自 `docs`**，`debug-logs` 半边 0 ⇒ **判据对这里恒空**（这也是 §2 ①-5 的根因之一）。

### 6.2 两份"手册陈述的外部来源"逐条核查

| 来源件 | 陈述（摘引） | 现算真值 | 判定 |
|---|---|---|---|
| `阶段13-组件操作面-设计定案.md:15/:18/:109/:314` | 指令形态 `/role operation <query\|modify> [player\|@s] <componentId>[#index] <payload…>`；护栏「多实例 `#index` 消歧与"命中多份即拒绝" · 仅在线玩家 · `query`/`modify` 动词 · 主线程」；裁定「多份且无 index ⇒ 拒绝；动词 = query + modify；仅在线玩家；op 与 args 合并为一个字符串交给组件自解析」 | 与落定实现一致 ✓（手册 §3.3:170-188 的回显表、`ComponentOperationCommand.isTargetToken`、`ComponentOperationDispatcher` 的拒绝路径均同口径） | **无漂移**（唯一例外是 `:135` 的权限两档，见 §4 ③-8） |
| `阶段13-手册引用维护约定.md:34/:38`（**扩写前版本**） | 口径 A = 命中行数；`189 + 104 = 293` | 独立重算 = `186 + 104 − 29 = 261` ⇒ 数字失效；★ 但该件**已自行更正**（现 264 行版 `:47-58` 的「读数更正声明」逐条给出 `189→186`、`293→186+75=261`、104 不可加） | **③ 语义面 → 已原地更正**（残留原文按"成文时读数"留档） |
| `阶段13-手册引用维护约定.md:39`（**扩写前版本**） | 「跟踪器需支持非 `.java` 引用源（现算：`plugin.yml` **4 行** + `.md` **2 行**）」 | 本卡独立重算 = **7 行**（`使用者手册.md` 的 `:75` · `:96` · `:222` · `:628` = `plugin.yml` 4 行；`:710` · `:715` · `:718` = `README.md` 3 行）⇒ 与**扩写后**版本的 §5.3 ①（`:116` 记 **7**）一致；原 `:39` 的"6 行"是**成文时少算 1 行** | **已原地更正**（新文 `:124` 明确写「§4② 的 6 行 ⇒ 现算 7 行」） |

### 6.3 归档件（**按设计留档，不与现树对照** ⇒ 不计漂移）

| 件 | 冲突靶词（现算） | 处置 |
|---|---|---|
| `阶段13-注释沿革-搬迁存档.md`（212554 B） | `core/hotbar` ×6 · `HotbarRenderer` ×5 · `渲染器` ×8 | 归档"删除前原文"，**禁止**被当作现状来源引用 |
| `阶段13-bukkit事件层-搬迁存档.md` | `broadcastChange` ×5（`:289/:328/:367/:381/:383`） | 同上（记录了 `t16`/`t27` 前的调用序） |
| `阶段13-派发边界-运行级读数.md` | `broadcastChange` ×1（`:29`） | **不是归档而是"读数件"** ⇒ 已按 §4 ③-10 记为语义面失效（建议就地加"历史读数"标注） |
| `阶段13-源码树整理-映射方案.md` | `core.hotbar` ×2 | 迁移映射件（记录搬迁前后）⇒ 留档 |
| `阶段13-RoleInstance直达钩子盘点.md` | `RoleInstance:[0-9]` ×1（`:14`） | 盘点件 ⇒ 行号已漂，见 §3 ②-26 |

### 6.4 全库量（供后续卡复核）

- `debug-logs` 内 `R-[1-8]`：**331 行 / 25 件**（`*.md`/`*.txt` 逐行读盘）—— 与 §2 ①-5 的"0 命中"陈述直接冲突。
- `debug-logs\测试记录\阶段13\`（26 件）逐靶词：`core/hotbar` 6 · `core.hotbar` 2 · `broadcastChange` 6 · `HotbarRenderer` 6 · `渲染器` 8 · `持有组件` **0** · `RoleInstance:[0-9]` 1。

---

## 7. 零改动纪律的证据

- 本卡**只新增本件**：核本卡期间的 `git status --porcelain` 净增量 = `?? docs/ai-generated/阶段7-文档语义漂移核报告.md`（唯一由**本卡**产生的新路径）。
- ★ **注意时效（本卡期间 HEAD 动过两次）**：
  | 时点 | HEAD | 工作区 |
  |---|---|---|
  | 取数时 | `70bcee0` | 6 件 ` M`（全属 `t4` inScope：`core/Role.java` · `core/RoleInfoImpl.java` · `core/RoleInstance.java` · `core/component/ComponentRegistry.java` · `core/ports/ComponentLookup.java` · `listener/hook/DamageHookListener.java`） |
  | 核 §11.1 时 | `ba06ab8`（`t4` 落地） | ` M` 清零 |
  | 收尾时 | `39e3dbf`（`docs: freeze the target selector requirements and the semantic drift audit`，**仅 +611 行**规格件） | **1 件 ` M` = `manager/RoleManager.java`（他卡在飞，不在本卡 inScope）** + **1 件 `??` = 本件** |
  ⇒ 正确判据是「**本卡**未新增/修改除本件外的任何路径」，而不是「porcelain 为空」（本卡期间工作区**从未**空过：先有 `t4`、后有他卡的 `RoleManager`）。核本卡收尾时的实测见上表第三行；**本件之外的路径没有一条是 `eng-framework` 动的**。验证命令：
  ```powershell
  git -C ShadowHunterRoles status --porcelain                                  # 逐条核对：凡是本件之外的路径，必须能说出它属于哪张卡
  git -C ShadowHunterRoles status --porcelain -- docs                          # 期望：只有本件（本卡提交后为 0 行）
  git -C ShadowHunterRoles diff --stat -- src                                  # 期望：0 行（他卡在飞时可能非 0 ⇒ 逐条核对归属，不要据以判定本卡违规）
  ```

---

## 8. 无漂移清单（核过且**仍然成立**的陈述，避免下个上下文重复劳动）

| # | 陈述 | 出处 | 为什么判定"无漂移"（证据） |
|---|---|---|---|
| N1 | 三条派发不变量 **1/1/10** | `交接-下一上下文.md:149-151`（§5.1） | @HEAD 现算：`preSanTE == newSanTE` = `:759`（1）· `guardedCall(entry.owner(` = `:816`（1）· `sanTEDispatching\|sanTEPendingValue` = 10 命中行（`736 737 750 761 762 766 773 774 775 783`，含 `:750` 的 javadoc 提及） |
| N2 | 唯一物品写点 `inv.setItem(` = **2 处** | 同上 `:152` | 现算 2：`HotbarItems.java:40`（清空）+ `hotbar/HotbarRenderer.java:136`（写入）；`setItem(` 全变体同为 2 ⇒ 无隐藏写点 |
| N3 | 平台侧通道 `notifyPlatform` = **3 行** | 同上 `:153` | 现算 3：`SanTEComponent.java:192`（唯一调用）+ `:205`（定义）+ `RoleInstance.java:798`（javadoc 提及）⇒ 口径 = **原始文本命中行数、注释计入**（写任何修卡时不得改这三行） |
| N4 | 「角色实例**不持有**任何组件」 | `交接-下一上下文.md:30`、`164` | @HEAD：`RoleInstance` 内 `HotbarRenderer` **0 命中**；`private final .*Component` 仅 2 处且均为**容器职责**（`ComponentRegistry` / `ComponentLookup`，用户 q4-A 已裁定保留）；`t24` 的 6 个组件字段确已 0 |
| N5 | 结构卡硬判据表（`t23`/`t24`/`t27`/`t16`/`t52`/`t53`） | `交接-下一上下文.md:58-63` | 逐条现算一致：`HotbarRenderer` in `RoleInstance` = 0 ✓ · 组件字段 = 0 ✓ · `broadcastChange` = 0 ✓ · `event/` 不存在 ✓ · `new (六类)Component(` in `RoleInstance` = 0、`ServiceComponents.java` = **171 行** ✓ · `core/hotbar/` 目录消失 ✓ |
| N6 | 端口面 = **4 件**（3 接口 + 1 服务集记录） | `组件模型.md:38` | 现算 `core/ports/` = 4：`ComponentLookup` · `RoleInfo` · `Self` · `ComponentServices` ✓ |
| N7 | 外观冻结清单 §1（**9 行**）与 §3 比对命令、其引的 4 份 `阶段13-外观dump-*` 字节数 | `外观冻结清单.md:5/:9/:107-110` | 抽查 4 行字节数全部吻合：`运行级读数.md` 6164 ✓ · `起服日志.log` 7796 ✓ · `第9行-运行级读数.md` 11577 ✓ · `第9行-起服日志.log` 9009 ✓（本卡未触及该件内容） |
| N8 | 「注册表里的 **15 个**组件 id（`RoleLoader.java:53-69`）」 | `使用者手册.md:200` | `RoleLoader.java` 现 **211 行**；`static final String ID_` 命中 = `:53-66` + `:69` = **15 个** ✓（与手册 §3.4 的 15 行表逐行对应） |
| N9 | 六条英文文案逐字 | `使用者手册.md:448-451` | 六行逐字读盘一致（仅**行号**漂，见 §3 ②-22；**文案本身零漂**） |
| N10 | 「冷却状态**每个组件实例各持一份**；框架不登记、不派发冷却」 | `使用者手册.md:418-419` | ✓ 与 `ActiveComponent.java:43`（注释）/`:74 cooldownUntilTick` 一致；`RoleInstance:91` 注释亦写"框架侧冷却表已删除" ⇒ ★ **该手册说法正确，而 `组件模型.md:28` 的"一张表"才是错的**（§2 ①-1） |
| N11 | 「本插件**不再发布 Bukkit 事件**」 | `使用者手册.md:326` | ✓ `event/` 目录不存在；全库 `callEvent\|ChangeEvent` 无生产调用点（`t16` 硬判据） |
| N12 | 旧 `docs/架构/**` **8 件已删除**（自检命令期望 0 件） | `README.md:38/:52` | `git ls-files docs/架构` = **0 件** ✓；`git grep -rn '架构/adr' -- docs README.md` 仅命中本文件的说明行 ✓ |
| N13 | 手册 §9「未实测 / 未覆盖项」如实申报 | `使用者手册.md:685-701` | ✓ 本卡核到的 6 条未实测标注（`debug sched` 数值 / 四类保护 / 阵营运行级 / 秒数逐刻 / `operation query` / 其它文档口径）均**未被本轮任何改动推翻** |
| N14 | 手册 §0 的引用约定（来源 + 行号；短路径以仓库根为起点） | `使用者手册.md:6-9` | ✓ 约定本身有效；也正是它使 `:443-:446` 的短路径可解析（其**未补全**才是 ②-4 的问题） |

---

## 9. 复核命令（照抄可复现本件任一条读数）

```powershell
$repo = (Resolve-Path 'ShadowHunterRoles').Path
$S    = 'src/main/java/com/shadowHunterRolesPlugin/'

# ① 行号读数（锚定 HEAD，避开在飞工作区）
$ri = & git -C $repo show "HEAD:$S`core/RoleInstance.java"
$ri.Count                                  # 1134（★ 全文行数口径；不要用 Get-Content 数行——CJK 无 BOM 会静默打坏）
$ri[845..856]                              # 846-857 帧末 flush 段
for($i=0;$i -lt $ri.Count;$i++){ if($ri[$i] -cmatch 'preSanTE == newSanTE'){ "L$($i+1)" } }        # L759
for($i=0;$i -lt $ri.Count;$i++){ if($ri[$i] -cmatch 'guardedCall\(entry\.owner\('){ "L$($i+1)" } } # L816
# ★ 不要写成 Select-String -InputObject $ri（把整个数组当一个对象 ⇒ 行号恒 1）；要按行定位用上面的逐行 -cmatch
# ★ 例外：Select-String -Path <文件> 的 LineNumber 是准的（可替代逐行循环）

# ② 全库靶词（-cmatch 大小写敏感）
Get-ChildItem "$repo\src" -Recurse -Filter *.java | ForEach-Object {
  $n=[IO.File]::ReadAllLines($_.FullName); for($i=0;$i -lt $n.Count;$i++){
    if($n[$i] -cmatch 'broadcastChange|inv\.setItem\('){ "$($_.Name):$($i+1)" } } }

# ③ debug-logs（★ 不得用 git grep：.gitignore:47/:85 覆盖 ⇒ 恒空）
Get-ChildItem "$repo\debug-logs" -Recurse -File -Include *.md,*.txt | ForEach-Object {
  $n=[IO.File]::ReadAllLines($_.FullName); for($i=0;$i -lt $n.Count;$i++){
    if($n[$i] -cmatch 'core/hotbar|broadcastChange'){ "$($_.FullName.Substring($repo.Length+1)):$($i+1)" } } }

# ④ 目录 / 件数（AI 向文档基线的现算）
Test-Path "$repo\$S`event"                                     # False
(Get-ChildItem "$repo\$($S.Replace('/','\'))" -Directory).Count # 10
(Get-ChildItem "$repo\$($S.Replace('/','\'))core" -Recurse -File -Filter *.java).Count          # 13
(Get-ChildItem "$repo\$($S.Replace('/','\'))roleComponent" -Recurse -File -Filter *.java).Count # 38

# ⑤ 规则 B（手册引用门账）
$m=[IO.File]::ReadAllLines("$repo\docs\ai-generated\使用者手册.md"); $e=0;$b=0;$x=0
for($i=0;$i -lt $m.Count;$i++){
  $ce=([regex]::Matches($m[$i],'[A-Za-z0-9_]+\.java:\d+')).Count
  $cb=([regex]::Matches($m[$i],'(?<!\.java):\d+')).Count
  if($ce){$e++}; if($cb){$b++}; if($ce -and $cb){$x++} }
"显式=$e 任含裸引=$b 两类同行=$x 命中行=$($e+$b-$x)"   # 186 104 29 261
```

---

## 10. 本件明确**不**做的事（边界申报）

1. **不改任何文档、不改任何源码** —— 只记录（含 `src` 注释漂 `IconState.java:10`，交给注释车道）。
2. **不逐段复核手册 §8 里与 `t23/t24/t27` 无关的行集群**（如 `command/**`、`listener/**`、`RoleLoader` 的大量行号）—— 那属规则 B 符号化车道（`t10`/`t20`）的活；本件只核**结构改动导致的**语义漂移与**坐实四项**。
3. **不把归档件当现状**（§6.3）：`注释沿革-搬迁存档` / `bukkit事件层-搬迁存档` / `源码树整理-映射方案` 是**留档**，判定为"不计漂移"而不是"待改"。
4. **不做跨语言/跨形态的推断**：凡本件未附"现算真值 + 证据"的条目一律未写入（宁可少写一条，不写一条无法复核的）。
5. ★ **未纳入扫描面**（`t41` 补登；逐件给**首见提交**以便复核 —— **未扫描 ≠ 判定为无漂移**）：
   | 件 | 首见提交 | 为什么未扫描 |
   |---|---|---|
   | `docs/ai-generated/阶段7-指令目标选择器-需求与口径.md`（611 行） | **`39e3dbf`**（**早于**本件 `8a49e50`） | 本件写作时点**已存在**却未纳入 ⇒ **属真缺口** |
   | `docs/ai-generated/阶段7-RoleInstance-零触须可行性评估.md` | **`9c97a03`**（**晚于**本件 `8a49e50` / `884a1b5`） | 写作时点**尚不存在** ⇒ 非本件缺口（★ 与 `V-D` 的原始认定不同：以 `git log --diff-filter=A` 的首见提交为准） |
   | `docs/ai-generated/阶段7-热键栏提交与回调契约.md` · `阶段7-组件架构合规审计.md` | `10f039f` · `32eea1c` | 同上（均晚于本件） |
   ★ **选择 = 只声明、不补扫**（补扫会改动本件 §0 口径与 §1–§9 的判定面 ⇒ 应由另立的独立卡做，并重开判定面）。

---

## 11. 时效附录（核本卡期间发生的两件事，均已如实并入上文）

### 11.1 `t4` 落地（`70bcee0` → `ba06ab8`）：本件锚点逐行复核 —— **全部不变**

| 锚点（`RoleInstance` @HEAD） | `70bcee0` | `ba06ab8` |
|---|---|---|
| 文件全文行数 | 1134 | **1134** |
| 真变化闸门 `preSanTE == newSanTE` | 759 | **759** |
| 逐监听器隔离 `guardedCall(entry.owner(` | 816 | **816** |
| 重入合并 `sanTEDispatching\|sanTEPendingValue` | 736,737,750,761,762,766,773,774,775,783 | **同上 10 行** |
| `dispatchSanTEChange` / `broadcastSanTEChange` | 757 / 803 | **757 / 803** |
| `triggerUpdate` / `HotbarRenderComponent::flush` / `consumeChanged` | 825 / 850 / 852,855 | **同上** |
| `initComponents` / `registerServiceComponents` / `HotbarItems.clearFrom(` | 478 / 295 / 1121 | **同上** |
| `SERVICE_ID_SANTE = ` / `handleCast` / `handleAttack` | 70 / 422 / 450 | **同上** |
| 触须组 `markHotbarDirty\|requestHotbarRepaint\|dispatchHotbarRendered` | 141,210,213,489,856,868 | **同上** |
| `isSkillReady` / `isMainWeaponReady` | 519 / 569 | **同上** |

⇒ 结论：`t4` 是**原地改写注释**（`--stat`：6 件 / 19 增 19 删，行数无变化）⇒ **本件 §3·§5.3 的所有 `RoleInstance` 行号对新 HEAD 仍然有效**。
★ 上文已一并覆盖：`t4` 落地时 `RoleInstance` 的**注释原文**已被改写 ⇒ 本件引用的**注释类**锚点（如 `:750` 的 javadoc 提及）若与现文措辞不符，属 `t4` 的正常改写，**判据是"该行仍存在且仍含该标识符"，不是"措辞未动"**（本件 §11.1 复核的正是前者，10/10 命中）。

### 11.2 方案件在核本卡期间被其它车道扩写（40 行 → 264 行）

| 项 | 核本卡时 | 收尾时 |
|---|---|---|
| `debug-logs/测试记录/阶段13-手册引用维护约定.md` | **40 行 / 2133 B**（全文口径；非空内容 **29 行** —— 两口径是两个数） | **264 行 / 18223 B**（`LastWriteTime` = 2026-09-25 11:00:30）；★ 队长通报 `t9` **正在继续扩到 283 行**（`§5` 追加、不动既有编号）⇒ 该件在收尾时**仍在增长** |
| 新增内容 | —— | §4「**读数更正声明**」`:47-58` · §5「不可符号化清单（五类）」`:44-126` · §5.3 ① 非 `.java` 源 **7 行** |
| 对本件的影响 | —— | 本件 §4 ③-7 与 §6.2 的两条已改为"**已原地更正**"口径；§5.4 的路径真值与行号引用**不受影响**（该件的**位置**没变，只是变长了） |

★ 该件被库内 `.gitignore:47`（`debug-logs`）+ `:85`（`debug-logs/**`）覆盖 ⇒ 它的变化**在 `git status --porcelain` 里无痕**（`porcelain` 当时为空）⇒ **凡引用 `debug-logs/**` 的行号，必须写明"取数时刻 / 文件行数"**，本附录即为该纪律的实例。

### 11.3 本件自身的引用复核记录（可复核）

- **复核方式**：从本件正则抽取全部 `文件名:行号` 引用（`([A-Za-z0-9_\u4e00-\u9fa5\-\./]+\.(?:java|md|txt|yml)):(\d+)`），按基名解析到实际文件（优先级 `src/main/java` > `src/test/java` > `src/main/resources` > `docs` > `debug-logs`，**排除 `debug-logs/回滚快照/**` 的历史快照件**），再用 `[IO.File]::ReadAllLines()` 校验"该行号存在于该文件"。
- **复核结果**：**唯一引用点 117** ｜ 行号在范围内 **114** ｜ 越界 **2** ｜ 未解析 **1** ｜ 同名多份（已按优先级取）3。
  ★ 该复核在**提交版（327 行）**上重跑过一次，与写作时（312 行）读数**完全一致** ⇒ 本章节自身的增补没有引入新的失效引用。
- **两条"越界"不是笔误，而是本件**故意**引用的漂移目标**：
  1. `HotbarRenderer.java:150` —— 手册 `:231` 原文引 `core/hotbar/HotbarRenderer.java:**150-170**`，而现件只有 **140 行** ⇒ 该引用**本身越界**正是 §3 ②-1 的判定依据。
  2. `HotbarRenderer.java:145` —— 手册 `:472` 原文引 `…:**145-173**`（写入在 `:168`），同上 ⇒ ②-8 的判定依据。
- **一条未解析**：`.scratch/t1/baseline.md:37` —— 是 `t1` 作业草稿（`.gitignore:90` 覆盖），**非文档件**，本件只在 §5.4 说明"`@../debug-logs` 的唯一命中在此"。
- ★ **与规格件口径的关系（防打架声明）**：本件**没有**引用"指令目标选择器"特性的**回绝措辞**（不引 `/role operation` 之外的任何新文案，也不引 `size==0`/`size>1` 的旧"共用一句"口径）⇒ 本件与 `docs/ai-generated/阶段7-指令目标选择器-需求与口径.md`（`39e3dbf` 入库版）**无冲突**。本件 `:129` 与 `:178` 提到的"回显措辞"只指**既有**`/role operation` 的动词文案（`query`/`modify` 共用一条权限节点），与选择器特性无关。
- ★ **口径补齐（`t41` 补登 —— 缺了这三项，上面的 117/114/2/1 无法被独立复核；补齐后与独立复核的 126/119/7/0 逐类可解释）**：
  1. **取数时刻**：本章节的复核在**本件 `884a1b5` 的收尾态**执行（当时 HEAD = **`39e3dbf`**；文件两轮 = 312 行 → 提交版 327 行）。此后被引源件继续变动（例：`RoleInstance` 1134 → 995 行）⇒ **同一条引用在不同时点会得到不同的"范围内 / 越界"判定**，任何复核都必须锚定时点（本件 §0.1 同纪律）。
  2. **被引源件的当时行数**（"越界"判定的依据，取数时刻的值）：`RoleInstance.java` **1134** · `HotbarRenderer.java` **140** · `IconState.java` **49** · `ServiceComponents.java` **171** · `使用者手册.md` **721**（全文口径）。
  3. **同名件解析优先级**：`src/main/java` > `src/test/java` > `src/main/resources` > `docs` > `debug-logs`，且**排除 `debug-logs/回滚快照/**`** —— 该层存有**同名历史快照件**（例如回滚快照里的 `RoleInstance.java` 只有 798 行、`HotbarRenderer.java` 55 行、`IconState.java` 12 行）⇒ **不排除它会整体错位**（这正是独立复核报 7 处"越界"、本件报 2 处的主要原因）；另 `debug-logs/**` 被 `.gitignore:47`/`:85` 覆盖 ⇒ **只能逐行读盘**（`git grep` 恒空）。
  4. **计数单位**：本件按「**唯一 (文件, 行号) 对**」计数 = **117**；若按「出现次数」或「原始引用串（`file:NNN-MMM` 的每一段各计一次）」计数会得更大值（独立复核的 126 属此类）⇒ 两个数不矛盾，但必须连单位一起报（本件 §0.3 同纪律）。
