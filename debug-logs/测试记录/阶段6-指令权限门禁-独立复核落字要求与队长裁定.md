# 阶段6 · 指令权限门禁（`t30`）· 独立复核落字要求与队长裁定

**来源**：`eng-验证` 对 `t30` 的完整**独立复算**（结论：主读数全部成立、无影响结论的 finding），提出三条**可复核性**要求。**本文为队长裁定与更正记录**，**不改 `t30` 任何既有件的原句**（沿用"追加更正"的既有处置）。

## 一、级别措辞：实测是 `ERROR`，不是 `SEVERE`

✅ 引用那行时应写 **`ERROR`**，逐字为 `[20:31:38] [Server thread/ERROR]: … cannot read ops.json …`。
**口径后果**：按**级别字段**扫描的 `ERROR/SEVERE` 总数 = **1**，与该轮 `formA=1` 对得上 ✓；但**不得**把它写成 `SEVERE` —— 措辞与实测不一致会让后人复核时找不到对应行。

## 二、承重件位置（队长裁定）：已复制入库

`t30d-clean-window.log`（**9710 B / `DA16A913…`**）原先落在 `.scratch\bot\`（**仓库外、不受版本控制**），而「**C=0 三条件**」是本轮的**承重判据** ⇒ 与台账 §12.3「**承重证据必须落在受版本控制的位置**」冲突。

**处置**：由队长**复制入库**为
`debug-logs/测试记录/阶段6-指令权限门禁-干净窗口切片.log`
复制后**逐字节相同**（bytes + SHA256 见下）。**原仓库外件保留不动**，但**不得**再作为引用对象；此后引用一律走**仓库内路径**。

| | bytes | SHA256 |
|---|---|---|
| 仓库外原件 | 9710 | `DA16A913…`（保留，不引用） |
| **入库件（引用对象）** | **9710** | **`DA16A913C5F30672…`** |

## 三、`entries` 口径：`entries=2` = 去重后的有效条目数

基线 `ops.json` 按**对象数**是 **3 条**（其中含一条**重复同名** `HueZhandouji`），而读取器按**名字去重** ⇒ 物证里的 `entries=2` 应读作「**去重后的有效条目数**」。
**并列不矛盾**：`3` = 文件里的对象数；`2` = 去重后的有效条目数。**基线那条重复条目不得清理**（已在 `t30` 的 A19 写死）。

## 四、附带确认（`eng-验证` 的独立复现）

- 冻结件 19198 B / 190 行 / `FB8C021C…`，与停机后 `latest.log` **逐字节相同** ✓
- 轮转侧证**独立复现**：`2026-09-20-2.log.gz` → 14830 B / `92987454…` = 起服前那份 ✓
- **全程无第二条** `Enabling ShadowHunterRolesPlugin` ⇒ 「**无需 reload 即生效**」与日志一致 ✓
- `/reload confirm` 被拒有**双证**：客户端 `Incorrect argument for command` + 服务端 `issued server command: /reload confirm` ✓
- 前代 `B8C3259E` / `305BA1AD` 逐字未变；`ops.json` 逐字节回基线 `2531C80A…`；run 侧 `config.yml` 已删；窗口 LISTENING 0 / java 0 ✓