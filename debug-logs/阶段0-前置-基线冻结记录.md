# 阶段 0 前置 · 重构基线冻结记录（after-baseline）

> **执行者**：eng-验证（verifier）｜**任务**：t4｜**attempt**：1
> **冻结窗口**：2026-09-16 21:52:36 – 21:54:36（+08:00）
> **判定**：**通过** —— t4 的 4 条验收判据全部满足。
> **未决问题**：0 blocker / 0 high / 0 medium / **3 low**（全部是指南附录 C 的文档口径，不影响代码事实）。
> **原始输出**：`debug-logs/回滚快照/after-baseline/_meta/证据-附录C复核.txt`（256 行，含命令原文与逐段原始输出）。

---

## 1. 交付物与冻结范围

### 1.1 产物

| 路径 | 说明 |
|---|---|
| `debug-logs/回滚快照/after-baseline/src/**` | 活动工作树 `ShadowHunterRoles/src/` 的**逐字节副本**（48 个文件 = 47 个 `.java` + `main/resources/plugin.yml`），且**保留原 mtime** |
| `debug-logs/回滚快照/after-baseline/build.gradle.kts`、`settings.gradle.kts`、`gradle.properties` | 三个构建脚本的副本（后续阶段若有人动构建配置，可立即判出） |
| `debug-logs/回滚快照/after-baseline/SHA256.txt` | 51 行，`<SHA256>  <相对路径>`（相对 `after-baseline/`），与 51 个基线副本一一对应 |
| `debug-logs/回滚快照/after-baseline/_meta/baseline-files.tsv` | 全量清单：`相对路径 \t 字节数 \t mtime(ISO, 毫秒+时区) \t SHA256` |
| `debug-logs/回滚快照/after-baseline/_meta/before-快照-参照清单.tsv` | 旧快照参照值：`before/` 48 个文件 + `回滚快照/` 根 3 个文件的大小/mtime/SHA256，用于将来证明旧快照未被改动 |
| `debug-logs/回滚快照/after-baseline/_meta/证据-附录C复核.txt` | 指南附录 C 与附录 B 相关断言的原始命令输出（C-1 … C-12 共 12 段） |

`_meta/` 是**快照元数据**，**不属于**基线副本，故不在 `SHA256.txt` 的 51 行覆盖范围内。

### 1.2 快照口径（与旧 `before/` 保持一致，不夸大）

- **纳入**：`src/**`（全部源码与资源）+ 3 个构建脚本。
- **不纳入**：`docs/`（工作文档，每阶段都会改）、`.gradle-work/`（构建缓存）、`build/`、`run/`、`bin/`、`.idea/`、`.git/`、`.gradle/`、gradle wrapper（本次重构无任何任务改动它们；`before/` 也未纳入 docs/）。
- 旧快照 `debug-logs/回滚快照/before/`、`integrity-sha256.txt`、`README-回滚说明.md`、`session-start-baseline.patch` **一个字节都没有被本任务写入**（见 §3.2）。

### 1.3 判据对照（任务卡 t4 acceptance）

| # | 验收判据 | 判定 | 依据 |
|---|---|---|---|
| 1 | `debug-logs/回滚快照/after-baseline/` 存在且内容完整；旧 `before/` 文件与 mtime 未被改动 | **通过** | §3.1 / §3.2 |
| 2 | SHA256 清单与快照文件一一对应：文件数一致、逐行可校验 | **通过** | §4 |
| 3 | 附录 C 每条抽样命令给出复现结果与关键输出片段，或给出失败原因 | **通过** | §5（3 条命令 + 9 条汇总断言逐条判定） |
| 4 | 未修改 `ShadowHunterRoles/src/` 下任何文件 | **通过** | §6 |

---

## 2. 冻结时刻与"改了什么"的判定口径（§6.1 纪律）

**mtime 切点 = `2026-09-16 21:52:36`（+08:00）** —— 该时刻快照目录被创建、`src/**` 被逐字节拷入。

判定规则（后续每个阶段的收口都照此执行）：

1. 取 `src/` 下所有 `LastWriteTime >= 2026-09-16 21:52:36` 的文件 → 即**本基线之后被写入过的文件集合**；
2. 与 `_meta/baseline-files.tsv` 求差集 → **新增文件**（不在基线中，回滚时需删除）；
3. 对交集逐个比 SHA256 → **内容改变的文件**（哈希不同即改过，与 mtime 无关）；
4. **不得**用 `git diff` / `git status` 判定改动归属（工作树含作者未提交改动；本节纪律禁止 `git checkout`/`git restore`/`git stash`）。本任务**未执行任何 git 命令**。

**当前状态锚点**：`src/` 递归最大 mtime = `2026-09-15 22:22:43.006`（`MeiqiheziEquipmentsPassive.java`），比冻结时刻早约 23.5 小时 → 基线与"作者+A/B 两流的既有改动"之间有干净的分界。

§6.1 提到的"`before/` 已过期（48 个文件是上一轮开工前，之后有 17 个文件被 A/B 两流改过）"在此获得**正向证据**：`before/` 47 个 `.java` 共 4794 行，活动工作树 47 个 `.java` 共 4825 行（+31 行），且 `RoleInstance.java`（25000 → 26141 B）、`RoleAPIImpl.java`（11829 → 11909 B）等文件哈希均已不同。

---

## 3. 验收判据 1：新基线完整 + 旧快照未被改动

### 3.1 快照 ↔ 活动工作树：逐文件哈希与 mtime 双重比对

```powershell
# 对 _meta/baseline-files.tsv 的 51 行逐行：活动树哈希 vs 记录哈希；活动树 mtime vs 记录 mtime
```

关键输出：

```
checked=51 hashMismatch=0 mtimeDiff=0 missing=0
```

→ 51 个文件**哈希全等、mtime 全等、无缺失**：该快照是活动工作树的**逐字节 + 逐 mtime** 副本。

### 3.2 旧快照未被改动

```powershell
# before/ 48 个文件逐个 Get-FileHash，与 integrity-sha256.txt 的 48 条 OK 记录比对
# 快照根目录 3 个文件取哈希；before/ 递归取最大 mtime
```

关键输出：

```
before/ 哈希一致 = 48, 不一致 = 0, 缺失 = 0
before/ 递归最大 mtime = 2026-09-15 21:56:52（< 冻结时刻 2026-09-16 21:52:36 → 未被重写）

CE3E5E7B0A4BED22834D7D8EF76AC193FC528846173335125EC73AFFF965D50C  integrity-sha256.txt  7213 bytes  2026-09-15 21:56:53.260
08ADCD6F7FFA46FE0AAB39E5119C1C4F6847769FA08B71C6C26C616F058325BB  README-回滚说明.md      6469 bytes  2026-09-15 21:57:05.073
23C8F34CEDE60FE609F3AB745109FC8AFB0EA6EE3A14AFA4B3800AF01A82EE07  session-start-baseline.patch  50091 bytes  2026-09-15 21:56:38.447
```

补充：`session-start-baseline.patch` 的实测哈希 `23C8F34C…` **等于** `integrity-sha256.txt` 第 49 行自己登记的 `FROZEN` 值 —— 内容与登记值自洽。

**如实说明**：新建 `after-baseline/` 会使父目录 `debug-logs/回滚快照/` 的目录 mtime 变为 2026-09-16 21:54:02（NTFS 语义：新增子项会触碰父目录）。`before/` **子树内部**的任何文件与目录 mtime 均未变化（最大仍为 2026-09-15 21:56:52）。

---

## 4. 验收判据 2：SHA256 清单与快照一一对应

```powershell
# SHA256.txt 行数 vs after-baseline 下受管文件数（排除 SHA256.txt 自身与 _meta/）；逐行重算哈希
```

关键输出：

```
SHA256.txt 行数 = 51 ；顶层基线副本数（不含 SHA256.txt 与 _meta/） = 51 ；逐行校验不符 = 0
```

- 51 = 48（`src/**`：47 `.java` + `plugin.yml`）+ 3（构建脚本）。**无遗漏、无多余、无重复**。
- 逐行重算 `Get-FileHash -Algorithm SHA256` 与清单值比对：**0 处不符**（0 = 全部通过）。
- 元数据文件自身哈希（供将来确认元数据未被改写）：

| 文件 | SHA256 | 字节数 |
|---|---|---|
| `after-baseline/SHA256.txt` | `230464328BE7E2E2E65981C00EA424EEEB09740F0F9633626A122A6EB0A2FEAF` | 7155 |
| `after-baseline/_meta/baseline-files.tsv` | `90832F72F38E482935D47743598A90FA1F5EA42FBFEEE575221588A9514721D4` | 8874 |
| `after-baseline/_meta/before-快照-参照清单.tsv` | `458106D553D0B891D36D0B8DBE6E482AF7DCF3569972D13C58E4B1D9D96B55FE` | 9231 |
| `after-baseline/_meta/证据-附录C复核.txt` | `7464CF28B0A6FFF0247B997345DF675667CB041C4EC57E49C48D398A555AC139` | 17697 |

---

## 5. 验收判据 3：指南附录 C 逐条复核（命令原文 + 关键输出）

执行时间 2026-09-16 21:53（+08:00），工作树 = 本基线。**命令一律按指南文本原样执行**，随后补递归等价命令对照。

### 5.1 附录 C 的三条命令

| # | 命令（原文） | 实测结果 | 判定 |
|---|---|---|---|
| C-1 | `Select-String -Path "$src\roleComponent\DefaultSanTEZeroPunishment.java" -Pattern 'taskId' -Context 2,2` | `:29  int taskId = -1;`；`:36  taskId = new BukkitRunnable() {`；`:114 }.runTaskTimer(ShadowHunterRolesPlugin.getInstance(), 0L, 40L).getTaskId();`；`:124 Bukkit.getScheduler().cancelTask(taskId);` | **复现**，且与 §2.3 O-6 引用的 `:29,36,114,123-125` 逐行吻合 |
| C-2 | `(Select-String -Path "$src\*\*.java","$src\*\*\*.java" -Pattern 'ShadowHunterRolesPlugin\.getInstance\(\)' \| Measure-Object).Count` | **24**（不是汇总表声称的 26；原因见 V-01） | **部分复现**：断言值 26 成立（递归实测 26），但**命令本身有覆盖缺陷** |
| C-3 | `Select-String -Path "$src\*\*.java","$src\*\*\*.java" -Pattern '\.trigger\('` | 0 条（递归亦 0 条） | **复现**（`PassiveSkill.trigger` 确为零调用） |

### 5.2 附录 C 编制时实测汇总（9 条断言）

| # | 指南断言 | 实测 | 判定 |
|---|---|---|---|
| C-4 | `ShadowHunterRolesPlugin.getInstance()` **26 处** | **26**：`core` 17（`RoleInstance` 12、`Skill` 2、`Role`/`MainWeapon`/`DamageUtil` 各 1）+ `roleComponent` 5（`DefaultSanTEZeroPunishment` 1、`MeiqiheziBloodySlashSkill` 1、`MeiqiheziCircleSlashSkill` 1、`RedSolitaryArroganceSkill` 2）+ `listener` 2 + `manager` 2 | **复现**，且**逐目录拆分与 §2.4 完全一致** |
| C-5 | `RoleManager.getInstance()` 在 `roleComponent` **2 处**（1 处死变量） | 2 处：`roleComponent/SkillUtil.java:22`、`roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java:72`（`RoleManager roleManager = RoleManager.getInstance();` 声明后未被使用） | **复现** |
| C-6 | `Bukkit.getScheduler()` **15 处**（RoleInstance 10、BuffManager 2、两个 listener 各 1） | 总数 **15** ✓；归属为 `RoleInstance` 10 + `BuffManager` 2 + `SkillListener` 1 + `MainWeaponListener` 1 + **`DefaultSanTEZeroPunishment` 1**（括号只列了 14，见 V-03） | 总数**复现**，归属口径不完整 |
| C-7 | `getGlobalRegionScheduler` **1 处** | 1（`RedSolitaryArroganceSkill.java`） | **复现** |
| C-8 | `new BukkitRunnable` **3 处** | 3（`DefaultSanTEZeroPunishment`、`MeiqiheziBloodySlashSkill`、`MeiqiheziCircleSlashSkill`） | **复现** |
| C-9 | `core/RoleInstance.java` **798 行 / 73 个 public 方法** | 798 行 ✓；`javap -classpath build\classes\java\main com.shadowHunterRolesPlugin.core.RoleInstance` → **73 个 public 成员**（含 1 个构造器）✓（源码正则口径为 72，差 1 属正则未覆盖换行签名，以编译产物为准） | **复现** |
| C-10 | `src` 下 **47 个 `.java`** | 47 | **复现** |
| C-11 | `src` 下 **4 811 行** | **4825** 行（`ReadAllLines` 口径与 LF 字节口径均为 4825；`Get-Content \| Measure-Object -Line` 口径为 3831，因其不计空行） | **未复现**（见 V-02） |
| C-12 | 产物：jar 102 878 B / mtime `2026-09-15 22:25:22`；无源码新于 jar | jar = **102878 B**，mtime **2026-09-15 22:25:22.767**；`build\classes` 最新 `.class` mtime = 同一时刻 22:25:22；`src` 下新于 jar 的 `.java` = **0** | **复现**（附录 B「产物与新鲜度」一节同样通过） |

### 5.3 与阶段 0/1 直接相关的一条基线事实

`DefaultSanTEZeroPunishment.java:114` 的 `runTaskTimer(...)` 在 **`.getTaskId()` 之后丢弃了任务对象**，只留下 `int taskId`；`:124` 的 `stop` 只 `cancelTask(taskId)`。这与 O-6「单 `taskId` 字段无重入保护」的描述一致，是阶段 1.2 的改动对象。

---

## 6. 验收判据 4：未修改 `ShadowHunterRoles/src/`

两条独立证据：

1. **mtime**：`src/` 下 `LastWriteTime >= 2026-09-16 21:52:36` 的文件数 = **0**；`src/` 递归最大 mtime = `2026-09-15 22:22:43.006`。
2. **哈希**：51 个基线副本与活动工作树逐文件 SHA256 比对，**0 处不符**（§3.1）。冻结后执行的读操作与写入均只落在 `debug-logs/回滚快照/after-baseline/**` 与 `debug-logs/阶段0-前置-基线冻结记录.md`。

---

## 7. 发现项（分级）

> 全部为**文档口径**问题，**不涉及代码事实**，不阻断任何后续阶段。建议由阶段收口文档（t8/t12 或指南维护者）顺手修正。

### V-01（low）附录 C 的计数命令覆盖不全，原样执行会少计 2 处

- **位置**：`debug-logs/最终重构指南.md:856`（附录 C 计数型断言）。
- **事实**：命令 `Select-String -Path "$src\*\*.java","$src\*\*\*.java"` 只传了 **1 层 + 2 层**两个通配，覆盖 30 + 10 = **40 / 47** 个文件；未覆盖 **7** 个（包根 2 个 + `roleComponent/meiqiHezi/{mainWeapon,passive,skill}/` 3 层 5 个）。二者中恰好有 2 处 `getInstance()`：
  - `roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java:74`
  - `roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java:81`
- **算术核对**：24（命令实测）+ 2（漏覆盖文件命中）= **26**（递归实测）= 指南汇总值。**断言值成立，命令不成立**。
- **复现**：见 `_meta/证据-附录C复核.txt` C-2 / C-11 段。
- **建议改法**：把附录 C 的计数命令改成递归形式 ——
  `Get-ChildItem "$src" -Recurse -File -Filter *.java | Select-String -Pattern '…' | Measure-Object`
  否则任何人照抄命令复核 I-1（阶段 2）都会得出错误的"已清零"结论。

### V-02（low）附录 C / §1 的源码行数（4 811）未复现

- **位置**：`debug-logs/最终重构指南.md:33`（§1「源码规模」）、`:861`（附录 C 汇总）。
- **事实**：当前基线实测 **4825 行**（`ReadAllLines` 与 LF 字节两种口径一致）。参考值：旧快照 `before/` 47 个 `.java` = 4794 行。
- **风险**：行数是"文档与代码是否脱节"的廉价指标，写错会让后续"文档说改完了"的判据失去锚点。
- **建议改法**：改为 `4825 行（2026-09-16 基线实测，ReadAllLines 口径）`，或删掉具体数字只保留"47 个 `.java`"；并注明计数口径（空行是否计入）。

### V-03（low）`Bukkit.getScheduler()` 15 处的归属只列了 14 处

- **位置**：`debug-logs/最终重构指南.md:861`（附录 C 汇总括注）。
- **事实**：括注写"`RoleInstance` 10、`BuffManager` 2、两个 listener 各 1" = 14；实测第 15 处在 `roleComponent/DefaultSanTEZeroPunishment.java:124`（`Bukkit.getScheduler().cancelTask(taskId);`）。**总数 15 正确**。
- **风险**：阶段 1.2/阶段 2 要处理"直接取调度器"，归属表少一处会漏改。
- **建议改法**：括注补 `DefaultSanTEZeroPunishment` 1 处。

---

## 8. 未能验证 / 未执行的动作（如实声明）

| 项 | 状态 | 原因 / 需要什么条件 |
|---|---|---|
| 端到端回滚演练（重建 HEAD 工作树 → 应用 `session-start-baseline.patch` → 与 `before/` 逐文件比对） | **未执行** | 不属 t4 范围（t4 只冻结新基线）；这是指南阶段 0.5、任务 t12 的交付内容。本次只验证了 `before/` 与 `integrity-sha256.txt` **登记值自洽**，未验证补丁可端到端重建。 |
| `.\gradlew build -x copyPluginJar` 复跑 | **未执行** | t4 的 acceptance/verify 不含构建；且 Gradle 必须串行，t4 期间不应占用文件锁。构建口径的独立复现属于 t7。本记录仅记录**既有产物**的新鲜度事实（附录 B 命令，C-12 段）。 |
| `.\gradlew runServer` 四行合格线 | **未执行** | 同上；且按 Captain 通知，起服四行合格线的**基线证据以 t2 的 `debug-logs/阶段0-交付小结.md` 为准**，t7 再独立复跑。 |
| `before/` 快照是否等于"作者 09-12/09-13 改动之前"的状态 | **未能验证** | `before/` 是 2026-09-15 21:56 的工作树副本，本身**已经包含**作者未提交改动；本任务不妄称它能代表 HEAD。 |
| 数值字面量多重集比对的基线值 | **未在本任务复算** | t4 只冻结文件哈希；多重集基数表将在各阶段收口时按 §6.2 现算并与其上一阶段比对。基线文件已冻结，任何阶段都能重算。 |

---

## 9. 给后续成员的复检命令（可直接粘贴）

```powershell
# 0) 前置：cd C:\Users\ROG\Desktop\插件\ShadowHunterRoles
$snap = "debug-logs\回滚快照\after-baseline"

# 1) 本基线之后被写过 / 新增 / 内容变化的文件（"本次改了什么"的唯一判据）
Get-Content "$snap\_meta\baseline-files.tsv" -Encoding utf8 | ForEach-Object {
  $p = $_ -split "`t"; $live = Join-Path (Get-Location) $p[0]
  if (-not (Test-Path -LiteralPath $live)) { "DELETED  $($p[0])" }
  elseif ((Get-FileHash -LiteralPath $live -Algorithm SHA256).Hash -ne $p[3]) { "MODIFIED  $($p[0])" }
}
Get-ChildItem src -Recurse -File | Where-Object { $_.LastWriteTime -ge [datetime]'2026-09-16 21:52:36' } |
  ForEach-Object { "TOUCHED  " + $_.FullName.Substring((Get-Location).Path.Length + 1) }

# 2) 快照自身完整性（51 = 51，0 处不符）
(Get-Content "$snap\SHA256.txt" -Encoding utf8).Count
(Get-ChildItem "$snap" -Recurse -File | Where-Object { $_.FullName -notlike "*\_meta\*" -and $_.Name -ne 'SHA256.txt' }).Count

# 3) 旧快照未被动过
Get-FileHash "$snap\..\integrity-sha256.txt" -Algorithm SHA256   # 期望 23C8F34C… 之外的根文件哈希见 §3.2
```

> **一句话总结**：新基线 `debug-logs/回滚快照/after-baseline/` 已冻结并通过 4/4 验收判据（51 文件逐字节 + 逐 mtime 一致、逐行 SHA256 可校验、旧 `before/` 48 文件未被改动、`src/` 零改动）；附录 C 的 3 条命令与 9 条汇总断言中 **9 条复现、2 条部分复现/未复现且均已定位为文档口径问题（V-01/V-02/V-03，全 low）**，无 blocker/high，可放行阶段 0 的实现工作。
