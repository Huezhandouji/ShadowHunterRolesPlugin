# 被迫停机交接（第二次 · 用户额度限额）

**性质**：用户因**额度已达限额**要求立即暂停一切任务。**非**任务失败、**非**质量事故。
**终态**：工作树干净、无服务器在跑、无后台作业。**上件 `被迫停机交接-20260919-2101.md` 已过时**（其称在办的 `t28`/`t29` 均已完成）⇒ **以本件为准**。

## 一、本会话已完成并全部入库
- `t1`–`t20`：阶段 5 的 4.4 渲染器接管 + 文档大修 + 初学者指南 5 份
- `t22`/`t24`：派发面能力化两刀 + 文档同步
- `t26`/`t27`：归零惩罚语义恢复 + 两处对齐（逐 tick 钉 0 · 回满推迟到 5 秒）
- `t28`：归零惩罚语义定稿后的文档同步
- `t29`：把「真空轮」陷阱 + verify 硬规则写进《任务卡编写指南》
- `t30`：**本插件全部指令加权限等级 ≥3 门禁 + 要求等级配置文件可调**（八相运行级全过）
- `t21`/`t25`：如实 **failed**（C-11 未闭合）；`t23`：流程事故**作废**
- 归档区 **11 代**制品；最新代 `68EAA080`（171579 B / 2026-09-20 20:29:17.252）

## 二、唯一未跟踪件：`todo.txt`（**用户自己的路线图**，未入库）
`
加入配置管理器
小规模重构角色组件系统，让需要物品栏的组件自己持有所在栏位，并提供一个方法在装配角色时指定栏位，
这样可用避免 Role 还需要维护一个组件栏位，降低耦合。
使得关于物品栏的组件提供一个比如 getItemStack 的方法，让框架直接从组件获取物品栏物品，
而不是自己管理组件物品的状态流转。框架应为 skill 和 mainWeapon 基类写好默认的物品获取逻辑，
如果有人需要特殊的物品渲染逻辑，可以自行重写。
`
**待用户裁定**：入库（例如挪成 `docs/路线图.md`）还是保持本地不入库。

## 三、开放项（按建议优先级）
1. **`saveDefaultConfig()` 缺失** ⇒ 配置默认值只在 jar 内生效、**不会落盘**到 `plugins/ShadowHunterRolesPlugin/` ⇒ 运维不手工建文件就发现不了 `command-permission-level`（需动**主类**，不在 `t30` 的 inScope 内）。**用户 todo 第 1 条「加入配置管理器」可作为其归宿。**
2. **`log-delta.ps1` 吞异常输出 `C=0`** ⇒「路径笔误 = 完美假绿灯」（实测四元组：甲 13/0/0/0 真空通过；乙 13/13/0/0 有效）。**写卡口径**已补进指南（`t29`），**工具本身未修**。
3. **C-11 运行级闭合**（仅静态级；三轮未闭合）—— 路线图与**一条已封死的路线**见 `开放项-C11运行级闭合的路线与一条已封死的路线.md`
4. **非继承组件的运行级派发证明**（需 src：测试用注册入口）
5. `DebugCooldownCommand:68` 仍是 `instanceof ActiveComponent`（命令面）· `getPlayerSanTE` 无角色时返回哨兵 **−78** · `plugin.yml` 的 `usage` 过时 · `src` 侧注释回收（`RoleInstance:79/:138`）· 属性修饰符残留 STUN · mineflayer 无法解析 Paper 1.21.11 的 `DUST`/`TOTEM_OF_UNDYING`/`SCULK_SOUL`

## 四、恢复时的环境事实
- **必须**前置 `\="\C:\Users\ROG\Desktop\插件\ShadowHunterRoles\.gradle-work"`（`C:\Users\ROG\.gradle` 被沙箱拒绝）
- 提交权**只在队长**；成员零 git 写；`debug-logs/**` 被忽略 ⇒ 新件须 `git add -f`
- CJK 用 `read` 工具或 `[System.IO.File]::ReadAllLines(…, UTF8)`；判大小写用 `-ceq`/`-cmatch`；`git grep` **看不见未跟踪件**；`javap` 不在 PATH（JDK 在 `.gradle-work`）
- **判据四忌**（本会话各踩过）：① 裸 grep 会命中**被引用的反例**；② git grep 对**未跟踪件恒空**；③ 路径笔误 ⇒ **真空通过**；④ PowerShell **内联子表达式**（把 `$(` … `)` 塞进字符串插值里）**解析失败** —— **命令没跑 ≠ 判据不成立**。正确写法：先把子表达式的结果赋给变量，再拼字符串。
