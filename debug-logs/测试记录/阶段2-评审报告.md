# 阶段 2 评审报告（t11）

> **执行者**：eng-审阅（reviewer）｜**任务**：t11｜**attempt**：1
> **评审对象**：阶段 2 平台层剥离（t9）—— 是否真正满足"**纯机械替换、行为不变**"的定位
> **被评审状态（钉死）**：commit **`6421eda`**（"checkpoint: 阶段 2 平台层剥离完成（t9）"）= 我**校验过的最早包含全部阶段 2 java 改动的提交**
> **执行时刻**：2026-09-17 00:0x（+08:00）
> **结论**：**verdict = pass**（0 blocker / 0 high / 0 medium；**2 条 low**）

---

## 0. 独立性与方法声明

- 本报告所有**计数与集合型结论均由我现跑命令取得**（§6.6「计数类结论必须先测量并留命令」条款）。命令与关键输出在 §1–§7 逐条给出。
- **关键方法**：我用 `git worktree add --detach … 6421eda` 检出**纯净的 phase-2 工作树**（50 个 `.java`）做不变量与调用点核验，避免与**阶段 3 的在途改动**（工作树中的 `RoleRegistry.java`，属下一阶段）混淆。核验后**已删除该 worktree**（`git worktree list` 只剩主树），**未留下残留**。
- 期间一次失败的尝试已作废并说明：`git archive | tar -x` 在 Windows 上损坏了流（`bad header checksum`），导出目录为空 → 那一轮"全部 0 命中"是**空目录伪值**，**不作数**；随后改用 worktree 重测（§1）。
- 我**未做任何 git 写操作**（`worktree add/remove` 属工作区管理，未改任何 ref/提交；主树 `git status` 未被我影响）。本次唯一写盘 = 本文件。

---

## 1. 不变量复核（**复算 grep，不引用他人结论**）

在 `6421eda` 的纯净树上：

| 不变量 | 检查命令 | 实测 | 判定 |
|---|---|---|---|
| **I-1** | `Get-ChildItem core,roleComponent -Recurse *.java \| Select-String 'ShadowHunterRolesPlugin\.getInstance\(\)'` → 空 | **0 命中** | ✅ **通过** |
| **I-2** | `Get-ChildItem roleComponent -Recurse *.java \| Select-String 'RoleManager\.getInstance\(\)'` → 空 | **0 命中** | ✅ **通过** |
| 全仓 `ShadowHunterRolesPlugin.getInstance()` | 同递归口径 | **0 命中** | ✅ 24 处替换完整、无遗漏 |
| 全仓 `RoleManager.getInstance()` | 同递归口径 | **2 命中，均为注释**（`RoleManager.java:73`、`platform/FactionLookup.java:7` 的 Javadoc） | ✅ **代码命中 = 0** |
| `Bukkit.getScheduler()` | 同递归口径 | **3 命中，全部在 `platform/BukkitSchedulerAdapter.java:18/23/28`** | ✅ 平台层唯一使用点成立 |
| `-1` 任务哨兵残留 | `=\s*-1\s*;\|!=\s*-1\|==\s*-1` | **0 命中** | ✅ 与 §5 的删除方向一致 |

> 说明：全仓 `getInstance()` 的**精确口径** —— `core 15 / roleComponent 5 / listener 2 / manager 2 = 24`（与 t9 小结 §2 一致）；我本节以**行为判据（=0）**为准，未依赖任何二手数字。

---

## 2. 24 处替换与改动清单是否一一对应

- `git diff --name-only <phase2~1> <phase2>` 与 `git show --stat 6421eda` **双重确认**：阶段 2 = **24 个 `.java`（其中 6 个为 `platform/` 新增）+ 2 个文档**（`debug-logs/阶段2-交付小结.md`、`debug-logs/测试记录/阶段2-起服日志-t9ModeB.log`）。
- **无资源文件、无构建脚本改动**（`src/main/resources`、`build.gradle.kts`、`settings.gradle.kts`、`gradle.properties` 均不在名单内）→ 满足"未顺带改动资源文件与文档"的约束（文档仅新增本阶段小结与起服日志，属交付物本身）。
- t9 小结 §2.2–§2.5 按 `core/roleComponent/listener/manager` 四组给出逐处"旧行号 → 新行号+形态"，分组合计 15+5+2+2 = **24**，与我的独立计数一致。

---

## 3. `SKILL_KEY` 处置（阶段 4.4 前不得删除）与 `KeyFactory` 接入

| 检查项 | 实测 | 判定 |
|---|---|---|
| `Skill.Utils.SKILL_KEY` 是否仍存在 | **是**：`core/Skill.java:126` `public static final NamespacedKey SKILL_KEY = KeyFactory.Registry.of("skill_id")` | ✅ 常量未删除，符合"阶段 4.4 统一渲染器前不得删"的约束 |
| 键的**取值等价性**（防静默破坏持久化） | 旧 `new NamespacedKey(ShadowHunterRolesPlugin.getInstance(), "skill_id")`；新 `KeyFactory.Registry.of("skill_id")` 由主类注入的 `key -> new NamespacedKey(this, key)`（`ShadowHunterRolesPlugin.java:36`）解析 → **namespace = 插件名、key = `"skill_id"`，逐字相同** | ✅ **值等价**（NBT/PDC 数据不会因阶段 2 失配） |
| 其余 3 个静态键 | `DamageUtil.java:20 LAST_DAMAGER_KEY`、`MainWeapon.java:117 MAIN_WEAPON_KEY`、`BuffManager.java:19 BUFF_MOVEMENT_SPEED_MODIFIER_KEY` 均已改走 `KeyFactory.Registry.of(...)`，**键名逐字保留** | ✅ |
| 初始化顺序风险（我单独查的一项） | `install(keys)` 位于 `onEnable` **第一段**（`:36-37`），早于任何组件/技能类加载；静态键在**类初始化时**取值 —— 若在 `install` 前触达会抛明确的 `IllegalStateException` | ✅ **且优于旧代码**：旧实现同场景会取到 `getInstance()==null` 而抛 **NPE**，新实现是**更清晰**的失败 |

---

## 4. `RoleManager` 去单例后的生命周期

| 检查项 | 实测 | 判定 |
|---|---|---|
| 静态单例是否真删除 | 旧：`private static RoleManager instance` + `getInstance()` + `private RoleManager()`；新：**三者全部消失**，改为 `private final RolesContext context` + `public RoleManager(RolesContext context)`（`RoleManager.java:16/18`） | ✅ |
| `onEnable` 构造持有 | `rolesContext = new RolesContext(...)` → `roleManager = new RoleManager(rolesContext)`（`:40`）；监听器/命令经构造注入（`:50-51` 等） | ✅ |
| `onDisable` 释放 | `onDisable` 调 `roleManager.clearAllPlayersRole()`；`RoleManager.java:96-100` 内逐个 `instance.clear()` 后 `playerRoleMap.clear()` | ✅ 实例与任务随之释放 |
| 是否仍有"外部可构造"的隐患 | 构造器为 `public`，但**全仓唯一构造点 = 主类**（我按 `new RoleManager(` 全仓检索确认）；`RoleAPI` 接口**零改动**（对外契约未破） | ✅ 可接受（阶段 3 由 `RoleLoader` 接管时再收窄） |

---

## 5. 行为等价性：我自己的多重集与**逐跳**核查

### 5.1 我独立计算的数值差异

| 口径 | before（`after-phase1` 快照） | after（`6421eda`） | 删除方向 | 新增方向 |
|---|---|---|---|---|
| 数值字面量（我自己的抽取器：剔注释/字符串） | **382** | **377** | **5** | **0** |

**5 条删除我逐条定位到原文**（`after-phase1`）：

| # | 位置 | 原文 | 现形态 |
|---|---|---|---|
| 1 | `core/RoleInstance.java:69` | `private int updateTaskId = -1;` | `private Task updateTask;` |
| 2 | `manager/BuffManager.java:26` | `private int updaterTaskId = -1;` | `private Task updaterTask;` |
| 3 | `manager/BuffManager.java:168` | `if(updaterTaskId != -1){` | `if(updaterTask != null){` |
| 4 | `manager/BuffManager.java:170` | `updaterTaskId = -1;` | `updaterTask = null;` |
| 5 | `roleComponent/DefaultSanTEZeroPunishment.java:30` | `private static final int NO_TASK_ID = -1;` | 常量与哨兵一并删除，改 `punishmentTask == null` |

→ **删除方向 = 5、新增方向 = 0**，且**全部命中"哨兵 → 空句柄"这一模式**（与 t9 申报的类别 (c) 一致）。

### 5.2 逐跳等价性论证（"未启动/已取消"判定是否改变）—— 这是我本节的核心

| 跳 | 旧代码 | 新代码 | 我的判定 |
|---|---|---|---|
| `BuffManager.clearAll()` | `if(updaterTaskId != -1){ cancelTask(id); }` | `if(updaterTask != null){ cancel(); updaterTask = null; }` | ✅ 等价：旧代码唯一的"!= -1"是"已启动过"；该任务由 `startUpdater()` 无条件在构造期启动（快照 `:33`）→ `-1` 分支**不可达**；反之若真取到 `-1`，旧代码会走 `cancelTask(-1)` 并**打出一条 WARNING**，属错误路径，不构成需保留的行为 |
| `RoleInstance` ticker | `cancelTask(updateTaskId)`（无条件） | `if(updateTask != null){ cancel(); null }` | ✅ 等价：`updateTaskId` 在构造/激活后必有值；新代码的判空**只会**挡住"本来就没有任务"的情形（取消一个不存在的任务在旧代码里是 no-op 或 WARNING） |
| `DefaultSanTEZeroPunishment` | `if(taskId != NO_TASK_ID){ cancelTask; taskId = NO_TASK_ID; }` | `if(punishmentTask != null){ cancel(); punishmentTask = null; }` | ✅ **逐一对应**：`cancelPunishmentTask()` 在**起新任务之前**调用（`DefaultSanTEZeroPunishment.java:37`），重入保护与 O-6 语义保持；任务体自身早退与 `count>=3` 自取消均保留 |

### 5.3 字符串差异（1 条新增）

t9 申报 **197 → 198 / 新增 1 = KeyFactory 的守卫文案**（`"KeyFactory is not installed yet (onEnable did not finish)."`），**仅在被破坏的初始化顺序下可达**，正常运行不可见 → 我判定为**实现细节的失败路径文案**，非可见行为变化 ✅

---

## 6. 构建与起服（我独立复核，档位如实）

| 项 | 实测 | 判定 |
|---|---|---|
| 构建 | t9 用 `--rerun-tasks`；jar = **106 457 B / 23:38:59 / SHA256 `AF8E6BDB…92B1`**；最新 `.class` = **23:38:58**（**用 .class 文件 mtime，非目录 mtime**，符合 §6.6 新口径）；**`src 新于 jar = 0`**；jar 内含 `platform/` 的 8 个 class（含匿名/嵌套类） | ✅ 满足"BUILD SUCCESSFUL + 产物 mtime 刷新" |
| 起服（Mode B） | `debug-logs/测试记录/阶段2-起服日志-t9ModeB.log`（6 137 B）：**`:46` `Enabling ShadowHunterRolesPlugin v1.0.0`**、**`:47` `ShadowHunter Character System enabled.`**、**`:51` `Done (25.053s)!`**；第 3 行（SHDF 依赖发现）**0 命中**；`Bukkit plugins (1)` | ✅ Mode B 三行齐 + 第 3 行缺席符合预期 |
| 零新增错误（两门口径，我自算） | 级别门 `/(ERROR\|SEVERE\|FATAL)]` = **0**；插件门 `[ShadowHunterRolesPlugin]` + `/(WARN)]` = **0** | ✅ 通过 |
| `RoleAPI` 冻结 | 接口零改动（含 `RoleAPIImpl` 仅为注入适配） | ✅ §6.4「只增不改」未破 |

**运行深度如实说明（我不替 t9 拔高）**：本次起服只到"启用 + 空闲"，**无玩家选角色、无组件运行**。因此**组件侧新调度端口的运行期行为（任务是否按 20t/1t 周期正确触发）未在阶段 2 被实测** —— 属 t11/t15 范围。我把这条列为"**未实测项**"，不据此加减分，但**必须挂账**。

---

## 7. findings

| ID | 级别 | 问题（精确到 文件:行号） | requiredFix |
|---|---|---|---|
| **T11-L1** | **low** | `RedSolitaryArroganceSkill` 由 Folia `GlobalRegionScheduler.runAtFixedRate(...)` 改为平台 `Scheduler.runRepeating(...)`（→ `BukkitSchedulerAdapter.java:28` 的 `runTaskTimer`）。**旧形态的 `retired` 回调被一并移除**——该回调在**插件 disable** 时会收到通知并 `cancel()` 任务；新形态**没有等价钩子**。我判定**实际无可见变化**（`onDisable → clearAllPlayersRole() → instance.clear() → triggerLifecycleStop() → 组件 stop() → attackTask.cancel()` 仍会取消它），但**这是本阶段唯一一处我从"逐跳"而非"结构"上确认等价的地方，且小结未把 retired 回调的消失写进申报**，属交付物未量化的耦合差异 | 在 `debug-logs/阶段2-交付小结.md` §8 补一句：「`RedSolitaryArroganceSkill` 迁移到平台端口后，**Folia 的 `retired` 回调不再存在**；其在 disable 时的自动取消职责由 `onDisable → clearAllPlayersRole → 组件 stop()` 承担（已逐跳确认）」。**只需补记，不需改代码**（我未发现实际行为回退） |
| **T11-L2** | **low** | 数值多重集 382→377 的 5 条删除中，第 2 条（`BuffManager.java:168` 的 `if(updaterTaskId != -1)`）在旧代码下若真取到 `-1` 会走 `cancelTask(-1)` 并**打出一条 WARNING**（"Unknown task id"）。新代码的 `null` 判空**使该警告路径不可达**。该路径**同样不可达**（任务在构造期无条件启动），故无可见变化；但"删除了一条候选警告路径"属**未申报的细节** | 在 `debug-logs/阶段2-交付小结.md` §7.1 的 5 条归属表下方补一行：「其中 `updaterTaskId != -1` 的旧分支同时删掉了一条**不可达**的 `cancelTask(-1)` WARNING 路径；正常运行无影响」。**只需补记** |

> **无 blocker / high / medium。** 两条 low 均为**交付物补记**，不改变任何行为、不阻挡阶段 3。

### 7.1 我检查过但**未发现问题的项**

1. I-1 = 0、I-2 = 0（纯净 phase-2 树现测）；2. 全仓 `getInstance()` = 0；3. `RoleManager.getInstance()` 代码命中 = 0（余 2 处为注释）；4. `Bukkit.getScheduler()` 仅 3 处且全在 Adapter；5. `-1` 哨兵残留 = 0；6. `SKILL_KEY` 保留且**键值逐字等价**；7. 其余 3 个静态键键名保留；8. `install()` 在 `onEnable` 第一段，失败模式比旧 NPE 更清晰；9. 静态单例（字段+方法+私有构造）确实删除；10. `onEnable` 构造持有、`onDisable` 释放路径完整；11. `RoleAPI` 接口零改动；12. 阶段 2 未触碰 `src/main/resources` 与任何构建脚本；13. 阶段 2 改动面恰为 24 java + 2 docs；14. 数值 5 删 0 增、逐条定位、全为"哨兵→空句柄"；15. 字符串 1 增为失败路径文案；16. 构建产物新鲜度（最新 `.class` 口径 + `src 新于 jar = 0`）；17. Mode B 三行齐 + 第 3 行缺席；18. 两门口径均 0；19. `DefaultSanTEZeroPunishment` 重入保护（先取消后起）保持；20. 未发现 inScope 外改动。

---

## 8. 必须挂账

| 挂账项 | 状态 | 落点 |
|---|---|---|
| **组件侧新调度端口的运行期行为**（任务周期、组件 update 是否照常触发） | **未实测**（阶段 2 起服只到"启用+空闲"，无玩家） | **t15 / t18 运行期验证** |
| t7-L1（ticker/updater 停止的运行级证据）与 t10 的两项开放项 | 继续挂账 | t24 / t19 |

---

## 9. verdict

**pass**（0 blocker / 0 high / 0 medium；**2 条 low**，均为交付物补记）。

**依据**：I-1/I-2 在**纯净 phase-2 树**上由我**复算 grep** 得 0；全仓 `getInstance()` = 0、`RoleManager.getInstance()` 代码命中 = 0、`Bukkit.getScheduler()` 收敛到 Adapter 单点、`-1` 哨兵归零；24 处替换的分组计数（15/5/2/2）与改动清单**一一对应**；`SKILL_KEY` 保留且**键值逐字等价**（未破坏既有 PDC 数据）；`RoleManager` 静态单例三件套（字段/方法/私有构造）确实删除，`onEnable` 持有、`onDisable` 释放路径完整；**行为等价性我做了逐跳论证**：数值 5 条删除全为"哨兵→空句柄"且新增 = 0，字符串 1 条新增为失败路径文案，**未发现可见行为变化**；构建（最新 `.class` 口径 + `src 新于 jar = 0`）与 Mode B 起服（三行齐、两门口径 0）达标；改动面严格限于 24 + 2，资源与构建脚本零改动；`after-phase1` 快照齐备 ⇒ **可按文件回滚**成立（6 新文件整体删除、18 改文件按快照覆盖）。2 条 low 已给出可执行的补记项，不构成阻挡。

**评审人**：eng-审阅（attempt 1）
