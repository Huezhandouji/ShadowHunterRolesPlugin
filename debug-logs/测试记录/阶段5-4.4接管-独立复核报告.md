# 阶段 5 · 4.4 接管与交接收口的独立复核报告（t4）

> **被复核对象**：`t2`（4.4 渲染器接管实现）· `t3`（交接两尾巴收口）
> **判据来源**：`debug-logs/测试记录/阶段5-需求与验收.md`（t1 冻结件；本件不引用实现者与需求件的**读数**，只把它们的**结论**当对照）
> **执行者**：`eng-验证`（本卡 = t4）· **边界**：不改 `src`、不改任何既有冻结件、零 git 写；`inScope` 三件证据文件：本报告 · `阶段5-起服日志-4.4复核.log` · `阶段5-4.4复核-停服物证.txt`
> **身份纪律**：身份一律**现算**（bytes + SHA256）；行号 = 现算现值（移动靶，本件同时给符号）；计数一律给「**模式 + 定域 + 口径**」；本件**不内嵌自身哈希**

---

## §0 结论（verdict + 分级计数）

- **verdict = pass**（对本卡的静态级 + 运行级范围）
- **分级计数：blocker 0 · high 0 · medium 1 · low 4**（逐条见 §9）
- 一句话：**t1 冻结的判据 C-01…C-14 / C-16 全部独立复算通过**；`HotbarRenderer` 已是**唯一写物品路径**（删除了 `updateHotbar()` 等 legacy 渲染方法，渲染写点收敛到 `core/hotbar` 内 1 处）；我**自出一轮**运行级证据（提交态、Mode B 三行齐、集合差只看 `C=0`、级别字段扫描 = 0、停服三路互证、F1 链 ORDER 通过）；`t3` 的两条尾巴按要求收口。
- **唯一 medium 是共享仪器缺陷**（不是被复核的代码问题，且 t2 已按真值处置并申报）⇒ 见 F-1。
- **未在本卡范围内闭合的一项**：**外观级**（四态 × 两 kind 的运行期截图/dump、PDC 读回、秒数逐 tick 递减的平滑性）= **C-15**，归 `t5`（本件按 A15 如实申报，不冒充运行级）。

---

## §1 开工基线与提交态（A7 / A8 / A16）

| 项 | 现算值（命令 = 见 §12 清单） |
|---|---|
| 开工时 HEAD | `b06225298ddf7e1a4cf384550ee04f9bfcc55d49`（t2 的 6 个 src + MANIFEST 已由队长入库） |
| 过程中 HEAD 前移 | `11bb7926125cf7a6e7515ab9dea2ed3866cc9a99`（队长补入库本代归档件；**未触碰 src**） |
| 被验证树 | `src/main` tree = **`a6a33c9b85a82ea25b15f8417e9dc880c588ecdb`**（`b062252` 与 `11bb792` 同值；`git diff b062252 HEAD -- src/main` = 0 行） |
| `git status --porcelain` | **0 行**（开工时与在线取证时均为 0）⇒ 本轮 = **提交态取证** |
| 本代 jar（`build/libs`） | **159512 B** / mtime **2026-09-19 12:54:07.286** / SHA256 **6A249993ECDE32C9AAB4E651191C5277C198F97DB5C35A603FC68893C67FF734** |
| 归档件（`…-verified-20260919-125407-6A249993.jar`） | **同 bytes / 同 mtime / 同 SHA256**（与 `build/libs` 逐字节相同）· 该归档件**已在 HEAD 内** |
| `MANIFEST.txt` 登记 7 | 与现算逐条一致（bytes 159512 / mtime 12:54:07.286 / SHA256 `6A249993…`） |
| **未重建证据** | runServer 的 Gradle 输出含 **`> Task :jar UP-TO-DATE`**（另 `:compileJava` / `:processResources` UP-TO-DATE）；jar 三时点（开工 13:04:57 / 起服前 13:08:37 / 结卡前）**逐字一致** |
| 本轮执行过的 gradle 任务 | 仅 `compileJava --console=plain`（`BUILD SUCCESSFUL in 14s`，jar 与最新 `.class` mtime 均未变）+ `runServer`（`:jar UP-TO-DATE`）；**未** `clean` / **未**删 `build/libs` / **未**强制 rebuild / **未** `build` |
| 重建等价性检查 | **未做**（无需要：本代制品本身有运行证据；t1 §9.3 允许的"副本上重建比对"在本卡无诉求） |
| 零 git 写 | 本卡未执行 `add/commit/checkout/restore/stash/reset`；本卡改动面 = 三份证据文件（均在 `inScope` 内，`debug-logs/**` 属 `.gitignore:84` 忽略 ⇒ 由队长 `-f` 入库） |

---

## §2 判据逐条自算（C-01 … C-16；A1 / A3）

> 每条都先做**判据自检**（§2.10 汇总）：用「已知有命中」样本证明 pathspec/仪器落到文件，并给出**负对照**。所有命令默认大小写敏感、`git -C ShadowHunterRoles` 前缀、作用域 = 仓库相对 POSIX 路径。

| 编号 | 定域 | 匹配形态 | 扣除口径 | 期望（t1 冻结） | 我现算 | 判定 |
|---|---|---|---|---|---|---|
| C-01 | 逐文件（3 条命令） | ERE `\bmarkDirty\(\);` / ERE 三串择一 | 仅调用点 | RoleInstance 3 · CooldownPortImpl 1 · BuffManager 1（宿主 = `removeBuff`） | **3 / 1 / 1**（RoleInstance:202/219/537 · CooldownPortImpl:53 · BuffManager:109）· BuffManager 宿主方法 = `removeBuff`（定义在 `:99`；`addBuff` 定义在 `:35` 且**无**置脏） | **pass** |
| C-02 | `src/main/java` | ERE `\bmarkDirty\(\);` | 仅调用点（结构性排除定义行/javadoc；注释行逐行判读） | after = **5** | **5**（CooldownPortImpl:53 · RoleInstance:202/219/537 · BuffManager:109）；**5 条全为语句行** | **pass** |
| C-03 | `src/main/java` | ERE `::markDirty\b\|\bmarkDirty,` | 行级（含注释；命中 0 无此情况） | after = **1** | **1**（RoleInstance:87 `private final Runnable markHotbarDirty = hotbarRenderer::markDirty;`） | **pass** |
| C-04 | `src/main/java` + 单文件 | ERE `isDirty\(\)\|clearDirty\(\)` · ERE `public void triggerUpdate` | 行级；扣定义行 | 三段同处且有序（isDirty → 写物品 → clearDirty） | `triggerUpdate()` = RoleInstance:719，方法尾 **736 `if(hotbarRenderer.isDirty() \|\| hasCoolingSkill()){` → 737 `hotbarRenderer.render();` → 738 `hotbarRenderer.clearDirty();`** 同方法体内、顺序正确 | **pass** |
| C-05 | `src/main/java` | ①固定串（裸）②ERE `\.clearDirty\(\);` | ①含定义 ②仅调用点 | after：裸 **2** / 调用 **1** | **2**（RoleInstance:738 调用 + HotbarRenderer:62 定义）/ 调用 **1** | **pass** |
| C-06 | `src/main/java` | ①固定串 ②ERE `\.isDirty\(\)` | ①含定义 ②仅调用点（不要求 `;`） | after：裸 **2** / 调用 **1** | **2**（RoleInstance:736 + HotbarRenderer:57）/ 调用 **1** | **pass** |
| C-07 | ①②④ `src/main/java`；③ `core/hotbar/` | 固定串（**带左括号**） | 裸计数；渲染写点 = ① − ② | after：① **3** · ② **2** ⇒ 写点 **1** 且 **③ = 1** | ① **3**（RoleInstance:478/:489 清空 + HotbarRenderer:81 渲染）· ② **2** · ⇒ **写点 = 1** · ③ **1**（HotbarRenderer:81 `inv.setItem(entry.getKey(), buildIcon(item));`）· ④ `setItemMeta(` **9**（不计入） | **pass** |
| C-08 | `src/main/java` | 固定串、签名限定 | 裸计数（归零组合法） | after = **0 / 0 / 0** | `createIconItem` **0** · `getDisplayName(RoleInstance` **0** · `getDisplayName(this)` **0** | **pass** |
| C-09 | `src/main/java` | ①固定串 ②ERE `\bupdateHotbar\(\);` ③ERE 定义 ④固定串 | ①含定义/注释 ②仅调用点 ③定义行 ④裸计数 | after：**0 / 0 / 0 / 0** | **0 / 0 / 0 / 0**（连**裸词** `updateHotbar` 也 = 0） | **pass** |
| C-10 | `src/main/java` | ERE 两符号分开数 + 固定串 | 含定义 | after = **0 / 0** | 结构口径 **0** · 裸词 `updateSkillItemMeta` **0** | **pass** |
| C-11 | `roleComponent`（目录前缀） | ERE 四串择一 | 含注释/javadoc | after = **0** | **0**（自检样本同 pathspec 命中 1 ⇒ 非假 0） | **pass** |
| C-12 | 单文件 | ERE record 组件声明行 | 行级 | after = **10** | **10**（`ComponentServices.java:11–:20`，逐行可读） | **pass** |
| C-13 | `api/**` | 三条命令 | 只针对本批路径 | 两条均空 + `ls-files` = 2 | `status --porcelain` **0 行** · `diff --name-only 42dde5e` **0 行** · `ls-files` **2** | **pass** |
| C-14 | 本批冻结日志 | Mode B 三行 / 集合差 / 级别字段 / F5 物证 | 静态级 vs 运行级分开 | 全部成立 | **由我自出一轮达成**：Mode B 3 行齐 · `C=0` · 级别字段 = 0 · 当轮物证（在线冻结 + 只 `job_kill` + 三路复证）见 §5 | **pass（本轮）** |
| C-15 | 外观级（四态 × 两 kind × 六维 + PDC 读回） | dump/截图 | 可见属性（非 Component 文本树） | 逐格一致 | **不在本卡范围**：本卡与 t2 轮**均无玩家/机器人** ⇒ 只有**静态级**对照（§6）；运行级归 `t5` | **未覆盖（已申报）** |
| C-16 | 制品代次 | 三时点 `Length`/`LastWriteTimeUtc`/`SHA256` | — | 本批只一代 · `t4` 不重建 | 三时点逐字一致（§1）；归档 + 登记 7 齐；**未重建** | **pass** |

### §2.1 收敛组两条的**裸计数与差值来源**（A5 明令）
- `markDirty()` 仅调用点 = **5**（上表 C-02）。
- 裸串 `markDirty`（固定串，含定义/注释/文案）= **9**；差值 **4** 逐行可解释：
  RoleInstance:87（方法引用 `hotbarRenderer::markDirty`，**无括号** ⇒ 不属 C-02/C-03 的互斥口径之一）· HotbarRenderer:26（javadoc `{@link #markDirty()}`）· HotbarRenderer:30（注释"组件可调用的 markDirty"）· HotbarRenderer:53（定义行 `public void markDirty() {`）。
- `markDirty\(\)`（ERE，含定义与 javadoc 提及）= **7**；差值 **2** = 定义行 + javadoc 行。
- 方法引用形态（C-03）= **1**；其**消费者**（同一持有者的三条程序化路径，**不新增直呼点**）：`runLater(markHotbarDirty, …)`（RoleInstance:270 / :337）· `markHotbarDirty.run();`（endCooldown:377 / scanCooldowns:399）⇒ 与 t1 §2.7 冻结拓扑**逐条对应**。

### §2.10 判据自检（A11）与负对照
| 自检样本 | 期望（before 口径） | 我现算 | 说明 |
|---|---|---|---|
| `stateOf` @ `core/hotbar` | 2 | **4** | pathspec 落到文件（渲染器扩容：javadoc :25/:67 + 调用 :94 + 定义 :181） |
| `stateOf` @ `core/hotba`（负对照） | 0（假 0） | **0** | 末段不是文件 ⇒ 该 0 **不是干净** |
| `addBuff` @ BuffManager | 1 | **2** | 定义 :35 + 注释 :108（R-1 声明行）⇒ 落到文件 |
| `\bmarkDirty` @ HotbarRenderer | 6 | **3** | javadoc :26 + 注释 :30 + 定义 :53 ⇒ 落到文件 |
| `::triggerUpdate\b` @ src | — | **1** | "方法引用形态"可命中（**替代样本**，见 F-2） |
| `::updateHotbar\b` @ src（C-03 原样本） | 2 | **0** | **原样本被被测改动本身作废**（`updateHotbar` 已删）⇒ 见 F-2 |
| `hasCoolingSkill` @ src | — | **2** | C-09 的替代样本（:736 引用 + :747 定义） |
| `BuffType.` @ roleComponent | 1 | **1** | 无此样本，C-11 的 0 就是假绿灯 |
| `ls-files -- …/api` | 2 | **2** | C-13 的 pathspec 有效 |
| `setItemMeta(` @ src | 11（before） | **9** | 旧渲染三处 `setItemMeta` 随方法删除（11 = 9 + 旧 3 − 渲染器新增 1） |

---

## §3 归零组 / 收敛组 / 唯一渲染者（A4 / A5 / A6）

- **A4【归零组】**`git grep -nE "createIconItem\(RoleInstance|getDisplayName\(RoleInstance" -- …/core/Skill.java …/core/MainWeapon.java` ⇒ **0 / 0**（两文件各自 0 命中）。
- **A5【收敛组】**`markDirty();` 仅调用点 = **5**；方法引用 = **1**；裸计数与差值来源见 §2.1。
- **A6【唯一渲染者】** 三层证据（**独立于 t2 的结论**）：
  1. `clearDirty()` 调用点 = **1**（RoleInstance:738，flush 尾）⇒ 有消费者；
  2. **写物品路径收敛**：`setItem(` 全树 = 3，其中清空写 2（RoleInstance:478/:489）⇒ **渲染写点 = 1**，且该唯一写点 **在 `core/hotbar/HotbarRenderer.java:81`**（`core/hotbar` 内 `setItem(` = 1）；
  3. **第二条路径已不存在**：`updateHotbar` 裸词 = **0**（调用点 0 + 定义 0）· `shouldUpdateHotbar` = **0** · `updateSkillItemMeta` / `updateAllSkillItemMeta` = **0**；并横扫其它物品写入 API：`setItemInMainHand(` = 0 · `setContents(` = 0 · `addItem(` = 0 · `getInventory().setItem` = 0。
  ⇒ **「HotbarRenderer 是唯一渲染者」在本卡口径下成立**（我开工前预申报的"`updateHotbar` 仍是第二条写物品路径"风险已按"删除"化解）。
- **旧渲染方法的调用点**（before，独立枚举于 `42dde5e5`）只有 3 处且都是 `this`：RoleInstance:470 / :477 / :504 ⇒ 旧 `instance == null` 短路与 `getDisplayName(RoleInstance)` 的 `"RoleInstance is Null!"` 文案属**不可达路径**，其消失不构成可见差异（渲染器 javadoc 亦然如此申报）。

---

## §4 基线自证（A2；不只自洽）

- **被比对方**：`.scratch\bot\src-snapshots\t44takeover-before`（92 个文件 + `SHA256.txt`）↔ 点名提交 **`42dde5e5069ff99876eea9a35977b47a0037dc95`** 的 `src/main` tree = **`f650384235fc328f3233397d8f9d0aed3ca5f2e5`**。
- **方法**：快照目录 `main/…` ↔ 仓库 `src/main/…` 做**内容身份比对**（两侧都用 `git hash-object` / `git rev-parse <commit>:<path>` 的 **blob SHA1**，不引行号、不引 mtime）；并以"未改动文件（`api/RoleAPI.java`）的 worktree 哈希 = `HEAD` blob 哈希"证明**该比对方法无 filter 偏差**。
- **结果（两侧计数）**：
  - `SHA256.txt` 登记 **91** 条（解析 0 失败）；**manifest 自洽 = 91/91**；
  - **快照逐件 blob == commit blob = 91/91，0 mismatch**；
  - 集合比对：commit 侧 **92** 件 vs 快照侧 **92** 件，**only-in-commit = 0 · only-in-snapshot = 0**；
  - **1 件未登记** = `main/resources/plugin.yml`（见 F-5；其对 commit blob 同样逐字节相同）。
- ⇒ 基线**被点名提交独立证明**，不是"自己跟自己对"。

---

## §5 运行级（A9；本轮 = 我自出的一轮）

- 窗口纪律：广播（eng-实现 / eng-取证 / captain）→ 三路互证（netstat LISTENING `:25566` = 0 行 · TCP connect 拒绝 · latest.log 独占打开 OK · java = 0）→ 起服。
- **Mode B 三行齐**（冻结件 `阶段5-起服日志-4.4复核.log` = 5698 B / 54 行 / SHA256 `8268E32C…`）：`Enabling ShadowHunterRolesPlugin v1.0.0`（13:09:03）· `ShadowHunter Character System enabled.`（13:09:04）· `Done (14.850s)!`（13:09:04）。
- **正序集合差（只看 C = 0）**：`log-delta.ps1`（锚 `阶段4-起服日志-T2b.log`）⇒ anchor/current 各 13 行、ADDED = 0、REMOVED = 0、A) oshi = 0、B) world = 0、**C) OTHER = 0**。
- **级别字段扫描** `\] \[[^]]*/(ERROR|SEVERE)\]` = **0**；裸 `ERROR|SEVERE` = 3（全为 oshi WMI 的 **WARN 正文**"Error was …"）⇒ 锚定级别字段是必要的。**范围声明**：该扫描覆盖**同一 artifact identity 的全部已冻结轮**（t2 轮 56 行 + 复核轮 54 行），两轮均 0/3。
- **制品载入自绑定（哈希级）**：`run/plugins/.paper-remapped/extra-plugins/index.json` 记录 `6A249993…: ShadowHunterRolesPlugin-1.0.0-1789793818094.jar` ⇒ Paper 按**源 jar 的 SHA256** 登记 remap 结果 ⇒ 本轮载入的插件 = 本代制品（与 `build/libs`/归档件 SHA256 逐字相同）。附注：本轮日志**无** `[PluginRemapper] Remapping` 行（缓存命中复用），故**不引"日志自述路径"**。
- 停机：**仍在线时先冻结**（共享读流复制，13:11:59.139；源 5698 B / mtime 13:09:05.863，**逐字节相同**）→ **只 `job_kill`**（未强杀 java）→ 三路复证（LISTENING = 0 · TCP 拒绝 · 独占打开 OK · java = 0）。
- **假新鲜三条件**（队长强制判据）：① job 启动时刻 = **13:08:37.393**；② 冻结内容起点 = **13:08:50** > ①（源 mtime 13:09:05.863 > ①）；③ 冻结件 SHA `8268E32C…` **≠** `…冷却分支补齐.log`（`73EEF15D…`）且 **≠** `…4.4接管.log`（`527183B0…`）⇒ 三条同时成立。自曝：未加时间闸门时，旧件正文里的 `Done (` 会让"本轮已起服"成立（我 13:08:43/13:08:47 两次轮询读到的就是旧件）。
- **F1 链（机器校验）**：jar `12:54:07.286` < log `13:11:59.139` < 物证 `13:15:01.821` ⇒ **ORDER = True**（含本报告后为四环，见 §11）。
- **mtime 语义（口径，非缺陷）**：冻结件 mtime = **冻结时刻**（新建文件）；源 mtime = **服务端最后写日志时刻**；两者**都晚于** jar ⇒ ORDER 在两种口径下都成立。与 t2 轮观察一致（复制件与源同 SHA256、不同 mtime）。

---

## §6 冻结可见差异独立比对（A10；**静态级**，运行级归 t5）

**方法**：不看实现者的结论，直接读**改动前**（`42dde5e5` 的 `Skill#createIconItem` / `#getDisplayName(RoleInstance)`、`MainWeapon#createIconItem` / `#getDisplayName(RoleInstance)`，逐行）与**改动后**（`HotbarRenderer#buildIcon` / `applySkill` / `applyMainWeapon` / `stateOf`）的**逐分支对照**，再与 t1 §5.2/§5.3 两张表逐格核对。状态顺序 = `stateOf`：`!ready → COOLDOWN` → `!canCast → DISABLED` → `energy < energyCost → ENERGY_LACK` → `READY`（与旧 `createIconItem` 的分支顺序一致）。

| 状态 × kind | 材质 | 名字（颜色 + 装饰 + 后缀） | lore 行序 | PDC | 判定 |
|---|---|---|---|---|---|
| 技能 COOLDOWN | `STRUCTURE_VOID` | GRAY+BOLD 基名 + 追加 `" " + String.format("%.1f", remainingSeconds) + "s"`（追加段自身 GRAY+BOLD） | `["Skill is on cooldown.", "====================", description]` | `Skill.Utils.SKILL_KEY` → `getId()` | **逐字一致** |
| 技能 DISABLED | `BARRIER` | RED+BOLD + `" DISABLED"` | `["Skill has been disabled.", …]` | 同上 | **逐字一致** |
| 技能 ENERGY_LACK | `STRUCTURE_VOID` | GRAY+BOLD + `" ENERGY LACK"` | **只有公共两行**（无状态行，非对称保留） | 同上 | **逐字一致** |
| 技能 READY | `getIcon()` | GREEN+BOLD，**无后缀** | `["Skill is ready.", …]` | 同上 | **逐字一致** |
| 主武器 COOLDOWN | `STRUCTURE_VOID` | GRAY+BOLD，**不带秒数**（冻结差异保留） | `["MainWeapon is on cooldown.", …]` | `MainWeapon.Utils.MAIN_WEAPON_KEY` → `getId()` | **逐字一致** |
| 主武器 DISABLED | `BARRIER` | RED+BOLD + `" DISABLED"` | `["MainWeapon has been disabled.", …]` | 同上 | **逐字一致** |
| 主武器 ENERGY_LACK | **不可达**：`super(…, 0, …)`（MainWeapon:32）且能量被 `Math.clamp(amount,0,max)`（RoleInstance:535）⇒ `energy < 0` 永假 | 渲染器该分支**空实现**（不造新外观） | — | — | **与旧实现一致（旧无该分支）** |
| 主武器 READY | `getIcon()` | GREEN+BOLD，**无后缀** | `["MainWeapon is ready.", …]` | 同上 | **逐字一致** |

- **公共两行 lore** 对**所有状态 × 两种 kind** 都追加（含"能量不足态只有这两行"这条非对称）⇒ 与旧实现逐字一致。
- **`『技能冷却名带 x.xs 秒数、主武器不带』成立**：`%.1f` 全树 **1** 处，且位于 `HotbarRenderer:136` 的**技能 COOLDOWN 分支**；主武器分支（`:158`）无秒数；`Locale`（定域 `core/**`）= **0**（R-5 未破坏）；`String.format` 全树 = **2**（渲染器 `%.1f` + `command/DebugCooldownCommand` 的 `%.2f`）。
- PDC 键与 id 来源：`"skill_id"` = 1（`Skill.java:40`）· `"main_weapon_id"` = 1（`MainWeapon.java:47`），**均不在 `core/hotbar/**`**（R-7 未破坏，渲染器引用 `Skill.Utils.SKILL_KEY` / `MainWeapon.Utils.MAIN_WEAPON_KEY`）；读回侧 `Skill.Utils.getSkillId`（:51）/ `MainWeapon.Utils.getWeaponId`（:57）**仍在**（运行级读回归 t5）。
- **口径声明**：以上为**静态级**（源码逐分支对照）；**运行期**外观（真实 `ItemStack` 的材质/displayName 纯文本/颜色/装饰/lore 行序/PDC 键值）**本卡未取证**。

---

## §7 三条红线（A11）与 §8.2 的处置

| 红线 | 命令与口径 | 期望 | 我现算 | 判定 |
|---|---|---|---|---|
| R-1 `BuffManager#addBuff` 不接线 | `grep -nE 'hotbarRenderer\|markDirty\|updateHotbar' -- …/manager/BuffManager.java`（含注释） | 1 命中且宿主 = `removeBuff` | **1**（:109 `instance.hotbarRenderer().markDirty();`）；`removeBuff` 定义 :99、`addBuff` 定义 :35 ⇒ 断言：置脏位于 `removeBuff` 轮询块内（:108 注释 + :109 代码，结构上属 `tickAllBuffs` 调用的 `removeBuff`） | **pass** |
| R-2 能量**无条件**置脏 | `grep -nF 'getEnergyCost' -- …/core/RoleInstance.java` | 0 | **0**；`:536-537` 为 `//触点④…无条件置脏` + `hotbarRenderer.markDirty();`（**无阈值条件**） | **pass** |
| R-3 不合并 ticker | `runRepeating` @ RoleInstance / BuffManager + `this::triggerUpdate` @ src | 1 / 1 / 1（与改动前一致） | **1**（RoleInstance:124）/ **1**（BuffManager:137）/ **1**（RoleInstance:125） | **pass** |
| R-5 不改浮点格式 | `%.1f` @ src · `Locale` @ `core/**` · `String.format` @ src | 1 / 0 / 2 | **1**（HotbarRenderer:136）/ **0** / **2** | **pass** |
| R-6 B-2 谓词存在且被 flush 入口引用 | 谓词 = **只数技能**且未到期 | 存在且被引用 | `private boolean hasCoolingSkill()`（:747-752，只遍历 `skillCooldowns` + `isSkillReady`）· 引用点 `:736` flush 入口 | **pass（静态级）**；运行级递减平滑性归 t5 |
| R-7 不复制键值字面量 | `main_weapon_id` / `skill_id` @ src 及 `core/hotbar` | 各 1 且不在渲染器 | 各 **1**（MainWeapon:47 / Skill:40）· `core/hotbar` 内 **0** | **pass** |
| §8.2 键可见性二选一 | `MAIN_WEAPON_KEY` | 选 (a) 或 (b) 并申报 | 选 **(a) 提升可见性**：`public static final NamespacedKey MAIN_WEAPON_KEY`（MainWeapon:46）；实现者已申报"可见性扩权"（说明件 §9） | **pass（已申报）** |

---

## §8 T3 两条尾巴的独立核对（A12）

1. **勘误件只指归档路径**：`git grep -n -F 'build/libs' -- debug-logs/测试记录/阶段5-勘误与读法说明.txt` ⇒ **0 命中**；同文件用裸串 `build` 的旁证命中 **1** 行（`:42`），该行是**冻结件内逐字引文**（`…\build\libs\…jar`，反斜杠形态）且**显式标注**"『当时路径』历史留痕 —— 仅存留痕、已作废为引用路径" ⇒ 符合 t3 卡面 A3 的两种合法形态之第二种。
2. **复核报告只追加**：`6712625` 提交的 `--numstat` = `阶段5-取证轮-独立复核报告.md` **84 插入 / 0 删除**（`阶段5-勘误与读法说明.txt` = 24 / 3，属卡面 A1 要求的"就地修订"，方向正确）；工作树 `git diff --numstat` 对该报告 = **0 行**（已入库、无未提交改动）⇒ **删除列 = 0 成立**。
3. 同族的第三处（t3 加做并申报的 `§3.3` 归属订正）我复核其**依据**：t63 的制品归属 = `0F2D901C…`（t2 的 F1 链 `22:01:06.944 > freezeB 21:55:30.018` 亦支持"t63 未重建"）⇒ 与 MANIFEST 登记 5 一致，**不构成新问题**。

---

## §9 判据缺陷 / 仪器缺陷（A13）—— 分级 findings

> 纪律：判据或仪器有缺陷时**单列一条 finding**，不写成"不成立"，也**不通过改判据回避**。本卡 findings **不在被复核的代码里**，但会影响后续引用。

| id | 级别 | 问题 | requiredFix |
|---|---|---|---|
| **F-1** | **medium** | **共享仪器 `numeric-multiset.ps1` 的 string 模式有一条幻影行**（不是本批代码问题）。机制：string 模式先剥块注释（`:56` `(?s)/\*.*?\*/`）再剥行注释（`:57`）⇒ `//**顺序说明**：…` 这类**行注释里的 `/*`** 被当块注释起点，吞掉直到下一个 `*/` 的长区间。**我现算复现**（独立于 t2）：after 树 `RoleInstance.java` 有 **2 个幻影区间**（**649..681**：吞 3 个字面量；**723..746**：吞 **1 个 `"registered"`** + 2 个字面量）、before 树 **1 个**（**693..725**：吞 3 个字面量）；`"registered"` 计数：**raw 3/3 · 工具口径 3/2 · 先剥行注释口径 3/3** ⇒ 工具 string net 报 **−10**、真值 **−9**，差值 = 那 **1** 个 `"registered"`。**触发点横扫**：全 `src/main/java` 91 文件里只有 `RoleInstance.java` 带该触发（`/*` 落在 `//` 行注释内 = 4 处/树，before/after 同）；**numeric 模式不受影响**（`:49` 是"块/行/字符串"单条交替，`//` 优先吃掉整行注释）。 | 后续任何引用该仪器 **string net** 的场合：① 先剥行注释（或自算/改用 numeric 口径），② 在报告里给出**工具值 + 真值 + 差值的逐条归因**。建议在下一批以最小卡在工具头注释登记该既有缺陷（**不在本卡 inScope**，我不改共享仪器）。 |
| **F-2** | low | **判据自检样本与被测变化同源 ⇒ 变化后失去区分力**（t1 的两条样本）：C-03 的样本 `::updateHotbar\b` = 2（before）在 after 树**必然**为 0（`updateHotbar` 被删）；C-08 的"算术闭合"样本 `getDisplayName(` = 25 = 22+2+1 在 after 树为 **15**（= 保留 8 + 渲染器新增 7；被删 17 = MainWeapon 7 + Skill 9 + RoleInstance:504 1）。样本不再"已知有命中"。 | 我已补**替代样本**（`::triggerUpdate\b` = 1、`hasCoolingSkill` = 2），并给出 15 的**分栏闭合**；建议 t1 类冻结件在写"after 期望"时，样本改选**不受本批改动影响的**符号（如同时给 before/after 双样本）。 |
| **F-3** | low | **行为边界差异（非冻结可见差异，未在 t1 §5 六维内）**：旧 per-tick 路径只改 `displayName` 且**仅当该槽位已有物品**时生效（旧 `updateSkillItemMeta` 有 `if(item == null) return;`，且会把**该槽位的任意物品**改名）；新 flush 是**全量重建**（材质+名字+lore+PDC 一并写回，空槽位会被重新填上）。⇒ 在"冷却中玩家手动移走/替换该槽位物品"的对抗场景下，新旧行为不同（新：恢复；旧：改名或不动作）。冻结对照表不覆盖该场景，故**不判 fail**。 | 由 `t5`/`t6` 决定是否把该场景纳入外观抽查，或在报告里明示"不承诺该场景"；**不改代码**、**不改判据**。 |
| **F-4** | low | **我自己的仪器缺陷（自曝）**：① 用 `git grep` 包装非 grep 子命令 ⇒ C-13a/b/c 首轮返回**假 0**（`git grep status --porcelain -- <path>` 实际是"在 api 文件里搜 `status`"）；② 幻影行横扫的前两版触发判据取错（`//[^\r\n]*/\*` 与"匹配前同行前缀含 `//`"）⇒ 两次**假 0**（正确判据 = `/\*` 的**前一字符**为 `/`，或同行前缀 `EndsWith('/')`）。 | 已用正确子命令/判据重算（C-13a/b/c 的真实值 = 0/0/2；幻影区间见 F-1）；**结论以重算值为准**。列入纪律：判据自检要覆盖"**仪器调用形态本身**"。 |
| **F-5** | low | **基线快照清单覆盖度**：`t44takeover-before/SHA256.txt` 登记 **91** 条，而快照实际 **92** 件（未登记 `main/resources/plugin.yml`）。该件我已另行与 commit blob 比对（**相同**），故不影响 A2 结论。 | 后续快照/清单：要么登记全部件，要么在 manifest 头注明"清单只覆盖 java 源码 + 资源另行比对"。 |

**计数**：blocker **0** · high **0** · medium **1**（F-1）· low **4**（F-2…F-5）。

---

## §10 范围声明与横扫（A14）

- **单条判据只支撑其定域**：C-11 的 0 只覆盖 `roleComponent/**`；C-09 的 0 覆盖 `src/main/java/**`；`ERROR/SEVERE` 的 0 覆盖**同一 artifact identity 的两个已冻结轮**（t2 轮 + 复核轮），**不**外推为"全历史轮零错误"（若要全局断言须横扫全部已冻结轮 —— 本卡不做该断言）。
- **本轮横扫范围（明确写出）**：
  - `markDirty` / `updateHotbar` / `getDisplayName(` / `setItem(` / `setItemMeta(` / `runRepeating` / `%.1f` / `Locale` / 键字面量：定域 = `src/main/java/**`（91 个已跟踪文件）；
  - `api/**` 零改动：`status --porcelain` + 点名 commit 的 `diff --name-only`；
  - 幻影行触发点：`src/main/java/**`（当前树）+ `42dde5e5:src/main/java/**`（before 树）**双侧横扫**（各 91 文件）；
  - 基线自证：快照 92 件 × commit tree 92 件**双向集合比对**。
- **静态级 vs 运行级**：§2/§3/§6/§7/§8 均为**静态级**；§5 为**运行级**（Mode B / 集合差 / 级别扫描 / 停机三路）。二者**分开写**，未互相冒充。

---

## §11 未覆盖项如实申报（A15）与 F1 四环

**未覆盖（逐条 + 最小闭合方式）**
1. **外观级（C-15）= 未覆盖**：无玩家/机器人上线 ⇒ 材质/名称颜色/后缀/lore 文本/PDC **读回**、以及"秒数逐 tick 递减"的平滑性都只有**静态级**依据。最小闭合 = `t5` 按 t1 §5 两表做物品栏 dump（含同一段冷却的 `t/T+10/T+20` 三点抽样，且**至少一个非平凡值**）。
2. **`setItem` 次数（性能契约）未量化**：本卡只给"空闲 tick 不置脏 / 冷却中每 tick 至少 flush 一次"的**结构性**论证。最小闭合 = `t17`/`t18` 挂计数探针。
3. **`BuffManager#addBuff` 可见时机（R-1 保护的差异）运行级未取证**：需构造"上 STUN 但不起冷却"的用例。最小闭合 = `t5` 或后续批次的定向冒烟。
4. **停服为 `job_kill`（无 graceful stop 行）**：无法从日志判"世界保存 / 插件 `onDisable`"。与既有各轮同形态，**不申报为已覆盖**。
5. **JVM 默认 locale 对 `%.1f` 的影响**：不改 locale 复现 ⇒ 只登记"不得改动"（R-5）。
6. **Folia 真机**未验证（与既有裁定一致，本批不做 Folia 适配）。
7. **制品载入自绑定的形态限制**：本轮无 `[PluginRemapper] Remapping` 行 ⇒ 我用 remap 索引的 SHA256 映射（等价强度更高，但**形态不同**于 t2 轮的"日志自述路径"）。

**F1 链（四环，机器校验）**：jar `12:54:07.286` < log `13:11:59.139` < 物证 `13:15:01.821` < 本报告（写盘时刻）⇒ **ORDER = True**（本报告为链尾）。逐件身份：log = 5698 B / 54 行(ReadAllLines) / SHA256 `8268E32C…`；物证 = 8873 B / 85 行 / SHA256 `0A55D1B6…`；jar = 159512 B / `6A249993…`。

---

## §12 边界声明（A16）与复算命令清单

**边界**：① **未改 `src`**（被验证树 `a6a33c9b…` = HEAD 的 `src/main` tree，`porcelain -- src` 全程 0 行）；② **未改任何历史证据**（既有 `*.log`、t1/t2/t3 的冻结件一律只读）；③ **零 git 写**（未 `add/commit/checkout/restore/stash/reset`）；④ 本卡只写入 `inScope` 的三份证据文件（`debug-logs/**` 被忽略 ⇒ 由队长 `-f` 入库）；⑤ 未申请提权。

**复算命令（按需逐条）**
```powershell
git -C ShadowHunterRoles rev-parse HEAD
git -C ShadowHunterRoles status --porcelain
git -C ShadowHunterRoles diff --name-only b062252 HEAD -- src/main
git -C ShadowHunterRoles ls-tree -r --name-only 42dde5e5069ff99876eea9a35977b47a0037dc95:src/main
git -C ShadowHunterRoles grep -nE '\bmarkDirty\(\);' -- src/main/java
git -C ShadowHunterRoles grep -nE '::markDirty\b|\bmarkDirty,' -- src/main/java
git -C ShadowHunterRoles grep -nE 'isDirty\(\)|clearDirty\(\)' -- src/main/java
git -C ShadowHunterRoles grep -nF 'setItem(' -- src/main/java
git -C ShadowHunterRoles grep -nF 'setItem(' -- src/main/java/com/shadowHunterRolesPlugin/core/hotbar
git -C ShadowHunterRoles grep -nE 'createIconItem\(RoleInstance|getDisplayName\(RoleInstance' -- src/main/java/com/shadowHunterRolesPlugin/core/Skill.java src/main/java/com/shadowHunterRolesPlugin/core/MainWeapon.java
git -C ShadowHunterRoles grep -nF 'updateHotbar' -- src/main/java
git -C ShadowHunterRoles grep -nE '\bupdateSkillItemMeta|\bupdateAllSkillItemMeta' -- src/main/java
git -C ShadowHunterRoles grep -nE 'HotbarRenderer|markDirty|createIconItem|getDisplayName\(RoleInstance' -- src/main/java/com/shadowHunterRolesPlugin/roleComponent
git -C ShadowHunterRoles grep -nE '^\s+(Self|EnergyPort|SanTEPort|VitalsPort|CooldownPort|BuffPort|FactionPort|DamagePort|TimerPort|ComponentLookup) ' -- src/main/java/com/shadowHunterRolesPlugin/core/ports/ComponentServices.java
git -C ShadowHunterRoles grep -nE 'hotbarRenderer|markDirty|updateHotbar' -- src/main/java/com/shadowHunterRolesPlugin/manager/BuffManager.java
git -C ShadowHunterRoles grep -nF 'getEnergyCost' -- src/main/java/com/shadowHunterRolesPlugin/core/RoleInstance.java
git -C ShadowHunterRoles grep -nF '%.1f' -- src/main/java
git -C ShadowHunterRoles grep -nF 'Locale' -- src/main/java/com/shadowHunterRolesPlugin/core
git -C ShadowHunterRoles grep -nF 'main_weapon_id' -- src/main/java
Get-Item ShadowHunterRoles\build\libs\ShadowHunterRolesPlugin-1.0.0.jar | Select-Object Length,LastWriteTime
Get-FileHash ShadowHunterRoles\build\libs\ShadowHunterRolesPlugin-1.0.0.jar -Algorithm SHA256
cd C:\Users\ROG\Desktop\插件; powershell -NoProfile -ExecutionPolicy Bypass -File .scratch\bot\log-delta.ps1 -Anchor C:\Users\ROG\Desktop\插件\ShadowHunterRoles\debug-logs\测试记录\阶段4-起服日志-T2b.log -Current C:\Users\ROG\Desktop\插件\ShadowHunterRoles\run\logs\latest.log -Tag t44verify
```

**尾部声明（口径）**：本机 `gradlew` 会向 stderr 打 `Couldn't open current thread, error = 5`（原生文件监视器）⇒ 构建判定一律看 `BUILD SUCCESSFUL` + `.class` mtime，**不看退出码**；`gradlew` 调用**实际执行形态** = 同一条 pwsh 前置 `$env:GRADLE_USER_HOME="$PWD\.gradle-work"`。运行期读 `run/logs/latest.log` **必须用共享读流**（`Get-FileHash`/`ReadAllText` 在该文件被服务端独占时会报 `being used by another process`）。
