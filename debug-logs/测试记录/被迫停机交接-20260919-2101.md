# 被迫停机交接（用户要求暂停 · 需关闭电脑）

**性质**：用户主动停机，**非**任务失败、**非**质量事故。停机前队长已把工作树**全部落盘**为一个 WIP 快照。

**停机时刻**：HEAD = c51553255ca983bededddc63d4602ca358bc933e（本次快照提交之前的 HEAD）；快照提交紧随其后。

## 一、被强行中断的在办卡（**未结卡**，恢复时优先处理）
- **`t28`** 归零惩罚语义定稿后的文档同步（执行者 `eng-手册B`）——目标：把 `docs/插件文档/` 中 13 行过时描述按**最终语义**改准（逐 tick 钉 0 + 5 秒回满 + 忽略重入），其中最要紧的是 `角色与技能.md` 里那句**已被推翻**的「触发时会先取消仍在跑的旧惩罚任务」。
- **`t29`** 把「真空轮」陷阱 + verify 硬规则写进 `debug-logs/测试记录/任务卡编写指南.md`（执行者 `eng-规范`）。

**恢复第一步**：先看这两个文件是否被改到一半（`git show --stat HEAD` 可看本次快照含哪些文件；必要时 `git revert` 或补完），**然后重新立卡**——不要续用一个中断的 attempt。

## 二、本会话已完成并全部入库的
- **`t1`–`t20`** 全部 completed（阶段 5 的 4.4 渲染器接管 + 文档大修 + 初学者指南 5 份）
- **`t21`** failed（如实退档）· **`t23`** cancelled（流程事故作废）· **`t22`** completed（派发面能力化两刀）
- **`t24`** completed（派发面文档同步）· **`t25`** failed（C-11 第三轮，如实退档）
- **`t26`** completed（归零惩罚语义恢复）· **`t27`** completed（钉 0 逐 tick + 回满推迟 5 秒）
- 归档区 **10 代**制品；当前代 `B8C3259E`（165857 B / 2026-09-19 20:53:53.918）

## 三、开放项（恢复时按此顺序）
1. **`log-delta.ps1` 吞异常输出 `C=0`** —— 工具缺陷，会让"路径笔误"变成**完美假绿灯**（实测四元组：甲 13/0/0/0 = 真空通过；乙 13/13/0/0 = 有效）⇒ 建议修脚本「失败即报错退出」，或永久改用三条件判据
2. **C-11 运行级闭合** —— 仅静态级（三轮未闭合）；路线图与**一条已封死的路线**在 `开放项-C11运行级闭合的路线与一条已封死的路线.md`
3. **非继承组件的运行级派发证明** —— 需 src（测试用注册入口）
4. `DebugCooldownCommand:68` 仍是 `instanceof ActiveComponent`（命令面）
5. `getPlayerSanTE` 无角色时返回哨兵 **−78**（API 卫生）
6. `plugin.yml` 的 `usage` 与实际不符 · `src` 侧注释回收（`RoleInstance:79/:138`）· 属性修饰符残留 STUN · `numeric-multiset.ps1` 幻影行
7. **mineflayer 无法解析 Paper 1.21.11 的 `DUST` / `TOTEM_OF_UNDYING` / `SCULK_SOUL`**（工具链，非插件问题）

## 四、恢复时的环境事实
- **必须** 前置 `\="\C:\Users\ROG\Desktop\插件\ShadowHunterRoles\.gradle-work"`（`C:\Users\ROG\.gradle` 被沙箱拒绝）
- 提交权**只在队长**；成员零 git 写
- `debug-logs/**` 被 `.gitignore` 忽略 ⇒ 新件需 `git add -f`
- CJK 一律用 `read` 工具或 `[System.IO.File]::ReadAllLines(…, UTF8)`；判大小写一律 `-ceq`/`-cmatch`；`git grep` 看不见未跟踪件