# 阶段 3–4 独立验证报告（t15）· 路径修正与后记

> **本文件不是报告本体**。报告本体在核验完成后由队长**整体迁入** `docs/测试记录/`；本文件按 t15 卡面 inScope（原路径）落一份**指针 + 路径修正 + 后记**，供 t16/t24 正确复现。

## 1. 报告本体位置与身份（现算）

```
路径 = docs/测试记录/阶段3-4-独立验证报告.md
身份 = 29757 B / 307 行 / mtime 2026-09-18 19:18:14.060
       SHA256 05070BF4AA0C5CE20426AC2A8A3C8FB55B3AA2F737C877C0630116F4AFD92FDB
（我写入原路径时 = 29664 B / 307 行 / SHA256 543B57CAD9B2A98DFBB9AC5658C0769D8DE0ADC566B2726B0F0ED515F16DC4FA
  → 迁移时行尾被规范化为本仓库工作树口径（core.autocrlf=true ⇒ CRLF）；行数与内容未变，V-01..V-13、基线锚与全部判据均在）
```

## 2. 路径修正（报告内引用的证据路径 → 迁移后的实际路径）

| 报告内引用（迁移前） | 现实际位置 |
|---|---|
| `docs/阶段1-起服锚点-t2ModeB.log` | `docs/测试记录/阶段1-起服锚点-t2ModeB.log` |
| `docs/阶段4-起服日志-B10.log` / `-T1.log` / `-T2.log` | `docs/测试记录/阶段4-起服日志-B10.log` / `-T1.log` / `-T2.log` |
| `docs/阶段4-T块独立验证报告.md`（t49，`366177ED…`） | `docs/测试记录/阶段4-T块独立验证报告.md` |
| `docs/阶段4-T块⑧④①③⑤独立复核.md`（eng-会话，`D43BF5BE…`） | `docs/测试记录/阶段4-T块⑧④①③⑤独立复核.md` |
| `docs/阶段3-交付小结.md` / `阶段4-交付小结.md` / `阶段4-迁移清单.md` | 同名文件，均在 `docs/测试记录/` 下 |
| `docs/重构进度与交接.md` | `docs/测试记录/重构进度与交接.md` |

> 迁移事实：`git status` 显示 50 个 `D`（旧路径）+ 新目录 `docs/测试记录/`（51 文件）；`HEAD` 已推进到 **`29a30bb`**，其提交信息即「gitignore allows logs under the new docs/test-records folder …」⇒ **证据未丢失，属整体归档迁移** ✓（我逐名核对：关键日志在磁盘各命中 1 份于新路径）。

## 3. 后记：核验完成后的工作树变化（**不影响本次判据**）

- **验证基线（判据所依据的冻结态）**：`HEAD = 0564b3be…`（2026-09-18 19:09:53），核验期间 `git status --porcelain` = **0 行** ✓ —— 报告 §0–§7 的全部命令与结论**对该冻结态成立**。
- **核验完成后（19:16–19:18）工作树发生变化**：
  1. **V-01/V-03 的修复已在途**：`core/RoleInstance.java`（+46/−11）、`listener/RoleEventListener.java`（+11/−17）、`roleComponent/DefaultSanTEZeroPunishment.java`（+1/−1），三者 mtime 均为 **09-18 19:16:22**（与裁定「容器直接派发 + 删 listener 转发 + 重入护栏」一致）；
  2. 证据整体迁入 `docs/测试记录/`；`HEAD → 29a30bb`。
- ⇒ **本报告的判据结论针对 `0564b3b` 冻结态有效**；修复落地后的**复核应由 t16 或后续卡按新 commit 现算**（不得沿用本报告对 I-15/I-14 的「不成立」结论去判修复后的状态）。

## 4. 结论摘要（与报告本体一致，便于快速引用）

**I-3/I-4/I-5/I-6/I-7/I-8/I-9/I-10/I-11/I-13 成立**（含 I-10 空转、I-6 口径注记）；**I-12 部分成立**（`createIconItem`/`getDisplayName(RoleInstance)` 各 2 处基类声明 = T 块第 ⑦ 项待办）；**I-14 部分成立**（`pre==now` ✓ / 不递归 ✗）；**I-15 不成立**（唯一派发入口是 Bukkit 事件总线、listener 转发未删）；**§3.4 等价性表 6/6 成立**；**§3.5.1 部分落地**（poll + push-after-cast + 两条写路径 ✓；端口 push 与帧末 flush = 4.4 待办，已在文档标注）；**Mode B + 构建成立**（三份日志三行齐 + `C = 0`；jar `135333 B / 76AC54D9…` + `src 新于 jar = 0`）；**停服为证据缺口**。

**分级 findings**：**V-01 blocker**（I-15）· **V-02 high**（I-15 判据命令 glob pathspec 假通过）· **V-03 high**（I-14 不递归未实现）· **V-04 medium**（I-14/I-11 的单测验收在本仓库物理不可执行 ⇒ 已按裁定映射为结构证据）· V-05 low（死 import）· V-06 low（I-12 = T-⑦ 挂账）· V-13 low（停服证据缺口）· V-07..V-12 info（I-10 空转 / I-6 口径 / `Self.id()` 口径 / §3.4 precheck 位置 / **t49-F1 已文档闭合** / §3.5.1 4.4 待办）。

**复现命令**：见报告本体 §9（一键清单）；**路径按本文件 §2 替换**后即可逐条复跑。