# 阶段 4 · 逐组件迁移清单（t25 只读勘察；供 B①…⑨ 与收尾块 T 机械执行）

> **勘察时点**：2026-09-17 17:28（本地 UTC+8）。**只读**：本文件之外未改动任何文件；未构建、未起服（避免与 eng-止血 的逐批构建/起服争用 `build/` 与 25566）。
> **树状态**：**t14 进行中**（eng-止血）——框架侧已落地（`roleComponent/RoleComponent.java`、`ActiveComponent.java`、`core/ports/*` 10 个端口 + `Self`/`ComponentLookup`、`core/dispatch/*`、`core/hotbar/*`、`core/*PortImpl`），**组件侧尚未迁移任何一批**（实测：`roleComponent/**` 下 `markMigrated` 命中 = **0**）。
> **计数口径（按指南 §6.6 第 7 例）**：下表"出现次数"= **标识符出现次数**（同一行出现两次算两次），**不是行数**。行号为**勘察时点**行号，实施时以"标识符 + 内容片段"为准（行号会漂移）。

---

## 0. 总表（14 个待迁移组件 / 9 个批次）

| 批次 | 组件（`src/main/java/…/roleComponent/` 下相对路径） | 行数 | 基类 | 旧入口出现次数 | `RoleInstance` 出现次数 | SHA256（勘察时点，前 16 位） | 预期改动面 |
|---|---|---|---|---|---|---|---|
| **B①** | `red/RedEvilShockSkill.java` | 46 | `Skill` | `onRightClick` ×1 | 2 → **0** | ✅ 已完成（3 文件回滚单元见 §2 B①） | 仅本文件 |
| **B②** | `meiqiHezi/skill/MeiqiheziUnconcernSkill.java` | ~~52~~ → 新文件 **2 167 B** | `Skill` | `onRightClick` ×1（迁移前） | 3 → **0** | **✅ B②-b-1 已完成**（2026-09-17 17:57）：迁移前 `48A292C0…` → 现 **`0080F3D8A3A3BCA1F1E24C340EFB0013A59529E920D7946884F0045FC15EDBC5`** | 仅本文件 |
| **B②** | `red/RedDeeplySorrowSkill.java` | 59 | `Skill` | `onRightClick` ×1 | 5 | `0EE4993DCECCC787` | 仅本文件 |
| **B③** | `red/RedSolitaryArroganceSkill.java` | ~~99~~ → 新文件 **5 014 B** | `Skill` | `onRightClick` ×1（迁移前） | 5 → **0**（严格口径：类型 0 / 字面量 0） | **✅ 已完成**：`4C86B56B308E67E9214663F87F7F877BFCEBD62E0FB13457F8BFDBB769D4AF23` | 仅本文件 |
| **B④** | `meiqiHezi/passive/MeiqiheziEquipmentsPassive.java` | ~~82~~ → **88** | `PassiveSkill` | 0（仅 `start/stop`） | 3 → **0** | **✅ 已完成**：`E652A4E97502CCAC31685101A6FA7729CB5701267182310EFECE211000CAAEE7` | 仅本文件 |
| **B④** | `red/RedEquipmentsPassive.java` | ~~82~~ → **88** | `PassiveSkill` | 0（仅 `start/stop`） | 3 → **0** | **✅ 已完成**：`6D26E6B386425C1C8B9FC8AE9CD0EC3CABFE31B88BD46703E300ECB67ECFCE0B` | 仅本文件 |
| **B⑤** | `AutoRecoverEnergyPassive.java` | ~~37~~ → **41** | `PassiveSkill` | 0（仅 `update`） | 2 → **0** | **✅ 已完成**：`9B3EA5FFE39C1A6D8403DDA99C63B8BD9A777C3CCF26F9ED172009248E1FFFAE` | 5 文件批（含 tick 广播） |
| **B⑤** | `AutoRecoverSanTEHealthPassive.java` | ~~40~~ → **44** | `PassiveSkill` | 0（仅 `update`） | 2 → **0** | **✅ 已完成**：`2D079C51394E274CB6022DF703017E9CD02DF11FDEED33D0E0253F522A0918A9` | 同上 |
| **B⑥** | `red/RedBleedPassive.java` | 204 | `PassiveSkill` | 0（`update` + 私有方法） | 7 | `A912FBCF16B1E1E1` | 仅本文件（**账本私有化 + 公开方法**，消费方 B⑦ 依赖它先落地） |
| **B⑦** | `red/RedSanctifiedBladeMainWeapon.java` | ~~53~~ → **62** | `MainWeapon` | `onAttack` ×1（迁移前） | 2 → **0** | **✅ 已完成**：`C4ABA308602424D070AD573E197C0CD1B5E1229345B7F6E77D2273D897E522B1` | 仅本文件 |
| **B⑦** | `meiqiHezi/mainWeapon/MeiqiheziJuejueMainWeapon.java` | 62 → **105** | `MainWeapon` | `onLeftClick` ×2（迁移前） | 2 → **0** | **✅ 已完成**：`63B59D6318E8E73AB08473A72E957915F0DB913E6EC5EB3173E126D9E240C909` | 仅本文件 |
| **B⑧** | `meiqiHezi/skill/MeiqiheziBloodySlashSkill.java` | 181 | `Skill` | `onRightClick` ×1 | 6 | `9DF5194441B24156` | 仅本文件（**顺带删 2 处渲染覆写**） |
| **B⑧** | `meiqiHezi/skill/MeiqiheziCircleSlashSkill.java` | 187 | `Skill` | `onRightClick` ×1 | 7 | `5821A37A7A0E73A8` | 仅本文件（**顺带删 2 处渲染覆写**） |
| **B⑨** | `DefaultSanTEZeroPunishment.java` | 137 | `PassiveSkill` | 0（`onSanTEChange` + `start/stop`） | 3 | `A65A372AFE6E3A65` | 仅本文件（**`setIsInSanTEPunishmentState` 降为私有字段**） |

**合计**：本 14 个组件内的旧入口出现次数 = `onRightClick` **×6**（B①②③⑧×2）、`onLeftClick` **×2**（`MeiqiheziJuejueMainWeapon`）、`onAttack` **×1**（`RedSanctifiedBladeMainWeapon`）、`onDrop` **×0**；`RoleInstance` 出现 **52** 次（= 迁移前需要清零的引用面）。
> **口径澄清（避免与全目录计数混淆）**：`roleComponent/**` **全部文件**的 `RoleInstance` 命中为 **55**；差额 **3** 在 `roleComponent/SkillUtil.java`（它不在这 14 个组件内，见 §4 R-2）。本表"52"只统计这 14 个待迁移组件。
> **漂移复核（同日 17:30 复算）**：14 个组件的行数、SHA256、`RoleInstance` 计数与 §0 表**逐项一致**；期间 `core/MainWeapon.java` 被 eng-止血 改动（**非本任务**），说明 t14 正在并行推进——批次开工前仍请按 R-3 现场复核。
**计数差异说明（实测事实，不下判断）**：本清单实测 **14 个**待迁移组件（9 个批次）；任务描述里的"10 个组件 / 剩余 9 个"与实测不符 —— 按设计文档 §11 的批次表逐行统计即为 **9 批 / 14 类**。实施时请以**本表的 14 类 + `markMigrated` 命中数**为清单真值。

---

## 1. 每处旧写法的**等价新写法**（统一对照表）

> 端口白名单 = `ComponentServices` 的 **10 个成员**：`self / energy / sante / vitals / cooldowns / buffs / factions / damage / timers / components`。

| 旧写法（`instance.…`） | 新写法 | 备注 |
|---|---|---|
| `getCurrentEnergy()` | `svc().energy().current()` | — |
| `decreaseEnergy(n)`（前面必有 `if (energy < n) return;`） | `if (!svc().energy().tryConsume(n)) return;` | **判空顺序**：`tryConsume` 自带不足判定，不要再写一次比较（否则双读） |
| `increaseEnergy(n)` | `svc().energy().gain(n)` | — |
| `getCurrentSanTE()` | `svc().sante().current()` | — |
| `getMaxSanTE()` | `svc().sante().max()` | — |
| `increaseSanTE(n)` | `svc().sante().gain(n)` | — |
| `decreaseSanTE(n)` | `svc().sante().decrease(n)` | — |
| `setCurrentSanTE(v)` | `svc().sante().set(v)` | — |
| `heal(x)` | `svc().vitals().heal(x)` | §10 裁决 6 |
| `getBuffManager().canCastSkill()` | `svc().buffs().canCastSkill()` | — |
| `getBuffManager().canUseMainWeapon()` | `svc().buffs().canUseMainWeapon()` | — |
| `getBuffManager().addBuff(BuffType.STUN, 100)` | `svc().buffs().add(BuffType.STUN, 100)` | — |
| `isHostileTo(p)` | `svc().factions().isHostile(p)` | — |
| `getFaction()` | `svc().factions().faction()` | — |
| `getPlayer()` | `svc().self().player()` | — |
| `isSkillReady(getId())` / `getRemainingSkillCooldownSeconds(getId())` | **删除**（渲染交框架：`HotbarRenderer` + `IconState`） | 组件的 `createIconItem` / `getDisplayName(RoleInstance)` 覆写一并删除 |
| `startSkillCooldown(getId(), getCooldownTicks())` | **删除**，`onCast` 末尾 `return CastResult.CAST;` | **冷却声明值成为唯一真值源**（框架按 `getCooldownTicks()` 启动） |
| `startMainWeaponCooldown(getId(), getCooldownTicks())` | 同上（返回 `CAST`） | — |
| `isValid()` 守卫（任务体内） | **删除**：任务经 `svc().timers()` 登记，框架在 `clear()` 时统一取消 | `TimerPort.track(task)` |
| `rolesContext().scheduler().runRepeating(...)` | `svc().timers().runRepeating(initialDelay, period, task)`（并 `track`） | 收尾块 T 要删 `rolesContext()` |
| `setContext(K,V)` / `getContext(K)`（跨组件账本） | **组件间通信只用 `getComponent(Class)`**：账本私有化 + 公开方法（B⑥ 先落地，B⑦ 消费） | 删 `Map<String,Object>` 上下文与 `*_CONTEXT_KEY` |
| `setIsInSanTEPunishmentState(b)` | 组件**私有字段**（B⑨） | — |
| `DamageUtil.xxx(...)` | `svc().damage().trueDamage/physicalDamage(victim, source, amount[, knockback])` | 由框架端口统一 |
| `applyPotionEffect(effect)` | ⚠️ **端口白名单里没有药水端口** → 见 §4 风险 R-1（4 个组件、6 处调用受影响） | **需 captain 裁定** |

---

## 2. 逐组件小节

### 2.0 **每批固定证据行（模板）** —— 制度化补救"绿灯 ≠ 语义正确"

> 每一批（B0b-3 / B①…B⑨ / T）在交付小结与本清单该节里**必须**写出下面三条**结构性证明**（captain 2026-09-17 定，源于 B① 被 §20 阻塞的教训）：
>
> 1. **`grep USE_COMPONENT_PIPELINE` = 0**（全局开关已彻底退场；若为 0 则不存在"开关 false 把组件送回旧路径"的静默失效通道）。
> 2. **该批组件的旧回调代码 = 0**（`onRightClick`/`onLeftClick`/`onDrop` 的**实现体**均已删除、仅允许保留 javadoc 说明），且 **`markMigrated()` 写在该组件自己的构造器里**（不是基类）。
>    ⚠️ **适用范围（契约事实，captain 2026-09-17 更正）**：**只有"有施放/攻击路径"的组件需要 `markMigrated()`（即 `ActiveComponent` 及其子类）；纯被动不需要** —— `markMigrated()/isMigrated()` **只定义在 `ActiveComponent`**（实测 `ActiveComponent.java:45/50` 两处定义），而 `PassiveSkill extends RoleComponent` **没有这两个方法** ⇒ 被动里调用会**编译失败**（eng-止血 首轮构建即报"找不到符号"）。逻辑上也无需分流：**被动只有生命周期钩子**（容器对**全部注册组件**广播 `awake/start/stop`），不存在"新旧两条施放路径"。
   ⇒ **`markMigrated();` 的期望值 = 4（仅 4 个主动组件：`RedEvilShockSkill:18`、`MeiqiheziUnconcernSkill:25`、`MeiqiheziCircleSlashSkill:39`、`RedSolitaryArroganceSkill:29`），**不是 6**。
> 3. **`runComponentPipeline` 在该组件 id 上的 `isMigrated()` 为真** —— 旧入口（6 个）首行 `return true` 即**不走旧路径**；等价地：`runComponentPipeline` 现在是 `getById` → `if(!(component instanceof ActiveComponent active) || !active.isMigrated()) return false;` → `return handleCast(trigger, caster);`（**单一入口委派、无全局开关**），`handleCast`/`handleAttack` 各自带 `isMigrated()` 自守卫。
>
> 三条同时为真 ⇒ "组件被送进新管道且旧路径不再兜底"这一语义**有结构性证据**，不能只看"构建/Mode B/集合差/数值 diff 全绿"。

> **⑦ 跨批依赖登记（第五次；模式同 §18）**：**某设施的删除，只能放在其"最后使用者消失的那一批"**。
> 已发生五次：① B① 前移 `requestResolve`（`RedBleedPassive`）｜② B④.5/B⑤ 的 `SkillUtil`（与两被动合块）｜③ B⑤ 的 `update()` 广播（容器 tick，否则被动静默失效）｜④ R-1 药水标志位（5 参重载）｜⑤ **B⑥→B⑦ 的 context 设施删除**（最后使用者 = `RedSanctifiedBladeMainWeapon:35`）。
> **纪律**：每批开工前先问一句"**我本批要删/要改的东西，它的最后使用者在这一批吗？**" —— 若不在，**只迁移不删除**，把删除登记到使用者所在批次，并在两批各记一行（本批记"保留 + 原因 + 最后使用者位置"，后批记"已删除 + 全仓 0"）。
> **⑧ 冻结契约优先于过渡机制的便利（captain 2026-09-17；本阶段第六次同类发现）**
> **原则**：**冻结契约（白名单 / 成员数 / 已冻结签名）优先于过渡机制的便利** —— 若某过渡机制**需要扩契约才能工作**，则**保留旧接口到"删除批"**，**不为过渡期扩契约**。
> **本次判据（B⑥ (A)/(B)）**：过渡桥需要 `instance` 才能 `setContext`，而"去 `LifecycleAware`"会删掉提供 `instance` 的 `start/stop(Player, RoleInstance)` —— **两者在不扩白名单的前提下不能同时成立** ⇒ 裁定 **(A)**：**B⑥ 保留 `LifecycleAware`（只为发布过渡桥）**，只转 `update()`；**(B) 扩白名单被拒**（10 成员是冻结契约，桥是 B⑦ 即删的临时机制）。
> **第六次同类发现**（前五次：B① `requestResolve` 前移｜`SkillUtil` 合块｜B⑤ `update()` 广播｜R-1 药水标志位｜context 设施移交）⇒ 与第 ⑦ 条互补：**⑦ 管"何时删"，⑧ 管"为过渡期不扩契约"**。
> **方法论纪律（captain 2026-09-17，源于「3 vs 4 文件」的交叉偏差）**：**变更面以「实施前最后一次裁定的原文」为准**；若收到新裁定而该块**已开工**，**在块边界处停并确认** —— **不得中途改范围，也不得按旧裁定硬做完再补**。>
> **⑤ "同批"是本质，"顺序"只在"会中途编译"时才有意义（captain 2026-09-17，采纳 eng-止血 的精细化）**
> **实例（B⑤ 剩余 4 文件）**：captain 要求的顺序是 ①改两被动 → ②改 `FactionPortImpl` → ③删 `SkillUtil.hasEnemyInRange`；**实施者实际按 ③→②→① 做**，但**四处在同一连续块内完成、中间未构建也未提交**，最后**一次构建通过** ⇒ **没有发生中途编译失败**。
> **纪律**：**块内一次性改完再构建**即可；**只有当不能保证"一次构建"时，才需要按依赖顺序（先改调用点、后删定义）**。两条都指向同一硬要求：**不得留下"删了定义但调用点未改"的可编译中间态**。
> **⑥ 预检里的事实必须是"开工前现算"的值，不得写成"改完之后将会是"的值（captain 2026-09-17；反面教训）**
> **实例**：`SkillUtil.hasEnemyInRange` 的调用点**开工前现算 = 3 处**（`core/FactionPortImpl.java:32` + `roleComponent/AutoRecoverEnergyPassive.java:20` + `roleComponent/AutoRecoverSanTEHealthPassive.java:20`，定义 `SkillUtil.java:17`），而预检一度写成"**唯一剩余调用点 = `FactionPortImpl:32`**" —— 那是**两被动迁完之后**的"未来态"。若下游按该表述判断"只需改一处"，**中途必编译失败**。
> ⇒ **凡写进预检的计数/引用面，一律按"开工前现算"标注时点**；确需写"完成后将会是"的值时，**必须显式标注"预期（改后）"**并与现算值分列。
> **④ 口径提醒（eng-止血 追加，与本模板配套）**：**只删"实现体"** —— 旧签名说明若写在 **javadoc** 里，**留到 T 块统一清**；否则 `onRightClick`/`onLeftClick`/`onDrop` 的 grep 命中会因 javadoc 而**非零**，t15 可能把"javadoc 命中"误判为"旧路径未删 = 回归"。⇒ 每批判定 ② 时必须写明是 **"仅代码命中"** 还是 **"含 javadoc"**（与 §11 的 `applyPotionEffect` 两个口径同一纪律）。

### B① `red/RedEvilShockSkill.java`（46 行 / `Skill`）——**✅ 已完成**（2026-09-17；§20 总闸修正落地后由 captain 独立核验通过）

> **解阻塞记录（前一轮被拒收 → 本轮已解）**：上一轮因**硬约束 §20**（commit `c39fadf`）被拒收——当时 `USE_COMPONENT_PIPELINE` = 5 处、**`:354` 是 `runComponentPipeline` 首行总闸**，开关 false ⇒ 组件回旧路径而旧覆写已删 ⇒ 落到 `Skill.onRightClick` 空实现（"煞气震赫"空操作），且**构建/Mode B/集合差/数值 diff 全绿也照样发生**。**本轮 §20 修正已落地并核验**：`USE_COMPONENT_PIPELINE` = **0**（字段、3 处使用、javadoc 描述全删）；`runComponentPipeline` = `getById` → `if(!(component instanceof ActiveComponent active) || !active.isMigrated()) return false;` → **`return handleCast(trigger, caster);`**（单一入口委派、**无全局开关**）；`handleCast`/`handleAttack` 去守卫并**各自加 `isMigrated()` 自守卫**；`markMigrated();` 调用 = **1**（`RedEvilShockSkill:18`）；组件内旧回调**仅剩 javadoc 说明**。
> ⇒ **§2.0 三条固定证据同时成立**：① 开关命中 0；② 旧回调实现 0 + `markMigrated()` 在自身构造器；③ `runComponentPipeline` 对该 id `isMigrated()` 为真（不走旧路径）。

- **回滚单元（本批 **3 文件**，SHA256 已与 `git ls-files` 复核逐字一致）**：
  | 文件 | SHA256 | 本批改动 |
  |---|---|---|
  | `core/RoleInstance.java` | `A31B28D9BEC68F6A8C6FF640CD8681D2C56F30A74FCDCBA693C2AAB4CD0BD094` | §20 总闸修正：删 `USE_COMPONENT_PIPELINE` 字段与 3 处使用 + javadoc；`runComponentPipeline` 改为**单一入口委派** `handleCast(...)`；`handleCast`/`handleAttack` 去全局守卫、各加 `isMigrated()` 自守卫 |
  | `roleComponent/red/RedEvilShockSkill.java` | `AD82307AB68122CD51AE025DBD18CDC4362296E848282A2389197991094F5665` | 旧覆写删除 → `onCast(CastSignal)`：`canCastSkill` 不满足 `return NO_COOLDOWN`；`self().player()` / `factions().isHostile` / `sante().gain(10)`；**删** `startSkillCooldown` 改 `return CAST`；范围 5、致盲(61,1)、缓慢(61,3)、层数 5、两句 `playSound` 不动；**调用 `requestResolve`**；`markMigrated()` 写在**自己构造器**（`:18`） |
  | `roleComponent/red/RedBleedPassive.java` | `D4B8CA759B672BB18BD16F77D0E9C64514AB52CC9B8CEEA6F02A4B753289BA3E` | **仅新增** `public void requestResolve(UUID victimId, int stacks)`（硬约束 §18 **前移项**），写入 `start()` 发布的**同一份** `playerBleedResolveRequests`；**未删**上下文字段 / 4 方法 / 两个 `*_CONTEXT_KEY` / `requestBleedResolve`（留 **B⑥**） |
- **证据（运行实测，captain 独立核验通过）**：`BUILD SUCCESSFUL in 16s`｜jar **139 688 B @17:47:21**｜最新 `.class` @17:47:20｜`src 新于 jar = 0`｜**Mode B 三行齐**（`docs/阶段4-起服日志-B1-§20修正.log`）+ 正序集合差 **C=0**｜已停服｜`getInstance()` **0**、`rolesContext()` 全仓 10 / `roleComponent/**` 5（**未增**）｜双口径：**数值 378 → 378（diff 0）**、**字符串 205 → 216（+11 全为新增）**。
- **原勘察内容（保留作对照）**：旧入口 `onRightClick(Player, RoleInstance)` ×1（`:25`；`instance.` 调用 5 处：`:26/:29/:33/:39/:41`）；目标骨架 = `onCast(CastSignal)` → `buffs.canCastSkill()` → 范围/致盲/缓慢 → `getComponent(RedBleedPassive.class)`（判空）→ `return CAST`；端口依赖 `buffs`/`factions`/`sante`/`cooldowns`/`components`。
- **四不变量实测结论**：① 无 `RoleInstance` ✓（迁移后经 `svc()` 端口）；② 跨组件只经 `getComponent` ✓（账本经 `requestResolve`）；③ 不参与渲染 ✓；④ 冷却声明值为唯一真值源 ✓（返回 `CAST`，`startSkillCooldown` 已删）。


### B② `meiqiHezi/skill/MeiqiheziUnconcernSkill.java` ——**✅ B②-b-1 已完成**（2026-09-17 17:57，eng-止血）

- **回滚单元（1 文件）**：`roleComponent/meiqiHezi/skill/MeiqiheziUnconcernSkill.java` = **`0080F3D8A3A3BCA1F1E24C340EFB0013A59529E920D7946884F0045FC15EDBC5`**（新文件 **2 167 B**；迁移前为 `48A292C0978436799721D1F9C8869E1B3FB00215EFA582F062C42E00898F21ED`，52 行）
- **本批改动**：构造器 `markMigrated();`；**删旧覆写** → `onCast(CastSignal)`；`canCastSkill` 不满足 → **`return NO_COOLDOWN`**；**删** `startSkillCooldown(...)` 改 **`return CAST`**（声明值 100）；药水 = `svc().buffs().applyPotionEffect(PotionEffectType.SPEED, 40, 4)`（R-1 四项齐：**SPEED / 40 / 4 / 记账条目一致**）；音效与粒子**逐字未改**；删 `RoleInstance` / `PotionEffect` import。
- **证据（运行实测）**：`BUILD SUCCESSFUL in 14s`｜jar **139 900 B @2026-09-17 17:57:58**｜最新 `.class` = `MeiqiheziUnconcernSkill.class @17:57:57`｜`src 新于 jar = 0`｜**Mode B 三行齐**（`docs/阶段4-起服日志-B2b1.log :45/:46/:50`，`Done (18.203s)`）+ 正序集合差 **`C = 0`**｜已停服｜`getInstance()` **0**｜`rolesContext()` 10（`roleComponent/**` 仍 **5**，未增 —— 下降发生在 B②-b-2）。
- **§2.0 第②条读数（本组件）**：旧回调 **仅代码命中 = 0 / 含 javadoc = 0** —— 本组件**连 javadoc 旧签名说明都没留**（与 `RedEvilShockSkill` "仅 javadoc 提及"的情形**不同，不得混用**）。
- **计数更新（按 §11 口径纪律标注）**：`markMigrated();` **1 → 2**（B②-b-2 后应为 **3**）；`svc().buffs().applyPotionEffect` **仅代码 0 → 1**（另有 **1 处 javadoc 提及**为实现的说明 ⇒ **宽松口径 = 2**）；**代码口径 `.applyPotionEffect(` 总数 9 → 9**（本批迁的是**接收者**，不是调用数）。
- **原勘察内容（保留作对照；已过期）**：旧入口 `onRightClick` ×1（`:28`）；`instance.` 3 处（`:29/:30/:40`）；目标 = `onCast` → `buffs.canCastSkill()` 判拒 → 自身 effect（R-1）→ `return CAST`；端口依赖 `buffs`、`cooldowns`（框架）。

### §11 前置（B②/B⑥/B⑧/B⑨ 共用）：`BuffPort` 新增方法签名

- **已裁定**（硬约束第 15 条 / §9.1）：在**既有 `BuffPort`** 内新增**一个**方法，**白名单成员数仍为 10**；实现**必须委托 `RoleInstance.applyPotionEffect(...)`** 以保住阶段 1 的药水记账（**绕过记账 = 药水泄漏 = 回归**）。
- **规范签名（captain 定，设计文档 §5.1，commit `762171d`）**：
  ```java
  void applyPotionEffect(PotionEffectType type, int durationTicks, int amplifier);
  ```
  > eng-止血 的早期提案 `addRecorded(...)` **已被覆盖**，不再使用。
  与 §9.1 三条硬要求逐条对齐：① 实现体委托 `RoleInstance.applyPotionEffect(type.createEffect(durationTicks, amplifier))` ⇒ **记账条目一致**；② 参数**恰好**暴露 `效果类型 + 时长 + 增幅` 三者，便于 B②/B⑥/B⑧/B⑨ 逐条出"三者不变 + 记账一致"的证据；③ **方法级新增**（`BuffPort` 现 5 个方法 → 6 个），**成员数 10 不变**。

- **✅ 已实现（B②-a，2026-09-17 17:52，eng-止血）** —— 回滚单元 **2 文件**：
  | 文件 | SHA256 | 本批改动 |
  |---|---|---|
  | `core/ports/BuffPort.java` | `55E904CD698C99275490CD5335A89281323C4C01E6F45EB6A97B824032E57495` | 新增 `void applyPotionEffect(PotionEffectType, int, int)`（方法数 **5 → 6**；`ComponentServices` 成员数 **10** 不变） |
  | `core/BuffPortImpl.java` | `1AE3123AA09339513D98BC8CD5F0B98BAFD4AB576AACEEAE55558F5F7635EAF4` | 实现 = `owner.applyPotionEffect(type.createEffect(durationTicks, amplifier))`（**已记账路径**，与旧调用点同源）。注：`RoleInstance.applyPotionEffect` 实测收的是 **`PotionEffect` 对象**（`:594`） |
  **证据**：`BUILD SUCCESSFUL in 13s`｜jar **139 868 B @17:52:48**｜**Mode B 三行齐** + 正序集合差 **`C = 0`**｜已停服｜`getInstance()` 0、`rolesContext()` 10（**未增**）｜**当前零调用点**（`svc().buffs().applyPotionEffect` = **0**）⇒ 属"纯新增未生效"，与 W1 同类。

- **✅ 已裁定 ①：R-1 = 方法族（3 参 + 5 参重载）**（captain 2026-09-17；设计文档 **§5.1**，commit **`181efad`**）
  ```java
  void applyPotionEffect(PotionEffectType type, int durationTicks, int amplifier);                                      // 3 参（已实现 B②-a）
  void applyPotionEffect(PotionEffectType type, int durationTicks, int amplifier, boolean ambient, boolean particles);  // 5 参重载（第 34 个端口方法）
  ```
  5 参实现 = `owner.applyPotionEffect(new PotionEffect(type, durationTicks, amplifier, ambient, particles))`（**同一条已记账路径**）；`MeiqiheziCircleSlashSkill` 调 `applyPotionEffect(SLOWNESS, 20, 2, true, false)` ⇒ **标志位逐字保真**；**3 参版本保留**（`MeiqiheziUnconcernSkill` 已用；B⑥/B⑧ 走默认标志）。**设计文档 §5.1 已据此改为"方法族"**。
  - **端口方法总数（现算复核；口径 = `core/ports/*.java` 的方法声明计数）**：**33 →（加 5 参重载后）34**；分项现算（2026-09-17 18:04:11）：`BuffPort` **6 → 7**、`ComponentLookup` 1、`ComponentServices` 0（record 无方法）、`CooldownPort` 4、`DamagePort` 4、`EnergyPort` 3、`FactionPort` 3、`SanTEPort` 5、`Self` 2、`TimerPort` 4、`VitalsPort` 1 ⇒ 合计 **33**。**`ComponentServices` 成员数仍 10**（**方法级**新增，不增成员）。
  - **② 已被否**（captain 原文理由）：把"`ambient` true→false、`particles` false→true"登记为"已申报可见变化"= **把本可避免的差异合法化**，T 块与后续批次还得一直背着它。
  - **变更面（B②-b-2）**：**3 文件（已交付并验收，captain `6fa2887`）** = `core/ports/BuffPort.java` + `core/BuffPortImpl.java` + `roleComponent/meiqihezi/skill/MeiqiheziCircleSlashSkill.java`；**等价性按 5 项**（类型 / 时长 / 增幅 / `ambient` / `particles`）。
    > **更正记录**：本节一度记为"4 文件（含 `core/RoleInstance.java`）"，那是**裁定 ① 与实施交叉造成的误记** —— eng-止血 按**最早那份"3 文件"**执行，captain **按已交付验收、不返工**；生命周期广播**独立为 `B②-c`（见 §12）**。
  **B②-b 只负责**：两个组件（`MeiqiheziUnconcernSkill` / `MeiqiheziCircleSlashSkill`）改用它 + 删旧覆写 + `markMigrated()`（b-1 与 b-2 均已完成）。

- **命名附带好处 + ⚠️ 口径分两步（eng-止血 实测，**对数必须写清用哪个口径**）**：与旧调用点 `instance.applyPotionEffect(...)` **同名**，故迁移前后可直接对照 —— 但**条数是两步变**：
  | 口径 | B②-a 之后（当前） | B②-b 之后（预期） |
  |---|---|---|
  | `instance.applyPotionEffect`（**宽松**：含 javadoc） | **8** = **7 处代码 + 1 处 javadoc**（`core/BuffPortImpl.java:43` 的说明） | **6**（代码 **7 → 5**） |
  | `instance.applyPotionEffect`（**仅代码**） | 7 | **5** |
  | `svc().buffs().applyPotionEffect` | **0** | **2** |
  > ⇒ 本清单**同时给出两个口径**；各批小结引用时**必须写明**"宽松（含 javadoc）/ 仅代码"，否则下一批对数会错位。
- 受影响 6 处的等价写法（勘察时点）：`svc().buffs().applyPotionEffect(PotionEffectType.SLOWNESS, 20, 2)`（`MeiqiheziCircleSlashSkill:49`）、`(SPEED, 40, 4)`（`MeiqiheziUnconcernSkill:40`）、`(REGENERATION, 45, 5)` / `(STRENGTH, 45, 2)`（`RedDeeplySorrowSkill:45/:46`）、`(RESISTANCE, 100, 1)` ×2（`RedBleedPassive:114/:199`）。
  > ⚠️ `DefaultSanTEZeroPunishment` 的 `STUN` 走的是**既有** `buffs().add(BuffType, ticks)`（B⑨），**不需要**新方法。
  > ⚠️ 实施前请以**内容片段**复核 `createEffect(...)` 的参数顺序（时长在前、增幅在后），行号会漂移。

### B② `red/RedDeeplySorrowSkill.java`（59 行 / `Skill`）
- **旧入口**：`onRightClick` ×1（`:27`）；另有 `update(...)`（`:36`）与 `onSanTEChange(...)`（`:53`）。
- **目标**：`onCast` → `buffs.canCastSkill()` → 自身药水（**R-1**）→ `return CAST`；`update()` 里每秒扣 SanTE → `svc().sante().decrease(10)`；`onSanTEChange(pre,now)` 覆写保留（**仅在 `pre != now` 派发**，由框架保证）。
- **端口依赖**：`buffs`、`sante`、`timers`。
- **四不变量**：无 `RoleInstance`（现 5 处）；不渲染；`startSkillCooldown` 三处（`:30/:47/:56`）全部删除。

### B③ `red/RedSolitaryArroganceSkill.java` ——**✅ 已完成**（eng-止血 2026-09-17 18:17；captain 验收中）

- **回滚单元（1 文件）**：`roleComponent/red/RedSolitaryArroganceSkill.java` = **5 014 B / mtime 18:17:13 / `4C86B56B308E67E9214663F87F7F877BFCEBD62E0FB13457F8BFDBB769D4AF23`**（**我的独立实测复核 18:21 逐条通过**）
- **本批改动**：构造器 `markMigrated();`；**删旧覆写** `onRightClick(Player, RoleInstance)` → `onCast(CastSignal)`；**现场核实**旧 `:34` 在 `canCastSkill` 不满足时**直接 return（不启冷却）** ⇒ 新写法 **`return CastResult.NO_COOLDOWN`**（等价说明写进代码 javadoc）；`instance.rolesContext().scheduler().runRepeating(…, 1L, 6)` → **`svc().timers().runRepeating(1L, 6, …)`**（**登记进本组件资源表**）；`:57` `SkillUtil.getPlayersInSightLine(cas, 5, 0.4)` **保持静态**（不进端口白名单）；`isHostileTo` → **`svc().factions().isHostile`**；`DamageUtil.dealtPhysicalDamage(victim, cas, 8)` → **`svc().damage().physicalDamage(victim, cas, 8)`（8 不变）**；`heal(4)` → **`svc().vitals().heal(4)`（4 不变）**；**删 `startSkillCooldown(...)`** ⇒ 末尾 **`return CAST`**（声明值 200）；**删 `isValid()` 守卫**（任务已登记资源表 ⇒ `clear()` 的 `cancelAllAndClear()` 必取消 ⇒ 不可达）；**去掉 `implements LifecycleAware` 与 import**，`start/stop` 改**无参覆写新基类钩子**（`stop()` 取消 `attackTask`）⇒ **同批完成 legacy→新钩子转换**（§12 打通的通道**首次被实际使用** ✓）。
- **判据（严格写法，不套用 CircleSlash 的宽松措辞）**：`RoleInstance` **类型引用 = 0 / 字面量 = 0** ⇒ 直接写 **「= 0」**；实测算得 `RoleInstance` 出现 **1** 次且**仅 javadoc**（`:33` 批次注释），`LifecycleAware` 出现 **1** 次且**仅 javadoc**（`:101`，说明与旧 `LifecycleAware.stop(...)` 的等价性）—— **类型 / 字面量 / javadoc 三分法**。
- **证据（运行实测）**：`BUILD SUCCESSFUL in 14s`｜jar **140 524 B @2026-09-17 18:17:50 / `A0416CD7E2457C0E385A044D6D3AC4AA02EAABB4DF711B5ACB927505C366CC5F`**｜最新 `.class` = `RedSolitaryArroganceSkill.class @18:17:49`｜`src 新于 jar = 0`｜**Mode B 三行齐**（`docs/阶段4-起服日志-B3.log :44/:45/:49`，`Done (16.037s)`）+ 正序集合差对 t6 锚点 **`C = 0`**｜**已停服**（java 0、25566 未监听）。
- **计数（我的独立实测，18:21）**：**`roleComponent/**` 的 `rolesContext()` 4 → 3、全仓 9 → 8（第二次下降）**；**`markMigrated();` 3 → 4**；`getInstance()` **0**；`USE_COMPONENT_PIPELINE` **0**；**`implements LifecycleAware` = 6**（`RoleInstance:720` + 5 个未迁移组件 —— `CircleSlash` 与 `RedSolitaryArroganceSkill` **均已退出**）。
- **原勘察内容（保留作对照，已过期）**：旧入口 `onRightClick` ×1（`:33`）；`:35` `instance.rolesContext().scheduler().runRepeating(...)`；`:38` `private RoleInstance casterIns = instance;`；端口依赖 `buffs`/`timers`/`factions`/`vitals`/`damage`/`cooldowns`。


### B④ （2 个装备被动）——**✅ 已完成并验收**（captain `4dcc6e9`；eng-止血 2026-09-17 18:23 **同批完成 legacy `*Aware` → 新钩子**）

> **身份以验收版为准（captain `4dcc6e9`）**：上表两行为**验收版 Hash**；我 18:22 首测的中间态（`E652A4E9…` 88 行 / `6D26E6B3…` 88 行）**已被取代**（文件在 18:23:34/37 再次更新为 87 行）——**再次印证"数值必须现算"**。

**回滚单元（2 文件，逐文件 SHA256）**（**我的独立实测，18:22:56**）
| 文件 | 行数 / 字节 / mtime | SHA256 | 状态读数 |
|---|---|---|---|
| `roleComponent/meiqiHezi/passive/MeiqiheziEquipmentsPassive.java` | 87 行 / 3 783 B / 18:23:34 | **`F2CD6C7FA9D4061EBABC601505F8B1FBA5F69EA725453BECAEA778FCD9EDA9AF`** | `RoleInstance` **0** / `LifecycleAware` **0** /（被动**无** `markMigrated`，见 §2.0 第②条） |
| `roleComponent/red/RedEquipmentsPassive.java` | 87 行 / 3 750 B / 18:23:37 | **`A01BAD3D4028910F4BB4D74D8D65E80D0E18EDBA3B852D3AFD3D524C661CF944`** | `RoleInstance` **0** / `LifecycleAware` **0** /（同上） |

> ⚠️ **预检值以现场为准**：captain 给的预检（`03A94590…` / `844CAD6D…`、各 82 行）是**迁移前**的值；本批**已落地并验收**，上表为**验收版现算**。**这正体现了"开工前现场复核"纪律**。

### B⑤ （2 个自动恢复被动 + `SkillUtil` 端口化 + `FactionPortImpl` + 容器 tick 广播）——**✅ 已完成（5 文件）**

| 文件 | SHA256（我的独立实测复核 18:39，逐条与你一致） | 状态 |
|---|---|---|
| `core/RoleInstance.java`（第 1 步 tick 广播） | `FA879F9F82EBA1CE0554FE0247B1AC07A3FA3598599965397B50A402C9C91D22` | ✅ |
| `roleComponent/SkillUtil.java` | `F44EF5C73721C6355016698EB2A6832F8CEA618F9A7409AB17C9250ACBFD57F2`（56 行；`RoleInstance` 代码 **0**（含注释 1）/ `rolesContext()` **0** ✓；**保留 `getPlayersInSightLine`**） | ✅ |
| `core/FactionPortImpl.java` | `311A4FC1C541E71B077EADD87E0E29ABC4CACA7FD9E5D679440124DD4BC2E119`（47 行；**逻辑逐字搬入**，含短路与"未选角色也算敌人"；自调 `owner.rolesContext()`） | ✅ |
| `roleComponent/AutoRecoverEnergyPassive.java` | `9B3EA5FFE39C1A6D8403DDA99C63B8BD9A777C3CCF26F9ED172009248E1FFFAE`（41 行） | ✅ |
| `roleComponent/AutoRecoverSanTEHealthPassive.java` | `2D079C51394E274CB6022DF703017E9CD02DF11FDEED33D0E0253F522A0918A9`（44 行） | ✅ |

**证据（eng-止血 交付 + 我的独立实测复核 18:39）**：`BUILD SUCCESSFUL in 14s`｜jar **140 525 B @18:35:05**｜最新 `.class` = `SkillUtil.class @18:35:04`｜`src 新于 jar = 0`｜**Mode B 三行齐**（`docs/阶段4-起服日志-B5.log :45/:46/:50`，`Done (18.001s)`）+ **`C = 0`**｜已停服｜`getInstance()` **0**｜`markMigrated();` **4**（被动不打标记）｜**组件 `UpdateAware` 4 → 2** ✓（实测剩 `RedBleedPassive:21`、`RedDeeplySorrowSkill:13`）｜组件 `LifecycleAware` **3**（`DefaultSanTEZeroPunishment:18`、`MeiqiheziBloodySlashSkill:20`、`RedBleedPassive:21`）。

**周期语义（对照附录 A，逐字不变）**：10 格 / 200t(=5s) / 20t(=1s) / 能量 +3 / SanTE +3 / 生命 +1；`sante().current() <= 0` 短路保持 ✅。

**执行顺序（我方担心的风险已被规避）**：eng-止血 在**一个连续块内**按 `SkillUtil`（删方法）→ `FactionPortImpl`（内联等价实现）→ 两个被动（改端口调用）依次编辑，**中间未构建/未提交**，最后**一次构建通过** ⇒ "先删方法后改被动导致中途编译失败"**未发生**（"块内不得停顿"落地）。

> **计数口径更正（eng-止血 指出 + captain 确认）**：`rolesContext()` 的**判据只看 `roleComponent/**`（3 → 2）**；**全仓保持 8** —— `SkillUtil` 的调用搬进了 `core/FactionPortImpl`（适配器自调 `owner.rolesContext()`）。T 块第 ⑥ 项只针对 `roleComponent/**`。

> **进度：8/14 组件已迁移**（主动 4 + 被动 4）；B⑤ 整批回滚单元 = **上表 5 文件**。

> ✅ **第 5 文件（容器 tick 广播 `component.update()`）已落地（2026-09-17 18:29，eng-止血；我的独立实测复核 18:33 通过）**：`core/RoleInstance.java` = **33 870 B / mtime 18:29:14 / `FA879F9F82EBA1CE0554FE0247B1AC07A3FA3598599965397B50A402C9C91D22`**；**`:802 runComponentUpdate("registered", component.getId(), component::update);`**（**legacy 三段 `:781/:787/:793` 之后**，且 `:800` 注明"新侧组件不再 implements `UpdateAware` ⇒ 只被这一条路径调用，**不会双触发**"）；`:861 componentRegistry.cancelAllAndClear();`（在 `:725 component.stop();` 之后 ⇒ 顺序仍成立）。运行证据：`BUILD SUCCESSFUL in 15s`｜jar **140 722 B @18:29:36**｜最新 `.class` @18:29:36｜`src 新于 jar = 0`｜Mode B 三行齐（`docs/阶段4-起服日志-B5-step1.log :45/:46/:50`，`Done (15.464s)`）｜**`C = 0`**｜已停服｜`getInstance()` 0｜`rolesContext()` 不增（8 / 3）｜`markMigrated();` **4**。
> **历史（为什么必须并入）**：两被动靠 `update()` 周期恢复，容器此前只广播 `awake/start/stop` ⇒ 迁移后 `update()` 永不执行 = **恢复静默失效**（O-4 同类；**只在有玩家选角色后暴露**，构建/Mode B/集合差/数值 diff 全绿也照样发生）。**该风险现已由第 5 文件落地消除**。

**captain 裁定（2026-09-17）：把容器 tick 广播并入 B⑤** —— 与"B②-c 并入 b-2"**同一模式**（避免"迁移后功能静默失效"）。

| # | 文件 | 本批改动 |
|---|---|---|
| 1 | `roleComponent/AutoRecoverEnergyPassive.java` | legacy→新钩子（`update(Player, RoleInstance)` → **无参 `update()`**）、去 `implements UpdateAware` + import、端口化（`energy().gain(3)`） |
| 2 | `roleComponent/AutoRecoverSanTEHealthPassive.java` | 同上（`sante().current() <= 0` 早退 → `sante().gain(3)` + `vitals().heal(1)`） |
| 3 | `roleComponent/SkillUtil.java` | **端口化（R-2，硬约束 §19 合块）**：`RoleInstance` **0**、`rolesContext()` **0**；`hasEnemyInRange` 语义**逐字冻结**（"未选角色也算敌人"） |
| 4 | `core/FactionPortImpl.java` | 与 `SkillUtil` **同批**改（现形态 `SkillUtil.hasEnemyInRange(owner, owner.getPlayer().getLocation(), radius)` ⇒ 组件侧改走 `svc().factions().hasEnemyInRange(radius)`） |
| 5 | `core/RoleInstance.java` | **tick 广播 `component.update()`**（容器现只广播 `awake/start/stop`） |

**为什么必须并入 tick 广播（裁定理由）**：两个 AutoRecover 被动靠 `update()` 周期恢复，而容器（B②-c）**只广播了 `awake/start/stop`** ⇒ 迁移后 **`update()` 永不执行 = 恢复静默失效**（与 O-4 同类、且比前几批更难在运行时发现）。

**B⑤ 剩余 4 文件 + 已商定的实现口径（eng-止血 2026-09-17；captain 同意"更省"路线：删方法而不是改签名）**
- **实测依据（我的独立复核 18:33）**：`SkillUtil.hasEnemyInRange(RoleInstance, Location, double)` **调用点共 3 处** —— `core/FactionPortImpl.java:32`、`roleComponent/AutoRecoverEnergyPassive.java:20`、`roleComponent/AutoRecoverSanTEHealthPassive.java:20`（定义在 `SkillUtil.java:17`）。⚠️ **不是 1 处**（eng-止血 信中"唯一剩余调用点 = `FactionPortImpl:32`"**不准确**）⇒ **删方法必须与两个被动的迁移在同一次编译里完成**，否则编译失败。
- **`roleComponent/SkillUtil.java`**：**删除** `hasEnemyInRange`（连带 `RoleInstance` / `Faction` import 与 `:25` 的 `rolesContext()` 一并消失），**保留** `getPlayersInSightLine`（纯几何静态工具）⇒ §9.2 的两条验收（`RoleInstance` = 0、`rolesContext()` = 0）**直接成立**；
- **`core/FactionPortImpl.java`**：`hasEnemyInRange(radius)` 把逻辑**逐字搬入**（含 `loc == null || world == null` 短路与"**未选角色的玩家也算敌人**"）；
- **两个 `AutoRecover*Passive`**：无参 `update()` + 去 `implements UpdateAware` + `svc().factions().hasEnemyInRange(10)` + `svc().energy().gain(3)` / `svc().sante().gain(3)` / `svc().vitals().heal(1)` / `svc().sante().current() <= 0` 早退 —— **数值/间隔逐字不变**（10 格、200t、20t、+3、+3、+1）。
- **计数（B⑤ 实测，captain `8cd5d72` 已验收）**：**8/14 组件**；组件 `UpdateAware` **4 → 2**；**`rolesContext()`：全仓 8 不变（判据不用它） / `roleComponent/**` 3 → 2（判据用这个，净下降 ✓）** —— 原因是该调用搬进了 `core/FactionPortImpl`（适配器自调 `owner.rolesContext()`）；T 块第 ⑥ 项**只针对 `roleComponent/**`**。；`markMigrated();` **仍 4**（被动不需要）。

**B⑤ 额外必证（captain 口径）**
1. **tick 广播与 legacy `UpdateAware` 扇出的先后/共存说明**（两套并存、未迁移组件无行为影响）；
2. `SkillUtil` 内 **`RoleInstance` = 0** 且 **`rolesContext()` = 0**、`hasEnemyInRange` **语义逐字冻结**、**三处调用点一起改**（`FactionPortImpl:32` + 两个 AutoRecover 被动）；
3. 两个 AutoRecover 的**周期语义**（数值/间隔对照附录 A **逐字不变**）且 **`update()` 迁移后确实被调用**（**结构性证明**，不得只看"构建/Mode B/集合差/数值 diff 全绿"）；
4. 其余照 §2.0 三条结构性证明 + 双口径（含 (d) 申报）+ 构建新鲜度 + Mode B 三行 + 正序集合差 **只看 `C`** + **停服** + `getInstance()` 0 + 逐文件 SHA256 回滚单元 + 运行深度声明。

**计数预期**：`rolesContext()` **3 → 1 或 2**（以现算为准）；`implements LifecycleAware` 实现者现 **3 组件**（`DefaultSanTEZeroPunishment:18`、`MeiqiheziBloodySlashSkill:20`、`RedBleedPassive:21`）+ `UpdateAware` 的**两个 AutoRecover** 本批退出；`markMigrated();` **保持 4**（被动不需要，见 §2.0 第②条）。


**同批 legacy→新钩子转换（B③ 范式，§12 通道第三次使用）**
- `start(Player, RoleInstance)` → **无参 `@Override public void start()`**（首行 `Player player = svc().self().player();`，**其余正文逐字不动**）；
- `stop(Player, RoleInstance)` → **无参 `stop()`**（四槽清空顺序与内容**逐字不动**）；
- **去掉 `implements LifecycleAware` 与 import**（两文件 `LifecycleAware` 命中 **0**）；
- **两文件均无 `UpdateAware`/`update()`** ⇒ 本批只动 `start`/`stop` ✓（实测无 `update` 覆写）。

**等价性要求（本批最关键）—— 四槽材质对照表（迁移后实测，逐件核对）**
| 槽位 | `MeiqiheziEquipmentsPassive` | `RedEquipmentsPassive` | 结论 |
|---|---|---|---|
| helmet | `Material.IRON_HELMET`（`:28`） | `Material.IRON_HELMET`（`:28`） | 相同 ✓ |
| chestplate | `Material.LEATHER_CHESTPLATE`（`:38`） | `Material.IRON_CHESTPLATE`（`:38`） | **正好相反** ✓（防串写） |
| leggings | `Material.IRON_LEGGINGS`（`:49`） | `Material.LEATHER_LEGGINGS`（`:47`） | **正好相反** ✓（防串写） |
| boots | `Material.LEATHER_BOOTS`（`:59`） | `Material.LEATHER_BOOTS`（`:59`） | 相同 ✓ |
**设置顺序 = `helmet → chest → legs → boots`**（`setHelmet:71 / setChestplate:72 / setLeggings:73 / setBoots:74`），**清空同序**（`:83/:84/:85/:86`，两文件一致）—— **顺序不变**是等价性的一部分（避免"先清后设"类错序）。

**判据（严格写法）**：两组件 **`RoleInstance` 类型引用 = 0 / 字面量 = 0 ⇒ 直接写「= 0」**（**它们无渲染覆写，不套 `CircleSlash` 的宽松措辞**）✓ 实测两文件 `RoleInstance` 总命中均为 **0**。

**计数预期 → 实测结果（全部命中）**
| 项 | 预期（captain） | 我的实测（18:22:56） |
|---|---|---|
| `markMigrated();` | 4 → **6** | **6** ✓ |
| 组件 `LifecycleAware` 实现者 | 5 → **3** | **3** ✓（全树 4 = `RoleInstance:720` + 3 组件：`DefaultSanTEZeroPunishment:18`、`MeiqiheziBloodySlashSkill:20`、`RedBleedPassive:21`） |
| `rolesContext()` | 不变（3 / 8） | **8（全树）/ 3（`roleComponent/**`）** ✓ 本批不动调度 |
| `USE_COMPONENT_PIPELINE` | 0 | **0** ✓ |

**交付要求（captain 口径，逐条）**：§2.0 三条结构性证明 · 双口径（含 (d) 申报）· 构建（最新 `.class` + `src 新于 jar = 0`）· **Mode B 三行 + 正序集合差只看 `C`** + **停服** · `getInstance()` 0 · 逐文件 SHA256 回滚单元 · **运行深度声明**。（以上由 eng-止血 在本批小结给出；我这里的实测见上表。）

**原勘察内容（保留作对照，已过期）**：`meiqiHezi/passive/MeiqiheziEquipmentsPassive.java`（82 行）、`red/RedEquipmentsPassive.java`（82 行）；旧入口仅 `start(Player, RoleInstance)`（`:22`）与 `stop(...)`（`:73`）；端口依赖仅 `self`；无冷却。

### B⑤ （2 个自动恢复被动）——**详见上方 §2 中"B⑤ …变更面 = 5 文件"块（本表为原文勘察，保留作对照）**
`AutoRecoverEnergyPassive.java`（37 行）、`AutoRecoverSanTEHealthPassive.java`（40 行）：
- **旧入口**：无；仅 `update(Player, RoleInstance)`（各 `:19`）。
- **目标**：`update()`（20 Hz）内：能量版 `svc().energy().gain(3)`；SanTE 版 `if (svc().sante().current() <= 0) return; svc().sante().gain(3); svc().vitals().heal(1);`
- **端口依赖**：`energy` / `sante`+`vitals`；**无敌 200t 逻辑**（如需计时改用 `timers`，勘察可见其依赖 `SkillUtil`，见 §4 R-2）。
- **四不变量**：无 `RoleInstance`（各 2 处）；不渲染；无冷却。
- **⚠️ 本批已扩为 5 文件（含容器 tick 广播）**：见上方"B⑤ …变更面 = 5 文件"块的裁定理由与额外必证。

### B⑥ `red/RedBleedPassive.java`（204 行 / `PassiveSkill`）——**本批是 B⑦ 的前置**
- **旧入口**：无 `onXxx`；`start`（`:54`）`setContext` 两键（`:55/:56`）、`stop`（`:60`）、`update`（`:78`）、私有 `resolveRequestedBleed`（`:149`）/`resolveBleed`（`:170`）。
- **目标**：**账本私有化**（删 `BLEED_RECORD_CONTEXT_KEY`/`BLEED_RESOLVE_REQUESTS_KEY` 与 `Map<String,Object>` 上下文），公开 `applyStacks(...)` / `requestResolve(...)` / `stacksOf(...)` 供 B⑦ 消费；`update` 内 `svc().sante().gain(4)`（`:115/:200`）、抵抗药水（`:114/:199`，**R-1**）。
- **端口依赖**：`sante`、`timers`、`damage`（真伤结算）、`self`。
- **四不变量**：无 `RoleInstance`（现 7 处）；**跨组件通信只经 `getComponent`**（否则 B⑦ 无法解析）；不渲染；无冷却。
- **改动面**：仅本文件；但**接口面**（公开方法签名）是 B⑦ 的输入，需与本清单一并冻结。
- **⚠️ 范围更新（硬约束 §18 裁定，2026-09-17）**：**`requestResolve(UUID victimId, int stacks)` 已在 B① 前移**（`RedBleedPassive.java: D4B8CA75…BA3E`，写入 `start()` 发布的同一份账本）⇒ **从 B⑥ 的清单里划掉这一项**。
  **B⑥ 仍负责（裁定 (i)，2026-09-17 缩小后）**：`applyStacks(UUID,int)` / `stacksOf(UUID)`（供 B⑦ 消费）＋ **账本私有化** ＋ **保留 `requestResolve`**（B① 前移项闭环）＋ **同批 legacy 转换**（去 `implements LifecycleAware, UpdateAware`）。
**⚠️ 移交 B⑦（第五次跨批依赖，模式同 §18）**：**删除 context 设施**（`RoleInstance` 的 **4 个方法** `setContext:251` / `getContext:255` / `removeContext:261` / `hasContext:265` + 字段 `context:72` + `context.clear():883` + 两个 `*_CONTEXT_KEY`）—— **因为其最后使用者属 B⑦**：`roleComponent/red/RedSanctifiedBladeMainWeapon.java:35` 仍 `getContext(RedBleedPassive.BLEED_RECORD_CONTEXT_KEY, Map.class)`。
**B⑥ 的 context 计数 = 5（我方现算 18:40:49，逐条给出处）**：`RoleInstance.setContext:251`（声明）+ `RoleInstance.getContext:255`（声明）+ `RedBleedPassive:68`（`setContext`）+ `RedBleedPassive:69`（`setContext`）+ `RedSanctifiedBladeMainWeapon:35`（`getContext`）。
⇒ **"context 全仓命中 = 0"这一判据随设施删除一起移交 B⑦**（§18④ 两批各记一次：**B⑥ 记"桥保留 + 原因 + 最后使用者位置"**；**B⑦ 记"已删除 + 全仓 0"**）。
**本批保留的过渡桥**：`setContext(...)` 两行 + 两个 `*_CONTEXT_KEY`（供 B⑦ 的武器继续读**同一份私有账本** ⇒ **不另建并行存储**）。
**另登记**：`RedBleedPassive` **同时** implements `LifecycleAware` 与 `UpdateAware` ⇒ 本批**三个钩子一起转**（`start:67` / `stop:73` / `update:91` → **无参**），是"同批 legacy 转换"新常态下**一次性转换最多钩子**的组件。
  **证据要求（按裁定 (i) 更正，2026-09-17）**：B⑥ **不删** context 设施，故本轮**不再要求**"4 方法/两键命中 = 0"；改为给：① **context 计数 = 5**（出处见上）；② 小结明写"**过渡桥 `setContext` 与私有账本 = 同一份数据、不另建并行存储**"（供 B⑦ 继续读同一本账）；③ **归零判据（4 方法 + 2 键 ⇒ 全仓 0）随设施删除移交 B⑦**（B⑥ 记"桥保留 + 原因 + 最后使用者位置"，B⑦ 记"已删除 + 全仓 0"）。
  **裁定 (A)（captain 2026-09-17；采纳 eng-止血 推荐，拒绝 (B) 扩白名单）**：B⑥ **保留 `implements LifecycleAware`**（`start/stop(Player, RoleInstance)` **原样**，**仅供发布过渡桥**）；**只把 `update(Player, RoleInstance)` 转无参**并去 `UpdateAware`。
  > **为什么不选 (B)**：白名单 **10 成员是冻结契约**，而过渡桥是 **B⑦ 即将删除的临时机制** ⇒ **不值得为过渡期扩契约**（见 §2.0 第 ⑧ 条原则）。
  **预期计数（B⑥ 完成后，按 (A) 修正）**：组件 **`UpdateAware` 2 → 1** ✓；**`LifecycleAware` 3 → 3（本批不降** —— `start/stop` 保留**）；`markMigrated();` **保持 4**；`rolesContext()` 判据 `roleComponent/**` **2（不变 —— 本批不动调度）**。（现算基线 18:53：`LifecycleAware` 组件 3 / `UpdateAware` 组件 2。）
  **⚠️ 判据第三次修正（captain 2026-09-17）—— 两段式，必须写进本批小结**
  > **"组件内 `RoleInstance` 严格 = 0"在本批不可能成立**（`start/stop` 签名仍带 `RoleInstance`）⇒ **B⑥ 判据 = 「除过渡桥所用的 legacy `start/stop(Player, RoleInstance)` 签名外 = 0」**，并给 **三分法现算**（类型引用 / 字面量 / javadoc）与**逐处残余清单**（应仅剩 `RedBleedPassive:93 start(...)` / `:99 stop(...)` 两处签名）。
  > **"严格 = 0"随 B⑦ 的删除生效**：B⑦ 记"**已归零 + 分项各自为 0**"（两段式：B⑥ 除外口径 / B⑦ 归零口径）。

### B⑦ （2 个主武器）
`red/RedSanctifiedBladeMainWeapon.java`（53 行 / `MainWeapon`）：`onAttack(attacker, victim, RoleInstance)` ×1（`:32`）；`:35` 用 `getContext(BLEED_RECORD_CONTEXT_KEY)` 读账本 → 改 `getComponent(RedBleedPassive.class)` + `applyStacks(...)`（**B⑥ 必须先落地**）；`:50` 删 `startMainWeaponCooldown` → 返回 `CAST`。
`meiqiHezi/mainWeapon/MeiqiheziJuejueMainWeapon.java`（62 行 / `MainWeapon`）：`onLeftClick` **×2**（`:34` 声明 + 语义上的第二处，另 `:27/:34` 为 `instance.` 调用）；能量阈值 20/扣 5/单体 8(+0.5 击退)/范围 14/半径 5/50 点圆 → `energy().current()` / `tryConsume(5)` / `damage().physicalDamage(...)`；**主武器 `energyCost` 恒为 0（附录 A）**，不得把 5 填进构造参数。
- **四不变量**：无 `RoleInstance`（各 2 处）；跨组件只 `getComponent`；不渲染；冷却声明值为唯一真值源。

### B⑧ （2 个 meiqi 技能，最大改动面）
> **批次映射备注（eng-止血 实际执行口径）**：`MeiqiheziCircleSlashSkill` 的**两组件同批**处理被拆成 **B②-b-1**（已完成的 `MeiqiheziUnconcernSkill`）+ **B②-b-2**（`MeiqiheziCircleSlashSkill`，下一窗口）—— 因为两者共用 R-1 的 `buffs().applyPotionEffect(...)`，一起做只需一次端口验证。**本节其余内容对该组件仍然生效**（渲染覆写保留属本节，见下）。
> ✅ **药水标志位：已按裁定 ① 落地（2026-09-17 18:06，eng-止血；设计文档 §5.1，commit `181efad`）** —— `BuffPort` **加 5 参重载** `applyPotionEffect(type, duration, amplifier, ambient, particles)`（方法级新增、`ComponentServices` 成员数仍 **10**；端口方法总数 **33 → 34**），本组件调 `(SLOWNESS, 20, 2, true, false)` ⇒ **标志位逐字保真**。
> **b-2 已落地文件的 Hash（3 文件，含 b-1 的 `Unconcern`）**：`core/ports/BuffPort.java` = **`892FEC26005BF228527BE09A314C39B2ED094593D174A071807DDDB114257E8D`**（含 5 参重载；B②-a 版 `55E904CD…` 已被取代）；`core/BuffPortImpl.java` = **`832CF4ED09B6AB8115CA5741022866F298C7BEEBC3694DA3E4B019EB555D404F`**（5 参实现 = `new PotionEffect(type,dur,amp,ambient,particles)` → `owner.applyPotionEffect(...)`，**同一已记账路径**；B②-a 版 `1AE3123A…` 已被取代）；`roleComponent/meiqihezi/skill/MeiqiheziCircleSlashSkill.java` = **`E12EC503575DB04EF90E5FF0A794F87FB10C863D2E90F34AF59AB7AAB614DA25`**（b-2 中间态；**已被 `B②-c` 的 `E25660A0…14EC` 取代**，见下）。
> **b-2 运行证据**：`BUILD SUCCESSFUL in 15s`｜jar **140 283 B @2026-09-17 18:06:00**｜最新 `.class` @18:06:00｜`src 新于 jar = 0`｜**Mode B 三行齐**（`docs/阶段4-起服日志-B2b2.log :45/:46/:50`，`Done (17.492s)`）+ 正序集合差 **`C = 0`**｜已停服｜`getInstance()` **0**。
> **保留的历史一句**：**裁定前不得把本组件改为 3 参调用**（否则会凭空出现粒子漩涡 + 图标明暗变化；构建/Mode B/集合差/数值 diff 全不报）—— 已由 5 参重载解决，此句仅作纪律留痕。
> **新计数（独立实测，2026-09-17 18:10，按 §11 口径纪律标注）**：**`rolesContext()`：`roleComponent/**` 5 → 4、全仓 10 → 9**（**第一次真正下降**；CircleSlash 内已由 `instance.rolesContext().scheduler()` 改为 `svc().timers().runLater(...)`，`:65`）；`markMigrated();` **2 → 3**（组件侧：`RedEvilShockSkill:18`、`MeiqiheziUnconcernSkill:25`、`MeiqiheziCircleSlashSkill:40`）；代码口径 `.applyPotionEffect(`：本组件 `instance.` **1 → 0**、`svc().buffs().` **0 → 1**（5 参）；`BuffPortImpl` `owner.` **1 → 2**；`svc().buffs().applyPotionEffect` 全仓 —— **仅代码 = 2** / **含 javadoc = 3**（按 §2.0 第④条分列）。
> **该组件判据口径（写死，防 t15 误判；已按 `B②-c` 落地更新）**：`RoleInstance` 引用判据**不是"= 0"**，而是「**除 B⑧ 的两份渲染覆写（类型引用 2 处：`createIconItem` / `getDisplayName(RoleInstance)`，现 `:114` / `:170`）与其字符串字面量（1 处：`"RoleInstance is Null!"`，现 `:172`）外 = 0**」—— **类型引用 / 字面量 / javadoc 三者分开计**。
> **`B②-c` 完成后的现算读数（我的独立实测，2026-09-17 18:15）**：`CircleSlash` 内 `RoleInstance` 出现 **4** 次 = **类型引用 2**（`:114` / `:170`）+ **字面量 1**（`:172`）+ **javadoc 提及 1**（`:43`，批次注释）；`LifecycleAware` 出现 **1** 次且**仅 javadoc 提及**（`:101`）⇒ **代码命中 = 0**；`stop()` 现为**无参基类覆写**（`:105`）。
> **保留的事实描述（供核对）**：`MeiqiheziCircleSlashSkill:47` 现写法 = `new PotionEffect(PotionEffectType.SLOWNESS, 20, 2, ambient=true, particles=false)`，**与 3 参适配的默认值（ambient=false / particles=true）不同** —— 若误用 3 参，会凭空出现粒子漩涡 + 图标明暗变化（**未申报可见变化，构建/Mode B/集合差/数值 diff 全不报**）。
> 📌 **裁定 ① 的生命周期广播 → 已独立为 `B②-c`（captain 2026-09-17；**B②-b-2 按已交付的 3 文件验收、不返工**）**：本条的机制要求**全部移入 §12 `B②-c`**；此处只留结论 —— **b-2 = 3 文件（已交付）**，而"容器为注册组件广播新生命周期钩子"= **独立块 B②-c，变更面 2 文件**（`core/RoleInstance.java` + `MeiqiheziCircleSlashSkill.java`）。
> **本条的历史记录（保留）**：曾按"并入 b-2 ⇒ 变更面 4 文件"登记；我的独立实测（18:10）确认第 4 文件当时**确实未落地**（`RoleInstance` 仍只向 legacy `*Aware` 扇出：`triggerLifecycleAwake:123` / `triggerLifecycleStart:124`；`componentServices` 注册在 `:149-153`）⇒ 经 captain 裁为**独立块**（顺序：`B② → B②-c → B③`）。
> **功能影响（不变）**：`MeiqiheziCircleSlashSkill:101` 的 `stop(Player, RoleInstance)` 仍靠 legacy 扇出生效 ⇒ **无功能回归**；"去 `RoleInstance`"待 B②-c 完成后达成。
> **必证口径修正（重要，勿按"= 0"核）**：b-2 的 `RoleInstance` 判据**不是"= 0"**，而是「**除 B⑧ 的两份渲染覆写（`:106` / `:162`）与其内字符串字面量（`:164`）外 = 0**」—— **类型引用与字面量分开计**，否则 t15 会误判。
> **一次性收益**：该机制就绪后，**T 块第 ⑤/⑥ 项**与其余 12 个组件的"legacy 生命周期 → 新钩子"一并解决 ⇒ 后续每批才能真正做到"删旧覆写 + 去 `RoleInstance`"。
> **B②-b-2 预检（eng-止血 下一窗口）**：`MeiqiheziCircleSlashSkill` = **187 行**（§0 的 SHA256 现场复核后照用）：`onCast` + 端口（`energy().tryConsume` → `NO_COOLDOWN`；`buffs().canCastSkill()`；`return CAST`，**声明值 200**；`factions().isHostile(victim)`；药水见上 ✅）+ `:57` 的 `runLater(new BukkitRunnable…)` → **`svc().timers().runLater(...)`**（⇒ `roleComponent/**` 的 `rolesContext()` **5 → 4**，这是"只减不增"的第一次下降）+ `:62 isValid()` 删除 + **四步等价链**（`clear()` → `componentRegistry.cancelAllAndClear()` 取消该组件资源表里的任务 ⇒ 延迟体不可达；**删守卫与改调度必须同批**）+ `:66` 保留 + **保留两份渲染覆写**（`:108-180`，属本批 B⑧）+ 双口径 + 逐文件 SHA256。

`meiqiHezi/skill/MeiqiheziBloodySlashSkill.java`（181 行）、`meiqiHezi/skill/MeiqiheziCircleSlashSkill.java`（187 行）：
- **旧入口**：各 `onRightClick` ×1（`:39` / `:41`）；`instance.` 调用 **6 / 7 处**（能量、冷却、任务、阵营、渲染、显示名）。
- **目标**：保留 `start/stop` 的任务句柄化成果，改走 `svc().timers()`；**删除两组渲染覆写**（`createIconItem(RoleInstance)`、`getDisplayName(RoleInstance)` 及其 `"RoleInstance is Null!"` 分支 —— 这是**渲染由框架统一负责**的落地）；`onCast` 返回 `CAST`。
- **端口依赖**：`energy`（`:40/:42/:110/:132/:172` 等）、`buffs`（`:41/:103/:162`）、`timers`（`:45/:57`）、`factions`（`:70/:83`）、`damage`、`cooldowns`（框架）。
- **四不变量**：无 `RoleInstance`（6/7 处）；不渲染（**删两处覆写是本批的核心验收点**）；`startSkillCooldown` 删除；`rolesContext()`（`:45/:57`）删除（T 块项）。

### B⑨ `DefaultSanTEZeroPunishment.java`（137 行 / `PassiveSkill`）
- **旧入口**：无 `onXxx`；`onSanTEChange(Player, RoleInstance, int, int)`（`:31`）、`start`（`:121`）、`stop`（`:134`）。
- **目标**：`onSanTEChange(pre, now)` 覆写；`setIsInSanTEPunishmentState(true/false)`（`:65/:112/:114`）→ **组件私有字段**；`getBuffManager().addBuff(STUN,100)`（`:67`）→ `svc().buffs().add(...)`；`rolesContext().scheduler()`（`:39`）→ `svc().timers()`；`setCurrentSanTE(getMaxSanTE())`（`:113`）→ `svc().sante().set(svc().sante().max())`。
- **端口依赖**：`buffs`、`sante`、`factions`（`:33`）、`timers`、`damage`（结算 0.3×最大生命×0.33333）。
- **四不变量**：无 `RoleInstance`（3 处）；不渲染；`isValid` 守卫由框架回收替代。

---

## 12. **B②-c（裁定 ① 的落地）：容器为注册组件广播新生命周期钩子**
**顺序**：`B② → **B②-c** → B③ → B④ → B⑤（含 SkillUtil）→ B⑥ → B⑦ → B⑧ → B⑨ → T`（captain 2026-09-17）。

**状态：✅ 已完成并验收（captain `a69cdd8`；eng-止血 2026-09-17 18:11 落地，我的独立实测复核 18:15/18:16 逐条通过）**。**为什么不并入 b-2**：① b-2 已按"**实施前最后一次裁定的原文**（3 文件）"交付并通过验收，**按防返工纪律不返工**；② 生命周期广播是**共享机制**改动（影响**全部 14 个组件**的 stop 语义），**值得独立成块 + 独立收官**。

**落地证据（eng-止血 交付 + 我的独立实测复核）**
| 文件 | SHA256 | 关键点（我的实测行号） |
|---|---|---|
| `core/RoleInstance.java` | **`10C4FBCC30F318302097C17D317E34B87FDCD82369E8DB4FE5EAE313786C97C2`**（33 194 B / mtime 18:10:45） | `:661-662 for(RoleComponent component : componentRegistry.all()){ component.awake();`、`:690-691 … component.start();`、`:724-725 … component.stop();`；**:853 `componentRegistry.cancelAllAndClear();` ⇒ `stop()` 广播严格早于兜底回收 ✓**；`:722` 已写**幂等说明**（组件自取消后，`cancelAllAndClear()` 仍会取消其资源表任务）；**legacy `*Aware` 三段扇出一字未动** |
| `roleComponent/meiqihezi/skill/MeiqiheziCircleSlashSkill.java` | **`E25660A00A026253B9CBCD9C38AEB22527F4E8005982DC17B1966F0CA7A214EC`**（7 943 B / mtime 18:11:00） | 去掉 `implements LifecycleAware` 与 import；**取消逻辑搬进新基类无参 `stop()`（`:105`）**；`LifecycleAware` 命中 **1** 且**仅 javadoc**（`:101`）⇒ 代码命中 **0** |

**运行证据**：`BUILD SUCCESSFUL in 16s`｜jar **140 341 B @2026-09-17 18:11:24**｜最新 `.class` @18:11:23｜`src 新于 jar = 0`｜**Mode B 三行齐**（`docs/阶段4-起服日志-B2c.log :45/:46/:50`，`Done (15.835s)`）+ 正序集合差 **`C = 0`**｜已停服｜`getInstance()` **0**｜`rolesContext()` **不增**（全仓 9 / `roleComponent/**` 4）｜`markMigrated();` **3**。

**B② 整批进度（a + b-1 + b-2 + c）**：**4 个文件全部落地**，回滚单元 SHA256：
`BuffPort 892FEC26…7E8D`｜`BuffPortImpl 832CF4ED…D404`｜`MeiqiheziUnconcernSkill 0080F3D8…EDBC5`｜`MeiqiheziCircleSlashSkill E25660A0…14EC`（b-2 中间态 `E12EC503…DA25` 已被取代）＋ **`RoleInstance 10C4FBCC…97C2`**（若把 c 独立计，则 b-2 = 3 文件、c = 1 文件，**两种记法并存**，引用时写明用哪种）。

**变更面 = 2 文件**：
| 文件 | 本批改动 |
|---|---|
| `core/RoleInstance.java` | 为**注册表内组件**广播新生命周期钩子 |
| `roleComponent/meiqihezi/skill/MeiqiheziCircleSlashSkill.java` | **去掉 `implements LifecycleAware`**（`:25`），把 `castTask` 取消搬进**新基类 `stop()`** |

**机制要求（须逐条落地）**：
1. `RoleInstance` 为**注册表内组件**在**与 legacy 扇出同一时序点**广播 `awake()` / `start()` / `stop()`；
2. **`stop()` 必须早于 `componentRegistry.cancelAllAndClear()`** —— 否则组件自己的 `castTask.cancel()` 与框架兜底取消的先后关系不成立；
3. **legacy `LifecycleAware` 扇出保留**（未迁移组件仍走它；新钩子对它们是基类默认空实现 ⇒ **无行为影响**）；
4. **停止顺序须与 legacy 逆序（`weapon → passive → skill`）等价**；
5. **双取消幂等**：组件 `stop()` 取消 `castTask` 后，`cancelAllAndClear()` 再取消其资源表任务（不得抛异常、不得重复副作用）。

**背景（为什么必须做）**：`MeiqiheziCircleSlashSkill` 的 `stop(Player, RoleInstance)` 是 **O-4 的前摇取消点**；容器 `triggerLifecycleStop()` **只向 legacy `*Aware` 扇出** ⇒ 若照字面删 `LifecycleAware` 而不广播新钩子 ⇒ **`castTask.cancel()` 无人调用 ⇒ 前摇任务跨角色清除仍执行 = O-4 回归**（"绿灯全过但语义错"）。

**完成判据**：
- `RoleInstance` 内存在**对新侧 `RoleComponent` 的 `awake()/start()/stop()` 广播**（现算：**0** —— 截至 2026-09-17 18:10 实测仍只向 legacy `*Aware` 扇出：`triggerLifecycleAwake:123` / `triggerLifecycleStart:124`）；
- `MeiqiheziCircleSlashSkill` 内 **`implements LifecycleAware` 命中 = 0**，且其 `RoleInstance` 引用**只剩 B⑧ 的两份渲染覆写**（`:110` / `:166`，含字面量 `:168`）；
- 每批固定证据三条（§2.0）+ 起服 Mode B 三行 + 正序集合差 `C=0` + 已停服。

**一次性收益**：该机制就绪后，**T 块第 ⑤/⑥ 项**与其余 12 个组件的"legacy 生命周期 → 新钩子"一并解决 ⇒ 后续每批才能真正"删旧覆写 + 去 `RoleInstance`"。---

## 3. 收尾块 T：7 项删除清单的**删除前基线**（实测 grep 命中数 + 文件:行号）

口径：`Get-ChildItem src/main/java/com/shadowHunterRolesPlugin -Recurse -Filter *.java | Select-String <pattern>`；时点同 §0。

| # | 删除项 | 命中总数 | 文件:行号明细 |
|---|---|---|---|
| 1 | `isMigrated` | **3** | `core/RoleInstance.java:356`、`core/Skill.java:28`、`roleComponent/ActiveComponent.java:45` |
| 2 | `markMigrated` | **3** | `roleComponent/ActiveComponent.java:29,44,50` |
| 3 | `USE_COMPONENT_PIPELINE` | **5** | `core/RoleInstance.java:96,183,204,348,354` |
| 4 | `runComponentPipeline` | **7** | `core/RoleInstance.java:302,317,332,353,397,409,421` |
| 5 | 旧入口 `onRightClick\|onLeftClick\|onDrop` | **29** | `core/MainWeapon.java:37,39,41`；`core/RoleInstance.java:308,323,338,396,403,408,415,427`；`core/Skill.java:27,34,36,38`；`listener/MainWeaponListener.java:59,63,88,92,112`；`roleComponent/ActiveComponent.java:28`；`meiqiHezi/mainWeapon/MeiqiheziJuejueMainWeapon.java:27,34`；`meiqiHezi/skill/MeiqiheziBloodySlashSkill.java:39`；`meiqiHezi/skill/MeiqiheziCircleSlashSkill.java:41`；`meiqiHezi/skill/MeiqiheziUnconcernSkill.java:28`；`red/RedDeeplySorrowSkill.java:27`；`red/RedEvilShockSkill.java:25`；`red/RedSolitaryArroganceSkill.java:33` |
| 6 | `bind(` | **6** | `core/Role.java:88`、`core/RoleInstance.java:155`、`core/ports/ComponentServices.java:8`、`roleComponent/RoleComponent.java:10,38,46` |
| 7 | `rolesContext()`（**全部**） | **10** | `core/RoleInstance.java:140`、`core/TimerPortImpl.java:26,33,40`、`manager/BuffManager.java:136`、`roleComponent/DefaultSanTEZeroPunishment.java:39`、`roleComponent/SkillUtil.java:25`、`meiqiHezi/skill/MeiqiheziBloodySlashSkill.java:45`、`meiqiHezi/skill/MeiqiheziCircleSlashSkill.java:57`、`red/RedSolitaryArroganceSkill.java:35` |
| 7b | 其中 **`roleComponent/**` 内**的 `rolesContext()` | **5** | `DefaultSanTEZeroPunishment.java:39`、`SkillUtil.java:25`、`MeiqiheziBloodySlashSkill.java:45`、`MeiqiheziCircleSlashSkill.java:57`、`RedSolitaryArroganceSkill.java:35` |

> **T 块判定建议**：1/2/3/4 命中应降到 **0**（且 `RoleInstance` 的整条旧管道分支删除）；5 的 14 处组件内命中随 B①…⑨ 清零，剩余 `core/*`、`listener/*`、`ActiveComponent` 的命中由 T 块删除；6 的 `RoleComponent.bind` 与 `RoleInstance:155` 是"过渡期绑定"的收尾对象（条件 ⑤：切回构造期注入后删 `bind`）；7 在 `roleComponent/**` 的 5 处必须清零，`core/`、`manager/` 内的 5 处属平台层（阶段 2 已交付形态，T 块只处理组件侧）。

---

## 4. 迁移前必须澄清的两处**风险**（会挡住批次执行，建议先裁）

- **R-1（阻塞 B②/B⑥/B⑧/B⑨ 的药水路径）**：端口白名单（`ComponentServices` 10 个成员）**没有药水端口**，但实测有 **4 个组件、6 处** `instance.applyPotionEffect(...)`：
  `MeiqiheziCircleSlashSkill.java:49`（缓慢）、`MeiqiheziUnconcernSkill.java:40`（速度）、`RedDeeplySorrowSkill.java:45/:46`（生命恢复/力量）、`RedBleedPassive.java:114/:199`（抗性）。
  可选路径：① 在 `BuffPort` 增一个"原样药水"方法（**不扩白名单**，改动最小）；② 新增 `PotionPort`（**扩白名单到 11 个**，需同步设计文档 §5 与硬约束）；③ 让这些组件暂缓迁移。**请裁一句**——在此之前，上述 4 个组件无法给出"每处旧写法 → 等价新写法"的完整映射。
- **R-2（`SkillUtil` 的归属）**：`roleComponent/SkillUtil.java` 仍持 `RoleInstance`（3 处）并在 `:25` 调 `rolesContext()`；B⑤ 两个被动与"无敌 200t"判定经它。若 T 块要删 `rolesContext()`，需先把它改为端口形态（`factions().hasEnemyInRange` 已在 `FactionPort` 白名单内）。
- **R-3（时间窗）**：本清单是**移动靶** —— eng-止血 的 B①…⑨ 会持续改动 `roleComponent/**`；每批开工前请**重新清点该批两个文件的行数 + SHA256 + 标识符出现次数**，不要直接沿用本表（本表用于"知道该改什么"，不用于"取代现场核对"）。

---

## 5. 复算命令（一键核对本清单）

```powershell
$src = "C:\Users\ROG\Desktop\插件\ShadowHunterRoles\src\main\java\com\shadowHunterRolesPlugin"
# ① 待迁移组件清单（未 markMigrated 的旧基类）
Get-ChildItem "$src\roleComponent" -Recurse -Filter *.java | ForEach-Object {
  $c=[System.IO.File]::ReadAllText($_.FullName)
  if($c -match 'extends\s+(Skill|PassiveSkill|MainWeapon)\b'){
    "{0} | lines={1} | {2}" -f $_.Name, ([System.IO.File]::ReadAllLines($_.FullName)).Count, (Get-FileHash $_.FullName -Algorithm SHA256).Hash } }
# ② 标识符出现次数（按出现次数，不按行）
Select-String -Path "$src\roleComponent\**\*.java" -Pattern 'onRightClick|onLeftClick|onDrop' | Measure-Object
# ③ T 块 7 项基线
foreach($p in @('isMigrated','markMigrated','USE_COMPONENT_PIPELINE','runComponentPipeline','bind\(','rolesContext\(\)')){
  "$p = " + @(Get-ChildItem $src -Recurse -Filter *.java | Select-String -Pattern $p).Count }
```

---

## 6. 交付边界（本任务只读，未越界）

- **只写**：`docs/阶段4-迁移清单.md`（本文件）。
- **未改**：`src/**`（逐字节未动）、未构建、未起服、未触碰 `build/`、未占用 25566、未改工装、未做 git 写、未申请提权。

---

## 7. **`MainWeapon`（B0b-3）执行节 —— 最高优先，供 eng-止血 直接照表执行**（勘察时点 2026-09-17 17:31:40）

> 这一节按 captain 的优先序先落盘；其余 8 个组件的执行表见 §0–§2，T 块基线见 §3。

### 7.1 勘察事实（只读）

| 项 | 值 |
|---|---|
| 文件 | `core/MainWeapon.java`（`core/` 下，**不是** `roleComponent/**`） |
| 行数 / 字节 | **147 行 / 5 936 B** |
| mtime / SHA256（勘察时点） | `2026-09-17 17:30:08` / **`39925F8289D0E1D146BD2A4D9389277C3935FC23D89A5C096EEFF7987ABC9CF4`** |
| 当前形态 | **改基已完成**：`public abstract class MainWeapon extends ActiveComponent implements CombatHook`（`:24`），构造器 `:33-35` = `super(id, displayName, description, cooldown, 0, icon, ItemKind.MAIN_WEAPON)` —— **`energyCost` 恒传 `0`** ✓（附录 A：武器图标永不出现 `ENERGY LACK`） |
| 旧回调现状 | `onAttack(Player,Player,RoleInstance)` `:43`、`onLeftClick` `:45`、`onRightClick` `:47`、`onDrop` `:49` **仍在**（过渡期保留，T 块删） |
| 新契约 | `onAttack(AttackSignal)` `:39` 默认 `return CastResult.CAST`（**今天 listener 攻击后无条件启动武器冷却 ⇒ 默认 CAST 等价**） |
| 两个子类 `super(...)` | **无需改动**（实测参数顺序 `id, displayName, description, icon, cooldown` 未变）：`MeiqiheziJuejueMainWeapon.java:15-21`、`RedSanctifiedBladeMainWeapon.java:22-28` |

### 7.2 标识符出现次数（**按出现次数清点，非行数**；`core/MainWeapon.java`）

| 标识符 | 次数 | 说明 |
|---|---|---|
| `icon` | **3** | 构造参数 `:33` + `getIcon()` 调用 `:63` + getter 注释 `:145` |
| `displayName` | **6** | 构造参数 `:33` + `getDisplayName()` 调用 5 处（`:72/:76/:80/:108/:112/:115` 中的 5 次） |
| `description` | **2** | 构造参数 `:33` + `getDescription()` `:85` |
| `id` | **8** | 构造参数 `:33` + `getId()` 3 处（`:54/:89/:104`）+ 注释/`getWeaponId` 等（`MAIN_WEAPON_KEY` 相关） |
| `getId()` | 3 | `:54`（isMainWeaponReady）、`:89`（PDC 写入）、`:104`（isReady 判定） |
| `getIcon()` | 1 | `:63` |
| `getDisplayName()` | 6 | `:72/:76/:80/:108/:112/:115` |
| `getDescription()` | 1 | `:85` |
| `createIconItem` | 1 | `:52`（**迁移后删除**） |
| `onAttack` | 3 | `:39`（新契约）/`:43`（旧契约）/注释 |
| `onLeftClick` / `onRightClick` / `onDrop` | 2 / 2 / 2 | 各 = 声明 1 + 注释 1（T 块删） |
| `RoleInstance` | **7** | `:43/:45/:47/:49`（旧回调签名 4）+ `:52/:99`（渲染覆写 2）+ `:101`（提示串） |
| `isMainWeaponReady` | 2 | `:54`、`:104` |
| `getBuffManager` | 2 | `:55`、`:105` |

> **上一块 `Skill` 的教训**：按行清点会漏"同一行第二处出现"（`Skill.java` 因此首次构建失败）。上表按 **出现次数** 清点。

### 7.3 需要的 import（改后视角）

- **保留**：`Component`、`Material`、`NamespacedKey`、`KeyFactory`、`ItemStack`、`ItemMeta`、`PersistentDataType`（`Utils` 仍在用）、`Player`（旧回调 T 前保留）、`AttackSignal`、`CastResult`、`CombatHook`、`ItemKind`、`ActiveComponent`。
- **删除**（随 `createIconItem`/`getDisplayName(RoleInstance)` 一起删）：`NamedTextColor`、`TextDecoration`、`java.util.ArrayList`、`java.util.List`。
- **注意**：`RoleManager`（`:3`）与 `RoleInstance`（同包，无 import）在删掉渲染覆写后**仍被旧回调签名引用** ⇒ 只能在 T 块连同旧回调一起清。

### 7.4 `createIconItem` / `getDisplayName(RoleInstance)` 正文的**等价改法**（交框架渲染）

| 旧分支 | 旧实现 | 新形态（框架渲染器） |
|---|---|---|
| 冷却中 | `Material.STRUCTURE_VOID` + `GRAY` + `BOLD`，lore `"MainWeapon is on cooldown."` | `IconState.COOLDOWN` → 同材质/颜色/lore 由 `HotbarRenderer` 产出 |
| 被禁用 | `Material.BARRIER` + `RED` + `" DISABLED"` 后缀，lore `"MainWeapon has been disabled."` | `IconState.DISABLED` → 同上（**后缀字符串逐字保留**） |
| 就绪 | `Material = getIcon()` + `GREEN`，lore `"MainWeapon is ready."` | `IconState.READY` |
| 公共 lore | `"===================="` + `getDescription()` | 同上（顺序不变） |
| PDC | `Utils.MAIN_WEAPON_KEY` ← `getId()` | 同上（框架写入） |
| `instance == null` | 返回 `null` / `Component.text("RoleInstance is Null!")` | **不需要**：框架侧不存在"实例为 null"的渲染调用 |

> ⚠️ **冻结差异（附录 A）**：**技能**冷却中的显示名带秒数（`X.Xs`），**主武器不带** —— 删掉本类覆写后，框架渲染器必须保持"主武器不带秒数"这一差异。这条是本批次**最重要的行为冻结点**。

### 7.5 隔离四不变量检查点

1. **不得出现 `RoleInstance`**：现 **7** 处（`:43/:45/:47/:49/:52/:99/:101`）—— 渲染两处随本批删除，旧回调四处随 **T 块**删除。
2. **组件间通信只用 `getComponent`**：本文件**不需要**（无跨组件读写）。
3. **不参与渲染**：删除 `createIconItem(RoleInstance)` 与 `getDisplayName(RoleInstance)`（**框架统一渲染**）。
4. **冷却声明值为唯一真值源**：`onAttack(AttackSignal)` 默认 `return CastResult.CAST`，冷却由框架按 `getCooldownTicks()` 启动；旧 `onAttack(Player,Player,RoleInstance)` 里的 `instance.startMainWeaponCooldown(...)` 在子类里统一删除（见 §2 B⑦）。

### 7.6 预期改动面（**已按 eng-止血 2026-09-17 的口径更正：拆成两行**）

| 批次 | 状态 | 改动面 |
|---|---|---|
| **B0b-3**（基类改基） | **已完成**（eng-止血，`core/MainWeapon.java` = 147 行 / 5 936 B / SHA256 `39925F82…9CF4`；**双方独立测算 SHA256 逐字相同** ✓） | **仅 `core/MainWeapon.java`**：`extends ActiveComponent implements CombatHook`、`super(..., cooldown, **0**, icon, ItemKind.MAIN_WEAPON)`、删五字段+五 getter、正文 12 处引用改走继承 getter（**按出现次数清点，一次编译通过**）。**未动任何子类、未加 `markMigrated()`、未删渲染覆写** |
| **B⑦**（武器子类迁移） | 待做 | **两个子类**：删 `instance.startMainWeaponCooldown(getId(), getCooldownTicks())`（`MeiqiheziJuejueMainWeapon.java:29,36`、`RedSanctifiedBladeMainWeapon.java:50`）+ 在各自构造器末尾调 `markMigrated()`；`onAttack(AttackSignal)` 返回 `CAST` |
| **B⑧ / T 块** | 待做 | 删除 `createIconItem(RoleInstance)` / `getDisplayName(RoleInstance)` 两处**渲染覆写**（框架统一渲染）；旧回调最终由 **T 块**删除 |

- **无共享文件改动**。
- ⚠️ **更正说明**：本节初版把"两个子类 + `markMigrated()` + 删 `startMainWeaponCooldown`"错误地写进了 **B0b-3** 的改动面；经 eng-止血 指出后按上表拆分 —— **t15 核 B0b-3 时只应看到 `core/MainWeapon.java` 一个文件的 diff**，不应去找子类 diff。

### 7.7 `markMigrated()` 落点（硬约束第 12 条）

- **必须写在两个子类各自的构造器里**（`MeiqiheziJuejueMainWeapon()` / `RedSanctifiedBladeMainWeapon()` 的 `super(...)` 之后一行），**不要写在 `MainWeapon` 基类构造器** —— 写在基类会把**尚未迁移的兄弟武器**一并标记，导致它们被送进新管道而 `onCast/onAttack(AttackSignal)` 是默认实现 ⇒ **旧回调永不触发 = 行为静默回归**。
- 标记后该组件的旧回调/渲染覆写即可在 T 块删除（T 在 B①…⑨ 全部完成后执行）。
---

## 8. T 块基线：**双边交叉核对**（我独立复算 vs eng-止血 已交的一份）

**我的复算时点**：2026-09-17 17:33:38（口径 = `Get-ChildItem src/main/java/com/shadowHunterRolesPlugin -Recurse -Filter *.java | Select-String -Pattern <p>` 的**行级命中数**，明细见下）。
**对照来源**：captain 转述的 eng-止血 数字（`isMigrated` 6 / `USE_COMPONENT_PIPELINE` 5 / `runComponentPipeline` 7 / 旧入口 26 / `bind` 1+2 / `rolesContext()` 10（`roleComponent` 内 5）/ 旧渲染 10）。

| # | 项 | **我（17:33:38）** | eng-止血 | 判定 |
|---|---|---|---|---|
| 1 | `isMigrated` | **4** | 6 | ❌ 差 2 —— 待对齐口径 |
| 2 | `markMigrated` | **3** | （未给） | — |
| 3 | `USE_COMPONENT_PIPELINE` | **5** | 5 | ✅ **两方一致** |
| 4 | `runComponentPipeline` | **7** | 7 | ✅ **两方一致** |
| 5 | 旧入口 `onRightClick\|onLeftClick\|onDrop` | **30** | 26 | ❌ 差 4 —— 待对齐口径 |
| 6 | `bind(` | **6** | 1+2（=3？） | ❌ 口径不同（"定义/调用"拆分 vs 全命中） |
| 7 | `rolesContext()` | **10**（`roleComponent/**` 内 **5**） | 10（内 5） | ✅ **两方一致** |
| 8 | 旧渲染 | `createIconItem\|getDisplayName(RoleInstance)` = **6** | 10 | ❌ 差 4 —— 我对"旧渲染"的口径可能更窄 |

### 8.1 我的逐项明细（文件:行号）—— 便于逐条定位差异

```
isMigrated(4):      core/MainWeapon.java:31, core/RoleInstance.java:356, core/Skill.java:28, roleComponent/ActiveComponent.java:45
markMigrated(3):    roleComponent/ActiveComponent.java:29,44,50
USE_COMPONENT_PIPELINE(5): core/RoleInstance.java:96,183,204,348,354
runComponentPipeline(7):   core/RoleInstance.java:302,317,332,353,397,409,421
旧入口(30):         core/MainWeapon.java:30,45,47,49 | core/RoleInstance.java:308,323,338,396,403,408,415,427 |
                    core/Skill.java:27,34,36,38 | listener/MainWeaponListener.java:59,63,88,92,112 |
                    roleComponent/ActiveComponent.java:28 |
                    roleComponent/meiqiHezi/mainWeapon/MeiqiheziJuejueMainWeapon.java:27,34 |
                    roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java:39 |
                    roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java:41 |
                    roleComponent/meiqiHezi/skill/MeiqiheziUnconcernSkill.java:28 |
                    roleComponent/red/RedDeeplySorrowSkill.java:27 | roleComponent/red/RedEvilShockSkill.java:25 |
                    roleComponent/red/RedSolitaryArroganceSkill.java:33
bind((6):           core/ports/ComponentServices.java:8, core/Role.java:88, core/RoleInstance.java:155, roleComponent/RoleComponent.java:10,38,46
rolesContext()(10): core/RoleInstance.java:140, core/TimerPortImpl.java:26,33,40, manager/BuffManager.java:136,
                    roleComponent/DefaultSanTEZeroPunishment.java:39, roleComponent/SkillUtil.java:25,
                    roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java:45,
                    roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java:57,
                    roleComponent/red/RedSolitaryArroganceSkill.java:35
旧渲染(6):          core/MainWeapon.java:52, core/RoleInstance.java:453,460, core/Skill.java:43,
                    roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java:100,
                    roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java:106
```

### 8.2 差异的**最可能原因**（待两方对齐口径，非"谁错"）

1. **计数规则不同**：我用 **`Select-String` 行级命中**；若 eng-止血 用**出现次数**（同一行多处算多次）或**含注释/javadoc**，则 `isMigrated` 类会偏高（`ActiveComponent`、`MainWeapon` 的注释里都提到过 `isMigrated()`）。
2. **时点漂移**：t14 正在并行改动（`core/MainWeapon.java` mtime `17:30:08`、`Skill.java` `00:54`）—— 我 **17:28** 的旧入口基线是 **29**，**17:33** 复算已是 **30**（`MainWeapon.java:30` 新增注释命中）。**旧入口 26/30、旧渲染 6/10 都可能同时含"口径差 + 时点差"。**
3. **`bind` 的拆分口径**：eng-止血 报"1+2"像是把"定义 1 处 + 调用 2 处"分开写；我的 `bind(` 全命中 6（含 `RoleComponent` 的 `/** ★容器专用 */` 注释 2 处 + 定义 1 处）。

### 8.3 建议（供 captain/eng-止血 选）

- **统一写死一个口径**：`命中 = Select-String 行级匹配数，含注释；同一行多处只算 1`（本项目其余证据都用这个），或反过来，但**只用一个**；`markMigrated` 等新增项一并纳入。
- **T 块开工前**再各跑一次（T 是最后一步，届时树已静止），不一致就按 §8.1 的 `文件:行号` 逐条对齐 —— 这正是双边举证的用法。
---

## 9. R-1 / R-2 裁定落地 + §3 的"扫描范围/模式"列（captain 2026-09-17，硬约束第 15 条，commit `de3f813`）

### 9.1 R-1 药水端口 → **裁定：方案① —— 在既有 `BuffPort` 内新增一个方法**（白名单成员数**仍为 10**，不新增 `PotionPort`）

**对 §1 药水行与 §4 R-1 的覆盖（以本节为准）**：
1. **必须走与 `RoleInstance.applyPotionEffect` 完全相同的"已记账"路径** —— 阶段 1 的药水记账用于在角色清理时回收效果；**绕过记账 = 药水泄漏 = 回归**。⇒ 新方法在端口实现里**只能调用** `RoleInstance.applyPotionEffect(...)`（`RoleInstance` 的账本 `appliedPotionTypes` 必须照旧登记），不得直接 `player.addPotionEffect(...)`。
2. **B②/B⑥/B⑧/B⑨ 的等价性表**必须逐条给出 **① 效果类型 ② 时长 ③ 增幅** 三者不变 + **记账条目一致** 的证据（即"迁移前后 `appliedPotionTypes` 的内容与数量一致"）。受影响 6 处（勘察时点）：
   `MeiqiheziCircleSlashSkill.java:49`（缓慢 20t amp 2）、`MeiqiheziUnconcernSkill.java:40`（速度 40t amp 4）、`RedDeeplySorrowSkill.java:45`（生命恢复 45t amp 5）、`:46`（力量 45t amp 2）、`RedBleedPassive.java:114`（抗性 100t amp 1）、`:199`（抗性 100t amp 1）。
   > 时长/增幅取值以**附录 A 冻结清单**为准（上表数值取自勘察时的源码；实施时以"内容片段"复核，行号会漂移）。
3. **该方法加入 `BuffPort` 的方法白名单**（**方法级新增、成员数不变**），并**同步设计文档 §5.1**（端口表）。

### 9.2 R-2 `SkillUtil` → **裁定：必须端口化，纳入本阶段范围**（批次记作 **B④.5**，作为 B⑤ 的前置子块独立收官）

- **验收（写死）**：`roleComponent/SkillUtil.java` 内 **`RoleInstance` 出现 = 0**、**`rolesContext()` 出现 = 0**。
- **语义冻结**：`factions().hasEnemyInRange(...)` 必须**逐字保持"未选角色也算敌人"**（即 `Faction.UNKNOWN` 一侧仍计入敌人；不得顺手改成"只有敌对阵营才算"）。
- **证据**：B④.5 单独出证据（`SkillUtil` 的迁移前/后 文件 SHA256 + 两处计数归零 + 语义对照），或并入 B⑤ 但在小结里单列一节。
- 现状（§3 基线）：`SkillUtil.java:25` 调 `rolesContext()`、`RoleInstance` 命中 3 处 —— **这 3 处就是 B④.5 要清零的对象。**

### 9.3 §3 各项的**"扫描范围 / 模式"**（补列；captain 要求"一行字即可"）

| # | 项 | 我 §3 的取值（时点） | **扫描范围** | **模式** | 与 eng-止血 差异的解释 |
|---|---|---|---|---|---|
| 1 | `isMigrated` | 3（17:28）→ **4**（17:33，`MainWeapon:31` 新增注释） | 整树 `com/shadowHunterRolesPlugin/**` | `Select-String` **行级**命中、**含注释** | 我**分列** `isMigrated` / `markMigrated`；captain 报的是**两者合并 7** |
| 2 | `markMigrated` | **3**（全在 `ActiveComponent.java:29,44,50`） | 整树 | 行级、含注释 | 同上（合并口径 = 4+3 = 7） |
| 3 | `USE_COMPONENT_PIPELINE` | **5** | 整树 | 行级、含注释 | ✅ 两方一致 |
| 4 | `runComponentPipeline` | **7** | 整树 | 行级、含注释 | ✅ 两方一致 |
| 5 | 旧入口 `onRightClick\|onLeftClick\|onDrop` | **29**（17:28）→ **30**（17:33）**整树**；§0 的**组件子口径** = `onRightClick`6 / `onLeftClick`2 / `onDrop`0 | ⚠️ **两个范围混用过**：§3 = 整树；§0 = 14 组件 | 行级、含注释 | captain 扫 `roleComponent/**` 得 7/3/1，我 §0 限定 14 组件得 6/2/0 —— **差数正是 `Skill.java` 的声明（`Skill.java:27,34,36,38`）**；**注：B⑧ 之后这些声明与 `Skill.java` 的旧入口一并退出（见 §15.1）** |
| 6 | `bind(` | **6** | 整树 | 行级、**裸 `bind(`**（会命中 `RoleComponent` 注释 2 处） | captain 用 **`.bind(`** 得 3 —— 少了注释里的裸写 |
| 7 | `rolesContext()` | **10**（整树）/ **5**（`roleComponent/**` 内） | 两个范围都给 | 行级、含注释 | ✅ 两方一致（两个范围都一致） |
| 8 | `applyPotionEffect` | **6 处 / 4 文件**（§9.1 已列） | 整树 | 行级、含注释 | ✅ 与 captain 一致 |

> **T 块验收口径（写死）**：**"按**声明范围**grep 归零"** —— 每一项在验收时必须**同时写出范围**（整树 / `roleComponent/**` / 14 组件）与**模式**（行级 or 出现次数、是否含注释）。范围不写明的"归零"结论**不予采信**（本清单 §3 先前混用过 §0/§3 两个范围，本节即为纠正）。
> **本次不重算数值**（captain 明确"一次即可"）；T 块开工前在**树静止**时按上表范围各跑一次，用 §8.1 的文件:行号逐条对齐。

### 9.4 批次安排更新（含 B④.5）

```
B0b-3  MainWeapon 基类+2 武器子类（§7 已交付，eng-止血 可照表执行）
B①  RedEvilShockSkill
B②  MeiqiheziUnconcernSkill / MeiqiheziCircleSlashSkill  ← R-1 端口（记账路径）【b-1+b-2 已交付并验收】
B②-c 生命周期钩子广播（core/RoleInstance.java + CircleSlash 去 LifecycleAware）  ← 2 文件【✅ 已完成并验收 a69cdd8】
B③  RedSolitaryArroganceSkill  ← 【✅ 已完成（`4C86B56B…AF23`，1 文件）；同批完成 legacy→新钩子】
B④  MeiqiheziEquipmentsPassive / RedEquipmentsPassive  ← 【✅ 已完成（2 文件，同批 legacy→新钩子；`markMigrated();` 4 → **6**）】
B⑤  2 个 AutoRecover 被动 + `SkillUtil` + `FactionPortImpl` + `RoleInstance`（tick 广播） = **5 文件**  ← **【✅ 已完成（`FA879F9F…`+4 文件；8/14 组件）】**
B⑥  `RedBleedPassive`（范围缩小 + 裁定 (A)）  ← **【✅ 已完成（t33，1 文件 `E008737D…BEB1`；构建 140 800 B @18:56:17 + Mode B 三行 + `C=0` + 停服；`UpdateAware` 2→1、`LifecycleAware` 3→3）】**
B⑦  两个主武器子类（`RedSanctifiedBladeMainWeapon` / `MeiqiheziJuejueMainWeapon`）改 `onAttack(AttackSignal)` + **删除面（4 项，同批达成）**：① **过渡桥**（`*_CONTEXT_KEY` ×2 + `RedBleedPassive:94/:95` 两处 `setContext`）② **`RedBleedPassive` 的 legacy `LifecycleAware` `start/stop(Player,RoleInstance)`**（⇒ **`LifecycleAware` 3 → 2**）③ **context 设施**（`RoleInstance` 4 方法 `:251/:255/:261/:265` + 字段 `context:72` + `context.clear():883`）④ **武器侧** `RedSanctifiedBladeMainWeapon:35` 的 `getContext` ⇒ 改 `getComponent(RedBleedPassive.class).stacksOf(...)` ⇒ **全仓 context 命中 = 0 且分项各自为 0**
B④  MeiqiheziEquipmentsPassive / RedEquipmentsPassive
B④.5 SkillUtil 端口化（R-2）：**已裁定与 B⑤ 合块**（硬约束 §19）
B⑤  AutoRecoverEnergyPassive / AutoRecoverSanTEHealthPassive
B⑥  RedBleedPassive（账本私有化 + 公开方法；R-1 端口）        ← B⑦ 前置
B⑦  RedSanctifiedBladeMainWeapon / MeiqiheziJuejueMainWeapon  ← **【✅ 已完成（t34 = B⑦ 首交付 4 文件；`c1bc752` 入库）】**
B⑦-a  **攻击路径接线 + 删两个委托壳（t35 = B⑦-b 本名）**  ← **【✅ 已完成并入库 `b86c2e8`；`handleAttack` **调用点 = 1（不含定义）/ 命中 = 2（含定义，`RoleInstance:196` + `MainWeaponListener:51`）**、两武器 `RoleInstance` 严格 0；证据见小结 §5.24 + 日志 `docs/阶段4-起服日志-B7b.log`】**
B⑧-a **（t37，eng-止血；裁定 (a) 拆分）**：`core/Skill.java` 补 `ENERGY LACK` 基类分支（**禁用之后、就绪之前**；`STRUCTURE_VOID` + `" ENERGY LACK"`（含前导空格）+ 不加 lore）+ **删 `MeiqiheziCircleSlashSkill` 两份覆写**；**不动 `HotbarRenderer`**（留 4.4/T）
B⑧-b **（t38，eng-会话；依赖 `[t35, t37]`）**：`MeiqiheziBloodySlashSkill` 的 **`onCast` 迁移** + **`markMigrated();` 6 → 7** + 删两份渲染覆写 ⇒ **`roleComponent/**` 的 `rolesContext()` 2 → 1（最后一处）**
B⑨  **裁定 (i)**：`DefaultSanTEZeroPunishment` 采用"真变化才派发" + **已申报可见变化**（五要素见 §15）；闭合 **O-6**（T 块不再保留）
T   收尾块：7 项删除（B①…⑨ 全部完成后）
```
---

## 10. 追加：R-2 的**顺序缺口**（eng-止血 实测，2026-09-17；必须写进批次安排）

**事实**：`roleComponent/SkillUtil.java` 的 `hasEnemyInRange(...)` 仍有 **3 个调用点**：
- `core/FactionPortImpl.java:32`（端口实现侧）
- `roleComponent/AutoRecoverEnergyPassive.java`（批次 **B⑤**）
- `roleComponent/AutoRecoverSanTEHealthPassive.java`（批次 **B⑤**）

**结论**：**B④.5（`SkillUtil` 端口化）不能独立排在 B⑤ 之前** —— 若先把 `SkillUtil` 改成端口形态，而未迁移的两个 AutoRecover 被动仍以旧的 `RoleInstance` 签名调用它，**编译不过**。

**处置 → 已裁定（2026-09-17，硬约束 §19）：取①——`B④.5` 并入 `B⑤`**（同一批次内先改 `SkillUtil` 再改两个被动，一次编译一次起服）。eng-止血 亦支持①。

**实测补充（eng-止血，2026-09-17）**：`core/FactionPortImpl.java:32` 那处调用实际形态是
```java
SkillUtil.hasEnemyInRange(owner, owner.getPlayer().getLocation(), radius)   // 把 RoleInstance 传下去
```
⇒ **合块时**它要改成"组件侧直接 `svc().factions().hasEnemyInRange(radius)`"，并由 `FactionPortImpl` / `SkillUtil` **同步去 `RoleInstance`**（这两个文件分别属 `core/` 与 `roleComponent/`，都在 t14 的 inScope 内 ✓）。

**验收不变**（§9.2 已写死）：`SkillUtil.java` 内 **`RoleInstance` = 0**、**`rolesContext()` = 0**；`factions().hasEnemyInRange` 语义**逐字保持"未选角色也算敌人"**。

> 本条替换 §9.4 批次表中"B④.5 作为 B⑤ **前置**"的表述 —— 前置会导致编译失败；**已裁定为与 B⑤ 合块**。

**验收补充（captain `a69cdd8`，2026-09-17）**
- **时序证据（严格递增 ⇒ `stop()` 广播早于兜底回收 ✓）**：`awake :662` → `start :691` → **`stop :725`** → **`cancelAllAndClear :853`**；
- **设计选择（captain 明确）**：**广播给全部注册组件**，**不按 `isMigrated()` 分支** —— 理由：未迁移组件对基类空实现无行为影响 + 避免引入第二套判据（与 §20"去掉全局开关"同类）；
- **判据切换结果（已生效）**：`CircleSlash` 的 `RoleInstance` 现算 = **类型引用 2**（仅 B⑧ 两份渲染覆写）+ **字面量 1**；§12 预写的「**仅 B⑧ 两份渲染覆写 + 字面量，其余 = 0**」**已成立** ⇒ §2 B⑧ 的两段式判据**切到第二段**；
- **回归计数（B②-c 未改这些）**：`markMigrated();` **3** · `USE_COMPONENT_PIPELINE` **0** · `rolesContext()` **9（全仓）/ 4（`roleComponent/**`）** · `getInstance()` **0** · jar **140 341 B @18:11:24** · Mode B 三行 + **C=0** · 已停服。

**新增可登记项：`LifecycleAware` 实现者（2026-09-17 18:16:25 实测）**
- **`implements LifecycleAware` 声明 = 7**：`core/RoleInstance.java:720` + 6 个**未迁移**组件（`DefaultSanTEZeroPunishment:18`、`MeiqiheziEquipmentsPassive:15`、`MeiqiheziBloodySlashSkill:20`、`RedBleedPassive:21`、`RedEquipmentsPassive:15`、`RedSolitaryArroganceSkill:19`）—— **`MeiqiheziCircleSlashSkill` 已退出**（`LifecycleAware` 仅剩 1 处 javadoc 提及）；
- **`LifecycleAware` 总命中 = 34**（`RoleInstance` 21 = 扇出实现与注释；`LifecycleAware.java` 1 = 接口自身；组件侧共 11 = 6 组件 ×2 − CircleSlash 剩 1）。

**范式说明（captain 2026-09-17，自 B③ 起适用）**
> **容器已广播新钩子 ⇒ 后续每批可"同批完成" legacy `*Aware` → 新钩子的转换**：把该组件的 `start/stop(Player, RoleInstance)`（或 `update(Player, RoleInstance)`）改为**无参 `awake()/start()/stop()/update()`** 覆写，**不必留到 T 块**。好处：① 每批自带"去 `RoleInstance`"的完整闭环；② `rolesContext()` 与 `RoleInstance` 引用数**逐批单调下降**（不再集中到 T 块）。
> ⇒ 后续批次的**变更面默认按"组件文件 + 必要端口/容器文件"计**，并在该批小结里同时给"legacy 转换完成"证据。

---

## 13. `B⑥` 预检块**首行警示** + `applyStacks`/`stacksOf` 契约（eng-止血 2026-09-17；captain 裁定 (i) 后落盘）

> ### ⚠️ **过渡桥与"去 `LifecycleAware`"不可兼得 —— 已裁 (A)**（captain 2026-09-17）
> 过渡桥两行（`instance.setContext(...)`，现 `RedBleedPassive:94/:95`）**需要 `RoleInstance`**；而"去掉 `implements LifecycleAware`"会让 `start/stop` 变无参 ⇒ **无 `instance`**。在**"不新增 `ComponentServices` 成员（白名单 10 不动）"**的前提下**两者不能同时成立**。
> **裁定 (A)（采纳 eng-止血 推荐；拒绝 (B) 扩白名单）**：**B⑥ 保留 `implements LifecycleAware`**（`start/stop(Player, RoleInstance)` 原样，**仅供发布过渡桥**），**只把 `update(Player, RoleInstance)` 转无参**并去 `UpdateAware` ⇒ **`UpdateAware` 2 → 1、`LifecycleAware` 3 → 3（本批不降）**；**B⑦ 删 context 设施时，桥与该 legacy 一起消失**（那时才 **3 → 2**）。**零新增契约、最小改动**。
> **判据（两段式）**：B⑥ = 「**除过渡桥所用的 legacy `start/stop(Player, RoleInstance)` 签名外 = 0**」（三分法 + 逐处残余清单）；**"严格 = 0"随 B⑦ 生效**。

> ### ⚠️ **context 设施（4 方法 + 2 键）不能在 B⑥ 删除**
> `roleComponent/red/RedSanctifiedBladeMainWeapon.java:35` 仍以 `getContext(RedBleedPassive.BLEED_RECORD_CONTEXT_KEY, Map.class)` 读取（**该使用者在批次 B⑦**）；B⑥ 若删 `RoleInstance` 的 4 个 context 方法与两个 `*_CONTEXT_KEY`，**这一行立即"找不到符号" ⇒ `BUILD FAILED`**。**删除并入 B⑦**（或与 B⑦ 合块）—— 裁定 **(i)** 已由 captain 拍定：**B⑥ 只做组件侧**。

**B⑥ 保留的过渡桥**：`RedBleedPassive:68/:69` 两处 `setContext` + 两个 `*_CONTEXT_KEY`（保证"新旧写同一份账本"）；**B⑦ 才把设施清零**（两批各记一次，见 §2.0 第 ⑦ 条）。

**签名契约已确认冻结（captain 2026-09-17）**：`void applyStacks(UUID, int)`（累加后 `Math.clamp(cur + stacks, 0, MAX_BLEED_STACK = 15)`）｜`int stacksOf(UUID)`（只读、无副作用）｜`void requestResolve(UUID, int)`（B① 前移项，保留）。
> **参数名一致性约束**：captain 用 `stacks`、eng-止血 用 `amount` —— **二者取其一，但 B⑥/B⑦ 两批必须一致**；⇒ **以 eng-止血 B⑥ 落地后的实际签名为准，并在本节补记实际参数名**（避免两批签名漂移）。
> **环境边界（指南第 21 条，commit `2d2b092`）**：`spawn EPERM`（沙箱禁止以管道捕获子进程输出）= **环境边界、非代码失败**；**不换写法重试**；受影响批次的**起服证据记"待补"**，**不得据以宣称"已起服验证"**（与 `jcmd`/JVM attach 不可用同类）。本清单的记录纪律同此：**取不到哈希/证据时如实标"待补"，不补造。**

**签名契约（B⑦ 的输入，建议在本批冻结）**
```java
void applyStacks(UUID victimId, int amount);    // 累加并 clamp 到 0..MAX_BLEED_STACK  ← 实际参数名 = amount（B⑥ 落地后现算）
int  stacksOf(UUID victimId);                   // 只读
void requestResolve(UUID victimId, int stacks); // B① 前移项，保留
```
> **参数名已定**（captain 的"以 B⑥ 落地后的实际签名为准"）：**`amount`**（实测 `RedBleedPassive:72` = `public void applyStacks(UUID victimId, int amount)`）⇒ **B⑦ 必须用 `amount`**（原草案里的 `stacks` 作废）。
> **B⑥ 子步 1 已落地（纯新增，零调用点）**：`RedBleedPassive.java` = **243 行 / 10 341 B / mtime 18:42:31 / `6406EDE49C6FC92A964E32838DAB33AD7B8E4D55A01A2AD34E84504074228A48`**（我 18:48 实测与 eng-止血 一致）；`applyStacks:72` / `stacksOf:84` / `requestResolve:58` 在位；jar **140 690 B @18:42:53**；**新方法外部调用点 = 0**（仅本文件内引用）。
> **起服证据：✅ 已重做并通过（18:49 干净停启，`C = 0`；我的独立复核 18:52 逐条通过）**
> - 日志 `docs/阶段4-起服日志-B6-step1.log` = **5 699 B / mtime 18:49:22 / `4D2784A364C1048E8297BE60BCA16207506CD5851320288FF58ED8B29F402B4F`**（**与 `.scratch/bot/baseline-runserver-B6step1-2026-09-17-1849.log` 逐字节相同** ⇒ 双份留存一致 ✓；不再是上次的 2 913 B / 26 行截断件）
> - **三行齐**：`:43 Enabling ShadowHunterRolesPlugin v1.0.0`｜`:44 ShadowHunter Character System enabled.`｜`:48 Done (13.886s)!`（**SHDF 依赖行 0 命中 = Mode B 常态**）
> - **正序集合差（我自己跑，对 t6 锚点）**：`ADDED 0 / REMOVED 0 / A) 0 / B) 0 / C) OTHER 0` ⇒ **上一轮的伪差（`ADDED 1 / REMOVED 10 / C 1`）已消除**
> ⇒ **该子步档位 = 纯新增（零调用点）+ 构建通过 + Mode B 三行 + `C = 0` + 已停服** ✓
> **根因反证**：伪差 = **强杀 java 截断日志** —— 下面的工具纪律条目由此**被实证**。
> **工具纪律（eng-止血 环境经验，2026-09-17）**：**不要强杀 java** —— 只用 `job_kill` 并**等待其自然退出**，再复制 `latest.log`；否则日志被截断会产生**伪差**（本次实测：`ADDED 1 / REMOVED 10 / C=1` 全是假信号）。与第 21 条同类：**证据不可信时标"待补"，不补造。**

**B⑦ 的删除判据（分项计数，eng-止血 采纳"列全"的做法）**
```
RoleInstance 侧 ：setContext:251 / getContext:255 / removeContext:261 / hasContext:265（4 方法）
                  + context 字段:72 + context.clear():883
RedBleedPassive 侧：BLEED_RECORD_CONTEXT_KEY:24 / BLEED_RESOLVE_REQUESTS_KEY:26（2 键）
                  + :68/:69 两处 setContext（过渡桥，本批保留、B⑦ 删）
武器侧           ：RedSanctifiedBladeMainWeapon:35 的 getContext
                  ⇒ 改调 getComponent(RedBleedPassive.class).stacksOf(...)
⇒ B⑦ 达成时：全仓 context 设施命中 = 0（分项各自为 0，不允许"总数对了但漏项"）
```
- **语义建议（eng-止血；captain 已确认 clamp）**：`applyStacks` 累加后 `clamp(0, MAX_BLEED_STACK)` —— 与现有武器侧写法同源（`RedSanctifiedBladeMainWeapon:39` = `Math.clamp(curVictimBleed + BLEED_STACKS_PER_HIT, 0, RedBleedPassive.MAX_BLEED_STACK)`）；`stacksOf` 只读。
- **冻结值（现算，声明处逐字）**：`MAX_BLEED_STACK = 15`（`RedBleedPassive:29`）｜`BLEED_DAMAGE_PER_SECOND = 2d`（`:32`）｜`BLEED_SANTE_RECOVER = 4`（`:35`）｜`BLEED_RESISTANCE_DURATION_TICKS = 100`（`:38`）｜`BLEED_SETTLE_INTERVAL_TICKS = 20`（`:41`）｜武器侧 `BLEED_STACKS_PER_HIT = 15`（`RedSanctifiedBladeMainWeapon:19`）。
- **两处 `RESISTANCE 100 / amp 1` 的已记账路径**（`RedBleedPassive:114/:199` 一带）必须保持经 `RoleInstance.applyPotionEffect` ⇒ B⑥/B⑦ 均不得绕开记账（§9.1 硬要求）；eng-止血 已写法 = `svc().buffs().applyPotionEffect(RESISTANCE, 100, 1)`（**3 参**）。
---

## 14. `B⑥` 其余（t33）**完成记录**（eng-会话 2026-09-17 18:55–18:57；captain 交班本卡）

**变更面：1 文件**（冻结范围）
| 文件 | 行/字节/mtime | SHA256（回滚单元） |
|---|---|---|
| `roleComponent/red/RedBleedPassive.java` | **265 行 / 11 808 B / 18:55:43** | **`E008737DFF1F21AC1ACEA3C4FA8E95B9AC26BC6D16A8A6E9B2A2183C26D7BEB1`** |
（**迁移前 = 243 行 / 10 341 B / `6406EDE49C6FC92A964E32838DAB33AD7B8E4D55A01A2AD34E84504074228A48`** —— 我用 `git show HEAD` + LF 还原后**逐字节复现**该身份 ✓ 作为 before 基线。）

**做了什么（按冻结范围逐条）**
1. 类声明：**去 `UpdateAware`**（保留 `LifecycleAware`）+ 删 import；**顺带删掉**已无用的 `DamageUtil` import（两处调用已端口化）。
2. **`update(Player, RoleInstance)` → 无参 `update()`**（首行 `Player player = svc().self().player();`）；`start/stop(Player, RoleInstance)` **原样保留**（过渡桥两行 = 现算 `:113/:114`）。
3. **端口化 6 处调用点**（数值逐字不变）：`svc().damage().trueDamage(victim, player, BLEED_DAMAGE_PER_SECOND)`（`:173`）/ `svc().buffs().applyPotionEffect(RESISTANCE, BLEED_RESISTANCE_DURATION_TICKS, 1)`（`:175`，**3 参、走已记账路径**）/ `svc().sante().gain(BLEED_SANTE_RECOVER)`（`:176`）；结算支路同三处（`:257/:260/:261`）。
4. 两个私有结算方法**去 `RoleInstance`**：`resolveRequestedBleed(Player caster)` / `resolveBleed(Player victim, Player caster, int amount)`，并同步调用点（`update()` 内 → `resolveRequestedBleed(player)`；其内 → `resolveBleed(victim, caster, amount)`）；**等价说明**：`casterInstance` ≡ `svc()`（组件与实例一对一，`bind` 注入）—— 已写进类 javadoc；原 `casterInstance == null` 守卫**由 `svc()` 未绑定即抛异常**取代（更强，非静默 null）。
5. 账本私有化：两个 `Map` 保持 `private final` ✓；**过渡桥与私有账本 = 同一份数据、不另建并行存储**（`start()` 发布的就是这两个 Map 本体）—— 已写入 javadoc。
6. **三入口签名未动**：`requestResolve(UUID,int)` / `applyStacks(UUID,int amount)` / `stacksOf(UUID)` ✓

**判据（两段式第一段，达标）**：三分法现算（本文件）= **签名类型引用 2**（`:112 start(...)`、`:118 stop(...)`）/ **字面量 0** / **javadoc 7** ⇒ 「**除过渡桥所用的 legacy `start/stop(Player, RoleInstance)` 签名外 = 0**」✓

**双口径（**路径对齐的单文件 before/after**，工具 `numeric-multiset.ps1 -Mode both`）**：**数值 42 → 42（diff rows 0）**；**字符串 5 → 5（diff rows 0）** ⇒ **本批零字面量变化 ⇒ 无需 (d) 申报**（比"逐条归属"更强的结论）。

**计数（仅代码口径，剥注释后现算）**：`implements UpdateAware` 组件 **2 → 1** ✓（剩 `RedDeeplySorrowSkill`）｜`implements LifecycleAware` 组件 **3 → 3**（`DefaultSanTEZeroPunishment` / `MeiqiheziBloodySlashSkill` / `RedBleedPassive`）✓ 与 (A) 一致｜`markMigrated();` **4**｜`rolesContext()` 判据 `roleComponent/**` **2**｜`USE_COMPONENT_PIPELINE` **0**｜`getInstance()` **0**。
> ⚠️ **口径提醒**：**行级（含注释）计数会因本批新增 javadoc 而虚高** —— 例如 `implements[^;{]*LifecycleAware` 行级 roleComponent = 4（多的是类 javadoc 里那句"保留 implements LifecycleAware"），`.setContext(` 行级 3、`.getContext(` 行级 2 同因。⇒ **引用时请用"仅代码口径"**（与 §2.0 第 ④ 条一致）。

**context 分项表（本批不删，移交 B⑦）**：`RoleInstance` 侧 4 方法（`setContext:251`/`getContext:255`/`removeContext:261`/`hasContext:265`）+ 字段 `context:72` + `context.clear():883`｜`RedBleedPassive` 侧 2 键（`:24/:26`）+ **2 处 `setContext`（`:113/:114`）**｜武器侧 `RedSanctifiedBladeMainWeapon:35` 的 `getContext` ⇒ **B⑦ 达成时全仓 = 0 且分项各自为 0**。

**证据（构建 / 起服 / 停服）**
- 构建：`BUILD SUCCESSFUL in 29s`（`-x copyPluginJar`，`GRADLE_USER_HOME=.gradle-work`）；jar **140 800 B / 18:56:17 / `B3B3A1B906B5F9DFB0493A48B54F64428D252ADA76F6F217165ABCCD248FA2AB`**；最新 `.class` @18:56:17；**`src 新于 jar = 0`** ✓
- 起服（**Mode B，独占**）：三行齐 —— `:45 Enabling ShadowHunterRolesPlugin v1.0.0`｜`:46 ShadowHunter Character System enabled.`｜`:50 Done (16.389s)!`（18:57:03–04）；**SHDF 依赖行 0 命中**（Mode B 常态）
- 正序集合差（对 t6 锚点，**只看 `C`**）：`ADDED 0 / REMOVED 0 / A) 0 / B) 0 / **C) OTHER 0**` ✓
- 日志冻存：`docs/阶段4-起服日志-B6.log` = **6 076 B / `F0E71B8C7D376E2EFB2D3ADC715A4DFC373EB1B0B26C882F553B249739E10F98`**（与工装侧副本**逐字节相同**）；**日志在停服前已冻结并完成 delta** ⇒ 不受停服动作影响
- **停服**：25566 监听 = **0** ✓；运行深度声明：**本批只到"启用 + 空闲"，无玩家选角色**（组件侧行为由端口等价性 + 结构判据覆盖）
---

## 15. **B⑧ / B⑨ 裁定登记**（captain 2026-09-17；时序：**B⑦/t34 入库后**再逐批授权）

### 15.1 B⑧ = **裁定 (a)**（3 文件）
| # | 文件 | 改动 |
|---|---|---|
| 1 | `core/Skill.java` | 补 **`ENERGY LACK`** 基类分支：**位于"禁用之后、就绪之前"**；表现 = `Material.STRUCTURE_VOID` + 显示名追加 **`" ENERGY LACK"`（含前导空格）** + **不加 lore** |
| 2 | `roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java` | **`onCast` 迁移 + `markMigrated()`** + 删两份渲染覆写 |
| 3 | `roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java` | 删两份渲染覆写（`createIconItem` / `getDisplayName(RoleInstance)`） |

- **前置已核实（captain 跨行正则独立提取，与我勘察一致）**：全仓**唯二非 0 能量消耗** = `MeiqiheziBloodySlashSkill`（冷却 **160** / 能量 **8**）与 `MeiqiheziCircleSlashSkill`（**200** / **15**）；**其余 5 个技能全为 0** ⇒ 补基类分支后：对这两者**逐字不变**、对其余**永不触发** ⇒ **零新增可见状态** ✓。
- **不动 `HotbarRenderer`**（统一渲染器骨架留待 4.4/T）✓。
- **同批约束**：`BloodySlash` 的 `onCast` 迁移必须与两份覆写删除**同批**（避免半迁移态）✓；**B⑧ 之后 `roleComponent/**` 的 `rolesContext()` = 0** ✓（`BloodySlash:45` 是最后一处）。

### 15.2 B⑨ = **裁定 (i)**（`DefaultSanTEZeroPunishment`，"真变化才派发" + **已申报可见变化**）
**五要素（captain 定，照此登记）**
1. **触发条件与唯一实例** = `RedDeeplySorrowSkill:43` 的 `decreaseSanTE(10)`（**SanTE 已 0 时**）；
2. **旧行为** = **重置**惩罚（粒子/标题/眩晕 `100t` 重新开始）；**新行为** = **不重置、不延长**；
3. **可见性** = 可见，但**仅在该边界**（SanTE 已 0 时再扣）；
4. **O-6 的重复任务路径由本批闭合** ⇒ **T 块的 O-6 项标注为"已由 B⑨ 闭合"，不再留到 T**；
5. **`SanTEChangeEvent` 对外发布保持无条件**（**只收紧组件侧钩子**）⇒ **不是 API 变更** ✓。

**映射要点（供实施与核对）**：去 `SanTEChangeAware` + `LifecycleAware`（组件 `LifecycleAware` **2 → 1**）｜`onSanTEChange(int pre, int now)`｜`:39` → `svc().timers().runRepeating(0L, 40L, …)`（**周期 40 tick = 2 秒、`count` 上限 3 ⇒ 惩罚总时长 6 秒；逐字取自源码**） ⇒ **`roleComponent/**` 的 `rolesContext()` 最后一次下降**｜`setIsInSanTEPunishmentState` → **组件私有字段**（并**删 `RoleInstance` 的该方法**）｜`addBuff(STUN,100)` → `svc().buffs().add(...)`（**既有方法，白名单不变**）｜真伤 → `svc().damage().trueDamage(player, null, …)`（**source 可空** ✓）。

### 15.3 时序与依赖
- **B⑧ / B⑨ 都等 B⑦（t34，5 文件）入库后**再逐批授权（captain 已发 `c98931c6` 裁定 (A) 授权；t34 的接线补做 = **B⑦-a**，见 `docs/阶段4-交付小结.md` §5.24）；
- **B⑧ 依赖 B⑦**：`BloodySlash` 的 `onCast` 与两份覆写删除必须同批，且它是 `rolesContext()` 在 `roleComponent/**` 的最后一处（B⑧ 后为 0）；
- **B⑨ 依赖 B⑧**（同属"最后一批组件迁移"），完成后 **`LifecycleAware` 组件 = 1**、`rolesContext()` 判据 = **0**。
> **T 块第 ③ 项（O-6 重复任务路径）的登记更新（captain 2026-09-17）**：**已由 B⑨ 闭合**（B⑨ 的"真变化才派发"消除了重复任务路径）⇒ **T 块不再保留该项**（§15.2 要素 4）。其余 T 项不变（1 `isMigrated` / 2 `markMigrated` / 3 `USE_COMPONENT_PIPELINE` / 4 `runComponentPipeline` / 5 旧入口 / 6 `bind(` / 7 `rolesContext()`）。
---

## 16. **B⑦ 完成记录（t34 + B⑦-a）**（2026-09-17 19:03–19:16，eng-会话）

**进度：11/14 组件已迁移**（主动 6 + 被动 4 + 主武器 2 —— 含 B⑦ 的两把武器）。**回滚单元 = 5 文件**：
| 文件 | 最终 SHA256（B⑦-a 后） |
|---|---|
| `listener/MainWeaponListener.java` | `1EF4741538B0A5C7FB1ED1E58D8DCFAB025538F1787F5D774A493BE86249E207` |
| `roleComponent/red/RedSanctifiedBladeMainWeapon.java` | `C4ABA308602424D070AD573E197C0CD1B5E1229345B7F6E77D2273D897E522B1` |
| `roleComponent/meiqiHezi/mainWeapon/MeiqiheziJuejueMainWeapon.java` | `63B59D6318E8E73AB08473A72E957915F0DB913E6EC5EB3173E126D9E240C909` |
| `roleComponent/red/RedBleedPassive.java`（**跨卡二次改动**） | `53A22892A540FE22E5851E064BC0FABF1D36A359186D64DE8A9FAA3055546F5F`（before = `E008737D…BEB1`，t33） |
| `core/RoleInstance.java` | `936E0D0827A9529C8437CD4A2BBE330AE8DE6DA195F79B3A4E3B80856C77E795` |

**本批达成的判据**：**context 设施全仓 = 0 且分项各自为 0**（仅代码口径 + 原始 `git grep` 亦 0）｜组件 **`LifecycleAware` 3 → 2** ｜`UpdateAware` = 1｜`markMigrated();` = **6**（4 → 6，两武器各自调用）｜`rolesContext()` 判据 `roleComponent/**` = **2**｜`getInstance()` = 0｜`USE_COMPONENT_PIPELINE` = 0｜**`handleAttack` 调用点 = 1（不含定义）/ 命中 = 2（含定义；此前两口径均为 0）**｜两武器 `return CastResult.CAST` 且 `startMainWeaponCooldown` 已删｜数值 `8/1/5/15` 与**可见数值清单**（`Juejue:77` 粒子圆半径 5/粒子 50、`:79` 判定圈半径 5、能量档位 20/5、单体 8(+0.5)/14）逐字不变。
**两处已申报偏差**：① **过渡委托壳**（B⑦ 首交付保留）**已由 B⑦-a 接线后删除** ⇒ 闭环；② **`markMigrated();` 4 → 6**（卡面"4 不变"与括号要求自相矛盾，按括号执行）。
**证据索引**：`docs/阶段4-交付小结.md` **§5.23**（首交付）+ **§5.24**（接线补做，含"近战命中能量≥20 范围伤害"等价性缺口的自查与修复）；起服日志 `docs/阶段4-起服日志-B7.log`。
---

## 17. **B⑧ 拆分（t36 → t37 / t38）与顺序**（captain 2026-09-17，采纳 eng-止血 分析）

| 块 | 卡 | 执行者 | 范围 | 关键判据 |
|---|---|---|---|---|
| **B⑧-a** | `t37` | **eng-会话（改派后实际执行者；卡面原写的 eng-止血 因 t14 占用执行位无法 claim）** | `core/Skill.java`（补 `ENERGY LACK`）+ 删 `MeiqiheziCircleSlashSkill` 两份覆写 | `Skill.java` 的 `ENERGY LACK` **必须先落地**（否则删覆写会丢该可见状态）；`HotbarRenderer` 不动 |
| **B⑧-b** | `t38` | eng-会话（依赖 `[t35, t37]`；**已完成**） | `MeiqiheziBloodySlashSkill` 的 **`onCast` 迁移** + **`markMigrated();` 6 → 7** + 删两份渲染覆写 | **`roleComponent/**` 的 `rolesContext()` 2 → 1**（最后一处 = `BloodySlash:45`） |

**顺序理由**：两块都"删覆写" ⇒ **`Skill.java` 的 `ENERGY LACK` 必须先有**（否则删 `CircleSlash`/`BloodySlash` 的覆写会丢掉 `ENERGY LACK` 这个可见状态）⇒ **t37 先行、t38 随后**；`t38` 的依赖写成 `[t35, t37]`，两者完成前不可 claim ✓。
**起服礼节（指南第 22 条）**：起服独占 —— **起服前先在消息里报一句"我要起服"**，另一方等待；停服后立刻回"已停服" ✓（t35 已按此执行：起服前已广播、结束后已报"已停服"，25566 监听 = 0）。
---

## 18. **B⑧ 完成记录（t37 = B⑧-a / t38 = B⑧-b）**（2026-09-17 19:22–19:35，eng-会话）

**进度：12/14 组件已迁移**（剩 **B⑨ `DefaultSanTEZeroPunishment`** → T）。

| 块 | 卡 | 文件（回滚单元） | 行/字节 | SHA256 |
|---|---|---|---|---|
| **B⑧-a** | `t37` | `core/Skill.java` | 151 / 6 315 B | `A2E3A35753FAE431BE27D73BC63A1DFED57EB2EC7433A484AFA7F4370282D5CF` |
| **B⑧-a** | `t37` | `roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java` | **195 → 105** / 4 068 B | `4EAF53F8A739D0E1076A4410BDE106F488518BAB98BB5E99EB9D7E64D861B042` |
| **B⑧-b** | `t38` | `roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java` | **181 → 100** / 3 699 B | `4F24BB3141888220FBB7FE24E4E91AE9902CB3B9A0985C5D0248A3983D364B94` |
| 日志 | `t37` | `docs/阶段4-起服日志-B8a.log` | 6 077 B | `63F4ED2963451C3F93110001B76E541112047E10EA37B1FB6F80D428590C0095` |
| 日志 | `t38` | `docs/阶段4-起服日志-B8b.log` | 6 077 B | `4FFF78055E5ECE2B1F60B2BF5565AA9CCC57782400CED30DFFA570268764B760` |

**本批达成**：**`ENERGY LACK` 基类三处**（材质链 + `createIconItem` 名链 + `getDisplayName(RoleInstance)` 链：`STRUCTURE_VOID` / GRAY+BOLD / `" ENERGY LACK"`（前导空格）/ **不加 lore**；顺序 = 冷却 → 禁用 → 能量不足 → 就绪）｜**`markMigrated();` 6 → 7**（`git grep -c` 恰 7 文件）｜**`roleComponent/**` 的 `rolesContext()` 2 → 1**（唯一残留 `DefaultSanTEZeroPunishment:39`，属 **B⑨**）｜**`createIconItem(RoleInstance)` / `getDisplayName(RoleInstance)` 全仓 = 0**（`git grep -c` 无输出）｜组件 `LifecycleAware` 仍 **2**（B⑧-b **按卡面字面执行、未做 legacy 生命周期转换** ⇒ B⑨ 再 2 → 1）｜`getInstance()` 0 / `USE_COMPONENT_PIPELINE` 0。
**双口径（(d) 申报）**：B⑧-a = 数值 **21 → 21（diff 0）**、字符串 **32 → 19**（13 条全删除，逐条都是"与被删覆写重复、基类已有同字面量"）；B⑧-b = 数值 **24 → 24（diff 0）**、字符串 **18 → 3**（15 条全删除，同性质）⇒ **两块均零新增、渲染文本不变**。
**构建/起服**：均 `--rerun-tasks` 强刷 `BUILD SUCCESSFUL`（17s / 17s）；jar **138 883 B @19:25:28 `7375F10E…06AA`**（B⑧-a）、**137 446 B @19:33:47 `A67A117C…2F87`**（B⑧-b）；**两次均"起服前先广播"（指南第 22 条）**、独立日志名、正序集合差 **`ADDED 0 / REMOVED 0 / C 0`**、**已停服**（25566 监听 0）；运行深度声明 = "启用 + 空闲，无玩家选角色"。
**清理项（如实列出）**：B⑧-a 删 7 条未使用 import（`NamedTextColor`/`TextDecoration`/`ItemStack`/`ItemMeta`/`PersistentDataType`/`ArrayList`/`List`）；B⑧-b 删 6 条（`BukkitRunnable`/`NamedTextColor`/`TextDecoration`/`ItemStack`/`ItemMeta`/`PersistentDataType`）。
---

## 19. **t39（可读性改名 `CAST` → `SUCCEED`）状态：in_progress / ⛔ 卡面 inScope 不可满足**（2026-09-17 19:40–19:42，eng-会话）

**已完成的工作（可编译 + 证据齐全）**：改名 **23 处 / 12 文件**（`git diff --numstat` = **+23/−23**）⇒ **`\bCAST\b`（大小写敏感）全仓 = 0**（`git grep -nE "\bCAST\b" -- src` **无输出、退出码 1**；`git grep -lE` 命中文件数 = **0**）｜**`CastResult.SUCCEED` = 12 / 9 文件**（现算：`MainWeapon`1 / `RoleInstance`2 / `Juejue`3 / `BloodySlash`1 / `CircleSlash`1 / `Unconcern`1 / `RedEvilShock`1 / `RedSanctifiedBlade`1 / `RedSolitary`1；枚举常量定义在 `CastResult.java:10`）
> **两种口径分列（captain 2026-09-17 复核指正，已更正）**：
> - **改名「前」（`aa16eaa` 树）**：`\bCAST\b` = **23 处 / 12 文件** —— `MainWeapon`2 · `RoleInstance`2 · `CastResult`1 · `CooldownPort`1 · `MainWeaponListener`1 · `Juejue`5 · `BloodySlash`2 · `CircleSlash`2 · `Unconcern`2 · `RedEvilShock`2 · `RedSanctifiedBlade`1 · `RedSolitary`2；
> - **改名「后」（`32d1de1` 树 / 现树）**：`CastResult.SUCCEED` = **12 行 / 9 文件** —— `Unconcern` 与 `RedEvilShock` 各 **1**（其 `:34` / `:24` 的 javadoc 用 `{@link CastResult#SUCCEED}`（**`#` 形式**）⇒ **不进入**该计数）。
> ⚠️ 兼记度量陷阱：**`@(<命令> | Measure-Object).Count` 恒等于 1**（那是 1 个 `Measurement` 对象，不是命中文件数）—— 正确写法 = `git grep -n`（看有无输出/退出码）或 `git grep -l … | Measure-Object | % Count`。（本条为 eng-会话 自曝的一次镜像踩坑。）｜构建 `BUILD SUCCESSFUL in 17s`，jar **137 485 B / 19:40:27 / `BB377CFDA0862FD8290827A225AA173CC5B83DC440804C4453C66DFEF241BF1A`**，最新 `.class` @19:40:26，`src 新于 jar = 0`｜**双口径（12 文件）数值 150→150 / 字符串 48→48，diff 均 0**｜**Mode B 三行 + `ADDED 0 / REMOVED 0 / C 0`**；日志 `docs/阶段4-起服日志-B39.log` = **6 077 B / `4800D52C0B24EA11892995F2B0E81138A20E08324B32C87CBF83F1CE1A81CEDC`**；**已停服**（25566 = 0）｜不变项：`markMigrated();` **7** / `LifecycleAware` **2** / `USE_COMPONENT_PIPELINE` **0** / `getInstance()` **0**。

**⛔ 阻塞（第 9 次"卡面 inScope 缺口"）**：t39 inScope 列了 11 个 src 文件，**漏列 `roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java`** —— 其 `:85 return CastResult.CAST;` 在枚举改名后**不改则编译必然失败**，且卡面判据"全仓 `\bCAST\b` = 0"在本卡 inScope 内**不可达**。校验器原文：`implementation cannot complete: …/MeiqiheziBloodySlashSkill.java is undeclared`（强制 changedPaths ⊆ inScope）。
**待 captain 二选一**：**(A)（推荐）**把该文件加入 t39 inScope（本批仅 2 处：1 行代码 + 1 行 javadoc）⇒ 立即收口；**(B)** 拆后续小卡 ⇒ 中间态不可编译（不建议）。
**注**：t37/t38 均已完成（B⑧-a/B⑧-b 见 §18）；本卡完成后 **`roleComponent` 内渲染覆写 = 0** 且 t40（活文档同步，eng-文档）依赖本卡。

---

## 20. **t42 归属登记：`MeiqiheziBloodySlashSkill.java` 的 B39 改名 2 处**（2026-09-17 19:50，eng-验证｜t42 attempt 1）

> **为什么有此节**：t39（`CastResult.CAST` → `CastResult.SUCCEED`）卡面 inScope 列了 **11 个** src 文件，**漏列** `roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java`（其 `return CastResult.CAST;` 在枚举改名后不改则**编译必然失败**）。校验器强制 `changedPaths ⊆ inScope` 且**任务范围不可事后编辑** ⇒ 队长裁定 **(A) 追认**：t39 按已声明的 **11 文件**结案，**第 12 个文件**的归属登记与独立复核由 **t42** 完成，避免它成为"无人认领的 diff"。
> **本节数字全部为 t42 现算**（复算命令见 §20.7），**不引用实施者结论**。

### 20.1 归属登记与差额说明

**第 12 个文件（t42 归属）**：`src/main/java/com/shadowHunterRolesPlugin/roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java` —— 改动由 **t39 实施**（2 处标识符改名），**归属由 t42 登记**。

**t39 结案按已声明的 11 个 src 文件** = 下表除第 12 行外的 11 行（按改名提交 `32d1de1` 的 `--numstat`，逐条现算）：

| # | 文件 | +/- |
|---:|---|---:|
| 1 | `core/MainWeapon.java` | +2 / −2 |
| 2 | `core/RoleInstance.java` | +2 / −2 |
| 3 | `core/dispatch/CastResult.java` | +1 / −1 |
| 4 | `core/ports/CooldownPort.java` | +1 / −1 |
| 5 | `listener/MainWeaponListener.java` | +1 / −1 |
| 6 | `roleComponent/meiqiHezi/mainWeapon/MeiqiheziJuejueMainWeapon.java` | +5 / −5 |
| 7 | `roleComponent/meiqiHezi/skill/MeiqiheziCircleSlashSkill.java` | +2 / −2 |
| 8 | `roleComponent/meiqiHezi/skill/MeiqiheziUnconcernSkill.java` | +2 / −2 |
| 9 | `roleComponent/red/RedEvilShockSkill.java` | +2 / −2 |
| 10 | `roleComponent/red/RedSanctifiedBladeMainWeapon.java` | +1 / −1 |
| 11 | `roleComponent/red/RedSolitaryArroganceSkill.java` | +2 / −2 |
| **12** | `roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java` ← **t42 归属（t39 卡面漏列）** | **+2 / −2** |

合计 **+23 / −23**，与改名**前**全仓 `\bCAST\b` 的 23 处一致 ✓。该提交另含 **2 个非代码路径**：`docs/阶段4-起服日志-B39.log`、本清单。**差额说明**：卡面 11 文件 = 上表前 11 行；第 12 行属"编译必需但未申报"，故单列归属到 t42。

### 20.2 独立复核（三条判据，我亲自复跑）

| 判据 | 原样命令 | 实测 |
|---|---|---|
| 旧标识符清零 | `git grep -nE "\bCAST\b" -- <该文件>` | **0 行** ✓（大小写敏感） |
| 新标识符 2 处 | `git grep -nE "\bSUCCEED\b" -- <该文件>` | **2 行** = `:39` javadoc「冷却改为 \`SUCCEED\`…」+ `:84 return CastResult.SUCCEED;` ✓ |
| 卡面原文「`CastResult.SUCCEED` = 2 行（1 代码 + 1 javadoc）」 | `git grep -n "CastResult.SUCCEED" -- <该文件>` | **1 行**（只有 `:84`）—— **卡面措辞需修正**：javadoc 那处是反引号提及 `SUCCEED`、**不含 `CastResult.` 前缀** ⇒ "1 代码 + 1 javadoc" 只能以 `\bSUCCEED\b` 计。本卡按**意图**判通过，并按实测口径登记 |

### 20.3 差异限定（**按批次 commit 锚定**，避免与 t41 混淆）

| 段 | 比较（blob） | 结果 |
|---|---|---|
| **段 1：t39 改名批次的贡献 —— 本卡题眼** | `bda103f…`（t38 交付版，即 `4F24BB31…` 的库内 LF 形态）→ `a621ca8…`（`32d1de1`） | `--numstat = **2 / 2**`，逐行**仅**：javadoc `` `CAST` `` → `` `SUCCEED` ``、`return CastResult.CAST;` → `return CastResult.SUCCEED;` ⇒ **无逻辑、无字面量变化** ✓ |
| **段 2：其后 t41（B⑨）对该文件的改动（单独记账）** | `a621ca8…`（`32d1de1`）→ `63ac851…`（`HEAD`） | `--numstat = 2 / 7`：删 `import …LifecycleAware`、`implements LifecycleAware`、空 `start(Player, RoleInstance)`、`stop(Player, RoleInstance)`，增**无参 `stop()`** ⇒ 属 **t41/B⑨ 已申报范围**，**不由 t39 承担** |

> **口径说明**：卡面写"与 t38 交付版 `4F24BB31…` 的差异仅为该 2 处" —— 这在**段 1（批次）口径**下成立 ✓；**整文件到当前 HEAD** 的差异还包含段 2（t41 的 2/7，另一批次、已申报）。故本卡**分两段登记**，不把 t41 的改动算到 t39 头上。

### 20.4 该文件的最终身份（t42 现算）

```
工作树字节（本仓库 core.autocrlf=true ⇒ 口径 = CRLF + 含尾换行）：
  roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java
  = 3486 B / 95 行 / mtime 2026-09-17 19:48:07.259
  = SHA256 A6913D94EB25138E8701FF23788D7F953FF7ECAA1FF2513411A21C5173A32907
库内身份（跨版本比对一律用此）：blob 63ac851bc6d6dc89670dbab40db58c028b49f8f9
  （t38 交付版 blob = bda103f…；改名后 blob = a621ca8…）
行数链：181 行（改名前后）→ 100 行（t38 交付，3699 B）→ 95 行（t41 后，当前）
```

### 20.5 可编译性（复用 t41 批次证据；我复算"新鲜度三件套"）

```
jar = build/libs/ShadowHunterRolesPlugin-1.0.0.jar
    = 137430 B / mtime 2026-09-17 19:48:54.922
    = SHA256 F057EB3D7013788FDDEB0480178EEA089A608E93941BC6B7469CAB9B77C008E5
最新 .class = 19:48:54.129（ShadowHunterRolesPluginLoader.class）
src 新于 jar 的文件数 = 0 ✓
本文件产物：MeiqiheziBloodySlashSkill.class（3293 B @19:48:53.924）
          + MeiqiheziBloodySlashSkill$1.class（4089 B @19:48:53.922）
          ⇒ 两者 mtime 均晚于源文件（19:48:07.259）⇒ 该文件在本批次**编译成功** ✓
t41 批次构建声明（源材料，与本行实测同值）：`--rerun-tasks` → `BUILD SUCCESSFUL in 15s`，jar 137430 B
```

### 20.6 顺带更正两处（**仅在此登记，不越界改 §19 正文**）

1. **§19 的逐文件拆分有 2 处错值**：`Unconcern` 实为 **1**（§19 写 2）、`RedEvilShock` 实为 **1**（写 2）⇒ §19 拆分合计 14 与其自身总数 12 矛盾（**总数 12 正确**）。改名后（`32d1de1`，commit 锚定）实测：`MainWeapon`1 · `RoleInstance`2 · `Juejue`3 · `BloodySlash`1 · `CircleSlash`1 · `Unconcern`**1** · `RedEvilShock`**1** · `RedSanctifiedBlade`1 · `RedSolitary`1 = **12 行 / 9 文件** ✓；改名**前**（`aa16eaa`）`\bCAST\b` = **23 处 / 12 文件**（`MainWeapon`2 · `RoleInstance`2 · `CastResult`1 · `CooldownPort`1 · `MainWeaponListener`1 · `Juejue`**5** · `BloodySlash`2 · `CircleSlash`2 · `Unconcern`2 · `RedEvilShock`2 · `RedSanctifiedBlade`1 · `RedSolitary`2）✓。
2. **`\bCAST\b` 判据必须大小写敏感**：`core/dispatch/ComponentRegistry.java:59` 的 `type.cast(component)`（小写 `Class.cast`）在不敏感搜索下是**常驻假阳性**；`docs/回滚快照/**` 内 17 个文件含旧快照的 `CAST` 字样（属预期，不影响 `src` 判据）。⇒ 一律用 `git grep -nE`（默认敏感）或 `-CaseSensitive`；**凡计数必须连作用域一起写**（否则会连带快照目录得到虚高值：本轮 `-- '*.java'` 得 137 行 vs 限定 `src` 得 25 行）。

### 20.7 复算命令（一键核对本节）

```powershell
cd <repo>/ShadowHunterRoles
$f = 'src/main/java/com/shadowHunterRolesPlugin/roleComponent/meiqiHezi/skill/MeiqiheziBloodySlashSkill.java'
git grep -nE '\bCAST\b' -- $f                    # 0 行
git grep -nE '\bSUCCEED\b' -- $f                 # 2 行（:39 javadoc + :84 代码）
git rev-parse aa16eaa:$f 32d1de1:$f HEAD:$f      # bda103f… / a621ca8… / 63ac851…
git diff --numstat aa16eaa 32d1de1 -- $f         # 2	2   （段 1 = t39 改名）
git diff --numstat 32d1de1 HEAD   -- $f          # 2	7   （段 2 = t41/B⑨）
(Get-FileHash $f -Algorithm SHA256).Hash         # A6913D94…（工作树字节口径 CRLF）
(Get-Item build/libs/ShadowHunterRolesPlugin-1.0.0.jar).Length   # 137430
```

**t42 结论**：第 12 个文件的归属**登记完成**；改名 2 处**就位**（`\bCAST\b` = 0、`\bSUCCEED\b` = 2）；其在 t39 批次的贡献**恰为 2 处标识符改名**（段 1 = `2/2`，零逻辑零字面量）；该文件在 t41 批次**编译成功**且产物新于源；最终身份 = **3486 B / 95 行 / `A6913D94…`（工作树 CRLF 口径）**、库内 blob `63ac851…`。
> **§15.2 的 `runRepeating` 取值更正（captain 2026-09-17 追认）**：原 §15.2 写的 `1L, 1L` **作废** ✗（属早期转写误差）——**权威 = 源码 `runRepeating(…, 0L, 40L)`**：周期 **40 tick（2 秒）**、`count` 上限 **3** ⇒ 惩罚总时长 **6 秒**，**逐字不变** ✓。该处曾被 t41 在开工前拦下（**第 12 次"拦下"**，且属最险一类：`1L,1L` 会让惩罚**快 40 倍**，而它**编译通过、Mode B 三行齐、集合差 `C=0`、双口径也不一定报** ✗）。**立规**：卡面/清单的"映射建议"不是权威，**源码才是**；凡"旧写法 → 新写法"的逐行映射，映射行的**旧侧必须逐字取自源码**，开工前逐条比对，不符以源码为准并申报 ✓。