# 阶段 4 · T 块（⑧ ④ ① ③ ⑤）独立复核报告

- **复核对象**：提交 **`04c0fd1`**（`T block item 5 complete - bind replaced by ComponentFactory construction-time injection …`；27 文件 = 25 src + 2 日志），父提交 `04c0fd1^`
- **复核者**：`eng-会话`（**未参与本批实现**；本批实现者 = `eng-收尾`，队长结卡）
- **复核方式**：**只读** —— 全部判据用 `git grep` / `git show` / `git diff --numstat` / 内联计算（PowerShell）现算；**未构建、未起服、未改 `src`、未做 git 写**；本报告是唯一写入文件
- **复核时刻**：2026-09-17 20:4x；工作树 `git status --porcelain` = **clean**（HEAD = `04c0fd1`）

---

## 1. 判据逐条（命令 + 输出 + 结论）

### 1.1 `bind(` 两口径同时 0 ✅
```
$ git grep -n "bind(" -- src/main/java            → 无输出（命中 raw = 0）
$ （内联：剥注释后全树计数）                        → 仅代码口径 = 0
```
> 复核者注：改造前为 **raw 7 / 代码 3**；其中 raw 多出的 4 处为 javadoc 提及（`Role.java:88`、`ComponentServices:8`、`RoleComponent:10` 与新增的 `ComponentFactory` javadoc），实现者已一并处理后归零 ✓。

### 1.2 id 值多重集净 0 ✅（14 组件 / 14 常量 / 16 条装配）
```
$ git grep -n "private static final String ID_" -- …/registry/RoleLoader.java
  → 恰 14 条常量（RoleLoader:50-63）
$ （父 ↔ 本提交 RoleLoader 字面量多重集）→ 父 19 处 / 本提交 33 处，差异 **+14**，
   新增明细 = 14 个 id，且值**逐字**与旧 ctor 内 `super("…")` 的 id 相同：
   autoRecoverEnergy_passive · autoRecoverSanTEPassive · default_san_te_zero_punishment ·
   meiqihezi_equippments_passive · meiqihezi_mainWeapon_juejue · meiqihezi_skill_bloody_slash ·
   meiqihezi_skill_circle_slash · meiqihezi_skill_unconcern · red_bleed_passive ·
   red_deeplySorrow_skill · red_equippments_passive · red_evilShock_skill ·
   red_mainWeapon_sanctifiedBlade · red_solitaryArrogance_skill
$ git grep -nE 'super\(\s*"' -- …/roleComponent   → 无输出（组件 ctor 内联 id = 0）
$ git grep -c "::new" -- …/registry                → 16 条**装配行**（17 处命中 = 16 装配 + 1 处 javadoc `组件::new`）
```
⇒ **id 从 14 个组件 ctor 移出（14 处移除）↔ 注册处 14 条常量声明（14 处新增）**，值逐字不变 ⇒ **净变化 = 0** ✓

### 1.3 构造期注入语义 ✅
```
$ git grep -n "private final|protected RoleComponent\(" -- …/roleComponent/RoleComponent.java
  RoleComponent:19  private final String id;
  RoleComponent:20  private final ComponentServices svc;          ← final，唯一赋值点在 ctor
  RoleComponent:22  protected RoleComponent(String id, ComponentServices services)
$ git grep -n "createServices|factory.create|register|awake" -- …/core/RoleInstance.java
  RoleInstance:140  private ComponentServices createServices(String componentId, ItemKind kind)
  RoleInstance:157  …服务集是在**构造期**交给组件的（{@code factory.create(id, services)}）⇒ 不存在"创建后尚未注入"的窗口
  RoleInstance:161  componentRegistry.register(component);
  RoleInstance:211/220/229   ComponentServices services = createServices(<id>, <kind>);
  RoleInstance:589  component.awake();
```
⇒ **组件在 `awake()` 之前即持有 `ComponentServices`**（构造器注入、字段 `final`、注册与 awake 均在其后）✓；
`RoleComponent` 已**不含** `bind(...)` 与 `markLifecycleStarted(...)` ✓

### 1.4 `TimerPortImpl` null 分支 + 同一实例论证 ✅
```
$ git grep -n "getById|null|等价性" -- …/core/TimerPortImpl.java
  :13  …改为捕获组件 id、登记时按 id 解析（{@code ComponentRegistry.getById}）。等价性：id 在角色实例内唯一…
  :17  {@code null} 分支：{@code getById} 返回 {@code null}（理论上只可能发生在"组件尚未登记/已被注销"的装配外状态）
  :18  时**安全跳过**登记，不做任何 substitute —— 与旧实现"不可能为 null"的差异只在该不可能态…
  :59/:60  RoleComponent component = registry.getById(componentId); if (component != null) { … }
```
⇒ null 分支**有 javadoc 明文 + 代码守卫**，且**不做替代** ✓；同一实例论证（装配 `(id, ::new, slot)` → 同一 `componentRegistry` 注册/解析、解析时点在 `freeze()` 之后）与 `Role.Builder` 的共用命名空间校验一致 ✓

### 1.5 红线 ✅
```
$ git diff --numstat 04c0fd1^ 04c0fd1 -- core/ports/ComponentServices.java
  2  2  …/ComponentServices.java                        ← 仅 javadoc（逐行核对：-2/+2 均 `*` 注释行）
$ git grep -n "private static final String ID_" （或直接读文件）→ 白名单成员 = **10**：
  Self self · EnergyPort energy · SanTEPort sante · VitalsPort vitals · CooldownPort cooldowns ·
  BuffPort buffs · FactionPort factions · DamagePort damage · TimerPort timers · ComponentLookup components
$ git diff --name-only 04c0fd1^ 04c0fd1 -- api/ event/  → **0 文件**
```
⇒ 白名单 **10 成员不变** ✓；`api/**` 与 `event/**` **零改动** ⇒ 两事件（`SanTEChangeEvent`/`EnergyChangeEvent`）对外发布不变 ✓

### 1.6 构建与起服取证（只读复核，未重跑）✅（1 项物证缺口见 2.1）
```
$ （读 build 产物）
  jar = 135 308 B / 20:40:56 / 139BC211719DE7BAA78B9C176314045156DFFD6131D5BB0B22D34C31DF7204CA
  最新 .class = ShadowHunterRolesPlugin.class @ 20:40:56
  src 新于 jar = 0
$ （读 docs/测试记录/阶段4-起服日志-T2b.log）
  6 078 B / 2335DFBC9ED500284F555FF638DBFE685B77A866EEDDD8FE2CAC257B9B701F0E
  :45 Enabling ShadowHunterRolesPlugin v1.0.0   :46 ShadowHunter Character System enabled.   :50 Done (18.395s)!
$ （内联归一化 delta：去 [时间戳][线程] 前缀后比对 T2.log 锚）
  anchor 13 行 / current 13 行 / 差异 = **0**；C) OTHER = **0** ✓
```
⇒ 构建新鲜度 ✓、Mode B 三行 ✓、正序集合差（归一化口径）**全 0** ✓

---

## 2. Findings（分级）

### 2.1 **F5-1（low，流程）**：本批**未留独立"停服物证"文件** ⇒ **证据缺口（不得推定）**
```
$ Get-NetTCPConnection -LocalPort 25566 -State Listen  → 命中 0
$ Get-Process java                                      → 6（均为常驻 daemon，非 runServer）
$ Get-ChildItem docs -Filter "*停服证据*"                → 0 个
```
- 现有**旁证**：日志可独占读（无并发写者）、`25566` 无监听、无 server java 进程。
- 按 **F5 新纪律**（清单 §21.4）：**只记为"证据缺口"** —— **不得**判为"已停服已证实" ✗；
- **建议**：本批按"停服物证缺失（low）"入账；后续批次按 §21.4 写 `…-停服证据.txt`（停服时刻 + `25566` 监听 = 0 的现算命令与输出）。

### 2.2 **F5-2（low，台账）**：同批存在**两份日志** ⇒ 需声明其中一份的状态
```
docs/测试记录/阶段4-起服日志-T2b.log   6 078 B / 2335DFBC…701F0E   ← 本批正式日志（三行齐、delta 全 0）
docs/测试记录/阶段4-起服日志-T2b1.log  （t48 卡面交付名副本：与 T2b.log 逐字节相同、非第二份证据）
```
- **建议**：队长在结卡说明或清单里**注明 `T2b1.log` 的角色**（作废/仅存档），避免后续核验把两份混用 ✗。

### 2.3 **INFO（方法学）**：复核者自查出的**第 11 例计数陷阱**
- 我第一版 id 比对用 `"([a-z_]+)"` 提取 → 因 id 含大写（如 `red_deeplySorrow_skill`）而**漏 9 条**，一度得出"新 RoleLoader 只有 9 个 id"的**假结论**；改为"取全部字符串字面量 + 多重集比对"后得到 **+14 = 14 个 id 逐字一致**的正确结论 ✓。
- 与清单已记的陷阱同类（`Select-String` 大小写、`Measure-Object` 对象计数）：**判据的正则必须覆盖目标字符集**；复核结论应在**换一种量法复现后**才落笔。

### 2.4 **INFO（口径）**：`::new` 的计数粒度
- `git grep -c "::new" -- registry` = **17 处**，其中 **16 处 = 装配行**、1 处 = `RoleLoader:47` javadoc 的 `组件::new` 示例 ⇒ 卡面/队长信的"**16 条装配**"在**装配行粒度**成立 ✓（引用时写明口径）。

---

## 3. 结论

| 判据 | 结论 |
|---|---|
| `bind(` 两口径同时 = 0 | ✅ 通过（raw 7 → 0 / 代码 3 → 0） |
| id 值多重集净 0（14 / 14 / 16） | ✅ 通过（逐字一致，`super("…")` 内联 id = 0） |
| 构造期注入语义（awake 前持有、`svc` final） | ✅ 通过 |
| `TimerPortImpl` null 分支 + 同一实例论证 | ✅ 通过（javadoc 明文 + 代码守卫、不替代） |
| 白名单 10 成员 / `api/` / 事件发布 | ✅ 通过（`ComponentServices` 仅 javadoc 2/2；`api/`+`event/` 0 文件） |
| 构建新鲜度 + Mode B 三行 + 集合差（归一化）| ✅ 通过（jar 135 308 B @20:40:56、`src 新于 jar = 0`；delta 0/0，`C = 0`） |
| **停服物证** | ⚠️ **证据缺口（F5-1，low）** —— 只记缺口，**不推定** |

**总评**：**判据 6/6 通过 + 1 项流程物证缺口（low）**，另有 1 项台账建议（F5-2，low）与 2 条口径/方法学 INFO。**本报告可作为 t15（阶段 3–4 独立验证）与 t16（评审）的输入**；F5-1 的处置（补写物证文件 or 入账为缺口）请队长裁定。

---

## 4. 复核者声明
- 本报告由**未参与本批实现**的成员出具；全部结论基于**只读现算**，命令与输出均已逐条列出，可复现。
- 复核**未重跑**构建/起服（避免污染取证窗口），只读核对了实现者产出的 jar/日志/哈希与上述结构判据；**停服一项按纪律判为缺口而非推定通过** ✓。
- 本批实现者与结卡者**均未参与**本报告的编写。
---

## 5. 附录（复核者追加，20:5x；**仅本文件**）

**INFO-3（口径/影响面）**：卡面 deliverables 列了 `registry/RoleRegistry.java`，而实现**未改该文件** —— 装配形态 `(id, ComponentFactory)` 完全落在 `RoleLoader`，纯容器（`RoleRegistry`）签名无需变动 ⇒ **影响面比卡面预期更小，属合理简化**；实现者已在交班中申报 ✓。**建议**：在结卡说明里登记该差异（"卡面列 `RoleRegistry`，实际未改，理由 = 装配形态在 `RoleLoader` 完成"）。

**INFO-4（环境观察，供后续取证复用）**：复核期间对 `run\logs\latest.log` 的 `FileShare.None` 独占打开曾被拒，同时该文件 **内容与 mtime 未变**（6 077 B / 20:42:05）、`25566` 无监听、无 20:40 之后启动的 java 进程 ⇒ **属 Gradle daemon / 工具短暂持句柄，不是新起服**。**不要据此判定"未停服"**；判停服仍按 §21.4 的三项旁证（日志可独占读 + 端口无监听 + 无 server 进程），或在后续批次改用 `…-停服证据.txt` 物证。

**INFO-5（与交班稿的差异，均已核实）**：① **id = 14**（交班稿曾写 10）—— 复核以现算 **14 组件 / 14 id 常量 / 16 条装配** 为准 ✓（清单 §21.1 已更正）；② **`core/TimerPortImpl.java` 属本批必需面**（旧构造捕获组件实例 ⇒ 改按 `componentId` 解析；队长已书面追认）—— 本报告 §1.4 已按"按 id 解析 = 同一实例"独立复核通过 ✓；③ `RoleRegistry` 未改见 INFO-3 ✓。
---

## 6. 附录二（20:5x；F5-1 状态更新，**仅本文件**）

实现者事后产出 **`docs/阶段4-起服日志-T2b-停服证据.txt`**（**6 678 B / 84 行 / `E762BD52754076DB2D7C5CE412C0BB3F4793146EBCA0BE30043B96E2A98763B7`**，复核时**未跟踪**）。只读复核结论：

- **结构合格、出处标注诚实** ✓：标题即写「**事后补记（post-hoc）**」；§1 = **当时观察**（起服 20:41:45 → 停服判定 20:45:37，含 20:45:37 以 `FileShare.None` **独占打开 `latest.log` 成功**、`25566 = 0`、无本批 Paper 进程、只用 `job_kill` 未强杀）；§2 = **事后现算**（20:52:40，并写明「**事后独占打开被拒 ≠ 未停服**」，与队长 `9a6d1a4` 的 gradle-daemon log-handle 澄清一致）；§4 = **边界与免责**，明说 §1/§2 **不得混用**为当时物证。
- **F5-1 状态更新**：由"**本批无物证文件**"降级为"**物证为事后补记、不能充当当时物证**"（**仍记 low**）；"当时未按 §21.4 落盘"这一原判**不变**，§附录一 INFO-4 的"不得据独占失败判定未停服"结论**不变** ✓。
- **未发现伪造或过度声明** ✓（文件自我限定为"可复核物证；不等价于当时的即时物证"）。
- **旁证更新**：`HEAD = 9a6d1a4`（队长追加小结 §5.28）；**自 `04c0fd1` 起 `src` 变更文件数 = 0** ✓（本批源码面冻结）。
---

## 7. 附录三（现算复核，20:5x；**仅本文件**）

1. **F5-1 状态：已补齐（事后补记版，provenance 已显式标注）** —— 队长已把实现者产出的物证文件**重命名并入库**：`docs/测试记录/阶段4-起服日志-T2b-停服证据（事后补记，非当时物证）.txt`（提交 **`375ad9c`**，内容与复核时的 `E762BD52…763B7` **逐字节一致**、**provenance 写进文件名**、现为 tracked）。⇒ 本报告 §2.1 的 F5-1 **由"证据缺口"改记为"事后补记版已补齐"**（**low 仍保留**：原始纪律问题 = 当时未按 §21.4 落盘，事实不变；不可追溯改写为"当时物证" ✓）。
2. **规则已制度化**：队长提交 **`6631c83`** ——「**post-hoc records are allowed when provenance is explicit in both filename and header, kept in separate sections, and never presented as contemporaneous evidence**」⇒ 与本报告 INFO-4 及该物证文件 §1/§2 分节 + 头尾免责的做法一致 ✓。**现值 `HEAD = 6631c83`**。
3. **INFO-3 已登记** ✓：队长在交付小结 **§5.28**（`9a6d1a4`）把四项后修正列全（**14 ids / `RoleRegistry` untouched / `TimerPortImpl` in scope / path-independent value-multiset caliber**），结卡记录亦逐条写了声明外路径 ⇒ **无需再提** ✓。
4. **本报告身份（现算，引用请以此为准）**：`docs/测试记录/阶段4-T块⑧④①③⑤独立复核.md` = **12711 B / 2026-09-17 20:53:31 / 74D3E33A65FD799BC0D60C6849A7DFB20FCB95B040D718ECC801B66EEFC43869**（附录二后为 12 711 B / `74D3E33A…3869`；本节追加后身份见交付回报）。**"引用请重算"** 纪律已按你方要求执行 ✓。
5. **源码面冻结确认（现算）**：**自 `04c0fd1` 起 `src` 变更 = 0 文件** ✓ ⇒ 本报告的 6/6 结论**仍然成立、未漂移** ✓。
---

## 8. 附录四（台账对齐 + 一处事实更正，20:5x；**仅本文件**）

1. **§2.1（F5-1）处置对齐权威台账** `docs/测试记录/重构进度与交接.md:118-121`：队长先判"入账为证据缺口、不补写"，**同日细化为例外规则** —— 允许事后补记，但必须 ① 文件名显式标注来源 ② 文首声明 post-hoc 并**分节**（§1 当时观察 vs §2 事后现算、写明"不等价"）③ 不得与当时物证混列、不得伪造控制台停服行；并明确 **F5 物证纪律自下一批起强制执行**，**本报告列出的旁证只作辅助** ✓。落地物证 = `375ad9c`（`…-停服证据（事后补记，非当时物证）.txt`，逐字节 `E762BD52…763B7`）与规则提交 `6631c83` ✓。⇒ **本报告 §2.1 的"只记缺口、不得判为已证实"大方向不变，处置已按上列例外更新**。
2. **§2.2（F5-2）**：队长已裁定 `docs/测试记录/阶段4-起服日志-T2b1.log` = **卡面交付名副本**，与 `T2b.log` **逐字节相同**（我现算：两份均 **6 078 B / mtime 20:45:22 / `2335DFBC9ED500284F555FF638DBFE685B77A866EEDDD8FE2CAC257B9B701F0E`**），**非第二份证据**；后续引用一律以 `T2b.log` 为权威、**冻结日志不追加注释** ✓ ⇒ §2.2 的建议**已落实**。
3. **事实更正（本报告 §2.2 第 103 行）**：`T2b1.log` **不是**"早期尝试留下的旧日志" ✗ —— 它是实现者在本批 **20:45:37** 为满足 t48 卡面 deliverables 的文件名而创建的 **`T2b.log` 字节相同副本**（同 SHA256 / 同 mtime / 同 6 078 B，**唯一差别是文件名**）；**正文该处措辞已就地更正** ✓，本附录作为更正留痕 ✓。
4. **身份（冻结前现算）**：本报告 = **14542 B / 2026-09-17 20:54:12 / B38E06F7E4CE2BECFA7E9F86BE1715A8307AC8519D4A29AD94C807109B8E0642**（本节追加前）；追加后身份以下一次交付回报为准（"引用请重算"纪律 ✓）。